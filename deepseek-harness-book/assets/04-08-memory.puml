@startuml
!theme plain
skinparam defaultFontName PingFang SC
skinparam defaultFontSize 16
skinparam shadowing false
skinparam wrapWidth 230
skinparam maxMessageSize 180
title 2.4 会话与上下文：项目约定怎样进入下一次对话
actor 读者
participant "DSH 工具调用\nproject_memory_set" as T
database "Storage\n当前项目的约定" as S
participant "System Prompt\n动态上下文提供方" as C
participant "DSH 新会话\n请求组装与模型调用" as M
读者 -> T : 要求记住测试命令
T -> S : 读取当前版本
S --> T : 现行约定、revision
T -> 读者 : 显示原值和新值，请求审批
读者 --> T : 允许一次
T -> S : 再次核对版本，保存新值
S --> T : 写入完成
T --> 读者 : saved: true
== 同一项目中新建会话 ==
读者 -> M : 询问已保存的测试命令
M -> C : DSH 组装请求上下文
C -> S : 按当前工作区 ID 读取现行约定
S --> C : test_command = npm test
C --> M : 带入本次请求
M --> 读者 : 回答 npm test
note over C,M
关闭 injectMemory 后跳过自动带入；
Storage 中的数据不因此删除。
end note
@enduml
