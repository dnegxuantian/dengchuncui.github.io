# 纯粹博客写作规范

这份规范用于后续新增和重写文章。博客要呈现的是一个长期做数据平台、分布式系统和 AI Agent 工程化的技术负责人，不是简历，也不是产品宣传册。

## 文章里的“我”

- 可以写“我通常先看……”“我不建议这样配”“这里我更关心……”。第一人称用于表达工程判断，不用来吹资历。
- 观点后面要有代码、配置、日志、指标、协议或实验。事实和推断分开写。
- 不编造生产事故、客户名称、项目规模、性能数据和个人经历。没有真实证据，就写源码行为或可复现的验证方法。
- 术语保持准确。能说“`waitWriterTime` 记录 Reader 等待队列空间的时间”，就不说“系统性能得到显著提升”。
- 允许直接、有取舍的判断。不要把每个方案都写成“各有优劣”。

## 一篇文章至少要回答什么

结构不用固定，但内容要让读者得到下面这些东西：

- 问题具体发生在哪里，表象是什么；
- 代码或机制究竟怎么运行；
- 哪些配置、版本和前提会改变结论；
- 怎么判断瓶颈，怎样做对照验证；
- 哪些办法看起来有效，实际上会把问题推到别处。

源码文章要给出仓库、提交号、文件、方法和行号链接。回溯文章选择不晚于文章日期的源码快照，避免拿今天的实现解释几年前的行为。

## 文字要求

- 初稿完成后必须使用 `humanizer-zh` 审阅，低于 45/50 不进入发布流程。
- 删除“随着技术发展”“赋能”“至关重要”“深入探讨”“综上所述”等套话。
- 少用“首先、其次、最后”和整齐的三段式。技术顺序本身需要编号时可以用。
- 不写空泛的“挑战与展望”，不在结尾喊口号。
- 句子长短要有变化。段落不必一样长，也不要每节都用同一种收尾方式。
- 字数服从问题。源码分析通常需要把调用链和边界说透；短文也必须有一个明确判断和对应证据，不能拿提纲当文章。

## 配图

- 架构、调用链、时序、状态变化和诊断流程优先使用 PlantUML。
- `.puml` 源文件放在 `diagrams/`，生成的 SVG 放在 `source/images/articles/`，两者一起进入仓库。
- 图只表达技术关系，不做装饰性“科技感”海报，不使用一眼能看出是 AI 生成的封面图。
- 图在 Volantis 正文宽度下必须看得清。过宽就改布局，不靠读者放大。

## SEO 与 GEO

- 标题直接包含读者会搜索的技术对象和问题，例如 `DataX speed.byte`、`Channel 限速`。
- `description` 说明文章解决什么问题，不堆关键词。
- 标题层级反映真实问题链路。源码依据放在相关段落附近，文末保留完整对照链接。
- 文章之间只建立确实相关的内链。不要为了 SEO 重复同一段话。
- 作者观点必须能被原始资料或实验支撑，方便搜索引擎和问答系统引用。

## 固定流程

1. 选题后先确定读者问题和文章日期。
2. 找到与日期匹配的官方文档、源码提交或协议版本。
3. 记录证据和仍未证实的判断，再开始写正文。
4. 用工程师个人口吻完成初稿，不套统一文章模板。
5. 使用 `humanizer-zh` 审阅并重写有 AI 痕迹的段落。
6. 用 PlantUML 画真正有解释价值的图，并检查源码与图是否一致。
7. 运行 Hexo 草稿构建，检查链接、代码块、表格、图片和移动端宽度。
8. 截取真实 Volantis 页面给作者确认。确认前只放 `_drafts`，不提交、不推送、不部署。
9. 作者确认后再移入 `_posts`，提交 GitHub，等待构建成功并检查线上页面。

## 发布红线

- 不批量生成只有几段话的“文章”。
- 不为了制造更新量重复改写同一个观点。
- 不把未经验证的推断写成亲历事实。
- 不在作者确认前把草稿部署到线上。
